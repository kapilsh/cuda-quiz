import { defineQuestions } from './_helpers.js'

// GPU Architecture — SM internals, tensor cores, async copy, TMA, generations
// (Volta/Turing/Ampere/Hopper/Blackwell), HBM, NVLink basics.
export default defineQuestions('architecture', [
  {
    d: 2,
    q: 'What are Tensor Cores designed to accelerate?',
    o: [
      'Atomic operations',
      'Small matrix multiply-accumulate operations (D = A·B + C) at high throughput',
      'Memory copies',
      'Branch prediction',
    ],
    a: 1,
    e: 'Tensor Cores are specialized units that perform fused matrix-multiply-accumulate on small tiles (e.g. 16×16) per instruction, delivering far higher throughput than CUDA cores for GEMM/convolution — the workhorse of deep learning.',
    ref: 'NVIDIA Volta+ whitepapers; CUDA C++ Programming Guide (WMMA)',
    tags: ['tensor-cores', 'gemm', 'architecture'],
  },
  {
    d: 3,
    q: 'Which compute capability / architecture FIRST introduced Tensor Cores?',
    o: ['Pascal (sm_60)', 'Volta (sm_70)', 'Turing (sm_75)', 'Ampere (sm_80)'],
    a: 1,
    e: 'Volta (V100, sm_70) introduced the first-generation Tensor Cores (FP16 input, FP32 accumulate). Later generations added more types (Turing INT8/INT4, Ampere TF32/BF16, Hopper FP8, Blackwell FP4) and the wgmma/async path.',
    ref: 'NVIDIA Volta Whitepaper',
    tags: ['tensor-cores', 'volta', 'generations'],
  },
  {
    d: 2,
    q: 'What kind of memory is HBM (used on data-center GPUs like A100/H100)?',
    o: [
      'On-chip SRAM',
      'High-Bandwidth Memory — stacked DRAM offering very high bandwidth via a wide interface',
      'Host system memory',
      'A type of cache',
    ],
    a: 1,
    e: 'HBM is stacked DRAM placed near the GPU die with a very wide bus, giving terabytes/sec of bandwidth (e.g. ~2 TB/s on H100 HBM3). It is the "global memory" device DRAM, distinct from the on-chip SRAM (L1/L2/shared).',
    ref: 'NVIDIA datacenter GPU whitepapers',
    tags: ['hbm', 'bandwidth', 'memory'],
  },
  {
    d: 3,
    q: 'What is the role of the L2 cache on an NVIDIA GPU?',
    o: [
      'Per-thread private storage',
      'A device-wide cache shared by all SMs that backs global-memory accesses and L1 misses',
      'Part of the register file',
      'Only used for constant memory',
    ],
    a: 1,
    e: 'L2 is a large unified cache shared across all SMs, sitting between the per-SM L1 caches and HBM. It captures reuse across blocks and reduces DRAM traffic. Hopper/Ampere also allow L2 residency control (persisting access window).',
    ref: 'CUDA C++ Programming Guide §5.3; architecture whitepapers',
    tags: ['l2-cache', 'memory-hierarchy'],
  },
  {
    d: 4,
    q: 'TF32 (TensorFloat-32), introduced on Ampere, has which format characteristic?',
    o: [
      'Same as FP32 (8-bit exponent, 23-bit mantissa)',
      '8-bit exponent (like FP32 range) but ~10-bit mantissa (like FP16 precision), used by tensor cores for FP32 math',
      '5-bit exponent, 10-bit mantissa',
      'It is an 8-bit integer format',
    ],
    a: 1,
    e: 'TF32 keeps FP32’s 8-bit exponent (full range) but truncates the mantissa to ~10 bits, letting tensor cores accelerate "FP32" matmuls with reduced precision and minimal accuracy impact for training, accumulating in FP32.',
    ref: 'NVIDIA Ampere Whitepaper; CUDA docs',
    tags: ['tf32', 'ampere', 'precision', 'tensor-cores'],
  },
  {
    d: 5,
    q: 'A "warpgroup" on Hopper refers to…',
    o: [
      'A single warp of 32 threads',
      'Four contiguous warps (128 threads) that cooperate to issue wgmma tensor-core instructions',
      'All warps in a block',
      'A group of GPUs',
    ],
    a: 1,
    e: 'Hopper’s wgmma instructions operate at warpgroup granularity — 4 warps / 128 threads acting together to feed the tensor cores from shared memory. This larger cooperative unit improves operand sharing and throughput over per-warp wmma.',
    ref: 'Hopper PTX ISA (wgmma); CUTLASS 3.x',
    tags: ['hopper', 'warpgroup', 'wgmma', 'tensor-cores'],
  },
  {
    d: 4,
    q: 'The "async copy" path (cp.async / memcpy_async) was introduced on which architecture and why is it significant?',
    o: [
      'Pascal — to enable unified memory',
      'Ampere — it copies global→shared without occupying registers, enabling deeper compute/transfer overlap',
      'Volta — to add tensor cores',
      'Turing — for ray tracing',
    ],
    a: 1,
    e: 'Ampere (sm_80) added cp.async, letting data stream from global directly into shared memory asynchronously, bypassing the register file. This underpins multi-stage software pipelines in modern GEMM/attention kernels, hiding HBM latency.',
    ref: 'NVIDIA Ampere Whitepaper; CUDA C++ Programming Guide',
    tags: ['cp-async', 'ampere', 'pipelining'],
  },
  {
    d: 3,
    q: 'What does the SFU (Special Function Unit) in an SM handle?',
    o: [
      'Integer addition',
      'Transcendental/approximate functions like sin, cos, exp, rsqrt, reciprocal',
      'Memory loads',
      'Atomic operations',
    ],
    a: 1,
    e: 'SFUs compute fast approximations of transcendental functions (sin, cos, exp2, log2, reciprocal, rsqrt). They have lower throughput than FMA units, so transcendental-heavy kernels can become SFU-bound. Intrinsics like __sinf use them directly.',
    ref: 'CUDA C++ Programming Guide §G; architecture whitepapers',
    tags: ['sfu', 'transcendental', 'sm'],
  },
  {
    d: 5,
    q: 'Blackwell (sm_100) and the GB200 notably add support for which low-precision format for tensor cores?',
    o: ['FP64', 'FP4 (and microscaling MXFP formats)', 'INT16', 'TF32'],
    a: 1,
    e: 'Blackwell’s second-generation Transformer Engine adds FP4/FP6 and microscaling (MX) formats, roughly doubling effective throughput over Hopper’s FP8 for inference, with per-block scaling to preserve accuracy at very low bit-widths.',
    ref: 'NVIDIA Blackwell Architecture Whitepaper',
    tags: ['blackwell', 'fp4', 'tensor-cores', 'precision'],
  },
  {
    d: 4,
    q: 'The Hopper "Transformer Engine" primarily provides…',
    o: [
      'A dedicated attention ASIC',
      'Hardware + software that dynamically uses FP8 (with per-tensor scaling) for transformer layers while maintaining accuracy',
      'A new interconnect',
      'A larger register file',
    ],
    a: 1,
    e: 'The Transformer Engine combines FP8-capable tensor cores with runtime statistics/scaling that pick per-tensor scale factors and switch between FP8 and higher precision, accelerating transformer training/inference without manual precision tuning.',
    ref: 'NVIDIA Hopper Whitepaper; Transformer Engine library',
    tags: ['hopper', 'transformer-engine', 'fp8'],
  },
  {
    d: 2,
    q: 'NVLink, compared to PCIe, provides…',
    o: [
      'Lower bandwidth but lower cost',
      'Much higher GPU-to-GPU bandwidth and a coherent interconnect for fast multi-GPU communication',
      'A replacement for HBM',
      'Connection only to the CPU',
    ],
    a: 1,
    e: 'NVLink is NVIDIA’s high-bandwidth GPU interconnect (hundreds of GB/s to ~900 GB/s aggregate with NVSwitch on Hopper), far exceeding PCIe. It enables fast peer-to-peer transfers and all-reduce in multi-GPU training.',
    ref: 'NVIDIA NVLink/NVSwitch documentation',
    tags: ['nvlink', 'interconnect', 'multi-gpu'],
  },
  {
    d: 3,
    q: 'On modern GPUs, how do FP64 (double) throughput rates typically compare to FP32 on consumer vs. data-center GPUs?',
    o: [
      'Always equal',
      'Data-center GPUs (e.g. A100/H100) have a much higher FP64 ratio (e.g. 1:2) while consumer GPUs cripple FP64 (e.g. 1:32 or worse)',
      'FP64 is always faster',
      'FP64 is unsupported on all GPUs',
    ],
    a: 1,
    e: 'Data-center parts dedicate real FP64 units (e.g. 1:2 of FP32 on A100/H100, boosted further by FP64 tensor cores), whereas GeForce GPUs have a tiny FP64 ratio (~1:32–1:64). HPC codes therefore favor data-center GPUs.',
    ref: 'NVIDIA GPU datasheets/whitepapers',
    tags: ['fp64', 'throughput', 'datacenter-vs-consumer'],
  },
  {
    d: 4,
    q: 'The L2 "persisting access" / access policy window (Ampere+) lets you…',
    o: [
      'Disable the L2 cache',
      'Mark a region of global memory to preferentially persist in L2, improving hit rate for frequently reused data',
      'Increase shared memory size',
      'Pin host memory',
    ],
    a: 1,
    e: 'Ampere added control over an L2 residency window: you can set an access policy (cudaAccessPropertyPersisting) so a hot data region tends to stay resident in L2, boosting reuse for irregular/streaming kernels that revisit the same buffer.',
    ref: 'CUDA C++ Programming Guide §5.3 (L2 access management)',
    tags: ['l2-cache', 'persisting', 'ampere'],
  },
  {
    d: 5,
    q: 'Why does TMA (Tensor Memory Accelerator) reduce register pressure compared to thread-driven cp.async?',
    o: [
      'It compresses data into registers',
      'A single thread issues a descriptor-based bulk copy of a whole tile, so threads no longer compute per-element addresses or hold them in registers',
      'It uses constant memory for addresses',
      'It eliminates shared memory',
    ],
    a: 1,
    e: 'With TMA, address generation for a multi-dimensional tile is offloaded to the hardware via a tensor map descriptor; one thread launches the copy. This removes the per-thread index arithmetic and address registers that cp.async required, freeing registers for compute.',
    ref: 'NVIDIA Hopper Whitepaper; CUDA C++ Programming Guide (TMA)',
    tags: ['tma', 'hopper', 'registers', 'async-copy'],
  },
  {
    d: 3,
    q: 'What is a GPC (Graphics/GPU Processing Cluster)?',
    o: [
      'A single CUDA core',
      'A cluster grouping several SMs (and shared resources) within the GPU; relevant to Hopper thread-block clusters',
      'The global memory controller',
      'A group of GPUs in a node',
    ],
    a: 1,
    e: 'A GPC bundles multiple SMs together with some shared front-end/raster resources. On Hopper, thread block clusters are co-scheduled within a GPC, which is what makes distributed shared memory and cluster barriers physically possible.',
    ref: 'NVIDIA architecture whitepapers',
    tags: ['gpc', 'architecture', 'clusters'],
  },
  {
    d: 2,
    q: 'CUDA cores vs. Tensor cores: which statement is correct?',
    o: [
      'They are the same units',
      'CUDA cores do general scalar/vector FP and integer ops; Tensor cores do dense matrix-multiply-accumulate at much higher throughput for supported types',
      'Tensor cores handle all integer math',
      'CUDA cores are only on consumer GPUs',
    ],
    a: 1,
    e: 'CUDA cores are general-purpose ALUs (one FMA/cycle each for scalar ops). Tensor cores are matrix engines that do a whole small matmul per instruction, giving order-of-magnitude higher throughput for GEMM-shaped, lower-precision work.',
    ref: 'NVIDIA architecture whitepapers',
    tags: ['cuda-cores', 'tensor-cores', 'architecture'],
  },
  {
    d: 4,
    q: 'Why are warp-level matrix instructions (WMMA/mma) needed to use Tensor Cores from CUDA C++?',
    o: [
      'Tensor cores are accessed per-thread like normal ALUs',
      'A tensor-core MMA is a collective warp operation: the warp cooperatively holds fragments of the matrices, so the API works on per-warp "fragments," not scalars',
      'They are only usable from cuBLAS',
      'They require atomics',
    ],
    a: 1,
    e: 'Tensor-core MMAs are warp-collective: the input/output matrix tiles are distributed across the warp’s lanes as "fragments." The WMMA API (or inline mma.sync/ wgmma) loads fragments, performs the MMA, and stores results — it cannot be expressed as independent per-thread math.',
    ref: 'CUDA C++ Programming Guide (Warp Matrix Functions); PTX ISA (mma)',
    tags: ['wmma', 'tensor-cores', 'warp-collective'],
  },
])
