import { defineQuestions } from './_helpers.js'

// Kernel Optimization — batch D. Autotuning, occupancy cliffs, epilogue fusion,
// warp-shuffle GEMM, mixed-precision tricks. Distinct from optimization.js/.b.js/.c.js.
export default defineQuestions(
  'optimization',
  [
    {
      d: 4,
      q: 'Autotuning a kernel (e.g. sweeping tile sizes / block sizes) is valuable because…',
      o: [
        'There is one optimal config',
        'The best configuration depends on problem shape, data types, and the specific GPU; searching the space finds the winner that static heuristics miss',
        'It reduces precision',
        'It avoids profiling',
      ],
      a: 1,
      x: [
        'It compiles faster',
        'It removes registers',
        'It disables tensor cores',
      ],
      e: 'Performance is sensitive to tile/block/unroll/stage choices that interact with shape and hardware. Autotuners (Triton, CUTLASS profiler, cuBLASLt heuristics) benchmark candidates per problem to pick the fastest — beating fixed defaults.',
      ref: 'Autotuning; CUTLASS profiler; Triton',
      tags: ['autotuning', 'tile-size', 'tuning'],
    },
    {
      d: 4,
      q: 'An "occupancy cliff" is when…',
      o: [
        'Occupancy increases smoothly',
        'A tiny change in register/shared-memory use crosses a granularity threshold, dropping resident blocks (and occupancy) in a sudden step',
        'Occupancy is always 100%',
        'The grid is too small',
      ],
      a: 1,
      x: [
        'A branch diverges',
        'The L2 misses',
        'Bandwidth saturates',
      ],
      e: 'Because resources are allocated in granular chunks, nudging register or shared-memory usage past a boundary can abruptly reduce how many blocks fit per SM. Watching ptxas -v and the occupancy calculator helps you stay below such cliffs.',
      ref: 'CUDA Occupancy Calculator; allocation granularity',
      tags: ['occupancy-cliff', 'granularity', 'registers'],
    },
    {
      d: 4,
      q: 'Fusing the GEMM epilogue (bias + activation) into the matmul kernel rather than a separate elementwise kernel saves…',
      o: [
        'Compute',
        'A full read+write of the output matrix to/from HBM (the separate kernel would reload and rewrite it) — a big memory-traffic saving',
        'Registers',
        'Occupancy',
      ],
      a: 1,
      x: [
        'Launch latency only',
        'Bank conflicts',
        'Divergence',
      ],
      e: 'A standalone bias/activation kernel rereads the GEMM output from HBM and writes it back. Fusing those into the GEMM epilogue applies them while the result is still in registers, eliminating that extra DRAM round-trip (cuBLASLt/CUTLASS support this).',
      ref: 'cuBLASLt/CUTLASS epilogue fusion',
      tags: ['epilogue-fusion', 'gemm', 'memory-traffic'],
    },
    {
      d: 5,
      q: 'A small-matrix (e.g. 16×16) batched GEMM is better done with one batched kernel than a loop of cublasSgemm because…',
      o: [
        'Batched is less accurate',
        'Per-call launch overhead and underutilization dominate for tiny matrices; a batched kernel processes all matrices in one launch, amortizing overhead and filling the GPU',
        'Loops use tensor cores',
        'Batched needs more memory',
      ],
      a: 1,
      x: [
        'Loops are faster',
        'Batched disables tensor cores',
        'Tiny GEMMs are compute-bound',
      ],
      e: 'Tiny GEMMs are launch- and occupancy-limited. cublasGemmStridedBatched/Batched (or a custom batched kernel) runs the whole batch in one launch, keeping the GPU busy and amortizing per-call overhead — far faster than a per-matrix loop.',
      ref: 'cuBLAS batched GEMM',
      tags: ['batched-gemm', 'small-matrices', 'launch-overhead'],
    },
    {
      d: 4,
      q: 'Using __shfl to broadcast a value computed by one lane to the rest of the warp avoids…',
      o: [
        'Tensor cores',
        'A shared-memory round-trip (write+__syncthreads+read) — the shuffle exchanges the value directly via registers, faster for warp-scope broadcast',
        'Global memory',
        'Divergence',
      ],
      a: 1,
      x: [
        'Registers',
        'The L2',
        'Coalescing',
      ],
      e: 'For a warp-wide broadcast/exchange, __shfl_sync moves register values between lanes directly, avoiding the latency and barrier of staging through shared memory — a common micro-optimization in warp-cooperative code.',
      ref: 'CUDA warp shuffle; broadcast',
      tags: ['shuffle', 'broadcast', 'warp'],
    },
    {
      d: 3,
      q: 'Choosing the loop nest so the innermost (unit-stride) dimension matches the warp’s thread index ensures…',
      o: [
        'More registers',
        'Coalesced global accesses — consecutive threads touch consecutive addresses in the hot inner loop',
        'Tensor-core usage',
        'Lower precision',
      ],
      a: 1,
      x: [
        'Fewer iterations',
        'No bounds checks',
        'Higher occupancy',
      ],
      e: 'Mapping threadIdx.x to the unit-stride dimension makes a warp’s inner-loop accesses contiguous (coalesced). Mismatched mapping causes strided, uncoalesced access — often the single biggest factor in a memory-bound kernel’s efficiency.',
      ref: 'CUDA Best Practices Guide; loop/thread mapping',
      tags: ['coalescing', 'loop-mapping', 'thread-index'],
    },
    {
      d: 5,
      q: 'In a Hopper GEMM, replacing cp.async (Ampere-style) loads with TMA improves performance because TMA…',
      o: [
        'Computes faster',
        'Offloads multidimensional address generation and bulk-copies tiles asynchronously, freeing registers and threads from indexing and enabling deeper overlap',
        'Uses less precision',
        'Avoids shared memory',
      ],
      a: 1,
      x: [
        'Disables tensor cores',
        'Reduces occupancy',
        'Adds atomics',
      ],
      e: 'TMA replaces per-thread cp.async with a single descriptor-driven bulk async copy: the hardware computes the tile addresses, so threads/registers aren’t spent on indexing, and copies overlap compute via mbarriers — key to peak Hopper GEMM.',
      ref: 'Hopper Tuning Guide; CUTLASS 3.x TMA',
      tags: ['tma', 'cp-async', 'hopper', 'gemm'],
    },
    {
      d: 4,
      q: 'For a bandwidth-bound kernel already at ~85% of peak, the realistic next step is…',
      o: [
        'Increase occupancy',
        'Reduce the bytes moved (fuse with neighbors, reuse via tiling, or use a smaller data type) — you can’t exceed the bandwidth roof, so move less data',
        'Add more atomics',
        'Use FP64',
      ],
      a: 1,
      x: [
        'Add more registers',
        'Increase the block size',
        'Disable the L2',
      ],
      e: 'Near the bandwidth roof, occupancy/compute tweaks won’t help. The only lever is cutting traffic: fuse to avoid intermediate round-trips, tile for reuse, or store in fewer bytes (FP16/BF16/INT8). Algorithmic byte reduction is the path.',
      ref: 'Roofline; bandwidth-bound optimization',
      tags: ['bandwidth-bound', 'fusion', 'precision'],
    },
    {
      d: 4,
      q: 'A reduction that loads multiple elements per thread AND uses warp shuffles for the final stages is fast because it combines…',
      o: [
        'Two slow techniques',
        'High memory throughput (each thread streams several coalesced elements) with low-overhead final combination (shuffles, no shared memory/barriers for the last warp)',
        'Atomics and locks',
        'Constant and texture memory',
      ],
      a: 1,
      x: [
        'FP64 and tensor cores',
        'Divergence and branching',
        'Global atomics only',
      ],
      e: 'The two refinements address different limiters: multi-element loads keep the reduction bandwidth-bound (its ideal), and shuffle-based final stages remove shared-memory/barrier overhead — together approaching peak reduction performance.',
      ref: 'Harris reduction; shuffle reductions',
      tags: ['reduction', 'shuffle', 'bandwidth'],
    },
    {
      d: 4,
      q: 'When a kernel spills registers to local memory, a first remedy that often helps is…',
      o: [
        'Increase the block size',
        'Reduce live state (smaller tiles, recompute instead of hold, restructure) or cap registers — though capping can trade spills for lower occupancy, so measure',
        'Add atomics',
        'Use constant memory for outputs',
      ],
      a: 1,
      x: [
        'Disable the L2',
        'Use FP64',
        'Increase the grid',
      ],
      e: 'Spills come from too much live data per thread. Cutting the working set (smaller micro-tiles, recompute cheap values, fewer simultaneous live variables) reduces register pressure. -maxrregcount caps registers but may spill anyway or cut occupancy — profile both.',
      ref: 'Register spilling; CUDA Best Practices Guide',
      tags: ['spilling', 'registers', 'optimization'],
    },
    {
      d: 5,
      q: 'In FlashAttention-2, a key improvement over FlashAttention-1 is…',
      o: [
        'Using FP64',
        'Reducing non-matmul work and improving parallelism/work partitioning (e.g. parallelizing over sequence length, fewer rescaling ops) to raise GPU utilization',
        'Materializing the score matrix',
        'Removing tiling',
      ],
      a: 1,
      x: [
        'Using the CPU',
        'Disabling tensor cores',
        'Avoiding softmax',
      ],
      e: 'FlashAttention-2 restructures the algorithm to cut the proportion of non-matmul (rescaling) FLOPs and parallelizes better across the sequence and warps, increasing tensor-core utilization versus the original — a systems-level optimization.',
      ref: 'Dao, "FlashAttention-2"',
      tags: ['flash-attention', 'attention', 'utilization'],
    },
    {
      d: 3,
      q: 'Precomputing and reusing an index/address that several statements share (common subexpression elimination) in a kernel…',
      o: [
        'Always hurts',
        'Avoids recomputing the same address arithmetic repeatedly, reducing instruction count in the hot path (the compiler often does this, but manual CSE helps complex cases)',
        'Increases divergence',
        'Reduces precision',
      ],
      a: 1,
      x: [
        'Uses tensor cores',
        'Pins memory',
        'Disables coalescing',
      ],
      e: 'Hoisting shared address/index computations out of repeated use trims redundant integer ops in the inner loop. Compilers handle simple cases; manual CSE matters where aliasing or complexity blocks the optimizer.',
      ref: 'CUDA Best Practices Guide; common subexpression elimination',
      tags: ['cse', 'index-arithmetic', 'optimization'],
    },
    {
      d: 4,
      q: 'A fused "softmax" kernel (max, exp-sum, normalize in one launch) outperforms three separate kernels mainly because…',
      o: [
        'It is more accurate',
        'It keeps the row in on-chip memory across the three passes instead of re-reading/writing it from HBM each time, cutting memory traffic',
        'It uses tensor cores',
        'It avoids the max',
      ],
      a: 1,
      x: [
        'It increases occupancy',
        'It reduces registers',
        'It needs no reduction',
      ],
      e: 'Three kernels would each stream the row through HBM. Fusing them (often with online/streaming softmax) keeps the data on-chip (registers/shared) across passes, eliminating the redundant global reads/writes — the usual win for fused elementwise+reduction ops.',
      ref: 'Fused softmax; online softmax',
      tags: ['fusion', 'softmax', 'memory-traffic'],
    },
    {
      d: 5,
      q: 'Why does padding a [N][N] shared tile to [N][N+1] sometimes cost negligible performance despite using more shared memory?',
      o: [
        'Padding is free',
        'The extra column eliminates 32-way bank conflicts on column access; the small shared-memory increase rarely changes occupancy, so the conflict-free access dominates',
        'It reduces registers',
        'It enables tensor cores',
      ],
      a: 1,
      x: [
        'It increases divergence',
        'It disables the L2',
        'It compresses data',
      ],
      e: 'For a 32-wide tile, padding to 33 offsets each row by one bank so a column maps across all 32 banks (no conflict). The few extra bytes seldom reduce resident blocks, so you gain conflict-free access at almost no occupancy cost.',
      ref: 'PMPP Ch. 6; bank-conflict padding',
      tags: ['padding', 'bank-conflict', 'shared-memory'],
    },
    {
      d: 4,
      q: 'Mixed-precision elementwise kernels often store in FP16/BF16 but compute in FP32 by…',
      o: [
        'Never converting',
        'Loading 16-bit, converting to FP32 for the math (accuracy), then converting back to 16-bit for storage — keeping memory traffic low while preserving compute accuracy',
        'Computing in INT8',
        'Using FP64',
      ],
      a: 1,
      x: [
        'Storing in FP32',
        'Avoiding conversion',
        'Using tensor cores only',
      ],
      e: 'For bandwidth-bound elementwise ops, 16-bit storage halves traffic while FP32 compute (cheap relative to memory) preserves accuracy. The load→upconvert→compute→downconvert→store pattern captures both benefits.',
      ref: 'Mixed-precision elementwise kernels',
      tags: ['mixed-precision', 'elementwise', 'bandwidth'],
    },
    {
      d: 3,
      q: 'Increasing the work per thread (coarsening) helps a LATENCY-bound kernel because it…',
      o: [
        'Reduces memory traffic',
        'Gives each thread more independent instructions/loads (ILP/MLP) to overlap latencies, even without more warps',
        'Increases occupancy',
        'Uses tensor cores',
      ],
      a: 1,
      x: [
        'Lowers precision',
        'Avoids the L2',
        'Pins memory',
      ],
      e: 'A latency-bound kernel lacks work in flight. Coarsening adds independent per-thread work (more ILP/MLP), filling latency gaps — useful when occupancy is capped. (Once bandwidth/compute-bound, coarsening stops helping.)',
      ref: 'Thread coarsening; latency-bound remedies',
      tags: ['coarsening', 'latency-bound', 'ilp'],
    },
    {
      d: 5,
      q: 'A GEMM kernel tuned for square matrices underperforms on tall-skinny (large K, small M,N) shapes. The fix is…',
      o: [
        'Use FP64',
        'Split-K / Stream-K to parallelize the contraction dimension so enough blocks fill the GPU, then reduce partials',
        'Larger tiles',
        'Disable tensor cores',
      ],
      a: 1,
      x: [
        'Smaller grids',
        'More registers',
        'Atomics on inputs',
      ],
      e: 'With small M,N there aren’t enough output tiles to fill the SMs, so the GPU is underutilized. Split-K/Stream-K assign chunks of the long K dimension to extra blocks (then combine partials), restoring parallelism for tall-skinny GEMMs.',
      ref: 'Split-K/Stream-K; GEMM shape tuning',
      tags: ['split-k', 'stream-k', 'gemm-shape'],
    },
    {
      d: 4,
      q: 'Replacing a data-dependent branch inside a warp with arithmetic (e.g. multiply by a 0/1 mask) is worthwhile when…',
      o: [
        'The branch bodies are large',
        'The bodies are short and the branch would diverge — computing both and masking is cheaper than serializing two paths',
        'The branch never diverges',
        'It uses tensor cores',
      ],
      a: 1,
      x: [
        'You want more registers',
        'The kernel is memory-bound',
        'It avoids coalescing',
      ],
      e: 'For short divergent bodies, predication/masked arithmetic keeps the warp converged and is cheaper than executing both paths serially. For long bodies, the wasted work of computing both sides may exceed the divergence cost — so it’s a judgment call.',
      ref: 'Branchless programming; divergence trade-offs',
      tags: ['branchless', 'divergence', 'masking'],
    },
    {
      d: 4,
      q: 'cudaFuncAttributeMaxDynamicSharedMemorySize must be set (opt-in) to use large dynamic shared memory because…',
      o: [
        'Shared memory is unlimited',
        'The default static cap is 48 KB; using the architecture’s larger dynamic shared memory (e.g. up to ~228 KB on Hopper) requires explicitly opting in per kernel',
        'It disables L1',
        'It increases registers',
      ],
      a: 1,
      x: [
        'It pins memory',
        'It enables tensor cores',
        'It reduces occupancy automatically',
      ],
      e: 'To exceed the 48 KB default per-block shared memory, allocate it dynamically and raise the limit via cudaFuncSetAttribute(..., MaxDynamicSharedMemorySize, bytes). This unlocks bigger tiles/deeper pipelines on newer GPUs.',
      ref: 'CUDA Runtime API; opt-in shared memory',
      tags: ['shared-memory', 'opt-in', 'dynamic'],
    },
    {
      d: 3,
      q: 'Using a grid-stride loop instead of launching exactly N threads helps reuse and tuning because…',
      o: [
        'It avoids indexing',
        'You launch an occupancy-sized grid (independent of N) and each thread handles multiple elements — decoupling grid size from problem size for easy tuning and good reuse',
        'It removes bounds checks',
        'It uses tensor cores',
      ],
      a: 1,
      x: [
        'It lowers precision',
        'It disables coalescing',
        'It needs atomics',
      ],
      e: 'Grid-stride loops let you size the grid to fill the GPU (not to N), so the same kernel handles any N, tunes occupancy independently, and each thread amortizes setup over several elements — a robust, portable pattern.',
      ref: 'NVIDIA "Grid-Stride Loops"',
      tags: ['grid-stride', 'tuning', 'reuse'],
    },
    {
      d: 5,
      q: 'The biggest single reason a custom Triton/CUTLASS kernel beats a generic library call is usually…',
      o: [
        'Faster math units',
        'Fusion — combining adjacent ops (e.g. matmul + bias + activation + dropout) so intermediates stay on-chip, eliminating HBM round-trips the separate library calls would incur',
        'Lower precision',
        'More occupancy',
      ],
      a: 1,
      x: [
        'Avoiding tensor cores',
        'Smaller grids',
        'Using the CPU',
      ],
      e: 'Libraries optimize single ops well; the win for custom kernels is fusing a chain so intermediate tensors never hit HBM. For memory-bound pipelines (common in DL), eliminating those round-trips is the dominant speedup — hence torch.compile/Triton fusion.',
      ref: 'Kernel fusion; Triton/CUTLASS vs library calls',
      tags: ['fusion', 'triton', 'cutlass', 'memory-traffic'],
    },
    {
      d: 4,
      q: 'A kernel becomes SFU-bound after switching exp() to the fast __expf(). The correct interpretation is…',
      o: [
        'Something is broken',
        'You sped up each exp (now SFU throughput, not accuracy, is the limit); further gains need fewer transcendentals or overlapping them with other pipes — accept the accuracy trade-off was applied',
        'It is memory-bound',
        'Occupancy is too low',
      ],
      a: 1,
      x: [
        'Registers spilled',
        'It is host-bound',
        'Tensor cores are idle',
      ],
      e: '__expf moved work onto the high-throughput SFU path; if the kernel is now SFU-bound, you’ve hit that pipe’s ceiling. Reduce the count of transcendentals or balance with other work; precision was already traded for speed by using __expf.',
      ref: 'CUDA fast intrinsics; SFU-bound',
      tags: ['sfu', 'intrinsics', 'compute-bound'],
    },
    {
      d: 4,
      q: 'For an elementwise kernel on a large tensor, vectorizing to float4 typically improves throughput by…',
      o: [
        'Increasing precision',
        'Cutting the instruction count ~4× and issuing wider, fully-coalesced 16-byte accesses — raising effective bandwidth for the memory-bound op',
        'Using tensor cores',
        'Reducing occupancy',
      ],
      a: 1,
      x: [
        'Adding atomics',
        'Avoiding the L2',
        'Lowering bandwidth',
      ],
      e: 'Processing 4 elements per thread via float4 reduces memory and arithmetic instructions and uses aligned 16-byte transactions, pushing a bandwidth-bound elementwise kernel closer to peak — provided alignment and a multiple-of-4 length (or a remainder tail).',
      ref: 'CUDA vectorized memory access',
      tags: ['vectorization', 'float4', 'elementwise'],
    },
    {
      d: 3,
      q: 'When two consecutive kernels share data, keeping it in GLOBAL memory between them (vs recomputing or copying to host) is correct because…',
      o: [
        'Global memory is cleared between kernels',
        'Device memory persists across kernel launches, and the launch boundary guarantees the first kernel’s writes are visible to the second — no copy needed',
        'It must go to the host',
        'Shared memory persists',
      ],
      a: 1,
      x: [
        'It needs a fence',
        'Registers persist',
        'It requires managed memory',
      ],
      e: 'Device allocations persist across launches and successive kernels are ordered with full visibility, so intermediate results just stay in global memory. Round-tripping to the host would waste PCIe bandwidth; shared memory/registers don’t persist across launches.',
      ref: 'CUDA kernel ordering; data residency',
      tags: ['data-residency', 'kernel-ordering', 'global-memory'],
    },
    {
      d: 5,
      q: 'In a deeply-pipelined GEMM, the number of pipeline stages is chosen to…',
      o: [
        'Maximize shared memory use',
        'Be just enough to fully hide the global-load latency behind compute; more stages waste shared memory/occupancy, fewer leave the tensor cores stalling on loads',
        'Always be 2',
        'Match the warp size',
      ],
      a: 1,
      x: [
        'Minimize registers',
        'Equal the tile size',
        'Match the SM count',
      ],
      e: 'Stages buy latency hiding at a shared-memory cost. Too few ⇒ math stalls waiting on loads; too many ⇒ shared memory/occupancy wasted with no extra hiding. The sweet spot covers the load latency given the compute per stage — tuned per arch/shape.',
      ref: 'CUTLASS pipeline stages; latency hiding',
      tags: ['pipelining', 'stages', 'gemm', 'tuning'],
    },
    {
      d: 4,
      q: 'A profiler shows a kernel is "L2-bound" (high L2 throughput, DRAM not saturated). A useful optimization is…',
      o: [
        'Add more atomics',
        'Increase L1/register reuse (tile so more accesses hit L1 or stay in registers) so fewer requests reach L2 — or improve locality to raise L1 hit rate',
        'Use FP64',
        'Lower occupancy',
      ],
      a: 1,
      x: [
        'Disable the L1',
        'Increase the grid only',
        'Use constant memory for outputs',
      ],
      e: 'L2-bound means traffic that could be served closer (L1/registers) is going to L2. Better tiling/reuse keeps data in L1 or registers, reducing L2 requests. The L2 persisting window can also help for revisited regions.',
      ref: 'Nsight Compute; L2-bound analysis',
      tags: ['l2-bound', 'reuse', 'tiling'],
    },
    {
      d: 3,
      q: 'Choosing block dimensions of (32, 8) instead of (8, 32) for a 2D kernel often matters because…',
      o: [
        'Total threads differ',
        'threadIdx.x should map to the unit-stride (coalesced) dimension; making the x-extent 32 aligns a warp with contiguous memory, while (8,32) may scatter the warp',
        'It changes occupancy',
        'It uses tensor cores',
      ],
      a: 1,
      x: [
        'It reduces registers',
        'It avoids divergence',
        'It enables FP64',
      ],
      e: 'Warps are formed along threadIdx.x first. Putting the contiguous memory dimension on x (e.g. 32 wide) makes a warp’s accesses coalesced; swapping the extents can break coalescing even with the same thread count.',
      ref: 'CUDA Best Practices Guide; 2D block shape',
      tags: ['block-shape', 'coalescing', 'warp'],
    },
    {
      d: 5,
      q: 'cuBLASLt heuristics (cublasLtMatmulAlgoGetHeuristic) help by…',
      o: [
        'Running on the CPU',
        'Returning a ranked list of candidate algorithms/tile configs for the specific matmul, which you can benchmark to pick the fastest — autotuning the GEMM per problem',
        'Disabling tensor cores',
        'Reducing precision',
      ],
      a: 1,
      x: [
        'Compiling kernels',
        'Allocating memory',
        'Profiling the host',
      ],
      e: 'cuBLASLt exposes a heuristic that proposes candidate algorithms for your exact matmul (shape, types, layouts); you can try the top few and cache the winner. This per-problem selection extracts more performance than a single fixed kernel.',
      ref: 'cuBLASLt heuristics/algorithm selection',
      tags: ['cublaslt', 'heuristics', 'autotuning', 'gemm'],
    },
    {
      d: 4,
      q: 'Reducing the number of __syncthreads in a tiled kernel (e.g. by double-buffering) helps because each barrier…',
      o: [
        'Corrupts shared memory',
        'Forces all warps to wait for the slowest, so removing avoidable barriers (overlapping load of the next tile with compute on the current) reduces stalling',
        'Spills registers',
        'Disables coalescing',
      ],
      a: 1,
      x: [
        'Flushes the L2',
        'Increases divergence',
        'Reduces occupancy',
      ],
      e: 'Barriers serialize the block at each occurrence. Double-buffering lets the next tile load while the current computes, cutting the number of synchronization points (and overlapping memory with compute) — a standard tiled-kernel optimization.',
      ref: 'Double buffering; reducing barriers',
      tags: ['double-buffering', 'syncthreads', 'overlap'],
    },
  ],
  { idPrefix: 'optimization-d' }
)
