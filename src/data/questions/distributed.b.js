import { defineQuestions } from './_helpers.js'

// Distributed Training — batch B. Memory accounting, schedules, ZeRO-offload,
// LR scaling, MFU, checkpointing, overlap. Distinct from distributed.js.
export default defineQuestions(
  'distributed',
  [
    {
      d: 4,
      q: 'For a model with P parameters trained with Adam in FP32, the optimizer states (momentum + variance) alone consume roughly…',
      o: [
        '0 bytes',
        '8P bytes (FP32 m and v)',
        '2P bytes',
        '16P bytes',
      ],
      a: 1,
      x: ['4P bytes', '12P bytes', '32P bytes'],
      e: 'Adam keeps first (m) and second (v) moments — two FP32 values per parameter = 8 bytes × P. Add FP32 master weights (4P) and gradients, and optimizer/master state dominates memory, which is exactly what ZeRO shards.',
      ref: 'ZeRO paper (memory accounting); Adam',
      tags: ['adam', 'optimizer-states', 'memory'],
    },
    {
      d: 3,
      q: 'The four main consumers of training memory are parameters, gradients, optimizer states, and…',
      o: [
        'The dataset',
        'Activations stored for the backward pass',
        'The CUDA context',
        'The kernel code',
      ],
      a: 1,
      x: [
        'The learning-rate schedule',
        'The random seed',
        'The communication buffers only',
      ],
      e: 'Activations stored for backprop can dominate, especially for long sequences/large batches. Techniques: activation checkpointing (recompute), sequence/context parallelism, and smaller micro-batches reduce activation memory.',
      ref: 'Ultra-Scale Playbook; memory breakdown',
      tags: ['activations', 'memory-breakdown'],
    },
    {
      d: 4,
      q: 'ZeRO-Offload moves which state to CPU memory to fit larger models?',
      o: [
        'The activations',
        'Optimizer states to CPU; update runs on CPU',
        'The dataset',
        'The model architecture',
      ],
      a: 1,
      x: [
        'The forward pass',
        'The communication buffers',
        'The CUDA kernels',
      ],
      e: 'ZeRO-Offload keeps the optimizer states and FP32 master weights in CPU memory and runs the optimizer step on the CPU, trading PCIe transfer + CPU compute for the ability to train models that wouldn’t fit in GPU memory. ZeRO-Infinity extends to NVMe.',
      ref: 'ZeRO-Offload / ZeRO-Infinity (DeepSpeed)',
      tags: ['zero-offload', 'cpu-offload', 'memory'],
    },
    {
      d: 4,
      q: 'GPipe’s pipeline schedule (all forwards, then all backwards) has a larger activation-memory footprint than 1F1B because…',
      o: [
        'It uses more stages',
        'Holds all micro-batch activations till backward',
        'It has no bubble',
        'It uses lower precision',
      ],
      a: 1,
      x: [
        'It recomputes everything',
        'It uses more GPUs',
        'It avoids checkpointing',
      ],
      e: 'GPipe runs every micro-batch forward (accumulating their activations) before starting backwards, so peak activation memory scales with the number of micro-batches. 1F1B interleaves forward/backward to bound in-flight activations to ~pipeline depth.',
      ref: 'GPipe; Megatron 1F1B; Ultra-Scale Playbook',
      tags: ['gpipe', 'pipeline', 'activation-memory'],
    },
    {
      d: 5,
      q: 'The interleaved (virtual-pipeline) schedule reduces the pipeline bubble further by…',
      o: [
        'Adding more micro-batches only',
        'Multiple virtual stages per GPU; finer pipeline fill',
        'Removing pipeline parallelism',
        'Using FP8',
      ],
      a: 1,
      x: [
        'Doubling the batch size',
        'Disabling data parallelism',
        'Recomputing activations',
      ],
      e: 'Interleaving gives each GPU several smaller stage chunks spread through the model. More, smaller stages shrink the bubble fraction (≈ (p/v−... )) at the cost of more point-to-point communication between chunks.',
      ref: 'Megatron-LM (interleaved 1F1B); Ultra-Scale Playbook',
      tags: ['interleaved-pipeline', 'virtual-stages', 'bubble'],
    },
    {
      d: 5,
      q: '"Zero-bubble" pipeline schedules aim to eliminate the bubble by…',
      o: [
        'Removing backward passes',
        'Schedule weight-grad work into idle bubble slots',
        'Using one micro-batch',
        'Disabling the optimizer',
      ],
      a: 1,
      x: [
        'Compressing activations',
        'Running on one GPU',
        'Doubling precision',
      ],
      e: 'Backprop computes gradient-w.r.t-input (needed to continue the pipeline) and gradient-w.r.t-weights (only needed before the optimizer step). Scheduling the flexible weight-grad work into bubble gaps can drive the bubble toward zero.',
      ref: 'Qi et al., "Zero Bubble Pipeline Parallelism"',
      tags: ['zero-bubble', 'pipeline', 'scheduling'],
    },
    {
      d: 4,
      q: 'The "linear scaling rule" for large-batch training says when you multiply the global batch size by k, you should…',
      o: [
        'Divide the learning rate by k',
        'Multiply LR by k (with warmup)',
        'Keep the learning rate fixed',
        'Square the learning rate',
      ],
      a: 1,
      x: [
        'Multiply the learning rate by k²',
        'Reduce the model size',
        'Increase precision',
      ],
      e: 'Goyal et al. showed scaling LR linearly with batch size (plus a warmup) preserves training dynamics up to a point. Data parallelism increases the effective batch, so LR must scale accordingly to converge well.',
      ref: 'Goyal et al., "Accurate, Large Minibatch SGD"',
      tags: ['learning-rate', 'large-batch', 'linear-scaling'],
    },
    {
      d: 3,
      q: 'In DDP, wrapping the loss.backward() of non-final gradient-accumulation micro-steps with model.no_sync() avoids…',
      o: [
        'Computing gradients',
        'Triggering an all-reduce on every micro-step',
        'Using the optimizer',
        'Storing activations',
      ],
      a: 1,
      x: [
        'The forward pass',
        'Loading data',
        'The learning-rate schedule',
      ],
      e: 'During gradient accumulation you only need to all-reduce once per optimizer step. no_sync() suppresses the per-micro-step gradient sync, accumulating locally and reducing once at the end — saving communication.',
      ref: 'PyTorch DDP (no_sync); gradient accumulation',
      tags: ['ddp', 'no-sync', 'gradient-accumulation'],
    },
    {
      d: 5,
      q: 'Megatron "sequence parallelism" complements tensor parallelism by parallelizing the parts of a layer that TP leaves replicated (LayerNorm, dropout). The added communication is…',
      o: [
        'A single broadcast',
        'All-gather + reduce-scatter around TP; same cost',
        'Nothing',
        'A pipeline send',
      ],
      a: 1,
      x: [
        'Two all-to-alls',
        'A gather to rank 0',
        'An extra optimizer step',
      ],
      e: 'Sequence parallelism shards the sequence dimension in the otherwise-replicated regions, cutting their activation memory. The TP all-reduce is decomposed into all-gather + reduce-scatter around the parallel region, with similar total volume.',
      ref: 'Korthikanti et al., "Reducing Activation Recomputation" (Megatron SP)',
      tags: ['sequence-parallelism', 'tensor-parallelism', 'activation-memory'],
    },
    {
      d: 4,
      q: 'Compared to all-reduce-based data parallelism, the classic parameter-server approach…',
      o: [
        'Always scales better',
        'Hub bottleneck; superseded by all-reduce',
        'Uses no network',
        'Requires no servers',
      ],
      a: 1,
      x: [
        'Eliminates communication',
        'Is bandwidth-optimal',
        'Needs only one worker',
      ],
      e: 'Parameter servers gather gradients and push back parameters, creating a hub bottleneck and asymmetry. All-reduce (ring/tree) spreads communication evenly across workers and is the standard for dense synchronous training; PS persists in some sparse/async settings.',
      ref: 'Distributed training literature; PS vs all-reduce',
      tags: ['parameter-server', 'all-reduce', 'architecture'],
    },
    {
      d: 4,
      q: 'Model FLOPs Utilization (MFU) measures…',
      o: [
        'GPU temperature',
        'Model FLOP/s as fraction of hardware peak FLOP/s',
        'Memory usage',
        'Network bandwidth',
      ],
      a: 1,
      x: [
        'The number of parameters',
        'The batch size',
        'The learning rate',
      ],
      e: 'MFU = (model FLOPs per step × steps/sec) / peak FLOP/s. It captures how well the whole pipeline (compute, comm, bubbles, data loading) uses the GPUs; large-LM training often reports 30–55% MFU.',
      ref: 'PaLM paper; MFU metric',
      tags: ['mfu', 'efficiency', 'utilization'],
    },
    {
      d: 4,
      q: 'Tensor parallelism for multi-head attention naturally splits across GPUs by…',
      o: [
        'Splitting the sequence',
        'Each GPU gets a subset of heads; output projection combines',
        'Splitting the batch',
        'Replicating all heads',
      ],
      a: 1,
      x: [
        'Splitting the embedding dimension per token',
        'Sharding the optimizer',
        'Splitting the vocabulary',
      ],
      e: 'Heads are independent, so each GPU handles some heads (column-parallel QKV), and the output projection is row-parallel with an all-reduce — mirroring the MLP TP pattern. This keeps per-layer communication to one all-reduce (forward).',
      ref: 'Megatron-LM (attention tensor parallelism)',
      tags: ['tensor-parallelism', 'attention', 'heads'],
    },
    {
      d: 5,
      q: 'Ring Attention enables extremely long context by…',
      o: [
        'Storing the full attention matrix',
        'K/V blocks circulate a ring; each GPU attends all keys',
        'Using FP8 only',
        'Replicating the sequence',
      ],
      a: 1,
      x: [
        'Truncating the context',
        'Using a parameter server',
        'Compressing tokens',
      ],
      e: 'Ring Attention shards queries/keys/values by sequence across devices; K/V tiles circulate around the ring while each device accumulates its blockwise (online-softmax) attention. Communication overlaps with compute, enabling near-arbitrary context length.',
      ref: 'Liu et al., "Ring Attention"',
      tags: ['ring-attention', 'context-parallel', 'long-context'],
    },
    {
      d: 3,
      q: 'A "straggler" in synchronous data-parallel training is…',
      o: [
        'A slow data file',
        'Slow rank; others wait at the all-reduce',
        'A diverged gradient',
        'A failed optimizer',
      ],
      a: 1,
      x: [
        'An extra GPU',
        'A communication buffer',
        'A learning-rate spike',
      ],
      e: 'Because synchronous SGD waits for everyone at the all-reduce barrier, the slowest rank sets the pace. Stragglers from hardware variance, thermal throttling, or load imbalance hurt scaling; balanced work and healthy hardware mitigate them.',
      ref: 'Distributed training; synchronization barriers',
      tags: ['straggler', 'synchronous-sgd', 'load-balancing'],
    },
    {
      d: 4,
      q: 'Sharded checkpointing (each rank saves its own shard) is used in large-scale training because…',
      o: [
        'It is required by the optimizer',
        'All-gather to save is too slow and memory-prohibitive',
        'It reduces model accuracy',
        'It avoids the need to checkpoint',
      ],
      a: 1,
      x: [
        'It compresses the model',
        'It runs on the CPU only',
        'It eliminates optimizer states',
      ],
      e: 'With sharded params/optimizer states (ZeRO/FSDP/TP), each rank writes its own portion in parallel, avoiding an all-gather to one rank. Loading can reshard to a different parallel layout. Frameworks provide distributed checkpoint APIs for this.',
      ref: 'PyTorch Distributed Checkpoint; DeepSpeed',
      tags: ['checkpointing', 'sharded', 'fsdp'],
    },
    {
      d: 4,
      q: 'Selective activation recomputation (vs. recomputing everything) chooses to…',
      o: [
        'Recompute nothing',
        'Recompute heavy activations; store cheap ones',
        'Recompute the optimizer',
        'Store all activations',
      ],
      a: 1,
      x: [
        'Recompute the dataset',
        'Recompute gradients only',
        'Recompute the weights',
      ],
      e: 'Full checkpointing recomputes ~a whole forward; selective recomputation targets the layers with the worst memory-to-FLOP ratio (e.g. attention softmax) for most of the memory saving at a fraction of the recompute cost.',
      ref: 'Korthikanti et al. (Megatron selective recomputation)',
      tags: ['activation-checkpointing', 'selective-recompute', 'memory'],
    },
    {
      d: 5,
      q: 'Ranking the per-step communication volume (lowest to highest) for the three classic parallelisms is roughly…',
      o: [
        'Tensor < Pipeline < Data',
        'PP (P2P) < DP (one all-reduce) < TP (per-layer)',
        'Data < Tensor < Pipeline',
        'They are all equal',
      ],
      a: 1,
      x: [
        'Tensor < Data < Pipeline',
        'Pipeline < Tensor < Data',
        'Data < Pipeline < Tensor',
      ],
      e: 'Pipeline only sends boundary activations between adjacent stages (small); data parallel does one all-reduce of gradients per step (overlappable); tensor parallel communicates every layer (multiple collectives/step), the heaviest — hence TP is confined to fast NVLink within a node.',
      ref: 'Megatron-LM; Ultra-Scale Playbook (communication analysis)',
      tags: ['communication-volume', 'parallelism', 'comparison'],
    },
    {
      d: 3,
      q: 'Why does ZeRO Stage 1 (sharding only optimizer states) already give a large memory saving for Adam?',
      o: [
        'It shards activations',
        '12 B/param optimizer state; sharding cuts by N',
        'It removes gradients',
        'It uses FP8',
      ],
      a: 1,
      x: [
        'It compresses the model',
        'It offloads to disk',
        'It reduces the batch size',
      ],
      e: 'For mixed-precision Adam, master weights (4B) + m (4B) + v (4B) = 12 bytes/param dwarf the 2-byte half weights/grads. Stage 1 shards just that dominant 12B/param across the DP group, the biggest bang-for-buck with minimal extra communication.',
      ref: 'ZeRO paper (stage 1)',
      tags: ['zero', 'optimizer-sharding', 'memory'],
    },
    {
      d: 4,
      q: 'FSDP wrapping granularity (which submodules become FSDP units) matters because it controls…',
      o: [
        'The learning rate',
        'All-gather size and overlap granularity',
        'The model accuracy',
        'The dataset order',
      ],
      a: 1,
      x: [
        'The random seed',
        'The optimizer choice',
        'The number of GPUs',
      ],
      e: 'Each FSDP unit all-gathers its params just-in-time. Wrapping per transformer block is a common sweet spot: units are large enough to overlap communication with compute yet small enough to bound peak unsharded memory.',
      ref: 'PyTorch FSDP (auto_wrap_policy)',
      tags: ['fsdp', 'wrapping', 'overlap'],
    },
    {
      d: 4,
      q: 'Gradient compression (e.g. quantization or top-k sparsification) for distributed training trades…',
      o: [
        'Memory for compute',
        'Less comm volume; loses some gradient fidelity',
        'Accuracy for memory',
        'Latency for precision',
      ],
      a: 1,
      x: [
        'Compute for accuracy only',
        'Nothing — it is free',
        'GPUs for CPUs',
      ],
      e: 'Compressing gradients (1-bit/quantized, low-rank, top-k) shrinks the all-reduce payload, useful on slow networks, but introduces error (often needing error-feedback) and compute overhead. Benefit depends on whether communication is the bottleneck.',
      ref: 'Gradient compression literature (1-bit Adam, PowerSGD)',
      tags: ['gradient-compression', 'communication', 'tradeoff'],
    },
    {
      d: 3,
      q: 'The effective (global) batch size in 3D-parallel training is computed as…',
      o: [
        'micro_batch × tensor_parallel_size',
        'micro_batch × grad_accum_steps × data_parallel_size',
        'micro_batch × pipeline_stages',
        'micro_batch only',
      ],
      a: 1,
      x: [
        'micro_batch × world_size',
        'micro_batch × tensor × pipeline',
        'global_batch × data_parallel_size',
      ],
      e: 'Tensor and pipeline parallelism split ONE model replica (they don’t add batch); data parallelism and gradient accumulation multiply the batch. So global batch = micro_batch × grad_accum × DP_size. Getting this right is essential for LR/convergence.',
      ref: 'Megatron-LM; batch-size accounting',
      tags: ['batch-size', '3d-parallelism', 'accounting'],
    },
    {
      d: 5,
      q: 'Overlapping the FSDP all-gather of layer N+1’s parameters with the compute of layer N is possible because…',
      o: [
        'They use the same stream',
        'Separate prefetch stream; overlaps with current layer',
        'Communication is instantaneous',
        'Parameters are never sharded',
      ],
      a: 1,
      x: [
        'The optimizer runs concurrently',
        'It uses the CPU',
        'Layers are independent of data',
      ],
      e: 'FSDP prefetches the next unit’s parameters (all-gather) on a communication stream while the current unit computes, so by the time layer N+1 runs its weights are present. Good prefetch + wrapping is what keeps FSDP throughput high.',
      ref: 'PyTorch FSDP (prefetch/overlap)',
      tags: ['fsdp', 'overlap', 'prefetch'],
    },
    {
      d: 3,
      q: 'Why is pipeline parallelism often placed ACROSS nodes while tensor parallelism stays within a node?',
      o: [
        'Pipeline needs more memory',
        'PP infrequent transfers tolerate inter-node; TP needs NVLink',
        'Tensor parallelism cannot cross GPUs',
        'Pipeline parallelism only works on one GPU',
      ],
      a: 1,
      x: [
        'Pipeline uses no communication',
        'Tensor parallelism is latency-insensitive',
        'Nodes cannot do tensor parallelism',
      ],
      e: 'TP communicates a lot and is latency-sensitive (every layer), so it needs NVLink within a node; PP only passes stage-boundary activations occasionally, which the inter-node network can handle. This mapping is the standard 3D layout.',
      ref: 'Megatron-LM; Ultra-Scale Playbook',
      tags: ['pipeline', 'tensor-parallelism', 'topology'],
    },
    {
      d: 4,
      q: 'Asynchronous SGD (no global gradient barrier) can speed up throughput but risks…',
      o: [
        'Nothing',
        'Stale gradients; degraded convergence',
        'Running out of GPUs',
        'Higher precision',
      ],
      a: 1,
      x: [
        'Deadlock always',
        'Loss of data',
        'Lower memory use',
      ],
      e: 'Async SGD lets workers update without waiting, eliminating straggler stalls, but workers apply gradients based on stale parameter versions. Staleness harms convergence, so most large-scale dense training stays synchronous.',
      ref: 'Async SGD literature (Hogwild!, DistBelief)',
      tags: ['async-sgd', 'staleness', 'convergence'],
    },
    {
      d: 5,
      q: 'In MoE training, the two All-to-All exchanges (dispatch and combine) can dominate cost. A common mitigation is…',
      o: [
        'Removing the experts',
        'Balance routing + capacity; overlap All-to-Alls',
        'Using a parameter server',
        'Disabling tensor parallelism',
      ],
      a: 1,
      x: [
        'Replicating all experts',
        'Switching to FP64',
        'Skipping the gating',
      ],
      e: 'MoE all-to-all moves tokens to expert-owning GPUs and back. Balancing routing (so no GPU is overloaded), capping per-expert capacity, and overlapping the communication with expert FFN compute keep the all-to-all from becoming the bottleneck.',
      ref: 'GShard; Switch Transformer; MoE systems',
      tags: ['moe', 'all-to-all', 'overlap', 'expert-parallelism'],
    },
    {
      d: 4,
      q: 'A simple way to estimate whether data-parallel training will scale well is to compare gradient all-reduce time to…',
      o: [
        'The dataset size',
        'Backward compute time; all-reduce must fit within it',
        'The learning rate',
        'The number of parameters only',
      ],
      a: 1,
      x: [
        'The forward time only',
        'The optimizer step time',
        'The validation loss',
      ],
      e: 'DP scales near-linearly when the per-step all-reduce overlaps with (and is shorter than) backward compute. As models/clusters grow or bandwidth shrinks, comm can exceed compute, capping scaling — motivating bucketing, faster interconnects, or other parallelism.',
      ref: 'Distributed training analysis; communication/compute overlap',
      tags: ['data-parallel', 'scaling', 'overlap'],
    },
  ],
  { idPrefix: 'distributed-b' }
)
