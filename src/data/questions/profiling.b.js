import { defineQuestions } from './_helpers.js'

// Performance & Profiling — batch B. Nsight metrics, stall reasons, efficiency
// counters, roofline, profiler workflow. Distinct from profiling.js (batch A).
export default defineQuestions(
  'profiling',
  [
    {
      d: 3,
      q: 'You want to know WHY a kernel is slow (which hardware unit limits it). Which tool/section?',
      o: [
        'Nsight Systems timeline',
        'Nsight Compute "Speed of Light" (SOL) section, which reports compute vs. memory utilization vs. peak',
        'nvidia-smi',
        'gdb',
      ],
      a: 1,
      x: [
        'The compiler’s -v output',
        'NVTX ranges',
        'cudaGetLastError',
      ],
      e: 'The Speed-of-Light section summarizes how close the kernel is to the compute and memory roofs, immediately classifying it as compute-bound, memory-bound, or latency-bound — the starting point of kernel analysis.',
      ref: 'Nsight Compute (GPU Speed of Light)',
      tags: ['speed-of-light', 'nsight-compute', 'bottleneck'],
    },
    {
      d: 3,
      q: 'In Nsight Compute, "global load efficiency" (sectors requested vs. ideal) below 100% indicates…',
      o: [
        'Too many registers',
        'Uncoalesced / partially-used cache-line accesses — the warp fetches more sectors than its data needs',
        'High occupancy',
        'Tensor-core underuse',
      ],
      a: 1,
      x: [
        'Bank conflicts in shared memory',
        'Excess atomics',
        'A divergent branch',
      ],
      e: 'Low load/store efficiency means each memory request pulls cache-line sectors that are only partly used — the signature of strided or misaligned (uncoalesced) access. Fixing the access pattern raises efficiency toward 100%.',
      ref: 'Nsight Compute (Memory Workload Analysis)',
      tags: ['coalescing', 'efficiency', 'nsight-compute'],
    },
    {
      d: 4,
      q: 'A high "MIO Throttle" or "LG Throttle" stall reason in Nsight Compute means warps are stalled on…',
      o: [
        'Tensor cores',
        'The memory input/output (load-store/shared) pipeline being saturated — too many in-flight memory instructions',
        'Branch mispredictions',
        'The host',
      ],
      a: 1,
      x: [
        'FP64 units',
        'The instruction cache',
        'Register bank conflicts',
      ],
      e: 'MIO/LG throttle stalls indicate the load-store unit / shared-memory pipe is the bottleneck (queue full). Remedies: reduce memory instruction pressure (vectorize, reuse, fewer shared ops) or rebalance toward compute.',
      ref: 'Nsight Compute (Warp State / Stall Reasons)',
      tags: ['stall-reasons', 'lsu', 'nsight-compute'],
    },
    {
      d: 3,
      q: '"Branch efficiency" (or warp execution efficiency) measures…',
      o: [
        'How often branches are predicted correctly',
        'The average fraction of threads in a warp that are active (not masked off by divergence)',
        'The number of branches per kernel',
        'Cache hit rate',
      ],
      a: 1,
      x: [
        'The number of warps per SM',
        'Instructions per cycle',
        'Memory throughput',
      ],
      e: 'Warp execution efficiency = active lanes / 32 averaged over executed instructions. Low values reveal divergence wasting execution slots. (GPUs don’t do CPU-style branch prediction; the cost is divergence, not misprediction.)',
      ref: 'Nsight Compute (Instruction Statistics)',
      tags: ['divergence', 'warp-efficiency', 'metrics'],
    },
    {
      d: 4,
      q: 'On the Nsight Compute roofline chart, a kernel’s dot is plotted using which two axes?',
      o: [
        'Occupancy vs. time',
        'Arithmetic intensity (FLOP/byte) on x, achieved performance (FLOP/s) on y, against the compute and bandwidth ceilings',
        'Registers vs. shared memory',
        'Block size vs. grid size',
      ],
      a: 1,
      x: [
        'Warps vs. blocks',
        'L1 vs. L2 hit rate',
        'Power vs. clock',
      ],
      e: 'The roofline places intensity on x and achieved FLOP/s on y. A dot under the sloped (bandwidth) roof is memory-bound; under the flat (compute) roof is compute-bound. Distance to the roof shows remaining headroom.',
      ref: 'Nsight Compute Roofline; Williams et al.',
      tags: ['roofline', 'arithmetic-intensity', 'nsight-compute'],
    },
    {
      d: 2,
      q: 'When should you reach for Nsight Systems rather than Nsight Compute?',
      o: [
        'To inspect a single kernel’s memory metrics',
        'To see the whole-application timeline — CPU/GPU overlap, stream concurrency, gaps, and where time goes across kernels and copies',
        'To count bank conflicts',
        'To view SASS',
      ],
      a: 1,
      x: [
        'To read the roofline',
        'To measure register usage',
        'To analyze warp stalls',
      ],
      e: 'Use Nsight Systems first for the big picture (is the GPU even busy? is the CPU the bottleneck? are copies overlapping?), then drill into a specific hot kernel with Nsight Compute.',
      ref: 'Nsight Systems vs. Nsight Compute workflow',
      tags: ['nsight-systems', 'workflow', 'timeline'],
    },
    {
      d: 4,
      q: 'Nsight Compute offers application replay vs. kernel replay. Kernel replay…',
      o: [
        'Re-runs the whole application',
        'Re-executes the same kernel multiple times (restoring its memory state) to collect all counters in one process run',
        'Skips profiling',
        'Runs on the CPU',
      ],
      a: 1,
      x: [
        'Replays only the first launch',
        'Disables the cache',
        'Profiles the host code',
      ],
      e: 'Kernel replay transparently reruns each kernel enough times to gather every requested metric, saving/restoring buffers between passes. Application replay instead re-runs the whole app per pass — needed when state can’t be restored.',
      ref: 'Nsight Compute (Kernel/Application Replay)',
      tags: ['nsight-compute', 'replay', 'metrics'],
    },
    {
      d: 3,
      q: 'You profile with ncu and it is extremely slow on a kernel launched thousands of times. A good mitigation is…',
      o: [
        'Disable the GPU',
        'Use --launch-skip and --launch-count (or -k regex / -s -c) to profile only a few representative launches',
        'Lower the clock',
        'Profile on the CPU',
      ],
      a: 1,
      x: [
        'Increase the block size',
        'Remove all kernels but one',
        'Use printf instead',
      ],
      e: 'Detailed profiling replays each launch, so profiling every iteration is wasteful. Restrict to specific kernels and a handful of launches (--launch-skip N --launch-count M) to get representative data quickly.',
      ref: 'Nsight Compute CLI (launch filtering)',
      tags: ['nsight-compute', 'cli', 'filtering'],
    },
    {
      d: 3,
      q: 'cudaProfilerStart() / cudaProfilerStop() around a region let you…',
      o: [
        'Speed up that region',
        'Limit profiler data collection to a chosen region (e.g. steady-state iterations), skipping warm-up',
        'Disable error checking',
        'Synchronize streams',
      ],
      a: 1,
      x: [
        'Reduce register usage',
        'Force coalescing',
        'Allocate profiling memory',
      ],
      e: 'Wrapping the region of interest with cudaProfilerStart/Stop (and running the profiler in "start disabled" mode) focuses capture on representative iterations, avoiding initialization noise and huge trace files.',
      ref: 'CUDA Profiler Control; Nsight Systems',
      tags: ['profiler-control', 'region', 'workflow'],
    },
    {
      d: 3,
      q: 'A low "achieved occupancy" but high "theoretical occupancy" together with short kernel duration often points to…',
      o: [
        'Register spilling',
        'The tail effect / too few blocks to fill the GPU for the kernel’s short lifetime (ramp-up + drain dominate)',
        'Bank conflicts',
        'Tensor-core misuse',
      ],
      a: 1,
      x: [
        'A memory leak',
        'Excess shared memory',
        'Host bottleneck',
      ],
      e: 'When resources allow high occupancy but the measured value is low for a brief kernel, the cause is usually insufficient/imbalanced blocks so the SMs never fully fill — the tail effect. Increase grid size or fuse work.',
      ref: 'Nsight Compute (Occupancy); tail effect',
      tags: ['occupancy', 'tail-effect', 'analysis'],
    },
    {
      d: 4,
      q: 'In the Nsight Compute memory chart, a low L2 hit rate on a kernel that revisits the same data suggests…',
      o: [
        'Perfect caching',
        'The working set exceeds L2 (or poor temporal locality), so reused data is evicted before reuse — consider tiling or L2 residency control',
        'Too many registers',
        'A divergent branch',
      ],
      a: 1,
      x: [
        'Bank conflicts',
        'Tensor-core starvation',
        'Host-side stalls',
      ],
      e: 'Reuse that misses in L2 means the data is evicted between accesses (working set too large or scattered). Tiling to fit L2, restructuring access order, or using the L2 persisting window can raise the hit rate.',
      ref: 'Nsight Compute (Memory Workload Analysis)',
      tags: ['l2-cache', 'hit-rate', 'locality'],
    },
    {
      d: 2,
      q: 'Pipe utilization metrics (FMA, ALU, LSU, Tensor) tell you…',
      o: [
        'The clock frequency',
        'Which functional units are busiest, helping identify whether you are bound on math, memory (LSU), or tensor cores',
        'The number of blocks',
        'Register count',
      ],
      a: 1,
      x: [
        'The cache size',
        'The PCIe bandwidth',
        'The number of streams',
      ],
      e: 'Per-pipe utilization shows where the work concentrates. A maxed FMA pipe → FP32 compute-bound; maxed LSU → memory-instruction-bound; low Tensor pipe on a GEMM → tensor cores are starved. This guides the optimization target.',
      ref: 'Nsight Compute (Compute Workload Analysis)',
      tags: ['pipe-utilization', 'bottleneck', 'nsight-compute'],
    },
    {
      d: 3,
      q: 'nvidia-smi reports a training job at 30% "GPU-Util." What does that number actually mean?',
      o: [
        'The fraction of CUDA cores doing math',
        'The fraction of recent time at least one kernel was running on the GPU — NOT how efficiently the SMs are used',
        'The memory bandwidth used',
        'The achieved occupancy',
      ],
      a: 1,
      x: [
        'The tensor-core utilization',
        'The percentage of memory full',
        'The power draw fraction',
      ],
      e: 'GPU-Util is a coarse "was a kernel active" duty-cycle, not SM/compute efficiency. A kernel can show 100% util while using few SMs poorly. Use Nsight (SM throughput, SOL) for real efficiency.',
      ref: 'nvidia-smi documentation; common misconception',
      tags: ['nvidia-smi', 'gpu-util', 'misconception'],
    },
    {
      d: 4,
      q: 'High "Stall Barrier" warp state in Nsight Compute indicates warps waiting at…',
      o: [
        'Memory loads',
        '__syncthreads()/barriers — threads idling for the rest of the block to arrive (load imbalance or too-frequent syncs)',
        'Tensor cores',
        'The host',
      ],
      a: 1,
      x: [
        'Atomic operations',
        'Branch divergence only',
        'Register allocation',
      ],
      e: 'Barrier stalls mean significant time is spent waiting at block barriers. Causes: imbalanced work before the barrier, or more barriers than necessary. Reduce sync frequency or balance per-thread work before each barrier.',
      ref: 'Nsight Compute (Warp State Statistics)',
      tags: ['stall-reasons', 'barrier', 'syncthreads'],
    },
    {
      d: 3,
      q: 'To correlate a hot SASS instruction back to your CUDA C++ source line in Nsight Compute, you should compile with…',
      o: [
        '-O0',
        '-lineinfo (and ideally -G only for debugging), which embeds source-line mapping without disabling optimizations',
        '--use_fast_math',
        '-arch=native',
      ],
      a: 1,
      x: [
        '-g3',
        '--ptxas-options=-v',
        '-rdc=true',
      ],
      e: '-lineinfo adds line tables so the profiler maps SASS/metrics to source lines while keeping optimizations on. -G (full device debug) disables optimizations and is for cuda-gdb, not performance analysis.',
      ref: 'NVCC -lineinfo; Nsight Compute source view',
      tags: ['lineinfo', 'sass-source', 'nsight-compute'],
    },
    {
      d: 2,
      q: 'In a Nsight Systems timeline, long gaps between GPU kernels with the CPU busy usually mean…',
      o: [
        'The GPU is the bottleneck',
        'The application is CPU-bound or launch-bound — the host can’t feed the GPU fast enough',
        'Memory bandwidth is saturated',
        'Occupancy is too high',
      ],
      a: 1,
      x: [
        'Tensor cores are idle by design',
        'The kernels are too large',
        'ECC is enabled',
      ],
      e: 'GPU idle while the CPU works indicates a host-side bottleneck (data prep, Python overhead, too many small launches). Fixes: reduce launch count (graphs/fusion), move work to the GPU, or overlap with streams.',
      ref: 'Nsight Systems (timeline analysis)',
      tags: ['nsight-systems', 'cpu-bound', 'timeline'],
    },
    {
      d: 4,
      q: 'Local memory load/store traffic showing up in Nsight Compute for a kernel that has no large local arrays usually means…',
      o: [
        'A profiler bug',
        'Register spilling — the compiler spilled registers to local memory',
        'Shared-memory bank conflicts',
        'Constant-memory misses',
      ],
      a: 1,
      x: [
        'Texture-cache activity',
        'Atomic contention',
        'Coalesced global access',
      ],
      e: 'Unexpected local memory traffic is the classic spill signature. Check ptxas -v register counts, then reduce register pressure (smaller tiles, -maxrregcount, refactor) to eliminate the spills.',
      ref: 'Nsight Compute; register spilling',
      tags: ['register-spilling', 'local-memory', 'nsight-compute'],
    },
    {
      d: 3,
      q: 'Compiling with ptxas-options=-v (or --resource-usage) prints, per kernel…',
      o: [
        'The runtime in milliseconds',
        'Register count, shared memory, and spill (local memory) usage — useful for occupancy/spill analysis at build time',
        'The achieved occupancy',
        'The number of SMs',
      ],
      a: 1,
      x: [
        'The DRAM bandwidth',
        'The grid dimensions',
        'The CUDA driver version',
      ],
      e: 'ptxas -v reports per-kernel registers, smem bytes, and "spill stores/loads." Watching these at compile time catches occupancy limiters and spills before you even profile.',
      ref: 'NVCC --ptxas-options=-v / --resource-usage',
      tags: ['ptxas', 'registers', 'resource-usage'],
    },
    {
      d: 5,
      q: 'A GEMM shows 80% Tensor pipe utilization but only 50% of peak FLOP/s. A likely explanation is…',
      o: [
        'The kernel is memory-bound',
        'Tensor cores are issuing often but stalling between MMAs (e.g. waiting on operands / barriers), so effective throughput trails the issue rate',
        'The GPU is broken',
        'Occupancy is too high',
      ],
      a: 1,
      x: [
        'It uses FP64',
        'Branch divergence',
        'Too few registers',
      ],
      e: 'Issue/utilization counts when the pipe is fed, but bubbles between MMAs (operand latency, barrier waits, imperfect pipelining) lower achieved FLOP/s. Deeper pipelining/warp specialization and better SMEM feeding close the gap.',
      ref: 'Nsight Compute (Tensor pipe); CUTLASS perf analysis',
      tags: ['tensor-core', 'utilization', 'gemm', 'pipelining'],
    },
    {
      d: 2,
      q: 'IPC (instructions per cycle) reported low while occupancy is high typically means…',
      o: [
        'The kernel is optimal',
        'Warps are frequently stalled (memory/dependency/barrier) so few issue each cycle, despite many being resident',
        'Too few warps',
        'Tensor cores are saturated',
      ],
      a: 1,
      x: [
        'Registers are spilling',
        'The grid is too large',
        'The block size is wrong',
      ],
      e: 'High occupancy but low IPC means residency isn’t the problem — stalls are. The Warp State / stall-reason section tells you what they’re waiting on (memory, barriers, math throttle) so you can target the real limiter.',
      ref: 'Nsight Compute (Scheduler / Warp State)',
      tags: ['ipc', 'stalls', 'occupancy'],
    },
    {
      d: 3,
      q: 'Shared-memory bank-conflict reporting in Nsight Compute appears as…',
      o: [
        'Global load efficiency',
        'Increased shared (l1tex) wavefronts / "bank conflicts" per request — more transactions than the ideal one per warp access',
        'Low occupancy',
        'High register count',
      ],
      a: 1,
      x: [
        'DRAM throughput',
        'Branch efficiency',
        'Tensor utilization',
      ],
      e: 'Conflicts show as extra shared-memory wavefronts/transactions per instruction. The fix is padding or relayout/swizzling so a warp’s accesses span distinct banks, dropping the transaction count back toward one.',
      ref: 'Nsight Compute (Memory tables, shared bank conflicts)',
      tags: ['bank-conflict', 'shared-memory', 'nsight-compute'],
    },
    {
      d: 4,
      q: 'Why might profiling change a kernel’s timing (the "observer effect"), and how does Nsight mitigate it for metrics?',
      o: [
        'It does not change timing',
        'Counter collection and replay perturb timing; Nsight reports metrics from controlled replays and flags durations separately rather than trusting wall-time during collection',
        'It overclocks the GPU',
        'It runs the kernel on the CPU',
      ],
      a: 1,
      x: [
        'It disables the kernel',
        'It uses the host timer only',
        'It compresses the data',
      ],
      e: 'Detailed counter capture and kernel replay add overhead, so wall-clock during profiling isn’t representative. Nsight gathers hardware counters under controlled conditions and presents derived metrics; for clean timing use a separate light pass or events.',
      ref: 'Nsight Compute (profiling overhead); CUPTI',
      tags: ['observer-effect', 'profiling-overhead', 'metrics'],
    },
    {
      d: 3,
      q: 'nsys profile by default captures CUDA API calls and GPU activity. To also see your application’s phases on the timeline you add…',
      o: [
        'cudaDeviceSynchronize everywhere',
        'NVTX ranges (nvtxRangePush/Pop) around code regions',
        'printf statements',
        'More streams',
      ],
      a: 1,
      x: [
        '-lineinfo',
        'cudaProfilerStop',
        'Atomic counters',
      ],
      e: 'NVTX annotations appear as labeled ranges on the Nsight Systems timeline, letting you correlate GPU/CPU activity with logical phases (data load, forward, backward) of your program.',
      ref: 'NVTX; Nsight Systems',
      tags: ['nvtx', 'nsight-systems', 'annotation'],
    },
    {
      d: 4,
      q: 'A kernel’s SM throughput and memory throughput are BOTH low (e.g. 20%). This most likely means it is…',
      o: [
        'Perfectly balanced',
        'Latency-bound — not enough parallelism/ILP in flight to keep either resource busy',
        'Compute-bound',
        'Memory-bound',
      ],
      a: 1,
      x: [
        'Tensor-core bound',
        'Host-bound only',
        'Register-spill bound',
      ],
      e: 'When neither compute nor memory is near peak, the kernel is latency-bound: too few eligible warps / too little ILP to hide stalls. Increase occupancy or expose more independent work per thread.',
      ref: 'Nsight Compute (Speed of Light); latency-bound diagnosis',
      tags: ['latency-bound', 'speed-of-light', 'diagnosis'],
    },
    {
      d: 2,
      q: 'CUPTI is…',
      o: [
        'A CUDA math library',
        'The CUDA Profiling Tools Interface — the low-level API that Nsight and other tools use to collect activity and counters',
        'A debugger',
        'A memory allocator',
      ],
      a: 1,
      x: [
        'A linker',
        'A scheduler',
        'A compression library',
      ],
      e: 'CUPTI exposes activity records, callbacks, and hardware counters that profilers (Nsight Systems/Compute) and custom tools build on. You can use it directly to build bespoke profiling/tracing.',
      ref: 'CUPTI documentation',
      tags: ['cupti', 'profiling-api'],
    },
    {
      d: 3,
      q: 'To measure clean end-to-end GPU time for one kernel without profiler overhead, the lightest method is…',
      o: [
        'Run Nsight Compute with all sections',
        'Bracket the kernel with cudaEventRecord(start) / cudaEventRecord(stop), synchronize, and read cudaEventElapsedTime — after a warm-up and averaged over runs',
        'Use the host wall clock around the launch only',
        'Read nvidia-smi',
      ],
      a: 1,
      x: [
        'Use printf timestamps',
        'Count instructions',
        'Use clock64() on the host',
      ],
      e: 'CUDA events timestamp on the GPU timeline, giving accurate device-side durations with minimal overhead. Host wall-clock around an async launch measures launch, not execution, unless you synchronize.',
      ref: 'CUDA Best Practices Guide §8 (Timing)',
      tags: ['events', 'timing', 'benchmarking'],
    },
    {
      d: 5,
      q: 'When comparing two kernels, Nsight Compute can show a "baseline" diff. The most rigorous comparison normalizes by…',
      o: [
        'Lines of code',
        'The same problem size and clocks (and ideally locked clocks), comparing metrics like duration, SOL, and bytes moved rather than just wall time',
        'The number of registers',
        'The file size',
      ],
      a: 1,
      x: [
        'The number of kernels',
        'Occupancy alone',
        'The grid dimensions only',
      ],
      e: 'Fair comparison fixes the workload and clock state (GPUs boost variably) and looks at duration plus SOL/memory metrics. Locking clocks (nvidia-smi -lgc) removes boost variability so differences reflect the code, not the clock.',
      ref: 'Nsight Compute (Baselines); clock locking',
      tags: ['comparison', 'clocks', 'methodology'],
    },
  ],
  { idPrefix: 'profiling-b' }
)
