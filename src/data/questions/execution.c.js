import { defineQuestions } from './_helpers.js'

// Execution & Scheduling — batch C. Shuffle patterns, match, cooperative-group
// ranks, allocation granularity, memory divergence. Distinct from execution.js/.b.js.
export default defineQuestions(
  'execution',
  [
    {
      d: 4,
      q: '__match_any_sync(mask, value) returns…',
      o: [
        'The sum of values in the warp',
        'Mask of lanes with the same value',
        'The maximum value',
        'A single boolean',
      ],
      a: 1,
      x: [
        'The lane id of the match',
        'The number of distinct values',
        'A pointer to matching data',
      ],
      e: '__match_any_sync (Volta+) partitions a warp by value: each lane gets the mask of peers sharing its value. It accelerates warp-level grouping, e.g. aggregating updates to the same key/bin before a single atomic.',
      ref: 'CUDA C++ Programming Guide §B.22 (match functions)',
      tags: ['match', 'warp', 'aggregation'],
    },
    {
      d: 3,
      q: 'A butterfly reduction within a warp uses which shuffle variant?',
      o: [
        '__shfl_up_sync',
        '__shfl_xor_sync',
        '__shfl_idx_sync',
        '__ballot_sync',
      ],
      a: 1,
      x: [
        '__shfl_down_sync only',
        '__activemask',
        '__popc',
      ],
      e: 'shfl_xor with offsets 16,8,4,2,1 implements a butterfly so every lane ends up with the full reduction (all-reduce within the warp). shfl_down gives the result only in lane 0; choose based on whether all lanes need the result.',
      ref: 'CUDA warp shuffle; reduction patterns',
      tags: ['shuffle', 'butterfly', 'reduction', 'warp'],
    },
    {
      d: 4,
      q: '__shfl_up_sync is the natural primitive for implementing…',
      o: [
        'A reduction to lane 0',
        'An inclusive warp scan (prefix sum)',
        'A broadcast from lane 31',
        'Atomic increments',
      ],
      a: 1,
      x: [
        'A histogram',
        'A transpose',
        'A sort',
      ],
      e: 'A warp scan shifts values up by 1,2,4,...,16 lanes (shfl_up) and accumulates, producing prefix sums in log2(32)=5 steps without shared memory. CUB’s WarpScan wraps this efficiently.',
      ref: 'CUDA warp shuffle; scan',
      tags: ['shuffle', 'scan', 'prefix-sum'],
    },
    {
      d: 4,
      q: 'The intrinsics __isGlobal(ptr) / __isShared(ptr) let device code…',
      o: [
        'Allocate memory',
        'Query which memory space a pointer is in',
        'Convert pointers',
        'Free memory',
      ],
      a: 1,
      x: [
        'Pin memory',
        'Check alignment',
        'Time accesses',
      ],
      e: 'Generic pointers can address global/shared/local; these predicates report the underlying state space at runtime, used by libraries (and the compiler) to pick the right load/store path. There are also __isConstant/__isLocal.',
      ref: 'CUDA C++ Programming Guide (pointer space predicates)',
      tags: ['pointer-space', 'generic-pointers', 'intrinsics'],
    },
    {
      d: 4,
      q: 'Registers are allocated to warps in fixed-size chunks (allocation granularity). A consequence is that…',
      o: [
        'Register count never affects occupancy',
        'A few extra registers can drop occupancy in steps',
        'Registers are unlimited',
        'Occupancy is continuous',
      ],
      a: 1,
      x: [
        'Registers are shared across blocks',
        'Granularity only matters for shared memory',
        'It improves coalescing',
      ],
      e: 'Because registers are allocated in granular chunks per warp, occupancy changes in discrete steps as register usage crosses thresholds. The occupancy calculator accounts for this granularity; one extra register can sometimes cost a whole resident block.',
      ref: 'CUDA Occupancy Calculator; allocation granularity',
      tags: ['registers', 'granularity', 'occupancy'],
    },
    {
      d: 3,
      q: 'Control divergence vs. memory divergence: "memory divergence" within a warp refers to…',
      o: [
        'Threads taking different branches',
        'Scattered addresses force multiple transactions',
        'Threads using different registers',
        'Warps on different SMs',
      ],
      a: 1,
      x: [
        'A barrier mismatch',
        'Two warps diverging',
        'A bank conflict in registers',
      ],
      e: 'Control divergence is branch-path serialization; memory divergence is when one load/store instruction’s lanes hit many cache lines, generating multiple memory transactions (instruction replays). Both reduce efficiency; both are reported by profilers.',
      ref: 'Nsight Compute; divergence types',
      tags: ['memory-divergence', 'coalescing', 'replays'],
    },
    {
      d: 4,
      q: 'cooperative_groups::this_grid() returns a grid_group whose .sync() requires…',
      o: [
        'Nothing special',
        'Cooperative launch, all blocks co-resident',
        'Dynamic parallelism',
        'Two GPUs',
      ],
      a: 1,
      x: [
        'A separate kernel',
        'Unified memory',
        'A high-priority stream',
      ],
      e: 'Grid-wide synchronization needs every block concurrently resident, so you must use cooperative launch and size the grid to the device’s capacity. Otherwise grid.sync() would deadlock waiting for blocks that haven’t started.',
      ref: 'CUDA C++ Programming Guide §C (grid_group)',
      tags: ['cooperative-groups', 'grid-sync', 'cooperative-launch'],
    },
    {
      d: 4,
      q: 'Within a tiled_partition tile, thread_rank() gives…',
      o: [
        'The global thread index',
        'Its index within the tile (0..size-1)',
        'The block index',
        'The warp id',
      ],
      a: 1,
      x: [
        'The SM id',
        'The lane id in the full warp always',
        'The number of tiles',
      ],
      e: 'thread_rank() is the calling thread’s position inside its cooperative group (here the tile). meta_group_rank()/meta_group_size() describe which tile this is among the partitioned tiles of the parent group.',
      ref: 'CUDA C++ Programming Guide §C (Cooperative Groups)',
      tags: ['cooperative-groups', 'thread-rank', 'tile'],
    },
    {
      d: 3,
      q: 'A block of 64 threads on an SM that allows up to 32 resident blocks and 2048 resident threads is limited by…',
      o: [
        'Threads (2048 / 64 = 32 blocks, exactly the block cap)',
        '32 blocks × 64 = 2048: full thread occupancy',
        'Shared memory always',
        'Registers always',
      ],
      a: 1,
      x: [
        'Nothing limits it',
        'The warp size',
        'The grid size',
      ],
      e: 'With 64-thread blocks you’d need 32 blocks to reach 2048 threads, exactly the typical max-blocks-per-SM. It works here, but smaller blocks risk hitting the block-count cap BEFORE filling the thread budget — a reason to prefer ≥128-thread blocks.',
      ref: 'CUDA Occupancy Calculator; block-count limits',
      tags: ['occupancy', 'block-size', 'limits'],
    },
    {
      d: 2,
      q: 'Divergence is a performance concern only WITHIN a warp, not across warps, because…',
      o: [
        'Warps share a program counter',
        'Warps are scheduled independently',
        'Warps always run in lockstep with each other',
        'Across-warp divergence is illegal',
      ],
      a: 1,
      x: [
        'Warps cannot branch',
        'All warps in a block must match',
        'Divergence is free everywhere',
      ],
      e: 'Each warp is an independent scheduling unit; warps naturally execute different instructions. The SIMT penalty arises only when lanes inside ONE warp take different paths, forcing serialization with masking.',
      ref: 'CUDA C++ Programming Guide §4; PMPP Ch. 4',
      tags: ['divergence', 'warp', 'scheduling'],
    },
    {
      d: 4,
      q: 'cooperative_groups::reduce(tile, val, cg::plus<T>()) on a 32-thread tile is convenient because it…',
      o: [
        'Uses global atomics',
        'Wraps the optimal warp reduction',
        'Runs on the host',
        'Requires shared memory',
      ],
      a: 1,
      x: [
        'Only works for sums',
        'Needs a cooperative launch',
        'Is slower than a loop',
      ],
      e: 'cg::reduce provides a portable, optimized group reduction (lowering to shfl_xor or hardware redux.sync where available), saving you from writing and tuning the warp-shuffle reduction yourself.',
      ref: 'CUDA C++ Programming Guide §C (cg::reduce)',
      tags: ['cooperative-groups', 'reduce', 'warp'],
    },
    {
      d: 3,
      q: 'How many warps does a block of 96 threads occupy, and how many lanes are inactive?',
      o: [
        '3 warps, 0 inactive',
        '3 warps, 0 inactive (96 / 32 = exactly 3)',
        '4 warps, 32 inactive',
        '2 warps, 32 inactive',
      ],
      a: 1,
      x: [
        '3 warps, 16 inactive',
        '6 warps, 0 inactive',
        '1 warp',
      ],
      e: '96 is a multiple of 32, so it forms exactly 3 full warps with no inactive lanes. A block of, say, 100 threads would form 4 warps with 28 inactive lanes in the last warp — wasting scheduling slots.',
      ref: 'CUDA C++ Programming Guide §4.1',
      tags: ['warps', 'block-size', 'occupancy'],
    },
    {
      d: 5,
      q: 'The warp scheduler’s "greedy-then-oldest" (GTO) policy tends to…',
      o: [
        'Round-robin every warp equally',
        'Issue one warp until it stalls, then the oldest',
        'Pick the newest warp',
        'Issue randomly',
      ],
      a: 1,
      x: [
        'Always pick the lowest lane',
        'Prefer tensor-core warps',
        'Issue two warps per cycle always',
      ],
      e: 'GTO favors the current warp (greedy) for locality until it stalls, then the oldest eligible warp. The scheduling policy interacts with cache behavior and is one reason measured performance can differ from simple occupancy models.',
      ref: 'GPU microarchitecture research (warp scheduling policies)',
      tags: ['warp-scheduler', 'gto', 'policy'],
    },
    {
      d: 4,
      q: 'cooperative_groups::binary_partition(tile, predicate) splits a group into…',
      o: [
        'Two halves by lane index',
        'Two subgroups by predicate true/false',
        'Even and odd lanes',
        'Two blocks',
      ],
      a: 1,
      x: [
        'A warp and a block',
        'Active and inactive lanes only',
        'Two grids',
      ],
      e: 'binary_partition (and the more general labeled_partition) groups lanes by a predicate/label so each subgroup can do collectives among its members — handy after a divergent decision to operate per-branch warp-cooperatively.',
      ref: 'CUDA C++ Programming Guide §C (partitioning)',
      tags: ['cooperative-groups', 'partition', 'divergence'],
    },
    {
      d: 3,
      q: 'Shared memory is also allocated to blocks at a granularity, so requesting slightly more shared memory can…',
      o: [
        'Have no effect',
        'Drop resident blocks per SM in a step',
        'Increase registers',
        'Improve coalescing',
      ],
      a: 1,
      x: [
        'Always increase occupancy',
        'Disable the L1',
        'Add more warps',
      ],
      e: 'Like registers, shared memory is allocated per block in granular amounts; nudging past a threshold reduces how many blocks fit per SM. Sizing tiles with the occupancy calculator avoids accidental occupancy cliffs.',
      ref: 'CUDA Occupancy Calculator; shared-memory granularity',
      tags: ['shared-memory', 'granularity', 'occupancy'],
    },
    {
      d: 4,
      q: 'An uncoalesced load shows up in Nsight as instruction "replays." A replay means…',
      o: [
        'The kernel restarts',
        'One instruction reissued per transaction',
        'A warp diverged',
        'The scheduler stalled',
      ],
      a: 1,
      x: [
        'The data is recomputed',
        'A barrier is re-executed',
        'The block is rescheduled',
      ],
      e: 'When a warp’s memory access spans more lines than one transaction can serve, the instruction replays for each additional transaction, inflating instruction count and lowering throughput. Coalescing/vectorizing reduces replays.',
      ref: 'Nsight Compute (instruction replays)',
      tags: ['replays', 'coalescing', 'memory-divergence'],
    },
    {
      d: 3,
      q: 'In a 32-thread tile, tile.shfl(value, srcLane) is equivalent to…',
      o: [
        'A global memory read',
        '__shfl_sync over the tile lanes',
        'An atomic operation',
        'A block barrier',
      ],
      a: 1,
      x: [
        'A broadcast from the host',
        'A reduction',
        'A texture fetch',
      ],
      e: 'Cooperative Groups tiles expose .shfl()/.shfl_down()/.shfl_xor() as portable wrappers over the warp-shuffle intrinsics, scoped to the tile’s lanes with the correct mask handled for you.',
      ref: 'CUDA C++ Programming Guide §C (tile shuffle)',
      tags: ['cooperative-groups', 'shuffle', 'tile'],
    },
    {
      d: 5,
      q: 'Why can two kernels that each report 50% occupancy still differ greatly in performance?',
      o: [
        'Occupancy fully determines speed',
        'Occupancy only bounds latency hiding, not speed',
        'One uses more blocks',
        'They run on different SMs',
      ],
      a: 1,
      x: [
        'Occupancy is measured wrong',
        'They use different grids only',
        'One is compiled with -O3',
      ],
      e: 'Occupancy is one input to latency hiding, not a performance predictor. A coalesced, high-ILP kernel at 50% can vastly outperform a divergent, uncoalesced one at the same occupancy. Always profile the actual limiter.',
      ref: 'Volkov; CUDA Best Practices Guide',
      tags: ['occupancy', 'performance', 'analysis'],
    },
    {
      d: 4,
      q: 'meta_group_rank() on a tiled_partition<8>(block) of a 256-thread block returns…',
      o: [
        'The lane within the tile',
        'Which tile this is (0..31 of 32)',
        'The block index',
        'The number of tiles',
      ],
      a: 1,
      x: [
        'The thread’s global rank',
        'The warp id',
        'Always 0',
      ],
      e: 'A 256-thread block split into 8-thread tiles has 32 tiles; meta_group_rank() identifies which tile the calling thread belongs to, while thread_rank() gives its position (0..7) inside that tile.',
      ref: 'CUDA C++ Programming Guide §C (meta_group)',
      tags: ['cooperative-groups', 'meta-group', 'tile'],
    },
    {
      d: 3,
      q: 'Warps from one block are distributed across the SM’s sub-partitions so that…',
      o: [
        'All warps go to one scheduler',
        'Warps spread across the 4 sub-partitions',
        'Each warp gets its own SM',
        'Warps share a single register',
      ],
      a: 1,
      x: [
        'Warps run on the host',
        'Only even warps are scheduled',
        'Warps merge into one',
      ],
      e: 'A block’s warps are assigned across the SM’s 4 sub-partitions (each with a scheduler and register-file slice), so several warps can issue concurrently. This is why having a few warps per scheduler aids throughput.',
      ref: 'NVIDIA SM microarchitecture',
      tags: ['sub-partition', 'warp-assignment', 'scheduling'],
    },
    {
      d: 4,
      q: 'CUB BlockScan / BlockReduce operate at block scope by…',
      o: [
        'Using global atomics',
        'Combining per-thread values block-wide',
        'Running one thread',
        'Calling the host',
      ],
      a: 1,
      x: [
        'Using constant memory',
        'Spawning child kernels',
        'Using texture memory',
      ],
      e: 'CUB’s block-scope primitives let all threads of a block contribute elements and get back the block-wide scan/reduction efficiently, handling the shared-memory staging and warp-shuffle steps internally — a building block inside custom kernels.',
      ref: 'CUB documentation (BlockScan/BlockReduce)',
      tags: ['cub', 'block-scope', 'scan-reduce'],
    },
    {
      d: 2,
      q: 'A kernel with a two-way branch where exactly half of each warp takes each side runs the divergent region at roughly…',
      o: [
        'Full speed',
        'Half efficiency: paths run serially',
        'Double speed',
        'Quarter speed',
      ],
      a: 1,
      x: [
        'Zero speed',
        'The same as no divergence',
        'It deadlocks',
      ],
      e: 'When a warp splits evenly, the hardware runs path A (16 lanes active), then path B (the other 16), so the divergent code executes at about 50% lane utilization. Minimizing or restructuring such branches recovers efficiency.',
      ref: 'PMPP Ch. 4; divergence cost',
      tags: ['divergence', 'efficiency', 'branching'],
    },
    {
      d: 5,
      q: 'Persistent-kernel grid sizing typically launches exactly cooperativeMaxActiveBlocks × SM_count blocks because…',
      o: [
        'More blocks are faster',
        'Every block co-resident, no oversubscription',
        'It minimizes registers',
        'It is required by the compiler',
      ],
      a: 1,
      x: [
        'It maximizes the grid size',
        'It avoids shared memory',
        'It enables tensor cores',
      ],
      e: 'A persistent/cooperative kernel sizes the grid to fill the device exactly once (occupancy × SMs), so all blocks run simultaneously and loop over work. Launching more blocks than fit would serialize waves and break grid-wide synchronization.',
      ref: 'Persistent threads; cooperative launch',
      tags: ['persistent-kernel', 'grid-sizing', 'cooperative-launch'],
    },
    {
      d: 4,
      q: 'Two independent global loads issued before either result is used (manual prefetch / batched loads) help because…',
      o: [
        'They reduce memory traffic',
        'Their latencies overlap (memory-level parallelism)',
        'They coalesce automatically',
        'They use tensor cores',
      ],
      a: 1,
      x: [
        'They avoid the L2',
        'They reduce register usage',
        'They eliminate divergence',
      ],
      e: 'Issuing several independent loads back-to-back exposes memory-level parallelism: the loads are in flight together and their latencies overlap, hiding more latency than dependent, one-at-a-time loads. This complements occupancy-based hiding.',
      ref: 'CUDA Best Practices Guide; memory-level parallelism',
      tags: ['mlp', 'prefetch', 'latency-hiding', 'ilp'],
    },
    {
      d: 3,
      q: '__shfl_sync requires a mask argument that must…',
      o: [
        'Always be 0',
        'Name participating lanes; all must execute',
        'Be the lane id',
        'Be the block size',
      ],
      a: 1,
      x: [
        'Be a pointer',
        'Equal the warp id',
        'Be computed by the hardware',
      ],
      e: 'The mask specifies which lanes take part; every participating lane must call the shuffle with a consistent mask, or behavior is undefined (a requirement of independent thread scheduling). Use __activemask() when the active set is dynamic.',
      ref: 'CUDA C++ Programming Guide §B.22',
      tags: ['shuffle', 'mask', 'warp'],
    },
    {
      d: 5,
      q: 'cooperative_groups::multi_grid_group (this_multi_grid) was designed to synchronize…',
      o: [
        'Threads in one block',
        'Cooperative kernels across multiple GPUs',
        'Two warps',
        'Host and device',
      ],
      a: 1,
      x: [
        'All blocks in one grid only',
        'Streams on one GPU',
        'CPU threads',
      ],
      e: 'multi_grid_group enabled grid-wide sync across several GPUs via cudaLaunchCooperativeKernelMultiDevice. It is more specialized (and now largely superseded by other multi-GPU patterns), but illustrates the cooperative-groups hierarchy up to multi-device.',
      ref: 'CUDA C++ Programming Guide §C (multi_grid_group)',
      tags: ['cooperative-groups', 'multi-grid', 'multi-gpu'],
    },
    {
      d: 4,
      q: 'Why might a kernel limited to 25% occupancy still be optimal?',
      o: [
        'It never is',
        'Register-heavy ILP hides latency at low occupancy',
        'Because the grid is small',
        'Because it uses constant memory',
      ],
      a: 1,
      x: [
        'Because divergence is high',
        'Because shared memory is unused',
        'Because it is memory-bound',
      ],
      e: 'High-ILP, register-heavy kernels (GEMM micro-kernels) deliberately trade occupancy for per-thread reuse and independent work. They can reach peak throughput at low occupancy — the occupancy-vs-ILP trade-off in action.',
      ref: 'Volkov; CUTLASS',
      tags: ['occupancy', 'ilp', 'registers', 'gemm'],
    },
    {
      d: 3,
      q: 'The number of warps in a block is computed as…',
      o: [
        'blockDim.x only',
        'ceil(threads_per_block / 32)',
        'gridDim.x / 32',
        'always 32',
      ],
      a: 1,
      x: [
        'threads_per_block / 64',
        'blockDim.x × blockDim.y',
        'warpSize',
      ],
      e: 'Warps = ceil(blockDim.x*blockDim.y*blockDim.z / 32). A partial final warp still occupies a full warp slot with inactive lanes, which is why block sizes that are multiples of 32 avoid waste.',
      ref: 'CUDA C++ Programming Guide §4.1',
      tags: ['warps', 'block-size', 'counting'],
    },
    {
      d: 5,
      q: 'In a warp-specialized producer/consumer kernel, named barriers (rather than __syncthreads) are needed because…',
      o: [
        '__syncthreads is deprecated',
        'Producers and consumers sync as separate groups',
        'Named barriers are faster',
        'It reduces registers',
      ],
      a: 1,
      x: [
        'Named barriers span blocks',
        'They avoid divergence',
        'They are required for shuffles',
      ],
      e: 'A whole-block __syncthreads would force producers and consumers to rendezvous together, defeating the decoupling. Named barriers (PTX bar.sync id, count) let each warp group synchronize independently across the pipeline stages.',
      ref: 'PTX named barriers; warp specialization',
      tags: ['named-barriers', 'warp-specialization', 'producer-consumer'],
    },
  ],
  { idPrefix: 'execution-c' }
)
