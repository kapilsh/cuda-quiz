import { defineQuestions } from './_helpers.js'

// Synchronization & Atomics — batch D. Half2 atomics, PTX fences, mbarrier init,
// ticket locks, seqlocks, reclamation. Distinct from synchronization.js/.b.js/.c.js.
export default defineQuestions(
  'synchronization',
  [
    {
      d: 4,
      q: 'atomicAdd on __half2 (CC 6.0+) is useful because it…',
      o: [
        'Is more accurate',
        'Atomically adds two packed FP16 values at once (e.g. accumulating into a half2 histogram/accumulator), doubling throughput vs two scalar half atomics',
        'Runs on the host',
        'Avoids contention',
      ],
      a: 1,
      x: [
        'Converts to FP32',
        'Disables tensor cores',
        'Only works in shared memory',
      ],
      e: 'Packed half2 atomics update two adjacent FP16 lanes in one atomic op, helpful for FP16 accumulation patterns. Note FP atomics are still order-dependent (nondeterministic) and contend like any atomic.',
      ref: 'CUDA C++ Programming Guide §B.14 (half2 atomics)',
      tags: ['atomics', 'half2', 'packed'],
    },
    {
      d: 5,
      q: 'cuda::atomic<T>::wait(old) / notify_all (libcu++) provide…',
      o: [
        'A spin loop only',
        'C++20-style atomic wait/notify so a thread can block until an atomic changes from a known value and be woken — building efficient condition-variable-like synchronization on the device',
        'A memory fence',
        'A reduction',
      ],
      a: 1,
      x: [
        'A barrier',
        'A lock',
        'Host signaling',
      ],
      e: 'atomic::wait/notify let threads efficiently wait for an atomic to change (rather than busy-spinning) and wake waiters, enabling condition-variable/flag patterns with backoff. Scope/ordering still apply; use carefully under SIMT.',
      ref: 'libcu++ <cuda/atomic> (wait/notify)',
      tags: ['atomic-wait', 'libcu++', 'notify'],
    },
    {
      d: 5,
      q: 'PTX fence.sc.gpu vs fence.acq_rel.gpu: fence.sc provides…',
      o: [
        'No ordering',
        'Sequential-consistency ordering (stronger, establishes a total order at that scope), whereas acq_rel only orders acquire/release pairs — sc is costlier',
        'Only release ordering',
        'A barrier',
      ],
      a: 1,
      x: [
        'Block-level sync',
        'A reduction',
        'Host visibility',
      ],
      e: 'fence.sc enforces sequential consistency (a single total order of sc operations) at the given scope; fence.acq_rel is the weaker, cheaper acquire-release fence. Most lock-free code only needs acq_rel; sc is reserved for stronger requirements.',
      ref: 'PTX ISA (fence); CUDA memory model',
      tags: ['fence', 'ptx', 'memory-order'],
    },
    {
      d: 4,
      q: 'A "ticket lock" implemented with atomicAdd provides fairness because…',
      o: [
        'It uses no atomics',
        'Each thread atomically takes the next ticket number and waits until a "now serving" counter reaches it — serving requesters in FIFO order rather than arbitrarily',
        'It never blocks',
        'It avoids contention',
      ],
      a: 1,
      x: [
        'It uses CAS only',
        'It is lock-free',
        'It runs on the host',
      ],
      e: 'A ticket lock (next = atomicAdd(&ticket,1); wait until serving==next) grants the lock in arrival order, avoiding the starvation a CAS spinlock can cause. On GPUs, though, lock fairness still battles SIMT/contention issues — lock-free is often preferred.',
      ref: 'Ticket locks; fair mutual exclusion',
      tags: ['ticket-lock', 'fairness', 'atomicAdd'],
    },
    {
      d: 5,
      q: 'A "seqlock" (sequence lock) lets readers proceed without locking by…',
      o: [
        'Using a mutex',
        'Reading a sequence counter before and after; if it’s unchanged and even, the read was consistent (writers increment it odd→even around updates) — readers retry on conflict',
        'Using atomics for every field',
        'Blocking writers',
      ],
      a: 1,
      x: [
        'Using a spinlock',
        'Using a barrier',
        'Copying the data',
      ],
      e: 'Seqlocks favor frequent reads/rare writes: a writer makes the counter odd, updates, then even; a reader checks the counter brackets its read and retries if a write intervened. It needs proper fences so data and counter ordering hold.',
      ref: 'Seqlock; concurrent reading patterns',
      tags: ['seqlock', 'lock-free', 'readers'],
    },
    {
      d: 5,
      q: 'Safely reclaiming memory in a lock-free GPU structure is hard because…',
      o: [
        'Memory is read-only',
        'A node freed by one thread might still be referenced by another in-flight thread; schemes like epoch-based reclamation or hazard pointers are needed to defer frees until no references remain',
        'Atomics are unsupported',
        'Freeing is always safe',
      ],
      a: 1,
      x: [
        'GPUs cannot free memory',
        'It needs a barrier only',
        'It requires FP64',
      ],
      e: 'Without garbage collection, freeing a node that another thread is about to dereference causes use-after-free. Epoch-based reclamation/hazard pointers track when it’s safe to free — complex on GPUs given the thread counts, so many designs avoid frees.',
      ref: 'Lock-free memory reclamation (EBR, hazard pointers)',
      tags: ['reclamation', 'lock-free', 'use-after-free'],
    },
    {
      d: 4,
      q: '__syncthreads() also acts as a COMPILER barrier, meaning the compiler will not…',
      o: [
        'Inline functions',
        'Reorder memory accesses across the barrier (so writes before it aren’t moved after, and vice versa) — preserving the producer/consumer ordering you rely on',
        'Allocate registers',
        'Use shared memory',
      ],
      a: 1,
      x: [
        'Unroll loops',
        'Use tensor cores',
        'Spill registers',
      ],
      e: 'Beyond the hardware barrier, __syncthreads prevents the compiler from reordering memory operations across it, which is essential for tiled producer/consumer correctness — the writes truly complete (and aren’t hoisted past) before the reads.',
      ref: 'CUDA C++ Programming Guide §7.6; compiler barriers',
      tags: ['syncthreads', 'compiler-barrier', 'ordering'],
    },
    {
      d: 4,
      q: 'mbarrier_init(&bar, expected_count) on Hopper initializes a shared-memory barrier with…',
      o: [
        'A lock',
        'The number of threads/arrivals expected for a phase, so arrive/wait operations know when the phase completes',
        'A timeout',
        'A priority',
      ],
      a: 1,
      x: [
        'A reduction',
        'A stream',
        'A texture',
      ],
      e: 'mbarrier_init sets the arrival count for an asynchronous barrier living in shared memory. Threads (and async copies via transaction counts) arrive; waiters block until the phase’s expected arrivals/bytes complete — the core of Hopper pipelines.',
      ref: 'CUDA C++ Programming Guide (mbarrier); PTX',
      tags: ['mbarrier', 'init', 'hopper'],
    },
    {
      d: 4,
      q: 'For a global counter shared across MULTIPLE GPUs via managed/peer memory, increments must use…',
      o: [
        'Device-scope atomicAdd',
        'System-scope atomics (atomicAdd_system / cuda::thread_scope_system) so updates are atomic and visible across GPUs (and the host)',
        'Block-scope atomics',
        'No atomics',
      ],
      a: 1,
      x: [
        'A spinlock',
        '__syncthreads',
        'Shared memory',
      ],
      e: 'Device-scope atomics only coordinate threads on one GPU. Cross-GPU (or GPU↔host) atomic updates to shared memory require system scope to guarantee atomicity and visibility across the whole system.',
      ref: 'CUDA C++ Programming Guide §B.14 (scoped atomics)',
      tags: ['system-scope', 'atomics', 'multi-gpu'],
    },
    {
      d: 4,
      q: 'A memory fence (__threadfence) is generally cheaper than __syncthreads when you need…',
      o: [
        'All threads to wait',
        'Only ORDERING/visibility of one thread’s memory operations (no thread needs to wait for others) — a barrier forces a rendezvous you may not require',
        'A reduction',
        'Grid sync',
      ],
      a: 1,
      x: [
        'A spinlock',
        'Tensor cores',
        'A broadcast',
      ],
      e: 'If correctness needs visibility ordering (e.g. publish data then a flag) but not all threads meeting at a point, a fence suffices and avoids the barrier’s synchronization cost. Use __syncthreads only when threads genuinely must rendezvous.',
      ref: 'CUDA C++ Programming Guide §B.5 vs §7.6',
      tags: ['fence', 'barrier', 'cost'],
    },
    {
      d: 3,
      q: 'Block-level reduction that accumulates into shared memory then does ONE global atomicAdd per block is preferred over per-thread global atomics because…',
      o: [
        'It is less accurate',
        'It collapses thousands of contended global atomics into one per block, drastically reducing L2 atomic contention/serialization',
        'It avoids shared memory',
        'It needs no barrier',
      ],
      a: 1,
      x: [
        'Global atomics are faster',
        'It uses tensor cores',
        'It removes the reduction',
      ],
      e: 'Privatized reduction: combine within the block (shared memory + shuffles), then one global atomic per block. This turns N global atomics into gridDim atomics, avoiding the contention/serialization of every thread hammering one address.',
      ref: 'PMPP Ch. 9–10; reduction patterns',
      tags: ['reduction', 'atomics', 'privatization'],
    },
    {
      d: 5,
      q: 'Why is a naive global spinlock (one lock for the whole grid) almost always a bad idea on a GPU?',
      o: [
        'Locks are illegal',
        'With thousands of threads contending on one lock, throughput collapses to serial, and SIMT/scheduling can deadlock — defeating the GPU’s parallelism',
        'It uses too much memory',
        'It is too fast',
      ],
      a: 1,
      x: [
        'It needs cooperative launch',
        'It disables atomics',
        'It requires FP64',
      ],
      e: 'A single global lock serializes the entire grid (and risks deadlock under SIMT), eliminating parallelism. Use fine-grained/lock-free designs, privatization, or reductions instead — global mutual exclusion is antithetical to GPU execution.',
      ref: 'GPU concurrency; global lock anti-pattern',
      tags: ['spinlock', 'contention', 'anti-pattern'],
    },
    {
      d: 4,
      q: 'Before calling __shfl_sync/__ballot_sync, the named lanes must be CONVERGED (or correctly masked) because…',
      o: [
        'It is faster',
        'These collectives exchange data among the masked lanes; if a named lane isn’t actually executing the instruction (diverged away), the result is undefined',
        'It reduces registers',
        'It avoids the L2',
      ],
      a: 1,
      x: [
        'Masks are ignored',
        'It needs a barrier',
        'It uses shared memory',
      ],
      e: 'Warp collectives require every lane in the mask to participate. Under independent thread scheduling, a lane that diverged and isn’t at the instruction breaks the assumption, giving undefined results — use __activemask()/__syncwarp to ensure participation.',
      ref: 'CUDA C++ Programming Guide §B.22; convergence',
      tags: ['warp-collectives', 'convergence', 'mask'],
    },
    {
      d: 4,
      q: 'A reader-writer scheme where many threads read and few write is best served on a GPU by…',
      o: [
        'A global mutex',
        'Lock-free reads with versioning (seqlock) or epoch schemes, or simply double-buffering so readers and writers touch different buffers — avoiding reader serialization',
        'Per-element spinlocks',
        'Atomics on every read',
      ],
      a: 1,
      x: [
        'A single global lock',
        'No synchronization',
        'Constant memory',
      ],
      e: 'Locks serialize the many readers. Versioned/lock-free reads or double-buffering (writers prepare a new buffer, atomically flip a pointer) let readers proceed in parallel — far better fits for GPU read-heavy workloads.',
      ref: 'Reader-writer patterns on GPUs; double buffering',
      tags: ['reader-writer', 'lock-free', 'double-buffer'],
    },
    {
      d: 3,
      q: 'atomicCAS is called "universal" because…',
      o: [
        'It is the fastest atomic',
        'Any read-modify-write operation can be built from a CAS-retry loop (read old, compute new, CAS; retry on failure) — including atomics not provided in hardware',
        'It works on the host',
        'It needs no memory',
      ],
      a: 1,
      x: [
        'It avoids contention',
        'It is lock-free by definition',
        'It is deterministic',
      ],
      e: 'CAS lets you implement arbitrary atomic updates (custom max/min, packed-field updates, float ops missing native support) via a loop. It’s the foundational primitive — though high contention makes CAS loops retry and serialize.',
      ref: 'CUDA atomics; CAS as universal primitive',
      tags: ['atomicCAS', 'universal', 'rmw'],
    },
    {
      d: 4,
      q: 'Updating two packed 16-bit fields in one 32-bit word atomically (without a per-field lock) is done by…',
      o: [
        'Two atomicAdds',
        'A single atomicCAS loop on the 32-bit word that computes the new packed value from the old — atomic with respect to the whole word',
        'A barrier',
        'A spinlock',
      ],
      a: 1,
      x: [
        'atomicOr only',
        'A reduction',
        'Texture memory',
      ],
      e: 'Packing two shorts in a word and CAS-updating the whole word atomically updates both fields together, avoiding sub-word atomic limitations and inter-field races — a common trick for compact atomic state (e.g. count+flag).',
      ref: 'CUDA atomics; packed CAS updates',
      tags: ['atomicCAS', 'packed', 'sub-word'],
    },
    {
      d: 5,
      q: 'In a multi-stage shared-memory pipeline, why are separate mbarriers per stage (rather than one barrier) important?',
      o: [
        'For speed only',
        'They let producers and consumers operate on DIFFERENT stages concurrently — the producer fills stage k+1 while the consumer drains stage k, tracked by independent barrier phases',
        'They reduce registers',
        'They avoid divergence',
      ],
      a: 1,
      x: [
        'They span blocks',
        'They synchronize the grid',
        'They replace __syncthreads everywhere',
      ],
      e: 'Per-stage barriers decouple the stages so production and consumption overlap (deep pipelining). One global barrier would force lockstep, preventing the overlap that hides memory latency — defeating the pipeline’s purpose.',
      ref: 'CUTLASS pipelining; mbarrier per stage',
      tags: ['mbarrier', 'pipeline', 'multi-stage'],
    },
    {
      d: 3,
      q: 'A correctness rule for __syncthreads in a kernel with an early return is…',
      o: [
        'Early returns are fine',
        'Threads that returned won’t reach later __syncthreads, so any barrier after a divergent return can deadlock — restructure so all threads reach every barrier',
        'Use atomics instead',
        'It only matters with shared memory',
      ],
      a: 1,
      x: [
        'Returns reset the barrier',
        'It is always safe',
        'Barriers ignore returned threads',
      ],
      e: 'If some threads return early (e.g. out-of-bounds) before a __syncthreads the remaining threads hit, the barrier can never be satisfied → hang/UB. Keep barriers in uniform control flow; do bounds handling without skipping shared barriers.',
      ref: 'CUDA C++ Programming Guide §7.6; barrier divergence',
      tags: ['syncthreads', 'early-return', 'deadlock'],
    },
    {
      d: 4,
      q: 'Why does FP atomicAdd make a reduction nondeterministic while integer atomicAdd does not?',
      o: [
        'Integer atomics are slower',
        'Integer addition is associative/exact, so any order gives the same result; FP addition is non-associative, so the scheduling-dependent order changes rounding',
        'FP atomics lose updates',
        'They are equally deterministic',
      ],
      a: 1,
      x: [
        'Integers overflow predictably',
        'FP atomics are not atomic',
        'It depends on the GPU only',
      ],
      e: 'Atomicity is guaranteed for both, but the COMBINATION order varies with scheduling. Integer addition is associative, so order doesn’t matter; FP addition isn’t, so different orders round differently — hence FP atomic sums differ run-to-run.',
      ref: 'Floating-point non-associativity; determinism',
      tags: ['atomics', 'determinism', 'floating-point'],
    },
    {
      d: 3,
      q: 'cuda::atomic_ref<T, scope>(x).fetch_add(v, memory_order_relaxed) is preferable to atomicAdd(&x, v) when you want…',
      o: [
        'Higher throughput always',
        'Explicit control over memory scope and ordering (e.g. block scope, relaxed) for a standard, portable, and potentially cheaper atomic',
        'To avoid atomicity',
        'Host execution',
      ],
      a: 1,
      x: [
        'A barrier',
        'A reduction',
        'Texture access',
      ],
      e: 'atomic_ref exposes scope and memory_order, so you can pick the minimal guarantees (e.g. relaxed, block scope) for performance and portability, versus the legacy atomicAdd whose ordering/scope are fixed/implicit.',
      ref: 'libcu++ atomic_ref; scoped atomics',
      tags: ['atomic-ref', 'scope', 'memory-order'],
    },
    {
      d: 4,
      q: 'A correct grid-wide reduction in a SINGLE kernel (without cooperative launch) uses the "last block" trick: each block writes its partial, __threadfence(), then atomicInc a counter; the block that sees the count == gridDim…',
      o: [
        'Restarts the kernel',
        'Knows all partials are written and visible, so it performs the final reduction over them',
        'Launches a child kernel',
        'Resets the counter only',
      ],
      a: 1,
      x: [
        'Synchronizes the host',
        'Broadcasts the result',
        'Frees memory',
      ],
      e: 'The fence makes each block’s partial globally visible; the atomic counter identifies the last finisher, which then safely reduces all partials — a single-launch grid reduction without grid.sync(). Ordering (fence before the counter) is essential.',
      ref: 'threadFenceReduction sample; PMPP Ch. 10',
      tags: ['reduction', 'threadfence', 'last-block'],
    },
    {
      d: 5,
      q: 'Why might a lock-free algorithm that is correct under sequential consistency be WRONG when ported to CUDA’s relaxed model without adding fences?',
      o: [
        'CUDA has no atomics',
        'CUDA’s weak memory model permits reorderings/delayed visibility that SC forbids, so reasoning assuming SC can break — you must insert the right acquire/release/fences',
        'Atomics are slower',
        'It always works',
      ],
      a: 1,
      x: [
        'CUDA forbids lock-free code',
        'Registers are shared',
        'It needs cooperative launch',
      ],
      e: 'Algorithms designed assuming sequential consistency rely on a global order CUDA doesn’t provide by default. Porting requires explicit ordering (acquire/release atomics, fences) to recover the needed happens-before relationships.',
      ref: 'CUDA memory model; lock-free porting',
      tags: ['memory-model', 'lock-free', 'porting'],
    },
    {
      d: 3,
      q: 'A simple barrier alternative for the LAST warp of a block reduction (warp-synchronous) avoids __syncthreads because…',
      o: [
        'Warps never need sync',
        'Once only one warp remains active, its 32 lanes are within a single warp; warp-level shuffles/__syncwarp suffice instead of a full block barrier',
        'It is incorrect',
        'It uses atomics',
      ],
      a: 1,
      x: [
        'It uses global memory',
        'It needs cooperative launch',
        'It disables divergence',
      ],
      e: 'When the reduction shrinks to ≤32 active threads (one warp), block barriers are unneeded; warp shuffles (with __syncwarp on Volta+) handle the final steps faster, removing __syncthreads overhead — a classic reduction optimization.',
      ref: 'Harris reduction; warp-synchronous tail',
      tags: ['reduction', 'warp-synchronous', 'syncthreads'],
    },
    {
      d: 4,
      q: 'Why is contention (not latency) usually the dominant cost of atomics in a histogram with few bins?',
      o: [
        'Atomics are always slow',
        'Many threads target the SAME few addresses, so updates serialize at those locations; with many distinct bins the same atomics would scale far better',
        'Latency is unbounded',
        'Bins are read-only',
      ],
      a: 1,
      x: [
        'The L2 is disabled',
        'Atomics use FP64',
        'It needs a barrier',
      ],
      e: 'Atomic throughput collapses when many threads hit the same address (serialization). Few bins ⇒ high contention. Privatization (per-block shared histograms) and warp aggregation reduce the conflicting updates per location, restoring throughput.',
      ref: 'PMPP Ch. 9; atomic contention',
      tags: ['atomics', 'contention', 'histogram'],
    },
    {
      d: 5,
      q: 'cuda::pipeline’s consumer_wait() blocks until…',
      o: [
        'The host signals',
        'The async copies for the current stage have completed (their mbarrier phase is satisfied), so the consumer can safely read that stage’s shared-memory data',
        'All stages finish',
        'A kernel launches',
      ],
      a: 1,
      x: [
        'A spinlock releases',
        'The grid syncs',
        'Registers free',
      ],
      e: 'consumer_wait() waits on the stage’s completion (tracked by the pipeline’s barriers/transaction counts) so the data has actually landed before the consumer uses it — then consumer_release() frees the buffer for the producer to refill.',
      ref: 'CUDA C++ Programming Guide (cuda::pipeline)',
      tags: ['pipeline', 'consumer-wait', 'async-copy'],
    },
    {
      d: 3,
      q: 'A producer thread that writes data then does data_ready.store(1, release) ensures the consumer (which does data_ready.load(acquire)) sees the data because…',
      o: [
        'release/acquire is decorative',
        'The release store and acquire load form a synchronizes-with edge: everything the producer wrote before the release is visible to the consumer after the acquire',
        'Both use relaxed',
        'A barrier is implied',
      ],
      a: 1,
      x: [
        'volatile guarantees it',
        'The compiler orders it',
        'It needs seq_cst',
      ],
      e: 'Release/acquire establishes happens-before: the consumer’s acquire load of the flag, once it sees the released value, guarantees visibility of all prior producer writes. This is the standard, minimal-cost publication pattern (no need for seq_cst).',
      ref: 'C++/CUDA memory model; release-acquire',
      tags: ['release-acquire', 'publication', 'memory-order'],
    },
    {
      d: 4,
      q: 'Warp-aggregated atomics combine same-target updates within a warp by…',
      o: [
        'Using a barrier',
        'Using __match_any_sync/__ballot to find lanes with the same target, computing a per-group sum with a warp scan, and having one leader issue a single atomic for the group',
        'Disabling atomics',
        'Using shared memory only',
      ],
      a: 1,
      x: [
        'Looping over lanes',
        'Using the host',
        'Using texture memory',
      ],
      e: 'Aggregation identifies lanes updating the same address (match/ballot), sums their contributions via a shuffle scan, and elects one lane to perform a single atomicAdd of the total — cutting up to 32× the atomic traffic for hot targets.',
      ref: 'NVIDIA "Warp-Aggregated Atomics"',
      tags: ['warp-aggregation', 'atomics', 'match'],
    },
  ],
  { idPrefix: 'synchronization-d' }
)
