import { defineQuestions } from './_helpers.js'

// CUDA Basics — batch B. Programming model, host API, compilation, device
// management. Distinct from basics.js (batch A).
export default defineQuestions(
  'basics',
  [
    {
      d: 1,
      q: 'When you construct a dim3 with fewer than three components, e.g. dim3 grid(8), what are the unspecified dimensions set to?',
      o: ['0', '1', 'Undefined', 'Equal to the first component'],
      a: 1,
      x: ['The warp size (32)', 'The maximum allowed value', 'Copied from blockDim'],
      e: 'dim3 components default to 1. dim3 grid(8) means (8, 1, 1). This is why a 1D launch only needs to set the .x component.',
      ref: 'CUDA C++ Programming Guide §2.2',
      tags: ['dim3', 'launch-config'],
    },
    {
      d: 1,
      q: 'Which call frees memory previously allocated with cudaMalloc?',
      o: ['free', 'cudaFree', 'cudaMemFree', 'delete'],
      a: 1,
      x: ['cudaDeviceReset only', 'cudaFreeHost', 'cudaUnmalloc'],
      e: 'cudaFree(ptr) releases device memory allocated by cudaMalloc. Host C/C++ free/delete must not be used on device pointers; cudaFreeHost releases pinned host memory.',
      ref: 'CUDA Runtime API',
      tags: ['memory', 'cudaFree'],
    },
    {
      d: 2,
      q: 'cudaGetErrorString(err) returns what?',
      o: [
        'The numeric error code',
        'A human-readable description of the cudaError_t value',
        'The name of the kernel that failed',
        'The line number of the error',
      ],
      a: 1,
      x: [
        'A pointer to the failing device memory',
        'The CUDA driver version',
        'Always the string "success"',
      ],
      e: 'cudaGetErrorString turns a cudaError_t into a readable message (e.g. "out of memory"). It is commonly used in error-checking macros that wrap CUDA calls.',
      ref: 'CUDA Runtime API',
      tags: ['error-handling'],
    },
    {
      d: 2,
      q: 'How are by-value kernel arguments (e.g. an int or a small struct) delivered to the device?',
      o: [
        'Through global memory the host must allocate',
        'They are passed in a dedicated constant/parameter bank, with a limited total size',
        'Each argument becomes a separate cudaMemcpy',
        'They are pushed onto each thread’s local stack individually',
      ],
      a: 1,
      x: [
        'Through shared memory of the first block',
        'They are broadcast over NVLink',
        'They are stored in the register file before launch',
      ],
      e: 'Kernel parameters are passed via a special constant parameter space and have a size limit (e.g. 4 KB on older archs, larger on Hopper). Large data should be passed as device pointers, not by value.',
      ref: 'CUDA C++ Programming Guide §7.3, parameter passing',
      tags: ['kernel-arguments', 'launch'],
    },
    {
      d: 2,
      q: 'You call printf() inside a __global__ kernel. What is true?',
      o: [
        'It is not allowed and fails to compile',
        'It is supported; output is buffered on the device and flushed to the host on synchronization',
        'It prints immediately, ahead of all host output',
        'It only works in the debugger',
      ],
      a: 1,
      x: [
        'It runs on the host via an RPC for each call',
        'It requires linking against the C++ iostream library',
        'It is limited to a single thread per block',
      ],
      e: 'Device-side printf writes to a fixed-size circular buffer that is copied to stdout at the next synchronization (or buffer overflow). Every participating thread prints, so guard it to avoid floods.',
      ref: 'CUDA C++ Programming Guide §B.17 (Formatted Output)',
      tags: ['printf', 'debugging'],
    },
    {
      d: 3,
      q: 'What does the __launch_bounds__(maxThreadsPerBlock, minBlocksPerSM) qualifier on a kernel tell the compiler?',
      o: [
        'To launch the kernel with exactly that configuration',
        'Limits used to guide register allocation so the kernel can hit the desired occupancy',
        'The amount of shared memory to reserve',
        'The number of streams to use',
      ],
      a: 1,
      x: [
        'A hard cap that aborts the launch if exceeded',
        'The grid dimensions to use by default',
        'A hint to place the kernel in constant memory',
      ],
      e: '__launch_bounds__ promises the kernel won’t be launched with more than maxThreadsPerBlock and asks for at least minBlocksPerSM resident; the compiler caps registers accordingly, trading register count for occupancy.',
      ref: 'CUDA C++ Programming Guide §7.34 (launch_bounds)',
      tags: ['launch-bounds', 'occupancy', 'registers'],
    },
    {
      d: 1,
      q: 'Inside a kernel, what does gridDim.x give you?',
      o: [
        'The index of the current block',
        'The number of blocks in the x-dimension of the grid',
        'The number of threads in the block',
        'The total number of threads',
      ],
      a: 1,
      x: [
        'The number of SMs on the GPU',
        'The x-coordinate of the current thread',
        'The warp size',
      ],
      e: 'gridDim is the grid’s size in blocks; gridDim.x is the number of blocks along x. blockIdx.x ranges over 0..gridDim.x-1.',
      ref: 'CUDA C++ Programming Guide §2.2',
      tags: ['built-in-variables', 'grid'],
    },
    {
      d: 1,
      q: 'What does blockDim.x give you inside a kernel?',
      o: [
        'The number of blocks in the grid',
        'The number of threads per block along the x-dimension',
        'The current block index',
        'The number of warps in the grid',
      ],
      a: 1,
      x: [
        'The number of threads in the entire grid',
        'The x index of the current thread',
        'The number of registers per thread',
      ],
      e: 'blockDim is the block’s dimensions in threads; blockDim.x is threads-per-block along x, used together with blockIdx.x and threadIdx.x to compute a global index.',
      ref: 'CUDA C++ Programming Guide §2.2',
      tags: ['built-in-variables', 'block'],
    },
    {
      d: 2,
      q: 'cudaMemset(ptr, 0, bytes) does what?',
      o: [
        'Allocates and zeroes device memory',
        'Sets each byte of a device buffer to the given value (here 0)',
        'Copies zeros from the host',
        'Frees the buffer',
      ],
      a: 1,
      x: [
        'Sets each 4-byte word to the value',
        'Initializes shared memory for all blocks',
        'Marks the memory read-only',
      ],
      e: 'cudaMemset fills device memory byte-wise with a value, analogous to C memset. Note it operates per byte, so cudaMemset(p, 1, n) sets bytes to 0x01, not integers to 1.',
      ref: 'CUDA Runtime API',
      tags: ['cudaMemset', 'memory'],
    },
    {
      d: 2,
      q: 'For a 2D block, which expression gives the thread’s row index in the block?',
      o: ['threadIdx.x', 'threadIdx.y', 'blockIdx.y', 'blockDim.y'],
      a: 1,
      x: ['threadIdx.x * blockDim.y', 'gridDim.y', 'threadIdx.z'],
      e: 'By convention threadIdx.y indexes the row and threadIdx.x the column within a 2D block. A global row is typically blockIdx.y*blockDim.y + threadIdx.y.',
      ref: 'PMPP Ch. 3',
      tags: ['indexing', '2d-block'],
    },
    {
      d: 3,
      q: 'How does nvcc handle a .cu file that mixes host and device code?',
      o: [
        'It compiles everything for the GPU',
        'It separates host and device code: device code → PTX/SASS, host code → handed to the host C++ compiler',
        'It requires you to split host and device into different files',
        'It runs host code on the GPU via emulation',
      ],
      a: 1,
      x: [
        'It interprets the file at runtime',
        'It compiles host code to PTX as well',
        'It only accepts pure-device files',
      ],
      e: 'nvcc splits the translation unit, compiling __global__/__device__ code to GPU assembly and forwarding host code to the system compiler (gcc/clang/msvc), then stitches them together.',
      ref: 'CUDA Compiler Driver NVCC documentation',
      tags: ['nvcc', 'compilation'],
    },
    {
      d: 2,
      q: 'Which API selects which GPU subsequent runtime calls operate on in a multi-GPU system?',
      o: ['cudaSetDevice', 'cudaChooseGPU', 'cudaBindDevice', 'cudaSelect'],
      a: 0,
      x: ['cudaUseDevice', 'cudaDeviceEnable', 'cudaSwitchGPU'],
      e: 'cudaSetDevice(i) makes GPU i current for the calling host thread; allocations, launches, and copies then target that device. cudaGetDeviceCount reports how many are present.',
      ref: 'CUDA Runtime API (Device Management)',
      tags: ['multi-gpu', 'device-management'],
    },
    {
      d: 3,
      q: 'You query cudaDeviceProp via cudaGetDeviceProperties. Which field tells you the number of SMs?',
      o: ['warpSize', 'multiProcessorCount', 'maxThreadsPerBlock', 'totalGlobalMem'],
      a: 1,
      x: ['clockRate', 'smCount', 'numCores'],
      e: 'cudaDeviceProp.multiProcessorCount is the SM count. Combined with maxThreadsPerMultiProcessor it bounds how many threads can be co-resident; there is no direct "CUDA core count" field.',
      ref: 'CUDA Runtime API (cudaDeviceProp)',
      tags: ['device-properties', 'sm'],
    },
    {
      d: 2,
      q: 'The compute capability of a GPU (e.g. 9.0 for H100) is exposed as which pair of cudaDeviceProp fields?',
      o: ['major and minor', 'arch and rev', 'gen and step', 'family and id'],
      a: 0,
      x: ['version and build', 'sm and sub', 'high and low'],
      e: 'cudaDeviceProp.major and .minor give the compute capability (e.g. 9 and 0). It governs available features and the -arch=sm_XY you compile for.',
      ref: 'CUDA Runtime API; Compute Capabilities',
      tags: ['compute-capability', 'device-properties'],
    },
    {
      d: 2,
      q: 'cudaMemcpy(dst, src, n, cudaMemcpyDeviceToDevice) does what?',
      o: [
        'Copies from host to device',
        'Copies between two device buffers (possibly on the same GPU)',
        'Copies device memory to a file',
        'Is not a valid copy kind',
      ],
      a: 1,
      x: [
        'Always copies between two different GPUs',
        'Maps one buffer onto another without copying',
        'Copies from device to host',
      ],
      e: 'DeviceToDevice copies between device addresses. Within one GPU it stays on-device (fast); across GPUs it may use peer-to-peer if enabled. Other kinds: HostToDevice, DeviceToHost, HostToHost, Default (inferred).',
      ref: 'CUDA Runtime API',
      tags: ['memcpy', 'device-to-device'],
    },
    {
      d: 3,
      q: 'What is the practical difference between cudaPeekAtLastError() and cudaGetLastError()?',
      o: [
        'They are identical',
        'Both return the last error, but cudaGetLastError also resets the error state to cudaSuccess; cudaPeekAtLastError leaves it',
        'cudaPeekAtLastError blocks until the device is idle',
        'cudaGetLastError only works in debug builds',
      ],
      a: 1,
      x: [
        'cudaPeekAtLastError clears the error and Get does not',
        'Peek is for the host, Get is for the device',
        'Get returns a string, Peek returns a code',
      ],
      e: 'Both report the most recent error; cudaGetLastError consumes (clears) it so the next check starts clean, while cudaPeekAtLastError reads it non-destructively. Error-check macros usually call cudaGetLastError after a launch.',
      ref: 'CUDA Runtime API (Error Handling)',
      tags: ['error-handling'],
    },
    {
      d: 1,
      q: 'What is the file extension conventionally used for CUDA C++ source compiled by nvcc?',
      o: ['.c', '.cpp', '.cu', '.ptx'],
      a: 2,
      x: ['.cuda', '.gpu', '.nvcc'],
      e: '.cu marks CUDA C++ sources (with .cuh headers). nvcc treats them as containing device code; plain .cpp files are compiled as host-only unless explicitly told otherwise.',
      ref: 'CUDA Compiler Driver NVCC documentation',
      tags: ['nvcc', 'file-extension'],
    },
    {
      d: 2,
      q: 'cudaMalloc’s signature is cudaMalloc(void** devPtr, size_t size). Why a void** (pointer-to-pointer)?',
      o: [
        'To support 2D allocations',
        'So the function can write the allocated device address back into your pointer variable, while returning a cudaError_t status',
        'Because device pointers are 128 bits',
        'To allow allocating multiple buffers at once',
      ],
      a: 1,
      x: [
        'Because device memory is reference counted',
        'So the pointer can be shared across GPUs automatically',
        'To let the host dereference it directly',
      ],
      e: 'C has no exceptions/multiple returns here, so cudaMalloc returns the status code and writes the new pointer through the void** out-parameter: cudaMalloc((void**)&d_ptr, bytes).',
      ref: 'CUDA Runtime API',
      tags: ['cudaMalloc', 'api'],
    },
    {
      d: 3,
      q: 'cudaDeviceReset() does what?',
      o: [
        'Reboots the physical GPU',
        'Destroys the current device’s context and frees all its resources for the calling process',
        'Clears all global memory to zero',
        'Resets only the default stream',
      ],
      a: 1,
      x: [
        'Resynchronizes the device clocks',
        'Re-runs the last kernel',
        'Resets the error state only',
      ],
      e: 'cudaDeviceReset cleans up the current process’s context on the current device (allocations, streams, etc.). It is often called at program end to ensure profiling tools flush data; it does not affect other processes.',
      ref: 'CUDA Runtime API (Device Management)',
      tags: ['device-management', 'context'],
    },
    {
      d: 4,
      q: 'cudaMallocPitch is preferred over cudaMalloc for 2D arrays because…',
      o: [
        'It is faster to allocate',
        'It pads each row to a hardware-friendly alignment (the "pitch"), so row accesses stay coalesced',
        'It allocates in shared memory',
        'It allows arrays larger than global memory',
      ],
      a: 1,
      x: [
        'It automatically transposes the data',
        'It stores the array in column-major order',
        'It compresses unused rows',
      ],
      e: 'cudaMallocPitch returns a per-row byte stride (pitch ≥ width) so each row begins at an aligned address, preserving coalescing. You index with row*pitch + col*sizeof(T) and copy with cudaMemcpy2D.',
      ref: 'CUDA Runtime API; Best Practices Guide',
      tags: ['cudaMallocPitch', '2d', 'alignment'],
    },
    {
      d: 2,
      q: 'Passing a host-allocated struct by value to a kernel works, but a pointer member inside that struct pointing to host memory will…',
      o: [
        'Be automatically copied to the device',
        'Be invalid on the device — the pointer still refers to host memory',
        'Cause a compile error',
        'Be converted to a device pointer',
      ],
      a: 1,
      x: [
        'Be promoted to managed memory',
        'Work because structs are deep-copied',
        'Trigger a cudaMemcpy behind the scenes',
      ],
      e: 'By-value copies the struct’s bytes, including the raw pointer value — which still addresses host memory and is unusable on the device. You must allocate device memory and store a device pointer (or use managed memory).',
      ref: 'CUDA C++ Programming Guide §6.2; common pitfalls',
      tags: ['structs', 'pointers', 'host-device'],
    },
    {
      d: 1,
      q: 'How many threads does <<<dim3(2,2), dim3(8,8)>>> launch in total?',
      o: ['64', '256', '32', '16'],
      a: 1,
      x: ['128', '512', '4'],
      e: 'Grid = 2×2 = 4 blocks; block = 8×8 = 64 threads. Total = 4 × 64 = 256 threads.',
      ref: 'CUDA C++ Programming Guide §2.2',
      tags: ['launch-config', '2d-grid'],
    },
    {
      d: 3,
      q: 'The maximum size of the x-dimension of a grid (gridDim.x) on compute capability 3.0+ is approximately…',
      o: ['65,535', '1,024', '2,147,483,647 (2^31 − 1)', '4,294,967,295'],
      a: 2,
      x: ['256', '1,048,576', '2^16'],
      e: 'Since CC 3.0 the grid x-dimension can be up to 2^31−1 blocks. The y and z dimensions remain limited to 65,535. (The old 65,535 x-limit was a Fermi-era constraint.)',
      ref: 'CUDA C++ Programming Guide, Compute Capabilities table',
      tags: ['limits', 'grid'],
    },
    {
      d: 2,
      q: 'A common error-checking macro wraps CUDA calls so that on failure it prints the error and aborts. Which function turns the returned code into text?',
      o: ['cudaGetErrorString', 'perror', 'strerror', 'cudaPrintError'],
      a: 0,
      x: ['cudaErrorText', 'cudaDescribe', 'gpuErrorString'],
      e: 'The macro typically does: if (e != cudaSuccess) { fprintf(stderr, "%s\\n", cudaGetErrorString(e)); exit(1); }. cudaGetErrorName gives the symbolic name; cudaGetErrorString gives the description.',
      ref: 'CUDA Runtime API; Best Practices Guide',
      tags: ['error-handling', 'macro'],
    },
    {
      d: 4,
      q: 'Why might cudaMemcpyAsync still behave synchronously (block the host) even though it is the "async" variant?',
      o: [
        'Async copies are never truly asynchronous',
        'If the host memory is pageable (not pinned), the runtime must stage through a pinned buffer, which serializes the copy',
        'Because the default stream is always used',
        'Because the destination is on the device',
      ],
      a: 1,
      x: [
        'Because the GPU has only one copy engine',
        'Because the transfer is larger than 2 GB',
        'Because cudaMemcpyAsync ignores the stream argument',
      ],
      e: 'True asynchronous DMA needs page-locked host memory. With pageable memory the driver copies into an internal pinned buffer first, which forces synchronization and removes overlap. Use cudaHostAlloc/cudaMallocHost.',
      ref: 'CUDA Best Practices Guide §9.1; Runtime API',
      tags: ['cudaMemcpyAsync', 'pinned-memory', 'overlap'],
    },
    {
      d: 2,
      q: 'Which built-in lets a 3D block index its depth dimension?',
      o: ['threadIdx.z', 'blockIdx.w', 'gridDim.z only', 'depthIdx'],
      a: 0,
      x: ['threadIdx.d', 'blockDim.w', 'zIndex'],
      e: 'Thread and block indices are 3D: threadIdx has .x/.y/.z (blockIdx and the Dim variables likewise). There is no .w component — CUDA grids/blocks are at most three-dimensional.',
      ref: 'CUDA C++ Programming Guide §2.2',
      tags: ['indexing', '3d'],
    },
    {
      d: 3,
      q: 'You launch a kernel, then immediately call cudaMemcpy to copy its results to the host (default stream). Do you need an explicit cudaDeviceSynchronize between them?',
      o: [
        'Yes, otherwise the copy reads stale data',
        'No — the blocking cudaMemcpy on the default stream waits for the kernel to finish first',
        'Only on multi-GPU systems',
        'Only if the kernel uses shared memory',
      ],
      a: 1,
      x: [
        'No, because kernels are synchronous by default',
        'Yes, cudaMemcpy never waits for kernels',
        'Only in release builds',
      ],
      e: 'Default-stream operations are ordered: a blocking cudaMemcpy waits for prior default-stream work (the kernel) to complete, so the data is valid. You DO need synchronization when using async copies on separate streams.',
      ref: 'CUDA C++ Programming Guide §6.2.8',
      tags: ['default-stream', 'ordering', 'synchronization'],
    },
    {
      d: 4,
      q: 'Roughly, kernel launch latency from the host is on the order of…',
      o: [
        'A few nanoseconds',
        'A few microseconds',
        'A few milliseconds',
        'A few seconds',
      ],
      a: 1,
      x: ['Hundreds of milliseconds', 'Exactly one GPU clock cycle', 'It is always zero'],
      e: 'Launch overhead is typically several microseconds. For kernels that run only microseconds themselves, this overhead dominates — motivating batching, kernel fusion, or CUDA Graphs to amortize it.',
      ref: 'CUDA Best Practices Guide; CUDA Graphs motivation',
      tags: ['launch-overhead', 'latency'],
    },
    {
      d: 2,
      q: 'cudaGetDeviceCount(&n) reports what?',
      o: [
        'The number of SMs on the current GPU',
        'The number of CUDA-capable GPUs visible to the process',
        'The number of streams created',
        'The number of running kernels',
      ],
      a: 1,
      x: ['The number of CPU cores', 'The CUDA runtime version', 'The number of contexts'],
      e: 'cudaGetDeviceCount returns how many CUDA devices are available (subject to CUDA_VISIBLE_DEVICES). You then iterate and cudaSetDevice to choose one.',
      ref: 'CUDA Runtime API (Device Management)',
      tags: ['device-management', 'multi-gpu'],
    },
    {
      d: 3,
      q: 'Setting the environment variable CUDA_VISIBLE_DEVICES=1 before running a program causes…',
      o: [
        'The program to use 1 GB of memory',
        'Only physical GPU 1 to be visible, and it appears to the program as device 0',
        'The program to run on the CPU',
        'Verbose CUDA logging',
      ],
      a: 1,
      x: [
        'GPUs 0 and 1 to both be visible',
        'The first GPU to be hidden but renumbered to 1',
        'Single-precision mode to be forced',
      ],
      e: 'CUDA_VISIBLE_DEVICES masks/reorders GPUs. With "1", only physical device 1 is exposed and is renumbered to logical device 0 — a key tool for pinning processes to specific GPUs.',
      ref: 'CUDA Programming Guide; environment variables',
      tags: ['multi-gpu', 'environment', 'device-management'],
    },
    {
      d: 1,
      q: 'Declaring shared memory statically inside a kernel uses which syntax?',
      o: [
        'shared float s[256];',
        '__shared__ float s[256];',
        '__device__ float s[256];',
        'extern float s[256];',
      ],
      a: 1,
      x: [
        '__local__ float s[256];',
        'shared__ float s[256];',
        '__smem__ float s[256];',
      ],
      e: '__shared__ float s[256]; declares per-block shared memory with a compile-time size. For a runtime size, use extern __shared__ and pass the byte count as the third launch parameter.',
      ref: 'CUDA C++ Programming Guide §5.3.2',
      tags: ['shared-memory', 'syntax'],
    },
    {
      d: 4,
      q: 'For indexing arrays whose total element count exceeds 2^31, the index variable should be…',
      o: [
        'int, which is always enough',
        'a 64-bit type such as size_t / long long, to avoid integer overflow',
        'short, to save registers',
        'float, for range',
      ],
      a: 1,
      x: [
        'unsigned int, which covers all cases',
        'char, since indices are small per thread',
        'It does not matter; CUDA promotes automatically',
      ],
      e: 'A 32-bit int overflows past ~2.1 billion. Large buffers (multi-GB tensors) need 64-bit indexing (size_t/int64) for the global index computation to avoid wraparound and out-of-bounds access.',
      ref: 'CUDA Best Practices Guide; large-tensor indexing',
      tags: ['indexing', 'overflow', '64-bit'],
    },
    {
      d: 3,
      q: 'cudaGetErrorName vs cudaGetErrorString — the difference is…',
      o: [
        'They are the same',
        'cudaGetErrorName returns the symbolic enum name (e.g. "cudaErrorMemoryAllocation"); cudaGetErrorString returns the prose description',
        'cudaGetErrorName works only on the device',
        'cudaGetErrorString returns a numeric code',
      ],
      a: 1,
      x: [
        'cudaGetErrorName clears the error',
        'cudaGetErrorString returns the enum name',
        'One is for driver API, the other for runtime API',
      ],
      e: 'cudaGetErrorName → the identifier; cudaGetErrorString → the human-readable explanation. Logging both is handy for debugging.',
      ref: 'CUDA Runtime API (Error Handling)',
      tags: ['error-handling'],
    },
    {
      d: 2,
      q: 'A grid-stride loop uses the stride blockDim.x * gridDim.x because that equals…',
      o: [
        'The number of blocks',
        'The total number of threads in the grid (so each pass advances by one full grid)',
        'The warp size times the block count',
        'The number of SMs',
      ],
      a: 1,
      x: [
        'The number of elements per block',
        'The shared memory size',
        'gridDim.x squared',
      ],
      e: 'blockDim.x*gridDim.x is the total thread count. Striding by it lets a fixed-size grid cover an arbitrarily large array: for (i = gid; i < n; i += blockDim.x*gridDim.x).',
      ref: 'NVIDIA Developer Blog, "Grid-Stride Loops"',
      tags: ['grid-stride', 'indexing', 'pattern'],
    },
    {
      d: 5,
      q: 'On Hopper, the kernel parameter limit was raised to allow passing larger argument blocks. The historical limit on earlier architectures was about…',
      o: ['256 bytes', '4 KB', '64 KB', '1 MB'],
      a: 1,
      x: ['512 bytes', '16 KB', '128 bytes'],
      e: 'Kernel parameters lived in a constant bank limited to ~4 KB (4096 bytes) through Volta/Ampere; Hopper increased it (to 32 KB). Beyond the limit you must pass data via device pointers.',
      ref: 'CUDA C++ Programming Guide, parameter passing; Hopper tuning guide',
      tags: ['kernel-arguments', 'limits', 'hopper'],
    },
  ],
  { idPrefix: 'basics-b' }
)
