import { defineQuestions } from './_helpers.js'

// CUDA Basics — batch F. Compile-time arch, JIT cache, debugging tools, device
// ordering, occupancy helpers, error scenarios. Distinct from basics.js/.b/.c/.d/.e.
export default defineQuestions(
  'basics',
  [
    {
      d: 4,
      q: 'The __CUDA_ARCH__ macro is used in device code to…',
      o: [
        'Detect the runtime GPU',
        'Conditionally compile different code paths per target compute capability at COMPILE time (e.g. use wgmma only when __CUDA_ARCH__ >= 900)',
        'Set the grid size',
        'Allocate memory',
      ],
      a: 1,
      x: [
        'Query the driver version',
        'Pick a stream',
        'Count SMs',
      ],
      e: '__CUDA_ARCH__ is defined during device compilation to the target SM version (e.g. 900 for sm_90), letting you guard architecture-specific instructions/intrinsics. It is undefined in host compilation, so it also separates host/device paths.',
      ref: 'CUDA C++ Programming Guide (__CUDA_ARCH__)',
      tags: ['cuda-arch', 'compile-time', 'dispatch'],
    },
    {
      d: 4,
      q: 'The environment variable CUDA_DEVICE_ORDER=PCI_BUS_ID changes…',
      o: [
        'The precision',
        'How devices are numbered: PCI_BUS_ID orders by physical bus (stable mapping) instead of the default FASTEST_FIRST — important for consistent device-to-rank assignment',
        'The clock speed',
        'The batch size',
      ],
      a: 1,
      x: [
        'The number of GPUs',
        'The warp size',
        'The driver',
      ],
      e: 'By default CUDA orders devices fastest-first, which can vary. Setting CUDA_DEVICE_ORDER=PCI_BUS_ID gives a deterministic ordering by bus id, so device indices map consistently to physical GPUs — important for multi-GPU binding/affinity.',
      ref: 'CUDA environment variables (CUDA_DEVICE_ORDER)',
      tags: ['device-order', 'environment', 'multi-gpu'],
    },
    {
      d: 4,
      q: 'When PTX is JIT-compiled at runtime, the result is cached on disk so that…',
      o: [
        'It recompiles every run',
        'Subsequent runs reuse the compiled SASS (in ~/.nv/ComputeCache) instead of re-JITting — CUDA_CACHE_DISABLE=1 turns this off, CUDA_FORCE_PTX_JIT=1 forces JIT',
        'It runs on the CPU',
        'It pins memory',
      ],
      a: 1,
      x: [
        'It compresses kernels',
        'It disables the GPU',
        'It profiles the kernel',
      ],
      e: 'The JIT compute cache stores JIT-compiled SASS so the (slow) PTX→SASS compilation happens once. Env vars control it: CUDA_CACHE_DISABLE disables caching, CUDA_CACHE_MAXSIZE sizes it, CUDA_FORCE_PTX_JIT forces JIT even when SASS exists.',
      ref: 'CUDA JIT cache; environment variables',
      tags: ['jit-cache', 'ptx', 'environment'],
    },
    {
      d: 3,
      q: 'The CUDA Runtime API is thread-safe, meaning…',
      o: [
        'Only one thread may call it',
        'Multiple host threads can call runtime functions concurrently (sharing the device’s primary context); you still manage per-thread current device and streams for correct concurrency',
        'It runs on the GPU',
        'It needs a mutex you provide',
      ],
      a: 1,
      x: [
        'It is single-threaded',
        'It disables streams',
        'It blocks the GPU',
      ],
      e: 'Runtime API calls are thread-safe and share the primary context. Each thread has its own current device (cudaSetDevice) and can use its own streams; with per-thread default streams, threads drive the GPU concurrently without extra locking.',
      ref: 'CUDA Runtime API thread safety',
      tags: ['thread-safety', 'runtime-api', 'multithreading'],
    },
    {
      d: 3,
      q: 'A robust CUDA error-checking macro typically captures __FILE__ and __LINE__ so that…',
      o: [
        'It runs faster',
        'When a call fails, the message pinpoints WHERE in the source the error occurred — invaluable since CUDA errors are otherwise reported generically',
        'It pins memory',
        'It disables errors',
      ],
      a: 1,
      x: [
        'It compresses output',
        'It increases occupancy',
        'It profiles the kernel',
      ],
      e: 'The common CHECK(call) macro wraps a call, tests the returned cudaError_t, and on failure prints cudaGetErrorString plus __FILE__/__LINE__ before aborting — so you immediately know which call and line failed, not just that something did.',
      ref: 'CUDA error-checking macro pattern',
      tags: ['error-handling', 'macro', 'debugging'],
    },
    {
      d: 4,
      q: '__trap() in device code…',
      o: [
        'Returns from the kernel',
        'Causes the kernel to abort execution (raising a trap/error), which surfaces as an error on the host — useful to halt on an unrecoverable device-side condition',
        'Pauses for the debugger only',
        'Skips a thread',
      ],
      a: 1,
      x: [
        'Resets the GPU',
        'Frees memory',
        'Synchronizes the block',
      ],
      e: '__trap() forcibly aborts the kernel (the context typically becomes unusable), reporting an error to the host. It is a hard stop for invariant violations; assert() is gentler (debug-only), and __brkpt() triggers a debugger breakpoint.',
      ref: 'CUDA C++ Programming Guide (__trap)',
      tags: ['trap', 'device', 'error'],
    },
    {
      d: 4,
      q: 'cuda-gdb lets you…',
      o: [
        'Profile kernels',
        'Debug device code interactively: set breakpoints in kernels, inspect per-thread/lane state, step through warps, and examine memory — like gdb but GPU-aware',
        'Compile PTX',
        'Allocate memory',
      ],
      a: 1,
      x: [
        'Measure bandwidth',
        'Lock clocks',
        'Run collectives',
      ],
      e: 'cuda-gdb extends gdb to the GPU: breakpoints inside kernels, switching focus among threads/warps/blocks, reading device variables, and catching faults. Compile with -G (device debug) for full debug info (at a performance cost).',
      ref: 'cuda-gdb documentation',
      tags: ['cuda-gdb', 'debugging', 'device'],
    },
    {
      d: 4,
      q: 'cudaOccupancyAvailableDynamicSMemPerBlock returns…',
      o: [
        'The total shared memory',
        'How much dynamic shared memory you could request per block while still achieving a desired number of resident blocks — helping size shared memory without dropping occupancy',
        'The register count',
        'The grid size',
      ],
      a: 1,
      x: [
        'The DRAM bandwidth',
        'The warp size',
        'The clock',
      ],
      e: 'Given a kernel and target blocks-per-SM, this API tells you the dynamic shared-memory budget that keeps that occupancy — so you can opt into the largest shared memory that doesn’t cost you a resident block.',
      ref: 'CUDA Runtime API (occupancy + shared memory)',
      tags: ['occupancy', 'shared-memory', 'api'],
    },
    {
      d: 3,
      q: 'The maximum y and z grid dimensions (gridDim.y, gridDim.z) are…',
      o: [
        '2^31 − 1 like x',
        '65,535 each (unlike the x-dimension, which is 2^31 − 1 since CC 3.0)',
        '1024',
        'Unlimited',
      ],
      a: 1,
      x: [
        '256',
        '4,294,967,295',
        '32',
      ],
      e: 'Only the grid x-dimension was raised to 2^31−1; y and z remain capped at 65,535. For very large 2D/3D problems, put the largest extent on x or fold dimensions into x to avoid exceeding the y/z limits.',
      ref: 'CUDA C++ Programming Guide, Compute Capabilities',
      tags: ['grid-limits', 'dimensions'],
    },
    {
      d: 5,
      q: 'In separate compilation with dynamic parallelism, cudaLimitDevRuntimePendingLaunchCount controls…',
      o: [
        'The stack size',
        'How many device-side child kernel launches can be pending (buffered) at once; exceeding it drops/launches fail, so deeply nested device launches may need it raised',
        'The grid size',
        'The shared memory',
      ],
      a: 1,
      x: [
        'The register count',
        'The block size',
        'The clock',
      ],
      e: 'Device-launched (dynamic parallelism) child grids queue in a buffer of bounded size. Heavy device-side launching can overflow it (launch errors); cudaDeviceSetLimit(cudaLimitDevRuntimePendingLaunchCount, n) enlarges it at a memory cost.',
      ref: 'CUDA dynamic parallelism; pending launch count',
      tags: ['dynamic-parallelism', 'limits', 'launch'],
    },
    {
      d: 4,
      q: 'nvcc -gencode arch=compute_80,code=sm_80 vs code=compute_80: the difference is…',
      o: [
        'They are identical',
        'code=sm_80 embeds SASS (native machine code) for sm_80; code=compute_80 embeds PTX (for JIT to future GPUs) — you often include both so the binary has native code AND forward compatibility',
        'arch sets the runtime',
        'code sets the grid',
      ],
      a: 1,
      x: [
        'compute is faster',
        'sm is virtual',
        'They set the block size',
      ],
      e: 'arch is the virtual (PTX) architecture; code lists the outputs. code=sm_XX = native SASS for that GPU; code=compute_XX = PTX (JIT-able to newer GPUs). Including both gives native performance plus forward compatibility via embedded PTX.',
      ref: 'CUDA Compiler Driver NVCC (-gencode)',
      tags: ['gencode', 'sass', 'ptx'],
    },
    {
      d: 3,
      q: 'A kernel returns cudaErrorInvalidValue immediately at launch. A likely cause is…',
      o: [
        'Out of memory',
        'An invalid launch argument (e.g. a bad dim3, a null/invalid stream, or an out-of-range value) — distinct from resource errors; check the launch configuration and arguments',
        'A race condition',
        'A divergent branch',
      ],
      a: 1,
      x: [
        'A spill',
        'A bank conflict',
        'ECC',
      ],
      e: 'cudaErrorInvalidValue signals a malformed argument to a CUDA call (bad dimensions, invalid handle/stream, out-of-range parameter). It is a programming error in how the call was made, caught at the API boundary — inspect the arguments.',
      ref: 'CUDA Runtime API error codes',
      tags: ['error', 'invalid-value', 'launch'],
    },
    {
      d: 4,
      q: 'To dispatch to architecture-specific kernels at RUNTIME (not compile time), you…',
      o: [
        'Use __CUDA_ARCH__',
        'Query the device’s compute capability (cudaDeviceGetAttribute / cudaDeviceProp.major/minor) and select among separately-compiled kernels accordingly',
        'Recompile each run',
        'Use a single kernel only',
      ],
      a: 1,
      x: [
        'Use NVTX',
        'Use cudaMemcpy',
        'Use constant memory',
      ],
      e: '__CUDA_ARCH__ is compile-time. At runtime, query the device capability and choose which precompiled kernel variant to launch — the pattern libraries use to pick the best kernel for the actual GPU from a fat binary of variants.',
      ref: 'Runtime arch dispatch; cudaDeviceGetAttribute',
      tags: ['runtime-dispatch', 'compute-capability'],
    },
    {
      d: 4,
      q: 'nvcc --threads N (or -t N) speeds up compilation by…',
      o: [
        'Running kernels in parallel',
        'Compiling multiple architecture targets / translation units in parallel threads — useful when -gencode lists many SASS targets (long build times)',
        'Using more SMs',
        'Disabling optimization',
      ],
      a: 1,
      x: [
        'Reducing registers',
        'Profiling',
        'Pinning memory',
      ],
      e: 'Building for many -gencode targets multiplies device-compilation work. --threads parallelizes those independent compilations, cutting build time for binaries that target several GPU architectures.',
      ref: 'NVCC --threads',
      tags: ['nvcc', 'compilation', 'parallel-build'],
    },
    {
      d: 3,
      q: 'cudaDeviceGetByPCIBusId is used to…',
      o: [
        'Reset a device',
        'Get the CUDA device index for a given PCI bus id string — letting you map a physical GPU (from topology tools) to a CUDA device number deterministically',
        'Launch a kernel',
        'Allocate memory',
      ],
      a: 1,
      x: [
        'Profile a device',
        'Lock clocks',
        'Pin memory',
      ],
      e: 'It resolves a PCI bus id (e.g. from nvidia-smi/topology) to the runtime device index, complementing cudaDeviceGetPCIBusId (the reverse). Useful for binding processes to specific physical GPUs in multi-GPU systems.',
      ref: 'CUDA Runtime API (cudaDeviceGetByPCIBusId)',
      tags: ['pci-bus-id', 'device-mapping', 'multi-gpu'],
    },
    {
      d: 4,
      q: 'Compiling with -G (full device debug) vs -lineinfo: -G…',
      o: [
        'Is for profiling',
        'Disables most device optimizations and adds full debug info for cuda-gdb (slow but fully debuggable); -lineinfo keeps optimizations and adds only line mappings (for profilers)',
        'Is faster',
        'Removes debug info',
      ],
      a: 1,
      x: [
        'Enables fast math',
        'Sets the arch',
        'Pins memory',
      ],
      e: '-G builds unoptimized, fully-debuggable device code for cuda-gdb (correctness debugging). -lineinfo keeps optimizations and adds source-line mapping for Nsight Compute. Use -G to debug logic, -lineinfo to profile.',
      ref: 'NVCC -G vs -lineinfo',
      tags: ['debug-flags', 'cuda-gdb', 'lineinfo'],
    },
    {
      d: 4,
      q: 'extern __shared__ float s[]; (dynamic shared memory) — if you need it aligned for float4 access, you should…',
      o: [
        'It is always aligned',
        'Ensure the dynamic shared-memory base/offsets meet the alignment (the base is suitably aligned, but sub-arrays you carve out must be aligned manually for vector types)',
        'Use constant memory',
        'Use global memory',
      ],
      a: 1,
      x: [
        'Disable vectorization',
        'Use registers',
        'Use texture memory',
      ],
      e: 'The dynamic shared-memory region base is aligned, but when you partition it into multiple typed sub-arrays you must align each (e.g. round offsets up) so vectorized (float4) accesses are correctly aligned — misalignment causes faults/slow paths.',
      ref: 'CUDA dynamic shared memory; alignment',
      tags: ['shared-memory', 'alignment', 'vectorization'],
    },
    {
      d: 3,
      q: 'A common reason cudaMalloc returns cudaErrorMemoryAllocation even though "free" memory seems sufficient is…',
      o: [
        'A bug in CUDA',
        'Fragmentation or other processes/context overhead consuming memory — the request needs contiguous space that isn’t available, or reserved memory (context, library handles) reduces the usable amount',
        'A divergent branch',
        'A bank conflict',
      ],
      a: 1,
      x: [
        'Too many registers',
        'ECC disabled',
        'A race',
      ],
      e: 'Allocation failures despite apparent free memory stem from fragmentation (no contiguous block) or overhead (context, cuDNN/cuBLAS workspaces, other processes). Caching allocators/pools and checking cudaMemGetInfo help diagnose; VMM-based growth reduces fragmentation.',
      ref: 'CUDA memory allocation; fragmentation',
      tags: ['allocation-failure', 'fragmentation', 'memory'],
    },
    {
      d: 5,
      q: 'cudaLimitDevRuntimeSyncDepth (legacy dynamic parallelism) bounded…',
      o: [
        'The grid size',
        'How deeply nested device-side cudaDeviceSynchronize calls could go (parent waiting on children at each level) — needed memory grew with depth; CDP2 removed device-side sync, eliminating this',
        'The register count',
        'The block size',
      ],
      a: 1,
      x: [
        'The shared memory',
        'The clock',
        'The warp size',
      ],
      e: 'Legacy CDP let a parent kernel device-synchronize on children, with nesting depth bounded by this limit (each level reserved memory). CDP2 removed device-side cudaDeviceSynchronize (costly/error-prone), so this limit no longer applies in the new model.',
      ref: 'CUDA dynamic parallelism; sync depth (legacy)',
      tags: ['dynamic-parallelism', 'sync-depth', 'cdp'],
    },
    {
      d: 4,
      q: 'When does using __launch_bounds__ HURT performance?',
      o: [
        'Never',
        'If you cap maxThreadsPerBlock/minBlocksPerSM too aggressively, the compiler over-restricts registers, causing SPILLS to local memory that cost more than the occupancy gained',
        'Always',
        'Only with FP64',
      ],
      a: 1,
      x: [
        'When the grid is small',
        'When using streams',
        'When using constant memory',
      ],
      e: '__launch_bounds__ guides the compiler to fit a register budget for a target occupancy. Setting it too tight forces register spilling, whose local-memory traffic can outweigh the extra resident warps — measure both ways before committing.',
      ref: 'CUDA __launch_bounds__; spills',
      tags: ['launch-bounds', 'registers', 'spilling'],
    },
    {
      d: 3,
      q: 'For debugging an illegal memory access, the recommended FIRST tool is…',
      o: [
        'Nsight Compute',
        'compute-sanitizer (memcheck) — it pinpoints the exact out-of-bounds/misaligned access and offending thread, often with a source line, far faster than guessing',
        'nvidia-smi',
        'ptxas -v',
      ],
      a: 1,
      x: [
        'cuobjdump',
        'A profiler',
        'NVTX',
      ],
      e: 'Illegal-address bugs (sticky, context-corrupting) are best caught with compute-sanitizer’s memcheck, which reports the precise faulting access, thread, and (with -lineinfo) source line — the standard first step for memory-correctness bugs.',
      ref: 'compute-sanitizer (memcheck); debugging',
      tags: ['compute-sanitizer', 'illegal-access', 'debugging'],
    },
    {
      d: 4,
      q: 'cudaStreamSynchronize on a stream after a kernel, vs reading results immediately on the host — the former is needed because…',
      o: [
        'The kernel is synchronous',
        'Async kernels/copies on that stream complete out-of-band; you must synchronize the stream (or use a blocking copy/event) before the host can safely read the results the kernel produced',
        'It frees memory',
        'It resets the GPU',
      ],
      a: 1,
      x: [
        'It increases occupancy',
        'It compresses data',
        'It locks clocks',
      ],
      e: 'On a non-default stream, work is asynchronous; the host must synchronize that stream (or the event/blocking copy) to guarantee the kernel finished and its writes are visible before reading results. Skipping this reads stale/incomplete data.',
      ref: 'CUDA stream synchronization',
      tags: ['stream-sync', 'async', 'correctness'],
    },
    {
      d: 5,
      q: 'A subtle multi-GPU bug: all processes allocate/launch on GPU 0 despite intending one GPU each. The cause is usually…',
      o: [
        'Out of memory',
        'Missing/incorrect cudaSetDevice(local_rank) before allocations/NCCL init — without binding, every process defaults to device 0, causing OOM and NCCL errors',
        'A race condition',
        'ECC',
      ],
      a: 1,
      x: [
        'Bank conflicts',
        'Divergence',
        'A spill',
      ],
      e: 'If processes don’t bind to their assigned GPU (cudaSetDevice with LOCAL_RANK) before allocating/initializing NCCL, they all use device 0 — exhausting its memory and breaking NCCL. Correct early device binding fixes it.',
      ref: 'Multi-GPU device binding',
      tags: ['multi-gpu', 'device-binding', 'bug'],
    },
    {
      d: 3,
      q: 'cudaDeviceReset() at program exit is sometimes recommended specifically to…',
      o: [
        'Speed up the next run',
        'Ensure profiling tools flush their data and all device resources are cleanly released for the process — without it, buffered profiler data may be lost',
        'Reset the clocks',
        'Free host memory',
      ],
      a: 1,
      x: [
        'Increase occupancy',
        'Pin memory',
        'Lock clocks',
      ],
      e: 'Calling cudaDeviceReset before exit cleans up the context and triggers profilers to flush their buffers, so traces aren’t truncated. It also helps tools detect leaks. It only affects the calling process’s context.',
      ref: 'CUDA Runtime API (cudaDeviceReset); profiling',
      tags: ['device-reset', 'profiling', 'cleanup'],
    },
    {
      d: 4,
      q: 'A "fat binary" that includes PTX for the newest virtual arch ensures…',
      o: [
        'Faster kernels',
        'Forward compatibility: on a GPU newer than any embedded SASS, the driver JIT-compiles the embedded PTX to that GPU’s SASS — so the program runs on future hardware',
        'Lower precision',
        'Smaller size',
      ],
      a: 1,
      x: [
        'Backward compatibility',
        'More registers',
        'Host execution',
      ],
      e: 'Embedding PTX (e.g. compute_90) provides forward compatibility: a future GPU without matching native SASS triggers a one-time JIT from the PTX. Without it, the program fails ("no kernel image") on newer-than-built architectures.',
      ref: 'CUDA forward compatibility; fat binary PTX',
      tags: ['fatbin', 'ptx', 'forward-compatibility'],
    },
    {
      d: 4,
      q: 'cudaGetDeviceProperties reports cooperativeLaunch=0 on a device. The consequence is…',
      o: [
        'No kernels run',
        'You cannot use cudaLaunchCooperativeKernel / grid.sync() on it — grid-wide cooperative synchronization isn’t supported, so you must use multi-kernel or other patterns',
        'Lower precision',
        'No streams',
      ],
      a: 1,
      x: [
        'No shared memory',
        'No atomics',
        'No tensor cores',
      ],
      e: 'A false cooperativeLaunch capability means grid-wide cooperative groups (grid.sync) aren’t available; algorithms relying on it must fall back to splitting into multiple kernel launches (using the launch boundary as the global barrier).',
      ref: 'cudaDeviceProp (cooperativeLaunch)',
      tags: ['cooperative-launch', 'capability', 'grid-sync'],
    },
    {
      d: 3,
      q: 'Why does CUDA_VISIBLE_DEVICES="2,3" make a 2-GPU job see those GPUs as devices 0 and 1?',
      o: [
        'It renames them',
        'The variable masks and REMAPS visible GPUs: the listed physical GPUs become logical devices 0..N-1 in listed order, hiding the others from the process',
        'It disables them',
        'It reorders by speed',
      ],
      a: 1,
      x: [
        'It pins them',
        'It resets them',
        'It overclocks them',
      ],
      e: 'CUDA_VISIBLE_DEVICES selects and renumbers GPUs: only the listed physical devices are visible, mapped to logical indices 0..N-1 in the given order. Combined with CUDA_DEVICE_ORDER, it controls exactly which GPUs a process uses and how they’re numbered.',
      ref: 'CUDA_VISIBLE_DEVICES',
      tags: ['visible-devices', 'environment', 'remapping'],
    },
    {
      d: 5,
      q: 'A kernel that worked at small scale crashes (illegal address) only for very large inputs. The most likely root cause is…',
      o: [
        'A race condition',
        '32-bit integer overflow in the index computation — the global index exceeds INT_MAX, wrapping negative and accessing out of bounds; use 64-bit (size_t) indexing',
        'A bank conflict',
        'Register spilling',
      ],
      a: 1,
      x: [
        'ECC error',
        'Divergence',
        'A missing __syncthreads',
      ],
      e: 'Large buffers (>2.1 G elements) overflow 32-bit index arithmetic, producing wrong (often negative) addresses and out-of-bounds access that only manifests at scale. Switching the index math to 64-bit (size_t/int64) fixes it.',
      ref: 'Integer overflow; large-tensor indexing',
      tags: ['integer-overflow', 'indexing', 'illegal-address'],
    },
    {
      d: 3,
      q: 'cuobjdump --dump-sass on a fatbin lets you…',
      o: [
        'Run the kernel',
        'Inspect the actual SASS (machine code) the GPU executes — to verify instruction selection (FMA, tensor-core ops), register usage, and whether optimizations happened',
        'Allocate memory',
        'Profile bandwidth',
      ],
      a: 1,
      x: [
        'Compile source',
        'Lock clocks',
        'Run collectives',
      ],
      e: 'Dumping SASS reveals what truly runs: whether the compiler emitted FMAs, used tensor-core instructions, vectorized loads, or spilled — ground truth beyond PTX. nvdisasm gives a richer disassembly with control-flow; both aid deep optimization.',
      ref: 'cuobjdump --dump-sass; nvdisasm',
      tags: ['cuobjdump', 'sass', 'disassembly'],
    },
    {
      d: 4,
      q: 'Why is it good practice to check the return value of EVERY CUDA API call (via a macro), not just kernels?',
      o: [
        'For speed',
        'API calls (cudaMalloc, cudaMemcpy, stream/event ops) can fail (OOM, invalid args, prior sticky errors); unchecked failures lead to confusing downstream errors far from the real cause',
        'It pins memory',
        'It increases occupancy',
      ],
      a: 1,
      x: [
        'It compresses data',
        'It locks clocks',
        'It disables errors',
      ],
      e: 'Silently ignoring a failed cudaMalloc/cudaMemcpy lets the program continue with invalid state, surfacing as a baffling later error. Checking each call (and the kernel via cudaGetLastError + sync) catches problems at their source.',
      ref: 'CUDA error-checking best practice',
      tags: ['error-handling', 'best-practice', 'debugging'],
    },
  ],
  { idPrefix: 'basics-f' }
)
