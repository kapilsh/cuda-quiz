import { defineQuestions } from './_helpers.js'

// Kernel Optimization — batch B. Thread coarsening, split-K, stencils,
// persistent kernels, shuffle reductions, transfer reduction. Distinct from
// optimization.js (batch A).
export default defineQuestions(
  'optimization',
  [
    {
      d: 3,
      q: 'Thread coarsening (having each thread compute several output elements) helps mainly by…',
      o: [
        'Increasing the number of threads',
        'Amortizes redundant work and exposes ILP',
        'Reducing register usage',
        'Eliminating global memory',
      ],
      a: 1,
      x: [
        'Forcing higher occupancy',
        'Removing the need for bounds checks',
        'Guaranteeing coalescing',
      ],
      e: 'Coarsening reuses per-thread setup and operands across multiple outputs and gives independent work (ILP), often outperforming the finest-grained mapping — but too much coarsening raises register pressure and lowers occupancy.',
      ref: 'PMPP Ch. 6; CUDA optimization patterns',
      tags: ['thread-coarsening', 'ilp', 'reuse'],
    },
    {
      d: 5,
      q: 'In GEMM, "split-K" parallelizes over the K (contraction) dimension. The catch is that…',
      o: [
        'It needs no extra work',
        'Partial products must be summed (combine step)',
        'It only works for square matrices',
        'It disables tensor cores',
      ],
      a: 1,
      x: [
        'It doubles the memory',
        'It requires double precision',
        'It removes the need for tiling',
      ],
      e: 'Split-K boosts parallelism for tall-skinny problems (small M,N but large K) by giving each chunk of K to different blocks, but their partial C tiles must be reduced together (atomic accumulate or a follow-up reduction), costing extra traffic.',
      ref: 'CUTLASS (split-K); GEMM parallelization',
      tags: ['split-k', 'gemm', 'reduction'],
    },
    {
      d: 3,
      q: 'For a stencil (e.g. 5-point) kernel, loading a tile plus its halo into shared memory…',
      o: [
        'Wastes memory with no benefit',
        'Neighbors come from fast shared memory',
        'Avoids the need for boundary handling',
        'Removes all divergence',
      ],
      a: 1,
      x: [
        'Eliminates the halo region',
        'Doubles arithmetic intensity automatically',
        'Requires atomics',
      ],
      e: 'Neighboring output points share input elements. Staging a (tile + halo) region in shared memory makes those reads cheap and reused, the standard stencil/convolution optimization — at the cost of loading and managing the halo border.',
      ref: 'PMPP Ch. 7 (stencils/convolution)',
      tags: ['stencil', 'halo', 'shared-memory', 'tiling'],
    },
    {
      d: 4,
      q: 'A "persistent kernel" keeps a fixed set of blocks resident and feeds them work from a queue. It is used to…',
      o: [
        'Increase occupancy',
        'Avoid launch overhead and balance load',
        'Reduce register usage',
        'Disable the scheduler',
      ],
      a: 1,
      x: [
        'Run multiple kernels at once',
        'Bypass global memory',
        'Force coalescing',
      ],
      e: 'Persistent-threads kernels launch exactly enough blocks to fill the GPU, then loop pulling tiles/tasks from a global work queue (often via atomics). This amortizes launch cost and helps irregular/load-imbalanced workloads.',
      ref: 'Gupta et al., "A Study of Persistent Threads"; GEMM split schedulers',
      tags: ['persistent-kernel', 'work-queue', 'load-balancing'],
    },
    {
      d: 3,
      q: 'A warp-level reduction using __shfl_down_sync is preferred over a shared-memory reduction within a warp because it…',
      o: [
        'Is more accurate',
        'Swaps registers directly, no shared memory',
        'Uses less power only',
        'Works across blocks',
      ],
      a: 1,
      x: [
        'Avoids using registers',
        'Requires no mask',
        'Is the only correct method',
      ],
      e: 'Shuffle reductions keep partial sums in registers and need no shared memory or block barrier for the final 32 elements, making the last stages of a reduction faster. Combine block-level shared reduction down to one warp, then finish with shuffles.',
      ref: 'Harris reduction; CUDA warp shuffle',
      tags: ['reduction', 'shuffle', 'warp'],
    },
    {
      d: 3,
      q: 'Vectorized stores (e.g. writing float4) help write-heavy kernels by…',
      o: [
        'Reducing arithmetic',
        'Fewer, wider stores (16 bytes/thread)',
        'Avoiding alignment requirements',
        'Eliminating the L2 cache',
      ],
      a: 1,
      x: [
        'Compressing the output',
        'Removing the need for coalescing',
        'Doubling occupancy',
      ],
      e: 'Like vectorized loads, float4/int4 stores cut the instruction count and increase per-thread memory parallelism. The output pointer must be 16-byte aligned and the access pattern coalesced.',
      ref: 'CUDA Best Practices Guide; vectorized memory access',
      tags: ['vectorized-stores', 'throughput', 'alignment'],
    },
    {
      d: 3,
      q: 'Compiling with --use_fast_math does what, and what is the trade-off?',
      o: [
        'Enables double precision; slower but exact',
        'Faster low-accuracy intrinsics; flushes denormals',
        'Disables all math',
        'Only affects integer ops',
      ],
      a: 1,
      x: [
        'Forces IEEE-compliant rounding',
        'Improves accuracy at a speed cost',
        'Turns on tensor cores',
      ],
      e: '--use_fast_math swaps in fast approximate intrinsics (__sinf, fast division, rsqrt), enables FMA contraction, and flushes denormals to zero. Great for tolerance-rich kernels (graphics, some ML), risky where accuracy matters.',
      ref: 'CUDA Compiler Driver NVCC; CUDA C++ Programming Guide §G',
      tags: ['fast-math', 'intrinsics', 'accuracy'],
    },
    {
      d: 2,
      q: 'For an iterative algorithm, keeping data resident on the GPU across iterations (instead of copying to/from host each step) matters because…',
      o: [
        'Host memory is larger',
        'PCIe transfers are slow vs on-device bandwidth',
        'The GPU cannot store data between kernels',
        'It improves occupancy',
      ],
      a: 1,
      x: [
        'Kernels cannot read device memory twice',
        'cudaMemcpy is free',
        'It reduces register pressure',
      ],
      e: 'PCIe bandwidth (tens of GB/s) is far below HBM bandwidth (TB/s). Minimizing host↔device traffic — keeping intermediate state on the device across kernels/iterations — is one of the highest-impact optimizations.',
      ref: 'CUDA Best Practices Guide §9 (Data Transfer)',
      tags: ['transfers', 'pcie', 'data-residency'],
    },
    {
      d: 4,
      q: 'Choosing a lookup table (in shared/constant memory) over recomputation is beneficial when…',
      o: [
        'The function is a single multiply',
        'Recompute is costly and the table is small/cached',
        'Always — tables are free',
        'The table is larger than global memory',
      ],
      a: 1,
      x: [
        'The kernel is memory-bound already',
        'You want more divergence',
        'The function changes every thread',
      ],
      e: 'On GPUs compute is cheap and memory is precious, so tables only win when the computation is genuinely costly and the table access is fast (constant broadcast or conflict-free shared). Otherwise recompute — the opposite of CPU intuition.',
      ref: 'CUDA Best Practices Guide; compute vs. memory trade-off',
      tags: ['lookup-table', 'compute-vs-memory'],
    },
    {
      d: 4,
      q: 'Caching a value reused several times by a thread in a register (instead of re-reading global memory) is effective because…',
      o: [
        'Registers are slower but larger',
        'Registers are fastest; cuts repeated global loads',
        'It increases occupancy',
        'Global memory cannot be read twice',
      ],
      a: 1,
      x: [
        'It reduces the instruction count to zero',
        'It forces coalescing',
        'It avoids the need for __syncthreads',
      ],
      e: 'Hoisting a repeatedly-used global value into a register removes redundant loads. This register-level reuse, alongside shared-memory tiling, is fundamental to turning memory-bound kernels compute-bound.',
      ref: 'CUDA Best Practices Guide; register reuse',
      tags: ['register-caching', 'reuse', 'arithmetic-intensity'],
    },
    {
      d: 5,
      q: 'A common Tensor Core MMA tile shape on Ampere for FP16 is m16n8k16. This means one mma.sync computes…',
      o: [
        'A 16-element vector',
        'A 16×8 tile accumulating over K=16',
        'A 16×16×16 cube per thread',
        'One scalar FMA',
      ],
      a: 1,
      x: [
        'A 32×32 tile per thread',
        'An 8×8 tile per lane',
        'A 16×16 tile with K=1',
      ],
      e: 'mma.sync.m16n8k16 has the warp compute a 16×8 accumulator tile from 16×16 and 16×8 operand fragments. Kernels tile larger problems into many such MMAs; CUTLASS/CuTe manage the fragment layouts.',
      ref: 'PTX ISA (mma); CUTLASS tile shapes',
      tags: ['tensor-core', 'mma', 'tile-shape', 'ampere'],
    },
    {
      d: 3,
      q: 'Binning/sorting irregular work so that threads in a warp do similar work reduces…',
      o: [
        'Memory capacity',
        'Divergence and load imbalance',
        'Register usage',
        'The number of kernels',
      ],
      a: 1,
      x: [
        'Coalescing',
        'Occupancy',
        'The need for atomics',
      ],
      e: 'Grouping work by type/branch outcome (binning) so each warp processes homogeneous items minimizes divergence and balances trip counts — a key technique for irregular workloads (sparse, graph, ray tracing).',
      ref: 'PMPP; irregular-workload optimization',
      tags: ['binning', 'divergence', 'load-balancing'],
    },
    {
      d: 3,
      q: 'The nvcc flag -maxrregcount=N (or __launch_bounds__) lets you…',
      o: [
        'Increase registers without limit',
        'Cap registers/thread to raise occupancy',
        'Set the number of blocks',
        'Choose the GPU architecture',
      ],
      a: 1,
      x: [
        'Disable register allocation',
        'Set shared memory size',
        'Control the warp size',
      ],
      e: 'Limiting registers can let more warps reside (higher occupancy), but if N is too low the compiler spills to local memory, which may cost more than the occupancy gains. Tune and profile both ways.',
      ref: 'CUDA Compiler Driver NVCC; occupancy tuning',
      tags: ['maxrregcount', 'occupancy', 'registers'],
    },
    {
      d: 4,
      q: 'Increasing the number of pipeline stages (e.g. 3–4 buffers) in a cp.async-based GEMM helps until…',
      o: [
        'It never stops helping',
        'SMEM/registers run out or latency is hidden',
        'The grid is too small',
        'Tensor cores are disabled',
      ],
      a: 1,
      x: [
        'The block size exceeds 1024',
        'You add a second GPU',
        'Occupancy reaches 100%',
      ],
      e: 'More stages deepen the software pipeline and hide more latency, but each stage costs shared memory (and bookkeeping). Returns diminish once latency is hidden; beyond that, stages reduce occupancy/SMEM headroom without benefit.',
      ref: 'CUTLASS pipeline stages; Ampere/Hopper GEMM',
      tags: ['pipelining', 'stages', 'cp-async', 'shared-memory'],
    },
    {
      d: 2,
      q: 'Replacing many tiny kernel launches with one batched/fused launch primarily reduces…',
      o: [
        'Numerical error',
        'Launch overhead and global round-trips',
        'Register usage',
        'Shared memory bank conflicts',
      ],
      a: 1,
      x: [
        'The arithmetic intensity',
        'The occupancy',
        'The warp size',
      ],
      e: 'Each launch costs microseconds and each separate kernel re-reads/writes DRAM. Batching/fusing amortizes launch overhead and keeps intermediates on-chip — important for sequences of small ops (common in DL).',
      ref: 'CUDA Best Practices Guide; kernel fusion',
      tags: ['batching', 'fusion', 'launch-overhead'],
    },
    {
      d: 4,
      q: 'A reduction kernel uses sequential addressing (`for (s=blockDim/2; s>0; s>>=1)`) instead of interleaved addressing with `%`. The main win is…',
      o: [
        'Fewer iterations',
        'No divergence and no bank conflicts',
        'Lower precision',
        'It removes __syncthreads',
      ],
      a: 1,
      x: [
        'It uses atomics',
        'It needs less shared memory',
        'It runs on the host',
      ],
      e: 'Sequential addressing keeps the active threads packed (0..s-1) each step, avoiding the divergence and bank conflicts that the modulo/interleaved version suffers — one of the classic reduction-optimization steps.',
      ref: 'Harris, "Optimizing Parallel Reduction in CUDA"',
      tags: ['reduction', 'bank-conflict', 'divergence'],
    },
    {
      d: 5,
      q: 'In FlashAttention, tiling the K/V over blocks while keeping the running softmax statistics avoids…',
      o: [
        'Using tensor cores',
        'Writing the full N×N score matrix to HBM',
        'Computing the softmax',
        'Any synchronization',
      ],
      a: 1,
      x: [
        'The need for Q',
        'Register usage',
        'The backward pass',
      ],
      e: 'By streaming K/V tiles and updating the online softmax (running max/sum) plus the output accumulator, FlashAttention keeps S on-chip and never writes the quadratic score matrix to HBM, drastically cutting memory traffic.',
      ref: 'Dao et al., "FlashAttention"',
      tags: ['flash-attention', 'tiling', 'softmax', 'memory'],
    },
    {
      d: 3,
      q: 'When a kernel is compute-bound on the FP32 pipe, a useful optimization is to…',
      o: [
        'Add more global memory accesses',
        'Cut instruction count or use tensor cores',
        'Increase the block size only',
        'Add atomics',
      ],
      a: 1,
      x: [
        'Lower the occupancy to zero',
        'Use more shared memory',
        'Disable the L2 cache',
      ],
      e: 'For compute-bound kernels you cut work: contract into FMAs, use fast intrinsics where tolerable, eliminate redundant computation, and offload matmul-shaped work to tensor cores. More memory or occupancy won’t help once compute is the limiter.',
      ref: 'CUDA Best Practices Guide; roofline (compute-bound)',
      tags: ['compute-bound', 'instruction-count', 'optimization'],
    },
    {
      d: 4,
      q: 'A prefetch distance that is too LARGE in a software-pipelined loop can hurt because…',
      o: [
        'It always helps',
        'More in-flight buffers lower occupancy/spill',
        'It causes incorrect results',
        'It disables coalescing',
      ],
      a: 1,
      x: [
        'It reduces ILP',
        'It forces double precision',
        'It deadlocks the kernel',
      ],
      e: 'Prefetching further ahead needs more buffer storage in flight. Past the point where latency is hidden, the extra resource use cuts occupancy and can cause spills — so the prefetch depth is a tuned parameter.',
      ref: 'Software pipelining; CUTLASS stage tuning',
      tags: ['prefetch', 'pipelining', 'occupancy'],
    },
    {
      d: 3,
      q: 'Loop interchange (swapping nested loop order) in a kernel can improve performance by…',
      o: [
        'Reducing the number of iterations',
        'Makes innermost accesses unit-stride/coalesced',
        'Removing the loop entirely',
        'Increasing divergence',
      ],
      a: 1,
      x: [
        'Adding atomics',
        'Lowering precision',
        'Increasing register count',
      ],
      e: 'The innermost loop’s stride determines locality/coalescing. Interchanging loops so the unit-stride dimension is innermost (for the warp) turns strided accesses into coalesced ones — a standard locality optimization.',
      ref: 'Loop optimization; PMPP Ch. 6',
      tags: ['loop-interchange', 'coalescing', 'locality'],
    },
    {
      d: 2,
      q: 'Why is recomputing a cheap value often better than storing and reloading it from global memory on a GPU?',
      o: [
        'Recomputation is always exact',
        'Compute is abundant; bandwidth is scarce',
        'Global memory cannot be reread',
        'It reduces register usage',
      ],
      a: 1,
      x: [
        'Recomputation uses tensor cores',
        'Storage requires atomics',
        'It improves coalescing',
      ],
      e: 'The compute-to-bandwidth ratio favors recomputation: a handful of arithmetic instructions are cheaper than a hundreds-of-cycles global load. This "recompute over store" mindset is central to GPU optimization.',
      ref: 'CUDA Best Practices Guide; arithmetic intensity',
      tags: ['recompute', 'compute-vs-memory', 'bandwidth'],
    },
    {
      d: 5,
      q: 'Stream-K (a GEMM scheduling strategy) improves on split-K by…',
      o: [
        'Removing the K dimension',
        'Even work across SMs, cutting the tail',
        'Using only one block',
        'Disabling tensor cores',
      ],
      a: 1,
      x: [
        'Doubling the precision',
        'Avoiding shared memory',
        'Eliminating the reduction',
      ],
      e: 'Stream-K assigns each block a contiguous chunk of the flattened MAC iteration space, so all SMs stay busy and partial tiles are reduced — yielding near-perfect load balance across many GEMM shapes, unlike fixed tile-per-block schedules.',
      ref: 'Osama et al., "Stream-K"; CUTLASS',
      tags: ['stream-k', 'gemm', 'load-balancing', 'scheduling'],
    },
    {
      d: 3,
      q: 'A rule-of-thumb starting point: aim for at least enough occupancy that there are several resident warps per scheduler, because…',
      o: [
        'More warps always means more speed',
        'A few warps per scheduler hide latency',
        'The hardware requires 64 warps',
        'Occupancy below 100% fails to launch',
      ],
      a: 1,
      x: [
        'One warp is always sufficient',
        'Occupancy must be exactly 50%',
        'Schedulers need 32 warps each',
      ],
      e: 'Latency hiding needs a handful of eligible warps per scheduler. Target "enough" occupancy to cover latency, then optimize memory/compute. Chasing 100% occupancy at the expense of registers/shared memory is often counterproductive.',
      ref: 'Volkov; CUDA Best Practices Guide',
      tags: ['occupancy', 'latency-hiding', 'tuning'],
    },
    {
      d: 4,
      q: 'Reducing precision of an intermediate buffer from FP32 to FP16/BF16 can speed a memory-bound kernel even without tensor cores because…',
      o: [
        'It changes the algorithm',
        'Halves bytes moved on a bandwidth-bound stage',
        'It increases occupancy',
        'It avoids divergence',
      ],
      a: 1,
      x: [
        'It doubles the FLOPs',
        'It removes the need for synchronization',
        'It enlarges the L2 cache',
      ],
      e: 'A bandwidth-bound stage scales with bytes. Storing/streaming intermediates in 16-bit halves traffic and runtime, independent of any tensor-core math benefit — provided the reduced precision is acceptable for the values involved.',
      ref: 'Mixed precision; roofline',
      tags: ['precision', 'bandwidth', 'memory-bound'],
    },
    {
      d: 3,
      q: 'Minimizing __syncthreads() calls in a kernel can help because each barrier…',
      o: [
        'Corrupts shared memory',
        'Makes all warps wait for the slowest',
        'Uses a register',
        'Disables coalescing',
      ],
      a: 1,
      x: [
        'Flushes the L2 cache',
        'Increases divergence',
        'Reduces occupancy permanently',
      ],
      e: 'Barriers serialize the block at that point; unnecessary or overly frequent syncs waste cycles waiting on stragglers. Use them only where memory ordering/correctness requires, and prefer warp-level sync where a full block barrier isn’t needed.',
      ref: 'CUDA Best Practices Guide; reduction optimization',
      tags: ['syncthreads', 'overhead', 'optimization'],
    },
    {
      d: 4,
      q: 'Output (C-tile) "register blocking" in GEMM where each thread holds an 8×8 accumulator in registers improves throughput because…',
      o: [
        'It reduces the matrix size',
        'One operand fetch feeds 64 register FMAs',
        'It avoids tensor cores',
        'It lowers precision',
      ],
      a: 1,
      x: [
        'It removes the K loop',
        'It increases divergence',
        'It needs no shared memory',
      ],
      e: 'An 8×8 micro-tile reuses each loaded A/B operand across a row/column of the accumulator (outer product), so a few shared-memory reads drive many FMAs in registers — the heart of high-performance CUDA-core and the analog of tensor-core tiling.',
      ref: 'CUTLASS; high-performance GEMM',
      tags: ['register-blocking', 'gemm', 'reuse', 'outer-product'],
    },
    {
      d: 2,
      q: 'Overlapping host-to-device copies with kernel execution requires splitting work into chunks and using…',
      o: [
        'A single default-stream pipeline',
        'Multiple streams, pinned memory, async copies',
        'More registers',
        'Constant memory',
      ],
      a: 1,
      x: [
        'A larger block size',
        'Unified memory only',
        'Atomic operations',
      ],
      e: 'Chunked, multi-stream execution (copy chunk i+1 while computing on chunk i) hides transfer time behind compute. It needs page-locked host memory and async copies on non-default streams.',
      ref: 'CUDA Best Practices Guide §9.1; streams',
      tags: ['overlap', 'streams', 'pinned-memory'],
    },
    {
      d: 5,
      q: 'Why can a hand-written GEMM that hits 90% of cuBLAS still be a poor choice in practice?',
      o: [
        'It is always faster',
        'cuBLAS covers all shapes/archs and is maintained',
        'Hand-written kernels cannot use tensor cores',
        '90% is unacceptable',
      ],
      a: 1,
      x: [
        'cuBLAS only does small matrices',
        'Hand-written kernels cannot be compiled',
        'It uses more memory',
      ],
      e: 'A one-off kernel may win on one shape but cuBLAS/CUTLASS cover the whole space and track new architectures. Maintaining peak performance across shapes and GPU generations is why most production code calls the libraries.',
      ref: 'cuBLAS/CUTLASS rationale',
      tags: ['gemm', 'cublas', 'production', 'maintenance'],
    },
    {
      d: 4,
      q: 'Avoiding atomics by using a segmented/hierarchical reduction is beneficial when…',
      o: [
        'There is no contention',
        'Local reduction cuts contended global atomics',
        'The output is unique per thread',
        'Atomics are unsupported',
      ],
      a: 1,
      x: [
        'The reduction is tiny',
        'You want more divergence',
        'Memory is unlimited',
      ],
      e: 'Hierarchical reduction (shuffle within warp → shared within block → few global atomics or a second pass) replaces a storm of contended global atomics with mostly-local work, scaling far better under contention.',
      ref: 'PMPP Ch. 9–10; reduction patterns',
      tags: ['reduction', 'atomics', 'hierarchical'],
    },
    {
      d: 3,
      q: 'Unrolling the inner accumulation loop of a dot product AND using multiple accumulators together address which two issues?',
      o: [
        'Memory capacity and divergence',
        'Loop overhead and the FMA dependency chain',
        'Coalescing and bank conflicts',
        'Occupancy and registers',
      ],
      a: 1,
      x: [
        'Precision and range',
        'Launch overhead and transfers',
        'Atomics and locks',
      ],
      e: 'Unrolling removes branch/increment overhead and exposes independent instructions; multiple accumulators break the dependency chain so independent FMAs hide each other’s latency. Together they push the inner loop toward peak FMA throughput.',
      ref: 'CUDA Best Practices Guide; GEMM inner loop',
      tags: ['unrolling', 'accumulators', 'ilp'],
    },
  ],
  { idPrefix: 'optimization-b' }
)
