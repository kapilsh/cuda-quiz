import { defineQuestions } from './_helpers.js'

// Distributed Training — batch D. SyncBN, distributed clipping, large-batch
// optimizers, DTensor/FSDP2, scaling efficiency, inference. Distinct from distributed.js/.b.js/.c.js.
export default defineQuestions(
  'distributed',
  [
    {
      d: 4,
      q: 'Synchronized BatchNorm (SyncBN) across data-parallel GPUs is needed when…',
      o: [
        'The model has no BN',
        'Small per-GPU batch; SyncBN all-reduces stats',
        'Using LayerNorm',
        'The learning rate is high',
      ],
      a: 1,
      x: [
        'For faster training',
        'To save memory',
        'For FP8',
      ],
      e: 'BatchNorm’s statistics depend on batch size. With small per-GPU batches, local stats are unreliable; SyncBN communicates mean/var across GPUs to normalize over the effective global batch — at the cost of extra collectives per BN layer.',
      ref: 'SyncBatchNorm; distributed training',
      tags: ['syncbn', 'batchnorm', 'statistics'],
    },
    {
      d: 4,
      q: 'Global-norm gradient clipping in data-parallel training requires…',
      o: [
        'Clipping each rank independently',
        'All-reduce global norm first; all clip uniformly',
        'No communication',
        'FP64 gradients',
      ],
      a: 1,
      x: [
        'A broadcast of weights',
        'Pipeline parallelism',
        'A barrier only',
      ],
      e: 'Since gradients are already all-reduced (identical across replicas in DDP) the norm is consistent; but with sharded grads (FSDP) you must reduce the partial norms to get the true global norm before clipping, keeping replicas in sync.',
      ref: 'Distributed gradient clipping',
      tags: ['gradient-clipping', 'global-norm', 'all-reduce'],
    },
    {
      d: 4,
      q: 'Large-batch optimizers like LARS/LAMB were developed because…',
      o: [
        'They are simpler',
        'Linear LR unstable at huge batches; LARS/LAMB',
        'They use less memory',
        'They avoid communication',
      ],
      a: 1,
      x: [
        'They quantize gradients',
        'They shard the model',
        'They are for inference',
      ],
      e: 'Scaling batch size (and LR) far enough breaks vanilla optimizers. LARS/LAMB adapt the learning rate per layer based on weight/gradient norms, enabling stable convergence at very large batches (e.g. training BERT in minutes).',
      ref: 'You et al., "LARS"/"LAMB"',
      tags: ['lamb', 'lars', 'large-batch'],
    },
    {
      d: 5,
      q: 'PyTorch FSDP2 / DTensor represents a sharded parameter as…',
      o: [
        'A plain tensor on one GPU',
        'Distributed tensor with explicit shard spec',
        'A NumPy array',
        'A CPU tensor',
      ],
      a: 1,
      x: [
        'A quantized tensor',
        'A gradient only',
        'A constant',
      ],
      e: 'DTensor pairs a local shard with a placement spec on a device mesh, enabling per-parameter sharding (FSDP2) and composability with tensor parallelism. It’s a cleaner abstraction than FSDP1’s flattened-bucket approach for expressing distributed state.',
      ref: 'PyTorch DTensor; FSDP2',
      tags: ['dtensor', 'fsdp2', 'sharding'],
    },
    {
      d: 5,
      q: 'Overlapping tensor-parallel communication with compute (e.g. "Domino"/async TP) works by…',
      o: [
        'Removing the all-reduce',
        'All-reduce one chunk while computing the next',
        'Using FP64',
        'Skipping layers',
      ],
      a: 1,
      x: [
        'Quantizing activations',
        'Disabling tensor parallelism',
        'Using the CPU',
      ],
      e: 'TP’s per-layer all-reduce is normally exposed (latency-bound). Async/overlapped TP splits the computation so communication of one chunk overlaps computation of another, reducing the exposed collective time — important for TP at scale/inference.',
      ref: 'Overlapped tensor parallelism (e.g. Domino, async-TP)',
      tags: ['tensor-parallel', 'overlap', 'communication'],
    },
    {
      d: 4,
      q: 'Strong scaling vs weak scaling in training: STRONG scaling means…',
      o: [
        'Bigger model per GPU',
        'Fixed total problem; per-GPU work shrinks',
        'More data per GPU',
        'Higher precision',
      ],
      a: 1,
      x: [
        'Constant per-GPU work',
        'Larger batches always',
        'Fewer GPUs',
      ],
      e: 'Strong scaling holds the problem fixed and adds GPUs (less compute each), so communication/overhead increasingly dominates — efficiency drops. Weak scaling grows the problem with the GPU count (constant per-GPU work), scaling better. Training mixes both.',
      ref: 'Parallel scaling; strong vs weak',
      tags: ['scaling', 'strong-weak', 'efficiency'],
    },
    {
      d: 4,
      q: 'The compute-to-communication ratio per step largely predicts data-parallel scaling efficiency because…',
      o: [
        'Communication is free',
        'High compute:comm ratio → near-linear scaling',
        'Compute is irrelevant',
        'Memory determines it',
      ],
      a: 1,
      x: [
        'Precision determines it',
        'The optimizer determines it',
        'The dataset determines it',
      ],
      e: 'Scaling efficiency hinges on whether communication hides behind compute. High compute:comm ratio (big per-GPU work, fast links) scales well; low ratio (tiny per-GPU work, slow network) bottlenecks on collectives — the core scaling trade-off.',
      ref: 'Distributed scaling analysis',
      tags: ['compute-comm-ratio', 'scaling', 'efficiency'],
    },
    {
      d: 5,
      q: 'Speculative decoding speeds up LLM inference by…',
      o: [
        'Quantizing the model',
        'Draft proposes tokens; target verifies them',
        'Sharding the KV cache',
        'Disabling attention',
      ],
      a: 1,
      x: [
        'Training faster',
        'Reducing parameters',
        'Using FP64',
      ],
      e: 'Autoregressive decoding is latency-bound (one token per forward). A cheap draft model guesses k tokens; the target model verifies them in parallel and accepts the longest correct prefix — amortizing the expensive forward over multiple tokens.',
      ref: 'Speculative decoding (Leviathan et al.; Chen et al.)',
      tags: ['speculative-decoding', 'inference', 'latency'],
    },
    {
      d: 4,
      q: 'Combining FSDP (data) with tensor parallelism (2D parallelism) is done to…',
      o: [
        'Reduce accuracy',
        'TP intra-node; FSDP across nodes',
        'Avoid communication',
        'Use one GPU',
      ],
      a: 1,
      x: [
        'Quantize weights',
        'Disable pipelines',
        'Increase precision',
      ],
      e: 'TP within a node reduces per-GPU layer memory/compute; FSDP shards the (still large) parameter/optimizer state across the data-parallel dimension. Composing them (on a 2D device mesh) scales bigger models than either method by itself.',
      ref: 'FSDP + TP (2D parallelism)',
      tags: ['fsdp', 'tensor-parallel', '2d-parallelism'],
    },
    {
      d: 4,
      q: 'Asynchronous checkpointing (save in the background) helps long training by…',
      o: [
        'Improving accuracy',
        'Snapshot to host; write in background',
        'Reducing model size',
        'Quantizing weights',
      ],
      a: 1,
      x: [
        'Avoiding checkpoints',
        'Using FP64',
        'Disabling NCCL',
      ],
      e: 'Synchronous checkpointing stalls training during slow disk writes. Async checkpointing snapshots state (e.g. to pinned host memory) fast and persists it in the background, so the GPU keeps training — important at scale where checkpoints are frequent and large.',
      ref: 'Asynchronous/distributed checkpointing',
      tags: ['checkpointing', 'async', 'operations'],
    },
    {
      d: 5,
      q: 'A practical relationship guiding checkpoint frequency is…',
      o: [
        'Checkpoint every step',
        'Balance checkpoint cost against lost work',
        'Never checkpoint',
        'Checkpoint randomly',
      ],
      a: 1,
      x: [
        'Frequency is irrelevant',
        'Only at the end',
        'Based on the learning rate',
      ],
      e: 'More GPUs ⇒ higher aggregate failure rate (lower MTBF). Checkpoint frequency trades I/O overhead against wasted recompute after a failure; the optimum depends on MTBF and checkpoint cost — async/sharded checkpoints lower that cost, allowing more frequent saves.',
      ref: 'Large-scale training reliability; checkpoint frequency',
      tags: ['checkpointing', 'mtbf', 'reliability'],
    },
    {
      d: 4,
      q: 'Tensor parallelism reduces not just parameter memory but also ACTIVATION memory per GPU because…',
      o: [
        'It replicates activations',
        'Each GPU stores only its shard of the activations',
        'It uses FP8',
        'It removes activations',
      ],
      a: 1,
      x: [
        'It offloads to CPU',
        'It quantizes activations',
        'It checkpoints everything',
      ],
      e: 'Since TP splits a layer’s computation across GPUs, each holds only its portion of the intermediate activations (plus replicated regions, which sequence parallelism then also shards). This reduces per-GPU activation memory, complementing parameter sharding.',
      ref: 'Megatron-LM; TP activation memory',
      tags: ['tensor-parallel', 'activation-memory'],
    },
    {
      d: 5,
      q: 'A 5D parallelism configuration might compose…',
      o: [
        'Five optimizers',
        'Data, tensor, pipeline, context, expert',
        'Five GPUs',
        'Five precisions',
      ],
      a: 1,
      x: [
        'FP32+FP16+FP8+INT8+FP4',
        'Five datasets',
        'Five learning rates',
      ],
      e: 'Beyond 3D (data/tensor/pipeline), context parallelism scales sequence length and expert parallelism scales MoE experts. Large frontier-model training composes several of these axes on a multi-dimensional device mesh.',
      ref: 'N-D parallelism; frontier-scale training',
      tags: ['5d-parallelism', 'context-parallel', 'expert-parallel'],
    },
    {
      d: 4,
      q: 'In pipeline parallelism, the warmup (fill) and cooldown (drain) phases are where…',
      o: [
        'The optimizer runs',
        'Some stages idle (the bubble) filling/draining',
        'Gradients are clipped',
        'Data is loaded',
      ],
      a: 1,
      x: [
        'All-reduce happens',
        'Weights are broadcast',
        'Checkpoints are saved',
      ],
      e: 'During fill (first micro-batches propagating forward) and drain (last backward passes), not all stages are busy — that idle time is the bubble. Steady state (middle) is efficient; the bubble fraction shrinks with more micro-batches / interleaved/zero-bubble schedules.',
      ref: 'Pipeline parallelism; bubble',
      tags: ['pipeline', 'warmup-cooldown', 'bubble'],
    },
    {
      d: 4,
      q: 'Keeping an exponential moving average (EMA) of weights in distributed training requires…',
      o: [
        'Nothing special',
        'EMA state must follow the weight sharding',
        'A broadcast every step',
        'FP64',
      ],
      a: 1,
      x: [
        'Pipeline parallelism',
        'All-to-all',
        'A separate GPU',
      ],
      e: 'EMA tracks a smoothed copy of the weights. In DDP every rank has the same weights so each maintains the EMA identically; in FSDP the EMA state is sharded like the parameters. Mismanaging this desyncs the averaged model across ranks.',
      ref: 'EMA in distributed training',
      tags: ['ema', 'distributed', 'weight-averaging'],
    },
    {
      d: 4,
      q: 'A simple per-GPU training-memory estimate with ZeRO-3/FSDP (sharded over N data-parallel ranks) is roughly…',
      o: [
        'Same as single-GPU',
        'State/N + activations + transient buffers',
        'N × single-GPU',
        'Just the activations',
      ],
      a: 1,
      x: [
        'Only the optimizer/N',
        'Params × N',
        'Zero',
      ],
      e: 'FSDP/ZeRO-3 divides the ~16 bytes/param state by N (the DP shard count), but each GPU still holds its activations and temporarily gathers a layer’s full params. So per-GPU memory ≈ sharded-state/N + activations + transient buffers.',
      ref: 'ZeRO/FSDP memory accounting',
      tags: ['memory', 'fsdp', 'estimation'],
    },
    {
      d: 5,
      q: 'For LLM inference, tensor parallelism is favored over pipeline parallelism for LATENCY because…',
      o: [
        'TP uses less memory',
        'TP parallelizes a layer; PP serializes stages',
        'PP is more accurate',
        'TP avoids communication',
      ],
      a: 1,
      x: [
        'PP uses NVLink',
        'TP needs no collectives',
        'They are identical',
      ],
      e: 'Single-request decode latency suffers in PP (a token traverses stages sequentially). TP computes each layer across GPUs in parallel (with per-layer all-reduce on NVLink), reducing per-token latency — hence TP-within-node for low-latency serving.',
      ref: 'LLM inference; TP vs PP latency',
      tags: ['tensor-parallel', 'pipeline', 'inference-latency'],
    },
    {
      d: 4,
      q: 'A symptom of pipeline-parallel load IMBALANCE (uneven stage assignment) is…',
      o: [
        'Perfect overlap',
        'Some stages finish early and idle',
        'Lower precision',
        'More memory',
      ],
      a: 1,
      x: [
        'Faster training',
        'Better accuracy',
        'Fewer GPUs',
      ],
      e: 'PP throughput is set by the slowest stage. Uneven partitioning (e.g. embedding/output-heavy first/last stages) makes some GPUs idle — balance the stages by FLOPs/time (and account for the loss/embedding stages) for good utilization.',
      ref: 'Pipeline parallelism; stage balancing',
      tags: ['pipeline', 'load-balance', 'stages'],
    },
    {
      d: 4,
      q: 'ZeRO-Infinity extends offloading to NVMe so that…',
      o: [
        'It runs faster',
        'State spills to NVMe beyond CPU RAM',
        'It avoids checkpoints',
        'It increases precision',
      ],
      a: 1,
      x: [
        'It quantizes weights',
        'It disables NCCL',
        'It uses tensor cores',
      ],
      e: 'ZeRO-Infinity tiers state across GPU/CPU/NVMe, prefetching as needed, so enormous models fit at the cost of slower NVMe/PCIe-bound steps. It trades throughput for the ability to train models that otherwise wouldn’t fit at all.',
      ref: 'DeepSpeed ZeRO-Infinity',
      tags: ['zero-infinity', 'nvme', 'offload'],
    },
    {
      d: 3,
      q: 'Why must all data-parallel replicas start from IDENTICAL weights?',
      o: [
        'For speed',
        'Replicas must stay in lockstep',
        'To save memory',
        'For FP8',
      ],
      a: 1,
      x: [
        'For data loading',
        'To shard the model',
        'For the optimizer only',
      ],
      e: 'If replicas began with different weights, averaging gradients wouldn’t keep them consistent and the "single model" abstraction breaks. So you broadcast initial weights (or seed identically) and apply the same all-reduced gradient everywhere.',
      ref: 'Synchronous SGD; replica consistency',
      tags: ['data-parallel', 'initialization', 'consistency'],
    },
    {
      d: 5,
      q: 'Why does increasing tensor-parallel degree eventually HURT efficiency even within a node?',
      o: [
        'It never hurts',
        'Smaller GEMMs, larger relative all-reduces',
        'It uses less memory',
        'It improves accuracy',
      ],
      a: 1,
      x: [
        'It disables NVLink',
        'It increases precision',
        'It removes activations',
      ],
      e: 'Splitting a layer across more GPUs shrinks each GEMM (worse arithmetic intensity/efficiency) and the per-layer all-reduce becomes a larger fraction of time. There’s an optimal TP degree (often ≤ a node) beyond which efficiency falls.',
      ref: 'Megatron-LM; TP degree trade-offs',
      tags: ['tensor-parallel', 'efficiency', 'degree'],
    },
    {
      d: 4,
      q: 'The dominant inference-memory consumer for long-context LLM serving is often…',
      o: [
        'The optimizer states',
        'The KV cache (can exceed the weights)',
        'The gradients',
        'The dataset',
      ],
      a: 1,
      x: [
        'The activations of training',
        'The embeddings only',
        'The vocabulary',
      ],
      e: 'Inference has no gradients/optimizer states; the KV cache dominates for long contexts/large batches. Techniques (paged/quantized KV cache, GQA/MQA, shorter context) target it directly to raise serving capacity.',
      ref: 'LLM serving; KV cache memory',
      tags: ['kv-cache', 'inference', 'memory'],
    },
    {
      d: 4,
      q: 'Grouped-Query / Multi-Query Attention (GQA/MQA) reduce inference memory/bandwidth by…',
      o: [
        'Quantizing weights',
        'Sharing K/V heads, shrinking the KV cache',
        'Removing attention',
        'Using FP64',
      ],
      a: 1,
      x: [
        'Adding more heads',
        'Sharding the model',
        'Pipeline parallelism',
      ],
      e: 'MQA uses a single K/V head for all query heads; GQA uses a few shared K/V heads. Both shrink the KV cache (fewer K/V to store/read), cutting memory and the bandwidth that dominates decode — a key inference-efficiency design choice.',
      ref: 'GQA/MQA; inference efficiency',
      tags: ['gqa', 'mqa', 'kv-cache', 'inference'],
    },
    {
      d: 4,
      q: 'When data parallelism is communication-bound, switching SOME of it to tensor/pipeline parallelism can help because…',
      o: [
        'It removes communication',
        'Lower DP degree; shift comm to faster links',
        'It increases precision',
        'It uses one GPU',
      ],
      a: 1,
      x: [
        'It disables NCCL',
        'It quantizes gradients',
        'It avoids the optimizer',
      ],
      e: 'If DP all-reduce dominates, reducing DP degree (by adding TP/PP) shrinks that collective and shifts communication onto intra-node NVLink (TP) or light P2P (PP). Tuning the parallelism mix to the hardware balances compute and communication.',
      ref: 'Parallelism balancing; 3D parallelism',
      tags: ['parallelism-balance', 'communication-bound', 'scaling'],
    },
    {
      d: 5,
      q: 'A reason FSDP2 (per-parameter DTensor sharding) improves on FSDP1 (flat-buffer) is…',
      o: [
        'It is slower',
        'Per-parameter sharding composes with TP',
        'It uses FP64',
        'It removes sharding',
      ],
      a: 1,
      x: [
        'It needs one GPU',
        'It disables overlap',
        'It quantizes weights',
      ],
      e: 'FSDP1 flattens parameters into buckets, complicating composition and introducing padding. FSDP2’s DTensor-based per-parameter sharding integrates with tensor parallelism and mixed sharding strategies more naturally, improving composability.',
      ref: 'PyTorch FSDP2 / DTensor',
      tags: ['fsdp2', 'dtensor', 'composability'],
    },
    {
      d: 3,
      q: 'Why is the global batch size (not per-GPU batch) the quantity that affects convergence?',
      o: [
        'It is not',
        'Updates use the gradient over the global batch',
        'Per-GPU batch sets convergence',
        'Only the dataset matters',
      ],
      a: 1,
      x: [
        'The model size sets it',
        'Precision sets it',
        'The seed sets it',
      ],
      e: 'Each optimizer step uses the gradient averaged over the global batch (per-GPU × DP × accumulation). Convergence (LR scaling, gradient noise) is governed by this global batch, so changing GPU count/accumulation requires adjusting it (and LR) thoughtfully.',
      ref: 'Large-batch training; global batch',
      tags: ['global-batch', 'convergence', 'learning-rate'],
    },
    {
      d: 4,
      q: 'Backup/redundant workers ("hedging") in synchronous training mitigate stragglers by…',
      o: [
        'Removing synchronization',
        'Proceed with the first N replicas to finish',
        'Quantizing gradients',
        'Using FP64',
      ],
      a: 1,
      x: [
        'Disabling collectives',
        'Sharding the model',
        'Lowering precision',
      ],
      e: 'Synchronous SGD waits for the slowest worker. Backup workers (compute more replicas than needed and proceed with the fastest N) reduce straggler impact, trading extra compute for reduced tail latency per step. (A classic data-center technique.)',
      ref: 'Backup workers; straggler mitigation',
      tags: ['stragglers', 'backup-workers', 'synchronous'],
    },
    {
      d: 5,
      q: 'Why can communication-overlap eventually fail to hide all-reduce as models scale, even with bucketing?',
      o: [
        'Bucketing is broken',
        'All-reduce outlasts the backward it hides in',
        'Overlap always works',
        'It uses FP64',
      ],
      a: 1,
      x: [
        'NCCL disables overlap',
        'The optimizer blocks it',
        'It needs more streams only',
      ],
      e: 'Overlap hides comm only while compute lasts. If communication grows (bigger gradients, slower/larger network) or compute per step shrinks, the collective outlasts the backward pass and becomes exposed — the fundamental limit of overlap-based scaling.',
      ref: 'Communication overlap limits; scaling',
      tags: ['overlap', 'all-reduce', 'scaling-limit'],
    },
  ],
  { idPrefix: 'distributed-d' }
)
