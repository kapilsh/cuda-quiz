import { defineQuestions } from './_helpers.js'

// Synchronization & Atomics — batch F. Contention/aggregation scenarios, ordering
// bugs, mbarrier/cluster cases, determinism. Distinct from synchronization.js/.b/.c/.d/.e.
export default defineQuestions(
  'synchronization',
  [
    {
      d: 4,
      q: 'A histogram with 256 bins and 1M elements does one global atomicAdd per element. Switching to per-block shared histograms then merging reduces global atomics by roughly…',
      o: [
        'No change',
        'From ~1M to ~numBlocks × 256 atomics',
        '2×',
        'It increases them',
      ],
      a: 1,
      x: ['10×', 'Half', 'Unbounded'],
      e: 'Privatization moves the 1M contended global atomics into shared memory (low-latency), and the global merge is only numBlocks × bins. For thousands of blocks that’s far fewer global atomics than per-element — drastically less contention.',
      ref: 'PMPP Ch. 9; histogram privatization',
      tags: ['histogram', 'privatization', 'atomics'],
    },
    {
      d: 5,
      q: 'Warp-aggregated atomics replace up to 32 same-target atomicAdds per warp with 1. The speedup is largest when…',
      o: [
        'Targets are all distinct',
        'Many lanes update the same address',
        'There is no contention',
        'Atomics are disabled',
      ],
      a: 1,
      x: [
        'Each lane is unique',
        'Using FP64',
        'For graphics',
      ],
      e: 'Aggregation (match/ballot + warp scan + one leader atomic) helps in proportion to how often warp lanes hit the same address. For a hot counter many lanes target, it approaches 32× fewer atomics; for all-distinct targets it offers little.',
      ref: 'Warp-aggregated atomics',
      tags: ['warp-aggregation', 'atomics', 'contention'],
    },
    {
      d: 4,
      q: 'Two counters that each thread increments lie 4 bytes apart (same cache line). Performance is poor due to…',
      o: [
        'A correctness bug',
        'False sharing on the cache line',
        'A deadlock',
        'Divergence',
      ],
      a: 1,
      x: [
        'Bank conflicts',
        'Register spilling',
        'A race',
      ],
      e: 'Distinct addresses keep results correct, but atomics serialize on the cache line, so adjacent hot counters contend (false sharing). Padding each to its own line eliminates the contention — a classic concurrency pitfall.',
      ref: 'False sharing; atomics',
      tags: ['false-sharing', 'atomics', 'padding'],
    },
    {
      d: 5,
      q: 'A spin-loop `while (atomicCAS(lock,0,1)!=0);` across all 32 lanes of a warp can livelock pre-Volta because…',
      o: [
        'Atomics fail',
        'The winner cannot exit while losers spin',
        'The lock overflows',
        'Shared memory is full',
      ],
      a: 1,
      x: [
        'CAS is too slow',
        'It needs FP64',
        'For graphics',
      ],
      e: 'Classic SIMT executes the warp in lockstep; the winner can’t advance past reconvergence while losers spin, and losers can’t win until the winner releases — a livelock. Volta’s independent scheduling helps, but warp-aware/lock-free designs are safer.',
      ref: 'SIMT locks; livelock',
      tags: ['spinlock', 'livelock', 'warp'],
    },
    {
      d: 4,
      q: 'A producer writes data then `ready=1` (plain store); a consumer spins on ready then reads data. The bug is…',
      o: [
        'None',
        'No release/acquire ordering on the flag',
        'The flag is too small',
        'Too many threads',
      ],
      a: 1,
      x: [
        'A deadlock',
        'A bank conflict',
        'Register spilling',
      ],
      e: 'The weak memory model allows reordering. Use release ordering on the flag store (so prior data writes are visible) and acquire on the load (so the data read isn’t hoisted before observing the flag) — the standard publication pattern.',
      ref: 'CUDA memory model; release/acquire',
      tags: ['release-acquire', 'ordering', 'bug'],
    },
    {
      d: 5,
      q: 'In an mbarrier-based TMA pipeline, the consumer waits on a barrier with an expected transaction count so that…',
      o: [
        'Threads are counted',
        'Wait completes when the expected bytes land',
        'The grid syncs',
        'The host signals',
      ],
      a: 1,
      x: [
        'Registers free',
        'A spinlock releases',
        'For graphics',
      ],
      e: 'Hopper mbarriers track a transaction (byte) count; the TMA copy updates it as bytes arrive. The consumer’s wait on that count ensures the tile is fully present before use — decoupling async data movement from compute cleanly.',
      ref: 'Hopper mbarrier; TMA transaction count',
      tags: ['mbarrier', 'tma', 'transaction-count'],
    },
    {
      d: 4,
      q: 'A reduction using atomicAdd into one float accumulator gives slightly different totals each run. This is because…',
      o: [
        'Atomics lose updates',
        'Varying order + non-associative FP rounding',
        'Overflow',
        'A race condition',
      ],
      a: 1,
      x: [
        'Atomics are inexact',
        'The GPU is faulty',
        'Bank conflicts',
      ],
      e: 'Atomicity is guaranteed (no lost updates), but the combination order is nondeterministic and FP addition isn’t associative, so the rounded sum varies. For reproducibility, use a fixed-order tree reduction instead of atomics.',
      ref: 'FP atomic nondeterminism',
      tags: ['atomics', 'determinism', 'floating-point'],
    },
    {
      d: 4,
      q: 'A device-set flag the HOST polls (mapped memory) isn’t seen by the host. The device should publish it with…',
      o: [
        '__threadfence_block',
        '__threadfence_system() (visible to the host)',
        '__syncthreads',
        'No fence',
      ],
      a: 1,
      x: [
        '__threadfence() (device scope)',
        'A spinlock',
        'cudaDeviceReset',
      ],
      e: 'Device-scope fences only order visibility among GPU threads. Making a write to mapped/managed memory visible to the host needs system-scope ordering (__threadfence_system or a system-scope atomic) — otherwise the host may never observe the update.',
      ref: 'CUDA memory model; system scope',
      tags: ['threadfence-system', 'host-flag', 'mapped-memory'],
    },
    {
      d: 4,
      q: 'A CAS-retry loop building a custom atomic on a hot address shows throughput COLLAPSING under high contention because…',
      o: [
        'CAS is broken',
        'Contended CAS attempts fail and retry',
        'It deadlocks',
        'It overflows',
      ],
      a: 1,
      x: [
        'CAS needs FP64',
        'For graphics',
        'It loses updates',
      ],
      e: 'Under heavy contention, CAS loops thrash: each successful update invalidates others’ expected values, forcing retries. Throughput degrades toward serial. Reduce contention (privatize, warp-aggregate, hierarchical reduction) rather than hammering one address.',
      ref: 'Lock-free CAS; contention',
      tags: ['cas', 'contention', 'lock-free'],
    },
    {
      d: 4,
      q: 'A kernel calls __syncthreads() inside `if (threadIdx.x < 100)` in a 256-thread block. The result is…',
      o: [
        'Correct',
        'Deadlock: some threads never reach it',
        'Slower but correct',
        'A bank conflict',
      ],
      a: 1,
      x: [
        'A race',
        'Register spilling',
        'It skips the barrier',
      ],
      e: '__syncthreads requires every thread in the block to reach the same barrier. Guarding it so only some threads execute it leaves the rest waiting indefinitely (or yields undefined behavior). Restructure so all threads hit the barrier uniformly.',
      ref: 'CUDA C++ Programming Guide §7.6',
      tags: ['syncthreads', 'divergence', 'deadlock'],
    },
    {
      d: 5,
      q: 'A single-launch grid reduction writes partials, then `__threadfence(); atomicInc(&count)`. If you SWAP the order (atomicInc before fence), the bug is…',
      o: [
        'None',
        'A not-yet-visible partial may be read',
        'Slower only',
        'A bank conflict',
      ],
      a: 1,
      x: [
        'Overflow',
        'A deadlock',
        'Divergence',
      ],
      e: 'The counter increment signals "my partial is ready." If it precedes the fence, another block may observe completion and read a not-yet-visible partial (stale data). The fence must order the partial write before the completion signal.',
      ref: 'threadFenceReduction; ordering',
      tags: ['reduction', 'threadfence', 'ordering'],
    },
    {
      d: 5,
      q: 'cluster.sync() in a 4-block cluster blocks until…',
      o: [
        'One block arrives',
        'All threads of the 4 cluster blocks arrive',
        'The grid syncs',
        'The host signals',
      ],
      a: 1,
      x: [
        'One warp arrives',
        'Two GPUs sync',
        'Registers free',
      ],
      e: 'cluster.sync() synchronizes all threads across all blocks of the cluster (a tier between __syncthreads and grid sync). It’s possible because cluster blocks are guaranteed co-resident within a GPC, and pairs with DSMEM for cross-block data sharing.',
      ref: 'Hopper cluster.sync',
      tags: ['cluster-sync', 'clusters', 'hopper'],
    },
    {
      d: 4,
      q: 'For a grid-wide barrier in a 3-phase algorithm, choosing TWO kernel launches over a cooperative grid.sync barrier is better when…',
      o: [
        'Always',
        'State fits in global; want an unconstrained grid',
        'Never',
        'State is huge in registers',
      ],
      a: 1,
      x: [
        'For one block',
        'For FP64',
        'For graphics',
      ],
      e: 'A kernel launch boundary is an implicit grid-wide barrier with full visibility and no grid-size restriction. If you don’t need to keep state on-chip across the barrier, two launches (larger grid, simpler) often beat a cooperative grid.sync.',
      ref: 'Multi-launch vs grid.sync',
      tags: ['grid-sync', 'multi-launch', 'tradeoff'],
    },
    {
      d: 4,
      q: 'atomicMax on a positive float via atomicMax on its bitcast int works because…',
      o: [
        'Floats are ints',
        'Nonnegative float bits are monotonic',
        'It is always exact',
        'CAS is required',
      ],
      a: 1,
      x: [
        'It loses precision',
        'For FP64 only',
        'It overflows',
      ],
      e: 'Nonnegative floats have a bitwise-monotonic representation (larger value = larger int pattern), so atomicMax on the reinterpreted int matches float max. Negative values invert this, so a sign-aware approach (or a CAS loop) is needed for general floats.',
      ref: 'IEEE-754 ordering; atomic float max',
      tags: ['atomic-max', 'float', 'bit-trick'],
    },
    {
      d: 4,
      q: 'A leader-initializes-then-barrier pattern: `if(tid==0) init(s); __syncthreads();`. Removing the barrier causes…',
      o: [
        'No effect',
        'A race: others read s before it is set',
        'A deadlock',
        'Slower correct code',
      ],
      a: 1,
      x: [
        'A bank conflict',
        'Overflow',
        'Register spilling',
      ],
      e: 'Without the barrier, the non-leader threads race ahead and may read uninitialized/partial shared state. __syncthreads after the leader’s write ensures it’s complete and visible before any thread reads it.',
      ref: 'Leader-then-barrier; shared init',
      tags: ['leader', 'syncthreads', 'race'],
    },
    {
      d: 5,
      q: 'A double-buffered tile loop uses two __syncthreads per iteration. Removing the SECOND barrier causes…',
      o: [
        'No issue',
        'The next load overwrites a buffer still read',
        'A deadlock',
        'Slower correct code',
      ],
      a: 1,
      x: [
        'A bank conflict',
        'Overflow',
        'Divergence',
      ],
      e: 'The two barriers bracket the read phase: one after the load (data ready), one after the compute (reads done) before reuse. Dropping the second lets the producer overwrite the tile mid-read — a race. Both are required for correctness.',
      ref: 'Tiled GEMM; double-buffer barriers',
      tags: ['syncthreads', 'double-buffer', 'race'],
    },
    {
      d: 4,
      q: 'A seqlock reader reads the sequence counter before AND after the data; it must RETRY when…',
      o: [
        'The counter is even',
        'The counter changed or is odd',
        'The data is zero',
        'Never',
      ],
      a: 1,
      x: [
        'The counter is large',
        'The reader is slow',
        'For FP64',
      ],
      e: 'A writer makes the counter odd, updates, then even. A reader brackets its read with the counter; if the counter differs (or is odd), a write intervened and the read may be torn — so it retries. Proper fences are needed for the ordering to hold.',
      ref: 'Seqlock; reader retry',
      tags: ['seqlock', 'readers', 'retry'],
    },
    {
      d: 4,
      q: 'For a global counter incremented by every thread, reading it correctly as the FINAL total requires…',
      o: [
        'Just atomicAdd',
        'A grid-wide sync after all increments',
        'A spinlock',
        'Constant memory',
      ],
      a: 1,
      x: [
        '__syncthreads',
        'A fence only',
        'Nothing',
      ],
      e: 'Atomicity ensures no lost increments, but there’s no point at which all blocks have finished unless you synchronize the grid. Read the total after grid.sync() or in a second kernel — not mid-kernel (which sees a partial sum).',
      ref: 'Grid-wide ordering; counters',
      tags: ['atomics', 'grid-ordering', 'counter'],
    },
    {
      d: 5,
      q: 'cuda::atomic with thread_scope_block is cheaper than device scope because…',
      o: [
        'It is non-atomic',
        'Block-only guarantees, resolved in the SM',
        'It spans the grid',
        'It uses the host',
      ],
      a: 1,
      x: [
        'It is slower',
        'It needs cooperative launch',
        'For graphics',
      ],
      e: 'Scoping to the block lets the hardware satisfy atomicity/ordering within the SM rather than at the device-wide L2 coherence point, which is cheaper. Match the scope to the actual sharing (block/cluster/device/system) for performance.',
      ref: 'libcu++ thread scopes',
      tags: ['atomics', 'block-scope', 'performance'],
    },
    {
      d: 4,
      q: 'Why does using __syncwarp() between warp-shuffle steps matter on Volta+ but "worked" on Pascal without it?',
      o: [
        'Shuffles changed',
        'Volta drops implicit lockstep',
        'It is faster',
        'For FP64',
      ],
      a: 1,
      x: [
        'Pascal had no shuffles',
        'It avoids divergence',
        'For graphics',
      ],
      e: 'Warp-synchronous code relied on implicit lockstep pre-Volta. With independent thread scheduling, lanes may not step together, so reads can race writes between shuffle steps. Use __syncwarp / the _sync intrinsics with correct masks to ensure participation.',
      ref: 'Volta ITS; warp-synchronous code',
      tags: ['syncwarp', 'shuffle', 'volta'],
    },
    {
      d: 4,
      q: 'A producer/consumer ring buffer in shared memory needs, besides head/tail indices…',
      o: [
        'Nothing',
        'Order the data write before the index',
        'A global lock',
        'Constant memory',
      ],
      a: 1,
      x: [
        'A reduction',
        'Texture memory',
        'FP64',
      ],
      e: 'The indices coordinate availability, but you also need ordering so the data write "happens before" the consumer’s read (release on produce / acquire on consume, or barriers). Otherwise the consumer can see an updated index but stale data.',
      ref: 'Ring buffer; ordering',
      tags: ['ring-buffer', 'ordering', 'producer-consumer'],
    },
    {
      d: 5,
      q: 'In a multi-stage pipeline, why use a separate mbarrier per stage rather than one for the whole pipeline?',
      o: [
        'For speed only',
        'Per-stage barriers let stages overlap',
        'To sync the grid',
        'To reduce registers',
      ],
      a: 1,
      x: [
        'For divergence',
        'For the host',
        'For graphics',
      ],
      e: 'Independent per-stage barriers decouple production and consumption so different stages proceed concurrently (deep pipelining). One global barrier would synchronize all stages together, removing the overlap that hides memory latency.',
      ref: 'CUTLASS pipelining; per-stage barriers',
      tags: ['mbarrier', 'pipeline', 'overlap'],
    },
    {
      d: 4,
      q: 'A relaxed-atomic counter read AFTER a grid barrier gives the correct total because…',
      o: [
        'Relaxed implies ordering',
        'Increments are atomic; barrier makes them visible',
        'It needs seq_cst',
        'It loses updates',
      ],
      a: 1,
      x: [
        'Relaxed is non-atomic',
        'The compiler serializes it',
        'For graphics',
      ],
      e: 'Relaxed atomics are still atomic; they just don’t order surrounding memory ops. Once a barrier ensures all increments happened and are visible, the read sees the correct sum — no need for stronger ordering for a pure counter read after the barrier.',
      ref: 'memory_order_relaxed; counters',
      tags: ['atomics', 'relaxed', 'grid-barrier'],
    },
    {
      d: 5,
      q: 'A lock-free stack push (CAS on head) is correct but suffers the ABA problem when…',
      o: [
        'The stack is empty',
        'A reused address lets a stale CAS succeed',
        'Two pushes race',
        'The head overflows',
      ],
      a: 1,
      x: [
        'A fence is missing',
        'It deadlocks',
        'For FP64',
      ],
      e: 'CAS checks only the value, not its history. If the head goes A→B→A (address reused), a stale CAS succeeds though the structure changed underneath. A version-tagged head (pointer + counter) makes the value differ even when the address repeats.',
      ref: 'ABA problem; lock-free stack',
      tags: ['aba', 'lock-free', 'cas'],
    },
    {
      d: 4,
      q: 'Why is reducing __syncthreads frequency (e.g. via double buffering) a performance win?',
      o: [
        'Barriers corrupt data',
        'Each barrier waits for the slowest warp',
        'It uses fewer registers',
        'It disables coalescing',
      ],
      a: 1,
      x: [
        'It flushes the L2',
        'It increases divergence',
        'For graphics',
      ],
      e: 'Barriers are synchronization points where warps wait for stragglers. Cutting their number (e.g. double-buffering to overlap the next load with current compute) reduces stall time — a common tiled-kernel optimization.',
      ref: 'Double buffering; barrier reduction',
      tags: ['syncthreads', 'double-buffer', 'optimization'],
    },
    {
      d: 5,
      q: 'A cuda::pipeline consumer must call consumer_release() after using a stage, or…',
      o: [
        'Nothing happens',
        'The producer stalls with no free buffer',
        'It runs faster',
        'It loses data',
      ],
      a: 1,
      x: [
        'It syncs the grid',
        'It uses the host',
        'For FP64',
      ],
      e: 'The pipeline tracks buffer availability; consumer_release frees a stage for the producer to reuse. Omitting it leaves all stages occupied, so the producer waits forever for a free buffer — a back-pressure deadlock.',
      ref: 'cuda::pipeline; consumer_release',
      tags: ['pipeline', 'consumer-release', 'deadlock'],
    },
    {
      d: 4,
      q: 'A global spinlock guarding a critical section that every thread enters makes a 100k-thread kernel…',
      o: [
        'Faster',
        'Effectively serial; SIMT can livelock',
        'More accurate',
        'Lower memory',
      ],
      a: 1,
      x: [
        'Higher occupancy',
        'Coalesced',
        'For graphics',
      ],
      e: 'A single global lock serializes the whole kernel and risks SIMT livelock/deadlock — the opposite of what GPUs are for. Replace mutual exclusion with privatization, lock-free structures, or reductions that avoid a global serialization point.',
      ref: 'Global lock anti-pattern',
      tags: ['spinlock', 'serialization', 'anti-pattern'],
    },
    {
      d: 4,
      q: 'A flag-based handshake works on one GPU but breaks when the producer and consumer are on DIFFERENT GPUs because…',
      o: [
        'GPUs cannot share memory',
        'Device scope is not visible across GPUs',
        'It is always fine',
        'A bank conflict',
      ],
      a: 1,
      x: [
        'Divergence',
        'Register spilling',
        'For graphics',
      ],
      e: 'Cross-GPU sharing requires system-scope atomics/fences (and appropriate P2P/managed memory) for the writes to be visible across devices. Device-scope ordering only covers one GPU’s threads, so a device-scope handshake fails across GPUs.',
      ref: 'System scope; multi-GPU handshake',
      tags: ['system-scope', 'multi-gpu', 'handshake'],
    },
    {
      d: 5,
      q: 'Why does a hierarchical reduction (warp shuffle → block shared → few global atomics) scale far better than per-thread global atomics?',
      o: [
        'It is less accurate',
        'Combines locally; few global atomics',
        'It uses more memory',
        'It needs the host',
      ],
      a: 1,
      x: [
        'It avoids reduction',
        'For FP64',
        'For graphics',
      ],
      e: 'Each tier reduces the number of contended operations: shuffles within a warp (no shared/atomic), shared+barrier within a block, then a handful of global atomics. This avoids the serialization of thousands of threads hammering global memory.',
      ref: 'Hierarchical reduction; contention',
      tags: ['reduction', 'hierarchical', 'contention'],
    },
  ],
  { idPrefix: 'synchronization-f' }
)
