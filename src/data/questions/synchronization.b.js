import { defineQuestions } from './_helpers.js'

// Synchronization & Atomics — batch B. Atomic variety, barrier-with-predicate,
// scopes, memory order, locks. Distinct from synchronization.js (batch A).
export default defineQuestions(
  'synchronization',
  [
    {
      d: 2,
      q: 'Which atomic returns the old value and stores a new one unconditionally (a swap)?',
      o: ['atomicAdd', 'atomicExch', 'atomicMax', 'atomicCAS'],
      a: 1,
      x: ['atomicOr', 'atomicSwap', 'atomicStore'],
      e: 'atomicExch(addr, val) atomically writes val and returns the previous value. It is handy for claiming slots or implementing locks (exchange a flag and inspect the old state).',
      ref: 'CUDA C++ Programming Guide §B.14',
      tags: ['atomics', 'exchange'],
    },
    {
      d: 3,
      q: 'Native atomicAdd on double precision is available starting at which compute capability?',
      o: ['2.0', '3.5', '6.0 (Pascal)', '8.0 (Ampere)'],
      a: 2,
      x: ['1.3', '5.0', '7.0'],
      e: 'Hardware atomicAdd for double was added in Pascal (CC 6.0). Before that you emulated it with an atomicCAS loop on the 64-bit bit pattern. Float atomicAdd has been available much longer.',
      ref: 'CUDA C++ Programming Guide §B.14, Compute Capabilities',
      tags: ['atomics', 'double', 'compute-capability'],
    },
    {
      d: 3,
      q: 'atomicInc(addr, val) has special wraparound semantics:',
      o: [
        'It simply adds 1',
        'Wraps to 0 at val (ring-buffer index)',
        'It decrements',
        'It saturates at val',
      ],
      a: 1,
      x: [
        'It multiplies by val',
        'It returns the new value, not the old',
        'It only works on signed ints',
      ],
      e: 'atomicInc increments but wraps to 0 once it reaches val, giving a free modular counter (e.g. circular buffer head). atomicDec wraps in the opposite direction. Both return the old value.',
      ref: 'CUDA C++ Programming Guide §B.14',
      tags: ['atomics', 'atomicInc', 'wraparound'],
    },
    {
      d: 3,
      q: '__syncthreads_count(predicate) returns…',
      o: [
        'Nothing; it just synchronizes',
        'Count of threads with predicate true (after barrier)',
        'The first thread index with predicate true',
        'A per-warp count',
      ],
      a: 1,
      x: [
        'The sum of the predicate values',
        'A boolean for the calling thread',
        'The block size',
      ],
      e: '__syncthreads_count barriers the block and returns the count of true predicates across all threads. Variants __syncthreads_and/_or return whether all/any threads’ predicates are true — handy block-wide reductions of a condition.',
      ref: 'CUDA C++ Programming Guide §B.6',
      tags: ['syncthreads', 'barrier', 'reduction'],
    },
    {
      d: 4,
      q: 'atomicAdd_system (system-scope atomic) is needed when…',
      o: [
        'Many threads update shared memory',
        'It must be visible to the host CPU or peer GPUs',
        'You want faster atomics',
        'The value is a double',
      ],
      a: 1,
      x: [
        'You update constant memory',
        'You use a single thread',
        'The address is in registers',
      ],
      e: 'The _system scope extends atomicity/visibility across the whole system (CPU + GPUs) for memory they share (managed/mapped). Plain device-scope atomics only coordinate threads on the same GPU. There is also a _block scope.',
      ref: 'CUDA C++ Programming Guide §B.14 (scoped atomics)',
      tags: ['atomics', 'system-scope', 'unified-memory'],
    },
    {
      d: 2,
      q: 'Why are atomics to SHARED memory generally faster than the same atomics to GLOBAL memory?',
      o: [
        'Shared atomics are not really atomic',
        'On-chip shared memory has much lower latency',
        'Global atomics are emulated',
        'Shared memory has more banks',
      ],
      a: 1,
      x: [
        'Global atomics require a kernel relaunch',
        'Shared atomics use tensor cores',
        'Global memory has no atomic unit',
      ],
      e: 'On-chip shared memory atomics resolve quickly; that is why privatization (accumulate in shared, then one global atomic per bin) is a standard optimization for histograms and reductions.',
      ref: 'PMPP Ch. 9; Best Practices Guide',
      tags: ['atomics', 'shared-memory', 'privatization'],
    },
    {
      d: 4,
      q: 'The CUDA memory consistency model is "weak/relaxed." A practical consequence is that…',
      o: [
        'All memory operations are globally ordered automatically',
        'Writes can become visible out of program order',
        'Shared memory is never coherent',
        'Atomics are unnecessary',
      ],
      a: 1,
      x: [
        'Loads are always cached',
        'Every write is immediately global',
        'The compiler never reorders',
      ],
      e: 'A weak model permits reordering and delayed visibility for performance. Correct cross-thread communication needs explicit ordering: memory fences (__threadfence*) or atomics with acquire/release semantics. Don’t assume sequential consistency.',
      ref: 'CUDA C++ Programming Guide (Memory Consistency Model)',
      tags: ['memory-model', 'consistency', 'ordering'],
    },
    {
      d: 5,
      q: 'In libcu++/CUDA C++ atomics, a load with cuda::memory_order_acquire paired with a store with memory_order_release guarantees…',
      o: [
        'Nothing in particular',
        'A release/acquire happens-before handshake',
        'Faster atomics',
        'Lower register usage',
      ],
      a: 1,
      x: [
        'Sequential consistency for all threads',
        'That the store is non-atomic',
        'That the load blocks until the store',
      ],
      e: 'Release/acquire establishes a happens-before edge: data written before the release becomes visible after the matching acquire reads the published value — the standard lock-free publication pattern, now expressible portably with cuda::atomic.',
      ref: 'libcu++ <cuda/atomic>; C++ memory model',
      tags: ['atomics', 'memory-order', 'acquire-release'],
    },
    {
      d: 4,
      q: 'A simple spin-lock on the GPU acquires with a loop like `while (atomicCAS(lock, 0, 1) != 0);` and must release with…',
      o: [
        'Just lock = 0;',
        '__threadfence(); atomicExch(lock, 0)',
        'cudaDeviceSynchronize()',
        'Nothing; CAS handles it',
      ],
      a: 1,
      x: [
        'A second atomicCAS',
        '__syncthreads()',
        'cudaMemset on the lock',
      ],
      e: 'Without a fence before releasing, the critical-section writes might not be visible to the next acquirer. Release the lock with a fence + atomic store. Note: lock-based code across a warp can deadlock if lanes contend — prefer lock-free designs.',
      ref: 'CUDA C++ Programming Guide; lock-free programming on GPUs',
      tags: ['spinlock', 'atomicCAS', 'threadfence'],
    },
    {
      d: 4,
      q: 'Writing a spin-lock so that all 32 lanes of a warp try to grab the SAME lock can deadlock (especially pre-Volta) because…',
      o: [
        'Locks are illegal in CUDA',
        'The holder waits at reconvergence for spinning lanes',
        'atomicCAS is too slow',
        'Shared memory is unavailable',
      ],
      a: 1,
      x: [
        'Warps cannot use atomics',
        'The lock variable overflows',
        'Only blocks can hold locks',
      ],
      e: 'Under lockstep SIMT, the winning lane can’t exit the critical section while losing lanes still loop, and the losers can’t win until the winner releases — a livelock/deadlock. Volta’s independent scheduling helps but warp-aware/lock-free designs are safer.',
      ref: 'CUDA C++ Programming Guide §4.2; Volta independent thread scheduling',
      tags: ['deadlock', 'warp', 'spinlock'],
    },
    {
      d: 2,
      q: 'After threads write to shared memory and before a different set of threads reads those values, you place __syncthreads() because it acts as…',
      o: [
        'Only a performance hint',
        'Both a barrier and a memory barrier',
        'A no-op in release builds',
        'A warp-level fence',
      ],
      a: 1,
      x: [
        'A global device fence',
        'An atomic operation',
        'A cache flush to host',
      ],
      e: '__syncthreads not only waits for all threads but also orders memory: writes issued before the barrier are visible to all block threads after it. This is why tiled algorithms place it between the load and compute phases.',
      ref: 'CUDA C++ Programming Guide §7.6, §B.5',
      tags: ['syncthreads', 'memory-barrier', 'shared-memory'],
    },
    {
      d: 4,
      q: 'cuda::barrier (an arrive/wait barrier from libcu++) generalizes __syncthreads by allowing…',
      o: [
        'Only block-wide sync',
        'Separate arrive() and wait() phases',
        'Synchronization across GPUs',
        'Lower precision',
      ],
      a: 1,
      x: [
        'Faster atomics',
        'Grid-wide barriers without cooperative launch',
        'Automatic kernel fusion',
      ],
      e: 'An arrive/wait barrier splits the barrier into arrive() and wait() phases, enabling pipelined producer/consumer patterns and tracking completion of asynchronous copies (mbarrier on Hopper) — more flexible than the all-or-nothing __syncthreads.',
      ref: 'CUDA C++ Programming Guide (Asynchronous Barrier); libcu++',
      tags: ['barrier', 'arrive-wait', 'pipelining'],
    },
    {
      d: 3,
      q: 'For a counter that many threads increment but whose intermediate order does not matter, using a relaxed atomic (memory_order_relaxed) instead of a stronger one…',
      o: [
        'Breaks correctness',
        'Fine and cheaper; only atomicity is needed',
        'Is impossible on GPUs',
        'Forces sequential consistency',
      ],
      a: 1,
      x: [
        'Makes the increment non-atomic',
        'Requires a fence anyway',
        'Disables the L2 cache',
      ],
      e: 'Relaxed atomics guarantee the operation is atomic but impose no ordering on surrounding memory ops. For pure counters/accumulators where you only read the total after a barrier, relaxed ordering avoids unnecessary fence cost.',
      ref: 'libcu++ <cuda/atomic>; memory_order_relaxed',
      tags: ['atomics', 'relaxed', 'counter'],
    },
    {
      d: 3,
      q: 'A block-level reduction in shared memory halves the active stride each step (s = blockDim/2, s/=2) with a __syncthreads() inside the loop. The barrier is required because…',
      o: [
        'It speeds up the loop',
        'Each step reads writes from the previous step',
        'It avoids atomics',
        'Shared memory is read-only otherwise',
      ],
      a: 1,
      x: [
        'It reduces divergence',
        'It coalesces the writes',
        'It is only needed once',
      ],
      e: 'Step k reads partial sums produced at step k−1 by different threads. Without a barrier between steps, a thread could read a not-yet-updated (or partially-updated) value, corrupting the reduction.',
      ref: 'Harris, "Optimizing Parallel Reduction"; PMPP Ch. 10',
      tags: ['reduction', 'syncthreads', 'shared-memory'],
    },
    {
      d: 4,
      q: 'To implement atomicMax on a float (no native float atomicMax historically), a common trick is…',
      o: [
        'Use atomicAdd with the max',
        'atomicMax on the float bit pattern as int',
        'It is impossible',
        'Use a double instead',
      ],
      a: 1,
      x: [
        'Cast to double and back',
        'Use atomicExch in a loop',
        'Store the float in shared memory',
      ],
      e: 'For nonnegative floats, the IEEE-754 bit pattern is monotonic, so atomicMax on the bitcast int works; negatives need care (their ordering reverses). Otherwise an atomicCAS retry loop on the float bits implements any custom atomic.',
      ref: 'CUDA atomics tricks; IEEE-754 ordering',
      tags: ['atomics', 'atomicMax', 'float', 'bit-tricks'],
    },
    {
      d: 5,
      q: 'On Hopper, an mbarrier tracks an "arrive count" AND a "transaction count" so that…',
      o: [
        'It can count warps only',
        'Wait until N bytes (e.g. a TMA copy) land',
        'It replaces all atomics',
        'It synchronizes across GPUs',
      ],
      a: 1,
      x: [
        'It compresses shared memory',
        'It schedules blocks',
        'It counts instructions',
      ],
      e: 'The transaction-count feature lets a TMA copy signal completion by the number of bytes transferred. Consumer threads wait on the mbarrier phase until the expected transaction bytes arrive, cleanly synchronizing async data movement with compute.',
      ref: 'CUDA C++ Programming Guide (mbarrier, TMA); Hopper Whitepaper',
      tags: ['mbarrier', 'tma', 'hopper', 'async'],
    },
    {
      d: 3,
      q: 'Two threads atomicAdd to two DIFFERENT counters that happen to lie in the same 128-byte cache line. The effect is…',
      o: [
        'A correctness bug',
        'Correct, but false-sharing contention',
        'A deadlock',
        'Undefined behavior',
      ],
      a: 1,
      x: [
        'No effect whatsoever',
        'Automatic merging of the counters',
        'A compile error',
      ],
      e: 'Distinct addresses keep results correct, but atomics serialize at line granularity, so counters sharing a cache line contend (false sharing). Padding hot counters to separate lines can restore throughput.',
      ref: 'GPU performance literature; false sharing',
      tags: ['atomics', 'false-sharing', 'contention'],
    },
    {
      d: 2,
      q: 'Which of these is NOT a valid integer atomic in CUDA?',
      o: ['atomicAnd', 'atomicOr', 'atomicXor', 'atomicDiv'],
      a: 3,
      x: ['atomicMin', 'atomicSub', 'atomicExch'],
      e: 'CUDA provides atomicAnd/Or/Xor, atomicMin/Max, atomicAdd/Sub, atomicExch, atomicCAS, atomicInc/Dec — but no atomicDiv. Division as an atomic isn’t provided (and would be unusual); build custom RMW with atomicCAS if ever needed.',
      ref: 'CUDA C++ Programming Guide §B.14',
      tags: ['atomics', 'bitwise'],
    },
    {
      d: 4,
      q: 'A flag-based handshake: producer writes data then sets a flag; consumer spins on the flag then reads data. To be correct the producer needs ___ and the consumer needs ___.',
      o: [
        'Nothing / nothing',
        'Release fence before the flag / acquire before the data',
        'Only __syncthreads / only atomics',
        'A larger block / more registers',
      ],
      a: 1,
      x: [
        'cudaDeviceSynchronize / cudaMemcpy',
        'A spinlock / a mutex',
        'Constant memory / texture memory',
      ],
      e: 'Without a release fence before the flag store, the data write may be reordered after the flag, so the consumer reads stale data. The consumer needs acquire ordering so the data read isn’t hoisted before observing the flag. This is the classic release/acquire pattern.',
      ref: 'CUDA C++ Programming Guide §B.5; memory consistency',
      tags: ['handshake', 'threadfence', 'ordering'],
    },
    {
      d: 3,
      q: 'For a zero-copy flag that the HOST polls to detect device completion, the device should publish it with…',
      o: [
        '__threadfence_block()',
        '__threadfence_system() (visible to host)',
        'No fence',
        '__syncthreads()',
      ],
      a: 1,
      x: [
        '__threadfence() (device scope only)',
        'atomicAdd to shared memory',
        'cudaDeviceReset()',
      ],
      e: 'Device-scope fences only order visibility among GPU threads. To make a write to mapped/managed memory visible to the host, you need system-scope ordering: __threadfence_system() (or a system-scope atomic).',
      ref: 'CUDA C++ Programming Guide §B.5 (__threadfence_system)',
      tags: ['threadfence-system', 'zero-copy', 'host-visibility'],
    },
    {
      d: 4,
      q: 'Why is a lock-free design (using atomicCAS retry loops) often preferred over locks on the GPU?',
      o: [
        'Locks are not supported',
        'Avoids warp deadlock; scales better',
        'Lock-free is always simpler',
        'Atomics are free',
      ],
      a: 1,
      x: [
        'Lock-free uses no memory',
        'Locks require cooperative launch',
        'Lock-free disables divergence',
      ],
      e: 'With tens of thousands of threads and SIMT execution, mutual-exclusion locks invite deadlock (warp lockstep) and heavy serialization. Lock-free CAS loops let threads retry independently and generally scale better, though they still serialize on contention.',
      ref: 'GPU concurrency literature; CUDA Best Practices',
      tags: ['lock-free', 'atomicCAS', 'scalability'],
    },
    {
      d: 2,
      q: 'atomicAdd returning the old value lets you implement a parallel append/compaction by…',
      o: [
        'Sorting the array first',
        'The old value gives a unique output slot',
        'Using shared memory only',
        'Calling it twice',
      ],
      a: 1,
      x: [
        'Broadcasting the value',
        'Using __syncthreads',
        'Reducing the array',
      ],
      e: 'pos = atomicAdd(&counter, 1); out[pos] = value; gives each appending thread a distinct slot. Warp-aggregating the atomic (one add per warp) greatly reduces contention for high-throughput compaction.',
      ref: 'PMPP Ch. 9; stream compaction',
      tags: ['atomics', 'compaction', 'append'],
    },
    {
      d: 5,
      q: 'In a producer/consumer kernel using a multi-stage shared-memory buffer, how many mbarriers/phases are typically needed?',
      o: [
        'One total',
        'One full/empty pair per buffer stage',
        'One per thread',
        'None',
      ],
      a: 1,
      x: [
        'One per warp',
        'One per block in the grid',
        'Two per element',
      ],
      e: 'Multi-stage (double/triple-buffered) pipelines track each stage’s fill/drain with barriers so the producer can load stage k+1 while the consumer computes on stage k. The number of stages trades shared memory for latency hiding.',
      ref: 'CUTLASS pipelining; CUDA async barriers',
      tags: ['pipelining', 'mbarrier', 'double-buffering'],
    },
    {
      d: 3,
      q: '__syncthreads() placed so that threads in a block reach DIFFERENT __syncthreads() calls (e.g. two barriers in opposite branches) is…',
      o: [
        'Perfectly fine',
        'Undefined; all threads must hit the same barrier',
        'A way to synchronize warps separately',
        'Faster than one barrier',
      ],
      a: 1,
      x: [
        'A method to split the block',
        'Required for divergence',
        'Equivalent to __syncwarp',
      ],
      e: 'Every thread in the block must execute the same __syncthreads() instance. Threads splitting to different barriers (or only some reaching one) is undefined and can hang. Barriers must be in uniform control flow across the block.',
      ref: 'CUDA C++ Programming Guide §7.6',
      tags: ['syncthreads', 'divergence', 'undefined-behavior'],
    },
    {
      d: 4,
      q: 'Named barriers (PTX bar.sync with an id and thread count) enable…',
      o: [
        'Grid-wide barriers',
        'Subsets of a block sync independently',
        'Cross-GPU sync',
        'Atomic operations',
      ],
      a: 1,
      x: [
        'Faster __syncthreads',
        'Synchronizing the host',
        'Reducing register usage',
      ],
      e: 'Beyond the default barrier 0 used by __syncthreads, the hardware offers multiple named barriers, each with its own participant count. This lets disjoint groups of warps in a block synchronize separately — essential for warp-specialized pipelines.',
      ref: 'PTX ISA (bar.sync/barrier); warp specialization',
      tags: ['named-barriers', 'warp-specialization', 'ptx'],
    },
    {
      d: 2,
      q: 'Reading a value written by another thread in the same warp via shared memory still needs ordering because…',
      o: [
        'Warps never share data',
        'Lanes need __syncwarp under ITS',
        'Shared memory is per-thread',
        'It always works without care',
      ],
      a: 1,
      x: [
        'Shared memory is read-only',
        'Atomics are required for all reads',
        'The L2 cache is involved',
      ],
      e: 'Pre-Volta "warp-synchronous" code assumed implicit lockstep; Volta+ can interleave diverged lanes. Use __syncwarp() between the write and read (or shuffle intrinsics that move data directly) to be correct.',
      ref: 'CUDA C++ Programming Guide §4.2, §B.6',
      tags: ['warp', 'syncwarp', 'shared-memory'],
    },
    {
      d: 3,
      q: 'A grid-wide reduction implemented as two kernel launches (partial reduce, then reduce the partials) avoids inter-block synchronization by…',
      o: [
        'Using atomics for everything',
        'The kernel boundary acts as a global barrier',
        'Using __syncthreads across blocks',
        'Running on two GPUs',
      ],
      a: 1,
      x: [
        'Using a spinlock',
        'Using constant memory',
        'Using dynamic parallelism',
      ],
      e: 'A kernel launch is an implicit device-wide barrier: everything in launch N completes before launch N+1’s effects begin. Splitting a reduction into two launches sidesteps the need for grid sync within a single kernel.',
      ref: 'PMPP Ch. 10; multi-pass reductions',
      tags: ['reduction', 'kernel-boundary', 'global-barrier'],
    },
    {
      d: 4,
      q: 'Using atomicAdd into a single global accumulator from every thread of a large grid is correct but slow primarily because…',
      o: [
        'Floating-point addition is non-associative',
        'All updates serialize on one location',
        'Atomics are not supported on global memory',
        'It deadlocks',
      ],
      a: 1,
      x: [
        'The accumulator overflows',
        'It requires __syncthreads',
        'Global memory is read-only',
      ],
      e: 'One hot address is a serialization point. Mitigate with hierarchical reduction: warp/block-level shared reductions, then a few global atomics — or a two-pass reduction. (Note FP atomics also make results order-dependent, hence slightly non-deterministic.)',
      ref: 'PMPP Ch. 9–10; Best Practices Guide',
      tags: ['atomics', 'contention', 'reduction'],
    },
    {
      d: 5,
      q: 'Floating-point atomicAdd makes a parallel sum non-deterministic across runs because…',
      o: [
        'Atomics can lose updates',
        'Varying add order + non-associative FP rounding',
        'The hardware is buggy',
        'It overflows randomly',
      ],
      a: 1,
      x: [
        'Atomics use lower precision',
        'The L2 cache reorders bits',
        'Threads skip some additions',
      ],
      e: 'Atomicity guarantees no lost updates, but the nondeterministic accumulation order combined with non-associative FP rounding yields slightly different sums each run. Deterministic reductions use fixed-order tree reductions instead.',
      ref: 'Floating-point reproducibility; PMPP Ch. 10',
      tags: ['atomics', 'floating-point', 'determinism'],
    },
  ],
  { idPrefix: 'synchronization-b' }
)
