import { defineQuestions } from './_helpers.js'

// Numerics & Mixed Precision — batch E. FP6, QAT/STE, W8A8 vs W4A16, FP8
// attention, scaling strategies, format trade-offs. Distinct from precision.js/.b/.c/.d.
export default defineQuestions(
  'precision',
  [
    {
      d: 5,
      q: 'FP6 formats (e.g. E2M3 and E3M2) sit between FP4 and FP8 and are useful because…',
      o: [
        'They are integers',
        'More precision/range than FP4; smaller than FP8',
        'They have no exponent',
        'They are larger than FP8',
      ],
      a: 1,
      x: [
        'They are 16-bit',
        'They are lossless',
        'They are FP32',
      ],
      e: 'FP6 (6 bits) provides an intermediate precision/footprint between FP4 and FP8. With micro-scaling (MXFP6), it can hold accuracy where FP4 degrades too much while saving over FP8 — part of Blackwell’s low-precision toolkit.',
      ref: 'OCP MX; FP6 formats',
      tags: ['fp6', 'mxfp', 'precision'],
    },
    {
      d: 5,
      q: 'The Straight-Through Estimator (STE) in Quantization-Aware Training is used to…',
      o: [
        'Quantize the loss',
        'Pass gradients through round (as identity); QAT',
        'Skip the backward pass',
        'Use FP64',
      ],
      a: 1,
      x: [
        'Disable gradients',
        'Compress weights',
        'Shard the model',
      ],
      e: 'Rounding has zero/undefined gradient, blocking backprop. STE approximates its gradient as 1 (identity) within the clipping range, letting QAT train through fake-quant ops so the model learns weights robust to quantization.',
      ref: 'Straight-Through Estimator; QAT',
      tags: ['ste', 'qat', 'quantization'],
    },
    {
      d: 4,
      q: '"Fake quantization" in QAT means the forward pass…',
      o: [
        'Skips quantization',
        'Quant+dequant in FP; injects quant error',
        'Uses FP64',
        'Removes weights',
      ],
      a: 1,
      x: [
        'Quantizes the loss',
        'Disables gradients',
        'Shards the model',
      ],
      e: 'Fake-quant ops quantize and immediately dequantize, injecting the quantization error into the FP forward pass. The model adapts to this error during training (via STE gradients), so the deployed integer model retains accuracy.',
      ref: 'QAT; fake quantization',
      tags: ['fake-quantization', 'qat', 'training'],
    },
    {
      d: 4,
      q: 'In LLM quantization, "W8A8" vs "W4A16" refer to…',
      o: [
        'Two GPUs',
        'W8A8: 8-bit W+A (compute); W4A16: 4-bit W, FP16 A',
        'Two optimizers',
        'Two layers',
      ],
      a: 1,
      x: [
        'Two batch sizes',
        'Two precisions of the loss',
        'Two datasets',
      ],
      e: 'W8A8 quantizes both operands so INT8 matmul runs on tensor cores (good for compute-bound prefill). W4A16 quantizes only weights (4-bit) with FP16 activations — minimizing the weight memory/bandwidth that dominates decode, at the cost of dequant overhead.',
      ref: 'LLM quantization; W8A8 vs W4A16',
      tags: ['w8a8', 'w4a16', 'quantization', 'llm'],
    },
    {
      d: 5,
      q: 'W4A16 (4-bit weight-only) is favored for LLM DECODE specifically because…',
      o: [
        'Decode is compute-bound',
        'Decode is BW-bound on weights; 4-bit cuts ~4×',
        'It uses FP64',
        'It shards the model',
      ],
      a: 1,
      x: [
        'It removes the KV cache',
        'It disables attention',
        'Decode is parallel',
      ],
      e: 'Decode reads the full weights each step (bandwidth-bound); 4-bit weights shrink that dominant traffic ~4×. Activations stay FP16 (cheap relative to weights and accuracy-sensitive). The dequant cost is outweighed by the bandwidth savings.',
      ref: 'W4A16; LLM decode',
      tags: ['w4a16', 'decode', 'bandwidth'],
    },
    {
      d: 5,
      q: 'Running attention in FP8 is harder than FP8 GEMM because…',
      o: [
        'Attention has no matmul',
        'Score/softmax range too wide for FP8',
        'It uses FP64',
        'It is compute-free',
      ],
      a: 1,
      x: [
        'Attention is linear',
        'It has no scores',
        'It avoids accumulation',
      ],
      e: 'Softmax and the score range are numerically delicate; naive FP8 there loses accuracy. FP8 attention keeps the sensitive parts (softmax, accumulation) in higher precision and applies FP8 selectively (with careful scaling) to the robust matmuls.',
      ref: 'FP8 attention; numerical sensitivity',
      tags: ['fp8', 'attention', 'softmax'],
    },
    {
      d: 5,
      q: '"Current" scaling vs "delayed" scaling for FP8: current scaling…',
      o: [
        'Uses a fixed scale',
        'Current: amax now; delayed: history-based',
        'Uses no scale',
        'Uses FP64',
      ],
      a: 1,
      x: [
        'Is always worse',
        'Quantizes the loss',
        'Shards the model',
      ],
      e: 'Current scaling derives the scale from this tensor’s amax (best fit, but needs an amax pass). Delayed scaling reuses a rolling amax history to avoid that pass (faster, marginally less responsive to sudden range changes). It’s a speed/accuracy trade.',
      ref: 'Transformer Engine; current vs delayed scaling',
      tags: ['fp8', 'scaling', 'amax'],
    },
    {
      d: 4,
      q: 'Quantizing the KV cache to FP8 requires a scale because…',
      o: [
        'FP8 has no exponent',
        'FP8 narrow; K/V need per-tensor/head scale',
        'It is integer',
        'KV is constant',
      ],
      a: 1,
      x: [
        'It avoids attention',
        'It uses FP64',
        'KV is tiny',
      ],
      e: 'Like any FP8 tensor, the KV cache needs scaling to fit FP8’s small range. KV-cache FP8 (with suitable per-tensor/head scales) halves cache memory/bandwidth for decode, but mis-scaling degrades attention — so scale choice matters.',
      ref: 'FP8 KV cache; scaling',
      tags: ['fp8', 'kv-cache', 'scaling'],
    },
    {
      d: 4,
      q: 'For tensor cores, FP16 vs BF16 ACCURACY differs such that…',
      o: [
        'They are identical',
        'FP16: more mantissa; BF16: more exponent (wider)',
        'BF16 is more precise',
        'FP16 has more range',
      ],
      a: 1,
      x: [
        'FP16 is integer',
        'BF16 has no exponent',
        'They have equal mantissa',
      ],
      e: 'FP16 (10 mantissa bits) resolves in-range values more finely; BF16 (7 mantissa, 8 exponent) trades precision for FP32-like range. Training favors BF16’s robustness; some inference/accuracy-sensitive cases benefit from FP16’s finer mantissa where range is controlled.',
      ref: 'FP16 vs BF16; mantissa vs exponent',
      tags: ['fp16', 'bf16', 'accuracy'],
    },
    {
      d: 4,
      q: 'Percentile (or KL-divergence) calibration for INT8 is preferred over naive min/max when…',
      o: [
        'There are no outliers',
        'Outliers stretch scale; percentile clips them',
        'Values are uniform',
        'For FP64',
      ],
      a: 1,
      x: [
        'Always',
        'Never',
        'For weights only',
      ],
      e: 'Min/max sets the scale by the extreme value, wasting INT8 codes on rare outliers. Percentile/KL calibration clips outliers to a threshold, allocating the range to the bulk of the distribution — improving accuracy for outlier-heavy activations.',
      ref: 'INT8 calibration; percentile/KL',
      tags: ['calibration', 'int8', 'outliers'],
    },
    {
      d: 5,
      q: 'Activation outliers concentrated in specific CHANNELS (common in LLMs) motivate which quantization choice?',
      o: [
        'Per-tensor activation scale',
        'Per-channel/per-token handles outlier channels',
        'FP64',
        'No scaling',
      ],
      a: 1,
      x: [
        'Per-layer only',
        'Random scaling',
        'Integer-only',
      ],
      e: 'A single per-tensor scale is dominated by outlier channels. Per-channel/per-token scales (or SmoothQuant’s shift of difficulty to weights) preserve precision for normal channels — key to accurate INT8/FP8 activation quantization in LLMs.',
      ref: 'LLM.int8(); SmoothQuant; per-channel',
      tags: ['outliers', 'per-channel', 'activations'],
    },
    {
      d: 4,
      q: 'Why is INT8 attractive for the PREFILL phase but FP16/4-bit-weights often better for DECODE?',
      o: [
        'They are the same',
        'Prefill: compute-bound; decode: bandwidth-bound',
        'Decode is compute-bound',
        'For FP64',
      ],
      a: 1,
      x: [
        'Prefill is bandwidth-bound',
        'They use the CPU',
        'INT8 is for decode',
      ],
      e: 'Prefill’s big matmuls benefit from INT8 compute throughput (W8A8). Decode’s bottleneck is reading weights, so weight-only 4-bit (W4A16) minimizes that bandwidth while keeping activations FP16 — different phases, different optimal precision schemes.',
      ref: 'Prefill vs decode quantization',
      tags: ['int8', 'w4a16', 'prefill', 'decode'],
    },
    {
      d: 5,
      q: 'Mixed-precision iterative refinement using tensor cores (FP16/TF32 solve, FP64 residual) can achieve FP64-accurate solutions because…',
      o: [
        'FP16 is exact',
        'Low-prec solve; high-prec residuals recover',
        'It avoids the solve',
        'It uses integers',
      ],
      a: 1,
      x: [
        'It needs no FP64',
        'It is always exact',
        'It shards the matrix',
      ],
      e: 'The expensive factorization runs in fast low precision; the residual b−Ax (computed in higher precision) drives corrections that converge to high accuracy. This leverages tensor-core throughput for the heavy step while reaching FP64-level results — used in HPL-MxP.',
      ref: 'Mixed-precision iterative refinement; HPL-MxP',
      tags: ['iterative-refinement', 'tensor-cores', 'fp64'],
    },
    {
      d: 4,
      q: 'Error feedback in low-precision/compressed gradient training works by…',
      o: [
        'Discarding errors',
        'Compression error fed into next gradient',
        'Using FP64',
        'Skipping steps',
      ],
      a: 1,
      x: [
        'Disabling gradients',
        'Sharding the model',
        'Randomizing',
      ],
      e: 'Naive compression/quantization drops the residual each step, harming convergence. Error feedback stores the residual and adds it back next step, so the lost signal is recovered over time — essential for 1-bit/low-rank gradient methods.',
      ref: 'Error feedback; gradient compression',
      tags: ['error-feedback', 'compression', 'convergence'],
    },
    {
      d: 4,
      q: 'The MX (microscaling) block size of 32 elements is a compromise because…',
      o: [
        'It is arbitrary',
        'Finer blocks fit better; 32 balances overhead',
        'It uses FP64',
        'It removes scaling',
      ],
      a: 1,
      x: [
        'Larger is always better',
        'It is integer-only',
        'It avoids tensor cores',
      ],
      e: 'A per-block scale’s granularity trades accuracy (finer = better local range fit) against metadata overhead and hardware regularity. 32 elements is the OCP MX choice balancing these, aligning with warp/tile structures the tensor cores use.',
      ref: 'OCP MX spec; block size',
      tags: ['mx', 'block-size', 'scaling'],
    },
    {
      d: 4,
      q: 'Why keep the SOFTMAX in attention at FP32/BF16 even in an otherwise low-precision (FP8) pipeline?',
      o: [
        'For speed',
        'exp/sum range too wide for FP8 precision',
        'FP8 has no exp',
        'It is integer',
      ],
      a: 1,
      x: [
        'Softmax is linear',
        'For FP64 only',
        'It uses the CPU',
      ],
      e: 'Softmax’s exp/sum need range and precision FP8 can’t provide (overflow/underflow, coarse mantissa). Even in FP8 GEMM pipelines, the softmax (and accumulation) is computed in higher precision to keep attention numerically sound.',
      ref: 'FP8 attention; softmax precision',
      tags: ['softmax', 'fp8', 'attention'],
    },
    {
      d: 5,
      q: 'The reason FP8 E5M2 is paired with E4M3 (rather than using one format everywhere) reflects…',
      o: [
        'Hardware limits',
        'E4M3 fwd (precision); E5M2 bwd (range)',
        'Random choice',
        'Integer math',
      ],
      a: 1,
      x: [
        'They are identical',
        'For FP64',
        'To save memory only',
      ],
      e: 'FP8 has only 8 bits to split between range and precision. E4M3 favors precision (forward weights/activations); E5M2 favors range (backward gradients, wide magnitude). Matching each to its role is the standard FP8 training recipe.',
      ref: 'FP8 formats; E4M3/E5M2 roles',
      tags: ['fp8', 'e4m3', 'e5m2', 'roles'],
    },
    {
      d: 4,
      q: 'When deploying a QAT model, the deployed INT8 weights/activations…',
      o: [
        'Differ from training',
        'Fake-quant matches training; model adapted',
        'Are random',
        'Use FP64',
      ],
      a: 1,
      x: [
        'Are unquantized',
        'Need recalibration',
        'Are FP16',
      ],
      e: 'QAT trains with the deployment quantization (scales/rounding) baked into fake-quant ops, so the model learned to tolerate it. Exporting to true INT8 reproduces that behavior, giving higher accuracy than post-training quantization at the same bit-width.',
      ref: 'QAT deployment; accuracy',
      tags: ['qat', 'int8', 'deployment'],
    },
    {
      d: 3,
      q: 'A reason BF16 became the default training precision (over FP16) despite FP16’s finer mantissa is…',
      o: [
        'BF16 is faster',
        'BF16 range avoids FP16 overflow/scaling issues',
        'FP16 is unsupported',
        'BF16 is integer',
      ],
      a: 1,
      x: [
        'BF16 uses less memory',
        'FP16 has no exponent',
        'For FP64',
      ],
      e: 'At scale, FP16’s narrow range makes training finicky (loss scaling, overflow). BF16’s wide range "just works" without scaling, and the coarser mantissa is usually fine — so robustness and simplicity made BF16 the default for large-model training.',
      ref: 'BF16 vs FP16; training default',
      tags: ['bf16', 'fp16', 'training-default'],
    },
    {
      d: 5,
      q: 'Why does increasing the contraction dimension K in a low-precision matmul eventually require attention to accumulation precision?',
      o: [
        'K is irrelevant',
        'FP16/FP8 input errors accumulate over large K',
        'It overflows always',
        'It is exact',
      ],
      a: 1,
      x: [
        'K reduces error',
        'Accumulation is integer',
        'For FP64 only',
      ],
      e: 'FP32 accumulation controls sum-rounding error, but each product already carries input-precision (FP16/FP8) error; over a large K these accumulate (~√K to K). For huge K or sensitive problems, higher input precision or compensation may be needed.',
      ref: 'Low-precision GEMM; K-dependent error',
      tags: ['accumulation', 'k-dimension', 'error'],
    },
    {
      d: 4,
      q: 'A reason per-token activation quantization pairs naturally with per-channel weight quantization in LLM inference is…',
      o: [
        'They conflict',
        'Row×col scales dequant as outer product',
        'For FP64',
        'Random pairing',
      ],
      a: 1,
      x: [
        'Both per-tensor',
        'It avoids matmul',
        'For graphics',
      ],
      e: 'Per-token (row) activation scales and per-channel (column) weight scales mean the INT matmul’s result is dequantized by scale_token[i] × scale_channel[j] — a clean, accurate scheme that handles activation outliers (per token) and weight ranges (per channel).',
      ref: 'LLM quantization; per-token × per-channel',
      tags: ['per-token', 'per-channel', 'quantization', 'llm'],
    },
    {
      d: 5,
      q: 'The reason FP4 needs micro-scaling (per-block scale) but FP16 does not is rooted in…',
      o: [
        'FP4 is integer',
        'FP4’s ~16 values need per-block scale',
        'FP16 is integer',
        'They both need it',
      ],
      a: 1,
      x: [
        'FP4 has more bits',
        'FP16 has no exponent',
        'For FP64',
      ],
      e: 'With only a handful of FP4 codes and minimal range, one scale per tensor crushes accuracy; per-block (micro) scales adapt to local magnitude. FP16’s range/precision is sufficient that per-tensor (or no) scaling suffices for typical values.',
      ref: 'FP4 micro-scaling vs FP16',
      tags: ['fp4', 'microscaling', 'fp16', 'range'],
    },
    {
      d: 4,
      q: 'Why is the OPTIMIZER step (Adam update) typically NOT done in FP8 even when matmuls are FP8?',
      o: [
        'For speed',
        'FP8 too coarse for moments; FP32 masters',
        'It is compute-free',
        'For graphics',
      ],
      a: 1,
      x: [
        'FP8 is exact',
        'Adam has no state',
        'It uses integers',
      ],
      e: 'Optimizer updates need precision for small increments and moment estimates that FP8 can’t represent. FP8 accelerates the matmuls, but master weights and optimizer states remain FP32 (then sharded via ZeRO) — applying low precision only where robust.',
      ref: 'FP8 training; optimizer precision',
      tags: ['fp8', 'optimizer', 'master-weights'],
    },
    {
      d: 4,
      q: 'For numerically robust FP8 training, gradient tensors typically use E5M2 because…',
      o: [
        'They need precision',
        'E5M2 wide exponent covers gradient range',
        'They are integer',
        'For graphics',
      ],
      a: 1,
      x: [
        'They are constant',
        'For FP64',
        'They are tiny',
      ],
      e: 'The backward pass produces gradients with a broad magnitude range; E5M2 (5 exponent bits) represents that range, avoiding overflow/underflow that would corrupt the gradient. Forward weights/activations use E4M3 for precision instead.',
      ref: 'FP8 gradients; E5M2',
      tags: ['fp8', 'gradients', 'e5m2'],
    },
    {
      d: 5,
      q: 'A subtle FP8 training failure mode is "scale lag" with delayed scaling, where…',
      o: [
        'Nothing happens',
        'Stale scale misses spikes → overflow',
        'The model speeds up',
        'It uses FP64',
      ],
      a: 1,
      x: [
        'Gradients vanish',
        'It shards the model',
        'For graphics',
      ],
      e: 'Delayed scaling sets the scale from past amax; if the current magnitude spikes, the stale scale may overflow before updating. Mitigations: margin in the scale, clipping, faster amax response, or current scaling for tensors with volatile ranges.',
      ref: 'FP8 delayed scaling; scale lag',
      tags: ['fp8', 'delayed-scaling', 'overflow'],
    },
    {
      d: 4,
      q: 'Why might a model quantized to INT8 with PTQ lose accuracy that QAT recovers?',
      o: [
        'PTQ is exact',
        'PTQ: round trained model; QAT: train with quant',
        'QAT uses FP64',
        'They are identical',
      ],
      a: 1,
      x: [
        'PTQ retrains',
        'QAT skips quantization',
        'For graphics',
      ],
      e: 'PTQ quantizes after training (no adaptation), so accuracy can drop, especially at low bits. QAT inserts fake-quant during training (via STE) so the network learns weights resilient to quantization — generally higher accuracy than PTQ at the same bit-width.',
      ref: 'PTQ vs QAT; accuracy',
      tags: ['ptq', 'qat', 'accuracy'],
    },
    {
      d: 5,
      q: 'The HPL-MxP (mixed-precision HPL) benchmark exists alongside HPL (FP64) because…',
      o: [
        'They are the same',
        'Tensor-core throughput to FP64 accuracy',
        'It is slower',
        'It avoids matmul',
      ],
      a: 1,
      x: [
        'It uses integers',
        'It is FP16 only',
        'For graphics',
      ],
      e: 'HPL-MxP solves the same dense system but uses low-precision tensor-core factorization plus iterative refinement to reach FP64 accuracy — gauging mixed-precision capability. It complements classic HPL (FP64) to reflect AI-era hardware in HPC rankings.',
      ref: 'HPL-MxP benchmark',
      tags: ['hpl-mxp', 'mixed-precision', 'iterative-refinement'],
    },
    {
      d: 4,
      q: 'When converting weights to FP8 with a per-tensor scale, choosing the scale so amax maps just BELOW the format max (with a small margin) is done to…',
      o: [
        'Waste range',
        'Amax just below max: safety margin against overflow',
        'Increase precision to FP16',
        'Use the CPU',
      ],
      a: 1,
      x: [
        'Underflow everything',
        'Disable tensor cores',
        'For FP64',
      ],
      e: 'Mapping amax exactly to the max risks rounding pushing values to inf. A small margin keeps the largest values representable (no overflow) while still using nearly the full range — balancing overflow safety against wasted dynamic range.',
      ref: 'FP8 scaling; margin',
      tags: ['fp8', 'scaling', 'margin', 'overflow'],
    },
    {
      d: 5,
      q: 'A reason low-precision training generally tolerates reduced precision better than low-precision SCIENTIFIC computing is…',
      o: [
        'Science uses integers',
        'DL self-corrects; HPC ill-conditioned, FP64',
        'Training is exact',
        'Science is random',
      ],
      a: 1,
      x: [
        'They are identical',
        'Training needs FP64',
        'For graphics',
      ],
      e: 'DL training tolerates noise (stochastic optimization, approximate objectives), so low precision rarely breaks it. HPC simulations/solvers can be ill-conditioned and accumulate rounding error, often requiring FP64 — hence the differing precision needs.',
      ref: 'Precision needs; DL vs HPC',
      tags: ['low-precision', 'training', 'hpc', 'robustness'],
    },
  ],
  { idPrefix: 'precision-e' }
)
