import { defineQuestions } from './_helpers.js'

// Numerics & Mixed Precision — batch B. IEEE specials, rounding, FP8 recipes,
// quantization (QAT/PTQ/weight-only), stability tricks. Distinct from precision.js.
export default defineQuestions(
  'precision',
  [
    {
      d: 2,
      q: 'The approximate maximum representable value of FP16 (half) is…',
      o: ['~3.4e38', '~65504', '~255', '~1.8e308'],
      a: 1,
      x: ['~6550', '~32767', '~1.0e6'],
      e: 'FP16’s 5-bit exponent caps the max normal at 65504. Values above overflow to inf — the reason FP16 training can overflow activations/gradients and needs loss scaling. (3.4e38 is FP32’s max; 1.8e308 is FP64’s.)',
      ref: 'IEEE 754 half precision',
      tags: ['fp16', 'range', 'overflow'],
    },
    {
      d: 3,
      q: 'Flushing denormals (subnormals) to zero in a kernel…',
      o: [
        'Improves accuracy near zero',
        'Speed up at cost of near-zero precision; --ftz=true',
        'Is required for tensor cores',
        'Has no effect',
      ],
      a: 1,
      x: [
        'Increases the exponent range',
        'Only affects integers',
        'Disables rounding',
      ],
      e: 'Subnormals extend range toward zero but can incur slow paths. Flush-to-zero (FTZ) treats them as 0 for speed/consistency, at the cost of underflow accuracy. CUDA lets you control FTZ per build (and fast-math enables it).',
      ref: 'CUDA C++ Programming Guide §G; --ftz',
      tags: ['denormals', 'ftz', 'underflow'],
    },
    {
      d: 2,
      q: 'In IEEE-754, what does 0.0/0.0 produce, and what is special about comparing it?',
      o: [
        'Zero; compares equal to 0',
        'NaN; and NaN != NaN (x != x is true for NaN)',
        'Infinity; compares greater than all',
        'It traps and halts the kernel',
      ],
      a: 1,
      x: [
        'One; compares equal to 1',
        'NaN; but NaN == NaN is true',
        'Undefined integer',
      ],
      e: '0/0 (and inf−inf, 0×inf) yields NaN. NaN propagates through arithmetic and is unordered: any comparison with NaN is false except !=. The self-inequality x != x is a common NaN-detection trick (or use isnan).',
      ref: 'IEEE 754; NaN semantics',
      tags: ['nan', 'ieee-754', 'comparison'],
    },
    {
      d: 3,
      q: 'The default IEEE rounding mode used by CUDA arithmetic is…',
      o: [
        'Round toward zero (truncate)',
        'Round to nearest, ties to even',
        'Round up',
        'Stochastic rounding',
      ],
      a: 1,
      x: [
        'Round toward negative infinity',
        'Round to nearest, ties away from zero',
        'No rounding',
      ],
      e: 'Round-to-nearest-even is the IEEE default and what CUDA uses unless you request a directed-rounding intrinsic (e.g. __fadd_rd for round-down). Ties-to-even avoids systematic bias in long sums.',
      ref: 'IEEE 754; CUDA C++ Programming Guide §G',
      tags: ['rounding', 'ieee-754', 'round-to-nearest-even'],
    },
    {
      d: 4,
      q: 'Stochastic rounding (rounding up or down with probability proportional to the residual) is valuable in low-precision training because…',
      o: [
        'It is faster than nearest rounding',
        'No systematic bias: small increments preserved in expectation',
        'It is exact',
        'It eliminates overflow',
      ],
      a: 1,
      x: [
        'It increases the exponent range',
        'It removes the need for FP32 masters',
        'It disables denormals',
      ],
      e: 'With nearest rounding, repeated tiny updates to a larger value round to zero and vanish (swamping). Stochastic rounding makes the expected update correct, letting low-precision (e.g. BF16/FP8) weights accumulate small gradients without an FP32 master.',
      ref: 'Stochastic rounding literature; low-precision training',
      tags: ['stochastic-rounding', 'low-precision', 'bias'],
    },
    {
      d: 4,
      q: 'Dynamic loss scaling (as in AMP) adjusts the scale factor by…',
      o: [
        'Keeping it fixed forever',
        'Increase on clean steps; back off (skip) on overflow',
        'Setting it to the learning rate',
        'Using the gradient norm',
      ],
      a: 1,
      x: [
        'Decreasing it every step',
        'Tying it to the batch size',
        'Randomizing it',
      ],
      e: 'AMP starts with a large scale, raises it periodically if gradients stay finite, and on detecting inf/NaN it halves the scale and skips that update. This auto-tunes the scale to keep FP16 gradients in range without manual tuning.',
      ref: 'NVIDIA Apex/AMP; mixed-precision training',
      tags: ['loss-scaling', 'dynamic', 'amp'],
    },
    {
      d: 3,
      q: 'For inference, BF16 is often preferred over FP16 because…',
      o: [
        'BF16 is more precise',
        'BF16 range avoids overflow; no loss scaling needed',
        'BF16 uses fewer bytes',
        'FP16 has no hardware support',
      ],
      a: 1,
      x: [
        'BF16 is always faster',
        'FP16 cannot represent zero',
        'BF16 has more mantissa bits',
      ],
      e: 'BF16 shares FP32’s exponent range, so models run without the overflow concerns (and loss scaling) that FP16 can require. Its coarser mantissa is usually fine for inference robustness; both are 2 bytes.',
      ref: 'BF16 vs FP16; inference practice',
      tags: ['bf16', 'fp16', 'inference', 'range'],
    },
    {
      d: 5,
      q: 'FP8 training with "delayed scaling" (as in Transformer Engine) keeps an amax history so that…',
      o: [
        'It can skip scaling',
        'Scale from recent amax history; no extra amax pass',
        'It uses FP32 everywhere',
        'It disables tensor cores',
      ],
      a: 1,
      x: [
        'It randomizes the scale',
        'It compresses weights',
        'It removes the need for E5M2',
      ],
      e: 'Computing amax of the current tensor before scaling would add a pass/latency. Delayed scaling uses a rolling history of recent amax values to set the scale for the current step, trading a little adaptivity for efficiency.',
      ref: 'NVIDIA Transformer Engine (delayed scaling)',
      tags: ['fp8', 'delayed-scaling', 'amax', 'transformer-engine'],
    },
    {
      d: 3,
      q: 'Post-Training Quantization (PTQ) vs Quantization-Aware Training (QAT): PTQ…',
      o: [
        'Retrains the model from scratch',
        'Calibrate a trained model; QAT simulates quant',
        'Is always more accurate',
        'Requires no data',
      ],
      a: 1,
      x: [
        'Uses FP64',
        'Only works for weights',
        'Is the same as QAT',
      ],
      e: 'PTQ is cheap: calibrate scales on a few batches and quantize. QAT inserts fake-quant ops in the forward/backward so the model learns to be robust to quantization, recovering more accuracy at low bit-widths but costing training time.',
      ref: 'Quantization literature; TensorRT/PyTorch quantization',
      tags: ['ptq', 'qat', 'quantization'],
    },
    {
      d: 4,
      q: 'INT8 calibration (e.g. entropy/percentile calibration) is needed to…',
      o: [
        'Train the model',
        'Pick per-tensor/channel scales mapping float to INT8',
        'Compress the weights losslessly',
        'Select the GPU',
      ],
      a: 1,
      x: [
        'Increase the bit-width',
        'Remove outliers permanently',
        'Pick the learning rate',
      ],
      e: 'Calibration runs representative data to estimate activation ranges and pick scales (e.g. clipping outliers via KL-divergence/percentile) so quantization preserves accuracy. Poor calibration (e.g. naive min/max with outliers) wastes the INT8 range.',
      ref: 'TensorRT INT8 calibration',
      tags: ['int8', 'calibration', 'quantization'],
    },
    {
      d: 4,
      q: 'Per-channel (vs per-tensor) quantization of weights helps because…',
      o: [
        'It uses fewer bits',
        'Own scale per channel — handles varying ranges',
        'It is faster',
        'It avoids calibration',
      ],
      a: 1,
      x: [
        'It removes the zero-point',
        'It quantizes activations too',
        'It increases the bit-width',
      ],
      e: 'Weight channels often have differing dynamic ranges; a single per-tensor scale wastes precision on small-range channels. Per-channel (per-output) scales fit each channel, markedly improving accuracy for INT8/INT4 weights at negligible cost.',
      ref: 'Quantization best practices; TensorRT',
      tags: ['per-channel', 'quantization', 'weights'],
    },
    {
      d: 5,
      q: 'Weight-only quantization methods like GPTQ/AWQ (e.g. INT4 weights, FP16 activations) are popular for LLM inference because…',
      o: [
        'They speed up training',
        '4-bit weights cut bandwidth; FP16 activations stay',
        'They use FP64',
        'They remove the KV cache',
      ],
      a: 1,
      x: [
        'They quantize gradients',
        'They require retraining from scratch',
        'They only work on CPUs',
      ],
      e: 'Decoder inference is dominated by reading the large weight matrices. 4-bit weight-only quantization shrinks that traffic/footprint (fitting bigger models in memory) and dequantizes on the fly, with activations kept in FP16 to preserve quality.',
      ref: 'GPTQ; AWQ; LLM inference quantization',
      tags: ['weight-only', 'int4', 'llm-inference', 'gptq'],
    },
    {
      d: 4,
      q: 'Quantizing the KV cache (e.g. to INT8/FP8) in long-context LLM inference primarily reduces…',
      o: [
        'Compute',
        'KV-cache memory/bandwidth at long sequences',
        'The number of layers',
        'The vocabulary size',
      ],
      a: 1,
      x: [
        'The model parameters',
        'The learning rate',
        'The batch size',
      ],
      e: 'The KV cache grows with sequence length × layers × heads and can exceed the weights for long contexts. Storing it in 8-bit (or FP8) roughly halves that memory/bandwidth, enabling longer contexts or larger batches with modest accuracy impact.',
      ref: 'KV-cache quantization; LLM serving',
      tags: ['kv-cache', 'quantization', 'long-context'],
    },
    {
      d: 4,
      q: 'The error of a low-precision (e.g. FP16-input) matmul generally grows with…',
      o: [
        'The output size only',
        'Grows with K (more accumulated products)',
        'The batch size only',
        'Nothing; it is constant',
      ],
      a: 1,
      x: [
        'The learning rate',
        'The number of GPUs',
        'The grid size',
      ],
      e: 'A longer dot product accumulates more rounding error. Tensor cores accumulate in FP32 precisely to keep this in check; accumulating in FP16 over a large K would lose accuracy badly. Error roughly scales with √K (or K worst case).',
      ref: 'Numerical analysis of matmul; mixed precision',
      tags: ['matmul', 'accumulation', 'error', 'k-dimension'],
    },
    {
      d: 3,
      q: 'The log-sum-exp trick computes log(Σ exp(x_i)) stably by…',
      o: [
        'Ignoring large terms',
        'Subtract max m; largest = exp(0)=1, no overflow',
        'Using FP64',
        'Clamping inputs to [0,1]',
      ],
      a: 1,
      x: [
        'Dividing by the count',
        'Taking the mean first',
        'Using integer math',
      ],
      e: 'Directly exponentiating large x_i overflows. Subtracting the max m before exp keeps terms ≤ 1; adding m back outside the log recovers the exact result. This is the same stabilization used inside softmax/cross-entropy.',
      ref: 'Numerical computing; softmax/cross-entropy stability',
      tags: ['log-sum-exp', 'stability', 'softmax'],
    },
    {
      d: 4,
      q: 'A mixed-precision Adam optimizer typically keeps which states in FP32?',
      o: [
        'None',
        'Master weights + Adam moments (m, v) stay FP32',
        'Only the gradients',
        'Only the activations',
      ],
      a: 1,
      x: [
        'Only the learning rate',
        'The input data',
        'The attention scores',
      ],
      e: 'Keeping master weights and Adam moments in FP32 preserves the tiny per-step updates that low precision would round away, while the compute-heavy forward/backward run in 16-bit. ZeRO then shards these FP32 states to save memory.',
      ref: 'Mixed-precision training; Adam',
      tags: ['adam', 'mixed-precision', 'master-weights'],
    },
    {
      d: 3,
      q: 'Many DL frameworks enable TF32 by default for FP32 matmuls/convolutions, which means…',
      o: [
        'Results are bit-identical to FP32',
        'FP32 matmuls use TF32 (~10-bit); results not bit-exact',
        'FP32 is unavailable',
        'It only affects FP16',
      ],
      a: 1,
      x: [
        'It uses FP64 internally',
        'It disables tensor cores',
        'It requires manual scaling',
      ],
      e: 'PyTorch/cuBLAS default to TF32 for big speedups on Ampere+; results are close to but not exactly FP32. For reproducibility or sensitive numerics, set torch.backends.cuda.matmul.allow_tf32 = False (or pedantic math).',
      ref: 'PyTorch TF32 flags; cuBLAS math mode',
      tags: ['tf32', 'frameworks', 'default'],
    },
    {
      d: 5,
      q: 'The FP4 "E2M1" format has how many exponent and mantissa bits, and why does it need block scaling?',
      o: [
        '3 exponent, 0 mantissa; no scaling needed',
        '2 exp, 1 mantissa (sign): ~16 values; per-block scale needed',
        '4 exponent, 0 mantissa; scaling optional',
        '1 exponent, 2 mantissa; never used',
      ],
      a: 1,
      x: [
        '2 exponent, 2 mantissa; per-tensor scale only',
        '0 exponent, 3 mantissa; integer-like',
        '5 exponent, 0 mantissa',
      ],
      e: 'E2M1 FP4 has 1 sign, 2 exponent, 1 mantissa bit — a tiny set of representable values with minimal range. Microscaling (e.g. a shared scale per block of 32 elements, MXFP4) is what makes it usable for real tensors on Blackwell.',
      ref: 'OCP Microscaling (MX) spec; NVIDIA Blackwell',
      tags: ['fp4', 'e2m1', 'microscaling', 'blackwell'],
    },
    {
      d: 4,
      q: 'NF4 (4-bit NormalFloat, used by QLoRA/bitsandbytes) differs from a uniform INT4 grid by…',
      o: [
        'Using more bits',
        'Levels at normal quantiles vs uniform spacing',
        'Being an integer format',
        'Requiring no scaling',
      ],
      a: 1,
      x: [
        'Using an exponent field',
        'Storing activations',
        'Being lossless',
      ],
      e: 'Neural-net weights are roughly normally distributed. NF4 spaces its levels by the normal quantiles so more levels land where data is dense, reducing error versus uniform INT4 — enabling 4-bit weights with little accuracy loss in QLoRA.',
      ref: 'Dettmers et al., "QLoRA" (NF4)',
      tags: ['nf4', 'quantization', 'qlora', '4-bit'],
    },
    {
      d: 2,
      q: 'Why must softmax accumulate the sum of exponentials in higher precision (e.g. FP32) even when inputs are FP16?',
      o: [
        'FP16 cannot store the inputs',
        'exp() sum can overflow FP16; FP32 accumulation needed',
        'It is required by tensor cores',
        'FP16 has no exp function',
      ],
      a: 1,
      x: [
        'To use more memory',
        'Because exp is integer',
        'To enable stochastic rounding',
      ],
      e: 'Even with the max-subtraction trick, the denominator sums many terms; doing this in FP16 loses precision and risks overflow. Accumulating in FP32 (then casting) keeps softmax numerically sound — standard in attention kernels.',
      ref: 'Mixed precision; FlashAttention numerics',
      tags: ['softmax', 'accumulation', 'fp32', 'stability'],
    },
    {
      d: 3,
      q: 'The hardware provides fast conversion between FP32 and BF16 essentially by…',
      o: [
        'A lookup table',
        'Round FP32 mantissa to 7 bits; exponent unchanged',
        'Re-encoding the exponent',
        'Software emulation only',
      ],
      a: 1,
      x: [
        'Scaling by a power of two',
        'Computing a logarithm',
        'Using INT8',
      ],
      e: 'Because BF16 shares FP32’s exponent and sign and just truncates the mantissa, conversion is nearly free (round the low mantissa bits). This cheap conversion is part of BF16’s appeal for mixed-precision pipelines.',
      ref: 'BF16 format; hardware conversion',
      tags: ['bf16', 'fp32', 'conversion'],
    },
    {
      d: 4,
      q: 'A subtle bug: accumulating a large reduction in FP16 gives a result that stops increasing partway through. The cause is…',
      o: [
        'Integer overflow',
        'Swamping: small terms below ULP, vanish',
        'A race condition',
        'Denormal flushing',
      ],
      a: 1,
      x: [
        'NaN propagation',
        'Bank conflicts',
        'Loss scaling',
      ],
      e: 'At large magnitudes the gap between representable FP16 values (ULP) exceeds the small addends, so the sum stagnates. Use FP32 accumulation (or Kahan/pairwise) for long reductions — a classic low-precision pitfall.',
      ref: 'Floating-point summation; swamping',
      tags: ['fp16', 'swamping', 'reduction', 'accumulation'],
    },
    {
      d: 3,
      q: 'Gradient clipping (by global norm) interacts with loss scaling because you must…',
      o: [
        'Clip before unscaling',
        'Unscale first, then clip to the true norm',
        'Skip clipping in mixed precision',
        'Clip the loss instead',
      ],
      a: 1,
      x: [
        'Clip the activations',
        'Multiply the clip value by the scale',
        'Disable clipping',
      ],
      e: 'Loss scaling multiplies gradients by S; clipping must operate on the real gradient norm, so AMP unscales (and checks for inf/NaN) before applying gradient clipping and the optimizer step.',
      ref: 'NVIDIA AMP; gradient clipping with loss scaling',
      tags: ['gradient-clipping', 'loss-scaling', 'amp'],
    },
    {
      d: 4,
      q: 'FP8 is increasingly used for inference (and training) of LLMs because, relative to FP16/BF16, it…',
      o: [
        'Has more mantissa bits',
        'Halves memory/bandwidth, doubles throughput (with scaling)',
        'Removes the need for tensor cores',
        'Is exact',
      ],
      a: 1,
      x: [
        'Uses 16 bits',
        'Eliminates the KV cache',
        'Requires FP64 accumulation',
      ],
      e: 'FP8 (1 byte) cuts memory/bandwidth versus 2-byte formats and doubles tensor-core math rate on Hopper, while scaling strategies (and E4M3/E5M2 choice) preserve enough accuracy — hence its rapid adoption for large-model inference and training.',
      ref: 'NVIDIA FP8 training/inference; Transformer Engine',
      tags: ['fp8', 'throughput', 'llm', 'memory'],
    },
    {
      d: 5,
      q: 'Why does microscaling (e.g. MXFP8/MXFP6/MXFP4) use a shared scale per small block (commonly 32 elements) rather than per-tensor?',
      o: [
        'To save metadata',
        'Per-block scale for local range; few bits need it',
        'To increase the bit-width',
        'To avoid tensor cores',
      ],
      a: 1,
      x: [
        'To compress losslessly',
        'To remove the exponent',
        'For host compatibility',
      ],
      e: 'With very few bits, a single per-tensor scale can’t fit elements of widely varying magnitude. A shared scale per 32-element block adapts to local range, keeping each block’s values representable — the core idea of OCP MX formats.',
      ref: 'OCP Microscaling spec; Blackwell MX support',
      tags: ['microscaling', 'block-scaling', 'mxfp', 'dynamic-range'],
    },
    {
      d: 3,
      q: 'Machine epsilon (the gap between 1.0 and the next representable value) is LARGEST for which type?',
      o: ['FP64', 'FP32', 'FP16', 'BF16'],
      a: 3,
      x: ['TF32', 'INT8', 'FP8 E4M3'],
      e: 'Epsilon depends on mantissa bits: BF16 has only 7 mantissa bits (ε ≈ 2^-8 ≈ 0.0039), coarser than FP16’s 10 bits (ε ≈ 2^-11). Fewer mantissa bits → larger epsilon → coarser precision near 1.0. (BF16 trades mantissa for FP32-like range.)',
      ref: 'IEEE 754; BF16 vs FP16 precision',
      tags: ['machine-epsilon', 'bf16', 'fp16', 'mantissa'],
    },
    {
      d: 4,
      q: 'When converting activations to FP8 with E4M3, why is choosing the scale so that the tensor’s max maps near the format’s max (but not over) important?',
      o: [
        'To use integer math',
        'Too small = zeros; too large = inf; scale to window',
        'To avoid tensor cores',
        'To increase K',
      ],
      a: 1,
      x: [
        'To enable stochastic rounding',
        'To remove the sign bit',
        'To double the bandwidth',
      ],
      e: 'FP8 has a narrow range and few codes. The per-tensor scale aligns the data’s amax just under the format max so small values stay representable and large ones don’t overflow — mis-scaling causes zeros or infs, breaking training.',
      ref: 'FP8 scaling; Transformer Engine',
      tags: ['fp8', 'scaling', 'e4m3', 'amax'],
    },
  ],
  { idPrefix: 'precision-b' }
)
