import { defineQuestions } from './_helpers.js'

// Distributed Training — data/tensor/pipeline parallelism, ZeRO, FSDP,
// sequence/context parallelism, overlap, sharding, communication trade-offs.
export default defineQuestions('distributed', [
  {
    d: 2,
    q: 'In pure data parallelism, what is replicated and what is split across GPUs?',
    o: [
      'Model is split; data is replicated',
      'Full model replicated on every GPU; batch sharded',
      'Both model and data are split',
      'Neither is split',
    ],
    a: 1,
    e: 'Data parallelism keeps a full model copy per GPU and divides the global batch among them. Each computes local gradients on its micro-batch, then an all-reduce averages gradients so all replicas stay in sync.',
    ref: 'PyTorch DDP; Ultra-Scale Playbook',
    tags: ['data-parallelism', 'replication'],
  },
  {
    d: 3,
    q: 'What is the main limitation that data parallelism alone cannot solve?',
    o: [
      'Slow data loading',
      'Model too big for one GPU; DP cannot shard it',
      'Too many GPUs',
      'Gradient averaging',
    ],
    a: 1,
    e: 'Because every GPU holds the entire model (plus optimizer states and activations), data parallelism cannot train models larger than one GPU’s memory. That requires sharding the model itself: tensor, pipeline, or ZeRO/FSDP sharding.',
    ref: 'Ultra-Scale Playbook; Megatron-LM',
    tags: ['data-parallelism', 'memory', 'limitations'],
  },
  {
    d: 4,
    q: 'Tensor (model) parallelism splits…',
    o: [
      'The batch across GPUs',
      'Weight matrices across GPUs; cooperative matmul',
      'The training steps across GPUs',
      'The dataset files',
    ],
    a: 1,
    e: 'Tensor parallelism partitions weight matrices (e.g. column/row splits of MLP and attention projections) across GPUs. Each GPU does part of the matmul; an all-reduce/all-gather combines partial results. It needs fast intra-node links (NVLink).',
    ref: 'Megatron-LM (Shoeybi et al.); Ultra-Scale Playbook',
    tags: ['tensor-parallelism', 'megatron', 'matmul'],
  },
  {
    d: 4,
    q: 'In Megatron-style tensor parallelism for an MLP (two linear layers), the standard split is…',
    o: [
      'Both layers split the same way with two all-reduces',
      'First column-parallel (no comm); second row-parallel, one all-reduce after',
      'Replicate both layers',
      'Split only the activation',
    ],
    a: 1,
    e: 'Column-parallel for the first GEMM keeps the GeLU local per shard (no comm); row-parallel for the second produces partial sums that are combined with one all-reduce. This minimizes communication to one all-reduce per MLP in forward (and one in backward).',
    ref: 'Megatron-LM paper',
    tags: ['tensor-parallelism', 'mlp', 'column-row-parallel'],
  },
  {
    d: 4,
    q: 'Pipeline parallelism splits the model by…',
    o: [
      'Splitting each weight matrix',
      'Layers split into stages; micro-batches streamed through',
      'Replicating layers',
      'Splitting the optimizer state',
    ],
    a: 1,
    e: 'Pipeline parallelism partitions the model depth-wise: GPU0 holds layers 1–k, GPU1 holds k+1–2k, etc. Micro-batches flow through stages so multiple are in flight at once. Communication is point-to-point activations between adjacent stages.',
    ref: 'GPipe; PipeDream; Megatron-LM',
    tags: ['pipeline-parallelism', 'stages', 'micro-batches'],
  },
  {
    d: 4,
    q: 'The "bubble" in pipeline parallelism refers to…',
    o: [
      'A memory leak',
      'Idle fill/drain time when not all stages have work',
      'A communication buffer',
      'A type of all-reduce',
    ],
    a: 1,
    e: 'The pipeline bubble is the idle stage-time during fill (ramp-up) and drain (ramp-down) of the pipeline. Bubble fraction ≈ (stages-1)/(micro-batches + stages-1); more micro-batches and schedules like interleaved/1F1B shrink it.',
    ref: 'GPipe; Megatron-LM interleaved schedule; Ultra-Scale Playbook',
    tags: ['pipeline-parallelism', 'bubble', 'efficiency'],
  },
  {
    d: 3,
    q: 'ZeRO (Zero Redundancy Optimizer) reduces memory by…',
    o: [
      'Using lower precision only',
      'Shards optimizer states, grads, and params across DP ranks',
      'Removing the optimizer',
      'Compressing activations',
    ],
    a: 1,
    e: 'ZeRO partitions the redundantly-replicated training state across data-parallel GPUs: Stage 1 shards optimizer states, Stage 2 adds gradient sharding, Stage 3 adds parameter sharding — cutting per-GPU memory ~linearly with the DP degree.',
    ref: 'Rajbhandari et al., "ZeRO"; DeepSpeed',
    tags: ['zero', 'sharding', 'memory'],
  },
  {
    d: 4,
    q: 'FSDP (Fully Sharded Data Parallel) is conceptually equivalent to which ZeRO stage?',
    o: ['ZeRO Stage 0', 'ZeRO Stage 3 (all three components sharded)', 'Pipeline parallelism', 'Tensor parallelism'],
    a: 1,
    e: 'PyTorch FSDP shards parameters, gradients, and optimizer states across data-parallel ranks — equivalent to ZeRO-3. It all-gathers a layer’s full params just-in-time for compute, then frees them, trading communication for memory.',
    ref: 'PyTorch FSDP docs; ZeRO paper',
    tags: ['fsdp', 'zero-3', 'sharding'],
  },
  {
    d: 5,
    q: 'In ZeRO-3 / FSDP, the extra communication compared to plain data parallelism is…',
    o: [
      'Nothing extra',
      'All-gather params per layer + reduce-scatter grads',
      'Only a broadcast at startup',
      'A pipeline send/recv',
    ],
    a: 1,
    e: 'Because parameters live sharded, FSDP must all-gather each layer’s full weights just before using them (forward and backward) and reduce-scatter gradients. This adds communication versus DDP but is overlapped with compute to hide much of the cost.',
    ref: 'ZeRO paper; FSDP design',
    tags: ['fsdp', 'all-gather', 'communication-overhead'],
  },
  {
    d: 5,
    q: 'Sequence/context parallelism is introduced primarily to address…',
    o: [
      'Too-small batch sizes',
      'Long-sequence activation memory; seq dim sharded across GPUs',
      'Slow optimizers',
      'Gradient noise',
    ],
    a: 1,
    e: 'For long contexts, attention activations grow with sequence length and dominate memory. Sequence/context parallelism partitions the sequence across GPUs (e.g. Megatron sequence parallelism, Ring Attention), exchanging keys/values via collectives.',
    ref: 'Megatron sequence parallelism; Ring Attention; Ultra-Scale Playbook',
    tags: ['sequence-parallelism', 'long-context', 'activation-memory'],
  },
  {
    d: 5,
    q: 'Expert parallelism in Mixture-of-Experts (MoE) models uses which collective to route tokens to the GPUs holding their selected experts?',
    o: ['AllReduce', 'All-to-All', 'Broadcast', 'Reduce'],
    a: 1,
    e: 'MoE dispatches each token to its top-k experts, which are sharded across GPUs. An All-to-All exchange sends tokens to the GPU owning the chosen expert and a second All-to-All returns the results — All-to-All is the dominant MoE communication.',
    ref: 'GShard; Switch Transformer; Ultra-Scale Playbook',
    tags: ['moe', 'expert-parallelism', 'all-to-all'],
  },
  {
    d: 4,
    q: '"3D parallelism" in large-model training combines which three?',
    o: [
      'Three different optimizers',
      'Data + tensor + pipeline parallelism',
      'FP32 + FP16 + FP8',
      'Three GPUs',
    ],
    a: 1,
    e: '3D parallelism composes data, tensor, and pipeline parallelism: tensor parallelism within a node (NVLink), pipeline across nodes, and data parallelism across replicas — the standard recipe (Megatron-DeepSpeed) for training models with hundreds of billions of parameters.',
    ref: 'Megatron-DeepSpeed; Ultra-Scale Playbook',
    tags: ['3d-parallelism', 'scaling', 'megatron'],
  },
  {
    d: 3,
    q: 'Gradient accumulation lets you…',
    o: [
      'Train without gradients',
      'Accumulate grads over micro-batches before one optimizer step',
      'Skip the backward pass',
      'Average across GPUs without NCCL',
    ],
    a: 1,
    e: 'By running multiple forward/backward micro-batches and accumulating their gradients before stepping the optimizer, you achieve a large effective batch without holding all activations at once — useful when memory limits the per-step batch.',
    ref: 'Standard DL training; Ultra-Scale Playbook',
    tags: ['gradient-accumulation', 'effective-batch', 'memory'],
  },
  {
    d: 4,
    q: 'Activation checkpointing (gradient checkpointing) trades…',
    o: [
      'Accuracy for speed',
      'Recompute activations (backward) for less memory',
      'Communication for compute',
      'Precision for range',
    ],
    a: 1,
    e: 'Instead of storing all forward activations for the backward pass, checkpointing keeps only a subset and recomputes the rest on demand during backprop. This cuts activation memory substantially at the cost of ~one extra forward pass of compute.',
    ref: 'Chen et al., "Training Deep Nets with Sublinear Memory Cost"',
    tags: ['activation-checkpointing', 'memory', 'recompute'],
  },
  {
    d: 5,
    q: 'Why is tensor parallelism usually kept within a single node while pipeline/data parallelism span nodes?',
    o: [
      'Tensor parallelism needs more GPUs',
      'TP needs NVLink BW (all-reduce/layer); PP/DP tolerate inter-node',
      'Pipeline parallelism cannot cross nodes',
      'Data parallelism only works on one node',
    ],
    a: 1,
    e: 'Tensor parallelism issues frequent, latency-sensitive collectives per layer, so it’s confined to NVLink/NVSwitch within a node. Pipeline (point-to-point activations) and data parallelism (one all-reduce per step, overlappable) communicate less and map onto inter-node networks.',
    ref: 'Megatron-LM; Ultra-Scale Playbook',
    tags: ['tensor-parallelism', 'topology', 'bandwidth', 'mapping'],
  },
  {
    d: 3,
    q: 'In synchronous data-parallel SGD, why must gradients be all-reduced before the optimizer step?',
    o: [
      'To save memory',
      'Replicas apply same averaged gradient; stay identical',
      'To increase the learning rate',
      'To shuffle the data',
    ],
    a: 1,
    e: 'Synchronous DP keeps all replicas bit-for-bit identical by averaging gradients (all-reduce) so each does the same update. Without it, replicas would diverge and the "single model" abstraction breaks. (Async SGD relaxes this at a convergence cost.)',
    ref: 'PyTorch DDP; distributed SGD literature',
    tags: ['synchronous-sgd', 'all-reduce', 'consistency'],
  },
  {
    d: 5,
    q: 'The 1F1B (one-forward-one-backward) pipeline schedule improves over naive GPipe by…',
    o: [
      'Eliminating the bubble entirely',
      'Bounds in-flight activations by interleaving fwd/bwd',
      'Removing pipeline parallelism',
      'Using larger micro-batches',
    ],
    a: 1,
    e: '1F1B steady-state alternates a forward and a backward, so each stage holds activations for only ~(pipeline depth) micro-batches rather than all of them (as in GPipe’s all-forward-then-all-backward), cutting peak activation memory with the same bubble.',
    ref: 'PipeDream/Megatron 1F1B; Ultra-Scale Playbook',
    tags: ['pipeline-parallelism', '1f1b', 'activation-memory', 'scheduling'],
  },
])
