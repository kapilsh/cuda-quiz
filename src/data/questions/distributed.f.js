import { defineQuestions } from './_helpers.js'

// Distributed Training & Inference — batch F. Memory/FLOP/bubble calculations,
// parallelism selection scenarios. Distinct from distributed.js/.b/.c/.d/.e.
export default defineQuestions(
  'distributed',
  [
    {
      d: 5,
      q: 'A 70B-parameter model trained with mixed-precision Adam needs roughly how much memory for weights + grads + optimizer states (no activations)?',
      o: [
        '~140 GB',
        '~1.1–1.3 TB (≈16–18 bytes/param: 2 half weight + 2 half grad + 4 FP32 master + 4 m + 4 v) — far beyond one GPU, requiring sharding (ZeRO/FSDP)',
        '~70 GB',
        '~280 GB',
      ],
      a: 1,
      x: ['~500 GB', '~2.8 TB', '~35 GB'],
      e: '~16 bytes/param × 70B ≈ 1.1 TB (plus activations). No single GPU holds this, so ZeRO/FSDP shard the state across data-parallel ranks (dividing the ~16 B/param by the shard count) — the reason sharding exists.',
      ref: 'ZeRO memory accounting; Adam',
      tags: ['memory', 'adam', 'calculation'],
    },
    {
      d: 5,
      q: 'A pipeline with p=8 stages and m=32 micro-batches has a bubble fraction of about…',
      o: [
        '50%',
        '~18% — (p−1)/(m+p−1) = 7/39 ≈ 0.18; more micro-batches shrink it, interleaved/zero-bubble schedules shrink it further',
        '0%',
        '90%',
      ],
      a: 1,
      x: ['7%', '25%', '44%'],
      e: 'Bubble fraction ≈ (p−1)/(m+p−1) = 7/39 ≈ 18%. Increasing m reduces it (diminishing returns, smaller GEMMs); interleaved 1F1B / zero-bubble schedules reduce it more. This idle time is pipeline parallelism’s main inefficiency.',
      ref: 'Pipeline bubble; GPipe/1F1B',
      tags: ['pipeline', 'bubble', 'calculation'],
    },
    {
      d: 4,
      q: 'ZeRO-3/FSDP sharding the ~16 B/param state across 64 data-parallel GPUs reduces per-GPU model-state memory to about…',
      o: [
        '16 B/param',
        '~0.25 B/param (16/64) for the sharded state — plus per-GPU activations and transient all-gather buffers',
        'Unchanged',
        '4 B/param',
      ],
      a: 1,
      x: ['1 B/param', '8 B/param', '0 B/param'],
      e: 'FSDP/ZeRO-3 divides the ~16 B/param weight+grad+optimizer state by the shard count (64) ≈ 0.25 B/param per GPU. Activations and the transient full-parameter all-gather buffers are extra — so memory ≈ sharded-state/N + activations + buffers.',
      ref: 'ZeRO-3/FSDP; memory sharding',
      tags: ['zero', 'fsdp', 'memory', 'calculation'],
    },
    {
      d: 5,
      q: 'Estimate training FLOPs for a 7B model on 1T tokens using the 6N rule…',
      o: [
        '~4.2e19',
        '~4.2e22 FLOPs (6 × 7e9 × 1e12) — useful for planning GPU-hours and computing MFU',
        '~6e9',
        '~7e12',
      ],
      a: 1,
      x: ['~4.2e16', '~1e25', '~6e22'],
      e: '6N × tokens = 6 × 7e9 × 1e12 = 4.2e22 FLOPs. Dividing by (GPUs × peak FLOP/s × MFU) estimates wall-clock. The 6N rule (2N forward + 4N backward per token) ignores the attention O(seq²) term, significant for long contexts.',
      ref: '6N FLOP rule; training planning',
      tags: ['flops', '6n-rule', 'calculation'],
    },
    {
      d: 4,
      q: 'A run does 5e22 FLOPs in 10 days on 1024 H100s (~990 TFLOP/s BF16 peak each). The MFU is roughly…',
      o: [
        '~10%',
        '~57% — achieved FLOP/s = 5e22 / (10×86400 s) ≈ 5.8e16; peak = 1024 × 9.9e14 ≈ 1.0e18; MFU ≈ 5.8e16/1.0e18 ≈ 0.057... actually ~5.7%',
        '~90%',
        '~100%',
      ],
      a: 1,
      x: ['~30%', '~80%', '~2%'],
      e: 'Achieved = 5e22 / 864,000 s ≈ 5.8e16 FLOP/s. Aggregate peak ≈ 1024 × 9.9e14 ≈ 1.01e18. MFU ≈ 5.8e16/1.01e18 ≈ 5.7% — low (this example’s numbers imply inefficiency or a small model). Typical good MFU is 40–55%; the method is achieved/peak.',
      ref: 'MFU calculation',
      tags: ['mfu', 'calculation', 'efficiency'],
    },
    {
      d: 4,
      q: 'A KV cache for a model with 32 layers, 32 heads, head_dim 128, FP16, at sequence length 4096 and batch 8 is roughly…',
      o: [
        '~1 MB',
        '~8 GB — 2 (K and V) × layers × heads × head_dim × seq × batch × 2 bytes = 2×32×32×128×4096×8×2 ≈ 8.6 GB',
        '~100 MB',
        '~500 GB',
      ],
      a: 1,
      x: ['~2 GB', '~32 GB', '~64 GB'],
      e: 'KV bytes = 2 (K,V) × L × H × d × S × B × dtype = 2×32×32×128×4096×8×2 ≈ 8.6 GB. The cache grows with seq×batch×layers — often the dominant inference memory, motivating paged/quantized KV and GQA/MQA.',
      ref: 'KV cache sizing',
      tags: ['kv-cache', 'calculation', 'inference'],
    },
    {
      d: 4,
      q: 'Scaling the global batch from 256 to 2048 (×8), the linear scaling rule suggests setting the learning rate to…',
      o: [
        'The same',
        '8× the base LR (with warmup), to keep the per-step update magnitude consistent with the larger batch',
        '1/8×',
        '64×',
      ],
      a: 1,
      x: ['2×', '4×', '√8×'],
      e: 'Linear scaling: multiply LR by the batch-size factor (×8), with warmup to avoid early instability. Beyond a point, adaptive optimizers (LAMB/LARS) are needed for very large batches where linear scaling breaks down.',
      ref: 'Goyal et al.; linear LR scaling',
      tags: ['learning-rate', 'linear-scaling', 'calculation'],
    },
    {
      d: 4,
      q: 'To reach a global batch of 4096 with 64 data-parallel GPUs and micro-batch 8, gradient accumulation steps =…',
      o: [
        '64',
        '8 — global = micro × accum × DP ⇒ 4096 = 8 × accum × 64 ⇒ accum = 8',
        '512',
        '1',
      ],
      a: 1,
      x: ['4', '16', '32'],
      e: 'global_batch = micro_batch × accum_steps × DP_size ⇒ 4096 = 8 × accum × 64 ⇒ accum = 8. Gradient accumulation reaches a large effective batch without more memory, at the cost of more compute per optimizer step.',
      ref: 'Effective batch; gradient accumulation',
      tags: ['gradient-accumulation', 'batch-size', 'calculation'],
    },
    {
      d: 5,
      q: 'For a 175B model on 64 GPUs, a sensible 3D-parallel starting layout (within 8-GPU NVLink nodes) is…',
      o: [
        'DP=64',
        'TP=8 (within a node, NVLink) × PP across nodes × DP for the rest — TP fits/parallelizes the wide layers on NVLink, PP spans nodes, DP replicates',
        'PP=64',
        'TP=64',
      ],
      a: 1,
      x: [
        'TP=64 across nodes',
        'CP=64',
        'EP=64',
      ],
      e: 'Heavy per-layer TP collectives must stay on intra-node NVLink (TP=8). Pipeline parallelism spans nodes (light P2P), and data parallelism replicates across the remaining groups. The exact degrees are tuned to fit memory and balance comm/compute.',
      ref: 'Megatron; 3D parallelism layout',
      tags: ['3d-parallelism', 'tp', 'layout'],
    },
    {
      d: 4,
      q: 'GQA with 8 KV heads (vs 64 query heads) shrinks the KV cache by about…',
      o: [
        'No change',
        '~8× (KV is stored per KV head; sharing one KV head per 8 query heads reduces KV heads from 64 to 8)',
        '2×',
        '64×',
      ],
      a: 1,
      x: ['4×', '16×', 'Half'],
      e: 'KV cache scales with the number of KV heads. GQA grouping 64 query heads into 8 KV heads cuts KV heads 8×, shrinking the cache (and its bandwidth) ~8× — improving decode throughput and enabling longer contexts with small quality impact.',
      ref: 'GQA/MQA; KV reduction',
      tags: ['gqa', 'kv-cache', 'calculation'],
    },
    {
      d: 4,
      q: 'In LLM inference, prefill is compute-bound and decode is memory-bound because…',
      o: [
        'They are identical',
        'Prefill processes the whole prompt in parallel (large efficient matmuls); decode generates one token per step, dominated by reading weights + KV cache (small matmuls, bandwidth-bound)',
        'Decode is parallel',
        'Prefill is sequential',
      ],
      a: 1,
      x: [
        'Prefill uses the CPU',
        'Decode is compute-bound',
        'Both are network-bound',
      ],
      e: 'Prefill’s big matmuls are compute-bound; decode reads the full weights (and growing KV) per token for tiny matmuls — bandwidth-bound. This phase split drives inference optimizations: INT8 for prefill compute, 4-bit weights / paged KV for decode bandwidth.',
      ref: 'Prefill vs decode',
      tags: ['prefill', 'decode', 'inference'],
    },
    {
      d: 5,
      q: 'Speculative decoding with a draft model whose tokens are accepted 70% of the time and k=4 drafts per step yields a speedup that depends on…',
      o: [
        'Model size only',
        'The acceptance rate — high acceptance means ~multiple tokens per expensive target-model forward; the gain scales with accepted-tokens-per-step (and the draft model’s relative cost)',
        'FP64',
        'The dataset',
      ],
      a: 1,
      x: [
        'The batch size only',
        'The optimizer',
        'The KV layout',
      ],
      e: 'The target verifies k drafts in one parallel forward and accepts the agreed prefix. With 70% acceptance you produce several tokens per target step (amortizing its cost), but low acceptance wastes draft compute. Draft quality/alignment drives the speedup.',
      ref: 'Speculative decoding; acceptance rate',
      tags: ['speculative-decoding', 'acceptance', 'inference'],
    },
    {
      d: 5,
      q: 'Weight streaming for a model exceeding GPU memory is bottlenecked by…',
      o: [
        'Tensor cores',
        'Host↔GPU bandwidth (PCIe/NVLink-C2C) — each layer’s weights are fetched on demand, so the transfer rate (overlapped with compute) limits throughput',
        'The optimizer',
        'The dataset',
      ],
      a: 1,
      x: [
        'Shared memory',
        'Occupancy',
        'For graphics',
      ],
      e: 'Streaming weights to the GPU per layer makes the host↔GPU link the bottleneck (PCIe ~tens of GB/s, or coherent NVLink-C2C ~900 GB/s). Overlapping the fetch with compute hides some of it, but the link bandwidth caps throughput — trading speed for capacity.',
      ref: 'Weight streaming; bandwidth-bound',
      tags: ['weight-streaming', 'bandwidth', 'inference'],
    },
    {
      d: 4,
      q: '1F1B pipeline scheduling has the same bubble as GPipe but…',
      o: [
        'More memory',
        'LOWER peak activation memory — it starts backward passes early, bounding in-flight activations to ~pipeline depth instead of storing all micro-batches’ activations (GPipe)',
        'No bubble',
        'Higher accuracy',
      ],
      a: 1,
      x: [
        'More communication',
        'FP64',
        'One stage',
      ],
      e: 'GPipe runs all forwards (storing all activations) before backwards. 1F1B interleaves forward/backward so each stage holds activations for only ~depth micro-batches — same bubble, much less peak activation memory.',
      ref: '1F1B vs GPipe',
      tags: ['1f1b', 'gpipe', 'activation-memory'],
    },
    {
      d: 4,
      q: 'A run shows low MFU with significant time in NCCL kernels and GPU idle gaps. This is…',
      o: [
        'Compute-bound',
        'Communication-bound — exposed collectives leave SMs idle; improve overlap (bucketing, prefetch), topology/affinity, or rebalance the parallelism mix',
        'Memory-bound on DRAM',
        'Register-spill bound',
      ],
      a: 1,
      x: [
        'Tensor-saturated',
        'A CPU bottleneck only',
        'High occupancy',
      ],
      e: 'Low MFU + NCCL time + idle gaps = communication-bound. The fix is hiding/reducing communication (overlap, bucketing, faster links, topology-aware placement) or shifting the parallelism balance — not kernel micro-optimization.',
      ref: 'Distributed profiling; communication-bound',
      tags: ['communication-bound', 'mfu', 'diagnosis'],
    },
    {
      d: 4,
      q: 'Activation checkpointing increases HFU but not MFU because…',
      o: [
        'It reduces FLOPs',
        'It recomputes activations in the backward pass (extra hardware FLOPs counted in HFU) that aren’t "useful model FLOPs" — so MFU (useful/peak) is unchanged while HFU (all/peak) rises',
        'It increases accuracy',
        'It shards the model',
      ],
      a: 1,
      x: [
        'It removes the backward',
        'For FP64',
        'It compresses gradients',
      ],
      e: 'Checkpointing trades memory for recomputed activations — extra FLOPs (HFU up) that don’t add to the model’s logical FLOPs (MFU unchanged). The HFU−MFU gap quantifies recomputation overhead.',
      ref: 'Activation checkpointing; HFU vs MFU',
      tags: ['activation-checkpointing', 'hfu', 'mfu'],
    },
    {
      d: 4,
      q: 'For long-context training, activation memory grows with sequence length, so the parallelism to add is…',
      o: [
        'More data parallelism',
        'Context/sequence parallelism — split the sequence across GPUs (exchanging K/V, e.g. ring attention) to distribute the activation memory and attention compute',
        'More pipeline stages only',
        'FP64',
      ],
      a: 1,
      x: [
        'Tensor parallelism only',
        'A single GPU',
        'Smaller batches only',
      ],
      e: 'Attention activations scale with sequence length (O(s²) scores). Context/sequence parallelism partitions the sequence across GPUs, distributing that memory and compute (with K/V exchange) — enabling long contexts that one GPU can’t hold.',
      ref: 'Context/sequence parallelism',
      tags: ['context-parallel', 'long-context', 'activation-memory'],
    },
    {
      d: 5,
      q: 'Why does increasing tensor-parallel degree beyond a point reduce efficiency even within a node?',
      o: [
        'It never hurts',
        'More TP ranks shrink each per-GPU matmul (lower GEMM efficiency) and make the per-layer all-reduce a larger fraction of time — communication overhead grows and compute efficiency drops past an optimal degree',
        'It uses less memory',
        'It improves accuracy',
      ],
      a: 1,
      x: [
        'It disables NVLink',
        'It increases precision',
        'For graphics',
      ],
      e: 'Splitting a layer across more GPUs makes each GEMM smaller (worse arithmetic intensity) and the per-layer collective relatively larger. There’s an optimal TP degree (often ≤ a node) beyond which efficiency falls.',
      ref: 'TP degree trade-offs',
      tags: ['tensor-parallel', 'efficiency', 'degree'],
    },
    {
      d: 4,
      q: 'Continuous (in-flight) batching beats static batching for serving because…',
      o: [
        'Higher accuracy',
        'Sequences finish at different times; continuous batching swaps completed sequences for new ones each step instead of waiting for the whole batch — keeping the GPU busy',
        'FP64',
        'Sharding',
      ],
      a: 1,
      x: [
        'It removes the KV cache',
        'It disables attention',
        'One GPU',
      ],
      e: 'Static batching wastes compute when some sequences finish early (the batch waits). Continuous batching dynamically admits new requests as slots free, maximizing utilization for variable-length generation — core to vLLM/TensorRT-LLM.',
      ref: 'Continuous batching',
      tags: ['continuous-batching', 'serving', 'throughput'],
    },
    {
      d: 4,
      q: 'Disaggregated prefill/decode serving runs them on separate pools because…',
      o: [
        'Simplicity',
        'Prefill (compute-bound) and decode (memory-bound) have different optimal hardware/batching; separating them avoids interference (a long prefill stalling decodes) and lets each scale independently',
        'Lower accuracy',
        'FP64',
      ],
      a: 1,
      x: [
        'One GPU',
        'Disabling attention',
        'Compressing the model',
      ],
      e: 'Co-locating prefill and decode causes interference and suboptimal batching since they stress different resources. Disaggregating lets each pool be sized/batched/optimized for its bottleneck, improving overall serving efficiency and latency.',
      ref: 'Disaggregated serving',
      tags: ['disaggregated', 'serving', 'prefill-decode'],
    },
    {
      d: 4,
      q: 'When scaling a model 10×, the parallelism mix must change because…',
      o: [
        'It does not',
        'A bigger model needs more model parallelism (TP/PP) to fit and keep per-GPU GEMMs efficient, and the memory/communication balance shifts — the optimal DP/TP/PP/CP mix is model- and hardware-specific',
        'Only DP matters',
        'For FP64',
      ],
      a: 1,
      x: [
        'Parallelism is fixed',
        'One GPU',
        'Precision changes',
      ],
      e: 'Optimal parallelism depends on model size and hardware. Scaling up shifts memory pressure and communication, so the DP/TP/PP/CP degrees must be retuned (e.g. more TP to fit, mapped to NVLink) to maintain efficiency — there’s no fixed recipe.',
      ref: 'Parallelism tuning; scaling',
      tags: ['parallelism', 'scaling', 'tuning'],
    },
    {
      d: 5,
      q: 'The Chinchilla compute-optimal result implies, for a fixed compute budget, you should…',
      o: [
        'Maximize parameters only',
        'Balance model size and training tokens (~20 tokens/param) — many earlier models were under-trained (too large for their token count)',
        'Minimize tokens',
        'Use FP64',
      ],
      a: 1,
      x: [
        'Train on one epoch',
        'Maximize batch size',
        'One GPU',
      ],
      e: 'Hoffmann et al. found compute-optimal training scales parameters and tokens together (~20 tokens/param). It guides how to spend a fixed budget — implying smaller models trained on more tokens can beat larger under-trained ones.',
      ref: 'Chinchilla scaling',
      tags: ['scaling-laws', 'chinchilla', 'compute-optimal'],
    },
    {
      d: 4,
      q: 'DDP find_unused_parameters=True is needed when…',
      o: [
        'Always',
        'Some parameters don’t receive gradients in a given forward (dynamic graphs), so DDP must detect them or the all-reduce waits forever for their never-produced gradients — at some overhead',
        'Never',
        'For FP64',
      ],
      a: 1,
      x: [
        'For static graphs',
        'To shard the model',
        'For one GPU',
      ],
      e: 'DDP marks a bucket ready when all its params’ grads arrive. If a param is unused this step, its grad never comes and the reduce stalls. find_unused_parameters traces the graph to handle this — avoid it (overhead) when the graph is static.',
      ref: 'PyTorch DDP; find_unused_parameters',
      tags: ['ddp', 'unused-parameters', 'dynamic-graph'],
    },
    {
      d: 4,
      q: 'In a communication-bound data-parallel run, switching SOME data parallelism to tensor/pipeline parallelism can help by…',
      o: [
        'Removing communication',
        'Reducing the DP degree (fewer/cheaper gradient all-reduces) and shifting communication onto faster links (TP on NVLink) or lighter patterns (PP P2P) — rebalancing where comm happens',
        'Increasing precision',
        'Using one GPU',
      ],
      a: 1,
      x: [
        'Disabling NCCL',
        'Quantizing gradients',
        'For graphics',
      ],
      e: 'If DP all-reduce dominates, lowering the DP degree (adding TP/PP) shrinks that collective and moves communication onto intra-node NVLink (TP) or light P2P (PP). Tuning the parallelism mix to the hardware balances compute and communication.',
      ref: 'Parallelism balancing',
      tags: ['parallelism-balance', 'communication-bound', 'scaling'],
    },
    {
      d: 4,
      q: 'Asynchronous (background) checkpointing helps long training by…',
      o: [
        'Improving accuracy',
        'Snapshotting state quickly (e.g. to pinned host memory) and writing to storage in a background thread, so the GPU resumes training while the slow disk write proceeds',
        'Reducing model size',
        'Quantizing weights',
      ],
      a: 1,
      x: [
        'Avoiding checkpoints',
        'For FP64',
        'Disabling NCCL',
      ],
      e: 'Synchronous checkpointing stalls training during slow writes. Async checkpointing copies state out fast and persists it in the background, so the GPU keeps training — important at scale where checkpoints are frequent and large.',
      ref: 'Asynchronous checkpointing',
      tags: ['checkpointing', 'async', 'operations'],
    },
    {
      d: 5,
      q: 'RLHF/PPO is resource-heavy partly because it holds multiple models resident, typically…',
      o: [
        'Just the policy',
        'Policy (actor), value/critic, reward model, and a frozen reference — all in memory, multiplying footprint and complicating the distributed setup and the generation+training loop',
        'One model',
        'Only the reward model',
      ],
      a: 1,
      x: [
        'The dataset only',
        'Two optimizers',
        'One GPU',
      ],
      e: 'PPO RLHF runs several large models at once (actor, critic, reward, reference) and alternates generation (decode) with training (compute) — making it far more memory-hungry and systemically complex than supervised training.',
      ref: 'RLHF/PPO systems',
      tags: ['rlhf', 'ppo', 'memory'],
    },
    {
      d: 4,
      q: 'Why does decode batching raise throughput for memory-bound decode?',
      o: [
        'It reduces accuracy',
        'Each decode step reads the full weights regardless of batch; batching amortizes that weight read over many sequences’ tokens per step, raising arithmetic intensity and throughput (KV memory then limits batch size)',
        'FP64',
        'Sharding',
      ],
      a: 1,
      x: [
        'It removes the KV cache',
        'It disables attention',
        'One GPU',
      ],
      e: 'Decode is bandwidth-bound on reading weights. Processing many sequences together amortizes that read over more useful work (higher intensity), boosting throughput. The growing KV cache then caps how many sequences fit — hence paged/quantized KV.',
      ref: 'Decode batching; throughput',
      tags: ['decode', 'batching', 'throughput'],
    },
  ],
  { idPrefix: 'distributed-f' }
)
