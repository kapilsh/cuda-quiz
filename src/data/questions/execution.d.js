import { defineQuestions } from './_helpers.js'

// Execution & Scheduling — batch D. Stall taxonomy, instruction throughput,
// block residency math, swizzling, elect.sync. Distinct from execution.js/.b.js/.c.js.
export default defineQuestions(
  'execution',
  [
    {
      d: 4,
      q: 'A "Not Selected" warp stall in Nsight Compute means…',
      o: [
        'The warp is waiting on memory',
        'Eligible, but another warp was issued instead',
        'The warp diverged',
        'The warp hit a barrier',
      ],
      a: 1,
      x: [
        'The warp finished',
        'The warp spilled registers',
        'The warp is on another SM',
      ],
      e: '"Not selected" indicates more eligible warps than issue slots — the scheduler is busy and parallelism is ample. Unlike memory/barrier stalls (which indicate problems), high not-selected often means the SM is well-utilized.',
      ref: 'Nsight Compute (warp stall reasons)',
      tags: ['stall-reasons', 'not-selected', 'scheduling'],
    },
    {
      d: 4,
      q: 'A "Short Scoreboard" stall (vs "Long Scoreboard") indicates the warp is waiting on…',
      o: [
        'A global memory load',
        'A short-latency dep (e.g. shared memory)',
        'A barrier',
        'The host',
      ],
      a: 1,
      x: [
        'A tensor-core op',
        'A divergent branch',
        'A register spill',
      ],
      e: 'The short scoreboard covers shorter-latency operations (e.g. shared memory / some MIO results); the long scoreboard covers long-latency global/local memory. High short-scoreboard stalls point to shared-memory pressure/dependencies, not DRAM.',
      ref: 'Nsight Compute (scoreboard stalls)',
      tags: ['stall-reasons', 'short-scoreboard', 'shared-memory'],
    },
    {
      d: 4,
      q: 'A "Math Pipe Throttle" stall means…',
      o: [
        'Memory is the bottleneck',
        'The math pipe is saturated',
        'A barrier is blocking',
        'Registers spilled',
      ],
      a: 1,
      x: [
        'The L2 missed',
        'A branch diverged',
        'The host stalled',
      ],
      e: 'Math pipe throttle indicates compute-pipe saturation: the kernel is compute-bound on that pipe (e.g. SFU-heavy or FMA-bound). The remedy is reducing or rebalancing that math (cheaper ops, fewer transcendentals, use tensor cores).',
      ref: 'Nsight Compute (Math Pipe Throttle)',
      tags: ['stall-reasons', 'math-throttle', 'compute-bound'],
    },
    {
      d: 4,
      q: 'How many resident blocks per SM does a kernel get if each block needs 40 KB shared memory on an SM with 100 KB usable shared memory (and other limits are looser)?',
      o: ['1', '2', '4', '10'],
      a: 1,
      x: ['3', '5', '8'],
      e: 'floor(100 KB / 40 KB) = 2 blocks fit by shared memory. The actual resident count is the MINIMUM across all limits (shared memory, registers, warps/threads, max-blocks). Here shared memory caps it at 2.',
      ref: 'CUDA Occupancy Calculator; shared-memory limit',
      tags: ['occupancy', 'shared-memory', 'block-residency'],
    },
    {
      d: 3,
      q: 'Instruction throughput differs by type: relative to a full-rate FMA, a transcendental (e.g. __sinf via SFU) is…',
      o: [
        'Faster',
        'Lower throughput (SFU-bound)',
        'The same',
        'Free',
      ],
      a: 1,
      x: [
        'Twice as fast',
        'Integer-only',
        'Run on tensor cores',
      ],
      e: 'FMA/ALU pipes are high throughput; SFUs (transcendentals, reciprocals) are lower throughput. A kernel dominated by sin/exp/sqrt can bottleneck on the SFU, visible as math-pipe throttle — reduce/approximate those ops to relieve it.',
      ref: 'CUDA C++ Programming Guide §G (instruction throughput)',
      tags: ['instruction-throughput', 'sfu', 'transcendental'],
    },
    {
      d: 5,
      q: 'elect.sync (or cg::invoke_one) is used to…',
      o: [
        'Synchronize the grid',
        'Elect one leader lane among active threads',
        'Reduce across the block',
        'Launch a child kernel',
      ],
      a: 1,
      x: [
        'Broadcast from lane 0',
        'Count active lanes',
        'Sort the warp',
      ],
      e: 'Leader election picks one lane to do a singleton action (e.g. the warp-aggregated atomic’s single update). elect.sync (Hopper) / cooperative-groups helpers provide an efficient way to choose and identify that leader among active lanes.',
      ref: 'PTX elect.sync; warp aggregation',
      tags: ['elect', 'leader-election', 'warp'],
    },
    {
      d: 4,
      q: 'The maximum resident blocks per SM (a hardware cap, e.g. 16/24/32 depending on architecture) becomes the binding occupancy limit when…',
      o: [
        'Blocks are very large',
        'Blocks are small, hitting the block-count cap',
        'Registers are high',
        'Shared memory is large',
      ],
      a: 1,
      x: [
        'The grid is small',
        'Divergence is high',
        'Tensor cores are used',
      ],
      e: 'With tiny blocks (e.g. 32 threads), reaching the max-blocks-per-SM cap may leave thread/warp slots unused. Using larger blocks (≥128) avoids hitting the block cap before the thread budget — a reason to prefer bigger blocks.',
      ref: 'CUDA Occupancy Calculator; max blocks per SM',
      tags: ['occupancy', 'max-blocks', 'block-size'],
    },
    {
      d: 4,
      q: 'Thread-block "swizzling" (rasterization order) primarily improves…',
      o: [
        'Register usage',
        'L2 cache locality across concurrent blocks',
        'Divergence',
        'Shared-memory banks',
      ],
      a: 1,
      x: [
        'Occupancy',
        'Precision',
        'Launch overhead',
      ],
      e: 'Remapping blockIdx to a tiled/space-filling traversal of output tiles clusters the working sets of simultaneously-active blocks, increasing L2 reuse of shared operands (e.g. in GEMM) and cutting HBM traffic.',
      ref: 'GEMM threadblock swizzle; L2 locality',
      tags: ['swizzling', 'l2-locality', 'gemm'],
    },
    {
      d: 4,
      q: 'On an SM with 2048 max threads and 64 KB registers per... actually 65536 registers, a kernel using 64 registers/thread can have at most how many resident threads (register-limited)?',
      o: ['2048', '1024', '512', '4096'],
      a: 1,
      x: ['256', '1536', '3072'],
      e: '65536 registers / 64 per thread = 1024 threads (register-limited), i.e. 50% of the 2048 max. Lowering to 32 registers/thread would allow the full 2048. This shows how register usage directly bounds occupancy.',
      ref: 'CUDA Occupancy Calculator; register limits',
      tags: ['registers', 'occupancy', 'calculation'],
    },
    {
      d: 3,
      q: 'Why does the SM keep issuing from many warps round-robin (roughly) rather than finishing one warp at a time?',
      o: [
        'To save power',
        'To hide latency by interleaving warps',
        'Because warps must finish together',
        'To reduce registers',
      ],
      a: 1,
      x: [
        'To increase divergence',
        'Because of coalescing',
        'To order memory',
      ],
      e: 'Interleaving warps is the core latency-hiding mechanism: a stalled warp simply isn’t eligible while others issue. Running one warp to completion would expose every stall, wasting cycles. (Policies like GTO add locality nuance.)',
      ref: 'PMPP Ch. 4; warp scheduling',
      tags: ['scheduling', 'latency-hiding', 'warps'],
    },
    {
      d: 4,
      q: 'Packed FP16 (half2) elementwise math reaches up to ~2× the FP32 FMA rate on CUDA cores because…',
      o: [
        'It uses tensor cores',
        'Two FP16 packed per 32-bit register',
        'FP16 is more accurate',
        'It avoids registers',
      ],
      a: 1,
      x: [
        'It halves precision automatically',
        'It runs on SFUs',
        'It uses the L2',
      ],
      e: 'half2 packed ops (hadd2/hfma2) process two halves per instruction, so peak FP16 elementwise throughput is about double FP32 on the CUDA cores — distinct from tensor-core matmul throughput. Kernels must use the *2 intrinsics and aligned data.',
      ref: 'CUDA Math API; half2 throughput',
      tags: ['half2', 'throughput', 'packed-math'],
    },
    {
      d: 5,
      q: 'A tensor-core GEMM shows low Tensor pipe utilization despite using mma. A common cause is…',
      o: [
        'Too many registers',
        'MMAs starved of operand supply',
        'High occupancy',
        'A divergent branch',
      ],
      a: 1,
      x: [
        'Too few blocks only',
        'ECC enabled',
        'FP64 usage',
      ],
      e: 'Tensor cores need a steady operand stream. If the shared-memory/ldmatrix/async-copy feed can’t keep up (poor pipelining, bank conflicts, small tiles), the MMA pipe stalls between ops. Deeper pipelining and better SMEM feeding raise utilization.',
      ref: 'Nsight Compute; tensor-pipe feeding',
      tags: ['tensor-core', 'utilization', 'operand-feed'],
    },
    {
      d: 3,
      q: 'An "IMC Miss" / constant-cache miss stall indicates warps waiting on…',
      o: [
        'Global memory',
        'A constant-cache miss',
        'A barrier',
        'A tensor-core op',
      ],
      a: 1,
      x: [
        'Shared memory',
        'A register spill',
        'A divergent branch',
      ],
      e: 'Constant-cache misses stall warps when the small constant cache misses (e.g. non-uniform constant access, or cold immediate constants). Uniform warp access to constant memory (broadcast) and good locality minimize these stalls.',
      ref: 'Nsight Compute (constant/IMC stalls)',
      tags: ['constant-cache', 'stall-reasons', 'imc'],
    },
    {
      d: 4,
      q: 'Why can a kernel with abundant warps still show low IPC due to "Long Scoreboard" stalls?',
      o: [
        'Too few registers',
        'Most warps wait on global-memory loads',
        'A barrier deadlock',
        'Tensor-core saturation',
      ],
      a: 1,
      x: [
        'Divergence only',
        'Register spilling',
        'A host bottleneck',
      ],
      e: 'High occupancy hides latency only if memory throughput/parallelism keeps up. When the kernel is memory-bound (or under-issuing loads), warps pile up on long-scoreboard waits and IPC drops — fix by reducing/streamlining memory traffic.',
      ref: 'Nsight Compute (long scoreboard); memory-bound',
      tags: ['long-scoreboard', 'ipc', 'memory-bound'],
    },
    {
      d: 4,
      q: 'Concurrent execution of TWO different kernels on the SAME SM is possible when…',
      o: [
        'Never — one kernel per SM',
        'Different streams and spare SM resources',
        'They share a block',
        'They use the default stream',
      ],
      a: 1,
      x: [
        'They have the same name',
        'They use tensor cores',
        'Only on multiple GPUs',
      ],
      e: 'If a kernel leaves spare SM capacity (registers/shared/warps), another stream’s kernel can place blocks on the same SM concurrently. Full-occupancy kernels leave no room, so concurrency benefits small/underutilizing kernels.',
      ref: 'CUDA concurrent kernels; resource sharing',
      tags: ['concurrent-kernels', 'sm', 'occupancy'],
    },
    {
      d: 3,
      q: 'GPU boost clocks affect cycle-based measurements (clock64) because…',
      o: [
        'The cycle counter stops',
        'Clock frequency varies with power/thermal',
        'Cycles are random',
        'It runs on the host',
      ],
      a: 1,
      x: [
        'clock64 measures host time',
        'It disables the SM',
        'Cycles double under boost',
      ],
      e: 'clock64 counts SM cycles, but the SM clock changes with boost, so cycle counts don’t map to a constant wall-time unless you lock the clock (nvidia-smi -lgc). For relative cycle comparisons within a run it’s fine; for absolute timing, lock clocks or use events.',
      ref: 'clock64; boost clocks; timing',
      tags: ['clock64', 'boost', 'timing'],
    },
    {
      d: 5,
      q: 'In a warp-specialized Hopper kernel, the producer warpgroup uses TMA + mbarrier while the consumer warpgroup runs wgmma. The benefit over a non-specialized kernel is…',
      o: [
        'Lower register usage only',
        'TMA loads and wgmma math overlap continuously',
        'Avoiding shared memory',
        'Higher occupancy',
      ],
      a: 1,
      x: [
        'Fewer blocks',
        'Lower precision',
        'No synchronization',
      ],
      e: 'Specialization assigns dedicated producer (TMA) and consumer (wgmma) warpgroups coordinated by mbarriers, so transfer and math pipelines run concurrently and continuously. This sustains tensor-core throughput better than every warp interleaving both roles.',
      ref: 'CUTLASS 3.x Hopper warp specialization',
      tags: ['warp-specialization', 'tma', 'wgmma', 'hopper'],
    },
    {
      d: 3,
      q: 'A block of 1024 threads (max) on an SM with 2048 max resident threads allows at most how many resident blocks (thread-limited)?',
      o: ['1', '2', '4', '8'],
      a: 1,
      x: ['3', '16', '32'],
      e: '2048 / 1024 = 2 blocks by the thread limit. Large blocks limit block-level concurrency per SM; if one block stalls at a barrier, only one other can hide it. Mid-size blocks (128–256) often balance this better.',
      ref: 'CUDA Occupancy Calculator; thread limit',
      tags: ['occupancy', 'block-size', 'residency'],
    },
    {
      d: 4,
      q: 'Why does excessive instruction count (e.g. from heavy unrolling) sometimes reduce performance via the instruction cache?',
      o: [
        'I-cache is infinite',
        'Large code overflows the instruction cache',
        'It increases occupancy',
        'It improves coalescing',
      ],
      a: 1,
      x: [
        'It reduces registers',
        'It disables the L2',
        'It always helps',
      ],
      e: 'Unrolling boosts ILP but inflates code size. Past the I-cache capacity, fetch misses add stalls. There’s a sweet spot: enough unrolling for ILP without blowing the instruction cache — measure both ways.',
      ref: 'Instruction cache; unrolling trade-offs',
      tags: ['instruction-cache', 'unrolling', 'code-size'],
    },
    {
      d: 4,
      q: 'cooperative_groups::invoke_one(group, fn) (or elect-based patterns) is a clean way to…',
      o: [
        'Run fn on all lanes',
        'Have one thread run fn, others skip',
        'Synchronize the grid',
        'Reduce values',
      ],
      a: 1,
      x: [
        'Broadcast a value',
        'Launch a kernel',
        'Sort the group',
      ],
      e: 'invoke_one elects a single member to run the callable, abstracting leader election. It’s used for warp/block-singleton work (one atomic per group, one-time setup) more safely than hard-coding threadIdx==0 under divergence.',
      ref: 'CUDA Cooperative Groups (invoke_one)',
      tags: ['cooperative-groups', 'invoke-one', 'leader'],
    },
    {
      d: 3,
      q: 'For latency-bound kernels (low occupancy, neither bandwidth nor compute saturated), increasing ILP helps because…',
      o: [
        'It reduces memory',
        'More independent work per thread hides stalls',
        'It increases divergence',
        'It uses tensor cores',
      ],
      a: 1,
      x: [
        'It lowers precision',
        'It avoids the L2',
        'It pins data',
      ],
      e: 'Latency-bound means too little work in flight. Adding ILP (unrolling, multiple accumulators, independent loads) gives the scheduler ready instructions during latencies — an alternative to raising occupancy, and the basis of low-occupancy peak kernels.',
      ref: 'Volkov; latency-bound remedies',
      tags: ['latency-bound', 'ilp', 'optimization'],
    },
    {
      d: 5,
      q: 'A cooperative-launch grid is sized to occupancyMaxActiveBlocksPerSM × numSMs so that…',
      o: [
        'It maximizes the grid',
        'Every block stays resident for grid.sync()',
        'It minimizes registers',
        'It enables tensor cores',
      ],
      a: 1,
      x: [
        'It avoids shared memory',
        'It increases divergence',
        'It is faster',
      ],
      e: 'grid.sync() needs all blocks co-resident. You query the per-SM resident-block capacity for the kernel and size the grid to that × SM count, guaranteeing co-residency. Oversizing breaks grid-wide synchronization.',
      ref: 'Cooperative Groups; cooperative launch sizing',
      tags: ['cooperative-launch', 'grid-sync', 'occupancy'],
    },
    {
      d: 3,
      q: 'Two warps in the same block accessing different shared-memory banks in the same cycle…',
      o: [
        'Always conflict',
        'Do not conflict; banks are per-warp',
        'Deadlock',
        'Serialize',
      ],
      a: 1,
      x: [
        'Share one bank',
        'Require a barrier',
        'Use the L2',
      ],
      e: 'Bank-conflict analysis is per-warp: it concerns whether the 32 lanes of ONE warp hit distinct banks. Different warps are scheduled separately and don’t cause conflicts with each other (though they contend for shared-memory bandwidth over time).',
      ref: 'CUDA C++ Programming Guide §5.3.2; bank conflicts',
      tags: ['bank-conflict', 'warp', 'shared-memory'],
    },
    {
      d: 4,
      q: 'The "achieved occupancy" being much lower than "theoretical" for a short kernel with few blocks is explained by…',
      o: [
        'Register spilling',
        'Tail effect: too short to fill the SMs',
        'Bank conflicts',
        'Divergence',
      ],
      a: 1,
      x: [
        'High register use',
        'A host bottleneck',
        'Tensor-core starvation',
      ],
      e: 'Theoretical occupancy is the steady-state ceiling; a brief kernel spends much of its life ramping up/draining without ever filling the SMs, so achieved occupancy is far lower. More blocks (or fused/longer work) help.',
      ref: 'Nsight Compute; tail effect',
      tags: ['achieved-occupancy', 'tail-effect', 'analysis'],
    },
    {
      d: 4,
      q: 'redux.sync (hardware warp reduction, Ampere+) accelerates…',
      o: [
        'Floating-point reductions only',
        'Warp-wide integer reductions in one instruction',
        'Global reductions',
        'Matrix multiply',
      ],
      a: 1,
      x: [
        'Block-wide barriers',
        'Texture sampling',
        'Atomic operations',
      ],
      e: 'redux.sync performs a warp-level integer reduction in hardware (one instruction), which cooperative-groups reduce/CUB use where available — faster than the log2(32)-step shuffle reduction for integer types. FP still uses shuffles.',
      ref: 'PTX redux.sync; Cooperative Groups',
      tags: ['redux', 'warp-reduction', 'ampere'],
    },
    {
      d: 5,
      q: 'Why does a kernel sometimes get FASTER when you REDUCE its occupancy by using more shared memory per block (bigger tiles)?',
      o: [
        'Lower occupancy is always faster',
        'Bigger tiles boost reuse, outweighing lost occupancy',
        'It reduces registers',
        'It enables tensor cores',
      ],
      a: 1,
      x: [
        'It avoids divergence',
        'It compresses data',
        'It pins memory',
      ],
      e: 'Larger shared-memory tiles cut global traffic via reuse, often the dominant factor for tiled GEMM/stencils. Even though occupancy drops, the higher arithmetic intensity wins — the occupancy-vs-reuse trade-off, opposite to the naive "max occupancy" rule.',
      ref: 'CUTLASS tiling; occupancy vs reuse',
      tags: ['occupancy', 'tiling', 'reuse', 'tradeoff'],
    },
    {
      d: 3,
      q: 'Across one kernel, if Nsight shows ~6 eligible warps per scheduler on average, you can infer…',
      o: [
        'The kernel is starved',
        'Ample parallelism; occupancy is sufficient',
        'Occupancy is too low',
        'It is latency-bound',
      ],
      a: 1,
      x: [
        'Registers spilled',
        'It is host-bound',
        'It has bank conflicts',
      ],
      e: 'Many eligible warps per scheduler means the SM rarely lacks something to issue, so latency is being hidden. The bottleneck is then throughput (bandwidth or a compute pipe), not occupancy — focus optimization accordingly.',
      ref: 'Nsight Compute (Scheduler Statistics)',
      tags: ['eligible-warps', 'scheduler', 'analysis'],
    },
    {
      d: 4,
      q: 'Dynamic parallelism adds overhead per child launch, so it is worthwhile mainly when…',
      o: [
        'Always',
        'Irregular recursion saves more than it costs',
        'The child kernel is tiny',
        'There is one block',
      ],
      a: 1,
      x: [
        'You want lower occupancy',
        'For elementwise ops',
        'To avoid streams',
      ],
      e: 'Each device-side launch costs overhead, so dynamic parallelism pays off for genuinely irregular/recursive algorithms (adaptive mesh, graph expansion) where it removes host synchronization or large amounts of speculative work — not for regular, flat workloads.',
      ref: 'CUDA dynamic parallelism; when to use',
      tags: ['dynamic-parallelism', 'overhead', 'irregular'],
    },
    {
      d: 5,
      q: 'On Hopper, a thread block cluster lets blocks cooperate via DSMEM and cluster.sync(). The scheduling guarantee that makes this safe is…',
      o: [
        'Blocks run in order',
        'Cluster blocks are co-scheduled on one GPC',
        'Blocks share registers',
        'Blocks run on the host',
      ],
      a: 1,
      x: [
        'Blocks share one SM',
        'Clusters span GPUs',
        'Blocks never sync',
      ],
      e: 'Unlike ordinary blocks (no co-residency guarantee), a cluster’s blocks are guaranteed concurrently resident within a GPC. That guarantee underpins cluster.sync() and DSMEM — a new cooperation tier between block and grid.',
      ref: 'CUDA C++ Programming Guide (Thread Block Clusters)',
      tags: ['clusters', 'co-residency', 'hopper'],
    },
  ],
  { idPrefix: 'execution-d' }
)
