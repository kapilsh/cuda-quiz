import { defineQuestions } from './_helpers.js'

// Memory — batch E. VMM access/granularity, multicast/fabric, pools/IPC,
// access-policy hitRatio, bandwidth math, fragmentation. Distinct from memory.js/.b/.c/.d.
export default defineQuestions(
  'memory',
  [
    {
      d: 5,
      q: 'cuMemSetAccess (Virtual Memory Management API) is used after mapping physical memory to…',
      o: [
        'Free the memory',
        'Grant specific devices access to a VA range',
        'Compress the memory',
        'Pin the memory',
      ],
      a: 1,
      x: [
        'Launch a kernel',
        'Profile access',
        'Reset the GPU',
      ],
      e: 'With the VMM API you reserve VA, allocate physical handles, map them, then set access descriptors (cuMemSetAccess) per device. This gives fine-grained control over which GPUs can access an allocation — more flexible than blanket peer access.',
      ref: 'CUDA Driver API (VMM, cuMemSetAccess)',
      tags: ['vmm', 'access-descriptors', 'p2p'],
    },
    {
      d: 5,
      q: 'cuMemGetAllocationGranularity is queried when using the VMM API because…',
      o: [
        'It sets the block size',
        'Allocations must align to the granularity',
        'It picks the stream',
        'It reduces precision',
      ],
      a: 1,
      x: [
        'It frees memory',
        'It profiles bandwidth',
        'It compresses pages',
      ],
      e: 'The VMM API operates at a hardware page granularity; allocations and mappings must be multiples of it. Querying the granularity lets you align VA reservations and physical chunks correctly for growable/segmented allocators.',
      ref: 'CUDA Driver API (cuMemGetAllocationGranularity)',
      tags: ['vmm', 'granularity', 'allocation'],
    },
    {
      d: 5,
      q: 'NVLink multicast (cuMulticast / multicast objects) lets multiple GPUs…',
      o: [
        'Share registers',
        'Broadcast/reduce a region across GPUs',
        'Run one kernel',
        'Compress data',
      ],
      a: 1,
      x: [
        'Use the CPU',
        'Disable NVLink',
        'Lower precision',
      ],
      e: 'Multicast objects map a region across GPUs so the NVLink fabric can multicast writes (or reduce reads) to all members — hardware support that NCCL (NVLS) and advanced kernels use for efficient one-to-many / reduction communication.',
      ref: 'CUDA multicast (cuMulticast); NVLS',
      tags: ['multicast', 'nvlink', 'nvls'],
    },
    {
      d: 5,
      q: 'Fabric memory handles (and Multi-Node NVLink, MNNVL) extend memory sharing by…',
      o: [
        'Using PCIe',
        'Share GPU memory across nodes over NVLink',
        'Compressing memory',
        'Running on the CPU',
      ],
      a: 1,
      x: [
        'Using InfiniBand only',
        'Disabling P2P',
        'Pinning host memory',
      ],
      e: 'MNNVL/fabric handles extend the NVLink address space beyond a single node (e.g. a 72-GPU NVLink domain), enabling cross-node GPU memory sharing over NVLink — the substrate for treating a rack as one large accelerator.',
      ref: 'NVIDIA MNNVL; fabric handles',
      tags: ['mnnvl', 'fabric', 'nvlink', 'multi-node'],
    },
    {
      d: 4,
      q: 'The accessPolicyWindow’s hitRatio parameter controls…',
      o: [
        'The cache size',
        'Fraction of the window persisting',
        'The block size',
        'The clock',
      ],
      a: 1,
      x: [
        'The DRAM size',
        'The warp size',
        'The precision',
      ],
      e: 'hitRatio lets you mark only a portion of a window as persisting when the hot region exceeds the L2 set-aside, balancing which accesses get preferential L2 residency. It tunes the persisting-access behavior for partially-cacheable working sets.',
      ref: 'CUDA C++ Programming Guide (access policy window, hitRatio)',
      tags: ['l2-persisting', 'hit-ratio', 'access-policy'],
    },
    {
      d: 4,
      q: 'Approximate shared-memory bandwidth per SM is computed as…',
      o: [
        'banks × clock',
        '32 banks × 4 bytes × SM clock',
        'DRAM bandwidth / 32',
        'warpSize × clock',
      ],
      a: 1,
      x: [
        '1 byte × clock',
        'L2 size × clock',
        'registers × clock',
      ],
      e: 'Shared memory delivers up to 32×4 bytes per cycle per SM (one word per bank), so aggregate on-chip bandwidth is enormous compared to HBM. Conflict-free access realizes this; bank conflicts reduce it by serializing.',
      ref: 'Shared-memory bandwidth; banks',
      tags: ['shared-memory', 'bandwidth', 'banks'],
    },
    {
      d: 3,
      q: 'cudaMemcpy (the blocking variant) on the default stream is "implicitly synchronizing," meaning…',
      o: [
        'It is asynchronous',
        'It waits for prior work and blocks the host',
        'It runs on the GPU only',
        'It never blocks',
      ],
      a: 1,
      x: [
        'It uses pinned memory',
        'It is the same as Async',
        'It only blocks for large sizes',
      ],
      e: 'Blocking cudaMemcpy synchronizes: it waits for relevant preceding device work and blocks the host until done. This is why a kernel followed by cudaMemcpy "just works" (the copy waits), but it also prevents overlap — use cudaMemcpyAsync for that.',
      ref: 'CUDA Runtime API; cudaMemcpy synchronization',
      tags: ['memcpy', 'synchronization', 'default-stream'],
    },
    {
      d: 4,
      q: 'cudaMemPoolSetAttribute(pool, cudaMemPoolAttrReleaseThreshold, value) controls…',
      o: [
        'The pool size',
        'Bytes the pool caches before releasing',
        'The block size',
        'The precision',
      ],
      a: 1,
      x: [
        'The number of streams',
        'The clock',
        'The bandwidth',
      ],
      e: 'The release threshold sets a "high-water mark" the pool retains for reuse; frees beyond it return memory to the driver. Tuning it trades allocation speed (reuse) against memory footprint in long-running services.',
      ref: 'CUDA stream-ordered allocator (release threshold)',
      tags: ['memory-pool', 'release-threshold', 'footprint'],
    },
    {
      d: 4,
      q: 'Memory FRAGMENTATION in a long-running GPU service manifests as…',
      o: [
        'Slow kernels',
        'OOM despite enough total free memory',
        'Bank conflicts',
        'Low occupancy',
      ],
      a: 1,
      x: [
        'High bandwidth',
        'Register spills',
        'Divergence',
      ],
      e: 'Repeated alloc/free of varied sizes fragments the heap, so a large contiguous allocation fails even with sufficient aggregate free memory. Caching allocators, pools, and VMM-based expandable segments mitigate fragmentation.',
      ref: 'GPU memory fragmentation; caching allocators',
      tags: ['fragmentation', 'allocation', 'oom'],
    },
    {
      d: 5,
      q: 'cudaMemPool IPC export (cudaMemPoolExportToShareableHandle) enables…',
      o: [
        'Cross-node sharing',
        'Sharing a memory pool across processes',
        'Compression',
        'Host execution',
      ],
      a: 1,
      x: [
        'Pinning memory',
        'Profiling',
        'Disabling pools',
      ],
      e: 'Pool IPC lets the stream-ordered allocator’s allocations be shared between processes (via shareable handles), extending interprocess sharing to the async-allocator world — useful for multi-process pipelines using pooled memory.',
      ref: 'CUDA memory pool IPC',
      tags: ['memory-pool', 'ipc', 'multi-process'],
    },
    {
      d: 3,
      q: 'The NVIDIA bandwidthTest sample reports H2D, D2H, and D2D bandwidths to…',
      o: [
        'Compile kernels',
        'Sanity-check transfer bandwidths',
        'Profile a kernel',
        'Train a model',
      ],
      a: 1,
      x: [
        'Lock clocks',
        'Allocate memory',
        'Detect races',
      ],
      e: 'bandwidthTest measures host↔device and device↔device copy bandwidth (with pinned/pageable options). Numbers far below the expected PCIe/NVLink/HBM rates indicate problems (pageable memory, wrong link, topology) worth fixing.',
      ref: 'CUDA samples (bandwidthTest)',
      tags: ['bandwidth-test', 'transfers', 'diagnostics'],
    },
    {
      d: 4,
      q: 'ECC and DRAM refresh reduce ACHIEVABLE bandwidth below the raw peak because…',
      o: [
        'They add compute',
        'ECC and refresh consume some bus time',
        'They disable the cache',
        'They lower the clock',
      ],
      a: 1,
      x: [
        'They increase bandwidth',
        'They are free',
        'They only affect latency',
      ],
      e: 'ECC stores/checks extra bits and DRAM must refresh, both eating into usable bandwidth and capacity. This (plus protocol/scheduling overheads) is why achieved bandwidth tops out around 80–90% of the datasheet peak even for ideal copies.',
      ref: 'DRAM/ECC overhead; achievable bandwidth',
      tags: ['ecc', 'refresh', 'bandwidth'],
    },
    {
      d: 4,
      q: 'Register-file bandwidth vastly exceeds shared-memory bandwidth, which is why…',
      o: [
        'Registers are slow',
        'Keep reused operands in registers, not SMEM',
        'Shared memory is unused',
        'Registers are off-chip',
      ],
      a: 1,
      x: [
        'Shared memory is faster',
        'Registers have banks conflicts always',
        'Registers are global',
      ],
      e: 'The register file supplies operands at the rate the ALUs/tensor cores consume them — higher than shared memory. High-performance kernels stage a larger tile in shared memory but keep the actively-multiplied micro-tile in registers for peak throughput.',
      ref: 'GEMM register blocking; register bandwidth',
      tags: ['register-bandwidth', 'shared-memory', 'gemm'],
    },
    {
      d: 4,
      q: 'Setting the L2 persisting set-aside is done via cudaDeviceSetLimit(cudaLimitPersistingL2CacheSize, bytes). If you set an access window LARGER than this set-aside…',
      o: [
        'It is faster',
        'Persistence thrashes beyond the set-aside',
        'It increases the L2',
        'It disables the L2',
      ],
      a: 1,
      x: [
        'It compresses data',
        'It pins host memory',
        'It crashes',
      ],
      e: 'The persisting window should fit within the reserved L2 set-aside; a larger window over-subscribes it and the persisting lines evict each other, negating the benefit. Use hitRatio or shrink the window to match the set-aside.',
      ref: 'CUDA L2 persisting; set-aside sizing',
      tags: ['l2-persisting', 'set-aside', 'tuning'],
    },
    {
      d: 3,
      q: 'A kernel that revisits a small (~few-MB) lookup table across all blocks benefits from…',
      o: [
        'Putting it in registers',
        'Keeping it resident/persisting in L2',
        'Recomputing it',
        'Putting it in local memory',
      ],
      a: 1,
      x: [
        'Putting it per-thread',
        'Using atomics',
        'Pinning host memory',
      ],
      e: 'A reused table that fits in L2 is served on-chip across all blocks, avoiding repeated HBM reads. The L2 persisting window can pin it. (If it’s tiny and uniformly accessed, constant memory broadcast is another option.)',
      ref: 'L2 reuse; persisting access',
      tags: ['l2-cache', 'lookup-table', 'reuse'],
    },
    {
      d: 5,
      q: 'In a cluster GEMM, distributed shared memory (DSMEM) lets a block READ a tile from a peer block’s shared memory. The benefit vs each block loading from HBM is…',
      o: [
        'Higher precision',
        'Fetch from HBM once, share on-chip',
        'Lower occupancy',
        'More registers',
      ],
      a: 1,
      x: [
        'It avoids tensor cores',
        'It compresses data',
        'It uses the host',
      ],
      e: 'DSMEM (with TMA multicast) lets cluster blocks share operand tiles via on-chip SM-to-SM transfers instead of each re-reading HBM, reducing DRAM traffic and improving GEMM efficiency on Hopper.',
      ref: 'Hopper DSMEM; cluster GEMM',
      tags: ['dsmem', 'clusters', 'bandwidth', 'gemm'],
    },
    {
      d: 4,
      q: 'A "bank conflict" of degree N in shared memory means the access…',
      o: [
        'Is N times faster',
        'Serializes into N transactions',
        'Uses N banks',
        'Fails',
      ],
      a: 1,
      x: [
        'Broadcasts to N threads',
        'Skips N threads',
        'Uses the L2',
      ],
      e: 'An N-way conflict forces the shared-memory access to be served in N sequential transactions, roughly N× the latency/throughput cost. Padding or relayout to spread a warp’s accesses across distinct banks restores single-transaction service.',
      ref: 'CUDA shared memory; bank conflict degree',
      tags: ['bank-conflict', 'degree', 'shared-memory'],
    },
    {
      d: 4,
      q: 'When P2P access between two GPUs is enabled, a kernel on GPU A reading GPU B’s memory…',
      o: [
        'Copies it to GPU A first',
        'Reads over the link on demand (NVLink/PCIe)',
        'Runs on GPU B',
        'Uses host memory',
      ],
      a: 1,
      x: [
        'Is always cached in A’s L2',
        'Is free',
        'Requires managed memory',
      ],
      e: 'P2P lets a kernel directly access peer memory over the link; performance depends on the interconnect (NVLink fast, PCIe slow) and the access pattern. Heavy remote access can bottleneck on the link, so locality/replication still matters.',
      ref: 'CUDA P2P; remote memory access',
      tags: ['p2p', 'nvlink', 'remote-access'],
    },
    {
      d: 3,
      q: 'A reason to prefer cudaMallocAsync (pooled) over repeated cudaMalloc in a serving loop is…',
      o: [
        'It is more accurate',
        'The async pool reuses blocks without syncs',
        'It compresses memory',
        'It pins memory',
      ],
      a: 1,
      x: [
        'It increases bandwidth',
        'It uses tensor cores',
        'It disables the L2',
      ],
      e: 'Frequent cudaMalloc/cudaFree add synchronization and latency. The stream-ordered pool serves repeated requests from cached blocks without device-wide syncs, keeping a high-throughput serving loop responsive.',
      ref: 'CUDA stream-ordered allocator; serving',
      tags: ['cudaMallocAsync', 'pool', 'serving'],
    },
    {
      d: 5,
      q: 'Why does the VMM-based "expandable segment" approach reduce OOM failures in training frameworks?',
      o: [
        'It compresses tensors',
        'Grows a contiguous buffer, no realloc-copy',
        'It uses the CPU',
        'It pins memory',
      ],
      a: 1,
      x: [
        'It increases total memory',
        'It disables caching',
        'It lowers precision',
      ],
      e: 'Fixed-size block allocators fragment over time, so a big contiguous request fails despite free memory. Expandable segments (VMM) grow a contiguous VA region by mapping pages, avoiding the fragmentation and copies — reducing OOMs.',
      ref: 'PyTorch expandable segments; VMM',
      tags: ['vmm', 'fragmentation', 'oom'],
    },
    {
      d: 4,
      q: 'For a warp loading 32 consecutive doubles (8 bytes each = 256 bytes), the global access maps to…',
      o: [
        'One 32-byte sector',
        'Two 128-byte lines, fully coalesced',
        '32 separate transactions',
        'No transactions',
      ],
      a: 1,
      x: [
        'One 128-byte line',
        '256 sectors',
        'Four sectors',
      ],
      e: '32 doubles span 256 bytes = two 128-byte lines, fully used when contiguous and aligned — coalesced. Element size affects how many lines a warp touches, but contiguity/alignment keep efficiency high regardless of 4- vs 8-byte elements.',
      ref: 'Coalescing; element size and cache lines',
      tags: ['coalescing', 'double', 'cache-lines'],
    },
    {
      d: 4,
      q: 'cudaMemAdvise(SetReadMostly) plus prefetching to multiple GPUs lets a read-only managed buffer…',
      o: [
        'Be writable everywhere',
        'Be replicated read-only on each GPU',
        'Be pinned on the host',
        'Be compressed',
      ],
      a: 1,
      x: [
        'Be device-only',
        'Be evicted',
        'Be atomic',
      ],
      e: 'ReadMostly permits read-only replicas; prefetching to each GPU creates local copies so multi-GPU readers don’t thrash a single migrating copy. Ideal for read-only data (e.g. embeddings/weights) shared across GPUs via managed memory.',
      ref: 'CUDA Unified Memory; ReadMostly replication',
      tags: ['unified-memory', 'read-mostly', 'replication'],
    },
    {
      d: 5,
      q: 'NVLink multicast (NVLS) specifically accelerates all-reduce by…',
      o: [
        'Compressing data',
        'In-fabric reduction halves traffic',
        'Using the CPU',
        'Disabling NVLink',
      ],
      a: 1,
      x: [
        'Lowering precision',
        'Skipping GPUs',
        'Pinning memory',
      ],
      e: 'NVLS uses multicast + in-fabric reduction so endpoints move about half the data of a ring all-reduce with lower latency. The multicast memory objects (cuMulticast) are the device-side mechanism NCCL leverages for this.',
      ref: 'NVLink SHARP/NVLS; multicast',
      tags: ['nvls', 'multicast', 'allreduce'],
    },
    {
      d: 3,
      q: 'A read-only input reused across threads but WRITTEN elsewhere in the kernel should NOT be tagged const __restrict__/__ldg because…',
      o: [
        'It is faster',
        'The read-only cache can return stale data',
        'It uses more registers',
        'It disables coalescing',
      ],
      a: 1,
      x: [
        'It is always safe',
        'It needs atomics',
        'It compresses data',
      ],
      e: 'The read-only/LDG path caches assuming the data won’t change. Routing data through it that is later written (even via an alias) risks stale reads. Reserve const __restrict__/__ldg for data that is genuinely read-only for the kernel’s lifetime.',
      ref: 'CUDA read-only cache; correctness',
      tags: ['read-only-cache', 'correctness', 'ldg'],
    },
    {
      d: 4,
      q: 'For a strided reduction over columns of a row-major matrix (each thread sums a column), performance suffers because…',
      o: [
        'Columns are contiguous',
        'Column access is strided and uncoalesced',
        'Reductions are slow',
        'It needs atomics',
      ],
      a: 1,
      x: [
        'Rows are uncached',
        'It overflows shared memory',
        'It uses tensor cores',
      ],
      e: 'In row-major storage, a column has elements spaced by the row length, so per-thread column reads are strided and uncoalesced. Reorganizing (transpose, or process columns via a shared-memory tile loaded coalesced) fixes the access pattern.',
      ref: 'Coalescing; column access',
      tags: ['coalescing', 'strided', 'reduction'],
    },
    {
      d: 5,
      q: 'The reason multicast objects must be created and bound BEFORE use (cuMulticastCreate/AddDevice/BindMem) is…',
      o: [
        'For speed only',
        'The fabric must know members and mapping',
        'To compress data',
        'To pin host memory',
      ],
      a: 1,
      x: [
        'To disable NVLink',
        'To lower precision',
        'To free memory',
      ],
      e: 'A multicast object enumerates participating devices and binds their physical memory, establishing the hardware mapping the NVLink fabric uses to multicast/reduce. Setup is explicit so the routing is well-defined before any multicast access.',
      ref: 'CUDA multicast API (cuMulticast*)',
      tags: ['multicast', 'setup', 'nvlink'],
    },
    {
      d: 3,
      q: 'A kernel that is L1/shared-bandwidth-bound (not DRAM-bound) might be improved by…',
      o: [
        'Adding more global loads',
        'Vectorize SMEM, cut transactions, use registers',
        'Lowering occupancy',
        'Using FP64',
      ],
      a: 1,
      x: [
        'Disabling the L2',
        'Increasing the grid',
        'Using constant memory for outputs',
      ],
      e: 'If the bottleneck is the on-chip memory pipe (not DRAM), reduce its pressure: vectorize/coalesce shared accesses, eliminate bank conflicts, and shift reused values into registers — lowering the shared-memory transaction count.',
      ref: 'Nsight Compute; shared-memory-bound kernels',
      tags: ['shared-memory-bound', 'vectorization', 'registers'],
    },
    {
      d: 4,
      q: 'Why does cudaMemcpyAsync between two pinned host buffers (H2H) on a stream still benefit from being async?',
      o: [
        'It uses the GPU',
        'It overlaps with other work, non-blocking',
        'It is faster than memcpy',
        'It uses tensor cores',
      ],
      a: 1,
      x: [
        'It pins the memory',
        'It compresses data',
        'It runs on the device',
      ],
      e: 'Stream-ordered async copies (including H2H) return immediately and order within the stream, enabling overlap with other operations and keeping the host non-blocked — useful in pipelines even for host-side staging copies.',
      ref: 'CUDA Runtime API (cudaMemcpyAsync H2H)',
      tags: ['memcpy-async', 'host-to-host', 'overlap'],
    },
    {
      d: 5,
      q: 'On an NVL72 system, treating 72 GPUs’ HBM as a shared pool (via fabric memory) is powerful for inference because…',
      o: [
        'It compresses weights',
        'A huge model spreads across HBM over NVLink',
        'It runs on the CPU',
        'It lowers precision',
      ],
      a: 1,
      x: [
        'It disables NVLink',
        'It uses PCIe',
        'It pins host memory',
      ],
      e: 'Fabric-shared GPU memory across an NVLink domain lets enormous models/KV caches reside across many GPUs’ HBM with fast NVLink access, so a single logical model can exceed one GPU’s memory while keeping remote-access penalties modest.',
      ref: 'NVIDIA NVL72; fabric memory',
      tags: ['nvl72', 'fabric', 'inference', 'memory-pool'],
    },
  ],
  { idPrefix: 'memory-e' }
)
