import { defineQuestions } from './_helpers.js'

// GPU Architecture — batch B. SM partitions, interconnects, MIG, sparsity,
// generations, DPX, chiplets. Distinct from architecture.js (batch A).
export default defineQuestions(
  'architecture',
  [
    {
      d: 3,
      q: 'A modern SM is divided into how many processing blocks (sub-partitions), each with its own warp scheduler?',
      o: ['1', '2', '4', '8'],
      a: 2,
      x: ['16', '32', '3'],
      e: 'Since Volta, an SM has 4 sub-partitions, each with a warp scheduler, register file slice, and execution units. Warps are assigned to a sub-partition; this is why 4 warps can issue per cycle on the SM.',
      ref: 'NVIDIA Volta/Ampere/Hopper whitepapers',
      tags: ['sm', 'sub-partition', 'warp-scheduler'],
    },
    {
      d: 4,
      q: 'The register file per SM on recent architectures (e.g. Ampere/Hopper) is about…',
      o: ['8 KB', '64 KB', '256 KB (65,536 32-bit registers)', '4 MB'],
      a: 2,
      x: ['16 KB', '128 KB', '1 MB'],
      e: 'Each SM has a 256 KB register file = 65,536 32-bit registers, shared by all resident threads. With a 255-register-per-thread cap and 2048 resident threads, register demand directly limits occupancy.',
      ref: 'CUDA C++ Programming Guide, Compute Capabilities',
      tags: ['register-file', 'sm', 'occupancy'],
    },
    {
      d: 4,
      q: 'Ampere added "structured sparsity" (2:4). What does it accelerate?',
      o: [
        'Random sparse matrices',
        'Matrices pruned so that 2 of every 4 elements are zero, letting tensor cores skip the zeros for ~2× throughput',
        'Sparse graph traversal',
        'All-reduce',
      ],
      a: 1,
      x: [
        'Block-sparse attention only',
        'Sparse embeddings',
        'Variable-length sequences',
      ],
      e: 'The Sparse Tensor Core exploits a fixed 2:4 pattern (2 nonzeros per group of 4): weights are pruned and compressed with metadata, and the hardware multiplies only nonzeros, roughly doubling math throughput for supported layers.',
      ref: 'NVIDIA Ampere Whitepaper; Sparse Tensor Cores',
      tags: ['sparsity', '2:4', 'ampere', 'tensor-cores'],
    },
    {
      d: 5,
      q: 'Hopper’s DPX instructions accelerate…',
      o: [
        'Double-precision matmul',
        'Dynamic programming algorithms (e.g. Smith-Waterman, Floyd-Warshall) via fused min/max/add operations',
        'Data prefetching',
        'Display output',
      ],
      a: 1,
      x: [
        'FP8 conversions',
        'Atomic reductions',
        'Texture filtering',
      ],
      e: 'DPX provides hardware-accelerated fused operations (e.g. combinations of add/min/max with saturation) that speed up dynamic-programming recurrences used in genomics, routing, and optimization.',
      ref: 'NVIDIA Hopper Whitepaper (DPX)',
      tags: ['dpx', 'hopper', 'dynamic-programming'],
    },
    {
      d: 3,
      q: 'Multi-Instance GPU (MIG) on A100/H100 lets you…',
      o: [
        'Combine multiple GPUs into one',
        'Partition one GPU into several isolated instances, each with dedicated SMs, L2 slices, and memory',
        'Overclock the GPU',
        'Share a GPU across nodes',
      ],
      a: 1,
      x: [
        'Run two kernels in one stream',
        'Double the memory',
        'Disable ECC',
      ],
      e: 'MIG carves a GPU into up to 7 hardware-isolated instances with partitioned compute and memory, giving predictable QoS for multi-tenant inference/serving where a whole GPU per job would waste resources.',
      ref: 'NVIDIA MIG User Guide',
      tags: ['mig', 'partitioning', 'multi-tenant'],
    },
    {
      d: 2,
      q: 'Why do data-center GPUs use HBM while most consumer GPUs use GDDR?',
      o: [
        'HBM is cheaper',
        'HBM offers much higher bandwidth (wide stacked interface) suited to HPC/AI, at higher cost; GDDR is cheaper and adequate for graphics',
        'GDDR is faster',
        'HBM has more capacity always',
      ],
      a: 1,
      x: [
        'HBM is only for graphics',
        'GDDR cannot be used for compute',
        'They are identical',
      ],
      e: 'HBM stacks deliver very high bandwidth (TB/s) for bandwidth-hungry training/HPC but cost more. GDDR (GDDR6/6X) is cheaper with high clocks over a narrower bus, fitting consumer graphics cards.',
      ref: 'NVIDIA GPU datasheets',
      tags: ['hbm', 'gddr', 'bandwidth'],
    },
    {
      d: 3,
      q: 'Unified Virtual Addressing (UVA) provides…',
      o: [
        'Automatic data migration',
        'A single virtual address space so the runtime can infer from a pointer whether it is host or device memory (enabling cudaMemcpyDefault and P2P)',
        'More physical memory',
        'Faster kernels',
      ],
      a: 1,
      x: [
        'ECC protection',
        'Shared memory across blocks',
        'Register spilling control',
      ],
      e: 'UVA (CC 2.0+, 64-bit) puts host and device allocations in one address space. cudaMemcpy can then auto-detect direction (cudaMemcpyDefault), and it underpins peer-to-peer and mapped memory. It is distinct from Unified (managed) Memory.',
      ref: 'CUDA C++ Programming Guide §6.2 (UVA)',
      tags: ['uva', 'address-space', 'p2p'],
    },
    {
      d: 4,
      q: 'NVLink (4th gen, Hopper) provides roughly how much aggregate GPU bandwidth per GPU?',
      o: ['~64 GB/s', '~300 GB/s', '~900 GB/s', '~5 TB/s'],
      a: 2,
      x: ['~100 GB/s', '~2 TB/s', '~16 GB/s'],
      e: 'Hopper NVLink 4 delivers about 900 GB/s aggregate per GPU (18 links), far exceeding PCIe Gen5 (~64 GB/s bidirectional). With NVSwitch, all 8 GPUs in a node get full bandwidth to each other.',
      ref: 'NVIDIA Hopper Whitepaper; NVLink',
      tags: ['nvlink', 'bandwidth', 'hopper'],
    },
    {
      d: 4,
      q: 'GPUDirect RDMA enables…',
      o: [
        'GPUs to render directly to a display',
        'A network adapter (NIC) to DMA directly to/from GPU memory, bypassing a staging copy through host memory',
        'Direct CPU access to registers',
        'Faster PCIe enumeration',
      ],
      a: 1,
      x: [
        'GPU-to-GPU NVLink only',
        'Compression of network packets',
        'Host-to-host copies',
      ],
      e: 'GPUDirect RDMA lets InfiniBand/RoCE NICs read/write GPU memory directly, removing the CPU bounce buffer for inter-node transfers. NCCL uses it for low-latency, high-bandwidth multi-node collectives.',
      ref: 'NVIDIA GPUDirect RDMA documentation',
      tags: ['gpudirect', 'rdma', 'multi-node'],
    },
    {
      d: 3,
      q: 'Turing (sm_75) extended tensor cores with which lower-precision integer types?',
      o: ['FP8 and FP4', 'INT8 and INT4', 'TF32 and BF16', 'FP64'],
      a: 1,
      x: ['INT16 and INT32', 'BF16 only', 'binary only via XNOR... and INT16'],
      e: 'Turing added INT8 and INT4 (and experimental INT1/binary) tensor-core modes for quantized inference. TF32/BF16 came with Ampere; FP8 with Hopper; FP4 with Blackwell.',
      ref: 'NVIDIA Turing Whitepaper',
      tags: ['turing', 'int8', 'int4', 'tensor-cores'],
    },
    {
      d: 5,
      q: 'Blackwell (B200) is notable architecturally for…',
      o: [
        'Being a single small die',
        'Using two reticle-limited dies connected by a high-bandwidth die-to-die link (NV-HBI), presented as one unified GPU',
        'Removing tensor cores',
        'Dropping NVLink',
      ],
      a: 1,
      x: [
        'Using GDDR instead of HBM',
        'A CPU-only design',
        'Quad-die with PCIe links',
      ],
      e: 'Blackwell joins two large dies with a ~10 TB/s NV-HBI interconnect so software sees one coherent GPU, sidestepping the reticle limit on single-die size — alongside 2nd-gen Transformer Engine (FP4/FP6) and HBM3e.',
      ref: 'NVIDIA Blackwell Architecture Whitepaper',
      tags: ['blackwell', 'chiplet', 'nv-hbi'],
    },
    {
      d: 2,
      q: 'PCIe Gen5 x16 offers roughly how much bandwidth per direction?',
      o: ['~8 GB/s', '~16 GB/s', '~32 GB/s', '~128 GB/s'],
      a: 2,
      x: ['~64 GB/s per direction', '~4 GB/s', '~256 GB/s'],
      e: 'PCIe Gen5 x16 is ~64 GB/s bidirectional, i.e. ~32 GB/s each way (double Gen4). This is the typical host↔device path, far below NVLink and HBM bandwidth — hence minimizing PCIe traffic matters.',
      ref: 'PCIe specifications; NVIDIA platform docs',
      tags: ['pcie', 'bandwidth', 'transfers'],
    },
    {
      d: 4,
      q: 'The Grace Hopper superchip connects the Grace CPU and Hopper GPU with…',
      o: [
        'PCIe Gen4',
        'NVLink-C2C, a coherent ~900 GB/s CPU–GPU link giving the GPU fast access to CPU (LPDDR) memory',
        'A standard Ethernet link',
        'Shared HBM',
      ],
      a: 1,
      x: [
        'A USB4 bridge',
        'InfiniBand',
        'A SATA connection',
      ],
      e: 'NVLink-C2C provides cache-coherent, high-bandwidth CPU↔GPU communication (~900 GB/s), letting the GPU access the CPU’s large LPDDR memory at far higher bandwidth than PCIe — useful for huge models/datasets.',
      ref: 'NVIDIA Grace Hopper documentation',
      tags: ['grace-hopper', 'nvlink-c2c', 'coherent'],
    },
    {
      d: 3,
      q: 'Each SM has a small, fast instruction cache. Why does instruction footprint sometimes matter for performance?',
      o: [
        'It never matters',
        'Very large/unrolled kernels can exceed the I-cache, causing instruction fetch stalls',
        'It changes occupancy directly',
        'It controls coalescing',
      ],
      a: 1,
      x: [
        'It limits the number of blocks',
        'It stores the registers',
        'It holds shared memory',
      ],
      e: 'Aggressive unrolling and huge kernels can blow the instruction cache, adding fetch stalls. There is a balance: unrolling helps ILP but excessive code size can hurt via I-cache misses.',
      ref: 'GPU microarchitecture; unrolling trade-offs',
      tags: ['instruction-cache', 'unrolling', 'code-size'],
    },
    {
      d: 4,
      q: 'GPU boost clocks mean a kernel’s runtime can vary because…',
      o: [
        'The code changes',
        'The GPU dynamically scales clocks with thermal/power headroom, so back-to-back runs may clock differently',
        'Occupancy changes each run',
        'Memory size changes',
      ],
      a: 1,
      x: [
        'The driver recompiles the kernel',
        'The grid is resized',
        'ECC toggles',
      ],
      e: 'Boost adjusts core/memory clocks within power and thermal limits, so timing varies between runs and as the chip heats. For repeatable benchmarks, lock clocks (nvidia-smi -lgc/-lmc) and warm up.',
      ref: 'nvidia-smi clock control; benchmarking',
      tags: ['boost-clocks', 'benchmarking', 'variability'],
    },
    {
      d: 2,
      q: 'A "GPC" (GPU Processing Cluster) groups together…',
      o: [
        'Several GPUs',
        'A set of SMs (plus shared front-end/raster resources) within the GPU',
        'The memory controllers only',
        'A single warp',
      ],
      a: 1,
      x: [
        'Two HBM stacks',
        'The PCIe controller',
        'All CUDA cores of one SM',
      ],
      e: 'A GPU contains multiple GPCs, each bundling several SMs. On Hopper, a thread block cluster is co-scheduled within a single GPC, which is what physically enables distributed shared memory and cluster barriers.',
      ref: 'NVIDIA architecture whitepapers',
      tags: ['gpc', 'sm', 'clusters'],
    },
    {
      d: 3,
      q: 'The memory subsystem is partitioned: each memory partition has a slice of L2 and a set of DRAM channels. A consequence is that…',
      o: [
        'All addresses map to one channel',
        'Addresses are interleaved across partitions/channels, so spreading accesses across the address space maximizes achievable bandwidth',
        'L2 is per-SM',
        'DRAM has a single channel',
      ],
      a: 1,
      x: [
        'Bandwidth is independent of access pattern',
        'Only one partition is active at a time',
        'Partitions are software-managed',
      ],
      e: 'Memory is striped across many channels/partitions. Accesses concentrated on few channels (partition camping) underutilize bandwidth; well-distributed access patterns engage all channels for peak throughput.',
      ref: 'GPU memory architecture; partition camping',
      tags: ['memory-partition', 'bandwidth', 'channels'],
    },
    {
      d: 4,
      q: 'FP8 tensor cores on Hopper accumulate in which precision?',
      o: ['FP8', 'FP16', 'FP32', 'INT32'],
      a: 2,
      x: ['BF16', 'FP64', 'TF32'],
      e: 'Like other tensor-core modes, FP8 (E4M3/E5M2) inputs are multiplied and accumulated in FP32 to preserve accuracy across the dot product, then optionally cast back to a lower precision for the next layer.',
      ref: 'NVIDIA Hopper Whitepaper; Transformer Engine',
      tags: ['fp8', 'accumulation', 'tensor-cores', 'hopper'],
    },
    {
      d: 3,
      q: 'Compute preemption (CC 6.0+) allows…',
      o: [
        'Kernels to run faster',
        'A long-running kernel to be preempted at instruction granularity so other contexts (e.g. the desktop/debugger) can use the GPU',
        'Two kernels to share a warp',
        'Blocks to migrate between SMs',
      ],
      a: 1,
      x: [
        'Warps to change size',
        'Memory to be compressed',
        'Faster context creation',
      ],
      e: 'Instruction-level preemption lets the GPU pause a kernel mid-execution and switch contexts, improving interactivity (no display freeze during long compute) and enabling robust debugging — versus older block-boundary preemption.',
      ref: 'NVIDIA Pascal Whitepaper (compute preemption)',
      tags: ['preemption', 'context-switch', 'pascal'],
    },
    {
      d: 5,
      q: 'Why does TMA on Hopper pair naturally with thread block clusters and DSMEM?',
      o: [
        'They are unrelated',
        'TMA can multicast a loaded tile into the shared memory of multiple blocks in a cluster, amortizing a global load across the cluster',
        'TMA requires clusters to compile',
        'Clusters disable TMA',
      ],
      a: 1,
      x: [
        'TMA runs only on one block',
        'DSMEM replaces TMA',
        'Clusters store TMA descriptors',
      ],
      e: 'TMA supports multicast so a single global→shared bulk copy can populate several cluster blocks’ shared memory at once, reducing redundant HBM reads — a key efficiency in cluster-based GEMM where blocks share operands.',
      ref: 'Hopper Whitepaper; CUTLASS cluster GEMM',
      tags: ['tma', 'clusters', 'dsmem', 'multicast'],
    },
    {
      d: 2,
      q: 'Tensor cores deliver their headline FLOP numbers only for workloads shaped like…',
      o: [
        'Elementwise vector ops',
        'Dense matrix multiply / convolution (GEMM-shaped) in supported precisions',
        'Pointer-chasing/graph traversal',
        'Random scatter/gather',
      ],
      a: 1,
      x: [
        'Reductions',
        'Sorting',
        'Branch-heavy code',
      ],
      e: 'Tensor cores accelerate dense MMA. Non-GEMM work (elementwise, reductions, irregular memory) runs on CUDA cores/LSUs and won’t approach the tensor-core peak — which is why peak TFLOP/s headline numbers assume matmul.',
      ref: 'NVIDIA architecture whitepapers; tensor-core applicability',
      tags: ['tensor-cores', 'gemm', 'applicability'],
    },
    {
      d: 4,
      q: 'Why are the per-SM "FP32 cores" sometimes able to also execute INT32, and what is the effect on a mixed loop?',
      o: [
        'They cannot do INT32',
        'Some lanes are shared FP32/INT32 units, so heavy integer index math competes with FP32 throughput on those lanes',
        'INT32 always runs on tensor cores',
        'INT32 is free',
      ],
      a: 1,
      x: [
        'FP32 and INT32 never overlap',
        'INT32 runs on the host',
        'It doubles FP64',
      ],
      e: 'On some architectures a portion of the datapath does FP32 OR INT32. A loop heavy in integer addressing can therefore contend with FP32 math; architectures with fully separate INT32 pipes avoid this. Know your target’s SM layout.',
      ref: 'NVIDIA Ampere/Ada/Hopper whitepapers',
      tags: ['fp32', 'int32', 'datapath', 'contention'],
    },
    {
      d: 3,
      q: 'The constant cache and read-only (texture) cache both serve reads; the constant cache is optimized for…',
      o: [
        'Large streaming data',
        'Uniform access where all threads in a warp read the same address (broadcast), with a small (64 KB) backing store',
        'Random writes',
        'Atomic operations',
      ],
      a: 1,
      x: [
        '2D spatial locality',
        'Per-thread private data',
        'Tensor-core operands',
      ],
      e: 'The constant cache excels when a warp reads one common address (one broadcast). The read-only/texture cache instead targets spatially-local, reused read-only data. Choose based on the access pattern.',
      ref: 'CUDA C++ Programming Guide §5.3.4; caches',
      tags: ['constant-cache', 'read-only-cache', 'broadcast'],
    },
    {
      d: 4,
      q: 'On Hopper, "thread block clusters" map to hardware such that all blocks in a cluster reside…',
      o: [
        'On any SMs across the GPU',
        'On SMs within a single GPC, enabling the SM-to-SM network for DSMEM and cluster barriers',
        'On different GPUs',
        'On the same SM',
      ],
      a: 1,
      x: [
        'In the L2 cache',
        'On the CPU',
        'Across two GPCs',
      ],
      e: 'A cluster’s blocks are co-scheduled on SMs of one GPC, which provides the dedicated SM-to-SM interconnect those blocks use for distributed shared memory access and cluster.sync().',
      ref: 'NVIDIA Hopper Whitepaper; CUDA clusters',
      tags: ['clusters', 'gpc', 'hopper'],
    },
    {
      d: 2,
      q: 'Why is the L2 cache size relevant when choosing a tiling/blocking strategy across thread blocks?',
      o: [
        'It is not relevant',
        'Data reused by many blocks (e.g. a shared operand) ideally fits in L2 so it is served on-chip rather than re-fetched from HBM',
        'L2 holds registers',
        'L2 is per-thread',
      ],
      a: 1,
      x: [
        'L2 stores shared memory',
        'L2 only caches constants',
        'L2 is on the host',
      ],
      e: 'Cross-block reuse is captured by L2. Sizing tiles/working sets to fit the (now tens-of-MB) L2 keeps reused data on-chip, cutting HBM traffic — and the L2 persisting window can pin a hot region.',
      ref: 'CUDA Best Practices Guide; L2 residency',
      tags: ['l2-cache', 'tiling', 'reuse'],
    },
    {
      d: 5,
      q: 'A reason FP64 matters far less for deep learning than for HPC is that…',
      o: [
        'DL never uses floating point',
        'DL tolerates low precision (FP16/BF16/FP8) for speed, while HPC simulations often need FP64 accuracy/stability',
        'FP64 is faster on all GPUs',
        'HPC uses only integers',
      ],
      a: 1,
      x: [
        'FP64 is unsupported',
        'DL requires FP64 master weights',
        'HPC uses tensor cores exclusively',
      ],
      e: 'Neural nets are robust to reduced precision, so DL exploits fast low-precision tensor cores. Many HPC codes (CFD, FEM) need FP64 for numerical stability, making the FP64 throughput ratio a key spec for HPC GPUs.',
      ref: 'NVIDIA HPC vs AI positioning',
      tags: ['fp64', 'hpc', 'deep-learning', 'precision'],
    },
    {
      d: 4,
      q: 'Why can a kernel that fits more shared memory per block on Hopper (up to ~228 KB) than on older GPUs enable better performance?',
      o: [
        'Larger shared memory is always slower',
        'Bigger tiles fit on-chip, raising data reuse (arithmetic intensity) and enabling deeper pipelines for GEMM/attention',
        'It increases the clock',
        'It removes the need for registers',
      ],
      a: 1,
      x: [
        'It reduces occupancy to zero',
        'It disables the L2 cache',
        'It enables FP64',
      ],
      e: 'More shared memory per block lets kernels stage larger tiles and more pipeline stages, increasing reuse and latency hiding. You opt in via cudaFuncSetAttribute for the larger dynamic shared-memory size.',
      ref: 'CUDA C++ Programming Guide, Compute Capabilities; Hopper tuning',
      tags: ['shared-memory', 'hopper', 'tiling', 'pipelining'],
    },
    {
      d: 3,
      q: 'The "LSU" (Load/Store Unit) count per sub-partition limits…',
      o: [
        'FP32 throughput',
        'The rate at which memory (global/shared/local) instructions can be issued, so memory-instruction-heavy kernels can bottleneck on it',
        'Tensor-core throughput',
        'Register allocation',
      ],
      a: 1,
      x: [
        'Branch divergence',
        'The number of blocks',
        'Clock speed',
      ],
      e: 'LSUs process memory instructions. A kernel issuing many loads/stores (including shared) can saturate the LSU pipe (seen as MIO/LG throttle), so vectorizing and reducing memory-instruction count helps.',
      ref: 'NVIDIA SM microarchitecture; Nsight pipe utilization',
      tags: ['lsu', 'memory-instructions', 'bottleneck'],
    },
    {
      d: 5,
      q: 'Blackwell’s 2nd-gen Transformer Engine adds micro-tensor scaling. Compared to Hopper’s per-tensor FP8 scaling, this…',
      o: [
        'Removes scaling entirely',
        'Applies finer-grained (per-block) scale factors, preserving accuracy at FP4/FP6 where a single per-tensor scale would lose too much range',
        'Uses FP32 everywhere',
        'Only works for inference of small models',
      ],
      a: 1,
      x: [
        'Scales per layer only',
        'Disables tensor cores',
        'Requires manual scaling',
      ],
      e: 'Finer (micro/block) scaling lets very low bit-widths (FP4/FP6) keep usable dynamic range per small group of elements, which a coarse per-tensor scale cannot — enabling Blackwell’s higher effective throughput at acceptable accuracy.',
      ref: 'NVIDIA Blackwell Whitepaper; OCP MX formats',
      tags: ['blackwell', 'transformer-engine', 'microscaling', 'fp4'],
    },
  ],
  { idPrefix: 'architecture-b' }
)
