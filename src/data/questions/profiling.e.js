import { defineQuestions } from './_helpers.js'

// Performance & Profiling — batch E. Tensor-core/bank-conflict/divergence metrics,
// occupancy limiter, replay modes, graph/Triton profiling. Distinct from profiling.js/.b/.c/.d.
export default defineQuestions(
  'profiling',
  [
    {
      d: 4,
      q: 'To confirm a kernel is actually USING tensor cores, inspect a metric like…',
      o: [
        'dram__bytes',
        'sm__pipe_tensor_op_hmma_cycles_active (or the Tensor pipe utilization) — nonzero/high means tensor-core (HMMA/IMMA) instructions are executing',
        'l2_hit_rate',
        'achieved_occupancy',
      ],
      a: 1,
      x: [
        'branch_efficiency',
        'register count',
        'block size',
      ],
      e: 'The tensor-pipe activity/utilization metrics show whether HMMA/IMMA (tensor-core) instructions are issuing. Zero tensor activity on a supposed tensor-core GEMM means it fell back to CUDA cores (wrong precision/path/alignment) — a key check.',
      ref: 'Nsight Compute (tensor pipe metrics)',
      tags: ['tensor-core', 'metrics', 'hmma'],
    },
    {
      d: 4,
      q: 'Shared-memory bank conflicts are quantified in Nsight Compute by…',
      o: [
        'DRAM throughput',
        'l1tex bank-conflict / shared-memory wavefronts-per-request being above the ideal (1 per access) — extra wavefronts indicate serialization from conflicts',
        'occupancy',
        'branch efficiency',
      ],
      a: 1,
      x: [
        'register count',
        'L2 hit rate',
        'clock rate',
      ],
      e: 'Conflicts show as more shared-memory transactions/wavefronts per instruction than the conflict-free ideal. The memory tables report bank conflicts; padding/swizzling to spread a warp across banks drives the count back toward one.',
      ref: 'Nsight Compute (shared bank conflicts)',
      tags: ['bank-conflicts', 'shared-memory', 'metrics'],
    },
    {
      d: 4,
      q: 'Branch/thread divergence can be quantified by comparing…',
      o: [
        'Bytes read vs written',
        'thread_inst_executed (counts active lanes) vs inst_executed (counts instructions) — a ratio below 32× the instruction count indicates lanes were predicated/masked off (divergence)',
        'L1 vs L2 hits',
        'Registers vs shared memory',
      ],
      a: 1,
      x: [
        'Occupancy vs grid size',
        'Clock vs power',
        'DRAM vs L2',
      ],
      e: 'thread_inst_executed sums active-lane executions; dividing by inst_executed gives average active lanes per instruction (warp execution efficiency). Below 32 means divergence/predication wasted lanes — pinpointing inefficient control flow.',
      ref: 'Nsight Compute (instruction/thread counts)',
      tags: ['divergence', 'warp-efficiency', 'metrics'],
    },
    {
      d: 4,
      q: 'The Nsight Compute Occupancy section’s "limited by" field tells you…',
      o: [
        'The runtime',
        'Which resource (registers, shared memory, block size, or block-per-SM limit) caps theoretical occupancy — directing exactly what to reduce to raise it',
        'The DRAM size',
        'The clock',
      ],
      a: 1,
      x: [
        'The grid dimensions',
        'The kernel name',
        'The stream',
      ],
      e: 'Rather than guessing, the occupancy analysis names the binding limiter (e.g. "limited by registers"). If occupancy is actually the bottleneck, you then know whether to cut registers, shared memory, or change block size.',
      ref: 'Nsight Compute (Occupancy limiter)',
      tags: ['occupancy', 'limiter', 'nsight-compute'],
    },
    {
      d: 4,
      q: 'Nsight Compute "Range Replay" (vs Kernel Replay) is used when…',
      o: [
        'Profiling one kernel',
        'You need to profile a RANGE spanning multiple kernels/operations as a unit (e.g. metrics across a sequence), replaying that range to gather counters',
        'There are no kernels',
        'For CPU code',
      ],
      a: 1,
      x: [
        'For single instructions',
        'To lock clocks',
        'For host functions',
      ],
      e: 'Range replay collects metrics over a defined range of work (multiple launches), useful when the unit of interest is a sequence rather than one kernel. Kernel replay reruns individual kernels; application replay reruns the whole app per pass.',
      ref: 'Nsight Compute (Range Replay)',
      tags: ['range-replay', 'nsight-compute', 'replay'],
    },
    {
      d: 4,
      q: 'When the workload uses CUDA Graphs, Nsight Systems shows…',
      o: [
        'Each kernel as a separate host launch',
        'The graph launch as a unit (the per-kernel host-launch overhead is gone), with the graph’s kernels executing back-to-back — visually confirming the launch-overhead reduction',
        'Nothing',
        'Only the CPU',
      ],
      a: 1,
      x: [
        'The kernels on the host',
        'Random gaps',
        'Higher overhead',
      ],
      e: 'Graphs collapse many host launches into one graph launch; on the timeline you see the single launch and tightly-packed kernels without the per-kernel CPU gaps — the visible payoff of graphs for launch-bound workloads.',
      ref: 'Nsight Systems; CUDA Graphs on timeline',
      tags: ['cuda-graphs', 'nsight-systems', 'timeline'],
    },
    {
      d: 3,
      q: 'Nsight Compute can profile a Triton-generated kernel because…',
      o: [
        'Triton runs on the CPU',
        'Triton compiles to PTX/SASS like any CUDA kernel, so ncu collects the same hardware metrics (occupancy, memory, stalls) for it — useful to tune Triton kernels',
        'It cannot',
        'Only nsys works',
      ],
      a: 1,
      x: [
        'Triton has its own profiler only',
        'It needs source',
        'It uses the CPU',
      ],
      e: 'Triton kernels are real GPU kernels (PTX/SASS), so Nsight Compute profiles them normally. Combined with Triton’s own tools, ncu metrics help diagnose whether a Triton kernel is memory/compute/occupancy-bound and how to tune it.',
      ref: 'Nsight Compute; Triton kernels',
      tags: ['triton', 'nsight-compute', 'profiling'],
    },
    {
      d: 4,
      q: 'High atomic contention shows up in Nsight Compute as…',
      o: [
        'High occupancy',
        'Elevated L2 atomic traffic / atomic-related stalls and serialization at the targeted addresses — pointing to hot atomic locations to privatize or aggregate',
        'Low DRAM throughput only',
        'Branch divergence',
      ],
      a: 1,
      x: [
        'Register spills',
        'Bank conflicts',
        'A host stall',
      ],
      e: 'Atomics resolve at L2; heavy contention appears as high atomic transaction counts and associated stalls/serialization. The fix is reducing conflicting atomics (privatization, warp aggregation, hierarchical reduction), which the metrics motivate.',
      ref: 'Nsight Compute; atomic metrics',
      tags: ['atomics', 'contention', 'metrics'],
    },
    {
      d: 4,
      q: 'The Nsight Compute Memory Workload Analysis "chart" (L1 ↔ L2 ↔ DRAM flows) helps you…',
      o: [
        'Edit the kernel',
        'See WHERE traffic goes and hit rates at each level, so you can tell if data is served from L1/L2 (good locality) or hammering DRAM (poor reuse)',
        'Lock clocks',
        'Count FLOPs',
      ],
      a: 1,
      x: [
        'Profile the CPU',
        'Allocate memory',
        'Pick a stream',
      ],
      e: 'The memory chart visualizes bytes flowing between L1, L2, and DRAM with hit rates, making it obvious whether reuse is captured on-chip or the kernel is DRAM-bound — guiding tiling/caching optimizations.',
      ref: 'Nsight Compute (Memory Workload Analysis)',
      tags: ['memory-chart', 'hit-rate', 'locality'],
    },
    {
      d: 5,
      q: 'You compare a kernel against the ACHIEVABLE (not just theoretical) roofline because…',
      o: [
        'They are the same',
        'Achievable peaks account for realistic limits (e.g. ~80–90% of bandwidth, ECC, clock state), so judging a kernel against the achievable ceiling gives a fairer sense of remaining headroom than the theoretical maximum',
        'Theoretical is lower',
        'It ignores intensity',
      ],
      a: 1,
      x: [
        'Achievable is unbounded',
        'It uses the CPU',
        'It is random',
      ],
      e: 'No kernel hits the datasheet peak (overheads exist). Measuring achievable bandwidth/FLOPs (e.g. via microbenchmarks) and comparing the kernel to that ceiling shows realistic headroom — a kernel at 90% of achievable is near-optimal even if below theoretical.',
      ref: 'Roofline; achievable vs theoretical',
      tags: ['roofline', 'achievable', 'methodology'],
    },
    {
      d: 4,
      q: 'A kernel’s profile shows FP64 pipe activity unexpectedly. A likely cause is…',
      o: [
        'Nothing',
        'Accidental double-precision literals/math (e.g. 1.0 instead of 1.0f, or double-precision intrinsics) promoting computation to FP64 — costly on consumer GPUs',
        'Tensor-core usage',
        'High occupancy',
      ],
      a: 1,
      x: [
        'Bank conflicts',
        'A host stall',
        'Register spills',
      ],
      e: 'Unintended FP64 often comes from untyped literals (1.0, 2.0) or double-returning math promoting the expression to double. On GPUs with low FP64 throughput this is slow; use f-suffixed literals/float intrinsics. The FP64 pipe metric reveals it.',
      ref: 'Nsight Compute; FP64 pipe; literal types',
      tags: ['fp64', 'literals', 'metrics'],
    },
    {
      d: 4,
      q: 'Nsight Compute rules flag "Uncoalesced Global Accesses" by analyzing…',
      o: [
        'The kernel name',
        'The sectors-requested vs sectors-ideal (load/store efficiency) — when far above ideal, it reports the access as uncoalesced with the offending source lines',
        'The clock',
        'The grid size',
      ],
      a: 1,
      x: [
        'Occupancy only',
        'Register count',
        'The stream',
      ],
      e: 'The rule interprets the sector/efficiency metrics: many extra sectors per request → uncoalesced access. With -lineinfo it maps the issue to source lines, turning raw counters into a specific, actionable finding.',
      ref: 'Nsight Compute (rules; uncoalesced access)',
      tags: ['rules', 'coalescing', 'source'],
    },
    {
      d: 5,
      q: 'For a stateful kernel whose correctness depends on prior launches, you must use ___ replay in Nsight Compute.',
      o: [
        'Kernel replay',
        'Application replay — it re-runs the whole application per pass, preserving cross-launch state that kernel replay (which only restores the kernel’s captured memory) would not reproduce',
        'Range replay',
        'No replay',
      ],
      a: 1,
      x: [
        'CPU replay',
        'Graph replay',
        'Host replay',
      ],
      e: 'Kernel replay restores only the profiled kernel’s memory between passes; if the kernel depends on state set up by earlier launches or external side effects, application replay (whole-app re-run per pass) is needed for faithful metrics.',
      ref: 'Nsight Compute (application replay)',
      tags: ['application-replay', 'stateful', 'nsight-compute'],
    },
    {
      d: 3,
      q: 'To attribute time across a multi-rank training step, correlating NVTX ranges with CUDA and NCCL activity in Nsight Systems lets you…',
      o: [
        'Edit the model',
        'See, per phase (forward/backward/optimizer/comm), how long the GPU spent computing vs communicating vs idle — revealing the dominant cost and overlap quality',
        'Lock clocks',
        'Count parameters',
      ],
      a: 1,
      x: [
        'Profile one kernel',
        'Allocate memory',
        'Disassemble SASS',
      ],
      e: 'NVTX labels phases; correlating them with kernel and NCCL timelines shows the compute/comm/idle breakdown per phase. This identifies whether the bottleneck is compute, exposed communication, or input/host stalls.',
      ref: 'Nsight Systems; NVTX + NCCL correlation',
      tags: ['nvtx', 'nccl', 'phase-analysis'],
    },
    {
      d: 4,
      q: 'A tiny kernel that runs in ~3 µs but is launched millions of times is best characterized as…',
      o: [
        'Compute-bound',
        'Launch/overhead-bound — per-launch CPU overhead and gaps dominate; fix with CUDA Graphs or kernel fusion, not kernel micro-optimization',
        'Memory-bound',
        'Tensor-bound',
      ],
      a: 1,
      x: [
        'Latency-bound on memory',
        'Register-spill bound',
        'Bank-conflict bound',
      ],
      e: 'When the kernel is shorter than or comparable to launch overhead and is launched constantly, the bottleneck is launch/scheduling, not the kernel’s internals. Graphs (capture+replay) or fusing kernels removes that overhead.',
      ref: 'Launch-bound; CUDA Graphs',
      tags: ['launch-bound', 'graphs', 'fusion'],
    },
    {
      d: 4,
      q: 'Power/energy profiling (e.g. via DCGM or nvidia-smi sampling) is useful to…',
      o: [
        'Find bugs',
        'Compare energy efficiency (e.g. tokens/Joule) of implementations and detect power/thermal throttling that caps sustained performance',
        'Count registers',
        'Lock clocks',
      ],
      a: 1,
      x: [
        'Disassemble SASS',
        'Allocate memory',
        'Profile one instruction',
      ],
      e: 'Energy metrics matter for efficiency (perf/Watt, tokens/Joule) and reveal throttling: if power hits the cap, clocks drop and sustained throughput falls. DCGM/nvidia-smi sample power, temperature, and throttle reasons over time.',
      ref: 'DCGM/nvidia-smi power profiling',
      tags: ['power', 'energy', 'throttling'],
    },
    {
      d: 4,
      q: 'Predicated-off instructions (executed but with results discarded due to predication) show up as…',
      o: [
        'Higher occupancy',
        'A gap between instructions executed and useful work — many predicated-off instructions indicate divergence handled by predication, wasting issue slots',
        'DRAM traffic',
        'Bank conflicts',
      ],
      a: 1,
      x: [
        'Register spills',
        'L2 misses',
        'A host stall',
      ],
      e: 'The compiler may predicate short branches; predicated-off lanes still issue but write nothing. A high predicated-off fraction signals divergence (work done but discarded), which the warp-efficiency/predication metrics expose.',
      ref: 'Nsight Compute; predication metrics',
      tags: ['predication', 'divergence', 'metrics'],
    },
    {
      d: 5,
      q: 'A GEMM shows high tensor-pipe utilization but achieved FLOP/s ~60% of peak. Inspecting the warp-state, you find high "Short Scoreboard" stalls. The interpretation is…',
      o: [
        'It is memory(DRAM)-bound',
        'The tensor cores stall waiting on shared-memory operand loads (short-scoreboard) between MMAs — improve the SMEM feed (vectorize ldmatrix, fix bank conflicts, deepen pipeline) to raise effective throughput',
        'It is latency-bound on DRAM',
        'Occupancy is too low',
      ],
      a: 1,
      x: [
        'It is host-bound',
        'Register spills',
        'Branch divergence',
      ],
      e: 'Short-scoreboard stalls point to waiting on shared-memory (operand) loads. The tensor cores issue but bubble between MMAs because operands aren’t ready — better SMEM feeding (conflict-free ldmatrix, deeper buffering) closes the gap to peak.',
      ref: 'Nsight Compute; tensor-core feeding analysis',
      tags: ['tensor-core', 'short-scoreboard', 'gemm'],
    },
    {
      d: 3,
      q: 'Why run a quick Nsight Systems pass BEFORE diving into Nsight Compute on a kernel?',
      o: [
        'To compile faster',
        'To verify that kernel is actually a significant fraction of runtime — optimizing a kernel that’s 2% of the timeline is wasted effort; nsys finds what dominates',
        'To lock clocks',
        'To count FLOPs',
      ],
      a: 1,
      x: [
        'To disassemble SASS',
        'To allocate memory',
        'To pick a stream',
      ],
      e: 'Amdahl’s law: optimize what dominates. Nsight Systems shows each kernel’s share of runtime (and whether the GPU is even the bottleneck), so you spend ncu effort on the kernels that actually matter.',
      ref: 'Profiling methodology; Amdahl',
      tags: ['methodology', 'nsight-systems', 'amdahl'],
    },
    {
      d: 4,
      q: 'cudaProfilerStart/Stop combined with nsys --capture-range=cudaProfilerApi lets you…',
      o: [
        'Speed up the app',
        'Restrict the trace to exactly the code between Start/Stop (e.g. steady-state steps), avoiding huge traces and warm-up noise',
        'Lock clocks',
        'Count registers',
      ],
      a: 1,
      x: [
        'Disassemble SASS',
        'Profile the CPU only',
        'Allocate memory',
      ],
      e: 'Bounding capture to the cudaProfiler range focuses the trace on representative iterations, keeping reports small and free of initialization noise — combine with NVTX to label what’s inside the captured window.',
      ref: 'Nsight Systems (capture range = cudaProfilerApi)',
      tags: ['capture-range', 'profiler-api', 'nsys'],
    },
    {
      d: 4,
      q: 'A kernel reports high "Stall Drain" at the end. This usually means…',
      o: [
        'Memory latency throughout',
        'Warps finishing and draining the pipeline near kernel end (tail) — significant drain stalls relate to the tail effect / imbalance at completion, not steady-state behavior',
        'A barrier deadlock',
        'Register spills',
      ],
      a: 1,
      x: [
        'Tensor-core saturation',
        'A host bottleneck',
        'Bank conflicts',
      ],
      e: 'Drain stalls occur as warps complete and the pipeline empties (kernel tail). Large drain contributions point to imbalance/tail effects at the end; more even work distribution or more blocks per wave reduces it.',
      ref: 'Nsight Compute (Stall Drain)',
      tags: ['stall-drain', 'tail-effect', 'metrics'],
    },
    {
      d: 5,
      q: 'You profile two attention kernels; both are tensor-bound but one has 2× the achieved FLOP/s. The most informative single comparison is…',
      o: [
        'Lines of code',
        'Tensor-pipe utilization AND the stall breakdown — the faster one likely keeps the tensor pipe fed (fewer operand/barrier stalls) via better tiling/pipelining',
        'Register count alone',
        'The grid size',
      ],
      a: 1,
      x: [
        'The block size only',
        'The file size',
        'Occupancy alone',
      ],
      e: 'Both being tensor-bound, the difference is how continuously the tensor cores are fed. Comparing tensor-pipe utilization and stall reasons (operand waits, barriers) reveals which kernel’s pipelining/feeding is better — the actionable distinction.',
      ref: 'Nsight Compute; comparative tensor analysis',
      tags: ['tensor-core', 'comparison', 'stalls'],
    },
    {
      d: 3,
      q: 'Nsight Systems "GPU starvation" (idle) periods during training most often point to…',
      o: [
        'A great pipeline',
        'A host-side bottleneck: data loading, Python overhead, or too many small synchronous launches — the GPU waits for the CPU to feed it work',
        'Memory bandwidth saturation',
        'Tensor-core overuse',
      ],
      a: 1,
      x: [
        'High occupancy',
        'ECC errors',
        'Register spills',
      ],
      e: 'GPU idle gaps with a busy CPU indicate the host can’t feed the GPU fast enough (input pipeline, Python/launch overhead, syncs). Remedies: faster/prefetched data loading, fewer/larger launches (graphs/fusion), and overlap — not kernel tuning.',
      ref: 'Nsight Systems; GPU starvation',
      tags: ['gpu-idle', 'host-bound', 'training'],
    },
    {
      d: 4,
      q: 'Profiling reveals a kernel spends most time in local memory load/store traffic. The root cause and fix are…',
      o: [
        'Bank conflicts; pad shared memory',
        'Register spilling; reduce live state (smaller tiles, recompute, fewer simultaneous variables) or adjust -maxrregcount to eliminate the spills',
        'Uncoalesced global access; transpose',
        'Atomic contention; privatize',
      ],
      a: 1,
      x: [
        'Divergence; use predication',
        'Low occupancy; add blocks',
        'Tensor starvation; deepen pipeline',
      ],
      e: 'Local memory traffic = register spills. The fix is reducing register pressure (smaller working set, recompute, restructure) so values stay in registers. Capping registers can help or hurt (more spills/lower occupancy) — measure.',
      ref: 'Nsight Compute; register spilling',
      tags: ['local-memory', 'spilling', 'registers'],
    },
    {
      d: 4,
      q: 'For a fair A/B of two kernels, locking clocks AND fixing the input is needed because…',
      o: [
        'It is optional',
        'Boost clocks vary with power/thermal state and different inputs change work; controlling both isolates the code change as the cause of any timing difference',
        'Clocks are constant',
        'Inputs do not matter',
      ],
      a: 1,
      x: [
        'It speeds up the kernel',
        'It reduces memory',
        'It increases occupancy',
      ],
      e: 'Without locked clocks and identical inputs, timing differences could come from clock variation or differing work rather than the code. Fixing both makes the comparison attributable to the implementation, supported by clock-independent counters.',
      ref: 'Benchmarking methodology; controlled A/B',
      tags: ['benchmarking', 'clocks', 'methodology'],
    },
    {
      d: 5,
      q: 'A multi-GPU training run shows good per-GPU kernel efficiency but poor overall scaling. The profiling focus should shift to…',
      o: [
        'Per-kernel ncu analysis',
        'The Nsight Systems multi-rank timeline: exposed NCCL communication, load imbalance/stragglers, and overlap quality — scaling losses live in communication/synchronization, not the kernels',
        'Register usage',
        'Bank conflicts',
      ],
      a: 1,
      x: [
        'Shared memory',
        'The block size',
        'FP64 usage',
      ],
      e: 'If individual kernels are efficient but scaling is poor, the problem is at the job level: exposed collectives, imbalance, or poor overlap. Multi-rank timelines (with NCCL traces) reveal it; deeper kernel profiling won’t.',
      ref: 'Scaling analysis; multi-GPU profiling',
      tags: ['scaling', 'nccl', 'multi-gpu', 'methodology'],
    },
    {
      d: 4,
      q: 'The metric "smsp__inst_executed.avg.per_cycle_active" (IPC per scheduler) being low while occupancy is high tells you…',
      o: [
        'The kernel is optimal',
        'Warps are resident but frequently STALLED (few issue per cycle) — the limiter is stalls (memory/barrier/dependency), not occupancy; consult stall reasons',
        'Too few warps',
        'Register spilling only',
      ],
      a: 1,
      x: [
        'Tensor-core saturation',
        'A host bottleneck',
        'Bank conflicts only',
      ],
      e: 'High occupancy + low IPC = warps present but not issuing (stalled). The warp-state/stall section then tells you what they wait on (long scoreboard, barrier, math throttle), directing the real fix.',
      ref: 'Nsight Compute (IPC; scheduler stats)',
      tags: ['ipc', 'stalls', 'occupancy'],
    },
    {
      d: 3,
      q: 'Why is achieved DRAM bandwidth a better optimization target than "GPU utilization" for a memory-bound kernel?',
      o: [
        'They are the same',
        'GPU-Util is a coarse duty cycle (was a kernel running); achieved DRAM bandwidth vs peak directly measures how efficiently the memory-bound kernel uses the resource that limits it',
        'Bandwidth is irrelevant',
        'Utilization is exact',
      ],
      a: 1,
      x: [
        'Utilization measures SMs',
        'Bandwidth ignores memory',
        'They both measure FLOPs',
      ],
      e: 'For a bandwidth-bound kernel, the meaningful number is achieved vs peak DRAM bandwidth (how close to the roof). GPU-Util only says a kernel was active, not how efficiently it used memory — so it can mislead.',
      ref: 'Nsight; bandwidth vs GPU-Util',
      tags: ['bandwidth', 'gpu-util', 'memory-bound'],
    },
    {
      d: 4,
      q: 'After fixing uncoalesced access, you re-profile and global load efficiency jumps to ~100% but runtime barely changes. The likely reason is…',
      o: [
        'The fix failed',
        'The kernel wasn’t bandwidth-bound — coalescing improved efficiency but another limiter (compute, latency, or another pipe) dominates; re-check the Speed-of-Light section',
        'Occupancy dropped',
        'ECC turned on',
      ],
      a: 1,
      x: [
        'Registers spilled',
        'The clock changed',
        'The grid shrank',
      ],
      e: 'Improving a metric that isn’t the bottleneck yields little speedup. If coalescing is now ideal but runtime is flat, the kernel was limited elsewhere (compute/latency). Always confirm the actual limiter (SoL) before and after a change.',
      ref: 'Profiling methodology; identify the true limiter',
      tags: ['methodology', 'coalescing', 'limiter'],
    },
  ],
  { idPrefix: 'profiling-e' }
)
