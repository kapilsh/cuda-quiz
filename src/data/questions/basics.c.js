import { defineQuestions } from './_helpers.js'

// CUDA Basics — batch C. Compilation/linking, function attributes, device
// globals, heap, attributes/limits. Distinct from basics.js / basics.b.js.
export default defineQuestions(
  'basics',
  [
    {
      d: 4,
      q: 'Separate compilation with relocatable device code requires which nvcc flag, and why?',
      o: [
        '-O3, for optimization',
        '-rdc=true, to link device symbols across files',
        '--use_fast_math',
        '-arch=native',
      ],
      a: 1,
      x: [
        '-G, for debugging',
        '-shared, for libraries',
        '-lineinfo',
      ],
      e: 'By default device code is compiled per–translation-unit (whole-program). -rdc=true enables relocatable device code and a device link stage (nvlink) so __device__ functions/variables can be referenced across .cu files — needed for dynamic parallelism and device-side libraries.',
      ref: 'CUDA Compiler Driver NVCC (separate compilation)',
      tags: ['rdc', 'separate-compilation', 'linking'],
    },
    {
      d: 3,
      q: 'A __device__ global variable (file-scope, e.g. __device__ int counter;) is…',
      o: [
        'Per-thread storage',
        'One shared global-memory instance',
        'Stored in shared memory',
        'Only visible to one block',
      ],
      a: 1,
      x: [
        'A host variable',
        'Allocated per block',
        'A constant',
      ],
      e: 'A __device__ global lives in device memory for the module’s lifetime and is shared across kernels/threads. The host accesses it via cudaMemcpyToSymbol/FromSymbol (you can’t take its address directly in host code).',
      ref: 'CUDA C++ Programming Guide §7.2 (__device__ variables)',
      tags: ['device-global', 'symbol', 'memory'],
    },
    {
      d: 3,
      q: 'Calling malloc()/free() (or new/delete) INSIDE a kernel allocates from…',
      o: [
        'Shared memory',
        'A device heap in global memory',
        'Host memory',
        'Registers',
      ],
      a: 1,
      x: [
        'Constant memory',
        'The CPU stack',
        'Unified memory automatically',
      ],
      e: 'Device-side malloc draws from a preallocated device heap (default a few MB; resize with cudaLimitMallocHeapSize before launch). It is convenient but relatively slow and can fragment — prefer pre-allocated buffers in hot code.',
      ref: 'CUDA C++ Programming Guide §B.20 (Dynamic Global Memory Allocation)',
      tags: ['device-malloc', 'heap', 'limits'],
    },
    {
      d: 2,
      q: 'For a row-major matrix C stored as a 1D array, the element C[row][col] of an N-column matrix is at linear index…',
      o: ['col*N + row', 'row*N + col', 'row + col', 'row*col'],
      a: 1,
      x: ['row*M + col (M rows)', 'col + row*row', '(row+col)*N'],
      e: 'Row-major layout places each row contiguously, so C[row][col] = C[row*N + col] for N columns. Getting this right (and matching cuBLAS’s column-major convention) avoids transpose bugs.',
      ref: 'PMPP Ch. 3; row-major layout',
      tags: ['indexing', 'row-major', 'matrix'],
    },
    {
      d: 3,
      q: 'A launch fails with "too many resources requested for launch." The most likely cause is…',
      o: [
        'The grid is too small',
        'Block needs too many registers/shared mem',
        'The kernel name is wrong',
        'The stream is invalid',
      ],
      a: 1,
      x: [
        'Global memory is full',
        'The GPU is busy',
        'The data is misaligned',
      ],
      e: 'This error means the requested block can’t fit: registers/thread × block size (or shared memory/block) exceeds the SM limit. Reduce block size, cap registers (-maxrregcount/__launch_bounds__), or shrink shared-memory usage.',
      ref: 'CUDA Runtime API; launch resource errors',
      tags: ['launch-error', 'resources', 'registers'],
    },
    {
      d: 3,
      q: 'cudaFuncSetCacheConfig(kernel, cudaFuncCachePreferShared) requests that the SM…',
      o: [
        'Disable caching',
        'Favor shared memory over L1 for that kernel',
        'Use constant memory',
        'Increase registers',
      ],
      a: 1,
      x: [
        'Prefer texture cache',
        'Pin the kernel to one SM',
        'Double the occupancy',
      ],
      e: 'On GPUs where L1 and shared memory share on-chip SRAM, this hint biases the carveout toward shared memory (or L1 with PreferL1). It is advisory; newer APIs use cudaFuncAttributePreferredSharedMemoryCarveout.',
      ref: 'CUDA Runtime API (cudaFuncSetCacheConfig)',
      tags: ['cache-config', 'l1-shared', 'carveout'],
    },
    {
      d: 2,
      q: 'cudaMemGetInfo(&free, &total) reports…',
      o: [
        'Host RAM usage',
        'Free and total device memory',
        'The L2 cache size',
        'Shared memory per block',
      ],
      a: 1,
      x: [
        'Registers per SM',
        'Pinned memory available',
        'The number of SMs',
      ],
      e: 'It returns currently-free and total device memory, useful to size allocations or detect fragmentation/leaks. Note other processes and the context itself consume some of "total."',
      ref: 'CUDA Runtime API (cudaMemGetInfo)',
      tags: ['memory', 'cudaMemGetInfo', 'capacity'],
    },
    {
      d: 4,
      q: 'Why are CUDA kernels typically written as C++ templates in high-performance libraries?',
      o: [
        'Templates run faster at runtime by themselves',
        'Compile-time params let the compiler specialize',
        'Templates reduce binary size',
        'They are required for __global__',
      ],
      a: 1,
      x: [
        'They enable runtime polymorphism on the GPU',
        'They avoid recompilation',
        'They disable inlining',
      ],
      e: 'Templating kernels on sizes/types gives the compiler compile-time constants to unroll loops, allocate exact shared memory, and pick instructions — the basis of CUTLASS/CUB performance. The cost is more compile time and code per instantiation.',
      ref: 'CUTLASS/CUB design; CUDA C++ templates',
      tags: ['templates', 'specialization', 'metaprogramming'],
    },
    {
      d: 3,
      q: 'assert() inside device code, when it fails, will…',
      o: [
        'Be silently ignored',
        'Halt the kernel and report it on next sync',
        'Crash the host immediately with no message',
        'Retry the kernel',
      ],
      a: 1,
      x: [
        'Pause and enter a debugger automatically in release',
        'Only print a warning and continue',
        'Reset the GPU',
      ],
      e: 'Device assert() (enabled in debug builds; NDEBUG disables it) prints the failed expression and halts the kernel; the error appears at the next sync. It’s useful for catching invariant violations during development.',
      ref: 'CUDA C++ Programming Guide §B.18 (assert)',
      tags: ['assert', 'debugging', 'device'],
    },
    {
      d: 4,
      q: 'cudaDeviceGetAttribute lets you query a single capability (e.g. cudaDevAttrMaxSharedMemoryPerBlockOptin). Why prefer it over reading cudaDeviceProp?',
      o: [
        'It is the only way to get attributes',
        'Lighter, and exposes attributes not in the struct',
        'cudaDeviceProp is deprecated',
        'It runs on the device',
      ],
      a: 1,
      x: [
        'It is always faster than launching a kernel',
        'It changes the device',
        'It allocates memory',
      ],
      e: 'cudaDeviceGetAttribute fetches a specific attribute cheaply and covers attributes (like opt-in max shared memory) not always present in cudaDeviceProp, making runtime feature/limit checks portable across architectures.',
      ref: 'CUDA Runtime API (cudaDeviceGetAttribute)',
      tags: ['device-attributes', 'portability', 'limits'],
    },
    {
      d: 2,
      q: 'A reasonable default block size for a simple elementwise kernel is often…',
      o: [
        '1 thread',
        '128 or 256 threads',
        '1024 always',
        '32 only',
      ],
      a: 1,
      x: [
        '100 threads',
        '7 threads',
        '2048 threads',
      ],
      e: 'Multiples of 32 (e.g. 128/256) are good defaults: full warps, decent occupancy, and room for several blocks per SM. The optimal value depends on registers/shared memory; cudaOccupancyMaxPotentialBlockSize can suggest one.',
      ref: 'CUDA Best Practices Guide; block-size selection',
      tags: ['block-size', 'occupancy', 'defaults'],
    },
    {
      d: 4,
      q: 'Marking a __device__ helper __forceinline__ in a hot kernel can help because…',
      o: [
        'It moves it to the host',
        'Avoids call overhead, optimizes across the boundary',
        'It reduces register usage always',
        'It enables tensor cores',
      ],
      a: 1,
      x: [
        'It makes the function recursive',
        'It disables optimization',
        'It forces shared memory use',
      ],
      e: '__forceinline__ inlines the callee, removing call/return overhead and enabling cross-call optimization. The compiler already inlines aggressively on the GPU; forcing it helps in hot paths but can bloat code (I-cache pressure).',
      ref: 'CUDA C++ Programming Guide; __forceinline__',
      tags: ['forceinline', 'inlining', 'optimization'],
    },
    {
      d: 3,
      q: 'Why might extern "C" be used on a __global__ kernel?',
      o: [
        'To run it on the CPU',
        'To prevent name mangling for a stable symbol',
        'To make it faster',
        'To enable templates',
      ],
      a: 1,
      x: [
        'To allocate constant memory',
        'To force inlining',
        'To disable error checking',
      ],
      e: 'extern "C" gives the kernel an unmangled symbol name, which matters when loading it by name via the driver API (cuModuleGetFunction), from Python/ctypes, or runtime compilation, where the mangled C++ name would be awkward.',
      ref: 'CUDA driver API; name mangling',
      tags: ['extern-c', 'symbols', 'driver-api'],
    },
    {
      d: 5,
      q: 'The difference between the CUDA Runtime API (cudart) and the Driver API (cuda) is that the Driver API…',
      o: [
        'Runs on the device',
        'Lower-level: explicit contexts and modules',
        'Is faster for all kernels',
        'Cannot launch kernels',
      ],
      a: 1,
      x: [
        'Only works on Windows',
        'Is required for every program',
        'Does not support streams',
      ],
      e: 'The Runtime API (<<<>>>, cudaMalloc) is the convenient high-level layer; the Driver API exposes explicit contexts, modules, and manual kernel loading/launch — the substrate for frameworks, JIT (NVRTC), and multi-language integration.',
      ref: 'CUDA Driver API documentation',
      tags: ['driver-api', 'runtime-api', 'contexts'],
    },
    {
      d: 4,
      q: 'NVRTC is used to…',
      o: [
        'Profile kernels',
        'Compile CUDA C++ to PTX at runtime (JIT)',
        'Link static libraries',
        'Manage streams',
      ],
      a: 1,
      x: [
        'Reduce register usage',
        'Run kernels on the CPU',
        'Allocate memory',
      ],
      e: 'NVRTC compiles kernel source strings to PTX in-process, so applications (and frameworks like PyTorch/Triton-adjacent tools) can JIT-generate specialized kernels (fused shapes, constants) and load them via the driver API.',
      ref: 'NVRTC documentation',
      tags: ['nvrtc', 'jit', 'runtime-compilation'],
    },
    {
      d: 3,
      q: 'PTX is best described as…',
      o: [
        'Final machine code for a specific GPU',
        'A virtual ISA JIT-compiled by the driver to SASS',
        'A profiling format',
        'A host object file',
      ],
      a: 1,
      x: [
        'A Python bytecode',
        'A memory layout',
        'A linker script',
      ],
      e: 'PTX is a stable virtual ISA. Embedding PTX lets a binary run on future GPUs: the driver JIT-compiles PTX to that GPU’s SASS. SASS is the actual hardware assembly; you can inspect both with cuobjdump/nvdisasm.',
      ref: 'PTX ISA documentation; NVCC',
      tags: ['ptx', 'sass', 'jit'],
    },
    {
      d: 2,
      q: 'cudaMemcpyAsync(dst, src, n, kind, stream) issued on a non-default stream returns to the host…',
      o: [
        'After the copy completes',
        'Immediately; the copy is ordered in the stream',
        'Only after a kernel finishes',
        'After cudaDeviceReset',
      ],
      a: 1,
      x: [
        'After the next launch',
        'Never; it blocks',
        'After cudaFree',
      ],
      e: 'On a non-default stream with pinned memory, cudaMemcpyAsync returns immediately and the copy executes asynchronously, enabling overlap. (With pageable memory it may behave synchronously.)',
      ref: 'CUDA Runtime API (cudaMemcpyAsync)',
      tags: ['memcpy-async', 'streams', 'async'],
    },
    {
      d: 4,
      q: 'cuobjdump / nvdisasm are used to…',
      o: [
        'Allocate device memory',
        'Disassemble PTX/SASS from compiled binaries',
        'Profile memory bandwidth',
        'Launch kernels',
      ],
      a: 1,
      x: [
        'Compile source code',
        'Manage CUDA contexts',
        'Generate random numbers',
      ],
      e: 'cuobjdump dumps sections (PTX/SASS, symbols) of a cubin/fatbin; nvdisasm disassembles SASS with control-flow info. Reading SASS reveals register usage, instruction selection, and whether optimizations (FMA, predication) happened.',
      ref: 'cuobjdump / nvdisasm documentation',
      tags: ['cuobjdump', 'sass', 'disassembly'],
    },
    {
      d: 3,
      q: 'A "fatbin" (fat binary) embedded by nvcc contains…',
      o: [
        'Only host code',
        'Device code for multiple architectures',
        'Profiling data',
        'The source code',
      ],
      a: 1,
      x: [
        'Only one SASS target',
        'Shared library symbols',
        'The CUDA driver',
      ],
      e: 'With -gencode you embed SASS for several compute capabilities plus PTX for forward compatibility into a fatbin. At load time the driver picks the best matching SASS, or JIT-compiles the PTX for newer GPUs.',
      ref: 'CUDA Compiler Driver NVCC (-gencode, fatbin)',
      tags: ['fatbin', 'gencode', 'compatibility'],
    },
    {
      d: 5,
      q: 'A kernel compiled only with PTX (no SASS) for an older arch will, on a newer GPU…',
      o: [
        'Fail to run',
        'Be JIT-compiled from PTX to the new GPU’s SASS',
        'Run on the CPU',
        'Require recompilation of the host code',
      ],
      a: 1,
      x: [
        'Run slower forever',
        'Use emulation',
        'Ignore the new features',
      ],
      e: 'Embedding PTX provides forward compatibility: the driver JIT-compiles it to the target SASS the first time (cached afterward). This is why shipping PTX (plus SASS for known targets) lets binaries run on GPUs released after build time.',
      ref: 'CUDA C++ Programming Guide (Compatibility); JIT',
      tags: ['ptx', 'jit', 'forward-compatibility'],
    },
    {
      d: 2,
      q: 'Which is true about launching a kernel with a block size of 0 threads?',
      o: [
        'It runs an empty kernel efficiently',
        'Invalid configuration; the launch fails',
        'It defaults to 32 threads',
        'It runs one thread',
      ],
      a: 1,
      x: [
        'It uses the warp size',
        'It allocates no resources but succeeds',
        'It is the same as <<<1,1>>>',
      ],
      e: 'Block and grid dimensions must be at least 1. A zero dimension is an invalid launch configuration (cudaErrorInvalidConfiguration). Always compute grid/block sizes to be ≥ 1.',
      ref: 'CUDA Runtime API; launch configuration',
      tags: ['launch-config', 'validation', 'error'],
    },
    {
      d: 3,
      q: 'CUDA_LAUNCH_BLOCKING=1 in the environment makes kernel launches…',
      o: [
        'Faster',
        'Synchronous; pinpoints a failing launch',
        'Run on the CPU',
        'Use more streams',
      ],
      a: 1,
      x: [
        'Skip error checking',
        'Run with more occupancy',
        'Disable the default stream',
      ],
      e: 'Setting CUDA_LAUNCH_BLOCKING=1 serializes launches so asynchronous errors surface at the offending launch (not later), greatly easing debugging — at a big performance cost, so it is a debug-only setting.',
      ref: 'CUDA environment variables; debugging',
      tags: ['launch-blocking', 'debugging', 'environment'],
    },
    {
      d: 4,
      q: 'Recursion in a __device__ function is…',
      o: [
        'Never allowed',
        'Allowed (CC 2.0+), but uses the per-thread stack',
        'Always free',
        'Only allowed in __global__ functions',
      ],
      a: 1,
      x: [
        'Converted to loops automatically',
        'Run on the host',
        'Limited to 2 levels',
      ],
      e: 'Device recursion works but each thread has a limited stack (set via cudaLimitStackSize). Deep or wide recursion across thousands of threads consumes lots of local memory and can overflow; iterative reformulations are usually preferred.',
      ref: 'CUDA C++ Programming Guide; device recursion, stack limit',
      tags: ['recursion', 'stack', 'limits'],
    },
    {
      d: 3,
      q: 'cudaDeviceSetLimit(cudaLimitStackSize, bytes) is sometimes needed when a kernel…',
      o: [
        'Uses too much shared memory',
        'Has deep recursion or large local arrays',
        'Launches too many blocks',
        'Uses atomics',
      ],
      a: 1,
      x: [
        'Uses constant memory',
        'Reads global memory',
        'Has many streams',
      ],
      e: 'The per-thread stack backs function call frames and some local data. Deep recursion or large local objects may exceed the default, causing errors; raising cudaLimitStackSize fixes it but increases local-memory footprint.',
      ref: 'CUDA Runtime API (cudaDeviceSetLimit)',
      tags: ['stack-size', 'limits', 'recursion'],
    },
    {
      d: 2,
      q: 'Passing a kernel argument by value that is a large array (e.g. float[1000]) is…',
      o: [
        'Efficient and recommended',
        'Wrong — it exceeds the param limit',
        'Automatically converted to a pointer',
        'Stored in shared memory',
      ],
      a: 1,
      x: [
        'Copied to each thread’s registers',
        'Placed in constant memory automatically',
        'Run-time resized',
      ],
      e: 'Kernel parameters have a small size limit (a few KB). Large data must be allocated on the device and passed as a pointer; by-value is only for small scalars/structs. (C arrays decay to pointers anyway, but the intent is wrong.)',
      ref: 'CUDA C++ Programming Guide; parameter passing',
      tags: ['kernel-arguments', 'pointers', 'limits'],
    },
    {
      d: 4,
      q: 'Why does cudaMalloc return memory aligned to at least 256 bytes?',
      o: [
        'To waste space',
        'For aligned vectorized/texture access',
        'For host compatibility',
        'To enable atomics',
      ],
      a: 1,
      x: [
        'To match the page size',
        'For ECC',
        'To support recursion',
      ],
      e: 'cudaMalloc guarantees generous alignment (≥256 bytes) so the start of every buffer supports the widest vector loads/stores and texture use. This is why the first thread of a warp reading a cudaMalloc’d float array is aligned.',
      ref: 'CUDA Runtime API; alignment guarantees',
      tags: ['alignment', 'cudaMalloc', 'vectorization'],
    },
    {
      d: 3,
      q: 'The compute capability you compile for (e.g. sm_80) primarily must be…',
      o: [
        'Higher than the GPU’s',
        '≤ the target GPU’s compute capability',
        'Exactly 7.5 always',
        'Irrelevant',
      ],
      a: 1,
      x: [
        'Always the newest available',
        'Equal to the CUDA version',
        'Lower than 3.0',
      ],
      e: 'SASS built for sm_90 won’t run on an sm_80 GPU. You compile SASS for the GPUs you target (≤ their capability) and embed PTX for forward compatibility to newer ones. Mismatch yields "no kernel image available" errors.',
      ref: 'CUDA C++ Programming Guide (Compatibility)',
      tags: ['compute-capability', 'compatibility', 'sass'],
    },
    {
      d: 5,
      q: 'The error "no kernel image is available for execution on the device" usually means…',
      o: [
        'Out of memory',
        'No SASS or compatible PTX for this GPU',
        'The kernel has a bug',
        'The driver is missing',
      ],
      a: 1,
      x: [
        'The grid is too large',
        'Shared memory overflow',
        'A race condition',
      ],
      e: 'This means the fatbin has no SASS matching the GPU and no PTX to JIT from. Fix by adding the right -gencode (SASS for the target arch and/or PTX for JIT). It is a build/packaging issue, not a runtime data error.',
      ref: 'CUDA compatibility; -gencode',
      tags: ['no-kernel-image', 'gencode', 'compatibility'],
    },
    {
      d: 3,
      q: 'cudaStreamCreate followed by passing that stream as the 4th launch parameter causes the kernel to…',
      o: [
        'Run on the host',
        'Run in that stream, concurrent with others',
        'Run synchronously',
        'Use more registers',
      ],
      a: 1,
      x: [
        'Run on a second GPU',
        'Ignore the stream',
        'Always overlap with copies',
      ],
      e: 'The optional 4th <<<grid, block, smem, stream>>> argument places the launch in a specific stream, so it can overlap with independent work on other streams (subject to resource availability).',
      ref: 'CUDA C++ Programming Guide §6.2.8; launch configuration',
      tags: ['streams', 'launch-config', 'concurrency'],
    },
  ],
  { idPrefix: 'basics-c' }
)
