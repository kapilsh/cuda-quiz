import { defineQuestions } from './_helpers.js'

// Multi-GPU & NCCL — batch B. Init/bootstrap, ops/datatypes, protocols, SHARP,
// P2P, send/recv, tuning. Distinct from multigpu.js (batch A).
export default defineQuestions(
  'multigpu',
  [
    {
      d: 3,
      q: 'To initialize a NCCL communicator across processes, all ranks must share a…',
      o: [
        'CUDA stream',
        'A ncclUniqueId shared across all ranks',
        'Global memory pointer',
        'File handle',
      ],
      a: 1,
      x: [
        'Hostname list only',
        'cuBLAS handle',
        'Random seed',
      ],
      e: 'One rank calls ncclGetUniqueId and distributes it (over MPI/sockets); every rank then calls ncclCommInitRank with that id, its rank, and the world size to join the communicator. The id bootstraps the connection setup.',
      ref: 'NCCL User Guide (Creating a Communicator)',
      tags: ['nccl', 'unique-id', 'bootstrap'],
    },
    {
      d: 3,
      q: 'ncclCommInitAll vs ncclCommInitRank: when do you use ncclCommInitAll?',
      o: [
        'For multi-node jobs',
        'Single-process multi-GPU; initializes all at once',
        'Only with MPI',
        'For host-only communication',
      ],
      a: 1,
      x: [
        'For one GPU per process always',
        'For CPU collectives',
        'When using unified memory',
      ],
      e: 'ncclCommInitAll sets up communicators for several GPUs controlled by ONE process/thread (single-process multi-GPU). The more common scalable pattern is one process per GPU using ncclCommInitRank with a shared unique id.',
      ref: 'NCCL User Guide',
      tags: ['nccl', 'init', 'single-process'],
    },
    {
      d: 2,
      q: 'The recommended scaling pattern for large jobs is…',
      o: [
        'One process controlling all GPUs',
        'One process (rank) per GPU, communicating via NCCL',
        'One thread per node',
        'One GPU shared by all processes',
      ],
      a: 1,
      x: [
        'One process per node only',
        'Threads sharing one CUDA context',
        'No processes; pure threads',
      ],
      e: 'One-process-per-GPU is the standard (e.g. torchrun, MPI): each rank owns a GPU and a NCCL communicator, avoiding single-process multi-GPU context contention and matching framework launchers.',
      ref: 'NCCL User Guide; PyTorch distributed',
      tags: ['nccl', 'process-per-gpu', 'scaling'],
    },
    {
      d: 2,
      q: 'Which is NOT a standard NCCL reduction operation?',
      o: ['ncclSum', 'ncclMax', 'ncclProd', 'ncclSort'],
      a: 3,
      x: ['ncclMin', 'ncclAvg', 'ncclBor (bitwise or)'],
      e: 'NCCL supports Sum, Prod, Max, Min, Avg, and bitwise ops (for integer types). There is no sort collective — sorting across ranks is not a NCCL primitive.',
      ref: 'NCCL User Guide (Reduction Operations)',
      tags: ['nccl', 'reduction-ops'],
    },
    {
      d: 4,
      q: 'NCCL protocols include Simple, LL, and LL128. The "LL" (low-latency) protocols trade…',
      o: [
        'Accuracy for speed',
        'Bandwidth for latency on small messages',
        'Correctness for speed',
        'Memory for compute',
      ],
      a: 1,
      x: [
        'Latency for bandwidth',
        'GPUs for CPUs',
        'Precision for range',
      ],
      e: 'LL/LL128 embed data with synchronization flags to avoid extra handshakes, minimizing latency for small messages at some bandwidth cost. NCCL auto-selects protocol (and ring vs tree) by size and topology; NCCL_PROTO can override.',
      ref: 'NCCL tuning; NCCL_PROTO',
      tags: ['nccl', 'protocols', 'll128', 'latency'],
    },
    {
      d: 4,
      q: 'In-network reduction with NVLink SHARP (on NVSwitch) speeds collectives by…',
      o: [
        'Compressing data',
        'In-switch reduction, not just at endpoints',
        'Skipping some GPUs',
        'Using the CPU',
      ],
      a: 1,
      x: [
        'Lowering precision',
        'Caching in L2',
        'Using PCIe',
      ],
      e: 'SHARP offloads reduction arithmetic into the NVSwitch, halving the data each GPU must send/receive for an all-reduce and reducing latency — the switch aggregates partial results in-network.',
      ref: 'NVIDIA NVLink SHARP; NCCL',
      tags: ['sharp', 'nvswitch', 'in-network-reduction'],
    },
    {
      d: 3,
      q: 'Before two GPUs can do direct peer-to-peer copies, you typically check and enable access with…',
      o: [
        'cudaMemcpyPeer always works without setup',
        'cudaDeviceCanAccessPeer then cudaDeviceEnablePeerAccess',
        'cudaSetDevice only',
        'ncclCommInitRank',
      ],
      a: 1,
      x: [
        'cudaHostRegister',
        'cudaStreamCreate',
        'cudaMallocManaged',
      ],
      e: 'Query cudaDeviceCanAccessPeer(&ok, dev, peer); if supported, call cudaDeviceEnablePeerAccess(peer, 0). Then kernels/copies can address peer memory directly over NVLink/PCIe. cudaMemcpyPeer copies between devices.',
      ref: 'CUDA C++ Programming Guide (Peer Device Memory Access)',
      tags: ['p2p', 'peer-access', 'multi-gpu'],
    },
    {
      d: 3,
      q: 'NCCL point-to-point ncclSend/ncclRecv enable…',
      o: [
        'Only collectives',
        'Directed comm between any two ranks, composable',
        'Host-to-host transfers',
        'Memory allocation',
      ],
      a: 1,
      x: [
        'Broadcasts only',
        'Reductions only',
        'Random shuffles',
      ],
      e: 'ncclSend/Recv give P2P building blocks. Wrapped in ncclGroupStart/End, matched sends/recvs implement neighbor exchange, pipeline-stage handoff, and all-to-all without deadlock.',
      ref: 'NCCL User Guide (Point-to-point)',
      tags: ['nccl', 'send-recv', 'point-to-point'],
    },
    {
      d: 4,
      q: 'The difference between "algorithm bandwidth" (algbw) and "bus bandwidth" (busbw) when benchmarking NCCL is that busbw…',
      o: [
        'Is always lower',
        'Normalized per-link bandwidth estimate',
        'Ignores the data size',
        'Measures latency',
      ],
      a: 1,
      x: [
        'Counts only host transfers',
        'Is the PCIe rate',
        'Measures compute',
      ],
      e: 'algbw = data_size / time (user-visible), while busbw applies the algorithm’s traffic factor to estimate actual per-link bandwidth, making it the right metric to compare against hardware peak and across world sizes.',
      ref: 'nccl-tests documentation (busbw)',
      tags: ['nccl', 'busbw', 'benchmarking'],
    },
    {
      d: 3,
      q: 'NCCL collectives can be issued from within a CUDA Graph capture so that…',
      o: [
        'They run on the CPU',
        'Recorded into the graph, replayed with low overhead',
        'They become synchronous',
        'They skip the network',
      ],
      a: 1,
      x: [
        'They are compressed',
        'They use more memory',
        'They bypass the communicator',
      ],
      e: 'NCCL supports CUDA Graph capture, letting an iteration’s collectives + kernels be captured once and replayed cheaply — valuable for reducing per-step CPU overhead in tight training loops.',
      ref: 'NCCL User Guide (CUDA Graph support)',
      tags: ['nccl', 'cuda-graphs', 'overhead'],
    },
    {
      d: 2,
      q: 'For very small messages, an all-reduce is typically bound by…',
      o: [
        'Link bandwidth',
        'Latency — too little data to saturate links',
        'GPU compute',
        'Memory capacity',
      ],
      a: 1,
      x: [
        'Disk I/O',
        'Register pressure',
        'The reduction op cost',
      ],
      e: 'Small collectives are latency-bound: the fixed per-step costs dominate. This is why NCCL uses low-latency protocols and tree algorithms for small sizes, and why gradient bucketing batches small tensors into larger transfers.',
      ref: 'NCCL performance; latency vs bandwidth',
      tags: ['nccl', 'latency', 'small-messages'],
    },
    {
      d: 4,
      q: 'CUDA-aware MPI and NCCL overlap in capability, but NCCL is usually preferred for DL collectives because it…',
      o: [
        'Is the only option',
        'Topology-aware, tuned for GPU collectives',
        'Runs on the CPU',
        'Requires no setup',
      ],
      a: 1,
      x: [
        'Supports more datatypes',
        'Is part of the OS',
        'Avoids the network',
      ],
      e: 'NCCL is purpose-built for GPU collectives with topology detection and tuned ring/tree algorithms over NVLink/IB. CUDA-aware MPI can move GPU buffers, but NCCL typically delivers better collective performance for training.',
      ref: 'NCCL vs MPI; HPC/DL practice',
      tags: ['nccl', 'mpi', 'collectives'],
    },
    {
      d: 3,
      q: 'Setting NCCL_DEBUG=INFO helps you…',
      o: [
        'Speed up collectives',
        'See topology and transport choices NCCL made',
        'Disable the network',
        'Reduce memory use',
      ],
      a: 1,
      x: [
        'Encrypt traffic',
        'Change the precision',
        'Pin memory',
      ],
      e: 'NCCL_DEBUG=INFO (and NCCL_DEBUG_SUBSYS) prints topology/transport decisions, helping diagnose when NCCL falls back to a slow path (e.g. PCIe instead of NVLink, or sockets instead of IB).',
      ref: 'NCCL User Guide (Environment Variables)',
      tags: ['nccl', 'debug', 'topology'],
    },
    {
      d: 4,
      q: 'An "in-place" collective (e.g. all-reduce where sendbuff == recvbuff) is supported and…',
      o: [
        'Always slower',
        'Reuses one buffer; NCCL handles it correctly',
        'Produces wrong results',
        'Requires two GPUs only',
      ],
      a: 1,
      x: [
        'Disables overlap',
        'Needs a separate communicator',
        'Doubles bandwidth',
      ],
      e: 'NCCL allows in-place operation for collectives like all-reduce/all-gather (with layout rules), so you don’t need a separate output buffer — handy for memory-tight training. Some patterns still require distinct buffers.',
      ref: 'NCCL User Guide (in-place operations)',
      tags: ['nccl', 'in-place', 'memory'],
    },
    {
      d: 5,
      q: 'NCCL "double binary tree" all-reduce achieves both low latency AND full bandwidth by…',
      o: [
        'Using one tree',
        'Two trees; leaf in one, internal in the other',
        'Skipping the reduction',
        'Using only NVLink',
      ],
      a: 1,
      x: [
        'Compressing gradients',
        'Using the CPU as root',
        'Halving the data precision',
      ],
      e: 'A single tree underuses links (leaves only receive). Two interleaved binary trees let each rank play both roles, so aggregate bandwidth approaches ring-level while keeping the tree’s logarithmic latency — great at large scale.',
      ref: 'NCCL "double binary tree" design',
      tags: ['nccl', 'double-binary-tree', 'allreduce', 'scaling'],
    },
    {
      d: 3,
      q: 'Multiple network rails (NICs) per node help NCCL by…',
      o: [
        'Reducing GPU memory use',
        'Parallel inter-node paths, one per NIC/rail',
        'Lowering precision',
        'Replacing NVLink',
      ],
      a: 1,
      x: [
        'Compressing data',
        'Increasing clock speed',
        'Pinning memory',
      ],
      e: 'Rail-optimized topologies pair GPUs with dedicated NICs; NCCL uses multiple channels across rails to aggregate inter-node bandwidth. Proper rail/affinity configuration is important for scaling collectives across nodes.',
      ref: 'NCCL topology; rail-optimized networks',
      tags: ['nccl', 'rails', 'nics', 'multi-node'],
    },
    {
      d: 4,
      q: 'NCCL uses multiple "channels" for a collective primarily to…',
      o: [
        'Provide error correction',
        'Run comms across links/SMs in parallel',
        'Encrypt data',
        'Reduce GPU memory',
      ],
      a: 1,
      x: [
        'Lower precision',
        'Run on the CPU',
        'Avoid the reduction',
      ],
      e: 'A channel is an independent ring/tree instance; running several in parallel uses more links and copy engines simultaneously, increasing achieved bandwidth. NCCL_MIN/MAX_NCHANNELS can tune this.',
      ref: 'NCCL internals (channels)',
      tags: ['nccl', 'channels', 'bandwidth'],
    },
    {
      d: 3,
      q: 'Why must NCCL collectives generally NOT be called from within divergent control flow across ranks?',
      o: [
        'It is slower',
        'All ranks must call the same op; skipping deadlocks',
        'It uses more memory',
        'It corrupts the data',
      ],
      a: 1,
      x: [
        'NCCL forbids branches',
        'It requires the default stream',
        'It needs MPI',
      ],
      e: 'Collectives are synchronizing across all ranks. Rank-dependent control flow that causes some ranks to skip or reorder a collective leads to deadlock or mismatched operations — a frequent multi-GPU bug.',
      ref: 'NCCL User Guide; collective correctness',
      tags: ['nccl', 'control-flow', 'deadlock'],
    },
    {
      d: 4,
      q: 'ncclCommAbort exists to…',
      o: [
        'Speed up the next collective',
        'Abort a stuck communicator, enabling fault recovery',
        'Reset the GPU clocks',
        'Free GPU memory only',
      ],
      a: 1,
      x: [
        'Pause communication',
        'Switch protocols',
        'Reduce latency',
      ],
      e: 'When a rank dies or a collective hangs, ncclCommAbort lets the application abort the communicator and recover/restart, rather than deadlocking forever — part of building fault-tolerant training.',
      ref: 'NCCL User Guide (fault handling)',
      tags: ['nccl', 'fault-tolerance', 'abort'],
    },
    {
      d: 2,
      q: 'Within a single DGX node, intra-node all-reduce among 8 GPUs primarily travels over…',
      o: [
        'PCIe to the CPU and back',
        'NVLink/NVSwitch high-bandwidth fabric',
        'InfiniBand',
        'Ethernet',
      ],
      a: 1,
      x: [
        'The host DRAM',
        'USB',
        'The display port',
      ],
      e: 'DGX-class nodes connect all GPUs via NVSwitch/NVLink, so intra-node collectives use that fabric (hundreds of GB/s) rather than PCIe. Inter-node traffic then uses InfiniBand/RoCE.',
      ref: 'NVIDIA DGX architecture; NCCL transports',
      tags: ['nvlink', 'nvswitch', 'intra-node'],
    },
    {
      d: 5,
      q: 'Hierarchical (two-level) collectives reduce inter-node traffic by…',
      o: [
        'Skipping some GPUs',
        'Reduce within node first, then one message per node',
        'Compressing all data',
        'Using FP16 only',
      ],
      a: 1,
      x: [
        'Running everything on one GPU',
        'Avoiding the reduction',
        'Using the CPU',
      ],
      e: 'By aggregating within a node first (fast NVLink), only one reduced message per node crosses the slow inter-node fabric, then results are scattered back locally. This hierarchy is how NCCL scales all-reduce efficiently across many nodes.',
      ref: 'NCCL hierarchical algorithms',
      tags: ['nccl', 'hierarchical', 'allreduce', 'multi-node'],
    },
    {
      d: 3,
      q: 'NCCL operations are enqueued on a CUDA stream, so to overlap an all-reduce with backward compute you…',
      o: [
        'Call cudaDeviceSynchronize after each',
        'Issue on a separate comm stream; events order with compute',
        'Run them on the CPU',
        'Use a single default stream',
      ],
      a: 1,
      x: [
        'Disable the communicator',
        'Use unified memory',
        'Lower the precision',
      ],
      e: 'Putting collectives on a dedicated stream lets them run concurrently with compute on other streams; events/stream dependencies enforce correctness (gradient ready before reduce, reduce done before optimizer). This is how DDP overlaps comm with backprop.',
      ref: 'NCCL streams; PyTorch DDP overlap',
      tags: ['nccl', 'streams', 'overlap'],
    },
    {
      d: 4,
      q: 'Sub-communicators (splitting a world communicator into groups) are used in 3D-parallel training to…',
      o: [
        'Reduce precision',
        'Separate communicator per parallelism axis',
        'Speed up the host',
        'Avoid NCCL',
      ],
      a: 1,
      x: [
        'Merge all GPUs into one',
        'Disable pipeline parallelism',
        'Encrypt gradients',
      ],
      e: 'You build a communicator per parallel dimension (e.g. a tensor-parallel group within a node, a data-parallel group across replicas). Each collective (TP all-reduce, DP all-reduce) runs only over its group’s ranks.',
      ref: 'Megatron-LM; NCCL communicator groups',
      tags: ['nccl', 'sub-communicators', '3d-parallelism'],
    },
    {
      d: 3,
      q: 'Why does gradient bucketing (fusing many small gradient tensors into larger buffers) speed up data-parallel training?',
      o: [
        'It reduces the model size',
        'Fewer, larger all-reduces amortize overhead',
        'It increases precision',
        'It removes synchronization',
      ],
      a: 1,
      x: [
        'It compresses gradients',
        'It skips some gradients',
        'It uses the CPU',
      ],
      e: 'Each collective has fixed overhead and small messages are latency-bound. Bucketing coalesces gradients into sizable all-reduces, amortizing overhead and reaching bandwidth-bound efficiency, and enabling overlap as each bucket fills.',
      ref: 'PyTorch DDP (gradient bucketing)',
      tags: ['nccl', 'bucketing', 'ddp', 'overlap'],
    },
    {
      d: 5,
      q: 'NCCL "non-blocking" communicator initialization (ncclCommInitRankConfig with blocking=0) helps because…',
      o: [
        'It makes collectives faster',
        'Non-blocking init; allows timeout/failure handling',
        'It uses less bandwidth',
        'It lowers precision',
      ],
      a: 1,
      x: [
        'It skips the unique id',
        'It runs on the CPU',
        'It compresses data',
      ],
      e: 'Non-blocking mode lets the application poll communicator state and impose timeouts, important for fault-tolerant/elastic training where a stalled peer should be detected and handled instead of hanging initialization.',
      ref: 'NCCL User Guide (non-blocking communicators)',
      tags: ['nccl', 'non-blocking', 'fault-tolerance'],
    },
    {
      d: 2,
      q: 'In data-parallel training, the all-reduced gradients must be divided by the world size (or use ncclAvg) so that…',
      o: [
        'The gradients fit in FP16',
        'Gives the mean gradient, matching single-GPU SGD',
        'The communication is faster',
        'The optimizer runs on the host',
      ],
      a: 1,
      x: [
        'They become integers',
        'The variance increases',
        'The model shards correctly',
      ],
      e: 'Summing gradients across N replicas gives N× the per-replica gradient; dividing by N (or using ncclAvg) yields the average, equivalent to one large-batch update. Forgetting this effectively scales the learning rate by N.',
      ref: 'Distributed SGD; PyTorch DDP',
      tags: ['allreduce', 'averaging', 'data-parallel'],
    },
    {
      d: 4,
      q: 'When NVLink is unavailable between two GPUs, NCCL may fall back to P2P over PCIe or through host memory. The performance impact is…',
      o: [
        'None',
        'Significant — PCIe/host has far lower bandwidth than NVLink',
        'Faster',
        'Only affects latency, not bandwidth',
      ],
      a: 1,
      x: [
        'It improves overlap',
        'It increases precision',
        'It reduces memory use',
      ],
      e: 'NVLink offers hundreds of GB/s; PCIe is tens of GB/s and a host-staged path is worse. A misconfigured topology (NCCL using PCIe/SHM instead of NVLink) is a common cause of unexpectedly slow multi-GPU training — check NCCL_DEBUG=INFO.',
      ref: 'NCCL transports; topology debugging',
      tags: ['nccl', 'nvlink', 'pcie', 'fallback'],
    },
  ],
  { idPrefix: 'multigpu-b' }
)
