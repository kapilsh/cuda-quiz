import { defineQuestions } from './_helpers.js'

// Profiling — batch C. ncu sections/metrics/CLI, nsys trace/stats, PC sampling,
// framework profilers, cluster monitoring. Distinct from profiling.js/.b.js.
export default defineQuestions(
  'profiling',
  [
    {
      d: 4,
      q: 'In Nsight Compute, the metric sm__throughput.avg.pct_of_peak_sustained_elapsed reports…',
      o: [
        'The clock frequency',
        'The SM (compute) pipeline utilization as a percentage of its sustainable peak — a key Speed-of-Light number',
        'The DRAM size',
        'The register count',
      ],
      a: 1,
      x: [
        'The occupancy',
        'The kernel duration',
        'The number of warps',
      ],
      e: 'This is the compute-side Speed-of-Light percentage. Compared against the analogous memory throughput metric, it classifies the kernel as compute- or memory-bound. Nsight’s named metrics follow a structured base.qualifier.rollup scheme.',
      ref: 'Nsight Compute metrics; Speed of Light',
      tags: ['nsight-compute', 'metrics', 'speed-of-light'],
    },
    {
      d: 3,
      q: 'Nsight Compute organizes analysis into "sections" (Occupancy, Memory Workload Analysis, Scheduler Statistics, etc.). Using --set basic vs full controls…',
      o: [
        'The GPU clock',
        'How many sections/metrics are collected (and thus how many replays and how long profiling takes)',
        'The kernel itself',
        'The output file format',
      ],
      a: 1,
      x: [
        'The block size',
        'The number of GPUs',
        'The precision',
      ],
      e: 'A larger set collects more sections/metrics, needing more kernel replays and time. Use a smaller set (or specific --metrics) for quick checks and the full set when you need a complete picture.',
      ref: 'Nsight Compute CLI (--set, sections)',
      tags: ['nsight-compute', 'sections', 'cli'],
    },
    {
      d: 4,
      q: 'PC sampling (statistical sampling of warp program counters) in profilers is used to…',
      o: [
        'Count exact instructions',
        'Statistically attribute stall reasons and hot spots to source/SASS locations without instrumenting every instruction',
        'Measure bandwidth',
        'Set the clock',
      ],
      a: 1,
      x: [
        'Allocate memory',
        'Replay the kernel',
        'Lock the clocks',
      ],
      e: 'PC sampling periodically records which instruction each warp is on and why it’s stalled, building a statistical profile of hotspots and stall reasons at low overhead — complementing exact hardware counters.',
      ref: 'Nsight Compute / CUPTI PC sampling',
      tags: ['pc-sampling', 'stall-reasons', 'profiling'],
    },
    {
      d: 3,
      q: 'nsys profile --trace=cuda,nvtx,osrt,cublas adds osrt and cublas tracing to…',
      o: [
        'Speed up the app',
        'Capture OS runtime (thread scheduling, syscalls) and cuBLAS calls on the timeline, helping locate CPU-side and library bottlenecks',
        'Reduce overhead',
        'Disable CUDA tracing',
      ],
      a: 1,
      x: [
        'Profile only the GPU',
        'Compress the trace',
        'Lock the clocks',
      ],
      e: 'Nsight Systems can trace multiple sources: CUDA API/GPU, NVTX ranges, OS runtime (osrt) for CPU threads/syscalls, and library calls (cuBLAS/cuDNN/NCCL). Adding the right traces helps pinpoint whether the CPU, a library, or the GPU is the bottleneck.',
      ref: 'Nsight Systems CLI (--trace)',
      tags: ['nsight-systems', 'trace', 'cli'],
    },
    {
      d: 3,
      q: 'nsys stats <report>.nsys-rep produces…',
      o: [
        'A new kernel',
        'Text/CSV summary tables (kernel durations, memcpy stats, API call counts) from a captured report — scriptable, no GUI needed',
        'A roofline chart',
        'A SASS dump',
      ],
      a: 1,
      x: [
        'A flame graph only',
        'A memory dump',
        'A compiled binary',
      ],
      e: 'nsys stats post-processes a report into summary tables (e.g. top kernels by time, memcpy throughput), great for CI/automation. The underlying data is also queryable via the exported SQLite database.',
      ref: 'Nsight Systems (nsys stats)',
      tags: ['nsight-systems', 'stats', 'automation'],
    },
    {
      d: 4,
      q: 'The Nsight Compute "rules" / guided analysis surface things like "Uncoalesced Global Accesses" by…',
      o: [
        'Guessing',
        'Applying built-in heuristics over collected metrics to flag likely bottlenecks and suggest fixes, turning raw counters into actionable advice',
        'Recompiling the kernel',
        'Running the kernel twice',
      ],
      a: 1,
      x: [
        'Reading the source comments',
        'Using NVTX',
        'Locking the clocks',
      ],
      e: 'Rules interpret the metric set (e.g. low load efficiency → uncoalesced access; high local traffic → spills) and present guidance. They’re a fast way to get a prioritized list of issues before deep manual analysis.',
      ref: 'Nsight Compute (rules / guided analysis)',
      tags: ['nsight-compute', 'rules', 'analysis'],
    },
    {
      d: 4,
      q: 'To compare two kernel implementations fairly under boost-clock variability, you should…',
      o: [
        'Run each once',
        'Lock the GPU clocks (nvidia-smi -lgc / ncu --clock-control) so timing differences reflect the code, not clock fluctuations',
        'Use a larger grid',
        'Disable the L2',
      ],
      a: 1,
      x: [
        'Profile on the CPU',
        'Use FP64',
        'Add more streams',
      ],
      e: 'GPUs boost clocks based on thermal/power state, so naive timing varies. Locking clocks makes runs comparable; Nsight Compute can also fix clocks during collection. Combine with warm-up and averaging.',
      ref: 'Nsight Compute (--clock-control); nvidia-smi -lgc',
      tags: ['clock-control', 'benchmarking', 'methodology'],
    },
    {
      d: 4,
      q: 'In Nsight Systems, enabling GPU metrics sampling (--gpu-metrics-device) adds timeline rows for…',
      o: [
        'Per-kernel registers',
        'Coarse hardware activity over time (SM active %, tensor/DRAM activity) sampled across the run, independent of per-kernel replay',
        'Source lines',
        'Bank conflicts',
      ],
      a: 1,
      x: [
        'Exact FLOP counts',
        'The roofline',
        'Stall reasons per instruction',
      ],
      e: 'GPU metrics sampling overlays low-overhead, time-based utilization (SM/tensor/DRAM activity) on the timeline, helping spot idle periods and under-utilization across the whole run — complementary to Nsight Compute’s per-kernel deep dive.',
      ref: 'Nsight Systems (GPU metrics sampling)',
      tags: ['nsight-systems', 'gpu-metrics', 'timeline'],
    },
    {
      d: 3,
      q: 'For profiling a PyTorch training step, a good first tool is…',
      o: [
        'cuobjdump',
        'torch.profiler (or Nsight Systems with NVTX) to see Python/CPU ops, CUDA kernels, and gaps on a timeline',
        'Nsight Compute on every kernel',
        'compute-sanitizer',
      ],
      a: 1,
      x: [
        'ptxas -v',
        'cuda-gdb',
        'nvdisasm',
      ],
      e: 'torch.profiler (with the Chrome-trace/TensorBoard view) or nsys captures the end-to-end step — data loading, Python overhead, kernel launches, and GPU gaps — to find the dominant cost before drilling into a specific kernel with ncu.',
      ref: 'PyTorch Profiler; Nsight Systems',
      tags: ['torch-profiler', 'nsight-systems', 'training'],
    },
    {
      d: 3,
      q: 'The metric dram__bytes.sum tells you…',
      o: [
        'The DRAM capacity',
        'The total bytes transferred to/from device memory during the kernel — used to compute achieved bandwidth and verify reuse',
        'The L1 size',
        'The register count',
      ],
      a: 1,
      x: [
        'The shared-memory usage',
        'The number of warps',
        'The clock rate',
      ],
      e: 'dram__bytes counts actual HBM traffic. Dividing by kernel time gives achieved bandwidth; comparing to the theoretical minimum bytes reveals wasted traffic from poor coalescing or missing reuse.',
      ref: 'Nsight Compute memory metrics',
      tags: ['dram-bytes', 'bandwidth', 'metrics'],
    },
    {
      d: 4,
      q: 'A high L1/TEX cache hit rate but a still-slow memory-bound kernel suggests…',
      o: [
        'Perfect performance',
        'The kernel may be limited by the cache/LSU throughput or instruction count rather than DRAM — look at pipe utilization and replays, not just hit rate',
        'Register spilling',
        'A divergent branch',
      ],
      a: 1,
      x: [
        'Tensor-core starvation',
        'A host bottleneck',
        'Low occupancy only',
      ],
      e: 'High hit rate means DRAM isn’t the issue, but the kernel can still bottleneck on the L1/LSU pipeline (too many memory instructions, replays). Check pipe utilization (LSU/MIO throttle) and reduce/vectorize memory instructions.',
      ref: 'Nsight Compute (memory pipe analysis)',
      tags: ['cache-hit-rate', 'lsu', 'bottleneck'],
    },
    {
      d: 3,
      q: 'launch__registers_per_thread (reported by ncu) is useful because…',
      o: [
        'It times the kernel',
        'It tells you the register usage that drives occupancy and spill behavior, so you can target -maxrregcount/__launch_bounds__',
        'It measures bandwidth',
        'It counts blocks',
      ],
      a: 1,
      x: [
        'It shows the grid size',
        'It reports the clock',
        'It lists stall reasons',
      ],
      e: 'Knowing registers/thread (also from ptxas -v at compile time) lets you reason about occupancy limits and decide whether to cap registers. Combined with spill metrics, it guides register-pressure tuning.',
      ref: 'Nsight Compute (launch statistics)',
      tags: ['registers', 'occupancy', 'metrics'],
    },
    {
      d: 4,
      q: 'Nsight Compute reports many smsp__ metrics. "smsp" refers to…',
      o: [
        'Shared memory',
        'The SM sub-partition (sub-processor) — metrics scoped to one of the SM’s 4 schedulers/datapaths',
        'A streaming multiprocessor pair',
        'A memory partition',
      ],
      a: 1,
      x: [
        'A symmetric multiprocessor',
        'The L2 slice',
        'A warp',
      ],
      e: 'smsp = SM sub-partition. Many scheduler/issue/warp-state metrics are reported per sub-partition since each has its own warp scheduler and execution units. Understanding the prefix helps interpret where a bottleneck lives.',
      ref: 'Nsight Compute metric naming (smsp)',
      tags: ['smsp', 'sub-partition', 'metrics'],
    },
    {
      d: 3,
      q: 'You can run Nsight Compute headless on a server and view results later by…',
      o: [
        'It is impossible',
        'Collecting with the ncu CLI to an .ncu-rep report file, then opening it in the Nsight Compute GUI on your workstation',
        'Only using printf',
        'Streaming the GUI live only',
      ],
      a: 1,
      x: [
        'Emailing the kernel',
        'Recompiling locally',
        'Using nvidia-smi',
      ],
      e: 'The typical remote workflow: ncu --set ... -o report on the server, copy the .ncu-rep, and open it in the GUI locally for full interactive analysis. Same pattern for nsys (.nsys-rep).',
      ref: 'Nsight Compute/Systems remote workflow',
      tags: ['nsight-compute', 'remote', 'report-file'],
    },
    {
      d: 4,
      q: 'DCGM (Data Center GPU Manager) is used in clusters to…',
      o: [
        'Profile a single kernel',
        'Continuously monitor GPU health, utilization, power, thermals, and errors across many GPUs/nodes (telemetry, not per-kernel profiling)',
        'Compile kernels',
        'Disassemble SASS',
      ],
      a: 1,
      x: [
        'Replace Nsight Compute',
        'Generate random numbers',
        'Run collectives',
      ],
      e: 'DCGM provides fleet-level telemetry and health checks (utilization, ECC errors, throttling, power) for data centers, integrating with schedulers/monitoring. It complements Nsight (deep per-kernel) with broad operational visibility.',
      ref: 'NVIDIA DCGM documentation',
      tags: ['dcgm', 'monitoring', 'cluster'],
    },
    {
      d: 4,
      q: 'A profile shows the GPU frequently throttling. nvidia-smi -q can reveal the throttle reason, which is often…',
      o: [
        'A software bug',
        'Power (TDP) or thermal limits (or clocks capped) — the GPU reduces frequency to stay within power/temperature budgets',
        'Too many registers',
        'A divergent branch',
      ],
      a: 1,
      x: [
        'A memory leak',
        'Low occupancy',
        'A missing fence',
      ],
      e: 'Clock throttling from power/thermal limits lowers achieved performance and adds run-to-run variance. nvidia-smi reports throttle reasons (SW power cap, HW thermal slowdown). Better cooling/power or locked clocks stabilize benchmarks.',
      ref: 'nvidia-smi (clocks throttle reasons)',
      tags: ['throttling', 'power', 'thermal'],
    },
    {
      d: 3,
      q: 'To capture only steady-state iterations with Nsight Systems (skip warm-up), you can use…',
      o: [
        'Nothing; it captures everything',
        '--capture-range with cudaProfilerStart/Stop (or -y delay / -d duration) to bound the capture window',
        'A larger block size',
        'compute-sanitizer',
      ],
      a: 1,
      x: [
        'ptxas -v',
        'cuda-gdb',
        'nvdisasm',
      ],
      e: 'Bounding the capture (delay/duration, or a cudaProfiler start/stop range) focuses the trace on representative steps, avoiding huge files and initialization noise. Pair with NVTX to label the region.',
      ref: 'Nsight Systems (capture range)',
      tags: ['nsight-systems', 'capture-range', 'workflow'],
    },
    {
      d: 4,
      q: 'In a multi-stream app, Nsight Systems shows two kernels you expected to overlap running sequentially. A likely cause is…',
      o: [
        'The GPU is broken',
        'A hidden synchronization (e.g. a default-stream operation, a blocking copy, or an event wait) is serializing them, or one kernel saturates the SMs',
        'Too few registers',
        'The kernels are too small',
      ],
      a: 1,
      x: [
        'ECC is enabled',
        'The block size is odd',
        'Tensor cores are off',
      ],
      e: 'Unexpected serialization usually comes from an implicit sync point (legacy default stream, blocking memcpy/alloc) or full SM occupancy leaving no room for concurrency. The timeline + checking for default-stream ops pinpoints it.',
      ref: 'Nsight Systems; stream concurrency debugging',
      tags: ['nsight-systems', 'serialization', 'streams'],
    },
    {
      d: 5,
      q: 'Achieved FLOP/s for a kernel can be computed in Nsight Compute from…',
      o: [
        'The kernel name',
        'FLOP-counting instruction metrics (e.g. sm__sass_thread_inst_executed_op_*fma_pred_on) scaled appropriately, divided by the kernel duration',
        'The register count',
        'The grid size only',
      ],
      a: 1,
      x: [
        'The occupancy',
        'The DRAM bytes',
        'The block size',
      ],
      e: 'Nsight exposes instruction-count metrics per op type (FFMA counts as 2 FLOPs, etc.); summing the relevant ones and dividing by time gives achieved FLOP/s, which placed against peak (and intensity) drives the roofline.',
      ref: 'Nsight Compute (instruction metrics, roofline)',
      tags: ['flops', 'metrics', 'roofline'],
    },
    {
      d: 3,
      q: 'For multi-node training, profiling the whole job is best approached by…',
      o: [
        'Running ncu on every kernel on every node',
        'Profiling a few representative ranks with Nsight Systems (per-rank reports), plus NCCL/communication tracing, then correlating — full ncu on all is impractical',
        'Using printf',
        'Only nvidia-smi',
      ],
      a: 1,
      x: [
        'Profiling the CPU only',
        'Disabling NCCL',
        'Using cuda-gdb',
      ],
      e: 'At scale you sample: capture nsys timelines on a subset of ranks (with NVTX and NCCL traces) to find comm/compute imbalance and stragglers. Deep ncu kernel analysis is reserved for the specific hot kernels you identify.',
      ref: 'Nsight Systems multi-node; profiling at scale',
      tags: ['multi-node', 'nsight-systems', 'nccl'],
    },
    {
      d: 4,
      q: 'compute-sanitizer --tool racecheck detects…',
      o: [
        'Global memory leaks',
        'Shared-memory data races (unsynchronized conflicting accesses within a block)',
        'Slow kernels',
        'Uncoalesced accesses',
      ],
      a: 1,
      x: [
        'Out-of-bounds global access (that is memcheck)',
        'Register spills',
        'Bank conflicts',
      ],
      e: 'racecheck flags shared-memory races (missing __syncthreads/ordering). Sibling tools: memcheck (OOB/misaligned global/shared, leaks), initcheck (uninitialized reads), synccheck (illegal barrier/sync usage). Use them before optimizing.',
      ref: 'compute-sanitizer documentation',
      tags: ['compute-sanitizer', 'racecheck', 'correctness'],
    },
    {
      d: 3,
      q: 'A training step’s nsys timeline shows the GPU idle while the CPU is busy in the data loader. The fix is to…',
      o: [
        'Buy a faster GPU',
        'Overlap/prefetch data loading (more workers, pinned memory, async H2D) so the next batch is ready before the GPU finishes the current one',
        'Reduce the model size',
        'Lower the learning rate',
      ],
      a: 1,
      x: [
        'Increase occupancy',
        'Use tensor cores',
        'Disable the L2',
      ],
      e: 'GPU idle + busy data loader = input pipeline bottleneck. Parallelize/prefetch loading (DataLoader workers, pinned memory + non_blocking copies, on-GPU augmentation) so data is ready in time, keeping the GPU fed.',
      ref: 'PyTorch input pipeline; Nsight Systems',
      tags: ['data-loading', 'bottleneck', 'overlap'],
    },
    {
      d: 5,
      q: 'The Nsight Compute roofline can show TWO ceilings (e.g. an FP32 roof and a Tensor-core roof). A GEMM dot below the tensor roof but near the FP32 roof means…',
      o: [
        'It is optimal',
        'It is not using tensor cores effectively — it is bound by the much lower CUDA-core (FP32) ceiling and should be routed through tensor cores',
        'It is memory-bound',
        'It uses too many registers',
      ],
      a: 1,
      x: [
        'It is latency-bound',
        'It is perfectly balanced',
        'It needs more occupancy',
      ],
      e: 'Hierarchical rooflines plot multiple compute ceilings. A matmul stuck at the FP32 roof (well under the tensor roof) signals it isn’t exploiting tensor cores (wrong precision/path/alignment) and is leaving large speedups on the table.',
      ref: 'Nsight Compute (hierarchical roofline)',
      tags: ['roofline', 'tensor-core', 'gemm'],
    },
    {
      d: 4,
      q: 'When profiling, "kernel replay" can give misleading results if the kernel…',
      o: [
        'Is deterministic',
        'Has side effects or depends on state modified by earlier launches, since each replay restores only the captured memory — stateful behavior across launches may not be reproduced correctly',
        'Uses shared memory',
        'Is compute-bound',
      ],
      a: 1,
      x: [
        'Uses tensor cores',
        'Has high occupancy',
        'Is small',
      ],
      e: 'Kernel replay restores the kernel’s memory between passes, but if correctness depends on cross-launch state or external side effects, replay may not capture it faithfully. Application replay (re-running the whole app per pass) is the alternative.',
      ref: 'Nsight Compute (replay caveats)',
      tags: ['kernel-replay', 'state', 'caveats'],
    },
    {
      d: 3,
      q: 'The single most useful question Nsight Systems answers first is…',
      o: [
        'How many registers per thread?',
        '"Is the GPU actually busy, and if not, why?" — revealing CPU bottlenecks, missing overlap, and idle gaps before any kernel tuning',
        'What is the bank-conflict rate?',
        'What is the roofline position?',
      ],
      a: 1,
      x: [
        'How many SMs are there?',
        'What is the warp size?',
        'What precision is used?',
      ],
      e: 'Before optimizing kernels, confirm the GPU is the bottleneck. Nsight Systems shows utilization and gaps; often the biggest wins are fixing CPU/data-loading stalls or enabling overlap, not micro-optimizing an already-fast kernel.',
      ref: 'Nsight Systems; profiling methodology',
      tags: ['nsight-systems', 'methodology', 'utilization'],
    },
    {
      d: 4,
      q: 'sm__warps_active.avg.pct_of_peak_sustained_active corresponds to…',
      o: [
        'Theoretical occupancy',
        'Achieved occupancy — the average active warps as a percentage of the maximum, measured during execution',
        'The register count',
        'DRAM throughput',
      ],
      a: 1,
      x: [
        'Branch efficiency',
        'IPC',
        'The clock rate',
      ],
      e: 'This metric is the achieved occupancy. Comparing it to the theoretical limit (from resource usage) reveals scheduling losses (tail effect, imbalance). It is one input to latency-hiding analysis, not a performance guarantee.',
      ref: 'Nsight Compute (occupancy metrics)',
      tags: ['achieved-occupancy', 'metrics', 'nsight-compute'],
    },
    {
      d: 5,
      q: 'You suspect a kernel is bound by instruction issue (not memory or math throughput). The metric to inspect is…',
      o: [
        'DRAM throughput',
        'Issue slot utilization / eligible-warps-per-scheduler — if few warps are eligible to issue each cycle, the scheduler is starved',
        'L2 hit rate',
        'Register count',
      ],
      a: 1,
      x: [
        'Shared-memory usage',
        'Block size',
        'Grid dimensions',
      ],
      e: 'Issue-bound kernels show low issued-instructions-per-cycle and few eligible warps per scheduler despite resources. The Scheduler Statistics section reveals whether warps are simply not ready (stalls) or whether issue slots are otherwise wasted.',
      ref: 'Nsight Compute (Scheduler Statistics)',
      tags: ['issue-bound', 'scheduler', 'metrics'],
    },
    {
      d: 3,
      q: 'Exporting an nsys report to SQLite (nsys export) is valuable because…',
      o: [
        'It speeds up the app',
        'You can run custom SQL queries / scripts over the raw trace data for automated, reproducible analysis and dashboards',
        'It reduces overhead',
        'It compiles the kernels',
      ],
      a: 1,
      x: [
        'It locks the clocks',
        'It disables tracing',
        'It profiles the CPU only',
      ],
      e: 'The SQLite export exposes the trace as queryable tables, enabling bespoke analyses (e.g. total NCCL time per rank, kernel time by name) and integration into automated pipelines beyond the GUI/stats views.',
      ref: 'Nsight Systems (SQLite export)',
      tags: ['nsight-systems', 'sqlite', 'automation'],
    },
    {
      d: 4,
      q: 'Why is profiling overhead a concern, and how does Nsight mitigate measurement distortion of timing?',
      o: [
        'It is not a concern',
        'Instrumentation/replay perturb timing; Nsight derives metrics from controlled counter collection and reports them separately from a clean duration, rather than trusting wall-time during heavy collection',
        'It overclocks the GPU',
        'It runs on the CPU',
      ],
      a: 1,
      x: [
        'It compresses results',
        'It disables the kernel',
        'It uses the host timer',
      ],
      e: 'Heavy counter capture/replay slows execution, so raw wall-clock during collection isn’t representative. Nsight gathers hardware counters under controlled conditions and reports durations/metrics derived from them; use a light pass or CUDA events for clean timing.',
      ref: 'Nsight Compute (overhead); CUPTI',
      tags: ['profiling-overhead', 'measurement', 'methodology'],
    },
  ],
  { idPrefix: 'profiling-c' }
)
