import { defineQuestions } from './_helpers.js'

// Kernel Optimization — tiling, register blocking, ILP, unrolling,
// vectorization, arithmetic intensity, fusion.
export default defineQuestions('optimization', [
  {
    d: 2,
    q: 'In tiled matrix multiplication, what is the primary purpose of loading tiles into shared memory?',
    o: [
      'To reduce register usage',
      'Reuse each element, cutting global traffic',
      'To avoid using __syncthreads',
      'To increase the block size',
    ],
    a: 1,
    e: 'Without tiling, each output element re-reads full rows/columns from global memory. Staging a tile in shared memory lets all threads in the block reuse it many times, raising arithmetic intensity and turning a memory-bound kernel toward compute-bound.',
    ref: 'PMPP Ch. 5; CUDA C++ Programming Guide §5.3.2',
    tags: ['tiling', 'gemm', 'shared-memory', 'reuse'],
  },
  {
    d: 3,
    q: 'For a tiled GEMM with tile size T, by what factor does shared-memory tiling reduce global memory accesses (ideally)?',
    o: ['No reduction', 'By a factor of T', 'By a factor of T²', 'By a factor of 2'],
    a: 1,
    e: 'Each element loaded into a T×T tile is reused T times (once per output along the tile dimension), so global traffic drops by roughly a factor of T. Larger tiles reuse more but cost more shared memory and registers.',
    ref: 'PMPP Ch. 5',
    tags: ['tiling', 'gemm', 'reuse'],
  },
  {
    d: 2,
    q: 'What is "arithmetic intensity"?',
    o: [
      'The number of registers used per thread',
      'FLOPs per byte of memory traffic',
      'The clock frequency of the GPU',
      'The fraction of threads doing math',
    ],
    a: 1,
    e: 'Arithmetic intensity = FLOPs ÷ bytes moved. It places a kernel on the roofline: low intensity → memory-bound, high intensity → compute-bound. Optimizations like tiling and fusion raise intensity by reusing data.',
    ref: 'Roofline model (Williams et al.); CUDA Best Practices Guide',
    tags: ['arithmetic-intensity', 'roofline'],
  },
  {
    d: 3,
    q: 'Register blocking (having each thread compute multiple output elements) helps GEMM because…',
    o: [
      'It reduces the number of threads needed',
      'More per-thread reuse and ILP',
      'It eliminates shared memory',
      'It avoids divergence',
    ],
    a: 1,
    e: 'When each thread computes, say, an 8×8 micro-tile, operands loaded from shared memory into registers are reused across many multiply-adds, raising the FLOP-to-load ratio and exposing instruction-level parallelism — key to high-performance GEMM.',
    ref: 'CUTLASS design docs; PMPP Ch. 5',
    tags: ['register-blocking', 'gemm', 'ilp'],
  },
  {
    d: 2,
    q: 'Loop unrolling with #pragma unroll primarily helps by…',
    o: [
      'Reducing the binary size',
      'Cuts loop overhead and exposes ILP',
      'Lowering register usage',
      'Forcing coalescing',
    ],
    a: 1,
    e: 'Unrolling eliminates branch/increment overhead and lets independent iterations’ instructions overlap, improving instruction-level parallelism and enabling other optimizations. The trade-off is higher register pressure and code size.',
    ref: 'CUDA Best Practices Guide; CUDA C++ Programming Guide',
    tags: ['unrolling', 'ilp', 'pragma'],
  },
  {
    d: 3,
    q: 'Kernel fusion (combining several elementwise kernels into one) improves performance mainly by…',
    o: [
      'Increasing occupancy',
      'Keeps intermediates off global memory',
      'Reducing register usage',
      'Enabling tensor cores',
    ],
    a: 1,
    e: 'Separate elementwise kernels each read inputs and write outputs to DRAM. Fusing them keeps intermediates in registers/shared memory, eliminating redundant global loads/stores — a big win for memory-bound pipelines (e.g. bias+activation).',
    ref: 'CUDA Best Practices Guide; deep-learning kernel fusion literature',
    tags: ['fusion', 'memory-bound', 'arithmetic-intensity'],
  },
  {
    d: 1,
    q: 'A kernel whose runtime is limited by DRAM bandwidth rather than compute is called…',
    o: ['Compute-bound', 'Memory-bound', 'Latency-bound', 'Occupancy-bound'],
    a: 1,
    e: 'A memory-bound (bandwidth-bound) kernel spends most time moving data; adding compute is "free" until you hit the bandwidth ceiling. The roofline’s sloped region marks memory-bound operation. Most elementwise/reduction ops are memory-bound.',
    ref: 'Roofline model; CUDA Best Practices Guide',
    tags: ['memory-bound', 'roofline'],
  },
  {
    d: 4,
    q: 'On the roofline model, a kernel sits at the "ridge point." This means…',
    o: [
      'It is using zero bandwidth',
      'Intensity where the two roofs meet (balanced)',
      'It has 100% occupancy',
      'It is purely latency-bound',
    ],
    a: 1,
    e: 'The ridge (knee) point is the arithmetic intensity at which peak bandwidth × intensity equals peak compute. Below it kernels are memory-bound; above it, compute-bound. Tuning aims to push intensity past the ridge for compute-heavy work.',
    ref: 'Williams et al., "Roofline"; NVIDIA Nsight Compute roofline',
    tags: ['roofline', 'arithmetic-intensity', 'ridge-point'],
  },
  {
    d: 3,
    q: 'Using __fmaf_rn / fused multiply-add (a*b+c in one instruction) helps because…',
    o: [
      'It uses less memory',
      'One rounding step; faster and more accurate',
      'It avoids tensor cores',
      'It reduces divergence',
    ],
    a: 1,
    e: 'FMA computes a*b+c with a single rounding (higher accuracy) and as one hardware instruction (higher throughput) versus separate multiply+add. Compilers contract a*b+c into FMA by default; it is foundational to GEMM throughput.',
    ref: 'CUDA C++ Programming Guide §G; Best Practices Guide',
    tags: ['fma', 'throughput', 'accuracy'],
  },
  {
    d: 4,
    q: 'Software pipelining / double buffering in a tiled kernel means…',
    o: [
      'Running two kernels at once',
      'Prefetch the next tile while computing the current',
      'Using two GPUs',
      'Doubling the block size',
    ],
    a: 1,
    e: 'Double buffering overlaps memory and compute: while the SM multiplies the current shared-memory tile, the next tile is asynchronously loaded into a separate buffer. This hides global-load latency and is standard in high-performance GEMM (cp.async/TMA).',
    ref: 'CUTLASS pipelining; CUDA C++ Programming Guide (async copy)',
    tags: ['double-buffering', 'pipelining', 'latency-hiding'],
  },
  {
    d: 4,
    q: 'cp.async (asynchronous global-to-shared copy, Ampere+) improves pipelined kernels because it…',
    o: [
      'Compresses the data',
      'Global→shared without registers, overlapping compute',
      'Guarantees coalescing',
      'Eliminates the need for __syncthreads',
    ],
    a: 1,
    e: 'Before Ampere, global→shared went through registers (load then store), blocking the thread. cp.async streams data directly into shared memory asynchronously, freeing registers and enabling overlap of transfer with computation in software pipelines.',
    ref: 'NVIDIA Ampere Whitepaper; CUDA C++ Programming Guide (memcpy_async)',
    tags: ['cp-async', 'ampere', 'pipelining'],
  },
  {
    d: 3,
    q: 'Why is grid-stride looping a recommended kernel pattern?',
    o: [
      'It avoids using blockIdx',
      'One kernel handles any size with a tuned grid',
      'It eliminates bounds checks',
      'It guarantees coalescing',
    ],
    a: 1,
    e: 'A grid-stride loop (for (i = gid; i < n; i += gridDim.x*blockDim.x)) decouples grid size from problem size: you launch enough blocks to fill the GPU and each thread processes multiple elements. It aids occupancy tuning, reuse, and debugging.',
    ref: 'NVIDIA Developer Blog, "CUDA Pro Tip: Grid-Stride Loops"',
    tags: ['grid-stride', 'pattern', 'portability'],
  },
  {
    d: 2,
    q: 'Replacing integer division/modulo by a constant in a hot loop with shifts/masks (for powers of two) helps because…',
    o: [
      'Division by a runtime value is free on GPUs',
      'Div/mod are costly; bitwise ops are cheap',
      'It improves coalescing',
      'It reduces register count',
    ],
    a: 1,
    e: 'GPU integer div/mod are multi-instruction and costly. For power-of-two constants, x / N → x >> log2(N) and x % N → x & (N-1). For general constants, the compiler may use multiply-shift, but avoiding div in hot loops still helps.',
    ref: 'CUDA Best Practices Guide; CUDA C++ Programming Guide §G',
    tags: ['integer-arithmetic', 'micro-optimization'],
  },
  {
    d: 5,
    q: 'In a high-performance GEMM, the dominant inner-loop instruction on tensor-core hardware (Hopper) is typically…',
    o: [
      'FFMA on CUDA cores',
      'wgmma (warpgroup MMA)',
      'atomicAdd',
      '__shfl_sync',
    ],
    a: 1,
    e: 'Hopper GEMM kernels issue wgmma.mma_async instructions where a warpgroup (128 threads) cooperatively feeds the tensor cores from shared memory, overlapped with TMA loads. This replaces the older mma/wmma path and drives peak throughput.',
    ref: 'CUTLASS 3.x; Hopper PTX ISA (wgmma)',
    tags: ['gemm', 'wgmma', 'tensor-core', 'hopper'],
  },
  {
    d: 3,
    q: 'A kernel is "latency-bound" (low occupancy, neither bandwidth nor compute saturated). A good first remedy is to…',
    o: [
      'Reduce the block size to 1',
      'More parallelism/ILP: more work in flight',
      'Add more atomics',
      'Switch to double precision',
    ],
    a: 1,
    e: 'Latency-bound means there isn’t enough work in flight to hide stalls. Increase warps (occupancy) and/or expose more independent instructions per thread (ILP, unrolling, multiple accumulators) so the scheduler always has something ready.',
    ref: 'Volkov, "Better Performance at Lower Occupancy"; Nsight Compute',
    tags: ['latency-bound', 'ilp', 'occupancy'],
  },
  {
    d: 4,
    q: 'Using multiple independent accumulators in a reduction/dot-product inner loop primarily helps by…',
    o: [
      'Reducing memory traffic',
      'Breaks the dependency chain, exposing ILP',
      'Avoiding shared memory',
      'Improving coalescing',
    ],
    a: 1,
    e: 'A single accumulator creates a dependency chain where each FMA waits on the previous, exposing full instruction latency. Several partial accumulators run independent chains in parallel, hiding latency and approaching peak FMA throughput (combined at the end).',
    ref: 'CUDA Best Practices Guide; high-performance GEMM literature',
    tags: ['ilp', 'accumulators', 'dependency-chain'],
  },
  {
    d: 2,
    q: 'The CUDA Occupancy API cudaOccupancyMaxPotentialBlockSize is used to…',
    o: [
      'Measure achieved occupancy at runtime',
      'Suggest a block size maximizing occupancy',
      'Set the grid size automatically',
      'Profile memory bandwidth',
    ],
    a: 1,
    e: 'It returns a block size (and minimum grid size) that maximizes occupancy based on the kernel’s register/shared-memory usage, providing a good default launch configuration without manual tuning of the occupancy calculator.',
    ref: 'CUDA Runtime API; Occupancy section',
    tags: ['occupancy-api', 'block-size', 'tuning'],
  },
  {
    d: 5,
    q: 'In FlashAttention-style kernels, why is the softmax computed in an "online" (streaming) fashion?',
    o: [
      'To use tensor cores for softmax',
      'Avoid materializing the N×N matrix (running max/sum)',
      'To reduce register count',
      'To enable double precision',
    ],
    a: 1,
    e: 'Online softmax rescales the running normalization (max and sum) as each key/value tile is streamed, so the O(N²) score matrix is never written to HBM. This makes attention memory-efficient and bandwidth-friendly — the core idea of FlashAttention.',
    ref: 'Dao et al., "FlashAttention"; online softmax (Milakov & Gimelshein)',
    tags: ['flash-attention', 'softmax', 'tiling', 'memory-efficiency'],
  },
  {
    d: 3,
    q: 'Why might increasing occupancy by shrinking the block’s shared-memory usage actually HURT a tiled GEMM?',
    o: [
      'Higher occupancy is always bad',
      'Smaller tiles lose reuse, raising DRAM traffic',
      'It causes compile errors',
      'It disables tensor cores',
    ],
    a: 1,
    e: 'Shared memory enables reuse; cutting tile size to free shared memory for occupancy lowers the reuse factor and increases DRAM traffic. There is a balance — peak GEMM often runs at moderate occupancy with large tiles and high reuse.',
    ref: 'CUTLASS tuning; CUDA Best Practices Guide',
    tags: ['occupancy', 'tiling', 'tradeoff'],
  },
  {
    d: 4,
    q: 'Bank-conflict-free shared-memory access for tensor-core operands often requires "swizzling." Swizzling means…',
    o: [
      'Randomizing the data',
      'Permuting the layout to hit distinct banks',
      'Compressing the tile',
      'Transposing in global memory',
    ],
    a: 1,
    e: 'Tensor-core load patterns (ldmatrix/wgmma) would conflict on a naive row-major shared layout. Swizzling applies an XOR-based permutation to addresses so each lane hits a different bank, eliminating conflicts. CuTe/CUTLASS encode this in layout types.',
    ref: 'CUTLASS/CuTe documentation; CUDA C++ Programming Guide',
    tags: ['swizzling', 'bank-conflict', 'tensor-core', 'cutlass'],
  },
  {
    d: 3,
    q: 'Using rsqrtf(x) instead of 1.0f/sqrtf(x) in a hot kernel is an example of…',
    o: [
      'Reducing memory traffic',
      'A faster hardware intrinsic for the op',
      'Avoiding divergence',
      'Improving coalescing',
    ],
    a: 1,
    e: 'rsqrtf maps to a fast hardware reciprocal-sqrt (single instruction via the SFU) instead of a sqrt followed by a divide. Similar fast intrinsics (__expf, __sinf, --use_fast_math) trade a little accuracy for throughput in tolerant kernels.',
    ref: 'CUDA C++ Programming Guide §G; Best Practices Guide',
    tags: ['intrinsics', 'fast-math', 'sfu'],
  },
])
