import { defineQuestions } from './_helpers.js'

// Numerics & Mixed Precision — batch C. ULP/error, FP8 ranges, stable algorithms
// (Welford/softplus), LLM quantization (SmoothQuant/AWQ/GPTQ). Distinct from precision.js/.b.js.
export default defineQuestions(
  'precision',
  [
    {
      d: 3,
      q: 'ULP (unit in the last place) refers to…',
      o: [
        'The largest representable value',
        'The spacing between two consecutive representable floating-point numbers at a given magnitude — error is often measured in ULPs',
        'The exponent bias',
        'The number of mantissa bits',
      ],
      a: 1,
      x: [
        'The rounding mode',
        'The sign bit',
        'A denormal value',
      ],
      e: 'ULP is the gap between adjacent floats near a value; it grows with magnitude. Function accuracy is specified in ULPs (e.g. an intrinsic accurate to 2 ULP), and "swamping" happens when an addend is smaller than the sum’s ULP.',
      ref: 'IEEE 754; numerical analysis',
      tags: ['ulp', 'precision', 'error'],
    },
    {
      d: 4,
      q: 'The maximum representable value of FP8 E4M3 is about…',
      o: ['57344', '448', '65504', '6.0'],
      a: 1,
      x: ['255', '127', '1000'],
      e: 'E4M3 (4 exp, 3 mantissa) maxes near 448 — small range, so per-tensor/block scaling is essential. E5M2 (more exponent) reaches ~57344 (wider range, used for gradients). 65504 is FP16’s max.',
      ref: 'NVIDIA FP8 formats; "FP8 Formats for Deep Learning"',
      tags: ['fp8', 'e4m3', 'range'],
    },
    {
      d: 4,
      q: 'Welford’s online algorithm is preferred for computing variance (e.g. in LayerNorm/BatchNorm) because it…',
      o: [
        'Is faster',
        'Computes mean and variance in one numerically stable pass, avoiding the catastrophic cancellation of the naive E[x²]−E[x]² formula',
        'Uses integer math',
        'Requires FP64',
      ],
      a: 1,
      x: [
        'Avoids the mean',
        'Needs two passes',
        'Uses tensor cores',
      ],
      e: 'The naive sum-of-squares-minus-square-of-sum subtracts two large nearly-equal numbers, losing precision. Welford updates running mean and M2 incrementally, staying stable — important for normalization layers in low precision.',
      ref: "Welford's algorithm; numerically stable variance",
      tags: ['welford', 'variance', 'stability', 'layernorm'],
    },
    {
      d: 4,
      q: 'The "outlier" problem in LLM activation quantization is that…',
      o: [
        'Weights are too small',
        'A few activation channels have very large magnitudes; a single per-tensor scale then wastes precision on the many normal values, hurting INT8 accuracy',
        'Activations are integers',
        'There are no outliers',
      ],
      a: 1,
      x: [
        'Gradients overflow',
        'The softmax is unstable',
        'The vocabulary is large',
      ],
      e: 'Large-magnitude outlier channels force a big scale that crushes the resolution of normal values. Methods like SmoothQuant and per-channel/group quantization address this so INT8/FP8 activation quantization keeps accuracy.',
      ref: 'LLM.int8(); SmoothQuant',
      tags: ['outliers', 'activation-quantization', 'llm'],
    },
    {
      d: 5,
      q: 'SmoothQuant improves activation quantization by…',
      o: [
        'Removing outliers',
        'Migrating quantization difficulty from activations to weights via a per-channel scaling (X·diag(s)⁻¹ and W·diag(s)), so both become easier to quantize',
        'Using FP64',
        'Skipping quantization',
      ],
      a: 1,
      x: [
        'Pruning the model',
        'Increasing the bit-width',
        'Disabling tensor cores',
      ],
      e: 'SmoothQuant rescales channels to shift the dynamic-range burden from hard-to-quantize activations onto weights (which are easier), keeping the product XW unchanged. This lets both be quantized to INT8 with little accuracy loss.',
      ref: 'Xiao et al., "SmoothQuant"',
      tags: ['smoothquant', 'quantization', 'activations'],
    },
    {
      d: 5,
      q: 'AWQ (Activation-aware Weight Quantization) decides which weights to protect by…',
      o: [
        'Random selection',
        'Using activation magnitudes to identify salient weight channels (those multiplying large activations) and scaling them to preserve their precision during low-bit weight quantization',
        'Quantizing activations',
        'Pruning experts',
      ],
      a: 1,
      x: [
        'Sorting by weight magnitude only',
        'Using gradients',
        'Increasing the bit-width uniformly',
      ],
      e: 'AWQ observes that weights interacting with large activations matter most; it scales those salient channels to reduce their quantization error, achieving good 4-bit weight-only quantization for LLM inference without retraining.',
      ref: 'Lin et al., "AWQ"',
      tags: ['awq', 'weight-quantization', 'llm'],
    },
    {
      d: 4,
      q: 'GPTQ quantizes weights to low bits (e.g. INT4) by…',
      o: [
        'Simple rounding',
        'Layer-wise, using second-order (Hessian) information to quantize columns one at a time while compensating remaining weights to minimize output error',
        'Retraining from scratch',
        'Pruning 50% of weights',
      ],
      a: 1,
      x: [
        'Quantizing activations',
        'Using FP8',
        'Random projection',
      ],
      e: 'GPTQ uses approximate second-order info to quantize weights greedily while adjusting the not-yet-quantized weights to compensate, yielding accurate 3–4 bit weight-only quantization for LLMs with only a calibration set.',
      ref: 'Frantar et al., "GPTQ"',
      tags: ['gptq', 'weight-quantization', 'second-order'],
    },
    {
      d: 3,
      q: 'For computing log(1+x) when x is tiny, log1p(x) is preferred over log(1+x) because…',
      o: [
        'It is faster',
        '1+x loses precision (x is swamped) before the log; log1p computes it accurately for small x',
        'It avoids the log',
        'It uses integers',
      ],
      a: 1,
      x: [
        'It is required by IEEE',
        'It handles negatives only',
        'It uses tensor cores',
      ],
      e: 'When x ≪ 1, the addition 1+x rounds away x’s low bits, so log(1+x) is inaccurate. log1p is designed to be accurate for small arguments. Similarly expm1(x) computes eˣ−1 accurately near zero.',
      ref: 'Numerical computing; log1p/expm1',
      tags: ['log1p', 'expm1', 'stability'],
    },
    {
      d: 4,
      q: 'A numerically stable softplus, log(1+eˣ), is computed for large x as…',
      o: [
        'log(1+exp(x)) directly',
        'max(x,0) + log1p(exp(-|x|)), which avoids overflow of exp(x) for large positive x',
        'exp(x)',
        'x²',
      ],
      a: 1,
      x: [
        'x - log(x)',
        '1/(1+exp(-x))',
        'log(x)',
      ],
      e: 'Directly exponentiating large x overflows. The stable form factors out max(x,0) and uses log1p(exp(-|x|)), keeping the exponent argument ≤ 0. The same max-subtraction idea stabilizes softmax and log-sum-exp.',
      ref: 'Numerically stable softplus',
      tags: ['softplus', 'stability', 'overflow'],
    },
    {
      d: 3,
      q: 'A numerically stable sigmoid for large negative x uses…',
      o: [
        '1/(1+exp(-x)) always',
        'exp(x)/(1+exp(x)) for x<0 (and 1/(1+exp(-x)) for x≥0) so the exponent argument stays ≤ 0, avoiding overflow',
        'tanh(x)',
        'x/(1+|x|)',
      ],
      a: 1,
      x: [
        'log(1+exp(x))',
        '0.5*(1+x)',
        'max(x,0)',
      ],
      e: 'For very negative x, exp(-x) overflows in the naive sigmoid. Branching on the sign so exp always sees a non-positive argument keeps it bounded. Libraries implement sigmoid this way for stability.',
      ref: 'Numerically stable sigmoid',
      tags: ['sigmoid', 'stability', 'overflow'],
    },
    {
      d: 4,
      q: 'Group-wise (block) weight quantization (e.g. GPTQ/AWQ group_size=128) means…',
      o: [
        'One scale for the whole tensor',
        'A separate scale (and zero-point) per contiguous group of 128 weights, capturing local range better than per-tensor at a small metadata cost',
        'No scaling',
        'Per-element scales',
      ],
      a: 1,
      x: [
        'Per-layer scales',
        'Per-batch scales',
        'Random grouping',
      ],
      e: 'Smaller groups adapt the scale to local weight magnitude, improving low-bit accuracy versus a single per-tensor/per-channel scale, with modest overhead (one scale per 128 weights). It’s the same idea as microscaling formats.',
      ref: 'GPTQ/AWQ group quantization; MX formats',
      tags: ['group-quantization', 'block-scaling', 'weight-quantization'],
    },
    {
      d: 4,
      q: 'QLoRA’s "double quantization" further compresses by…',
      o: [
        'Quantizing the activations twice',
        'Quantizing the quantization constants (the per-block scales) themselves, saving additional memory on top of NF4 weights',
        'Using FP64 scales',
        'Pruning the weights',
      ],
      a: 1,
      x: [
        'Doubling the bit-width',
        'Re-training the model',
        'Removing the scales',
      ],
      e: 'NF4 weights need per-block scales; double quantization quantizes those scale constants too (e.g. to 8-bit) to shave more memory, helping fit large models for fine-tuning on a single GPU (QLoRA).',
      ref: 'Dettmers et al., "QLoRA" (double quantization)',
      tags: ['qlora', 'double-quantization', 'nf4'],
    },
    {
      d: 3,
      q: 'Per-token (dynamic) activation quantization computes the scale…',
      o: [
        'Once for the whole model',
        'Per token at runtime from that token’s activation statistics, adapting to each token’s range (vs a fixed per-tensor scale)',
        'From the weights',
        'Never',
      ],
      a: 1,
      x: [
        'Per layer only',
        'Per batch only',
        'From gradients',
      ],
      e: 'Dynamic per-token quantization derives each token’s scale from its own activations, handling token-to-token range variation better than a static scale — common in LLM inference (often paired with per-channel weight quantization).',
      ref: 'LLM quantization; per-token activation scaling',
      tags: ['per-token', 'activation-quantization', 'dynamic'],
    },
    {
      d: 4,
      q: 'Error in a sum of n floating-point numbers grows, in the worst case, proportionally to…',
      o: [
        'log(n)',
        'n (roughly n·ε for sequential summation), while pairwise/tree summation reduces it to ~log(n)·ε',
        '1/n',
        'It does not grow',
      ],
      a: 1,
      x: [
        'n²',
        '√n always exactly',
        'constant',
      ],
      e: 'Naive sequential summation accumulates O(n)·ε error; pairwise/tree reduction lowers it to O(log n)·ε, and compensated (Kahan) summation makes it nearly independent of n. GPUs’ tree reductions naturally have better error behavior than a serial loop.',
      ref: 'Higham, "Accuracy and Stability"; summation error',
      tags: ['summation', 'error-growth', 'reduction'],
    },
    {
      d: 3,
      q: 'Comparing two floats for equality with == is fragile; a better approach is…',
      o: [
        'Always use ==',
        'Compare within a tolerance (|a−b| ≤ ε scaled to magnitude, i.e. relative/ULP comparison), since rounding makes exact equality unreliable',
        'Cast to int',
        'Use only FP64',
      ],
      a: 1,
      x: [
        'Compare the bit patterns directly',
        'Use abs() only',
        'Round to integers',
      ],
      e: 'Floating-point rounding means mathematically-equal computations may differ by a few ULPs. Use a relative/absolute tolerance (or ULP distance) appropriate to the magnitudes, not exact ==, when checking results.',
      ref: 'Floating-point comparison best practices',
      tags: ['float-comparison', 'tolerance', 'ulp'],
    },
    {
      d: 5,
      q: '"Double rounding" can cause subtle errors when…',
      o: [
        'Rounding once',
        'A value is rounded to an intermediate precision and then to a final precision, occasionally giving a different result than rounding directly to the final precision',
        'Using integers',
        'Using FP64 only',
      ],
      a: 1,
      x: [
        'Truncating',
        'Using stochastic rounding',
        'Flushing denormals',
      ],
      e: 'Rounding through an intermediate format (e.g. FP32→FP80→FP32, or accumulate-then-cast paths) can double-round and differ from a single correctly-rounded result. It’s a concern when reasoning about exact bit-level reproducibility across precisions/hardware.',
      ref: 'IEEE 754; double rounding',
      tags: ['double-rounding', 'reproducibility', 'precision'],
    },
    {
      d: 4,
      q: 'Why can the SAME model give slightly different outputs on two different GPU architectures?',
      o: [
        'The weights change',
        'Different kernels/reduction orders, tensor-core vs CUDA-core paths, and TF32/precision defaults change FP rounding, so results differ at the ULP level despite identical math',
        'Different ISAs corrupt data',
        'It should be bit-identical always',
      ],
      a: 1,
      x: [
        'The model is non-deterministic by design',
        'Memory is incoherent',
        'Random seeds differ',
      ],
      e: 'Cross-hardware bitwise reproducibility isn’t guaranteed: kernels pick different algorithms/reduction orders, may use TF32/tensor cores differently, and FP non-associativity makes the rounding differ. Outputs match to tolerance, not exactly.',
      ref: 'Floating-point reproducibility across hardware',
      tags: ['reproducibility', 'cross-hardware', 'floating-point'],
    },
    {
      d: 3,
      q: 'Relative error vs absolute error: relative error is the right measure when…',
      o: [
        'Values are near zero',
        'Values span many magnitudes — relative error (|approx−exact|/|exact|) is scale-invariant, unlike absolute error which is dominated by large values',
        'Using integers',
        'Always; absolute is never useful',
      ],
      a: 1,
      x: [
        'Comparing bit patterns',
        'Only for FP64',
        'Never',
      ],
      e: 'Relative error normalizes by magnitude, making it meaningful across scales (a 0.01 error is huge for 0.001 but tiny for 1e6). Absolute error is appropriate near zero or for fixed-scale quantities. Choose based on the data’s dynamic range.',
      ref: 'Numerical analysis; error metrics',
      tags: ['relative-error', 'absolute-error', 'metrics'],
    },
    {
      d: 4,
      q: 'TF32’s internal representation uses how many total bits for the multiply inputs (sign+exponent+mantissa)?',
      o: [
        '32 bits',
        '19 bits (1 sign + 8 exponent + 10 mantissa)',
        '16 bits',
        '8 bits',
      ],
      a: 1,
      x: ['11 bits', '24 bits', '13 bits'],
      e: 'TF32 keeps FP32’s 8-bit exponent but truncates the mantissa to 10 bits (1+8+10 = 19 bits of input precision), accumulating in FP32. It is a tensor-core input mode, not a storage format — data is stored as FP32.',
      ref: 'NVIDIA Ampere TF32',
      tags: ['tf32', 'bit-layout', 'tensor-cores'],
    },
    {
      d: 5,
      q: 'Hardware stochastic rounding support (e.g. in some BF16/FP8 paths) is valuable for…',
      o: [
        'Faster matmul',
        'Accumulating many small updates in low precision without bias, since rounding direction is randomized proportional to the residual — preserving small increments in expectation',
        'Avoiding tensor cores',
        'Reducing memory',
      ],
      a: 1,
      x: [
        'Increasing the exponent range',
        'Disabling denormals',
        'Sorting',
      ],
      e: 'Hardware stochastic rounding lets low-precision accumulation (e.g. BF16 weight updates) avoid the systematic bias of round-to-nearest that drops tiny updates, sometimes removing the need for an FP32 master copy in training.',
      ref: 'Stochastic rounding hardware; low-precision training',
      tags: ['stochastic-rounding', 'hardware', 'accumulation'],
    },
    {
      d: 3,
      q: 'FP8 E5M2 is typically used for GRADIENTS rather than weights/activations because…',
      o: [
        'It has more mantissa bits',
        'Its wider exponent range (5 bits) better covers the large dynamic range of gradients, at the cost of precision (2 mantissa bits)',
        'Gradients are integers',
        'It is faster',
      ],
      a: 1,
      x: [
        'It has no exponent',
        'Weights need more range',
        'It avoids scaling',
      ],
      e: 'Gradients span a wide magnitude range, so E5M2 (range-favoring) suits the backward pass, while E4M3 (precision-favoring) suits weights/activations in the forward pass — the standard FP8 mixed-format recipe.',
      ref: 'NVIDIA FP8 training; E4M3/E5M2 usage',
      tags: ['fp8', 'e5m2', 'gradients'],
    },
    {
      d: 4,
      q: 'Why does INT8 quantization usually keep a higher-precision (FP32/FP16) accumulator for the matmul?',
      o: [
        'INT8 cannot be multiplied',
        'Summing many INT8 products would overflow INT8/INT16 and lose information; accumulating in INT32/FP32 preserves the result before requantizing',
        'For speed',
        'To use tensor cores',
      ],
      a: 1,
      x: [
        'To store the weights',
        'To avoid scaling',
        'For denormals',
      ],
      e: 'INT8×INT8 products and their sums grow quickly; INT8 tensor cores accumulate in INT32 (then dequantize/scale). Accumulating in 8-bit would overflow, so a wider accumulator is essential — analogous to FP32 accumulation for FP16 matmul.',
      ref: 'INT8 GEMM; tensor-core accumulation',
      tags: ['int8', 'accumulation', 'overflow'],
    },
    {
      d: 3,
      q: 'Uniform quantization with scale s introduces a worst-case rounding error of about…',
      o: [
        's',
        's/2 (half a quantization step) per element, the fundamental quantization error',
        '2s',
        '0',
      ],
      a: 1,
      x: ['s²', 's/4', '1/s'],
      e: 'Rounding to the nearest level introduces at most ±s/2 error per value. Smaller scale (finer steps, more bits, or smaller groups) reduces this; the trade-off is bits/memory. This bounds quantization’s impact per element.',
      ref: 'Quantization theory',
      tags: ['quantization-error', 'scale', 'rounding'],
    },
    {
      d: 4,
      q: 'When training in BF16, the OPTIMIZER states (Adam m, v) and master weights are usually kept in…',
      o: [
        'BF16',
        'FP32, to accurately accumulate small updates and second moments that BF16’s 8-bit mantissa would lose',
        'FP8',
        'INT8',
      ],
      a: 1,
      x: [
        'FP16',
        'BF16 with stochastic rounding only',
        'INT32',
      ],
      e: 'Even with BF16 compute, optimizer states and master weights are typically FP32 to preserve precision of small increments and variance estimates. ZeRO/FSDP then shard these FP32 states to control memory.',
      ref: 'Mixed-precision Adam; BF16 training',
      tags: ['adam', 'fp32', 'optimizer-states', 'bf16'],
    },
    {
      d: 5,
      q: 'Why does accumulating attention scores/softmax in FP32 matter even on FP16/BF16 tensor-core hardware?',
      o: [
        'It is required by the API',
        'The softmax sum and the score·value accumulation involve many terms; FP32 accumulation preserves accuracy and dynamic range that 16-bit accumulation would degrade (especially for long sequences)',
        'FP16 has no exp',
        'It is faster',
      ],
      a: 1,
      x: [
        'To use more memory',
        'To enable stochastic rounding',
        'It does not matter',
      ],
      e: 'Attention sums over the sequence (scores, softmax denominator, weighted values). Long sequences accumulate error; FP32 accumulation (as in FlashAttention) keeps it stable while inputs/outputs can be 16-bit — preserving accuracy.',
      ref: 'FlashAttention numerics; mixed precision',
      tags: ['attention', 'accumulation', 'fp32', 'stability'],
    },
    {
      d: 3,
      q: 'The bias (offset) in the exponent field of IEEE floats exists so that…',
      o: [
        'It speeds up math',
        'Exponents can represent both negative and positive powers using an unsigned stored value (e.g. bias 127 for FP32), simplifying comparisons',
        'It stores the sign',
        'It enables denormals only',
      ],
      a: 1,
      x: [
        'It rounds values',
        'It compresses the mantissa',
        'It is unused',
      ],
      e: 'The stored exponent is biased (FP32 bias 127, FP16 bias 15) so the actual exponent = stored − bias, letting an unsigned field cover negative and positive exponents and making magnitude comparison of same-sign floats reduce to integer comparison.',
      ref: 'IEEE 754; exponent bias',
      tags: ['ieee-754', 'exponent-bias', 'representation'],
    },
    {
      d: 4,
      q: 'Why is mixed-precision INFERENCE generally easier than mixed-precision TRAINING?',
      o: [
        'Inference uses FP64',
        'Inference has no gradients/optimizer updates to preserve (no small-update accumulation, no loss scaling), so lower precision rarely breaks the forward pass that training’s backward would',
        'Training has no matmuls',
        'They are equally hard',
      ],
      a: 1,
      x: [
        'Inference uses more memory',
        'Training needs no precision',
        'Inference is sequential',
      ],
      e: 'Training must preserve tiny gradient updates and avoid overflow in the backward pass (hence loss scaling, FP32 masters). Inference only runs the forward pass once, so it tolerates aggressive quantization (INT8/FP8/INT4) far more easily.',
      ref: 'Mixed-precision inference vs training',
      tags: ['inference', 'training', 'mixed-precision'],
    },
    {
      d: 5,
      q: 'NVFP4 / MXFP4 on Blackwell make 4-bit usable by combining…',
      o: [
        'FP4 with no scaling',
        'An FP4 (E2M1) element format WITH a shared micro-scale per small block (e.g. 16–32 elements), so local dynamic range is preserved despite only ~16 representable values per element',
        'INT4 with per-tensor scale',
        'FP8 with FP4 fallback',
      ],
      a: 1,
      x: [
        'FP4 with FP32 accumulate only',
        'Two FP8 values',
        'A lookup table',
      ],
      e: 'Raw FP4 has too few values/range to be useful alone. Pairing it with a per-block (micro) scale (MX/NVFP4) adapts to local magnitude, making 4-bit accurate enough for inference — Blackwell’s tensor cores support these formats natively.',
      ref: 'OCP MX; NVIDIA Blackwell NVFP4',
      tags: ['nvfp4', 'mxfp4', 'microscaling', 'blackwell'],
    },
  ],
  { idPrefix: 'precision-c' }
)
