import { defineQuestions } from './_helpers.js'

// Streams, Events & Graphs — concurrency, overlap, default stream semantics,
// events, CUDA Graphs.
export default defineQuestions('streams', [
  {
    d: 2,
    q: 'What is a CUDA stream?',
    o: [
      'A type of GPU memory',
      'Ordered queue; other streams may overlap',
      'A network connection',
      'A thread on the host',
    ],
    a: 1,
    e: 'A stream is a sequence of GPU operations that execute in order relative to each other. Independent streams can overlap (kernels with copies, or kernels with kernels) if resources allow, which is how you achieve concurrency on the GPU.',
    ref: 'CUDA C++ Programming Guide §6.2.8',
    tags: ['streams', 'concurrency'],
  },
  {
    d: 2,
    q: 'To overlap a host-to-device copy with a kernel, you need…',
    o: [
      'Two GPUs',
      'Pinned memory + separate streams',
      'A larger block size',
      'Unified memory only',
    ],
    a: 1,
    e: 'Overlap requires cudaMemcpyAsync from page-locked memory on one stream and the kernel on another (or the same, but different engines). The copy engine and SMs then run concurrently. Pageable memory or the serializing default stream prevents overlap.',
    ref: 'CUDA Best Practices Guide §9.1',
    tags: ['streams', 'overlap', 'pinned-memory'],
  },
  {
    d: 3,
    q: 'What is special about the legacy default stream (stream 0)?',
    o: [
      'It runs on the CPU',
      'Implicitly syncs with all other blocking streams',
      'It has the highest priority',
      'It cannot run kernels',
    ],
    a: 1,
    e: 'The legacy default stream is implicitly synchronizing: an operation in it won’t begin until all prior work in blocking streams completes, and vice versa. This serializes concurrency. Per-thread default streams or explicit non-blocking streams avoid it.',
    ref: 'CUDA C++ Programming Guide §6.2.8.5',
    tags: ['default-stream', 'serialization'],
  },
  {
    d: 3,
    q: 'CUDA events (cudaEvent_t) are used to…',
    o: [
      'Allocate memory',
      'Record timestamps; order streams via wait',
      'Launch kernels',
      'Define communicators',
    ],
    a: 1,
    e: 'Events mark points in a stream. You can time between two events (cudaEventElapsedTime) and synchronize streams: cudaStreamWaitEvent makes a stream wait until another stream reaches a recorded event — building cross-stream dependencies.',
    ref: 'CUDA C++ Programming Guide §6.2.8.7',
    tags: ['events', 'timing', 'dependencies'],
  },
  {
    d: 4,
    q: 'CUDA Graphs improve performance primarily by…',
    o: [
      'Making kernels run faster internally',
      'Capture once and replay — cuts per-launch CPU overhead',
      'Compressing memory',
      'Increasing occupancy',
    ],
    a: 1,
    e: 'A CUDA Graph records a DAG of operations (kernels, copies, dependencies) so it can be launched as a single unit, amortizing per-launch CPU overhead and enabling driver-side optimization. The win is largest for workloads of many short kernels (e.g. inference).',
    ref: 'CUDA C++ Programming Guide §6.2.9; "Getting Started with CUDA Graphs"',
    tags: ['cuda-graphs', 'launch-overhead'],
  },
  {
    d: 4,
    q: 'A common way to build a CUDA Graph from existing code is…',
    o: [
      'Rewriting every kernel',
      'cudaStreamBeginCapture / EndCapture wraps ops into a graph',
      'Using cuBLAS',
      'Calling cudaDeviceSynchronize repeatedly',
    ],
    a: 1,
    e: 'Stream capture records the work issued to a stream (and its dependencies) into a cudaGraph_t without manual node construction. You then instantiate it (cudaGraphInstantiate) into an executable graph and relaunch it cheaply many times.',
    ref: 'CUDA C++ Programming Guide §6.2.9 (graph capture)',
    tags: ['cuda-graphs', 'stream-capture'],
  },
  {
    d: 3,
    q: 'When are CUDA Graphs LEAST beneficial?',
    o: [
      'Many tiny kernels launched in a tight loop',
      'One big, long-running kernel (launch overhead negligible)',
      'Inference with fixed shapes',
      'Repeated identical iterations',
    ],
    a: 1,
    e: 'Graphs amortize launch overhead, so they help when overhead is significant (many small kernels). For one big kernel, launch cost is tiny relative to execution, and the graph capture/replay machinery adds little benefit.',
    ref: 'CUDA Graphs documentation; Best Practices',
    tags: ['cuda-graphs', 'applicability'],
  },
  {
    d: 3,
    q: 'cudaStreamSynchronize(stream) vs. cudaDeviceSynchronize(): the difference is…',
    o: [
      'They are identical',
      'StreamSync: one stream only; DeviceSync: all streams/work',
      'DeviceSynchronize is faster',
      'StreamSynchronize runs on the GPU',
    ],
    a: 1,
    e: 'cudaStreamSynchronize waits for completion of just the specified stream, allowing other streams to keep running and the host to proceed sooner. cudaDeviceSynchronize is a coarser barrier waiting on the entire device.',
    ref: 'CUDA Runtime API',
    tags: ['synchronization', 'streams'],
  },
  {
    d: 4,
    q: 'Why might using many streams NOT yield more concurrency on the GPU?',
    o: [
      'Streams always serialize',
      'Full SM saturation leaves no room for concurrent overlap',
      'Streams require separate GPUs',
      'The driver ignores extra streams',
    ],
    a: 1,
    e: 'Concurrency needs idle resources. A kernel that already fills the SMs leaves nothing for concurrent kernels, so extra streams won’t overlap compute (though copy/compute overlap on separate engines can still help). Concurrency benefits small or unbalanced kernels most.',
    ref: 'CUDA C++ Programming Guide; Best Practices Guide',
    tags: ['streams', 'concurrency', 'occupancy'],
  },
])
