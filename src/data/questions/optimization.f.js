import { defineQuestions } from './_helpers.js'

// Kernel Optimization — batch F. Reuse/roofline/fusion calculations, GEMM tiling
// scenarios, scan/attention cases. Distinct from optimization.js/.b/.c/.d/.e.
export default defineQuestions(
  'optimization',
  [
    {
      d: 4,
      q: 'In a tiled GEMM with a 32×32 shared tile, each loaded element is reused approximately…',
      o: [
        'Once',
        '32 times (loads drop ~32×)',
        '1024 times',
        'Twice',
      ],
      a: 1,
      x: ['16 times', '64 times', 'No reuse'],
      e: 'A T×T tile reuses each loaded operand T times (once per output along that dimension), cutting global traffic by ~T (here 32). Larger tiles reuse more but cost more shared memory/registers — the reuse-vs-occupancy trade-off.',
      ref: 'PMPP Ch. 5; tiling reuse',
      tags: ['tiling', 'reuse', 'gemm'],
    },
    {
      d: 4,
      q: 'A GPU has 60 TFLOP/s FP32 and 3 TB/s bandwidth. The roofline ridge point (FLOP/byte) is…',
      o: ['2', '20', '60', '0.05'],
      a: 1,
      x: ['10', '5', '180'],
      e: 'Ridge = peak compute / peak bandwidth = 60e12 / 3e12 = 20 FLOP/byte. Kernels below 20 are memory-bound; above are compute-bound. SAXPY (~0.17) is far below — bandwidth-bound; a well-tiled GEMM exceeds the ridge.',
      ref: 'Roofline; ridge point calculation',
      tags: ['roofline', 'ridge-point', 'calculation'],
    },
    {
      d: 4,
      q: 'Fusing a bias+activation into a GEMM whose output is 4096×4096 FP16 saves about…',
      o: [
        'Nothing',
        '~64 MB of HBM round-trip',
        'A reduction',
        'Compute only',
      ],
      a: 1,
      x: [
        '4 KB',
        'The matmul',
        'A barrier',
      ],
      e: '4096×4096×2 bytes = 32 MB. A separate bias/activation kernel rereads and rewrites that 32 MB (≈64 MB traffic). Fusing applies them in registers in the epilogue, removing the extra DRAM round-trip — a clear win for this memory-bound step.',
      ref: 'Epilogue fusion; memory traffic',
      tags: ['fusion', 'epilogue', 'calculation'],
    },
    {
      d: 5,
      q: 'Register blocking where each thread computes an 8×8 output micro-tile yields how many FMAs per pair of loaded operands (one column of A, one row of B)?',
      o: [
        '8',
        '64 (16 loads drive 64 FMAs)',
        '16',
        '1',
      ],
      a: 1,
      x: ['32', '128', '256'],
      e: 'An 8×8 accumulator updated by the outer product of an 8-element A column and 8-element B row does 64 FMAs from 16 loaded operands — a 4:1 FMA-to-load ratio in registers. This reuse is central to high-performance GEMM.',
      ref: 'Register blocking; GEMM micro-kernel',
      tags: ['register-blocking', 'gemm', 'reuse'],
    },
    {
      d: 4,
      q: 'A GEMM with M=N=128, K=16384 underutilizes the GPU (few output tiles). The fix is…',
      o: [
        'Larger tiles',
        'Split-K/Stream-K over the long K',
        'Smaller grid',
        'FP64',
      ],
      a: 1,
      x: [
        'More registers',
        'Disable tensor cores',
        'Atomics on inputs',
      ],
      e: 'Small M,N means few output tiles → few blocks → idle SMs. Split-K/Stream-K assigns chunks of the large K to additional blocks (combining partials afterward), restoring parallelism for tall-skinny GEMMs.',
      ref: 'Split-K / Stream-K; tall-skinny GEMM',
      tags: ['split-k', 'stream-k', 'gemm-shape'],
    },
    {
      d: 5,
      q: 'Naive attention writes the N×N score matrix to HBM (O(N²) bytes); FlashAttention’s online softmax reduces this to…',
      o: [
        'O(N²) still',
        'O(N·d) — no full score matrix',
        'O(1)',
        'O(N³)',
      ],
      a: 1,
      x: ['O(N log N)', 'O(d²)', 'O(N²/2)'],
      e: 'By processing K/V tiles and updating running max/sum + output (online softmax), FlashAttention avoids storing the quadratic score matrix in HBM. Memory traffic becomes linear in sequence length (×head dim) — the key to its efficiency on long sequences.',
      ref: 'FlashAttention; HBM traffic',
      tags: ['flash-attention', 'memory-traffic', 'softmax'],
    },
    {
      d: 4,
      q: 'Coalescing a previously strided (stride-8) memory-bound kernel could improve its bandwidth by up to roughly…',
      o: [
        'Nothing',
        '~8× (stride-8 wasted ~7/8 of each line)',
        '2×',
        'It would slow down',
      ],
      a: 1,
      x: ['1.1×', '32×', 'Half'],
      e: 'A stride-8 access fetches full cache lines but uses ~1/8 of each, wasting ~7/8 of the bandwidth. Restructuring to stride-1 (SoA, transpose via shared memory) can recover up to ~8× of the wasted bandwidth for the memory-bound kernel.',
      ref: 'Coalescing; over-fetch',
      tags: ['coalescing', 'bandwidth', 'stride'],
    },
    {
      d: 5,
      q: 'A dot-product inner loop with a single accumulator stalls on FMA latency. Using 4 accumulators helps because…',
      o: [
        'It reduces memory',
        'Breaks the dependency chain with 4 FMA chains',
        'It increases precision',
        'It avoids tensor cores',
      ],
      a: 1,
      x: [
        'It uses less registers',
        'It coalesces',
        'For graphics',
      ],
      e: 'One accumulator serializes (each FMA waits on the previous). Four independent accumulators give the ILP to hide the ~4-cycle FMA latency, approaching peak throughput; combine them after the loop. More accumulators cost registers.',
      ref: 'Multiple accumulators; ILP',
      tags: ['accumulators', 'ilp', 'fma'],
    },
    {
      d: 4,
      q: 'Replacing repeated `x / d` (constant d) in a hot loop with `x * (1/d)` helps because…',
      o: [
        'It is more accurate',
        'Division is slow; precompute the reciprocal',
        'It uses tensor cores',
        'It coalesces',
      ],
      a: 1,
      x: [
        'It increases registers',
        'It avoids divergence',
        'For FP64',
      ],
      e: 'Division is expensive on GPUs. If the divisor is loop-invariant, precompute r = 1/d and multiply — replacing N divides with one divide and N cheap multiplies. Strength reduction; mind precision (--prec-div / fast math).',
      ref: 'Strength reduction; reciprocal',
      tags: ['strength-reduction', 'reciprocal', 'division'],
    },
    {
      d: 4,
      q: 'A 3-pass softmax (max, exp-sum, normalize) fused into one kernel saves…',
      o: [
        'Compute',
        'Extra HBM reads/writes of the row',
        'A reduction',
        'Registers',
      ],
      a: 1,
      x: [
        'Launch latency only',
        'Bank conflicts',
        'For graphics',
      ],
      e: 'Three separate passes each stream the row through HBM. Fusing keeps it in registers/shared across the max, exp-sum, and normalize steps (streaming/online softmax), eliminating the redundant global reads/writes — the usual win for this memory-bound op.',
      ref: 'Fused softmax; memory traffic',
      tags: ['fusion', 'softmax', 'memory-traffic'],
    },
    {
      d: 4,
      q: 'Vectorizing an elementwise kernel from scalar float to float4 reduces the load/store instruction count by about…',
      o: [
        'Nothing',
        '~4× (4 elements per instruction)',
        '2×',
        '32×',
      ],
      a: 1,
      x: ['1.1×', '8×', 'Half'],
      e: 'float4 processes 4 elements per load/store, cutting the instruction count ~4× and issuing wider aligned 16-byte transactions. For a memory-bound elementwise kernel this improves achieved bandwidth (given 16-byte alignment).',
      ref: 'Vectorized access; float4',
      tags: ['vectorization', 'float4', 'calculation'],
    },
    {
      d: 5,
      q: 'Stream-K eliminates the tail (wave-quantization) inefficiency of tile-per-block GEMM by…',
      o: [
        'Larger tiles',
        'A fixed wave splits the total MAC work evenly',
        'One block',
        'Disabling tensor cores',
      ],
      a: 1,
      x: [
        'Smaller grids',
        'More registers',
        'For FP64',
      ],
      e: 'Fixed tile-per-block schedules leave a ragged final wave. Stream-K distributes the flattened MAC work evenly across a full wave of persistent blocks (with partial-tile reductions), keeping all SMs busy and removing the tail across many shapes.',
      ref: 'Stream-K; tail elimination',
      tags: ['stream-k', 'tail-effect', 'gemm'],
    },
    {
      d: 4,
      q: 'A reduction with each thread summing 8 elements (grid-stride) before the tree reduction primarily improves…',
      o: [
        'Precision',
        'Memory throughput (coalesced loads)',
        'Occupancy',
        'Divergence',
      ],
      a: 1,
      x: [
        'Register count',
        'Tensor-core use',
        'For graphics',
      ],
      e: 'Summing several elements per thread (grid-stride) raises the useful read work per launched thread and sustains high read bandwidth, pushing the reduction toward the bandwidth roof. The tree/shuffle combination is comparatively cheap.',
      ref: 'Reduction; multi-element per thread',
      tags: ['reduction', 'grid-stride', 'bandwidth'],
    },
    {
      d: 5,
      q: 'CUB’s decoupled look-back scan is single-pass (vs a 2-pass scan), nearly bandwidth-optimal, because each block…',
      o: [
        'Sorts first',
        'Publishes aggregate, looks back for its prefix',
        'Uses atomics per element',
        'Runs on the host',
      ],
      a: 1,
      x: [
        'Recomputes everything',
        'Uses FP64',
        'For graphics',
      ],
      e: 'Decoupled look-back has each block publish its aggregate and then look back at predecessors (aggregate/prefix) to compute its exclusive prefix, propagating prefixes in one pass — so device scan reads the data essentially once (bandwidth-optimal).',
      ref: 'Merrill & Garland; CUB scan',
      tags: ['scan', 'decoupled-lookback', 'cub'],
    },
    {
      d: 4,
      q: 'Padding a GEMM’s M, N, K to multiples of the tensor-core MMA tile (e.g. 16) avoids…',
      o: [
        'Compute',
        'Partial MMA tiles and fallbacks',
        'A reduction',
        'Coalescing',
      ],
      a: 1,
      x: [
        'Registers',
        'Occupancy',
        'For graphics',
      ],
      e: 'Tensor cores work on fixed tile shapes; non-multiple dimensions force partial/masked tiles or fallbacks, losing efficiency. Padding (or choosing) dimensions to MMA multiples keeps the tensor cores fully utilized.',
      ref: 'Tensor-core tiling; padding',
      tags: ['padding', 'tensor-cores', 'gemm'],
    },
    {
      d: 5,
      q: 'A kernel hits 90% of peak bandwidth (memory-bound). The ONLY way to go faster is…',
      o: [
        'More occupancy',
        'Reduce the bytes moved',
        'More registers',
        'Larger grid',
      ],
      a: 1,
      x: [
        'Add atomics',
        'Use FP64',
        'Disable the L2',
      ],
      e: 'At 90% of the bandwidth roof, compute/occupancy tuning is futile. The lever is algorithmic byte reduction: fusion (avoid intermediate round-trips), reuse via tiling/L2, or smaller data types — fewer bytes is the only path to higher throughput.',
      ref: 'Roofline; bandwidth-bound limit',
      tags: ['bandwidth-bound', 'fusion', 'precision'],
    },
    {
      d: 4,
      q: 'Choosing channels-last (NHWC) for a conv on tensor cores matters because…',
      o: [
        'It uses less memory',
        'Fast conv kernels want channels contiguous',
        'It increases precision',
        'It avoids tensor cores',
      ],
      a: 1,
      x: [
        'It reduces parameters',
        'For FP64',
        'For graphics',
      ],
      e: 'Channels-last makes channel vectors contiguous, matching how implicit-GEMM/tensor-core conv kernels load operands (coalesced, vectorized). NCHW strides channels by H×W, hurting those accesses or triggering transposes/slower kernels.',
      ref: 'NHWC; tensor-core convolution',
      tags: ['nhwc', 'convolution', 'tensor-cores'],
    },
    {
      d: 4,
      q: 'A double-buffered cp.async GEMM uses 2 stages but tensor cores still stall on loads. The likely fix is…',
      o: [
        'Fewer stages',
        'Add stages to hide the load latency',
        'More atomics',
        'FP64',
      ],
      a: 1,
      x: [
        'Smaller tiles',
        'Disable tensor cores',
        'For graphics',
      ],
      e: 'If two buffers can’t hide the load latency (math waits), deepening the pipeline (more stages) keeps more tiles in flight, covering the latency. Each stage costs shared memory, so add stages only until the loads are hidden.',
      ref: 'Software pipelining; stage count',
      tags: ['pipelining', 'stages', 'cp-async'],
    },
    {
      d: 4,
      q: 'Eliminating a 16-way bank conflict in a shared-memory access can speed that access by up to…',
      o: [
        'Nothing',
        '~16× (16 transactions → 1)',
        '2×',
        'It slows down',
      ],
      a: 1,
      x: ['1.5×', '32×', 'Half'],
      e: 'An N-way conflict serializes into N transactions; removing it (padding/swizzle) restores single-transaction service, up to ~N× faster for that access. Whether it speeds the whole kernel depends on how shared-memory-bound it is.',
      ref: 'Bank conflicts; speedup',
      tags: ['bank-conflict', 'shared-memory', 'speedup'],
    },
    {
      d: 5,
      q: 'A fused kernel is SLOWER than two separate kernels despite less HBM traffic. The most likely cause is…',
      o: [
        'Fusion never helps',
        'Resource demand cut occupancy or spilled',
        'More launches',
        'Divergence',
      ],
      a: 1,
      x: [
        'Coalescing',
        'Tensor-core use',
        'For graphics',
      ],
      e: 'Fusion increases per-thread live state. If that tanks occupancy or spills registers, the kernel can be slower than two simpler ones despite fewer DRAM round-trips. Profile registers/shared memory and occupancy to confirm the trade-off.',
      ref: 'Fusion; resource pressure',
      tags: ['fusion', 'occupancy', 'registers'],
    },
    {
      d: 4,
      q: 'A transpose kernel writes uncoalesced. Staging the tile in shared memory and writing it out transposed makes both global accesses coalesced; padding the tile to [32][33]…',
      o: [
        'Wastes memory uselessly',
        'Removes the 32-way column bank conflict',
        'Slows it down',
        'Disables coalescing',
      ],
      a: 1,
      x: [
        'Increases registers',
        'For FP64',
        'For graphics',
      ],
      e: 'A 32-wide shared tile causes a 32-way conflict on column access during transpose; padding to 33 offsets columns across banks (conflict-free). Combined with shared staging, both global reads and writes stay coalesced — the classic fast transpose.',
      ref: 'Matrix transpose; shared padding',
      tags: ['transpose', 'bank-conflict', 'coalescing'],
    },
    {
      d: 4,
      q: 'Thread coarsening (each thread computes 4 outputs) helps a latency-bound kernel by…',
      o: [
        'Reducing memory',
        'Independent per-thread work (ILP/MLP)',
        'Increasing occupancy',
        'Using tensor cores',
      ],
      a: 1,
      x: [
        'Lowering precision',
        'Avoiding the L2',
        'For graphics',
      ],
      e: 'Coarsening gives each thread more independent instructions/loads (ILP/MLP), filling latency gaps even at fixed occupancy — and amortizes per-thread setup. Beyond a point it spills registers/lowers occupancy, so it’s a tuned factor.',
      ref: 'Thread coarsening; latency-bound',
      tags: ['coarsening', 'ilp', 'latency-bound'],
    },
    {
      d: 5,
      q: 'In FlashAttention, keeping the query tile in registers while streaming K/V is chosen because…',
      o: [
        'Q is largest',
        'Q is reused against every K/V tile',
        'K is constant',
        'It avoids softmax',
      ],
      a: 1,
      x: [
        'Registers are off-chip',
        'It needs no shared memory',
        'For FP64',
      ],
      e: 'The query participates in all key/value blocks, so register-caching it (maximal reuse) while streaming K/V once each (low reuse) keeps the inner loop compute-bound and HBM-light — the data-movement strategy behind FlashAttention’s efficiency.',
      ref: 'FlashAttention; data movement',
      tags: ['flash-attention', 'registers', 'reuse'],
    },
    {
      d: 4,
      q: 'A kernel is SFU-bound after switching to __expf. Going faster now requires…',
      o: [
        'More memory accesses',
        'Cut the count of transcendentals',
        'More occupancy',
        'Larger grid',
      ],
      a: 1,
      x: [
        'Disable the L2',
        'Use FP64',
        'Add __syncthreads',
      ],
      e: 'Once SFU-bound, you’ve maxed the special-function pipe (and traded accuracy via __expf). Further gains need fewer transcendental ops (factor/reuse) or balancing with non-SFU work — not occupancy/memory tuning.',
      ref: 'SFU-bound; intrinsics',
      tags: ['sfu', 'intrinsics', 'compute-bound'],
    },
    {
      d: 4,
      q: 'For an iterative algorithm, keeping intermediate state in GLOBAL memory across kernel launches (vs copying to host each step) matters because…',
      o: [
        'Host memory is larger',
        'PCIe is far slower than HBM',
        'Global memory clears between launches',
        'It improves occupancy',
      ],
      a: 1,
      x: [
        'It needs managed memory',
        'For FP64',
        'For graphics',
      ],
      e: 'Device allocations persist across launches and the launch boundary guarantees visibility, so intermediates stay in HBM (TB/s) instead of bouncing over PCIe (tens of GB/s) each step — one of the highest-impact optimizations for iterative GPU code.',
      ref: 'Data residency; PCIe vs HBM',
      tags: ['data-residency', 'pcie', 'iterative'],
    },
    {
      d: 5,
      q: 'A GEMM tuned for Ampere (cp.async + mma) underperforms on Hopper. To reach Hopper peak you typically need…',
      o: [
        'Nothing',
        'A Hopper kernel: TMA, wgmma, clusters',
        'More registers',
        'FP64',
      ],
      a: 1,
      x: [
        'Smaller tiles',
        'Disable tensor cores',
        'For graphics',
      ],
      e: 'Hopper’s peak relies on TMA/wgmma/clusters/bigger shared memory and warp-specialized pipelines. Ampere-tuned cp.async/mma code runs but leaves throughput unused; reaching peak usually means a Hopper-specific rewrite (e.g. via CUTLASS 3.x).',
      ref: 'Hopper tuning; CUTLASS 3.x',
      tags: ['hopper', 'tma', 'wgmma', 'tuning'],
    },
    {
      d: 4,
      q: 'Top-k for small k is best done WITHOUT a full sort by…',
      o: [
        'Always sorting',
        'Track only the k largest',
        'atomicMax only',
        'The CPU',
      ],
      a: 1,
      x: [
        'A reduction',
        'A histogram',
        'For FP64',
      ],
      e: 'For small k, tracking just the k largest (bitonic top-k, heaps, or threshold-based selection) is far cheaper than fully sorting n elements. Libraries provide tuned top-k; the principle is to avoid the unnecessary full sort.',
      ref: 'GPU top-k; selection',
      tags: ['top-k', 'selection', 'sorting'],
    },
    {
      d: 4,
      q: 'A kernel is L2-bound (high L2 throughput, DRAM not saturated). A useful step is…',
      o: [
        'More atomics',
        'More L1/register reuse, fewer L2 hits',
        'Use FP64',
        'Lower occupancy',
      ],
      a: 1,
      x: [
        'Disable the L1',
        'Larger grid only',
        'Constant memory for outputs',
      ],
      e: 'L2-bound means traffic that could be served by L1/registers is reaching L2. Better tiling/reuse keeps data closer (L1/registers), reducing L2 requests; the L2 persisting window can also help for revisited regions.',
      ref: 'Nsight Compute; L2-bound',
      tags: ['l2-bound', 'reuse', 'tiling'],
    },
    {
      d: 5,
      q: 'The single biggest reason a custom fused Triton/CUTLASS kernel beats calling separate library ops is usually…',
      o: [
        'Faster math units',
        'Fusion keeps intermediates on-chip',
        'Lower precision',
        'More occupancy',
      ],
      a: 1,
      x: [
        'Avoiding tensor cores',
        'Smaller grids',
        'For graphics',
      ],
      e: 'Libraries optimize single ops; the leverage for custom kernels is fusing a chain so the intermediate tensors never touch HBM. For memory-bound DL pipelines, removing those round-trips is the dominant speedup (the basis of torch.compile/Triton fusion).',
      ref: 'Kernel fusion; Triton/CUTLASS',
      tags: ['fusion', 'triton', 'cutlass', 'memory-traffic'],
    },
  ],
  { idPrefix: 'optimization-f' }
)
