import { defineQuestions } from './_helpers.js'

// Multi-GPU & NCCL — batch E. PXN/topology, tuner plugins, device-initiated comm,
// congestion, fusion, scaling. Distinct from multigpu.js/.b/.c/.d.
export default defineQuestions(
  'multigpu',
  [
    {
      d: 5,
      q: 'NCCL "PXN" (PCI × NVLink) routing improves multi-node collectives by…',
      o: [
        'Disabling NVLink',
        'Letting a GPU reach a remote NIC via a PEER GPU over NVLink (instead of a slower local PCIe path), so traffic egresses through the best-connected NIC — improving inter-node bandwidth on certain topologies',
        'Using the CPU',
        'Compressing data',
      ],
      a: 1,
      x: [
        'Lowering precision',
        'Skipping ranks',
        'Pinning memory',
      ],
      e: 'PXN forwards a GPU’s inter-node traffic over NVLink to a peer GPU that has a better NIC path, optimizing rail usage on topologies where a GPU’s own PCIe-to-NIC path is suboptimal — raising achieved inter-node bandwidth.',
      ref: 'NCCL PXN; topology routing',
      tags: ['nccl', 'pxn', 'topology', 'multi-node'],
    },
    {
      d: 4,
      q: 'NCCL_TOPO_FILE / NCCL_TOPO_DUMP_FILE let you…',
      o: [
        'Set the precision',
        'Inspect the topology NCCL detected (dump) or override it with a hand-specified topology (file) — useful when auto-detection is wrong or for tuning on custom hardware',
        'Pick the batch size',
        'Disable NCCL',
      ],
      a: 1,
      x: [
        'Lock clocks',
        'Compress data',
        'Train the model',
      ],
      e: 'Dumping the detected topology helps debug why NCCL chose certain paths; providing a topology file lets you correct mis-detection or describe non-standard systems, so NCCL builds optimal rings/trees and uses the right links.',
      ref: 'NCCL topology environment variables',
      tags: ['nccl', 'topology', 'debugging'],
    },
    {
      d: 5,
      q: 'A NCCL "tuner plugin" allows…',
      o: [
        'Training tuning',
        'Customizing NCCL’s algorithm/protocol selection (and channel counts) for a specific cluster, overriding the built-in heuristics to extract more performance',
        'Quantizing gradients',
        'Disabling RDMA',
      ],
      a: 1,
      x: [
        'Pinning memory',
        'Compressing data',
        'Picking the optimizer',
      ],
      e: 'The tuner plugin interface lets operators inject custom logic for choosing ring/tree/NVLS, protocol, and channels per message size/topology — useful on bespoke clusters where the default heuristics aren’t optimal.',
      ref: 'NCCL tuner plugin',
      tags: ['nccl', 'tuner', 'tuning'],
    },
    {
      d: 5,
      q: 'Device-initiated communication (NVSHMEM) differs from NCCL fundamentally because…',
      o: [
        'It is slower',
        'A running KERNEL issues the communication (one-sided put/get) itself, enabling fine-grained overlap of comm with compute, whereas NCCL collectives are launched from the HOST as bulk operations',
        'It uses the CPU',
        'It only does broadcast',
      ],
      a: 1,
      x: [
        'It has no GPUs',
        'It is host-only',
        'It disables NVLink',
      ],
      e: 'NCCL collectives are host-launched, coarse-grained operations. NVSHMEM lets device code initiate remote memory access directly, so communication interleaves with computation at fine granularity — valuable for fused comm/compute kernels.',
      ref: 'NVSHMEM vs NCCL; device-initiated comm',
      tags: ['nvshmem', 'device-initiated', 'overlap'],
    },
    {
      d: 5,
      q: 'On a fat-tree network with 2:1 oversubscription, scaling an all-reduce across many nodes can underperform because…',
      o: [
        'Bandwidth is unlimited',
        'The bisection bandwidth is half the full-bandwidth ideal, so cross-cluster collective traffic contends on oversubscribed uplinks — limiting achievable inter-node bandwidth at scale',
        'Latency is zero',
        'It uses NVLink',
      ],
      a: 1,
      x: [
        'Precision drops',
        'It avoids the network',
        'For one GPU',
      ],
      e: 'Oversubscription means upper-tier links can’t carry full bisection traffic. Collective patterns that cross the oversubscribed cut bottleneck on those uplinks, reducing effective bandwidth — a reason large training clusters favor full-bisection (rail-optimized) networks.',
      ref: 'Network oversubscription; bisection bandwidth',
      tags: ['fat-tree', 'oversubscription', 'bisection'],
    },
    {
      d: 4,
      q: 'InfiniBand adaptive routing helps large collectives by…',
      o: [
        'Compressing data',
        'Dynamically spreading flows across alternative network paths to avoid congestion hotspots, improving achieved bandwidth under heavy all-to-all/all-reduce load',
        'Lowering precision',
        'Using the CPU',
      ],
      a: 1,
      x: [
        'Disabling RDMA',
        'Pinning memory',
        'Skipping ranks',
      ],
      e: 'Static routing can concentrate flows on some links (congestion). Adaptive routing reroutes around hotspots across equivalent paths, smoothing utilization — important for collective-heavy training where many flows traverse the network simultaneously.',
      ref: 'InfiniBand adaptive routing',
      tags: ['infiniband', 'adaptive-routing', 'congestion'],
    },
    {
      d: 4,
      q: 'Why does capturing an iteration’s NCCL collectives into a CUDA Graph help at scale?',
      o: [
        'It compresses gradients',
        'It removes per-collective CPU launch overhead and reduces jitter, so the (already overlapped) communication and compute replay with less host-side variability — tightening step time across ranks',
        'It increases bandwidth',
        'It disables NCCL',
      ],
      a: 1,
      x: [
        'It lowers precision',
        'It uses the CPU more',
        'It skips ranks',
      ],
      e: 'At scale, host-launch overhead and jitter across many ranks hurt synchronization. Graph-capturing the step (including capture-safe NCCL ops) cuts launch overhead and variability, helping all ranks stay in step and improving throughput.',
      ref: 'NCCL + CUDA Graphs',
      tags: ['nccl', 'cuda-graphs', 'jitter'],
    },
    {
      d: 4,
      q: 'NCCL_NVLS_ENABLE / detecting NVLS support matters because in-network reduction (NVLS) is only available…',
      o: [
        'On any GPU',
        'On systems with NVSwitch + the requisite hardware/driver support (NVLink SHARP) — otherwise NCCL falls back to ring/tree without the in-fabric reduction benefit',
        'On PCIe systems',
        'On the CPU',
      ],
      a: 1,
      x: [
        'On one GPU',
        'With FP64',
        'On InfiniBand only',
      ],
      e: 'NVLS (NVLink SHARP) needs NVSwitch-based systems with SHARP support. Where available it halves all-reduce traffic via in-fabric reduction; otherwise NCCL uses ring/tree. Verifying it’s active (NCCL_DEBUG) ensures you get the speedup.',
      ref: 'NCCL NVLS; NVSwitch/SHARP',
      tags: ['nccl', 'nvls', 'nvswitch'],
    },
    {
      d: 5,
      q: 'Fusing communication with computation (e.g. overlapping a reduce-scatter with the matmul that produces its input) is most beneficial when…',
      o: [
        'Communication is free',
        'The workload is communication-bound or comm is a large fraction of step time — overlapping/fusing hides it behind compute, improving scaling efficiency; if comm is already tiny, the benefit is small',
        'There is one GPU',
        'Precision is low',
      ],
      a: 1,
      x: [
        'Compute is zero',
        'For FP64 only',
        'It always doubles speed',
      ],
      e: 'Comm/compute fusion pays off proportional to how much communication it can hide. For comm-heavy steps (large models/clusters), tightly overlapping collectives with the producing compute (sometimes via NVSHMEM/fused kernels) materially improves scaling.',
      ref: 'Comm/compute overlap and fusion',
      tags: ['fusion', 'overlap', 'scaling'],
    },
    {
      d: 4,
      q: 'A simple model: with per-GPU compute time C and exposed (non-overlapped) communication time M per step, data-parallel scaling efficiency is roughly…',
      o: [
        'Always 1',
        'C / (C + M) — efficiency drops as exposed communication M grows relative to compute C; perfect overlap drives M→0',
        'M / C',
        'C × M',
      ],
      a: 1,
      x: [
        'C − M',
        '1 / M',
        'Independent of M',
      ],
      e: 'If communication overlaps compute, only the exposed portion M matters; step time ≈ C + M and efficiency ≈ C/(C+M). Maximizing overlap (bucketing, separate streams, faster links) shrinks M toward 0 and pushes efficiency toward 1.',
      ref: 'Scaling efficiency; overlap model',
      tags: ['scaling', 'overlap', 'model'],
    },
    {
      d: 4,
      q: 'Why must NCCL operations across ranks use a CONSISTENT data type, count, and op?',
      o: [
        'For logging',
        'A collective is a coordinated operation — every rank must agree on the buffer size/type/operation; mismatches cause hangs, corruption, or errors',
        'For speed only',
        'For precision',
      ],
      a: 1,
      x: [
        'It is optional',
        'For memory',
        'For the optimizer',
      ],
      e: 'All ranks participate in the same collective with matching parameters. Inconsistent count/dtype/op (e.g. a bug where ranks disagree on tensor size) breaks the collective — manifesting as a hang or corrupted result. Consistency is a hard requirement.',
      ref: 'NCCL collective semantics',
      tags: ['nccl', 'consistency', 'correctness'],
    },
    {
      d: 5,
      q: 'MNNVL (multi-node NVLink, e.g. NVL72) changes the collective landscape by…',
      o: [
        'Using PCIe',
        'Extending the high-bandwidth NVLink domain across nodes, so collectives that would cross a slow inter-node network instead use NVLink — dramatically improving large-scale TP/EP communication',
        'Lowering precision',
        'Disabling NCCL',
      ],
      a: 1,
      x: [
        'Using the CPU',
        'For one GPU',
        'Compressing data',
      ],
      e: 'Normally inter-node traffic uses slower IB/RoCE; MNNVL puts many nodes in one NVLink domain so even cross-node collectives run at NVLink speed — enabling communication-heavy parallelism (tensor/expert) across far more GPUs efficiently.',
      ref: 'NVIDIA MNNVL; NVL72',
      tags: ['mnnvl', 'nvlink', 'collectives', 'scale'],
    },
    {
      d: 4,
      q: 'For inter-node collectives, missing GPUDirect RDMA means…',
      o: [
        'Faster transfers',
        'Data must stage through HOST memory (extra copies, CPU involvement), reducing bandwidth and increasing latency — the NIC can’t DMA directly to/from GPU memory',
        'Higher precision',
        'No effect',
      ],
      a: 1,
      x: [
        'NVLink is used',
        'More channels',
        'Pinned memory only',
      ],
      e: 'Without GPUDirect RDMA, the NIC can’t access GPU memory directly, so inter-node transfers bounce through host memory (copies + CPU), throttling bandwidth/latency. Enabling it (drivers, hardware support) is key to fast multi-node collectives.',
      ref: 'GPUDirect RDMA; inter-node bandwidth',
      tags: ['gpudirect-rdma', 'multi-node', 'bandwidth'],
    },
    {
      d: 3,
      q: 'The busbw metric is used (over algbw) to compare collective performance across GPU counts because it…',
      o: [
        'Ignores data size',
        'Normalizes by the collective’s communication pattern (e.g. the 2(N-1)/N factor for all-reduce), so it reflects achieved LINK bandwidth and is comparable across N',
        'Measures latency',
        'Counts FLOPs',
      ],
      a: 1,
      x: [
        'Measures compute',
        'Is the PCIe rate',
        'Is host bandwidth',
      ],
      e: 'algbw (data/time) varies with the algorithm’s traffic factor; busbw applies that factor to estimate per-link bandwidth, which should approach the hardware peak regardless of N — the right metric to judge collective efficiency at different scales.',
      ref: 'nccl-tests busbw',
      tags: ['busbw', 'benchmarking', 'scaling'],
    },
    {
      d: 4,
      q: 'A symmetric-memory model for collectives (each rank exposes a region at a known offset) helps NCCL/NVSHMEM by…',
      o: [
        'Compressing data',
        'Letting peers address each other’s buffers directly (registered/symmetric), enabling efficient one-sided/zero-copy transfers without per-op handshakes',
        'Lowering precision',
        'Using the CPU',
      ],
      a: 1,
      x: [
        'Disabling RDMA',
        'Pinning host memory',
        'Skipping ranks',
      ],
      e: 'Symmetric/registered memory means each rank’s buffer is addressable by peers, so transfers can be one-sided/zero-copy with minimal overhead — the model NVSHMEM uses and NCCL’s user-buffer registration approximates for repeated collectives.',
      ref: 'Symmetric memory; NVSHMEM/NCCL registration',
      tags: ['symmetric-memory', 'one-sided', 'registration'],
    },
    {
      d: 4,
      q: 'When data-parallel training is communication-bound, increasing the per-GPU batch (more compute per step) can improve scaling efficiency because…',
      o: [
        'It reduces accuracy',
        'It raises the compute time C relative to the (roughly fixed) gradient communication M per step, so the C/(C+M) efficiency improves — more compute to hide the same comm behind',
        'It removes communication',
        'It uses FP64',
      ],
      a: 1,
      x: [
        'It shards the model',
        'It disables NCCL',
        'It lowers precision',
      ],
      e: 'Gradient all-reduce volume depends on parameters, not batch size; a bigger per-GPU batch increases compute per step without increasing comm, improving the compute-to-comm ratio and thus overlap/scaling — at the cost of more memory and a larger global batch.',
      ref: 'Scaling; batch size vs communication',
      tags: ['batch-size', 'communication-bound', 'scaling'],
    },
    {
      d: 5,
      q: 'Expert-parallel MoE often pairs all-to-all with computation overlap because…',
      o: [
        'All-to-all is free',
        'The dispatch/combine all-to-alls are large and latency/bandwidth-sensitive; overlapping them with expert (FFN) compute — and balancing routing — keeps the network from serializing the experts',
        'It uses FP64',
        'It avoids experts',
      ],
      a: 1,
      x: [
        'It disables NCCL',
        'For one GPU',
        'It compresses tokens',
      ],
      e: 'MoE’s two all-to-alls can dominate. Overlapping them with the expert compute (and balancing tokens so no GPU/expert is overloaded) hides communication and avoids the network becoming the bottleneck — central to efficient MoE systems.',
      ref: 'MoE; all-to-all overlap',
      tags: ['moe', 'all-to-all', 'overlap', 'expert-parallel'],
    },
    {
      d: 3,
      q: 'NCCL_DEBUG=INFO showing "Channel 00/02 : ... [NVLink]" confirms…',
      o: [
        'The precision',
        'NCCL set up its communication channels over NVLink for those GPU connections — verifying the fast intra-node path is in use (vs PCIe/SHM)',
        'The batch size',
        'The optimizer',
      ],
      a: 1,
      x: [
        'The learning rate',
        'The grid size',
        'The dataset',
      ],
      e: 'The debug log enumerates channels and their transports. Seeing NVLink for intra-node links (and IB for inter-node) confirms the fast paths are used; PCIe/SHM where NVLink/IB was expected indicates a topology/config problem.',
      ref: 'NCCL_DEBUG=INFO; channels/transports',
      tags: ['nccl', 'debug', 'channels'],
    },
    {
      d: 4,
      q: 'Why is one-process-per-GPU with proper device binding important for NCCL bandwidth (not just correctness)?',
      o: [
        'It looks cleaner',
        'It ensures each rank drives its own GPU with correct GPU↔NIC/NVLink affinity; wrong binding (or one process for many GPUs) can route traffic over suboptimal paths and serialize host-side launches',
        'It reduces precision',
        'It compresses data',
      ],
      a: 1,
      x: [
        'It disables NVLink',
        'It uses the CPU',
        'It pins host memory',
      ],
      e: 'Correct per-GPU process binding gives each rank the right NIC/NVLink affinity and parallel host-side launching. Misbinding (or single-process multi-GPU) can pick poor transport paths and bottleneck on one host thread, lowering achieved bandwidth.',
      ref: 'NCCL setup; affinity and bandwidth',
      tags: ['device-binding', 'affinity', 'bandwidth'],
    },
    {
      d: 5,
      q: 'A reason large-scale training tolerates slow INTER-node links for pipeline parallelism but not tensor parallelism is…',
      o: [
        'PP uses more data',
        'PP sends only stage-boundary activations occasionally (light, latency-tolerant), while TP does latency-sensitive all-reduces every layer (heavy) — so PP maps to inter-node, TP to intra-node NVLink',
        'TP uses no communication',
        'They are identical',
      ],
      a: 1,
      x: [
        'PP needs NVLink',
        'TP is latency-tolerant',
        'For FP64',
      ],
      e: 'TP’s frequent per-layer collectives demand NVLink’s low latency/high bandwidth (intra-node). PP’s infrequent point-to-point activation transfers tolerate slower inter-node networks. This drives the standard 3D mapping (TP in-node, PP/DP across nodes).',
      ref: 'Parallelism mapping; communication intensity',
      tags: ['pipeline', 'tensor-parallel', 'topology'],
    },
    {
      d: 4,
      q: 'NCCL collective LATENCY (not bandwidth) is what dominates when…',
      o: [
        'Messages are huge',
        'Messages are SMALL (e.g. tiny gradient tensors, or many separate small collectives) — fixed per-step costs dominate, which is why gradient bucketing and low-latency protocols/algorithms help',
        'Using FP64',
        'On one GPU',
      ],
      a: 1,
      x: [
        'For large all-to-all',
        'It never dominates',
        'For broadcast only',
      ],
      e: 'Small collectives are latency-bound: setup/step costs outweigh transfer time. Bucketing small tensors into large transfers, and NCCL’s low-latency protocols (LL/LL128) and tree/one-shot algorithms, mitigate this for small-message regimes.',
      ref: 'NCCL latency; small messages',
      tags: ['latency', 'small-messages', 'bucketing'],
    },
    {
      d: 4,
      q: 'GPU-to-GPU bandwidth measured by nccl-tests being ~half NVLink peak on an all-reduce is…',
      o: [
        'A bug',
        'Expected — ring all-reduce moves ~2× the data per GPU, so busbw (which factors that out) should approach peak even though raw per-direction throughput looks like a fraction; check busbw, not raw algbw',
        'A failure',
        'Too high',
      ],
      a: 1,
      x: [
        'Impossible',
        'A precision issue',
        'A driver error',
      ],
      e: 'Because all-reduce inherently moves ~2(N-1)/N× the data, raw algbw looks lower than peak; busbw accounts for the pattern and should near the link peak. Comparing busbw (not algbw) to NVLink peak gives the right efficiency read.',
      ref: 'nccl-tests; busbw vs algbw',
      tags: ['nccl-tests', 'busbw', 'allreduce'],
    },
    {
      d: 3,
      q: 'A practical first step when multi-node training is unexpectedly slow is to…',
      o: [
        'Rewrite the model',
        'Run nccl-tests across the same ranks/topology and check busbw against expected IB/NVLink peak — isolating whether the bottleneck is the network/collectives or the model code',
        'Lower precision',
        'Add more GPUs',
      ],
      a: 1,
      x: [
        'Profile one kernel',
        'Disable NCCL',
        'Reduce the batch',
      ],
      e: 'Before blaming the model, benchmark the collectives in isolation (nccl-tests). Low busbw vs expected peak points to network/topology/transport issues (sockets instead of IB, wrong affinity); healthy busbw redirects you to the application.',
      ref: 'nccl-tests; multi-node debugging',
      tags: ['nccl-tests', 'debugging', 'multi-node'],
    },
    {
      d: 5,
      q: 'Why might overlapping the optimizer’s all-gather (FSDP) of the NEXT layer with the current layer’s backward fail to fully hide communication at very large scale?',
      o: [
        'Overlap always works',
        'As the model/cluster grows, the per-layer all-gather volume or network contention can exceed the current layer’s backward compute, so the gather isn’t fully hidden — exposing communication',
        'Backward is free',
        'For FP64 only',
      ],
      a: 1,
      x: [
        'NCCL disables overlap',
        'It needs the CPU',
        'Streams serialize',
      ],
      e: 'Overlap hides comm only up to the available compute. At extreme scale (bigger gathers, more contention, smaller per-GPU compute), the all-gather can outlast the overlapping backward, leaving exposed communication — the fundamental limit FSDP/ZeRO-3 hit at scale.',
      ref: 'FSDP overlap limits; scaling',
      tags: ['fsdp', 'overlap', 'all-gather', 'scaling'],
    },
    {
      d: 4,
      q: 'For TP within a node on NVSwitch, the all-reduce per layer is fast because…',
      o: [
        'It uses PCIe',
        'NVSwitch provides full all-to-all NVLink bandwidth and (with NVLS) in-network reduction, so the per-layer all-reduce completes with low latency/high bandwidth — making frequent TP collectives affordable',
        'It uses the CPU',
        'It compresses data',
      ],
      a: 1,
      x: [
        'TP has no collectives',
        'For FP64',
        'It avoids NVLink',
      ],
      e: 'TP issues an all-reduce every layer; NVSwitch’s full-bandwidth all-to-all (and NVLS in-fabric reduction) make those collectives fast enough that TP within a node is efficient — which is why TP is confined to NVLink/NVSwitch domains.',
      ref: 'TP on NVSwitch; NVLS',
      tags: ['tensor-parallel', 'nvswitch', 'nvls', 'allreduce'],
    },
    {
      d: 4,
      q: 'A correctness/robustness reason to set a finite NCCL collective timeout is…',
      o: [
        'Speed',
        'Without a timeout, a hung collective (failed/slow rank, mismatch) blocks forever; a timeout converts it into an error the framework can detect and recover from (abort/restart)',
        'Precision',
        'Memory savings',
      ],
      a: 1,
      x: [
        'It disables NCCL',
        'It compresses data',
        'It lowers latency',
      ],
      e: 'Collectives can deadlock on a misbehaving rank. A timeout (watchdog) ensures the job fails fast with a diagnosable error instead of hanging indefinitely — essential for fault tolerance and operability at scale.',
      ref: 'NCCL timeout/watchdog',
      tags: ['nccl', 'timeout', 'fault-tolerance'],
    },
    {
      d: 3,
      q: 'AllReduce being decomposable into ReduceScatter + AllGather is why…',
      o: [
        'It is slower',
        'Ring all-reduce is bandwidth-optimal (each phase moves (N-1)/N of the data) and ZeRO/FSDP can use just ReduceScatter (gradients) + AllGather (params) for sharded training',
        'It needs the CPU',
        'For FP64',
      ],
      a: 1,
      x: [
        'It avoids the network',
        'It compresses data',
        'For one GPU',
      ],
      e: 'The decomposition underlies both bandwidth-optimal ring all-reduce and sharded training: FSDP/ZeRO use ReduceScatter for gradients and AllGather for parameters rather than full all-reduce — matching the sharded data layout.',
      ref: 'AllReduce = ReduceScatter + AllGather; FSDP',
      tags: ['allreduce', 'reduce-scatter', 'all-gather', 'fsdp'],
    },
    {
      d: 5,
      q: 'Why can adding more NCCL channels stop helping (or hurt) beyond a point?',
      o: [
        'Channels are free',
        'Channels consume SMs/copy resources and link bandwidth; once the links are saturated, more channels add contention/overhead without raising throughput — there’s an optimal channel count per topology/size',
        'They lower precision',
        'They disable NVLink',
      ],
      a: 1,
      x: [
        'They always help',
        'They use the CPU',
        'For FP64',
      ],
      e: 'Channels parallelize a collective across links/SMs, but the underlying link bandwidth is finite. Past saturation, extra channels just add management overhead and resource contention. NCCL tunes channel count; manual overrides should respect this.',
      ref: 'NCCL channels; diminishing returns',
      tags: ['nccl', 'channels', 'saturation'],
    },
    {
      d: 4,
      q: 'For elastic training that can lose a node mid-run, the distributed stack must support…',
      o: [
        'Nothing special',
        'Communicator teardown/recreation (re-rendezvous) and resume-from-checkpoint, so the surviving ranks re-form the group and continue — turning a node failure into a recoverable event',
        'FP64',
        'Larger batches',
      ],
      a: 1,
      x: [
        'Disabling NCCL',
        'A single GPU',
        'Lower precision',
      ],
      e: 'Elastic/fault-tolerant training detects failure (timeouts/abort), re-forms the process group with the surviving (or replacement) ranks, and resumes from a checkpoint — essential at scale where node failures over long runs are common.',
      ref: 'Elastic training; communicator recovery',
      tags: ['elastic', 'fault-tolerance', 'recovery'],
    },
  ],
  { idPrefix: 'multigpu-e' }
)
