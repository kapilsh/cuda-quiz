import { defineQuestions } from './_helpers.js'

// Execution & Scheduling — batch B. Warp vote/shuffle, lanes, scheduling
// internals, cooperative groups, dynamic parallelism. Distinct from execution.js.
export default defineQuestions(
  'execution',
  [
    {
      d: 3,
      q: '__ballot_sync(mask, predicate) returns what?',
      o: [
        'The number of threads with predicate true',
        '32-bit mask: bit i set if lane i had pred true',
        'The first lane with predicate true',
        'A boolean: true if all lanes agree',
      ],
      a: 1,
      x: [
        'The lane id of the calling thread',
        'The sum of the predicates as a float',
        'A pointer to the warp’s registers',
      ],
      e: '__ballot_sync produces a bitmask of which lanes evaluated the predicate as nonzero. Combined with __popc (population count) it yields counts; it underpins warp-aggregated atomics and compaction.',
      ref: 'CUDA C++ Programming Guide §B.22 (Warp Vote Functions)',
      tags: ['ballot', 'warp-vote'],
    },
    {
      d: 2,
      q: '__all_sync(mask, pred) returns true when…',
      o: [
        'Any lane in the mask has pred true',
        'All lanes in the mask have pred true',
        'Exactly half the lanes have pred true',
        'The calling lane has pred true',
      ],
      a: 1,
      x: [
        'No lane has pred true',
        'The first lane has pred true',
        'The mask is nonzero',
      ],
      e: '__all_sync is a warp vote: it returns nonzero only if the predicate is true for every participating lane. __any_sync returns true if any lane’s predicate is true.',
      ref: 'CUDA C++ Programming Guide §B.22',
      tags: ['warp-vote', 'all-sync'],
    },
    {
      d: 3,
      q: 'How do you compute a thread’s lane index within its warp?',
      o: [
        'threadIdx.x / 32',
        'threadIdx.x % warpSize (or %laneid)',
        'blockIdx.x % 32',
        'threadIdx.x * warpSize',
      ],
      a: 1,
      x: [
        'warpSize - threadIdx.x',
        'threadIdx.y % 32',
        'gridDim.x % 32',
      ],
      e: 'For a 1D block the lane is threadIdx.x % 32; the warp index is threadIdx.x / 32. The hardware also exposes %laneid. Lane id selects which bit of a ballot or which source lane in a shuffle.',
      ref: 'CUDA C++ Programming Guide §B.22; PTX special registers',
      tags: ['lane-id', 'warp', 'indexing'],
    },
    {
      d: 4,
      q: '__activemask() is useful when…',
      o: [
        'You always want the full 0xFFFFFFFF mask',
        'You need the active lanes to feed a _sync primitive',
        'You want to deactivate lanes',
        'You need the block’s thread count',
      ],
      a: 1,
      x: [
        'You want the warp id',
        'You need to synchronize blocks',
        'You want to count SMs',
      ],
      e: '__activemask returns the lanes currently active at that point. Under independent thread scheduling you can’t assume a full mask, so library code queries the active set to feed __shfl_sync/__ballot_sync correctly. Use deliberate masks where possible.',
      ref: 'CUDA C++ Programming Guide §B.22; Volta migration',
      tags: ['activemask', 'warp', 'divergence'],
    },
    {
      d: 3,
      q: 'Zero-overhead warp scheduling means…',
      o: [
        'Warps never stall',
        'Switching resident warps costs zero cycles',
        'There is no scheduler',
        'Warps run on the host',
      ],
      a: 1,
      x: [
        'Context switches flush registers to memory like a CPU',
        'Only one warp can be resident',
        'Scheduling happens at compile time',
      ],
      e: 'Unlike CPUs that save/restore state, GPU warp contexts (registers, PCs) remain resident, so the scheduler can switch among ready warps every cycle for free — the mechanism behind latency hiding.',
      ref: 'NVIDIA architecture whitepapers; PMPP Ch. 4',
      tags: ['scheduling', 'latency-hiding', 'zero-overhead'],
    },
    {
      d: 3,
      q: 'In Nsight terms, what is an "eligible" warp?',
      o: [
        'A warp that has finished',
        'A resident warp ready to issue this cycle',
        'A warp that diverged',
        'A warp waiting on __syncthreads',
      ],
      a: 1,
      x: [
        'A warp in another block',
        'A warp with the lowest id',
        'A warp using tensor cores',
      ],
      e: 'Active warps are resident; eligible warps are the subset ready to issue (no pending stalls); the scheduler selects one eligible warp per cycle (the "selected" warp). Few eligible warps per cycle indicates stalls limiting throughput.',
      ref: 'Nsight Compute (Scheduler Statistics)',
      tags: ['scheduling', 'eligible-warps', 'nsight'],
    },
    {
      d: 4,
      q: 'A typical FMA instruction on an SM has a latency of a few cycles but a throughput of one per cycle (per pipe). To keep the pipe full you must…',
      o: [
        'Reduce the block size',
        'Keep independent instructions in flight',
        'Use double precision',
        'Avoid shared memory',
      ],
      a: 1,
      x: [
        'Use only one warp',
        'Insert __syncthreads between FMAs',
        'Disable the L1 cache',
      ],
      e: 'Pipelined units accept a new op each cycle but results aren’t ready for a few cycles. Dependent back-to-back FMAs stall; independent work (ILP via multiple accumulators, or more warps) fills the latency and sustains throughput.',
      ref: 'CUDA Best Practices Guide; instruction latency/throughput',
      tags: ['latency-throughput', 'ilp', 'pipeline'],
    },
    {
      d: 2,
      q: 'An uncached global memory load has a latency on the order of…',
      o: [
        'A few cycles',
        'Tens of cycles',
        'Several hundred cycles',
        'Millions of cycles',
      ],
      a: 2,
      x: ['Exactly 32 cycles', 'One cycle', 'Tens of thousands of cycles'],
      e: 'Global (DRAM) latency is hundreds of cycles. Hiding it requires many warps and/or ILP. Shared memory and L1 are ~20–40 cycles; registers are immediate.',
      ref: 'GPU microbenchmark literature; PMPP Ch. 5',
      tags: ['latency', 'global-memory', 'scheduling'],
    },
    {
      d: 4,
      q: 'cudaOccupancyMaxActiveBlocksPerMultiprocessor(&n, kernel, blockSize, dynamicSMem) returns…',
      o: [
        'The total number of blocks to launch',
        'Resident blocks per SM for that kernel and block size',
        'The achieved occupancy after a run',
        'The number of SMs',
      ],
      a: 1,
      x: [
        'The optimal grid dimensions',
        'The number of warps per block',
        'The register count of the kernel',
      ],
      e: 'It computes the theoretical resident-block count per SM for a kernel + block size + shared-memory configuration — the basis for occupancy-aware grid sizing and the cooperative-launch capacity check.',
      ref: 'CUDA Runtime API (Occupancy)',
      tags: ['occupancy-api', 'scheduling'],
    },
    {
      d: 3,
      q: 'On Ampere/Hopper, the maximum number of resident threads per SM is typically…',
      o: ['1024', '2048', '4096', '512'],
      a: 1,
      x: ['256', '8192', '1536'],
      e: 'Most recent architectures support up to 2048 resident threads per SM = 64 warps. Block size and resource use determine how close you get. (The per-block max is separately 1024 threads.)',
      ref: 'CUDA C++ Programming Guide, Compute Capabilities',
      tags: ['limits', 'occupancy', 'sm'],
    },
    {
      d: 4,
      q: 'Dynamic parallelism allows…',
      o: [
        'The host to launch kernels faster',
        'A kernel to launch child kernels on-device',
        'Kernels to resize blocks at runtime',
        'Warps to change size',
      ],
      a: 1,
      x: [
        'Two GPUs to share a kernel',
        'Kernels to run on the CPU',
        'Automatic kernel fusion',
      ],
      e: 'Dynamic parallelism (CC 3.5+) lets device code call <<<>>> to spawn child grids, useful for recursive/adaptive algorithms (e.g. adaptive mesh refinement) without round-tripping to the host — at the cost of launch overhead per child.',
      ref: 'CUDA C++ Programming Guide §C (Dynamic Parallelism)',
      tags: ['dynamic-parallelism', 'device-launch'],
    },
    {
      d: 3,
      q: 'The compiler often converts a short if-body into predicated instructions. The benefit is…',
      o: [
        'It uses more registers',
        'Avoids a branch via per-lane predicates',
        'It guarantees coalescing',
        'It increases occupancy',
      ],
      a: 1,
      x: [
        'It moves code to constant memory',
        'It eliminates the need for warps',
        'It runs the body on the host',
      ],
      e: 'For tiny conditionals, predication (executing the instruction but writing results only where the predicate holds) is cheaper than a real branch with divergence. The compiler weighs body length against the cost of branching.',
      ref: 'CUDA C++ Programming Guide; SASS predication',
      tags: ['predication', 'divergence', 'compiler'],
    },
    {
      d: 4,
      q: 'cooperative_groups::tiled_partition<32>(block) gives you…',
      o: [
        'A new block',
        'A fixed-size tile with collective ops',
        'A grid-wide group',
        'A device handle',
      ],
      a: 1,
      x: [
        'A stream',
        'A pointer to shared memory',
        'A list of SMs',
      ],
      e: 'Cooperative Groups lets you partition a block into fixed-size tiles (e.g. 32, 16, 8) and call portable collective primitives (shuffle, vote, reduce, scan) on them — a cleaner, safer alternative to raw warp intrinsics.',
      ref: 'CUDA C++ Programming Guide §C (Cooperative Groups)',
      tags: ['cooperative-groups', 'tiled-partition', 'warp'],
    },
    {
      d: 4,
      q: 'coalesced_group (from cooperative groups) represents…',
      o: [
        'All threads in the grid',
        'The currently-converged active lanes',
        'Threads accessing coalesced memory',
        'A block of 32 threads always',
      ],
      a: 1,
      x: [
        'The threads in the first warp',
        'A group spanning two blocks',
        'The host threads',
      ],
      e: 'coalesced_threads() captures the lanes currently active at that point (e.g. inside a divergent branch) so you can rank them and perform safe collective operations — the modern way to do warp-aggregated work.',
      ref: 'CUDA C++ Programming Guide §C; warp-aggregated atomics',
      tags: ['cooperative-groups', 'coalesced-group', 'divergence'],
    },
    {
      d: 3,
      q: 'clock64() inside a kernel returns…',
      o: [
        'Wall-clock time in nanoseconds',
        'A per-SM cycle counter for relative timing',
        'The host CPU time',
        'The kernel launch timestamp',
      ],
      a: 1,
      x: [
        'The number of instructions executed',
        'The GPU clock frequency',
        'The number of cycles since boot on all SMs',
      ],
      e: 'clock()/clock64() read a per-SM cycle counter. Differences measure cycle counts for code sections, but values aren’t comparable across SMs and the clock can vary, so use for relative microbenchmarks, not absolute time.',
      ref: 'CUDA C++ Programming Guide §B.10 (Time Function)',
      tags: ['clock', 'timing', 'microbenchmark'],
    },
    {
      d: 4,
      q: '__nanosleep(ns) is used to…',
      o: [
        'Halt the whole GPU',
        'Back off a thread ~ns in spin-wait loops',
        'Synchronize a block',
        'Sleep the host',
      ],
      a: 1,
      x: [
        'Lower the clock frequency',
        'Yield to another kernel',
        'Wait for a memory fence',
      ],
      e: '__nanosleep (Volta+) suspends a thread for a hint duration, letting spin-loops on a lock or flag back off exponentially to reduce memory pressure and power versus busy-spinning.',
      ref: 'CUDA C++ Programming Guide §B.11 (__nanosleep)',
      tags: ['nanosleep', 'spin-wait', 'backoff'],
    },
    {
      d: 2,
      q: 'A loop whose trip count depends on per-thread data (e.g. while (x[i] > tol)) can hurt performance because…',
      o: [
        'It uses too much shared memory',
        'The warp runs until its slowest lane finishes',
        'It cannot be compiled',
        'It disables coalescing entirely',
      ],
      a: 1,
      x: [
        'Loops are not allowed in kernels',
        'It forces double precision',
        'It always deadlocks',
      ],
      e: 'Data-dependent loops cause intra-warp imbalance: the warp keeps executing until every lane’s condition is false, so a single long-running lane stalls the rest. Load balancing or work redistribution mitigates it.',
      ref: 'PMPP Ch. 4; divergence and load imbalance',
      tags: ['divergence', 'load-imbalance', 'loops'],
    },
    {
      d: 5,
      q: 'Warp specialization in advanced kernels (e.g. Hopper GEMM) refers to…',
      o: [
        'Making all warps identical',
        'Different roles per warp: producers vs consumers',
        'Running warps on different GPUs',
        'Disabling some warps',
      ],
      a: 1,
      x: [
        'Splitting a warp into 16-thread halves',
        'Using warps only for memory',
        'Compiling each warp separately',
      ],
      e: 'Warp specialization dedicates some warps to data movement (TMA/cp.async producers) and others to math (tensor-core consumers), decoupled through shared-memory buffers and mbarriers — a key pattern in CUTLASS 3.x pingpong/cooperative kernels.',
      ref: 'CUTLASS 3.x; Hopper kernel design',
      tags: ['warp-specialization', 'pipelining', 'hopper', 'gemm'],
    },
    {
      d: 3,
      q: 'On an Ampere SM (e.g. GA100), how many FP32 CUDA cores are there per SM?',
      o: ['32', '64', '128', '256'],
      a: 2,
      x: ['16', '512', '96'],
      e: 'GA100 has 64 FP32 cores plus 64 cores that do FP32 or INT32, giving 128 FP32 lanes per SM. Consumer Ampere (GA10x) also exposes 128 FP32/SM. Exact counts vary by architecture and SKU.',
      ref: 'NVIDIA Ampere Whitepaper',
      tags: ['cuda-cores', 'ampere', 'sm'],
    },
    {
      d: 4,
      q: 'Separate INT32 and FP32 pipelines (Volta+) allow…',
      o: [
        'INT and FP instructions to issue concurrently',
        'Faster double precision only',
        'More shared memory',
        'Larger warps',
      ],
      a: 0,
      x: [
        'INT32 to run on tensor cores',
        'Floats to be stored as ints',
        'Branchless execution',
      ],
      e: 'Having dedicated INT32 units lets index/pointer arithmetic execute in parallel with FP32 math rather than competing for the same pipe, improving throughput in loops that mix addressing and computation.',
      ref: 'NVIDIA Volta/Turing Whitepapers',
      tags: ['int32-pipe', 'concurrency', 'architecture'],
    },
    {
      d: 3,
      q: 'After a divergent if/else within a warp, where do the lanes "reconverge" (pre-Volta model)?',
      o: [
        'They never reconverge',
        'At the post-dominator of the branch',
        'At the next kernel launch',
        'At a __syncthreads only',
      ],
      a: 1,
      x: [
        'At the warp scheduler',
        'At the end of the block',
        'Randomly',
      ],
      e: 'Classic SIMT executes one path then the other with masking, reconverging at the post-dominator so the full warp proceeds together. Volta’s independent thread scheduling relaxes guaranteed lockstep, so don’t rely on implicit reconvergence — use __syncwarp.',
      ref: 'CUDA C++ Programming Guide §4.2',
      tags: ['divergence', 'reconvergence', 'simt'],
    },
    {
      d: 2,
      q: 'cooperative_groups::this_thread_block() returns a handle whose .sync() is equivalent to…',
      o: ['cudaDeviceSynchronize()', '__syncthreads()', '__syncwarp()', 'a memory fence only'],
      a: 1,
      x: ['cudaStreamSynchronize()', 'grid.sync()', '__threadfence()'],
      e: 'thread_block::sync() is the Cooperative Groups equivalent of __syncthreads() — a block-wide barrier. CG offers a typed, composable API (warp tiles, blocks, grids) over the raw sync primitives.',
      ref: 'CUDA C++ Programming Guide §C (Cooperative Groups)',
      tags: ['cooperative-groups', 'thread-block', 'barrier'],
    },
    {
      d: 4,
      q: 'A warp scheduler can issue back-to-back instructions from the SAME warp in consecutive cycles only if…',
      o: [
        'The warp has the lowest id',
        'They are independent (no data hazard)',
        'The block is small',
        'Shared memory is unused',
      ],
      a: 1,
      x: [
        'They are both memory loads',
        'The warp is fully converged',
        'The instructions use tensor cores',
      ],
      e: 'Dependent instructions from one warp must wait out the producer’s latency. Independent instructions (ILP within a warp) can issue in consecutive cycles, which is why exposing ILP helps even at modest occupancy.',
      ref: 'CUDA Best Practices Guide; instruction-level parallelism',
      tags: ['ilp', 'scheduling', 'hazards'],
    },
    {
      d: 5,
      q: 'A thread block cluster’s cluster.sync() barrier synchronizes…',
      o: [
        'All threads in the grid',
        'All threads in all blocks of the cluster',
        'Only threads in one block',
        'Only one warp per block',
      ],
      a: 1,
      x: [
        'Threads on two different GPUs',
        'Only the producer warps',
        'The host and device',
      ],
      e: 'cluster.sync() is a barrier across every thread of every block in the cluster, enabled because clusters are guaranteed co-resident on a GPC. It sits between the block barrier (__syncthreads) and grid-wide cooperative sync.',
      ref: 'CUDA C++ Programming Guide (Thread Block Clusters)',
      tags: ['clusters', 'cluster-sync', 'hopper'],
    },
    {
      d: 3,
      q: '__syncwarp(mask) differs from __syncthreads() in that it…',
      o: [
        'Synchronizes the whole device',
        'Syncs only the named lanes of one warp',
        'Is slower',
        'Also flushes the L2 cache',
      ],
      a: 1,
      x: [
        'Synchronizes all warps in the grid',
        'Only works on Pascal',
        'Requires cooperative launch',
      ],
      e: '__syncwarp forces the named lanes of one warp to reconverge and orders their shared-memory accesses — needed under independent thread scheduling for warp-synchronous code. __syncthreads is the broader block barrier.',
      ref: 'CUDA C++ Programming Guide §B.6; Volta migration',
      tags: ['syncwarp', 'warp', 'volta'],
    },
    {
      d: 4,
      q: 'Why does the GPU keep a warp resident (occupying register/shared resources) even while it is stalled on memory?',
      o: [
        'To save power',
        'So it can resume instantly when data arrives',
        'Because it cannot be evicted',
        'To prevent divergence',
      ],
      a: 1,
      x: [
        'To keep the L2 warm',
        'Because warps run on the host',
        'To guarantee ordering',
      ],
      e: 'Each warp’s registers and PC stay live on the SM. A stalled warp simply isn’t "eligible" until its dependency resolves, at which point it can issue immediately. This is why register/shared usage caps how many warps fit (occupancy).',
      ref: 'PMPP Ch. 4; architecture whitepapers',
      tags: ['scheduling', 'occupancy', 'latency-hiding'],
    },
    {
      d: 2,
      q: 'Two blocks of the same kernel cannot reliably communicate through global memory mid-kernel mainly because…',
      o: [
        'Global memory is read-only',
        'Both blocks may not be resident at once',
        'Blocks have separate global memories',
        'It is too slow',
      ],
      a: 1,
      x: [
        'Atomics are disabled across blocks',
        '__syncthreads spans blocks',
        'Blocks use different streams',
      ],
      e: 'Block scheduling is unconstrained: block B may not start until block A finishes. So a producer/consumer handshake between arbitrary blocks can deadlock. Grid-wide cooperation requires cooperative launch (all blocks co-resident).',
      ref: 'CUDA C++ Programming Guide §2.2; Cooperative Groups',
      tags: ['inter-block', 'scheduling', 'cooperative-groups'],
    },
    {
      d: 5,
      q: 'In a "ping-pong" (cooperative) Hopper GEMM, what overlaps to hide latency across warpgroups?',
      o: [
        'Two GPUs',
        'wgmma math overlaps the next TMA loads',
        'Host and device',
        'FP32 and FP64',
      ],
      a: 1,
      x: [
        'Two separate kernels',
        'Compute and graphics',
        'L1 and L2 caches',
      ],
      e: 'Ping-pong scheduling overlaps the math of one warpgroup with the data movement (TMA) for upcoming tiles via multi-stage shared-memory buffers and async barriers, keeping the tensor cores continuously fed.',
      ref: 'CUTLASS 3.x (Hopper kernel schedules)',
      tags: ['gemm', 'pingpong', 'pipelining', 'hopper'],
    },
    {
      d: 3,
      q: '"Wave quantization" hurts performance when…',
      o: [
        'The grid size is a multiple of the SM resident-block capacity',
        'A nearly-empty final wave runs while SMs idle',
        'There are too few registers',
        'Shared memory is unused',
      ],
      a: 1,
      x: [
        'The block size is 256',
        'The kernel uses tensor cores',
        'Occupancy is 100%',
      ],
      e: 'Blocks run in "waves" limited by resident-block capacity across all SMs. If the grid spills a small amount into an extra wave, that final wave underutilizes the GPU — the tail/wave-quantization effect. Size grids to whole waves where possible.',
      ref: 'CUDA Best Practices Guide; Nsight Compute',
      tags: ['wave-quantization', 'tail-effect', 'scheduling'],
    },
    {
      d: 4,
      q: 'Within Cooperative Groups, calling .reduce(val, op) on a 32-thread tile (CC 8.0+) may use…',
      o: [
        'Global atomics',
        'Hardware redux.sync or a shuffle reduction',
        'Shared memory only',
        'The host',
      ],
      a: 1,
      x: [
        'Constant memory',
        'A single thread looping',
        'Texture fetches',
      ],
      e: 'On Ampere+ the tile reduce can lower to redux.sync hardware reductions for integer ops, or fall back to __shfl_xor_sync trees. Cooperative Groups picks the best available implementation portably.',
      ref: 'CUDA C++ Programming Guide §C; PTX redux.sync',
      tags: ['cooperative-groups', 'reduction', 'redux'],
    },
  ],
  { idPrefix: 'execution-b' }
)
