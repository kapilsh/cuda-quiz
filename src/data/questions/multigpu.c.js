import { defineQuestions } from './_helpers.js'

// Multi-GPU & NCCL — batch C. Topology tools, env tuning, torch.distributed,
// algorithms (NVLS/one-shot), GPUDirect Storage. Distinct from multigpu.js/.b.js.
export default defineQuestions(
  'multigpu',
  [
    {
      d: 2,
      q: 'nvidia-smi topo -m prints…',
      o: [
        'GPU temperatures',
        'The connectivity matrix between GPUs/NICs (NVLink, PCIe path, NUMA affinity) — how each pair communicates',
        'Kernel timings',
        'Memory usage',
      ],
      a: 1,
      x: [
        'The driver version only',
        'Register counts',
        'The CUDA version',
      ],
      e: 'The topology matrix shows whether GPU pairs are linked by NVLink (NV#), share a PCIe switch (PIX/PXB), cross the host bridge (SYS), and their NUMA/NIC affinity — essential for understanding and debugging collective performance.',
      ref: 'nvidia-smi topo -m',
      tags: ['topology', 'nvlink', 'nvidia-smi'],
    },
    {
      d: 3,
      q: 'In PyTorch, torch.distributed.init_process_group(backend="nccl") sets up…',
      o: [
        'A single GPU',
        'The process group used for collectives, with each process getting a rank and world_size (typically one process per GPU)',
        'A data loader',
        'A CUDA stream',
      ],
      a: 1,
      x: [
        'The optimizer',
        'The model',
        'A profiler',
      ],
      e: 'init_process_group establishes the distributed group (NCCL backend for GPUs). Each process learns its rank and the world_size; collectives (all_reduce, etc.) then operate across the group. torchrun sets the env (RANK, WORLD_SIZE, LOCAL_RANK).',
      ref: 'PyTorch torch.distributed',
      tags: ['torch-distributed', 'nccl', 'process-group'],
    },
    {
      d: 2,
      q: 'In a multi-node job, LOCAL_RANK vs RANK: LOCAL_RANK identifies…',
      o: [
        'The node id',
        'The GPU index WITHIN a node (used to cudaSetDevice), while RANK is the global rank across all nodes',
        'The world size',
        'The NCCL channel',
      ],
      a: 1,
      x: [
        'The CPU core',
        'The stream id',
        'The batch index',
      ],
      e: 'LOCAL_RANK selects which GPU on the local node a process uses (set device = LOCAL_RANK); RANK is the unique global id across the whole job. Getting device binding right (one process per GPU via LOCAL_RANK) is essential for correct NCCL setup.',
      ref: 'torchrun; distributed launch',
      tags: ['local-rank', 'rank', 'device-binding'],
    },
    {
      d: 4,
      q: 'The NCCL_ALGO=NVLS option selects the "NVLink SHARP" algorithm, which…',
      o: [
        'Uses PCIe',
        'Offloads reduction into the NVSwitch fabric (in-network reduction), reducing data movement for all-reduce on NVLink-switched systems',
        'Disables NVLink',
        'Uses the CPU',
      ],
      a: 1,
      x: [
        'Compresses gradients',
        'Runs a ring only',
        'Is for single GPU',
      ],
      e: 'NVLS uses NVLink SHARP so the switch performs part of the reduction, halving endpoint traffic and lowering latency for all-reduce/reduce-scatter on NVSwitch systems. NCCL auto-selects among Ring/Tree/NVLS/CollNet by size and topology.',
      ref: 'NCCL (NVLS algorithm); NVLink SHARP',
      tags: ['nccl', 'nvls', 'sharp', 'algorithm'],
    },
    {
      d: 4,
      q: 'GPUDirect Storage (GDS) accelerates…',
      o: [
        'GPU-to-GPU copies',
        'Direct data path between storage (NVMe/filesystem) and GPU memory, bypassing a CPU bounce buffer for high-throughput I/O',
        'Collective reductions',
        'Kernel launches',
      ],
      a: 1,
      x: [
        'Tensor-core math',
        'Network packets',
        'Texture filtering',
      ],
      e: 'GDS lets the GPU DMA data directly from NVMe/storage into device memory (via the filesystem and NICs/controllers), removing the CPU staging copy — important for data-loading-bound training and large dataset I/O.',
      ref: 'NVIDIA GPUDirect Storage documentation',
      tags: ['gpudirect-storage', 'io', 'data-loading'],
    },
    {
      d: 3,
      q: 'NCCL_SOCKET_IFNAME and NCCL_IB_HCA environment variables are used to…',
      o: [
        'Set the precision',
        'Tell NCCL which network interface (sockets) or InfiniBand HCA(s) to use for inter-node communication, fixing wrong-NIC selection',
        'Choose the GPU',
        'Set the batch size',
      ],
      a: 1,
      x: [
        'Disable NCCL',
        'Pick the algorithm',
        'Lock clocks',
      ],
      e: 'On multi-NIC nodes NCCL may pick a slow/wrong interface. These vars pin it to the right Ethernet interface or IB HCAs (rails), a common fix for poor or failing multi-node collective performance.',
      ref: 'NCCL environment variables',
      tags: ['nccl', 'env', 'networking'],
    },
    {
      d: 5,
      q: 'A "one-shot" all-reduce (vs ring/two-shot) is preferable for SMALL messages on NVSwitch because…',
      o: [
        'It uses more bandwidth',
        'It minimizes the number of communication steps/latency (each GPU sends to all and reduces locally), beating bandwidth-optimal algorithms whose extra steps dominate at small sizes',
        'It compresses data',
        'It avoids NVLink',
      ],
      a: 1,
      x: [
        'It uses the CPU',
        'It is more accurate',
        'It needs fewer GPUs',
      ],
      e: 'Small collectives are latency-bound, so minimizing steps wins even if total bytes are higher. One-shot/direct algorithms (full-mesh on NVSwitch) cut latency; ring/two-shot (bandwidth-optimal) win for large messages. NCCL crosses over by size.',
      ref: 'NCCL algorithm selection; collective trade-offs',
      tags: ['nccl', 'one-shot', 'latency', 'algorithm'],
    },
    {
      d: 3,
      q: 'torch.distributed.barrier() is used to…',
      o: [
        'Reduce gradients',
        'Synchronize all processes in the group (each waits until everyone reaches the barrier) — e.g. before/after checkpoint save',
        'Broadcast weights',
        'Allocate memory',
      ],
      a: 1,
      x: [
        'Average the loss',
        'Shard the model',
        'Set the learning rate',
      ],
      e: 'A distributed barrier blocks each rank until all ranks call it, useful to coordinate phases (e.g. only rank 0 writes a file, others wait). On the NCCL backend it issues a small collective; misuse (some ranks skipping) deadlocks.',
      ref: 'PyTorch torch.distributed.barrier',
      tags: ['barrier', 'torch-distributed', 'synchronization'],
    },
    {
      d: 4,
      q: 'Scatter vs Gather collectives: Scatter…',
      o: [
        'Sums across ranks',
        'Distributes distinct chunks of a buffer from a root to each rank; Gather is the inverse (root collects each rank’s chunk)',
        'Copies one buffer to all',
        'Reduces and distributes',
      ],
      a: 1,
      x: [
        'Is the same as Broadcast',
        'Averages tensors',
        'Sorts data',
      ],
      e: 'Scatter sends chunk i of the root’s buffer to rank i; Gather collects each rank’s contribution onto the root. (Broadcast sends the SAME data to all; AllGather gives everyone all chunks.) Choose by the data-movement pattern needed.',
      ref: 'NCCL/MPI collectives',
      tags: ['scatter', 'gather', 'collectives'],
    },
    {
      d: 4,
      q: 'Reducing gradient all-reduce traffic by communicating in FP16/BF16 instead of FP32…',
      o: [
        'Is never done',
        'Halves the bytes moved (faster comm) but can lose precision; frameworks often all-reduce in the compute precision (BF16) or use FP32 for stability, sometimes with compensation',
        'Always loses accuracy badly',
        'Requires the CPU',
      ],
      a: 1,
      x: [
        'Increases the data size',
        'Is the same as compression',
        'Only works on one GPU',
      ],
      e: 'Communicating gradients in lower precision cuts all-reduce volume (helpful when comm-bound), but reduces precision of the averaged gradient. BF16 is common (its range avoids overflow); some setups keep FP32 reduction for stability.',
      ref: 'Mixed-precision distributed training; communication precision',
      tags: ['allreduce', 'precision', 'communication'],
    },
    {
      d: 4,
      q: 'For GPUDirect P2P over PCIe (no NVLink), a common requirement is that the two GPUs…',
      o: [
        'Are on different nodes',
        'Share a PCIe path that supports peer access (often under the same PCIe switch/root complex); some topologies disallow P2P, forcing host staging',
        'Use the same stream',
        'Have identical memory',
      ],
      a: 1,
      x: [
        'Run the same kernel',
        'Use unified memory',
        'Have ECC enabled',
      ],
      e: 'PCIe P2P depends on the topology: GPUs under a common switch can usually do direct P2P; crossing the CPU root complex may not support it (cudaDeviceCanAccessPeer returns false), so transfers fall back through host memory. NVLink avoids this.',
      ref: 'CUDA P2P; PCIe topology constraints',
      tags: ['p2p', 'pcie', 'topology'],
    },
    {
      d: 3,
      q: 'The torch.distributed "gloo" backend differs from "nccl" in that gloo…',
      o: [
        'Is faster on GPUs',
        'Is a CPU-oriented backend (works without GPUs, supports CPU tensors), while NCCL is the high-performance GPU collective backend',
        'Only does point-to-point',
        'Requires InfiniBand',
      ],
      a: 1,
      x: [
        'Uses tensor cores',
        'Is for single GPU only',
        'Replaces MPI',
      ],
      e: 'gloo handles CPU tensors and some operations NCCL historically didn’t, and works without CUDA; for GPU training you use the NCCL backend for performance. Some workflows use gloo for CPU-side coordination and NCCL for gradients.',
      ref: 'PyTorch distributed backends',
      tags: ['gloo', 'nccl', 'backends'],
    },
    {
      d: 5,
      q: 'NCCL user-buffer registration (registering buffers with the communicator) can improve performance by…',
      o: [
        'Compressing data',
        'Avoiding internal staging/copies — registered buffers can be used directly by zero-copy/RDMA paths, reducing overhead for repeated collectives on the same buffers',
        'Disabling the network',
        'Lowering precision',
      ],
      a: 1,
      x: [
        'Adding more channels',
        'Using the CPU',
        'Skipping the reduction',
      ],
      e: 'Registering persistent communication buffers lets NCCL set up RDMA/NVLink mappings once and reuse them, avoiding per-call registration/staging overhead — beneficial when the same buffers are all-reduced every step.',
      ref: 'NCCL user buffer registration',
      tags: ['nccl', 'buffer-registration', 'rdma'],
    },
    {
      d: 4,
      q: 'When scaling data-parallel training from 8 to 64 GPUs, all-reduce time per step ideally stays roughly constant (ring) because…',
      o: [
        'Bandwidth grows with GPUs',
        'Ring all-reduce moves ~2×(N-1)/N of the data per GPU, which approaches a constant ~2× as N grows — so per-GPU traffic is largely independent of N',
        'The data shrinks',
        'It uses the CPU',
      ],
      a: 1,
      x: [
        'Fewer gradients are sent',
        'Precision drops',
        'Latency dominates',
      ],
      e: 'Ring all-reduce is bandwidth-optimal: per-GPU traffic is ~constant regardless of N, so with adequate interconnect, step time stays flat (latency grows with N though). Real scaling also depends on overlap and network topology.',
      ref: 'Ring all-reduce scaling',
      tags: ['allreduce', 'scaling', 'ring'],
    },
    {
      d: 3,
      q: 'A multi-node run fails with "NCCL WARN ... no IB devices ... falling back to socket." The likely consequence is…',
      o: [
        'Faster training',
        'Much slower inter-node collectives, because NCCL is using TCP sockets instead of InfiniBand/RoCE',
        'A correctness error',
        'Higher precision',
      ],
      a: 1,
      x: [
        'No effect',
        'A deadlock always',
        'Less memory use',
      ],
      e: 'Socket fallback means NCCL couldn’t use the fast RDMA fabric, so inter-node bandwidth collapses to slow TCP. Fix the IB/RoCE setup (drivers, NCCL_IB_HCA, GPUDirect RDMA) so NCCL uses the high-speed network.',
      ref: 'NCCL debugging; transport fallback',
      tags: ['nccl', 'infiniband', 'fallback', 'debugging'],
    },
    {
      d: 4,
      q: 'Topology-aware rank placement (e.g. consecutive ranks on the same node/NVLink domain) matters because…',
      o: [
        'It changes the math',
        'It lets NCCL keep the heaviest communication (e.g. tensor-parallel groups) on fast NVLink while data-parallel groups span the slower inter-node network',
        'It reduces memory',
        'It increases precision',
      ],
      a: 1,
      x: [
        'It disables the network',
        'It is purely cosmetic',
        'It avoids NCCL',
      ],
      e: 'Mapping parallelism dimensions onto the hardware topology — TP within NVLink domains, PP/DP across nodes — minimizes traffic on slow links. Wrong rank-to-GPU placement can put latency-sensitive collectives on the network, hurting throughput.',
      ref: 'Megatron rank mapping; NCCL topology',
      tags: ['rank-placement', 'topology', 'parallelism'],
    },
    {
      d: 2,
      q: 'AllReduce vs ReduceScatter for distributing the reduced result: ReduceScatter leaves each rank with…',
      o: [
        'The full reduced tensor',
        'Only its shard of the reduced result (used in ZeRO/FSDP and as the first half of ring all-reduce)',
        'The original data',
        'A copy from the root',
      ],
      a: 1,
      x: [
        'Nothing',
        'A broadcast',
        'A random shard',
      ],
      e: 'ReduceScatter reduces across ranks but scatters the result so each rank holds a distinct slice — exactly what ZeRO/FSDP need for sharded gradients, and the building block (with AllGather) of ring all-reduce.',
      ref: 'NCCL collectives; ZeRO/FSDP',
      tags: ['reduce-scatter', 'fsdp', 'collectives'],
    },
    {
      d: 5,
      q: 'CollNet / in-network reduction (e.g. SHARP on InfiniBand) helps multi-NODE all-reduce by…',
      o: [
        'Using NVLink',
        'Performing reduction in the network switches (IB SHARP), so leaf nodes send less data and the switch returns the reduced result — cutting inter-node traffic',
        'Compressing gradients',
        'Using the CPU',
      ],
      a: 1,
      x: [
        'Disabling RDMA',
        'Lowering precision',
        'Skipping ranks',
      ],
      e: 'InfiniBand SHARP offloads aggregation into the switch fabric across nodes, analogous to NVLink SHARP within a node. NCCL’s CollNet path uses it to reduce endpoint bandwidth and latency for large-scale all-reduce.',
      ref: 'NCCL CollNet; IB SHARP',
      tags: ['collnet', 'sharp', 'infiniband', 'allreduce'],
    },
    {
      d: 3,
      q: 'Why must you call cudaSetDevice(LOCAL_RANK) (bind the process to its GPU) BEFORE NCCL init and allocations?',
      o: [
        'For logging',
        'So the communicator, streams, and buffers are created on the correct GPU; a wrong/default device binding causes NCCL errors or all processes hitting GPU 0',
        'To set the precision',
        'To pick the algorithm',
      ],
      a: 1,
      x: [
        'To enable tensor cores',
        'To compress data',
        'To lock clocks',
      ],
      e: 'Each process must own a distinct GPU. Binding the device first ensures NCCL and allocations target the right GPU; forgetting it (everyone on device 0) causes out-of-memory or NCCL initialization failures — a classic multi-GPU bug.',
      ref: 'NCCL setup; device binding',
      tags: ['device-binding', 'nccl', 'multi-gpu'],
    },
    {
      d: 4,
      q: 'The crossover where NCCL switches from Tree to Ring (or vice versa) depends primarily on…',
      o: [
        'The precision',
        'Message size — trees win for small (latency-bound) messages, rings for large (bandwidth-bound) ones; NCCL picks per collective/size/topology',
        'The GPU model only',
        'The number of streams',
      ],
      a: 1,
      x: [
        'The optimizer',
        'The batch size',
        'The kernel code',
      ],
      e: 'NCCL chooses algorithm (and protocol) by tuned thresholds: small messages favor low-latency trees; large ones favor bandwidth-optimal rings (or NVLS). You can inspect/override with NCCL_ALGO and see decisions via NCCL_DEBUG=INFO.',
      ref: 'NCCL tuning (algorithm selection)',
      tags: ['nccl', 'tree', 'ring', 'crossover'],
    },
    {
      d: 3,
      q: 'In pipeline parallelism across nodes, the activation handoff between stages uses…',
      o: [
        'AllReduce',
        'Point-to-point send/recv (ncclSend/Recv or P2P) of boundary activations between adjacent stage ranks',
        'Broadcast',
        'ReduceScatter',
      ],
      a: 1,
      x: [
        'AllGather',
        'Scatter',
        'A barrier',
      ],
      e: 'Pipeline stages pass activations (forward) and gradients (backward) to neighbors via point-to-point communication, not collectives. This is relatively light traffic, which is why pipeline parallelism tolerates slower inter-node links.',
      ref: 'Pipeline parallelism; NCCL P2P',
      tags: ['pipeline', 'send-recv', 'activations'],
    },
    {
      d: 4,
      q: 'NCCL collectives are deterministic given the same communicator/algorithm, but switching algorithms (ring vs tree) between runs can…',
      o: [
        'Never change results',
        'Change the floating-point reduction order, yielding slightly different (still valid) bitwise results — relevant to exact reproducibility',
        'Cause wrong answers',
        'Deadlock',
      ],
      a: 1,
      x: [
        'Change the model size',
        'Always increase error',
        'Require FP64',
      ],
      e: 'Different algorithms reduce in different orders, and FP addition is non-associative, so the bitwise result can differ between runs that pick different NCCL algorithms. For strict reproducibility, pin the algorithm/protocol and seeds.',
      ref: 'NCCL determinism; FP reproducibility',
      tags: ['nccl', 'determinism', 'floating-point'],
    },
    {
      d: 2,
      q: 'A single process controlling 8 GPUs (rather than 8 processes) is discouraged at scale mainly because…',
      o: [
        'It cannot use NCCL',
        'One host thread/context becomes a bottleneck driving 8 GPUs (launch overhead, GIL in Python, contention); one-process-per-GPU parallelizes the host side',
        'It uses more memory',
        'It disables tensor cores',
      ],
      a: 1,
      x: [
        'It is incorrect',
        'It needs InfiniBand',
        'It lowers precision',
      ],
      e: 'Single-process multi-GPU concentrates all host-side work (kernel launches, Python overhead) in one process, limiting throughput. One process per GPU spreads launch/host work across cores and matches framework launchers (torchrun/MPI).',
      ref: 'NCCL/PyTorch best practices; process-per-GPU',
      tags: ['process-per-gpu', 'host-overhead', 'scaling'],
    },
    {
      d: 5,
      q: 'Why might an 8-GPU all-reduce on an NVSwitch node achieve higher busbw than a ring of 8 GPUs connected only pairwise by NVLink?',
      o: [
        'NVSwitch compresses data',
        'NVSwitch gives full all-to-all bandwidth (any pair at full speed), so NCCL can use algorithms (NVLS/full-mesh) that aren’t limited by a fixed ring’s slowest link',
        'Rings are always faster',
        'It uses PCIe',
      ],
      a: 1,
      x: [
        'It lowers precision',
        'It uses fewer GPUs',
        'It avoids the reduction',
      ],
      e: 'A point-to-point NVLink ring is constrained by its topology; NVSwitch provides non-blocking all-to-all bandwidth, enabling NVLS/in-network reduction and full-mesh patterns that exceed the effective bandwidth of a fixed ring.',
      ref: 'NVSwitch vs NVLink ring; NCCL algorithms',
      tags: ['nvswitch', 'busbw', 'topology', 'nvls'],
    },
    {
      d: 3,
      q: 'Setting NCCL_DEBUG=INFO and seeing "via NET/IB" vs "via P2P/CUMEM" tells you…',
      o: [
        'The precision used',
        'Which transport NCCL chose for a connection (InfiniBand network vs direct GPU P2P), helping confirm it’s using the fast path',
        'The batch size',
        'The kernel name',
      ],
      a: 1,
      x: [
        'The learning rate',
        'The optimizer',
        'The block size',
      ],
      e: 'The debug log reports the transport per peer connection (NVLink P2P, shared memory, PCIe, IB). Verifying intra-node uses NVLink and inter-node uses IB (not sockets) is a key step when diagnosing slow collectives.',
      ref: 'NCCL_DEBUG=INFO; transports',
      tags: ['nccl', 'transport', 'debugging'],
    },
    {
      d: 4,
      q: 'All-to-All (used in MoE/expert parallelism) differs from All-Gather in that each rank…',
      o: [
        'Receives the same data',
        'Sends a DIFFERENT chunk to every other rank and receives a different chunk from each — a full personalized exchange',
        'Sends nothing',
        'Reduces the data',
      ],
      a: 1,
      x: [
        'Broadcasts to all',
        'Gathers all chunks identically',
        'Only talks to its neighbor',
      ],
      e: 'In All-to-All, rank i sends chunk j to rank j (personalized), unlike All-Gather where everyone ends with the same concatenation. MoE dispatch/combine uses two All-to-Alls to route tokens to expert-owning ranks and back.',
      ref: 'All-to-All; MoE communication',
      tags: ['all-to-all', 'moe', 'collectives'],
    },
    {
      d: 4,
      q: 'A correctness pitfall: calling an all_reduce only on ranks where loss is finite (skipping it on a rank that hit NaN) will…',
      o: [
        'Average correctly',
        'Hang the job — the other ranks wait forever for the missing participant in the collective',
        'Speed up training',
        'Fix the NaN',
      ],
      a: 1,
      x: [
        'Reduce memory',
        'Lower precision',
        'Change the algorithm',
      ],
      e: 'Every rank must participate in each collective. Conditionally skipping it on some ranks (e.g. due to a NaN branch) deadlocks the group. Handle such cases uniformly (e.g. all ranks proceed and zero/skip after a synchronized check).',
      ref: 'NCCL collective correctness; deadlock',
      tags: ['nccl', 'deadlock', 'control-flow'],
    },
    {
      d: 5,
      q: 'For tensor-parallel inference, the per-layer all-reduce latency can dominate. A mitigation on NVSwitch systems is…',
      o: [
        'Use PCIe',
        'Use NVLS/in-network reduction and keep the TP group within one NVLink domain to minimize all-reduce latency per layer',
        'Increase the batch only',
        'Switch to FP64',
      ],
      a: 1,
      x: [
        'Disable tensor parallelism',
        'Use sockets',
        'Add more nodes',
      ],
      e: 'TP issues an all-reduce every layer, so latency matters. Confining the TP group to a fast NVLink/NVSwitch domain and using NVLS (in-network reduction) minimizes per-layer collective latency, improving inference token throughput.',
      ref: 'TP inference; NVLS; topology',
      tags: ['tensor-parallel', 'inference', 'nvls', 'latency'],
    },
  ],
  { idPrefix: 'multigpu-c' }
)
