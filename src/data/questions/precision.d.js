import { defineQuestions } from './_helpers.js'

// Numerics & Mixed Precision — batch D. AMP autocast/GradScaler, IEEE flags,
// iterative refinement, FP64 emulation, conditioning. Distinct from precision.js/.b.js/.c.js.
export default defineQuestions(
  'precision',
  [
    {
      d: 3,
      q: 'Under AMP autocast, which operations are typically kept in FP32 rather than FP16/BF16?',
      o: [
        'Matrix multiplications',
        'Softmax, norms, loss — autocast keeps them FP32',
        'All operations',
        'Convolutions only',
      ],
      a: 1,
      x: [
        'Activations',
        'Embeddings only',
        'Element-wise adds',
      ],
      e: 'Autocast applies a per-op policy: GEMM/conv (compute-heavy, robust) run in FP16/BF16 on tensor cores, while precision-sensitive ops (softmax, normalization, loss, large reductions) stay FP32 to preserve accuracy/stability.',
      ref: 'PyTorch AMP autocast op policy',
      tags: ['amp', 'autocast', 'mixed-precision'],
    },
    {
      d: 4,
      q: 'torch.cuda.amp.GradScaler.step(optimizer) does what beyond a normal optimizer step?',
      o: [
        'Nothing different',
        'Unscale→check inf/NaN→skip if bad, else step',
        'It clips gradients',
        'It shards the model',
      ],
      a: 1,
      x: [
        'It quantizes weights',
        'It averages gradients',
        'It runs on the CPU',
      ],
      e: 'GradScaler implements dynamic loss scaling: it unscales grads, inspects for overflow, skips+backs-off on inf/NaN, and otherwise applies the step and periodically increases the scale — automating FP16 scaling. (BF16 usually doesn’t need it.)',
      ref: 'PyTorch GradScaler',
      tags: ['gradscaler', 'loss-scaling', 'amp'],
    },
    {
      d: 4,
      q: 'The nvcc flag --fmad=false changes behavior by…',
      o: [
        'Enabling fast math',
        'Disable FMA contraction; two roundings vs one',
        'Forcing FP64',
        'Removing tensor cores',
      ],
      a: 1,
      x: [
        'Disabling division',
        'Enabling denormals',
        'Changing the warp size',
      ],
      e: 'By default the compiler contracts a*b+c into one FMA (one rounding, higher accuracy and throughput). --fmad=false keeps them separate (two roundings), which can be needed for bit-exact cross-platform results but is usually slower and less accurate.',
      ref: 'NVCC --fmad; CUDA C++ Programming Guide §G',
      tags: ['fma', 'fmad', 'compiler-flags'],
    },
    {
      d: 4,
      q: 'Why does FMA (a*b+c in one step) often produce MORE accurate results than separate multiply then add?',
      o: [
        'It uses FP64',
        'One rounding: product exact before add',
        'It is slower',
        'It uses tensor cores',
      ],
      a: 1,
      x: [
        'It truncates',
        'It uses integers',
        'It avoids the add',
      ],
      e: 'FMA computes the exact product and adds c with a single final rounding, so no precision is lost rounding the intermediate product. This single-rounding property both improves accuracy and enables exact algorithms (e.g. error-free transforms).',
      ref: 'IEEE 754 FMA; numerical accuracy',
      tags: ['fma', 'accuracy', 'rounding'],
    },
    {
      d: 5,
      q: 'Mixed-precision iterative refinement solves Ax=b accurately while exploiting fast low precision by…',
      o: [
        'Solving entirely in FP16',
        'Low-prec solve + high-prec residual corrections',
        'Using only FP64',
        'Avoiding the solve',
      ],
      a: 1,
      x: [
        'Quantizing the matrix',
        'Random projection',
        'Sharding the matrix',
      ],
      e: 'You do the expensive factorization in low precision (e.g. FP16/TF32 on tensor cores), then refine x by repeatedly computing the residual b−Ax in higher precision and correcting — achieving FP64-level accuracy with much of the low-precision speed.',
      ref: 'Mixed-precision iterative refinement (Higham et al.)',
      tags: ['iterative-refinement', 'mixed-precision', 'linear-solve'],
    },
    {
      d: 5,
      q: 'Emulating higher precision from FP32 (e.g. "double-single" or the Ozaki scheme) works by…',
      o: [
        'Using FP64 hardware',
        'Unevaluated FP32 sum + error-free transforms',
        'Rounding to FP16',
        'Using integers',
      ],
      a: 1,
      x: [
        'Compressing the mantissa',
        'Using tensor cores only',
        'Quantizing',
      ],
      e: 'Splitting a number into a sum of FP32 components and using two-sum/two-product error-free transforms simulates ~double precision with FP32 (or even tensor cores). It’s used where native FP64 is slow (consumer GPUs) or to exploit fast low-precision matmul.',
      ref: 'Double-single / Ozaki scheme; error-free transforms',
      tags: ['fp64-emulation', 'double-single', 'error-free-transform'],
    },
    {
      d: 4,
      q: 'A matrix with a large condition number means…',
      o: [
        'It is easy to solve',
        'Rounding errors amplified greatly',
        'It is sparse',
        'It is symmetric',
      ],
      a: 1,
      x: [
        'It has many zeros',
        'It is identity',
        'It is small',
      ],
      e: 'The condition number bounds how much relative error in data/arithmetic propagates to the result. Ill-conditioned problems amplify rounding error, so they demand higher precision (or refinement) — a key reason HPC values FP64.',
      ref: 'Numerical analysis; condition number',
      tags: ['condition-number', 'error-amplification', 'stability'],
    },
    {
      d: 4,
      q: 'The E8M0 format used as the SCALE in microscaling (MX) formats is…',
      o: [
        'A 32-bit float',
        '8-bit exponent only; power-of-two scale',
        'An INT8',
        'A 16-bit float',
      ],
      a: 1,
      x: [
        'A sign bit only',
        'A 4-bit value',
        'A full FP8',
      ],
      e: 'MX block scales are E8M0 (8 exponent bits, no mantissa) — i.e. a power-of-two scale. Applying it is just an exponent shift, keeping the per-block scaling cheap while covering wide dynamic range for the tiny element formats (FP8/FP6/FP4).',
      ref: 'OCP Microscaling (MX) spec; E8M0 scale',
      tags: ['mx', 'e8m0', 'block-scale'],
    },
    {
      d: 3,
      q: 'Symmetric INT8 quantization commonly maps weights to the range…',
      o: [
        '[0, 255]',
        '[-127,127]; scale, no zero-point',
        '[-1, 1]',
        '[1, 256]',
      ],
      a: 1,
      x: [
        '[-128, 128]',
        '[0, 127]',
        '[-255, 255]',
      ],
      e: 'Symmetric quantization uses a zero-centered range, typically [-127, 127] (dropping -128 to stay symmetric), with a single scale and no zero-point — efficient for weights. Asymmetric (with zero-point) suits non-negative activations.',
      ref: 'INT8 quantization conventions',
      tags: ['int8', 'symmetric', 'quantization'],
    },
    {
      d: 4,
      q: 'A "saturating" cast to INT8 (vs wrapping) is important because…',
      o: [
        'It is faster',
        'Clamps vs wrapping (wrapping flips sign)',
        'It uses FP64',
        'It avoids scaling',
      ],
      a: 1,
      x: [
        'It increases range',
        'It enables tensor cores',
        'It rounds to even',
      ],
      e: 'Quantization should clamp overflows to the representable extreme; wrapping (modular) would flip a large value to the opposite sign, corrupting results. Saturating casts (and the right scale) keep quantized values sane.',
      ref: 'Quantization; saturating cast',
      tags: ['saturation', 'int8', 'casting'],
    },
    {
      d: 5,
      q: 'A compensated (Kahan / two-sum) dot product improves accuracy by…',
      o: [
        'Using FP64',
        'Captures per-add rounding error; near-exact sum',
        'Sorting the inputs',
        'Using tensor cores',
      ],
      a: 1,
      x: [
        'Truncating small terms',
        'Using integers',
        'Skipping additions',
      ],
      e: 'Kahan/compensated summation captures the low-order bits lost in each add (via two-sum) and reincorporates them, drastically reducing accumulated error for long sums/dot products without going to higher native precision.',
      ref: 'Kahan summation; compensated dot product',
      tags: ['kahan', 'compensated', 'dot-product'],
    },
    {
      d: 3,
      q: 'The smallest positive NORMAL value of FP16 is about 6.1e-5; values below that down to ~6e-8 are…',
      o: [
        'Not representable',
        'Subnormal: near-zero, reduced precision',
        'Infinity',
        'NaN',
      ],
      a: 1,
      x: [
        'Normals',
        'Integers',
        'Rounding errors',
      ],
      e: 'Below the smallest normal, subnormals fill the gap to zero with gradually fewer significant bits. FP16’s narrow range plus FTZ (flush-to-zero) is why tiny gradients underflow — motivating loss scaling. BF16’s wider exponent avoids this.',
      ref: 'IEEE 754 half; subnormals',
      tags: ['fp16', 'subnormals', 'underflow'],
    },
    {
      d: 4,
      q: 'Why are reordering FP operations (e.g. parallel tree reduction vs sequential) NOT guaranteed to give identical results?',
      o: [
        'Reordering changes the math',
        'FP non-associative: order changes rounding',
        'It causes overflow',
        'It is a bug',
      ],
      a: 1,
      x: [
        'Integers reorder too',
        'It loses data',
        'Only FP64 is affected',
      ],
      e: 'Non-associativity means (a+b)+c ≠ a+(b+c) in floating point due to rounding. Parallel reductions, vectorization, and different algorithms reorder operations, producing slightly different (all valid) results — the root of FP non-reproducibility.',
      ref: 'Floating-point non-associativity',
      tags: ['non-associativity', 'reordering', 'reproducibility'],
    },
    {
      d: 4,
      q: 'For FP8 training, the loss is still computed and the optimizer step is still done in…',
      o: [
        'FP8',
        'FP32 masters; FP8 for matmuls only',
        'INT8',
        'FP4',
      ],
      a: 1,
      x: [
        'BF16 only',
        'FP16 only',
        'On the CPU',
      ],
      e: 'FP8 accelerates the GEMMs (with per-tensor scaling), but master weights, optimizer moments, and the loss/update remain FP32 to preserve convergence. FP8 is applied where it’s robust (matmuls), not to the precision-critical accumulation/update path.',
      ref: 'FP8 training recipe; Transformer Engine',
      tags: ['fp8', 'master-weights', 'training'],
    },
    {
      d: 3,
      q: 'IEEE-compliant division/sqrt vs fast approximate versions on the GPU: the compliant ones (--prec-div=true) are…',
      o: [
        'Faster',
        'IEEE-correct but slow; fast flags trade accuracy',
        'Identical',
        'Integer-only',
      ],
      a: 1,
      x: [
        'Less accurate but slower',
        'For FP64 only',
        'Disabled by default',
      ],
      e: 'GPUs offer correctly-rounded div/sqrt (slower) and fast approximations (faster, fewer ULPs of accuracy). Flags (--prec-div, --prec-sqrt) and intrinsics (__fdividef, __frsqrt_rn) choose; --use_fast_math flips many to the fast variants.',
      ref: 'CUDA C++ Programming Guide §G; precision flags',
      tags: ['division', 'sqrt', 'precision-flags'],
    },
    {
      d: 5,
      q: 'Why can low-precision (FP16) GEMM with FP32 accumulation still be inaccurate for very large K despite FP32 accumulation?',
      o: [
        'FP32 is exact',
        'Input-level FP16 rounding accumulates over large K',
        'FP32 accumulation overflows',
        'It is always exact',
      ],
      a: 1,
      x: [
        'K is irrelevant',
        'Tensor cores are inexact',
        'It loses data',
      ],
      e: 'FP32 accumulation prevents sum-rounding error growth, but the FP16 inputs are already rounded, so each term has ~FP16 relative error. Summing many such terms accumulates that input error — large-K or ill-conditioned matmuls may need higher input precision.',
      ref: 'Mixed-precision GEMM error analysis',
      tags: ['fp16', 'accumulation', 'input-error', 'gemm'],
    },
    {
      d: 4,
      q: 'In AMP, the model weights are stored in FP32 but autocast computes matmuls in FP16/BF16. The conversion happens…',
      o: [
        'Once at startup',
        'On the fly; FP32 masters updated by optimizer',
        'Never',
        'Only in the backward pass',
      ],
      a: 1,
      x: [
        'Only for the loss',
        'On the CPU',
        'In the data loader',
      ],
      e: 'Autocast wraps the forward pass, casting operands of eligible ops to FP16/BF16 at runtime; gradients flow back and the FP32 master weights are updated. This keeps fast low-precision compute with stable FP32 parameter updates.',
      ref: 'PyTorch AMP; autocast + master weights',
      tags: ['amp', 'autocast', 'master-weights'],
    },
    {
      d: 4,
      q: 'A practical reason BF16 training often needs no GradScaler while FP16 does is…',
      o: [
        'BF16 is more precise',
        'BF16 FP32-range; FP16 tiny grads underflow',
        'BF16 uses INT8',
        'FP16 has no exponent',
      ],
      a: 1,
      x: [
        'BF16 is slower',
        'FP16 is more robust',
        'They are identical',
      ],
      e: 'Loss scaling exists to keep FP16 gradients within its narrow exponent range. BF16 shares FP32’s wide exponent range, so gradients stay representable without scaling — simplifying training (at the cost of fewer mantissa bits).',
      ref: 'BF16 vs FP16 training; loss scaling',
      tags: ['bf16', 'fp16', 'gradscaler'],
    },
    {
      d: 5,
      q: 'The "two-sum" error-free transform computes s = a+b AND the exact rounding error e such that…',
      o: [
        'e is discarded',
        'a+b = s+e exactly; e captures lost bits',
        'e is random',
        's is exact',
      ],
      a: 1,
      x: [
        'e is always zero',
        's + e overflows',
        'It needs FP64',
      ],
      e: 'two-sum returns the rounded sum and the exact error term, so a+b is represented exactly as the pair (s, e). Algorithms reincorporate e (Kahan) or carry it (double-double) to get higher effective precision from base precision.',
      ref: 'Error-free transforms; two-sum',
      tags: ['two-sum', 'error-free-transform', 'compensated'],
    },
    {
      d: 3,
      q: 'Comparing the dynamic RANGE (largest/smallest representable magnitude) of BF16 vs FP16…',
      o: [
        'FP16 has more range',
        'BF16 ~1e38; FP16 ~65504 (overflow-prone)',
        'They have equal range',
        'BF16 has less range',
      ],
      a: 1,
      x: [
        'BF16 is integer',
        'FP16 has no max',
        'Both ~1e38',
      ],
      e: 'BF16 trades mantissa for range: its FP32-like exponent gives ~1e±38 range, versus FP16’s ~6.5e4 max and ~6e-8 min. The wider range is why BF16 resists overflow/underflow in training despite coarser precision.',
      ref: 'BF16 vs FP16 dynamic range',
      tags: ['bf16', 'fp16', 'dynamic-range'],
    },
    {
      d: 4,
      q: 'Iterative solvers (e.g. CG) can require higher precision than direct methods because…',
      o: [
        'They are always exact',
        'Errors accumulate per iter; FP64 for ill-cond',
        'They use no arithmetic',
        'They are integer-based',
      ],
      a: 1,
      x: [
        'They never converge',
        'They avoid the matrix',
        'They use tensor cores',
      ],
      e: 'Each iteration injects rounding error; over many iterations (and with ill-conditioning) this can stagnate convergence or reduce attainable accuracy. HPC iterative solvers often use FP64, or low-precision inner steps with high-precision residuals.',
      ref: 'Iterative solvers; precision and convergence',
      tags: ['iterative-solvers', 'precision', 'convergence'],
    },
    {
      d: 4,
      q: 'When quantizing activations dynamically per token, the scale is computed from…',
      o: [
        'The weights',
        'That token’s own amax at runtime',
        'A fixed constant',
        'The gradient',
      ],
      a: 1,
      x: [
        'The loss',
        'The learning rate',
        'The batch mean only',
      ],
      e: 'Per-token dynamic quantization derives each token’s scale from its activations (e.g. amax), adapting to per-token range variation better than a static per-tensor scale — common in INT8/FP8 LLM inference paired with per-channel weight scales.',
      ref: 'Per-token dynamic quantization',
      tags: ['per-token', 'dynamic-quantization', 'activations'],
    },
    {
      d: 5,
      q: 'Why do tensor cores accumulate in FP32 even for FP8/INT8 inputs rather than the input precision?',
      o: [
        'For speed',
        'Products overflow; wide FP32/INT32 accumulator',
        'It is required by the API',
        'To use more memory',
      ],
      a: 1,
      x: [
        'Inputs are exact',
        'FP8 has no products',
        'It avoids scaling',
      ],
      e: 'Across the contraction dimension, products accumulate; doing so in the tiny input format would overflow (INT8) or lose precision (FP8). Wide FP32/INT32 accumulation keeps the running sum accurate, then the result is scaled/cast at the end.',
      ref: 'Tensor-core accumulation; low-precision GEMM',
      tags: ['accumulation', 'fp32', 'tensor-cores'],
    },
    {
      d: 3,
      q: 'In mixed-precision training, the final loss is typically computed in…',
      o: [
        'FP16',
        'FP32 — loss stays full precision',
        'INT8',
        'FP8',
      ],
      a: 1,
      x: [
        'BF16 only',
        'FP4',
        'On the CPU',
      ],
      e: 'The loss involves reductions (e.g. cross-entropy over the batch) sensitive to precision; computing it in FP32 avoids accuracy loss. Loss scaling then multiplies it before the FP16 backward (with GradScaler handling the rest).',
      ref: 'Mixed-precision training; loss in FP32',
      tags: ['loss', 'fp32', 'mixed-precision'],
    },
    {
      d: 4,
      q: 'A "calibration" dataset for static INT8 quantization is used to…',
      o: [
        'Train the model',
        'Observe ranges; pick scales before deploy',
        'Test accuracy only',
        'Shard the model',
      ],
      a: 1,
      x: [
        'Generate gradients',
        'Pick the optimizer',
        'Set the batch size',
      ],
      e: 'Static (post-training) quantization runs a small calibration set to estimate activation distributions and pick clipping/scales that minimize information loss. It’s cheap (no retraining) but needs representative data; poor calibration hurts accuracy.',
      ref: 'INT8 calibration; PTQ',
      tags: ['calibration', 'int8', 'ptq'],
    },
    {
      d: 5,
      q: 'Why might FP8 inference need PER-TENSOR (or finer) scales recomputed for activations but fixed scales for weights?',
      o: [
        'Weights change each step',
        'Static weights: fixed scale; activations: dynamic',
        'Activations are constant',
        'Weights are random',
      ],
      a: 1,
      x: [
        'Both are static',
        'Neither needs scaling',
        'Weights are integers',
      ],
      e: 'Fixed weights have a known range, so a precomputed scale works. Activations depend on the input and shift in magnitude per sample, so their scaling must adapt (dynamic per-tensor/token, or calibrated conservatively) to avoid overflow/underflow in FP8.',
      ref: 'FP8 inference; activation vs weight scaling',
      tags: ['fp8', 'scaling', 'activations', 'weights'],
    },
    {
      d: 4,
      q: 'The accuracy benefit of accumulating a reduction in FP32 (vs FP16) is largest when…',
      o: [
        'The array is tiny',
        'Large n or wide range; FP16 swamping grows',
        'All values are equal',
        'Values are integers',
      ],
      a: 1,
      x: [
        'There is one element',
        'The sum is zero',
        'Using tensor cores',
      ],
      e: 'Error grows with the number of additions and magnitude disparity. For long or wide-ranging reductions, FP16 accumulation swamps small terms and accumulates error; FP32 accumulation (or compensated summation) preserves accuracy — hence its use in softmax/norms.',
      ref: 'Summation error; FP32 accumulation',
      tags: ['accumulation', 'fp32', 'reduction', 'error'],
    },
    {
      d: 5,
      q: 'Stochastic rounding implemented in hardware for FP8/BF16 accumulation can REMOVE the need for an FP32 master copy because…',
      o: [
        'It increases the exponent',
        'Probabilistic rounding preserves increments',
        'It is exact',
        'It uses FP64',
      ],
      a: 1,
      x: [
        'It disables denormals',
        'It sorts the updates',
        'It compresses weights',
      ],
      e: 'Round-to-nearest drops tiny updates (they’re below the ULP), which is why FP32 masters exist. Stochastic rounding makes the expected accumulated value correct, so low-precision weights can accumulate small gradients directly — potentially eliminating the master copy.',
      ref: 'Stochastic rounding; master-weight elimination',
      tags: ['stochastic-rounding', 'master-weights', 'accumulation'],
    },
    {
      d: 3,
      q: 'For numerically reproducible results across runs on the SAME GPU, a key setting is…',
      o: [
        'Use FP16',
        'Fixed reduction order + deterministic algorithms',
        'Increase the batch size',
        'Use more streams',
      ],
      a: 1,
      x: [
        'Use atomics everywhere',
        'Enable fast math',
        'Use more GPUs',
      ],
      e: 'Run-to-run reproducibility requires removing nondeterminism: deterministic library algorithms (fixed reduction order, no atomic-based reductions), consistent seeds, and matching precision settings (e.g. TF32 on/off). Atomics and autotuning can otherwise vary results.',
      ref: 'Determinism; reproducibility settings',
      tags: ['determinism', 'reproducibility', 'tf32'],
    },
  ],
  { idPrefix: 'precision-d' }
)
