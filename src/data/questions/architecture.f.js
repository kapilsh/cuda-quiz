import { defineQuestions } from './_helpers.js'

// GPU Architecture — batch F. Specs/throughput numbers, generation comparisons,
// design rationale scenarios. Distinct from architecture.js/.b/.c/.d/.e.
export default defineQuestions(
  'architecture',
  [
    {
      d: 4,
      q: 'H100 SXM has approximately how many SMs?',
      o: ['66', '108', '132', '256'],
      a: 2,
      x: ['80', '144', '96'],
      e: 'H100 SXM (GH100) exposes ~132 SMs (A100 had 108). The SM count, with resident-blocks-per-SM, sets how many blocks run concurrently — sizing grids to fill SMs (whole waves) avoids the tail effect.',
      ref: 'NVIDIA H100 (GH100) whitepaper',
      tags: ['sm-count', 'h100', 'specs'],
    },
    {
      d: 4,
      q: 'A100 has 108 SMs; H100 has ~132. Beyond SM count, H100’s per-SM advances include…',
      o: [
        'Nothing',
        'TMA, async wgmma tensor cores, thread-block clusters/DSMEM, larger shared memory, and FP8 support — architectural features, not just more SMs',
        'A different ISA entirely',
        'No tensor cores',
      ],
      a: 1,
      x: [
        'Fewer registers',
        'Smaller L2',
        'Slower clocks only',
      ],
      e: 'H100 (Hopper) adds TMA, async warpgroup MMA (wgmma), clusters/DSMEM, FP8 tensor cores, and bigger shared memory/L2 — new data paths that need code changes to exploit, beyond the SM-count increase.',
      ref: 'NVIDIA Hopper Whitepaper',
      tags: ['hopper', 'features', 'generations'],
    },
    {
      d: 4,
      q: 'Each SM has 4 tensor cores (one per sub-partition). On a 132-SM H100 that totals…',
      o: ['132', '264', '528', '4'],
      a: 2,
      x: ['1056', '64', '128'],
      e: '4 tensor cores/SM × 132 SMs = 528 tensor cores. Their aggregate throughput (per precision/sparsity) drives the headline matmul FLOP/s; feeding them (TMA/shared memory/pipelining) is the practical performance limiter.',
      ref: 'NVIDIA H100; tensor cores per SM',
      tags: ['tensor-cores', 'count', 'h100'],
    },
    {
      d: 4,
      q: 'Why does FP4 roughly double inference throughput vs FP8 on Blackwell tensor cores?',
      o: [
        'Higher clocks',
        'FP4 packs twice the elements per byte of FP8, so the tensor cores process about double the elements per cycle — with micro-scaling preserving enough accuracy',
        'More SMs',
        'Larger HBM',
      ],
      a: 1,
      x: [
        'Software only',
        'FP4 is more precise',
        'For graphics',
      ],
      e: 'Halving bit-width (FP8→FP4) doubles the elements a tensor core handles per cycle. Micro-scaling (NVFP4) keeps accuracy acceptable, so supported inference roughly doubles in throughput — the low-precision scaling trend.',
      ref: 'NVIDIA Blackwell; FP4',
      tags: ['fp4', 'throughput', 'blackwell'],
    },
    {
      d: 4,
      q: 'The register file per SM (~256 KB = 65,536 32-bit registers) is large because…',
      o: [
        'Registers are cheap',
        'Latency hiding keeps many warps resident, each needing private fast storage simultaneously — the big register file holds all those live contexts for zero-overhead warp switching',
        'It caches global memory',
        'For graphics',
      ],
      a: 1,
      x: [
        'It replaces shared memory',
        'For FP64',
        'For the host',
      ],
      e: 'Throughput design keeps up to 2048 threads resident, each with private registers. The 256 KB register file holds all their state so the scheduler switches warps with no save/restore — the hardware basis of latency hiding (and why register use caps occupancy).',
      ref: 'NVIDIA SM; register file',
      tags: ['register-file', 'occupancy', 'design'],
    },
    {
      d: 4,
      q: 'L2 is a single device-wide cache (not per-SM) primarily because…',
      o: [
        'It is cheaper',
        'It must capture reuse ACROSS blocks/SMs and serve as the device-wide coherence point for global atomics — per-SM L2 couldn’t share data or order atomics between SMs',
        'L2 is slower',
        'For graphics',
      ],
      a: 1,
      x: [
        'For FP64',
        'To replace HBM',
        'For the host',
      ],
      e: 'Cross-block reuse and coherent global atomics need a cache visible to all SMs — the shared L2. Per-SM L1s are private and non-coherent, so the unified L2 is where device-wide sharing and atomic ordering occur (and where hot atomics contend).',
      ref: 'GPU memory hierarchy; L2',
      tags: ['l2-cache', 'coherence', 'design'],
    },
    {
      d: 5,
      q: 'Blackwell B200’s two dies linked by NV-HBI present to software as…',
      o: [
        'Two GPUs',
        'A SINGLE CUDA device with one unified memory space — the coherent ~10 TB/s die-to-die link services cross-die accesses transparently, preserving the single-GPU model',
        'A CPU+GPU pair',
        'Two nodes',
      ],
      a: 1,
      x: [
        'Two contexts',
        'A MIG instance',
        'For graphics',
      ],
      e: 'NV-HBI’s bandwidth/coherence lets the two dies appear as one device with one memory space, so kernels/allocations work as on a single GPU. This sidesteps the reticle limit on single-die size while keeping the familiar programming model.',
      ref: 'NVIDIA Blackwell; NV-HBI',
      tags: ['blackwell', 'multi-die', 'nv-hbi'],
    },
    {
      d: 4,
      q: 'A consumer RTX GPU and a data-center A100 share the Ampere ISA, but the A100 is far better for HPC FP64 because it has…',
      o: [
        'A different ISA',
        'Dedicated FP64 units (and FP64 tensor cores) giving ~1:2 FP64:FP32, versus the consumer GPU’s tiny FP64 ratio (~1:32–1:64)',
        'More registers',
        'A bigger warp',
      ],
      a: 1,
      x: [
        'A faster CPU',
        'No tensor cores',
        'For graphics',
      ],
      e: 'Data-center parts dedicate real FP64 hardware (1:2 ratio, plus FP64 tensor cores) for HPC; consumer GPUs minimize FP64 to save area. So FP64-heavy codes run far faster on A100/H100 despite the shared architecture family.',
      ref: 'NVIDIA FP64 ratios; datacenter vs consumer',
      tags: ['fp64', 'datacenter-vs-consumer', 'ampere'],
    },
    {
      d: 4,
      q: 'TMA reduces register pressure vs cp.async because…',
      o: [
        'It compresses data',
        'A single thread issues a descriptor-driven bulk copy, so threads no longer compute per-element addresses or hold them in registers — hardware generates the addresses',
        'It uses constant memory',
        'For graphics',
      ],
      a: 1,
      x: [
        'It avoids shared memory',
        'For FP64',
        'For the host',
      ],
      e: 'cp.async had each lane compute/hold addresses (register- and instruction-heavy). TMA offloads multidimensional address generation to hardware via a tensor map; one thread triggers the tile copy, freeing registers/issue slots for compute.',
      ref: 'Hopper TMA; register pressure',
      tags: ['tma', 'registers', 'hopper'],
    },
    {
      d: 4,
      q: 'Why does each successive architecture grow per-SM shared memory (48→96→164→228 KB)?',
      o: [
        'To reduce registers',
        'Larger on-chip tiles enable more reuse and deeper pipelines to feed the rising tensor-core throughput without stalling on HBM — capacity scales with compute',
        'For graphics',
        'To replace HBM',
      ],
      a: 1,
      x: [
        'To shrink L2',
        'For FP64',
        'For the host',
      ],
      e: 'As tensor cores get faster, kernels need bigger operand tiles and more pipeline stages on-chip to keep them fed. Growing shared-memory capacity per SM supports that reuse/pipelining, which is why each generation increases it.',
      ref: 'Shared-memory scaling; generations',
      tags: ['shared-memory', 'generations', 'reuse'],
    },
    {
      d: 5,
      q: 'GB200 NVL72 connects 72 Blackwell GPUs into one NVLink domain so that…',
      o: [
        'They share one die',
        'Communication-heavy parallelism (tensor/expert) can span all 72 GPUs at NVLink bandwidth — the rack behaves like one large accelerator for trillion-parameter models',
        'They use PCIe',
        'They run on one CPU',
      ],
      a: 1,
      x: [
        'They share HBM physically',
        'They use InfiniBand internally',
        'For graphics',
      ],
      e: 'NVL72’s NVLink-switched domain gives all 72 GPUs high all-to-all bandwidth, so latency/bandwidth-sensitive collectives (TP/EP) scale across them efficiently — enabling models far beyond an 8-GPU node as if on one machine.',
      ref: 'NVIDIA GB200 NVL72',
      tags: ['nvl72', 'nvlink', 'scale'],
    },
    {
      d: 4,
      q: 'INT8 tensor-core throughput is typically what multiple of FP16 tensor-core throughput?',
      o: ['Half', '~2×', 'Same', '10×'],
      a: 1,
      x: ['4×', '1/4', 'Unrelated'],
      e: 'Halving bit-width roughly doubles tensor-core math rate: INT8 ≈ 2× FP16, FP8 similar to INT8, FP4 ≈ 2× FP8. This throughput scaling is the motivation for quantized inference, traded against accuracy.',
      ref: 'NVIDIA tensor-core throughput',
      tags: ['int8', 'throughput', 'tensor-cores'],
    },
    {
      d: 4,
      q: 'The SM-to-SM network on Hopper exists specifically to enable…',
      o: [
        'Faster global memory',
        'Distributed shared memory (DSMEM): a block accessing a peer cluster block’s shared memory directly over a low-latency intra-GPC interconnect',
        'NVLink',
        'A bigger L2',
      ],
      a: 1,
      x: [
        'Tensor cores',
        'For graphics',
        'For the host',
      ],
      e: 'DSMEM requires one SM to read/write another SM’s shared memory; the dedicated SM-to-SM network within a GPC provides that low-latency path for co-scheduled cluster blocks — far faster than going through global memory.',
      ref: 'Hopper; SM-to-SM network',
      tags: ['sm-to-sm', 'dsmem', 'hopper'],
    },
    {
      d: 4,
      q: 'Why does sustained (not peak) performance depend on the GPU’s power/thermal limits?',
      o: [
        'It does not',
        'At high utilization the GPU may hit its power/thermal cap and throttle clocks, lowering sustained throughput and adding variance — adequate cooling/power and locked clocks stabilize it',
        'For graphics only',
        'For FP64 only',
      ],
      a: 1,
      x: [
        'For the host',
        'To save memory',
        'For atomics',
      ],
      e: 'Peak specs assume full clocks; under heavy load power/thermal limits can throttle the GPU, reducing sustained performance and adding run-to-run variance. Provisioning power/cooling (and locking clocks for benchmarks) addresses it.',
      ref: 'GPU throttling; sustained performance',
      tags: ['throttling', 'power', 'sustained'],
    },
    {
      d: 5,
      q: 'Blackwell’s Tensor Memory (TMEM) holding tensor-core accumulators (vs the register file) addresses…',
      o: [
        'Memory bandwidth',
        'Register-file pressure: wider/higher-throughput MMAs would need too many registers for accumulators; TMEM offloads that so tensor throughput scales without exhausting registers',
        'Clock speed',
        'Network bandwidth',
      ],
      a: 1,
      x: [
        'Shared memory',
        'L2 size',
        'Host memory',
      ],
      e: 'As MMA tiles grow, register-held accumulators bottleneck occupancy/throughput. Dedicated Tensor Memory stores accumulators/operands separately, letting the tensor cores scale without the register file constraining them — but kernels must use the new instructions.',
      ref: 'NVIDIA Blackwell; TMEM',
      tags: ['blackwell', 'tmem', 'register-pressure'],
    },
    {
      d: 3,
      q: 'The work distributor assigns blocks to SMs in an unspecified, dynamic order so that…',
      o: [
        'It confuses programmers',
        'Uneven block runtimes are load-balanced and the same grid scales across GPUs with different SM counts — block independence makes this safe',
        'To save power',
        'For graphics',
      ],
      a: 1,
      x: [
        'For FP64',
        'For atomics',
        'For coalescing',
      ],
      e: 'Dynamic block-to-SM assignment balances load (blocks vary in runtime) and makes code portable across GPU sizes. The cost is no guaranteed block ordering/co-residency — true grid cooperation needs cooperative launch or clusters.',
      ref: 'CUDA scalability; block scheduling',
      tags: ['block-scheduling', 'load-balancing', 'scalability'],
    },
    {
      d: 4,
      q: 'H200 improves on H100 primarily via…',
      o: [
        'More SMs',
        'HBM3e: higher memory capacity (~141 GB) and bandwidth (~4.8 TB/s), helping memory/capacity-bound LLM inference — the compute is the same Hopper',
        'A new ISA',
        'FP4 tensor cores',
      ],
      a: 1,
      x: [
        'GDDR memory',
        'A second die',
        'Lower precision',
      ],
      e: 'H200 keeps Hopper compute but upgrades to HBM3e (more capacity/bandwidth). For bandwidth/capacity-bound inference (large weights, KV cache, decode), this yields significant gains over H100 without changing the compute architecture.',
      ref: 'NVIDIA H200 specs',
      tags: ['h200', 'hbm3e', 'inference'],
    },
    {
      d: 4,
      q: 'Why are tensor cores far more efficient (per area/Watt) than CUDA cores for matmul?',
      o: [
        'Higher clocks',
        'One tensor-core instruction does a whole small matrix multiply-accumulate, amortizing fetch/decode/schedule over many MACs and using dense systolic-like datapaths — vs one FMA per CUDA-core instruction',
        'They use the CPU',
        'They avoid memory',
      ],
      a: 1,
      x: [
        'They use FP64',
        'More registers',
        'For graphics',
      ],
      e: 'Doing many MACs per instruction amortizes control overhead and uses efficient dense datapaths, giving far higher throughput-per-watt/area for GEMM-shaped work than CUDA cores’ one-FMA-per-instruction — at the cost of generality.',
      ref: 'Tensor-core design; efficiency',
      tags: ['tensor-cores', 'efficiency', 'design'],
    },
    {
      d: 4,
      q: 'A kernel runs identically on a 20-SM laptop GPU and a 132-SM data-center GPU (just faster on the latter) because…',
      o: [
        'Magic',
        'Block independence + dynamic scheduling: the same grid’s blocks are mapped to whatever SMs exist, so more SMs simply run more blocks concurrently — no code change needed',
        'A fixed grid',
        'The CPU',
      ],
      a: 1,
      x: [
        'Shared L1',
        'NVLink',
        'For FP64',
      ],
      e: 'CUDA’s scalability comes from independent blocks scheduled dynamically onto available SMs. The bigger GPU runs more blocks at once; the smaller one runs them in more waves — identical results, different throughput, same binary.',
      ref: 'CUDA scalability model',
      tags: ['scalability', 'block-scheduling', 'portability'],
    },
    {
      d: 5,
      q: 'Why must thread-block clusters fit within ONE GPC?',
      o: [
        'For convenience',
        'Cluster features (DSMEM, cluster.sync) rely on the intra-GPC SM-to-SM network and co-residency guarantee; spanning GPCs would lack that low-latency interconnect and co-scheduling',
        'For FP64',
        'For graphics',
      ],
      a: 1,
      x: [
        'To save power',
        'For the host',
        'For atomics',
      ],
      e: 'A cluster’s blocks are co-scheduled on SMs of a single GPC, which provides the SM-to-SM network DSMEM/cluster.sync need. Crossing GPCs would break the co-residency guarantee and lack the interconnect, so clusters are GPC-scoped.',
      ref: 'Hopper clusters; GPC scope',
      tags: ['clusters', 'gpc', 'hopper'],
    },
    {
      d: 4,
      q: 'The achievable fraction of peak tensor FLOP/s on a real GEMM is usually limited by…',
      o: [
        'The ISA',
        'How well the operand-feeding path (shared memory/TMA/registers/pipelining) keeps the tensor cores fed — feeding, not the tensor cores themselves, is the practical limit',
        'The SM count',
        'The host',
      ],
      a: 1,
      x: [
        'The warp size',
        'The clock only',
        'For FP64',
      ],
      e: 'Tensor cores can consume operands faster than naive feeding supplies them, so sustained GEMM efficiency depends on the data path (tiling, async copy/TMA, pipelining, conflict-free swizzle). Peak FLOPs assume perfect feeding rarely fully achieved.',
      ref: 'GEMM efficiency; operand feeding',
      tags: ['tensor-cores', 'feeding', 'gemm'],
    },
    {
      d: 4,
      q: 'NVLink (Hopper, 4th gen) per-GPU aggregate bandwidth is ~900 GB/s; PCIe Gen5 x16 is ~64 GB/s bidirectional. The implication for multi-GPU training is…',
      o: [
        'PCIe is fine',
        'Communication-heavy parallelism (tensor/expert) needs NVLink; PCIe-only links (~14× less bandwidth) bottleneck collectives, so heavy comm is confined to NVLink domains',
        'They are equal',
        'NVLink is slower',
      ],
      a: 1,
      x: [
        'PCIe is faster',
        'For FP64',
        'For graphics',
      ],
      e: 'NVLink offers roughly an order of magnitude more GPU-to-GPU bandwidth than PCIe. So latency/bandwidth-sensitive collectives (TP/EP) map to NVLink/NVSwitch domains, while PCIe-tolerant traffic (pipeline P2P, overlappable DP all-reduce) can cross slower links.',
      ref: 'NVLink vs PCIe; multi-GPU',
      tags: ['nvlink', 'pcie', 'bandwidth'],
    },
    {
      d: 4,
      q: 'Why are both CUDA cores and tensor cores present on each SM?',
      o: [
        'Redundancy',
        'Real kernels mix general work (indexing, activations, softmax, reductions → CUDA cores) with dense matmul (→ tensor cores); both are needed and often run cooperatively',
        'Tensor cores are backups',
        'CUDA cores are deprecated',
      ],
      a: 1,
      x: [
        'They never run together',
        'For graphics',
        'For FP64',
      ],
      e: 'A GEMM-heavy kernel still needs CUDA cores for address math, epilogue activations, and reductions, while tensor cores do the matmul. Co-locating them lets the SM handle the whole computation efficiently with cooperative use of both.',
      ref: 'SM design; CUDA + tensor cores',
      tags: ['cuda-cores', 'tensor-cores', 'sm'],
    },
    {
      d: 5,
      q: 'The recurring per-generation theme enabling higher tensor throughput is…',
      o: [
        'Higher clocks only',
        'Lower-precision formats (FP16→TF32/BF16→FP8→FP4) PLUS better operand feeding (cp.async→TMA) and larger cooperative MMA units (warp→warpgroup→wider) — with scaling techniques to keep accuracy',
        'More FP64',
        'Bigger warps',
      ],
      a: 1,
      x: [
        'Smaller registers',
        'Removing tensor cores',
        'Slower memory',
      ],
      e: 'Each generation pushes precision down (more MACs/byte), improves how operands reach the tensor cores, and enlarges the cooperative MMA granularity — together driving the headline throughput gains, alongside scaling (micro-scaling, Transformer Engine) for accuracy.',
      ref: 'NVIDIA architecture evolution',
      tags: ['tensor-cores', 'generations', 'trends'],
    },
    {
      d: 3,
      q: 'The L2 cache on H100 is roughly…',
      o: ['6 MB', '20 MB', '50 MB', '256 KB'],
      a: 2,
      x: ['10 MB', '128 MB', '2 MB'],
      e: 'H100 has ~50 MB L2 (A100 ~40 MB). A large L2 captures cross-block reuse and reduces HBM traffic; sizing working sets/tiles to fit L2 (and the persisting window) keeps reused data on-chip.',
      ref: 'NVIDIA H100; L2 size',
      tags: ['l2-cache', 'h100', 'specs'],
    },
    {
      d: 4,
      q: 'Why does Confidential Computing on Hopper matter for some deployments?',
      o: [
        'Faster kernels',
        'It isolates/encrypts GPU workloads (memory, PCIe traffic) in a trusted execution environment, protecting sensitive models/data from the host and other tenants in untrusted/multi-tenant settings',
        'More memory',
        'Better tensor cores',
      ],
      a: 1,
      x: [
        'Compression',
        'Lower precision',
        'Faster NVLink',
      ],
      e: 'Hopper Confidential Computing provides hardware-based isolation/encryption of GPU compute so models/data stay protected on untrusted hosts or shared infrastructure — important for regulated or multi-tenant secure deployments.',
      ref: 'NVIDIA Hopper Confidential Computing',
      tags: ['confidential-computing', 'security', 'hopper'],
    },
    {
      d: 4,
      q: 'A100 (HBM2e ~2 TB/s) vs H100 (HBM3 ~3.35 TB/s): for a purely memory-bound decode kernel, the speedup ceiling is about…',
      o: [
        '1×',
        '~1.7× (the bandwidth ratio) — a bandwidth-bound kernel scales with memory bandwidth, so ~3.35/2 ≈ 1.7×',
        '10×',
        'No change',
      ],
      a: 1,
      x: ['3×', '0.5×', 'Unbounded'],
      e: 'A bandwidth-bound kernel’s runtime scales inversely with bandwidth. H100’s ~3.35 TB/s vs A100’s ~2 TB/s gives a ceiling of ~1.7×, independent of compute. (Compute-bound kernels would scale with FLOP/s instead.)',
      ref: 'Bandwidth scaling; memory-bound',
      tags: ['bandwidth', 'scaling', 'memory-bound'],
    },
    {
      d: 4,
      q: 'MIG partitions an H100 into isolated instances because…',
      o: [
        'It overclocks',
        'Some workloads (inference, multi-tenant) underuse a whole GPU; hardware partitioning gives each tenant guaranteed, isolated SMs/L2/memory with predictable QoS',
        'It increases bandwidth',
        'It enables tensor cores',
      ],
      a: 1,
      x: [
        'It shares one context',
        'It compresses memory',
        'For graphics',
      ],
      e: 'MIG carves the GPU into hardware-isolated instances (dedicated compute, cache, memory) so multiple jobs share it with guaranteed slices and no interference — ideal for serving many small models where a full GPU per job wastes capacity.',
      ref: 'NVIDIA MIG',
      tags: ['mig', 'partitioning', 'isolation'],
    },
    {
      d: 5,
      q: 'Why does async wgmma (vs synchronous mma.sync) help keep Hopper tensor cores busy?',
      o: [
        'More accuracy',
        'Async issue lets the warpgroup launch an MMA and immediately issue the next operand loads (TMA/async copy) without blocking — overlapping data movement with math so the cores don’t idle between MMAs',
        'It uses the CPU',
        'Less memory',
      ],
      a: 1,
      x: [
        'It avoids accumulation',
        'Lower precision',
        'For graphics',
      ],
      e: 'Synchronous MMA blocks until done, serializing math and loads. Async wgmma decouples issue from completion, so the warpgroup overlaps the next tiles’ loads with the in-flight MMA — sustaining continuous tensor-core utilization.',
      ref: 'Hopper wgmma; async pipelining',
      tags: ['wgmma', 'async', 'tensor-cores'],
    },
    {
      d: 4,
      q: 'The memory subsystem interleaves addresses across many channels; a tiling/access pattern that camps on a few channels…',
      o: [
        'Maximizes bandwidth',
        'Underutilizes aggregate DRAM bandwidth (partition camping) — distributing accesses across all channels engages the full bandwidth',
        'Has no effect',
        'Improves coalescing',
      ],
      a: 1,
      x: [
        'Uses the L1',
        'For FP64',
        'For graphics',
      ],
      e: 'DRAM bandwidth is the sum over channels/partitions; concentrating accesses on a few (partition camping) leaves others idle. Address patterns (tiling/block mapping) that spread across partitions realize the full bandwidth.',
      ref: 'Memory partitions; partition camping',
      tags: ['partition-camping', 'bandwidth', 'channels'],
    },
  ],
  { idPrefix: 'architecture-f' }
)
