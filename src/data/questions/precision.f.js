import { defineQuestions } from './_helpers.js'

// Numerics & Mixed Precision — batch F. Calculation/scenario questions: scaling,
// error bounds, format choices, stability bugs. Distinct from precision.js/.b/.c/.d/.e.
export default defineQuestions(
  'precision',
  [
    {
      d: 3,
      q: 'Storing a 1B-element buffer in FP16 instead of FP32 changes its size from 4 GB to…',
      o: ['4 GB', '2 GB', '1 GB', '8 GB'],
      a: 1,
      x: ['3 GB', '0.5 GB', '16 GB'],
      e: '1e9 × 2 bytes = 2 GB (vs 4 GB in FP32). Halving element size halves memory AND bandwidth, so a bandwidth-bound kernel on this data can run up to ~2× faster — independent of any tensor-core math benefit.',
      ref: 'FP16 vs FP32; size/bandwidth',
      tags: ['fp16', 'bandwidth', 'calculation'],
    },
    {
      d: 4,
      q: 'FP16 training loss stalls and gradients are mostly zero. The fix is…',
      o: [
        'Lower the learning rate',
        'Loss scale: FP16 grads in range; unscale after',
        'Use FP64',
        'More GPUs',
      ],
      a: 1,
      x: [
        'Disable gradients',
        'Larger batch',
        'Bank conflicts',
      ],
      e: 'FP16’s small exponent range underflows tiny gradients to zero. Scaling the loss up shifts gradients into range (unscaled before the update). BF16’s FP32-like range usually avoids the issue entirely (no loss scaling needed).',
      ref: 'Loss scaling; FP16 underflow',
      tags: ['loss-scaling', 'fp16', 'underflow'],
    },
    {
      d: 4,
      q: 'An FP8 (E4M3) tensor with values up to ~5000 overflows to inf without scaling because…',
      o: [
        'FP8 has no exponent',
        'E4M3 max ~448; scale down or overflow',
        'FP8 is integer',
        'It is fine',
      ],
      a: 1,
      x: [
        'E4M3 max is 65504',
        'It needs FP64',
        'For graphics',
      ],
      e: 'E4M3 maxes near 448. A tensor with larger magnitudes must be scaled into FP8’s window (per-tensor/block scale from amax). Without scaling, values above 448 saturate to inf — corrupting the computation.',
      ref: 'FP8 E4M3; scaling',
      tags: ['fp8', 'e4m3', 'scaling', 'overflow'],
    },
    {
      d: 4,
      q: 'Uniform INT8 quantization with scale s introduces a per-element rounding error of at most…',
      o: ['s', 's/2', '2s', '0'],
      a: 1,
      x: ['s²', 's/4', '1/s'],
      e: 'Rounding to the nearest quantization level gives at most ±s/2 error per value — the fundamental quantization error. Smaller scale (more bits, finer groups) reduces it; the trade-off is memory/bandwidth.',
      ref: 'Quantization error',
      tags: ['int8', 'quantization-error', 'calculation'],
    },
    {
      d: 4,
      q: 'A 70B model’s weights in W4A16 (4-bit weights) occupy about how much vs FP16 weights?',
      o: [
        'Same',
        '~35 GB vs ~140 GB FP16 (~4×)',
        '~1/2',
        '~1/16',
      ],
      a: 1,
      x: ['~1/8', 'Larger', '~1/3'],
      e: '70B × 4 bits ≈ 35 GB vs 70B × 16 bits ≈ 140 GB — a 4× reduction. Since decode is bandwidth-bound on reading weights, W4A16 (4-bit weights, FP16 activations) substantially speeds it with small accuracy loss.',
      ref: 'W4A16; weight memory',
      tags: ['w4a16', 'memory', 'calculation'],
    },
    {
      d: 4,
      q: 'Machine epsilon (gap above 1.0) is LARGER for which: FP16 (10 mantissa bits) or BF16 (7)?',
      o: [
        'FP16',
        'BF16 7-bit mantissa: coarser ε than FP16',
        'They are equal',
        'Neither has epsilon',
      ],
      a: 1,
      x: ['FP32', 'INT8', 'TF32'],
      e: 'ε depends on mantissa bits: BF16 (7) ⇒ ε ≈ 2^-8 ≈ 0.0039; FP16 (10) ⇒ ε ≈ 2^-11 ≈ 0.00049. BF16 is coarser near 1.0 but covers FP32’s exponent range — the precision-vs-range trade.',
      ref: 'Machine epsilon; BF16 vs FP16',
      tags: ['machine-epsilon', 'bf16', 'fp16'],
    },
    {
      d: 4,
      q: 'A softmax over FP16 logits produces NaN. The most likely cause and fix are…',
      o: [
        'Underflow; scale up',
        'Subtract max; largest = exp(0)=1, sum in FP32',
        'Division by the count',
        'A race condition',
      ],
      a: 1,
      x: [
        'Bank conflicts',
        'Too few threads',
        'Use FP64 inputs',
      ],
      e: 'Large logits overflow exp in FP16. The standard fix subtracts the max before exponentiating (the shift cancels) so the largest term is exp(0)=1 — and accumulate the denominator in FP32. This is the numerically stable softmax used in attention.',
      ref: 'Stable softmax; FP16 overflow',
      tags: ['softmax', 'nan', 'stability'],
    },
    {
      d: 3,
      q: 'For large-model training, BF16 is chosen over FP16 mainly because…',
      o: [
        'BF16 is more precise',
        'BF16 range avoids FP16 overflow/scaling issues',
        'BF16 uses less memory',
        'FP16 is unsupported',
      ],
      a: 1,
      x: [
        'BF16 is integer',
        'FP16 has no exponent',
        'For FP64',
      ],
      e: 'At scale FP16’s narrow range makes training finicky (overflow, loss scaling). BF16 shares FP32’s range, so gradients rarely over/underflow — no loss scaling needed. The coarser mantissa is usually fine, making BF16 the default for large-model training.',
      ref: 'BF16 vs FP16; training default',
      tags: ['bf16', 'fp16', 'training'],
    },
    {
      d: 4,
      q: 'In mixed-precision Adam, master weights and the moments (m, v) are kept in FP32 because…',
      o: [
        'FP32 is faster',
        'Tiny updates lost in 16-bit; FP32 preserves',
        'It saves memory',
        'The GPU requires it',
      ],
      a: 1,
      x: [
        'For graphics',
        'To use tensor cores',
        'It is random',
      ],
      e: 'Small updates added to comparatively large 16-bit weights round away (swamping); moments need precision too. Keeping FP32 master weights and Adam states preserves convergence, while matmuls run in low precision. ZeRO/FSDP then shard these FP32 states.',
      ref: 'Mixed-precision Adam; master weights',
      tags: ['adam', 'fp32', 'master-weights'],
    },
    {
      d: 4,
      q: 'Enabling TF32 (default on Ampere+) for an FP32 matmul means results are…',
      o: [
        'Bit-identical to FP32',
        'TF32: ~10-bit mantissa; not bit-exact FP32',
        'Lower than FP16',
        'Integer',
      ],
      a: 1,
      x: [
        'Always exact',
        'FP64',
        'For graphics',
      ],
      e: 'TF32 keeps FP32’s exponent but truncates the mantissa to ~10 bits for tensor-core speed (accumulating in FP32). Results are close to but not exactly FP32; set allow_tf32=False (or pedantic math) when bit-exact FP32 matters.',
      ref: 'TF32; bit-exactness',
      tags: ['tf32', 'precision', 'frameworks'],
    },
    {
      d: 5,
      q: 'Hardware stochastic rounding lets BF16 weight updates accumulate small gradients without an FP32 master because…',
      o: [
        'It is exact',
        'Probabilistic rounding; expected value correct',
        'It increases range',
        'It disables denormals',
      ],
      a: 1,
      x: [
        'It uses FP64',
        'It sorts updates',
        'For graphics',
      ],
      e: 'Round-to-nearest drops sub-ULP updates (swamping), which is why FP32 masters exist. Stochastic rounding makes the expected update correct, letting low-precision weights accumulate tiny gradients directly — potentially removing the master copy.',
      ref: 'Stochastic rounding; low-precision training',
      tags: ['stochastic-rounding', 'bf16', 'accumulation'],
    },
    {
      d: 4,
      q: 'INT8 activation quantization loses accuracy due to a few large-magnitude OUTLIER channels. SmoothQuant fixes this by…',
      o: [
        'Removing outliers',
        'Per-channel scale shifts burden to weights',
        'Using FP64',
        'Skipping quantization',
      ],
      a: 1,
      x: [
        'Pruning the model',
        'Per-tensor only',
        'For graphics',
      ],
      e: 'Outlier channels force a coarse activation scale. SmoothQuant rescales channels to shift the range burden onto weights (easier to quantize), keeping the product equal — enabling accurate INT8 activations + weights (W8A8).',
      ref: 'SmoothQuant',
      tags: ['smoothquant', 'outliers', 'quantization'],
    },
    {
      d: 4,
      q: 'Per-channel weight quantization beats per-tensor when…',
      o: [
        'It uses fewer bits',
        'Per-channel scales fit; per-tensor wastes',
        'Always identical',
        'For FP64',
      ],
      a: 1,
      x: [
        'Per-tensor is finer',
        'For activations only',
        'For graphics',
      ],
      e: 'Per-tensor scale is set by the largest-range channel, crushing precision elsewhere. Per-channel (per-output) scales adapt to each channel’s range, markedly improving INT8/INT4 weight accuracy at negligible cost.',
      ref: 'Per-channel quantization',
      tags: ['per-channel', 'quantization', 'weights'],
    },
    {
      d: 5,
      q: 'FP4 (E2M1) needs micro-scaling (per-block scale) but FP16 does not because…',
      o: [
        'FP4 is integer',
        'FP4’s ~16 values need per-block scale',
        'FP16 is integer',
        'They both need it',
      ],
      a: 1,
      x: [
        'FP4 has more bits',
        'FP16 has no exponent',
        'For FP64',
      ],
      e: 'With only a handful of FP4 codes and minimal range, one scale per tensor destroys accuracy; per-block (micro) scales adapt to local magnitude (MXFP4/NVFP4). FP16’s range/precision suffices that per-tensor (or no) scaling works for typical values.',
      ref: 'FP4 micro-scaling vs FP16',
      tags: ['fp4', 'microscaling', 'fp16'],
    },
    {
      d: 4,
      q: 'In FP8 training, gradients use E5M2 (vs E4M3 for weights/activations) because…',
      o: [
        'Gradients need precision',
        'E5M2 wide exponent covers gradient dynamic range',
        'Gradients are integer',
        'For graphics',
      ],
      a: 1,
      x: [
        'E5M2 has more mantissa',
        'For FP64',
        'They are constant',
      ],
      e: 'The backward pass yields wide-magnitude gradients; E5M2 (5 exponent bits) represents that range, avoiding overflow/underflow. Forward weights/activations use E4M3 (more mantissa) for precision — the standard FP8 mixed-format recipe.',
      ref: 'FP8 E4M3/E5M2 roles',
      tags: ['fp8', 'e5m2', 'gradients'],
    },
    {
      d: 4,
      q: 'A reduction via FP atomicAdd gives slightly different sums each run. To make it deterministic you…',
      o: [
        'Use more threads',
        'Fixed-order tree reduction; atomic order varies',
        'Use relaxed atomics',
        'Use FP64 atomics',
      ],
      a: 1,
      x: [
        'Use system-scope atomics',
        'Increase occupancy',
        'For graphics',
      ],
      e: 'Atomics are correct but their order varies with scheduling, and FP addition isn’t associative — so the rounded sum differs run to run. A fixed-order tree reduction applies additions in the same sequence every run, giving reproducible results.',
      ref: 'FP atomic nondeterminism; deterministic reduction',
      tags: ['atomics', 'determinism', 'reduction'],
    },
    {
      d: 5,
      q: 'Mixed-precision iterative refinement solves Ax=b to FP64 accuracy using tensor cores by…',
      o: [
        'Solving in FP16 only',
        'Low-prec solve; high-prec residuals correct',
        'Avoiding the solve',
        'Using integers',
      ],
      a: 1,
      x: [
        'Using only FP64',
        'Random projection',
        'For graphics',
      ],
      e: 'The expensive factorization runs in fast low precision; the residual (computed in higher precision) drives corrections that converge to high accuracy. This exploits tensor-core throughput for the heavy step — the HPL-MxP approach.',
      ref: 'Mixed-precision iterative refinement; HPL-MxP',
      tags: ['iterative-refinement', 'tensor-cores', 'fp64'],
    },
    {
      d: 4,
      q: 'Training NaNs appear in FP16 after a few steps. The usual root cause to check first is…',
      o: [
        'A bug in CUDA',
        'Overflow → inf → NaN; scale too high',
        'BF16 storage',
        'A race condition',
      ],
      a: 1,
      x: [
        'Bank conflicts',
        'Too few threads',
        'Low occupancy',
      ],
      e: 'FP16 NaNs usually stem from overflow (inf) feeding invalid ops (inf−inf, 0×inf), frequently from an aggressive loss scale. Dynamic loss scaling detects the inf/NaN, skips the step, and lowers the scale — the standard mitigation.',
      ref: 'FP16 NaN debugging; loss scaling',
      tags: ['nan', 'fp16', 'overflow', 'debugging'],
    },
    {
      d: 4,
      q: 'With loss scaling, gradient clipping by global norm must…',
      o: [
        'Clip before unscaling',
        'Unscale first, then clip to the true norm',
        'Skip clipping',
        'Clip the loss',
      ],
      a: 1,
      x: [
        'Clip the activations',
        'Multiply the clip value by the scale',
        'Use FP64',
      ],
      e: 'Loss scaling multiplies gradients by S; clipping must operate on the real norm, so AMP unscales (and checks inf/NaN) before applying gradient clipping and the optimizer step. Clipping the scaled gradients would clip at the wrong magnitude.',
      ref: 'AMP; gradient clipping order',
      tags: ['gradient-clipping', 'loss-scaling', 'amp'],
    },
    {
      d: 4,
      q: 'A long FP16 reduction (millions of terms) stops increasing partway through. The cause is…',
      o: [
        'Integer overflow',
        'Swamping: small addends below ULP, vanish',
        'A race',
        'Denormal flushing',
      ],
      a: 1,
      x: [
        'NaN propagation',
        'Bank conflicts',
        'Loss scaling',
      ],
      e: 'At large magnitudes FP16’s ULP exceeds the small addends, so the sum stagnates (swamping). Accumulate in FP32 (or use compensated/pairwise summation) for long reductions — a classic low-precision pitfall (also why softmax sums in FP32).',
      ref: 'Swamping; FP32 accumulation',
      tags: ['fp16', 'swamping', 'reduction'],
    },
    {
      d: 4,
      q: 'Tensor cores multiply FP8/FP16 inputs but accumulate in FP32 because…',
      o: [
        'For speed',
        'Products overflow in low prec; FP32 accumulates',
        'It is required by the API',
        'To use more memory',
      ],
      a: 1,
      x: [
        'Inputs are exact',
        'FP8 has no products',
        'For graphics',
      ],
      e: 'Across the contraction dimension, products accumulate; doing so in the tiny input format would overflow (or lose precision). FP32 accumulation keeps the running sum accurate, then the result is scaled/cast — standard for mixed-precision GEMM.',
      ref: 'Tensor-core accumulation',
      tags: ['accumulation', 'fp32', 'tensor-cores'],
    },
    {
      d: 4,
      q: 'Quantizing the KV cache to FP8 halves its memory but requires a scale because…',
      o: [
        'FP8 has no exponent',
        'FP8 narrow range; K/V need per-head scale',
        'KV is integer',
        'KV is constant',
      ],
      a: 1,
      x: [
        'It avoids attention',
        'For FP64',
        'KV is tiny',
      ],
      e: 'Like any FP8 tensor, the KV cache needs scaling to fit FP8’s small range. FP8 KV (with suitable scales) cuts cache memory/bandwidth ~2× for decode, but mis-scaling degrades attention — so the scale choice matters.',
      ref: 'FP8 KV cache; scaling',
      tags: ['fp8', 'kv-cache', 'scaling'],
    },
    {
      d: 5,
      q: 'INT8 matmul accumulates in INT32 (not INT8) because…',
      o: [
        'INT8 can’t multiply',
        'INT8 products overflow; INT32 accumulates',
        'For speed',
        'To use tensor cores',
      ],
      a: 1,
      x: [
        'To store weights',
        'To avoid scaling',
        'For graphics',
      ],
      e: 'INT8×INT8 products and their sums grow fast; INT8 tensor cores accumulate in INT32, then dequantize/scale. Accumulating in 8-bit would overflow, so a wider accumulator is essential.',
      ref: 'INT8 GEMM; INT32 accumulation',
      tags: ['int8', 'accumulation', 'int32'],
    },
    {
      d: 4,
      q: 'Computing log(1+x) for tiny x as log1p(x) (not log(1+x)) is more accurate because…',
      o: [
        'It is faster',
        'x swamped in 1+x; log1p accurate for small x',
        'It avoids the log',
        'It uses integers',
      ],
      a: 1,
      x: [
        'It is required by IEEE',
        'For FP64 only',
        'For graphics',
      ],
      e: 'When x ≪ 1, the addition 1+x rounds away x’s low-order bits, so log(1+x) is inaccurate. log1p is designed for accuracy at small arguments; expm1 is the analog for eˣ−1. Use them for numerically sensitive small-value math.',
      ref: 'log1p/expm1; numerical stability',
      tags: ['log1p', 'expm1', 'stability'],
    },
    {
      d: 4,
      q: 'A numerically stable variance computation (e.g. in LayerNorm) uses Welford’s algorithm to…',
      o: [
        'Run faster',
        'Stable one-pass mean+variance',
        'Use integers',
        'Avoid the mean',
      ],
      a: 1,
      x: [
        'Need two passes',
        'Use FP64 only',
        'For graphics',
      ],
      e: 'The naive sum-of-squares-minus-square-of-sum subtracts large nearly-equal values, losing precision. Welford updates running mean and M2 incrementally, staying stable — important for normalization layers, especially in low precision.',
      ref: "Welford's algorithm; stable variance",
      tags: ['welford', 'variance', 'layernorm'],
    },
    {
      d: 4,
      q: 'For inference, W8A8 (INT8 weights AND activations) suits PREFILL while W4A16 suits DECODE because…',
      o: [
        'They are the same',
        'Prefill: compute-bound; decode: bandwidth-bound',
        'Decode is compute-bound',
        'For FP64',
      ],
      a: 1,
      x: [
        'Prefill is bandwidth-bound',
        'They use the CPU',
        'For graphics',
      ],
      e: 'Prefill’s big matmuls benefit from INT8 compute (W8A8); decode’s bottleneck is reading weights, so weight-only 4-bit (W4A16) minimizes that bandwidth while keeping activations FP16. Different phases, different optimal precision schemes.',
      ref: 'W8A8 prefill; W4A16 decode',
      tags: ['w8a8', 'w4a16', 'prefill-decode'],
    },
    {
      d: 5,
      q: 'A delayed-scaling FP8 tensor suddenly spikes in magnitude and produces inf. The cause ("scale lag") is…',
      o: [
        'A CUDA bug',
        'Stale scale; magnitude spike → overflow',
        'Underflow',
        'A race',
      ],
      a: 1,
      x: [
        'Bank conflicts',
        'For FP64',
        'For graphics',
      ],
      e: 'Delayed scaling sets the scale from past amax; a sudden magnitude increase can overflow the stale scale before it updates. Mitigations: a safety margin, clipping, faster amax response, or current scaling for tensors with volatile ranges.',
      ref: 'FP8 delayed scaling; scale lag',
      tags: ['fp8', 'delayed-scaling', 'overflow'],
    },
    {
      d: 4,
      q: 'Why does attention accumulate scores/softmax in FP32 even on FP16/BF16 hardware?',
      o: [
        'API requirement',
        'Many-term sums degrade in 16-bit; FP32',
        'FP16 has no exp',
        'For speed',
      ],
      a: 1,
      x: [
        'To use more memory',
        'For graphics',
        'It does not',
      ],
      e: 'Attention sums over the sequence (scores, softmax denominator, weighted values); 16-bit accumulation loses accuracy/overflows for long sequences. FP32 accumulation (as in FlashAttention) keeps it stable while inputs/outputs can be 16-bit.',
      ref: 'Attention; FP32 accumulation',
      tags: ['attention', 'fp32', 'accumulation'],
    },
    {
      d: 4,
      q: 'Two mathematically-equal reductions (sequential vs parallel tree) give slightly different FP results because…',
      o: [
        'A bug',
        'FP non-associative: order changes rounding',
        'Overflow',
        'Integer truncation',
      ],
      a: 1,
      x: [
        'One loses data',
        'For FP64 only',
        'For graphics',
      ],
      e: 'Non-associativity means (a+b)+c ≠ a+(b+c) in floating point. Sequential vs tree reductions reorder operations, producing slightly different rounded results — the reason parallel/vectorized reductions aren’t bitwise-identical to a serial loop.',
      ref: 'FP non-associativity; reproducibility',
      tags: ['non-associativity', 'reduction', 'reproducibility'],
    },
    {
      d: 5,
      q: 'The same model gives slightly different outputs on two GPU architectures because…',
      o: [
        'The weights change',
        'Different kernels/TF32 paths change rounding',
        'Different ISAs corrupt data',
        'It should be bit-identical',
      ],
      a: 1,
      x: [
        'It is non-deterministic by design',
        'Memory is incoherent',
        'Seeds differ',
      ],
      e: 'Cross-hardware bitwise reproducibility isn’t guaranteed: kernels pick different algorithms/reduction orders, may use TF32/tensor cores differently, and FP non-associativity makes the rounding differ. Outputs agree to tolerance, not exactly.',
      ref: 'Cross-hardware reproducibility',
      tags: ['reproducibility', 'cross-hardware', 'floating-point'],
    },
    {
      d: 4,
      q: 'FP6 (e.g. E3M2) is useful as a middle ground because…',
      o: [
        'It is integer',
        'Between FP4 and FP8 in precision and size',
        'It has no exponent',
        'It is larger than FP8',
      ],
      a: 1,
      x: [
        'It is 16-bit',
        'It is lossless',
        'For graphics',
      ],
      e: 'FP6 (6 bits) sits between FP4 and FP8 in precision/footprint. With micro-scaling it holds accuracy where FP4 degrades while saving over FP8 — part of Blackwell’s low-precision toolkit for tuning the accuracy/size trade-off.',
      ref: 'FP6; MX formats',
      tags: ['fp6', 'mxfp', 'precision'],
    },
    {
      d: 5,
      q: 'Why is low-precision generally safer for DL TRAINING than for HPC scientific computing?',
      o: [
        'Science uses integers',
        'DL: noise-tolerant; HPC ill-conditioned, FP64',
        'Training is exact',
        'They are identical',
      ],
      a: 1,
      x: [
        'Training needs FP64',
        'Science is random',
        'For graphics',
      ],
      e: 'Neural-net training tolerates noise (stochastic optimization, approximate objectives), so low precision rarely breaks it. HPC simulations/solvers can be ill-conditioned and accumulate error, often requiring FP64 — hence the differing precision needs.',
      ref: 'Precision needs; DL vs HPC',
      tags: ['low-precision', 'training', 'hpc'],
    },
  ],
  { idPrefix: 'precision-f' }
)
