import { defineQuestions } from './_helpers.js'

// Streams, Events & Graphs — batch B. Per-thread default stream, priorities,
// callbacks, stream-ordered allocation, graph nodes/update. Distinct from streams.js.
export default defineQuestions(
  'streams',
  [
    {
      d: 3,
      q: 'Compiling with --default-stream per-thread changes the legacy default stream so that…',
      o: [
        'It is removed entirely',
        'Per-thread non-synchronizing default stream',
        'It becomes higher priority',
        'It runs on the device only',
      ],
      a: 1,
      x: [
        'It serializes all threads',
        'It disables async copies',
        'It merges all streams into one',
      ],
      e: 'Per-thread default stream gives each CPU thread an independent non-synchronizing default stream, removing the legacy null-stream serialization and improving concurrency in multi-threaded host code without rewriting to explicit streams.',
      ref: 'CUDA C++ Programming Guide §6.2.8.5; NVCC --default-stream',
      tags: ['default-stream', 'per-thread', 'concurrency'],
    },
    {
      d: 3,
      q: 'cudaStreamCreateWithPriority lets you…',
      o: [
        'Run streams on different GPUs',
        'Favor this stream’s work over lower-priority streams',
        'Make a stream synchronous',
        'Increase the stream’s bandwidth',
      ],
      a: 1,
      x: [
        'Set the block size',
        'Pin the stream to an SM',
        'Disable the default stream',
      ],
      e: 'Stream priorities bias the scheduler so high-priority stream work is favored (e.g. a latency-critical kernel over background work). Query the valid range with cudaDeviceGetStreamPriorityRange; it is a hint, not strict preemption.',
      ref: 'CUDA Runtime API (stream priorities)',
      tags: ['stream-priority', 'scheduling'],
    },
    {
      d: 4,
      q: 'cudaStreamWaitEvent(streamB, event) makes streamB…',
      o: [
        'Record a timestamp',
        'Pause until a cross-stream event completes',
        'Run on the host',
        'Stop permanently',
      ],
      a: 1,
      x: [
        'Synchronize the whole device',
        'Copy memory',
        'Increase its priority',
      ],
      e: 'It inserts a wait in streamB on an event from streamA, so producer/consumer work across streams is correctly ordered without blocking the host. This is the building block for expressing DAG-like dependencies among streams.',
      ref: 'CUDA Runtime API (cudaStreamWaitEvent)',
      tags: ['events', 'cross-stream', 'dependencies'],
    },
    {
      d: 3,
      q: 'Creating events with cudaEventCreateWithFlags(..., cudaEventDisableTiming) is recommended when…',
      o: [
        'You need precise timing',
        'Events used only for ordering, not timing',
        'You profile the kernel',
        'You want host callbacks',
      ],
      a: 1,
      x: [
        'You measure elapsed time',
        'You use a single stream',
        'You need higher priority',
      ],
      e: 'Timing-enabled events carry timestamp overhead. For pure ordering (cudaStreamWaitEvent), cudaEventDisableTiming (often with cudaEventBlockingSync) reduces overhead. Use timing-enabled events only when you’ll call cudaEventElapsedTime.',
      ref: 'CUDA Runtime API (event flags)',
      tags: ['events', 'flags', 'overhead'],
    },
    {
      d: 4,
      q: 'cudaLaunchHostFunc (or the older cudaStreamAddCallback) enqueues a host function in a stream. A strict rule is that the host function…',
      o: [
        'Must allocate device memory',
        'Must NOT call any CUDA runtime/driver API',
        'Must run a kernel',
        'Must synchronize the device',
      ],
      a: 1,
      x: [
        'Must be a __global__ function',
        'Must return a cudaError_t',
        'Must use pinned memory',
      ],
      e: 'The host callback runs when the stream reaches that point, but calling CUDA APIs from it is forbidden and can deadlock. Use it for CPU-side bookkeeping/signaling that must be ordered with GPU work, not for issuing GPU work.',
      ref: 'CUDA Runtime API (cudaLaunchHostFunc)',
      tags: ['host-callback', 'stream', 'restrictions'],
    },
    {
      d: 4,
      q: 'The stream-ordered memory allocator (cudaMallocAsync / cudaFreeAsync with memory pools) improves over cudaMalloc by…',
      o: [
        'Allocating on the host',
        'Pooled stream-ordered alloc/free; no device sync',
        'Doubling memory',
        'Disabling caching',
      ],
      a: 1,
      x: [
        'Pinning all memory',
        'Removing the need to free',
        'Compressing allocations',
      ],
      e: 'cudaMalloc/cudaFree synchronize the device; cudaMallocAsync/cudaFreeAsync schedule alloc/free on a stream using a memory pool, so they don’t stall concurrency and can reuse freed blocks cheaply — important for graphs and high-frequency alloc.',
      ref: 'CUDA C++ Programming Guide (Stream Ordered Memory Allocator)',
      tags: ['cudaMallocAsync', 'memory-pool', 'stream-ordered'],
    },
    {
      d: 4,
      q: 'A CUDA Graph is a DAG of nodes. Which is NOT a typical graph node type?',
      o: ['Kernel node', 'Memcpy node', 'Host function node', 'Compiler node'],
      a: 3,
      x: ['Memset node', 'Child-graph node', 'Event-record node'],
      e: 'Graphs contain kernel, memcpy, memset, host, child-graph, event record/wait, and (newer) memory alloc/free and conditional nodes — but not a "compiler" node. The DAG edges encode dependencies.',
      ref: 'CUDA C++ Programming Guide §6.2.9 (Graph node types)',
      tags: ['cuda-graphs', 'nodes', 'dag'],
    },
    {
      d: 4,
      q: 'cudaGraphExecUpdate (or cudaGraphExecKernelNodeSetParams) lets you…',
      o: [
        'Recompile the graph',
        'Patch node params in place (no re-instantiation)',
        'Run the graph on the host',
        'Change the GPU',
      ],
      a: 1,
      x: [
        'Add new nodes at runtime',
        'Profile the graph',
        'Capture a new graph',
      ],
      e: 'Re-instantiation is costly. If only parameters change between iterations (different buffers, sizes), updating the executable graph in place lets you replay it cheaply — keeping the launch-overhead win across iterations.',
      ref: 'CUDA C++ Programming Guide (graph update)',
      tags: ['cuda-graphs', 'update', 'parameters'],
    },
    {
      d: 3,
      q: 'A GPU typically has separate copy engines for H2D and D2H. The benefit is…',
      o: [
        'Faster kernels',
        'H2D + D2H + compute concurrently',
        'Larger memory',
        'Lower latency loads',
      ],
      a: 1,
      x: [
        'More registers',
        'Coalesced access',
        'Higher occupancy',
      ],
      e: 'With independent copy engines, you can stream data in (H2D) while results stream out (D2H) and kernels run — triple overlap. Pinned memory and multiple streams are required to exploit it (e.g. classic pipelined chunked processing).',
      ref: 'CUDA Best Practices Guide §9; copy engines',
      tags: ['copy-engines', 'overlap', 'transfers'],
    },
    {
      d: 3,
      q: 'cudaStreamQuery(stream) is useful because it…',
      o: [
        'Blocks until the stream is done',
        'Non-blocking poll: done (cudaSuccess) or still running',
        'Cancels the stream',
        'Times the stream',
      ],
      a: 1,
      x: [
        'Synchronizes the device',
        'Raises the priority',
        'Flushes the cache',
      ],
      e: 'cudaStreamQuery polls completion without blocking the host, letting you do other CPU work and check progress. cudaEventQuery does the same for an event. Use it to build non-blocking progress loops.',
      ref: 'CUDA Runtime API (cudaStreamQuery)',
      tags: ['stream', 'query', 'non-blocking'],
    },
    {
      d: 5,
      q: 'When capturing a graph that includes a cuBLAS or NCCL call, the library call must…',
      o: [
        'Be skipped',
        'Be capture-safe: only issue capturable stream work',
        'Run on the host',
        'Use the default stream',
      ],
      a: 1,
      x: [
        'Be replaced by a custom kernel',
        'Allocate with cudaMalloc',
        'Disable tensor cores',
      ],
      e: 'During capture, operations are recorded, not run. cuBLAS/NCCL operations that are capture-safe enqueue their kernels (no disallowed synchronizations) so the whole iteration — including library calls — becomes a replayable graph.',
      ref: 'CUDA Graphs capture; cuBLAS/NCCL graph support',
      tags: ['cuda-graphs', 'capture', 'libraries'],
    },
    {
      d: 2,
      q: 'How many kernels can execute CONCURRENTLY on a GPU (resources permitting)?',
      o: [
        'Exactly one',
        'Several, on different streams with spare SM capacity',
        'Unlimited, always',
        'Only kernels from different processes',
      ],
      a: 1,
      x: [
        'Only two',
        'One per SM',
        'Only on multiple GPUs',
      ],
      e: 'GPUs support concurrent kernel execution: kernels on independent streams can co-reside if one kernel doesn’t already saturate the SMs. Small/underutilizing kernels benefit most; a full-occupancy kernel leaves no room for concurrency.',
      ref: 'CUDA C++ Programming Guide (Concurrent Kernel Execution)',
      tags: ['concurrent-kernels', 'streams', 'occupancy'],
    },
    {
      d: 3,
      q: 'cudaEventElapsedTime between two events is only valid when…',
      o: [
        'The events are on different GPUs',
        'Both events on the same device with timing enabled',
        'The host clock is synced',
        'A kernel is running',
      ],
      a: 1,
      x: [
        'Timing was disabled',
        'They are in different processes',
        'The stream is the default',
      ],
      e: 'Elapsed time is computed from device timestamps, so both events must be on the same device and created with timing enabled (not cudaEventDisableTiming). Cross-device timing isn’t meaningful with this API.',
      ref: 'CUDA Runtime API (cudaEventElapsedTime)',
      tags: ['events', 'timing', 'same-device'],
    },
    {
      d: 3,
      q: 'A stream created with cudaStreamNonBlocking differs from the regular created stream in that it…',
      o: [
        'Cannot run kernels',
        'No implicit sync with the legacy null stream',
        'Has higher priority',
        'Runs on the host',
      ],
      a: 1,
      x: [
        'Is always faster',
        'Disables events',
        'Uses pinned memory',
      ],
      e: 'By default a created stream still synchronizes with the legacy null stream. The cudaStreamNonBlocking flag removes that implicit ordering, so it runs truly concurrently with default-stream work — important for overlap.',
      ref: 'CUDA Runtime API (cudaStreamCreateWithFlags)',
      tags: ['stream', 'non-blocking', 'default-stream'],
    },
    {
      d: 4,
      q: 'For an inference server running the same small model repeatedly, CUDA Graphs help most by…',
      o: [
        'Making each kernel faster internally',
        'Capture the fixed kernel sequence once; replay cuts overhead',
        'Reducing memory',
        'Increasing precision',
      ],
      a: 1,
      x: [
        'Compressing the model',
        'Using tensor cores',
        'Avoiding the network',
      ],
      e: 'Fixed-shape inference is the ideal graph case: dozens of tiny kernels per step where launch overhead dominates. One capture + cheap replays remove that overhead and tighten the timeline, often a large latency win.',
      ref: 'CUDA Graphs; inference serving',
      tags: ['cuda-graphs', 'inference', 'launch-overhead'],
    },
    {
      d: 2,
      q: 'To pipeline a large array through the GPU, you split it into chunks and, per chunk, issue H2D copy, kernel, and D2H copy on a rotating set of streams. The goal is to…',
      o: [
        'Reduce memory usage',
        'Overlap transfers with compute across chunks',
        'Increase occupancy',
        'Avoid kernels',
      ],
      a: 1,
      x: [
        'Lower precision',
        'Use fewer registers',
        'Avoid pinned memory',
      ],
      e: 'Classic streamed pipelining: while chunk i computes, chunk i+1 copies in and chunk i−1 copies out, on separate streams with pinned memory. Throughput approaches the slowest stage instead of the sum of all stages.',
      ref: 'CUDA Best Practices Guide §9.1 (overlapping transfers)',
      tags: ['pipelining', 'streams', 'overlap'],
    },
    {
      d: 4,
      q: 'Why might launching many kernels on the SAME stream prevent overlap even though they are small?',
      o: [
        'Same-stream kernels run on the host',
        'Single stream is strictly ordered; ops cannot overlap',
        'Streams cannot run kernels',
        'Small kernels never overlap',
      ],
      a: 1,
      x: [
        'They use too many registers',
        'The default stream is faster',
        'Events block them',
      ],
      e: 'Within one stream, ordering is strict and sequential. To overlap independent small kernels you must place them on different streams (and have spare SM capacity). Same-stream work is serialized by definition.',
      ref: 'CUDA C++ Programming Guide §6.2.8',
      tags: ['streams', 'ordering', 'concurrency'],
    },
    {
      d: 5,
      q: 'Conditional and loop nodes (newer CUDA Graph features) let a captured graph…',
      o: [
        'Run only on the host',
        'Dynamic if/while inside the graph, no re-capture',
        'Compress kernels',
        'Skip synchronization',
      ],
      a: 1,
      x: [
        'Change the GPU mid-graph',
        'Allocate unlimited memory',
        'Replace streams',
      ],
      e: 'Conditional nodes allow runtime-decided branches/loops within a graph, extending graphs beyond strictly static DAGs to workloads with data-dependent control flow while keeping the low-overhead replay benefit.',
      ref: 'CUDA C++ Programming Guide (conditional graph nodes)',
      tags: ['cuda-graphs', 'conditional-nodes', 'control-flow'],
    },
    {
      d: 3,
      q: 'cudaStreamBeginCapture has modes (Global, ThreadLocal, Relaxed). Capture mode controls…',
      o: [
        'The kernel priority',
        'Strictness of unsafe-op detection during capture',
        'The graph’s memory size',
        'The replay speed',
      ],
      a: 1,
      x: [
        'The number of nodes',
        'The stream priority',
        'The device selection',
      ],
      e: 'Capture mode governs how the runtime guards against disallowed operations (like cudaDeviceSynchronize) during capture and how it scopes that checking across host threads — Global being strictest, Relaxed most permissive.',
      ref: 'CUDA C++ Programming Guide (stream capture modes)',
      tags: ['cuda-graphs', 'capture-mode', 'safety'],
    },
    {
      d: 4,
      q: 'After building a cudaGraph_t, you must call cudaGraphInstantiate before launching because…',
      o: [
        'It copies the graph to the host',
        'Bakes the graph into cudaGraphExec_t for fast replay',
        'It frees the graph',
        'It runs the graph once',
      ],
      a: 1,
      x: [
        'It compiles PTX',
        'It validates the GPU',
        'It pins memory',
      ],
      e: 'The template graph (topology) is instantiated into an executable form once (the expensive step), then cudaGraphLaunch replays it many times cheaply. Amortizing instantiation across many launches is where the savings come from.',
      ref: 'CUDA C++ Programming Guide (cudaGraphInstantiate/Launch)',
      tags: ['cuda-graphs', 'instantiate', 'launch'],
    },
    {
      d: 2,
      q: 'Events serve two distinct purposes in CUDA. They are…',
      o: [
        'Allocation and deallocation',
        'GPU timing and cross-stream ordering/sync',
        'Compilation and linking',
        'Kernel launch and teardown',
      ],
      a: 1,
      x: [
        'Logging and debugging',
        'Compression and encryption',
        'Profiling and tracing only',
      ],
      e: 'A cudaEvent_t marks a point in a stream. With timing you measure durations (cudaEventElapsedTime); for synchronization you wait (cudaEventSynchronize) or order other streams (cudaStreamWaitEvent).',
      ref: 'CUDA C++ Programming Guide §6.2.8.7',
      tags: ['events', 'timing', 'synchronization'],
    },
    {
      d: 4,
      q: 'A reason cudaMallocAsync pairs especially well with CUDA Graphs is that…',
      o: [
        'It disables the pool',
        'Capturable; no device-wide sync',
        'It runs on the host',
        'It compresses memory',
      ],
      a: 1,
      x: [
        'It needs no free',
        'It doubles bandwidth',
        'It pins host memory',
      ],
      e: 'Plain cudaMalloc/cudaFree synchronize and can’t be captured. The stream-ordered allocator integrates with graphs (memory nodes) and avoids those syncs, so allocation can live inside captured, replayable iterations.',
      ref: 'CUDA Graphs + stream-ordered allocator',
      tags: ['cudaMallocAsync', 'cuda-graphs', 'capture'],
    },
    {
      d: 3,
      q: 'You want a low-latency kernel to run ahead of background work competing for the GPU. The mechanism is…',
      o: [
        'A larger block size',
        'Run it on a high-priority stream',
        'cudaDeviceSynchronize',
        'Constant memory',
      ],
      a: 1,
      x: [
        'More registers',
        'A separate process',
        'Pinned memory',
      ],
      e: 'Stream priorities let the scheduler prefer the high-priority stream’s blocks when streams contend for SMs — useful to keep a latency-critical task responsive amid throughput work. It is a scheduling hint, not strict preemption.',
      ref: 'CUDA Runtime API (stream priorities)',
      tags: ['stream-priority', 'latency', 'scheduling'],
    },
    {
      d: 5,
      q: 'In a tight training loop, replacing per-step Python/host launches with a captured CUDA Graph mainly attacks which bottleneck?',
      o: [
        'GPU memory bandwidth',
        'CPU overhead + inter-kernel idle gaps',
        'Tensor-core throughput',
        'Network bandwidth',
      ],
      a: 1,
      x: [
        'Disk I/O',
        'Register pressure',
        'ECC overhead',
      ],
      e: 'When a step is many small kernels, Python + CUDA launch overhead can leave the GPU starved between kernels. Capturing the step as a graph collapses that to one launch, closing the gaps — a common speedup for small-model or short-sequence training.',
      ref: 'CUDA Graphs in training (e.g. PyTorch CUDA graphs)',
      tags: ['cuda-graphs', 'cpu-bound', 'training'],
    },
    {
      d: 2,
      q: 'Overlapping a memcpy with a kernel requires the memcpy to be…',
      o: [
        'A blocking cudaMemcpy on the default stream',
        'cudaMemcpyAsync from pinned memory on a different stream',
        'Larger than the kernel’s data',
        'Done before the kernel launches',
      ],
      a: 1,
      x: [
        'On the same default stream',
        'From pageable memory',
        'Synchronous',
      ],
      e: 'Concurrency needs an asynchronous copy (cudaMemcpyAsync) from page-locked memory issued on a stream that can run alongside the kernel’s stream. A blocking copy or pageable memory or shared default-stream ordering prevents overlap.',
      ref: 'CUDA Best Practices Guide §9.1',
      tags: ['overlap', 'memcpy-async', 'pinned-memory'],
    },
    {
      d: 4,
      q: 'Why is it usually better to create a fixed pool of streams and reuse them rather than creating/destroying streams frequently?',
      o: [
        'Streams cannot be destroyed',
        'Small pool reused; avoids create/destroy overhead',
        'More streams always means more speed',
        'Streams consume no resources',
      ],
      a: 1,
      x: [
        'Reuse increases precision',
        'Pools disable the default stream',
        'It reduces memory bandwidth',
      ],
      e: 'Creating/destroying streams repeatedly adds overhead and potential synchronization. A reused pool (sized to the concurrency you can actually exploit) keeps steady-state loops efficient.',
      ref: 'CUDA Best Practices Guide; stream management',
      tags: ['streams', 'pooling', 'overhead'],
    },
    {
      d: 3,
      q: 'cudaDeviceGetStreamPriorityRange returns the valid priority range. A common convention is that…',
      o: [
        'Higher numbers are higher priority',
        'Lower (more negative) numbers represent HIGHER priority',
        'Only zero is valid',
        'Priorities are floating point',
      ],
      a: 1,
      x: [
        'The range is always 0..100',
        'Priority equals the stream id',
        'Negative values are invalid',
      ],
      e: 'CUDA uses "lower value = higher priority," and the device reports its supported [greatest, least] range (e.g. [-2, 0]). Clamp your chosen priority to that range when creating prioritized streams.',
      ref: 'CUDA Runtime API (stream priority range)',
      tags: ['stream-priority', 'range', 'convention'],
    },
  ],
  { idPrefix: 'streams-b' }
)
