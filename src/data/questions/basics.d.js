import { defineQuestions } from './_helpers.js'

// CUDA Basics — batch D. Device props/bandwidth, versions, contexts/IPC, lazy
// loading, intrinsics, half types, compute modes. Distinct from basics.js/.b.js/.c.js.
export default defineQuestions(
  'basics',
  [
    {
      d: 4,
      q: 'Theoretical peak memory bandwidth from cudaDeviceProp is computed as…',
      o: [
        'memoryClockRate × multiProcessorCount',
        '(memoryClockRate(kHz) × 1000 × memoryBusWidth(bits)/8 × 2) — clock × bus width, ×2 for DDR',
        'totalGlobalMem / clockRate',
        'busWidth × warpSize',
      ],
      a: 1,
      x: [
        'memoryClockRate × 4',
        'busWidth only',
        'l2CacheSize × clockRate',
      ],
      e: 'Peak BW = effective memory clock × bus width / 8, with a ×2 for double-data-rate. cudaDeviceProp gives memoryClockRate (kHz) and memoryBusWidth (bits). Achieved bandwidth (from a benchmark) is compared to this peak.',
      ref: 'CUDA Best Practices Guide §8; cudaDeviceProp',
      tags: ['bandwidth', 'device-properties', 'theoretical-peak'],
    },
    {
      d: 3,
      q: 'cudaDriverGetVersion and cudaRuntimeGetVersion can differ; the compatibility rule is…',
      o: [
        'They must be identical',
        'The installed driver version must be ≥ the CUDA runtime/toolkit version the app was built with (newer driver runs older runtime)',
        'The runtime must be newer than the driver',
        'Versions are unrelated',
      ],
      a: 1,
      x: [
        'The driver must equal the runtime',
        'Either may be newer',
        'Only the runtime matters',
      ],
      e: 'CUDA is forward-compatible at the driver level: a given driver supports its toolkit and older toolkits. So driver ≥ runtime is required; a too-old driver for a newer toolkit causes "insufficient driver" errors.',
      ref: 'CUDA compatibility; driver vs runtime versions',
      tags: ['versions', 'compatibility', 'driver'],
    },
    {
      d: 4,
      q: 'Lazy module loading (CUDA_MODULE_LOADING=LAZY, default in recent CUDA) improves startup by…',
      o: [
        'Compiling kernels faster',
        'Loading kernels/modules into the context only when first used, reducing initial load time and memory for large binaries with many unused kernels',
        'Disabling JIT',
        'Skipping error checks',
      ],
      a: 1,
      x: [
        'Caching results',
        'Using more streams',
        'Pinning memory',
      ],
      e: 'Eager loading initializes every kernel/module at context creation; lazy loading defers it until a kernel is launched, cutting startup latency and memory footprint — significant for large libraries (e.g. frameworks) that ship many kernels.',
      ref: 'CUDA lazy module loading',
      tags: ['lazy-loading', 'startup', 'modules'],
    },
    {
      d: 4,
      q: 'cudaSetDeviceFlags(cudaDeviceScheduleBlockingSync) changes how the host waits at synchronization to…',
      o: [
        'Spin on the CPU (busy-wait)',
        'Block the host thread (yielding the CPU) instead of spinning, lowering CPU usage at the cost of slightly higher wake-up latency',
        'Disable synchronization',
        'Use more GPUs',
      ],
      a: 1,
      x: [
        'Increase clocks',
        'Pin memory',
        'Use the default stream',
      ],
      e: 'The default may spin-wait (low latency, high CPU use). BlockingSync makes cudaDeviceSynchronize/event waits block (sleep) the thread, freeing the CPU — useful when oversubscribed or power-sensitive, trading a little latency.',
      ref: 'CUDA Runtime API (cudaSetDeviceFlags)',
      tags: ['sync-policy', 'blocking-sync', 'cpu-usage'],
    },
    {
      d: 5,
      q: 'CUDA IPC (cudaIpcGetMemHandle / cudaIpcOpenMemHandle) lets you…',
      o: [
        'Copy memory faster',
        'Share a device memory allocation between separate PROCESSES on the same node (e.g. for zero-copy producer/consumer across processes)',
        'Share memory across nodes',
        'Allocate pinned memory',
      ],
      a: 1,
      x: [
        'Launch kernels remotely',
        'Profile across processes',
        'Compress memory',
      ],
      e: 'IPC handles export a device pointer so another process on the same GPU/node can map and access the same memory directly, avoiding copies. NCCL and multi-process pipelines use it; it’s node-local (not across machines).',
      ref: 'CUDA C++ Programming Guide (Interprocess Communication)',
      tags: ['ipc', 'multi-process', 'shared-memory'],
    },
    {
      d: 3,
      q: 'The __half and __half2 types (from cuda_fp16.h) are used to…',
      o: [
        'Store FP32',
        'Represent FP16 values; __half2 packs two halves so packed instructions (hadd2, hmul2) process two at once',
        'Represent FP64',
        'Store integers',
      ],
      a: 1,
      x: [
        'Store bfloat16 only',
        'Hold pointers',
        'Represent FP8',
      ],
      e: '__half is 16-bit float; __half2 is a packed pair enabling SIMD-within-a-register half math (two FLOPs per instruction), boosting throughput for FP16 elementwise work. (__nv_bfloat16/2 are the BF16 analogs.)',
      ref: 'CUDA Math API (cuda_fp16.h)',
      tags: ['half', 'half2', 'packed-math'],
    },
    {
      d: 3,
      q: '__popc(x) computes…',
      o: [
        'The position of the highest set bit',
        'The population count — the number of set (1) bits in x',
        'x modulo the population',
        'The parity only',
      ],
      a: 1,
      x: [
        'The number of leading zeros',
        'The lowest set bit index',
        'The bit reversal',
      ],
      e: '__popc returns how many bits are set; combined with __ballot_sync it counts active/matching lanes in a warp (e.g. warp-aggregated atomics). Related: __clz (count leading zeros), __ffs (find first set), __brev (bit reverse).',
      ref: 'CUDA C++ Programming Guide (integer intrinsics)',
      tags: ['popc', 'intrinsics', 'bit-manipulation'],
    },
    {
      d: 3,
      q: 'cudaMemsetAsync differs from cudaMemset in that it…',
      o: [
        'Sets a different value',
        'Is stream-ordered/asynchronous (returns immediately, runs on the given stream), enabling overlap',
        'Runs on the host',
        'Sets 4-byte words',
      ],
      a: 1,
      x: [
        'Is slower',
        'Cannot be used on device memory',
        'Requires pinned memory',
      ],
      e: 'cudaMemsetAsync queues the memset on a stream and returns without blocking, so it overlaps with other work and orders correctly within the stream — the async analog of cudaMemset, used in pipelined/graph code.',
      ref: 'CUDA Runtime API (cudaMemsetAsync)',
      tags: ['memset', 'async', 'streams'],
    },
    {
      d: 4,
      q: 'The device printf buffer size is controlled by…',
      o: [
        'It is unlimited',
        'cudaDeviceSetLimit(cudaLimitPrintfFifoSize, bytes) — raising it prevents dropped output when many threads print',
        'The block size',
        'cudaMemset',
      ],
      a: 1,
      x: [
        'cudaLimitStackSize',
        'The grid size',
        'A compile flag only',
      ],
      e: 'Device printf writes to a fixed FIFO flushed at synchronization; if it overflows, output is lost. Increasing cudaLimitPrintfFifoSize accommodates heavy printing (though guarding prints to few threads is usually better).',
      ref: 'CUDA C++ Programming Guide §B.17; cudaDeviceSetLimit',
      tags: ['printf', 'limits', 'fifo'],
    },
    {
      d: 4,
      q: 'GPU "compute mode" (e.g. EXCLUSIVE_PROCESS via nvidia-smi -c) controls…',
      o: [
        'The precision',
        'Whether multiple processes/contexts may use the GPU concurrently — exclusive modes restrict it to one process, preventing accidental sharing',
        'The clock speed',
        'The memory size',
      ],
      a: 1,
      x: [
        'The warp size',
        'The number of SMs',
        'The driver version',
      ],
      e: 'Compute mode (DEFAULT/EXCLUSIVE_PROCESS/PROHIBITED) governs process access. EXCLUSIVE_PROCESS ensures one process owns the GPU (common with MPS or schedulers), avoiding contention/oversubscription in shared clusters.',
      ref: 'nvidia-smi compute mode',
      tags: ['compute-mode', 'nvidia-smi', 'sharing'],
    },
    {
      d: 3,
      q: 'Enabling "persistence mode" (nvidia-smi -pm 1) helps because it…',
      o: [
        'Saves GPU memory',
        'Keeps the driver loaded/initialized so the first CUDA call in a process doesn’t pay the (slow) driver init cost each time',
        'Increases clocks',
        'Enables MIG',
      ],
      a: 1,
      x: [
        'Pins host memory',
        'Locks the clocks',
        'Disables ECC',
      ],
      e: 'Without persistence mode, the driver may unload when no process is using the GPU, so the next process pays initialization latency. Persistence mode keeps it resident, reducing startup overhead — useful for short, frequent jobs.',
      ref: 'nvidia-smi persistence mode',
      tags: ['persistence-mode', 'startup', 'driver'],
    },
    {
      d: 4,
      q: 'A "primary context" (driver API) is…',
      o: [
        'A user-created context',
        'The per-device context the CUDA runtime manages and shares; most apps use it implicitly via the runtime API, rather than creating their own contexts',
        'A host thread',
        'A stream',
      ],
      a: 1,
      x: [
        'A kernel',
        'A memory pool',
        'A graph',
      ],
      e: 'The runtime API uses each device’s primary context (reference-counted, shared across host threads). The driver API can create additional explicit contexts, but mixing is usually unnecessary — the primary context backs typical runtime programs.',
      ref: 'CUDA Driver API (primary context)',
      tags: ['primary-context', 'driver-api', 'context'],
    },
    {
      d: 2,
      q: 'A downside of allocating large amounts of pinned (page-locked) host memory is that it…',
      o: [
        'Is slower to access on the host',
        'Reduces the pageable memory available to the OS/other processes (it can’t be swapped), so over-pinning can degrade overall system performance',
        'Cannot be used for transfers',
        'Is stored on the GPU',
      ],
      a: 1,
      x: [
        'Disables the cache',
        'Requires ECC',
        'Is read-only',
      ],
      e: 'Pinned memory is locked in physical RAM (non-swappable), so excessive pinning starves the OS and other apps of pageable memory. Use pinned buffers judiciously (e.g. staging buffers) rather than pinning everything.',
      ref: 'CUDA Best Practices Guide §9; pinned memory caveats',
      tags: ['pinned-memory', 'system-impact', 'allocation'],
    },
    {
      d: 3,
      q: 'make_float4(a,b,c,d) and the float4 type are used to…',
      o: [
        'Allocate memory',
        'Construct a 16-byte vector type so a single load/store moves 4 floats (vectorized memory access), improving memory-instruction efficiency',
        'Run on the host only',
        'Store a matrix',
      ],
      a: 1,
      x: [
        'Reduce precision',
        'Enable tensor cores',
        'Compress data',
      ],
      e: 'Vector types (float2/float4, int4, etc.) let each thread load/store 8/16 bytes in one instruction, reducing instruction count and increasing memory parallelism — provided the pointer is suitably aligned (e.g. 16 bytes for float4).',
      ref: 'CUDA vector types; vectorized access',
      tags: ['float4', 'vector-types', 'vectorization'],
    },
    {
      d: 4,
      q: 'Atomic operations on 8-bit (char) or 16-bit (short) types are…',
      o: [
        'Fully supported natively',
        'Not directly supported for most ops — you typically operate on a 32-bit container with atomicCAS and bit-masking to update a sub-word',
        'Faster than 32-bit atomics',
        'Only on the host',
      ],
      a: 1,
      x: [
        'Done in shared memory only',
        'Automatically promoted',
        'Available since CUDA 1.0',
      ],
      e: 'Hardware atomics target 32-/64-bit words. To atomically update a byte/short, you read-modify-write the enclosing 32-bit word via atomicCAS with masking/shifting. (Some newer features add narrower atomics, but the CAS pattern is the portable approach.)',
      ref: 'CUDA atomics; sub-word atomics',
      tags: ['atomics', 'sub-word', 'cas'],
    },
    {
      d: 3,
      q: 'On compute capability 6.0+, managed memory supports concurrent access by CPU and GPU because…',
      o: [
        'It is copied each time',
        'Hardware page faulting allows on-demand migration at page granularity, so CPU and GPU can access managed memory concurrently (with proper synchronization) rather than requiring exclusive phases',
        'It is read-only',
        'It uses constant memory',
      ],
      a: 1,
      x: [
        'It disables the GPU',
        'It pins all pages',
        'It uses the L2 only',
      ],
      e: 'Pascal+ added GPU page faulting, enabling fine-grained on-demand migration and concurrent CPU/GPU access to managed memory. Pre-Pascal required the GPU to have exclusive access during kernels (coarse, all-or-nothing migration).',
      ref: 'CUDA C++ Programming Guide §K; Pascal page faulting',
      tags: ['unified-memory', 'page-faulting', 'pascal'],
    },
    {
      d: 4,
      q: 'A kernel launches fine on your dev GPU but fails with "invalid device function" on a deployment GPU. The cause is usually…',
      o: [
        'Out of memory',
        'The binary lacks SASS/PTX compatible with the deployment GPU’s architecture (wrong -gencode), so no matching kernel image exists',
        'A race condition',
        'Too many registers',
      ],
      a: 1,
      x: [
        'A null pointer',
        'A missing stream',
        'ECC disabled',
      ],
      e: '"invalid device function" / "no kernel image available" means the fatbin has no code for that GPU’s compute capability. Add the right -gencode targets (and PTX for JIT) so the kernel image exists for the deployment hardware.',
      ref: 'CUDA compatibility; -gencode',
      tags: ['invalid-device-function', 'gencode', 'deployment'],
    },
    {
      d: 4,
      q: '__half2 packed math (e.g. __hadd2) is beneficial because it…',
      o: [
        'Improves accuracy',
        'Performs two FP16 operations per instruction (SIMD within a 32-bit register), roughly doubling FP16 elementwise throughput on the CUDA cores',
        'Uses tensor cores',
        'Reduces memory',
      ],
      a: 1,
      x: [
        'Halves the precision',
        'Runs on the host',
        'Avoids registers',
      ],
      e: 'Packing two halves into a 32-bit register and using *2 intrinsics doubles FP16 throughput for elementwise math on CUDA cores (separate from tensor-core matmul). Frameworks/kernels use this for fast FP16 activation/elementwise ops.',
      ref: 'CUDA Math API (half2 intrinsics)',
      tags: ['half2', 'packed-math', 'fp16'],
    },
    {
      d: 3,
      q: 'cudaDeviceProp.sharedMemPerMultiprocessor vs sharedMemPerBlock tells you…',
      o: [
        'They are the same',
        'The total shared memory per SM vs. the per-block default limit; the per-SM total bounds how many blocks’ shared memory can coexist (occupancy)',
        'The register count',
        'The L2 size',
      ],
      a: 1,
      x: [
        'The global memory',
        'The warp size',
        'The clock rate',
      ],
      e: 'Per-block shared memory limits one block; per-SM shared memory caps the sum across resident blocks. Both (plus opt-in max) matter for occupancy: a block using lots of shared memory reduces how many fit per SM.',
      ref: 'cudaDeviceProp; occupancy',
      tags: ['shared-memory', 'device-properties', 'occupancy'],
    },
    {
      d: 5,
      q: 'In CUDA 12’s dynamic parallelism (CDP2), a notable change is that a parent kernel…',
      o: [
        'Can no longer launch children',
        'Cannot call cudaDeviceSynchronize on the device to wait for child grids (that legacy behavior was removed); synchronization uses other mechanisms (e.g. tail launch / streams)',
        'Must run on the host',
        'Always waits for children',
      ],
      a: 1,
      x: [
        'Launches faster than the host',
        'Shares registers with children',
        'Cannot use streams',
      ],
      e: 'CDP2 removed device-side cudaDeviceSynchronize (parent explicitly waiting on children), which was costly and error-prone. Parent/child ordering is handled differently (e.g. fire-and-forget with tail launches), simplifying the model.',
      ref: 'CUDA C++ Programming Guide (Dynamic Parallelism, CDP2)',
      tags: ['dynamic-parallelism', 'cdp2', 'synchronization'],
    },
    {
      d: 2,
      q: 'cudaMemcpy with cudaMemcpyDefault (instead of an explicit kind) works because…',
      o: [
        'It guesses randomly',
        'Under Unified Virtual Addressing the runtime infers the direction from the pointers’ memory types',
        'It always copies host-to-device',
        'It runs on the CPU',
      ],
      a: 1,
      x: [
        'It is slower',
        'It requires managed memory',
        'It copies nothing',
      ],
      e: 'With UVA, host and device pointers are distinguishable, so cudaMemcpyDefault lets the runtime determine H2D/D2H/D2D automatically — convenient for generic code that doesn’t know the buffers’ locations a priori.',
      ref: 'CUDA Runtime API; UVA, cudaMemcpyDefault',
      tags: ['memcpy', 'uva', 'default-kind'],
    },
    {
      d: 4,
      q: '__funnelshift_l/_r and bit intrinsics like __brev are useful for…',
      o: [
        'Floating-point math',
        'Efficient bit-level operations (funnel shifts, bit reversal) used in hashing, bit-packing, sorting (radix), and crypto-style kernels',
        'Memory allocation',
        'Reductions only',
      ],
      a: 1,
      x: [
        'Tensor-core math',
        'Texture sampling',
        'Stream management',
      ],
      e: 'These map to single hardware instructions for bit manipulation (combined shifts, reversal, leading-zero counts), accelerating bit-twiddling-heavy kernels (radix sort digit extraction, hashing, bitboards) versus emulating with arithmetic.',
      ref: 'CUDA C++ Programming Guide (integer intrinsics)',
      tags: ['bit-intrinsics', 'funnelshift', 'brev'],
    },
    {
      d: 3,
      q: 'On WSL2 (Windows Subsystem for Linux), CUDA works because…',
      o: [
        'It emulates the GPU on the CPU',
        'A GPU-paravirtualization path lets the Linux CUDA stack use the Windows host’s NVIDIA driver, exposing the real GPU to WSL2',
        'It uses a software renderer',
        'It requires a second GPU',
      ],
      a: 1,
      x: [
        'It runs in the cloud',
        'It needs no driver',
        'It only supports CPU kernels',
      ],
      e: 'WSL2 uses a virtual GPU interface so the Linux-side CUDA libraries talk to the Windows host driver and run on the physical GPU. Some features differ from native Linux, but standard CUDA development is supported.',
      ref: 'NVIDIA CUDA on WSL documentation',
      tags: ['wsl2', 'virtualization', 'driver'],
    },
    {
      d: 4,
      q: 'cudaDeviceProp.regsPerBlock and regsPerMultiprocessor matter because…',
      o: [
        'They set the clock',
        'They cap the total registers available; per-thread register usage × resident threads must fit, so they bound occupancy alongside shared memory',
        'They set the L2 size',
        'They are unused',
      ],
      a: 1,
      x: [
        'They define the warp size',
        'They set the grid limit',
        'They control bandwidth',
      ],
      e: 'The register file per SM (regsPerMultiprocessor) and per-block limit constrain how many warps/blocks can reside given each thread’s register count — a core occupancy input. The occupancy calculator uses these fields.',
      ref: 'cudaDeviceProp; occupancy',
      tags: ['registers', 'device-properties', 'occupancy'],
    },
    {
      d: 5,
      q: 'Why might two host threads sharing one device benefit from each having its own stream and using the per-thread default stream?',
      o: [
        'It is required',
        'The legacy default stream serializes across threads; per-thread default streams (or explicit streams) let each thread’s work proceed concurrently without implicit cross-thread synchronization',
        'It saves memory',
        'It increases precision',
      ],
      a: 1,
      x: [
        'It pins memory',
        'It enables tensor cores',
        'It locks clocks',
      ],
      e: 'With the legacy default stream, operations from different host threads serialize through stream 0. Per-thread default streams (compile flag) or explicit non-blocking streams give each thread an independent queue, enabling true concurrency.',
      ref: 'CUDA per-thread default stream; multi-threaded host',
      tags: ['per-thread-stream', 'multithreading', 'concurrency'],
    },
    {
      d: 3,
      q: 'cudaDeviceProp.concurrentKernels indicates…',
      o: [
        'The number of kernels',
        'Whether the device supports running multiple kernels concurrently (on different streams) — a capability flag',
        'The grid size',
        'The warp size',
      ],
      a: 1,
      x: [
        'The number of SMs',
        'The clock rate',
        'The memory size',
      ],
      e: 'concurrentKernels is a boolean capability (true on modern GPUs) signaling support for concurrent kernel execution across streams. Other useful flags: asyncEngineCount (copy engines), unifiedAddressing, managedMemory.',
      ref: 'cudaDeviceProp capability flags',
      tags: ['concurrent-kernels', 'device-properties', 'capability'],
    },
    {
      d: 4,
      q: 'asyncEngineCount in cudaDeviceProp tells you…',
      o: [
        'The number of SMs',
        'How many copy engines (DMA) the GPU has — e.g. 2 means it can overlap H2D and D2H transfers with compute simultaneously',
        'The number of streams',
        'The register count',
      ],
      a: 1,
      x: [
        'The warp size',
        'The clock rate',
        'The L2 size',
      ],
      e: 'asyncEngineCount reports the DMA/copy engines. A value of 2 enables simultaneous bidirectional copy + compute overlap (triple buffering). Knowing it helps design pipelined streaming that exploits all available engines.',
      ref: 'cudaDeviceProp (asyncEngineCount)',
      tags: ['copy-engines', 'device-properties', 'overlap'],
    },
    {
      d: 4,
      q: 'min(a,b) and max(a,b) for integers in CUDA typically compile to…',
      o: [
        'A branch (if/else)',
        'Single hardware min/max instructions (branchless), so using them avoids divergence versus a manual comparison',
        'A function call',
        'A loop',
      ],
      a: 1,
      x: [
        'Atomic operations',
        'A division',
        'A texture fetch',
      ],
      e: 'CUDA maps integer/float min/max to single branchless instructions. Using them (and fminf/fmaxf) for selection avoids the divergence of explicit conditionals — a simple, effective micro-optimization in hot code.',
      ref: 'CUDA C++ Programming Guide §G; min/max intrinsics',
      tags: ['min-max', 'branchless', 'intrinsics'],
    },
    {
      d: 5,
      q: 'cuModuleLoadDataEx (driver API) with JIT options lets you…',
      o: [
        'Run kernels on the CPU',
        'Load PTX and JIT-compile it at runtime with control over options (target arch, optimization, register cap) and retrieve the compilation log — used by frameworks/JIT systems',
        'Profile kernels',
        'Allocate pinned memory',
      ],
      a: 1,
      x: [
        'Disable the driver',
        'Pin a context',
        'Compress modules',
      ],
      e: 'The driver API can JIT PTX with options (e.g. CU_JIT_MAX_REGISTERS, target SM, optimization level) and capture logs — the mechanism behind runtime kernel specialization (NVRTC output → cuModuleLoadDataEx → cuLaunchKernel).',
      ref: 'CUDA Driver API (cuModuleLoadDataEx, JIT options)',
      tags: ['driver-api', 'jit', 'ptx'],
    },
  ],
  { idPrefix: 'basics-d' }
)
