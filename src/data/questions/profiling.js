import { defineQuestions } from './_helpers.js'

// Performance & Profiling — Nsight Systems/Compute, metrics, roofline,
// bottleneck analysis, timing.
export default defineQuestions('profiling', [
  {
    d: 2,
    q: 'What is the primary use of Nsight Systems (nsys)?',
    o: [
      'Per-kernel instruction-level analysis',
      'System-wide timeline profiling: CPU/GPU activity, kernel/memcpy timing, streams, and gaps/overlap',
      'Editing CUDA source code',
      'Counting bank conflicts',
    ],
    a: 1,
    e: 'Nsight Systems gives a timeline view of the whole application — host threads, CUDA API calls, kernels, memory transfers, and streams — ideal for spotting serialization, missing overlap, and CPU bottlenecks. Deep per-kernel metrics are Nsight Compute’s job.',
    ref: 'NVIDIA Nsight Systems documentation',
    tags: ['nsight-systems', 'timeline', 'profiling'],
  },
  {
    d: 2,
    q: 'What is the primary use of Nsight Compute (ncu)?',
    o: [
      'Timeline of the whole application',
      'Detailed single-kernel analysis: occupancy, memory throughput, instruction mix, roofline, stall reasons',
      'Multi-node cluster profiling',
      'Compiling kernels',
    ],
    a: 1,
    e: 'Nsight Compute is a kernel profiler: it replays a kernel to collect hardware counters and presents occupancy, memory/compute throughput, warp-stall reasons, roofline, and source-level metrics for deep optimization of a specific kernel.',
    ref: 'NVIDIA Nsight Compute documentation',
    tags: ['nsight-compute', 'kernel-analysis'],
  },
  {
    d: 3,
    q: 'When timing a kernel with CUDA events, why must you record a stop event and call cudaEventSynchronize before reading the elapsed time?',
    o: [
      'Events are stored on the host',
      'Kernel launches are asynchronous, so you must wait for the GPU to actually reach the stop event before the timestamp is valid',
      'cudaEventElapsedTime resets the clock',
      'Otherwise the time is in nanoseconds',
    ],
    a: 1,
    e: 'Because launches are async, the CPU races ahead. cudaEventSynchronize(stop) blocks until the GPU completes work up to the stop event; only then does cudaEventElapsedTime(start, stop) report the true GPU duration (in ms).',
    ref: 'CUDA Best Practices Guide §8; CUDA Runtime API',
    tags: ['events', 'timing', 'async'],
  },
  {
    d: 3,
    q: 'In Nsight Compute, "achieved occupancy" differs from "theoretical occupancy" because…',
    o: [
      'They always match',
      'Achieved reflects the actual average active warps during execution, affected by load imbalance, tail effects, and divergence',
      'Theoretical is measured after the run',
      'Achieved ignores register usage',
    ],
    a: 1,
    e: 'Theoretical occupancy is the ceiling from resource limits. Achieved occupancy is measured at runtime and is usually lower due to imbalanced work, short kernels, the tail effect, and uneven warp completion. A big gap suggests scheduling issues.',
    ref: 'Nsight Compute documentation; CUDA Best Practices Guide',
    tags: ['occupancy', 'nsight-compute', 'metrics'],
  },
  {
    d: 4,
    q: 'A kernel shows high "Long Scoreboard" stalls in Nsight Compute. This typically indicates…',
    o: [
      'Too many atomic operations',
      'Warps waiting on global/local memory loads (data dependency on a pending long-latency memory access)',
      'Register bank conflicts',
      'Excessive __syncthreads',
    ],
    a: 1,
    e: 'The "long scoreboard" tracks long-latency memory operations. High stalls there mean warps are blocked waiting for global/local memory results — a sign of being memory-latency-bound. Remedies: more ILP/occupancy, better locality, prefetch.',
    ref: 'Nsight Compute warp-stall reasons documentation',
    tags: ['stall-reasons', 'memory-latency', 'nsight-compute'],
  },
  {
    d: 3,
    q: 'To find whether a kernel is compute-bound or memory-bound in Nsight Compute, you would look at…',
    o: [
      'The kernel name only',
      'The SM (compute) throughput vs. memory throughput utilization (and the roofline chart)',
      'The number of registers',
      'The grid dimensions',
    ],
    a: 1,
    e: 'Nsight Compute’s "Speed of Light" section reports compute (SM) and memory throughput as % of peak. Whichever is closer to 100% is the limiter; the roofline chart confirms by plotting the kernel against the compute and bandwidth ceilings.',
    ref: 'Nsight Compute Speed-of-Light / Roofline sections',
    tags: ['speed-of-light', 'roofline', 'bottleneck'],
  },
  {
    d: 2,
    q: 'Why is it important to "warm up" the GPU (run the kernel once untimed) before benchmarking?',
    o: [
      'To clear global memory',
      'To absorb one-time costs: context/JIT initialization, autotuning, and clock ramp-up, so timed runs reflect steady state',
      'To increase occupancy',
      'It is never necessary',
    ],
    a: 1,
    e: 'First-run overhead (context creation, PTX JIT, library handle init, lazy module loading) and GPU clock ramp-up distort timings. A warm-up run plus averaging several timed iterations yields representative, stable measurements.',
    ref: 'CUDA Best Practices Guide §8',
    tags: ['benchmarking', 'warmup', 'timing'],
  },
  {
    d: 4,
    q: 'In Nsight Systems, you see kernels and H2D copies running strictly back-to-back on the default stream. The fix to overlap them is to…',
    o: [
      'Increase the block size',
      'Use multiple non-default streams with pinned memory and cudaMemcpyAsync so copies overlap with compute',
      'Add more __syncthreads',
      'Reduce register usage',
    ],
    a: 1,
    e: 'The default (null) stream serializes work. Splitting transfers and kernels across multiple streams, with page-locked host memory and async copies, lets the copy engines and SMs run concurrently — visible as overlapping timeline rows.',
    ref: 'CUDA Best Practices Guide §9; Nsight Systems',
    tags: ['streams', 'overlap', 'nsight-systems'],
  },
  {
    d: 3,
    q: 'The metric "DRAM throughput" reported as a % of peak tells you…',
    o: [
      'How full the L1 cache is',
      'How close the kernel’s device-memory bandwidth usage is to the GPU’s peak HBM bandwidth',
      'The PCIe transfer rate',
      'The occupancy',
    ],
    a: 1,
    e: 'DRAM throughput % shows achieved HBM bandwidth relative to peak. Near 100% on a memory-bound kernel means you’re essentially at the bandwidth roof — further speedups require reducing bytes moved (reuse/fusion/lower precision), not more compute.',
    ref: 'Nsight Compute memory workload analysis',
    tags: ['dram-throughput', 'bandwidth', 'metrics'],
  },
  {
    d: 4,
    q: 'compute-sanitizer with --tool memcheck is used to detect…',
    o: [
      'Slow kernels',
      'Out-of-bounds and misaligned global/shared memory accesses and leaks',
      'Low occupancy',
      'Bank conflicts',
    ],
    a: 1,
    e: 'compute-sanitizer (successor to cuda-memcheck) has tools: memcheck (OOB/misaligned/leaks), racecheck (shared-memory races), initcheck (uninitialized reads), synccheck (illegal barrier/sync use). It is the first stop for correctness bugs.',
    ref: 'NVIDIA compute-sanitizer documentation',
    tags: ['compute-sanitizer', 'memcheck', 'correctness'],
  },
  {
    d: 5,
    q: 'You profile a GEMM and tensor-core (Tensor) pipe utilization is 30% while memory is 95%. The kernel is…',
    o: [
      'Compute-bound on tensor cores',
      'Memory-bound — tensor cores are starved waiting for operands; improve data movement (larger tiles, TMA, better reuse/precision)',
      'Perfectly balanced',
      'Limited by registers',
    ],
    a: 1,
    e: 'High memory utilization with low tensor-pipe utilization means the math units idle waiting for data. The fix targets the feeding path: deeper pipelining/double buffering, TMA, larger tiles for reuse, or lower-precision operands to cut bytes.',
    ref: 'Nsight Compute pipe utilization; CUTLASS performance guides',
    tags: ['tensor-core', 'memory-bound', 'gemm', 'profiling'],
  },
  {
    d: 2,
    q: 'What does the NVTX library let you do in profiles?',
    o: [
      'Automatically optimize kernels',
      'Annotate code with named ranges/markers that appear on the Nsight Systems timeline for easier correlation',
      'Count FLOPs',
      'Detect races',
    ],
    a: 1,
    e: 'NVTX (NVIDIA Tools Extension) lets you push/pop named ranges and markers (e.g. nvtxRangePush("forward")) that show up on the profiler timeline, making it easy to map timeline activity to phases of your application.',
    ref: 'NVTX documentation; Nsight Systems',
    tags: ['nvtx', 'annotation', 'timeline'],
  },
  {
    d: 3,
    q: 'When a kernel’s runtime barely changes as you increase occupancy, it most likely means…',
    o: [
      'The profiler is broken',
      'The kernel already has enough parallelism to hide latency (it is bandwidth- or compute-bound), so more occupancy does not help',
      'Occupancy never affects runtime',
      'You must increase the block size further',
    ],
    a: 1,
    e: 'If extra occupancy yields no speedup, latency is already hidden and the limiter is elsewhere (bandwidth or compute throughput). Profiling should then focus on reducing memory traffic or increasing useful compute, not on occupancy.',
    ref: 'CUDA Best Practices Guide; Nsight Compute',
    tags: ['occupancy', 'analysis', 'bottleneck'],
  },
  {
    d: 4,
    q: 'Why can profiling a kernel under Nsight Compute be much slower than a normal run?',
    o: [
      'It recompiles the kernel each time',
      'ncu may replay the kernel multiple times (kernel replay) to gather all hardware counters that cannot be collected in a single pass',
      'It runs on the CPU',
      'It disables the cache',
    ],
    a: 1,
    e: 'Hardware has a limited number of counters, so Nsight Compute replays the kernel several times (saving/restoring state) to collect the full metric set. This "kernel replay" makes detailed profiling slower but gives a complete picture.',
    ref: 'Nsight Compute documentation (kernel replay)',
    tags: ['nsight-compute', 'kernel-replay', 'overhead'],
  },
])
