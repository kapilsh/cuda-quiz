import { defineQuestions } from './_helpers.js'

// CUDA Basics — batch E. Launch attributes, grid-constant, func attributes,
// error stickiness, INT8 intrinsics, cluster launch. Distinct from basics.js/.b/.c/.d.
export default defineQuestions(
  'basics',
  [
    {
      d: 5,
      q: 'cudaLaunchKernelEx (with a cudaLaunchConfig and attributes) is the way to launch a kernel with…',
      o: [
        'Only a different stream',
        'Extended launch attributes such as thread-block cluster dimensions, programmatic dependent launch, or cooperative launch — things the <<<>>> syntax cannot express',
        'More registers',
        'A host function',
      ],
      a: 1,
      x: [
        'A smaller grid',
        'Lower precision',
        'Pinned memory',
      ],
      e: 'cudaLaunchKernelEx takes a config struct plus an array of launch attributes (cluster dims, PDL, cooperative, priority), enabling Hopper+ features. The <<<>>> form only covers grid/block/shmem/stream, so newer launches use the Ex API.',
      ref: 'CUDA Runtime API (cudaLaunchKernelEx); launch attributes',
      tags: ['launch-attributes', 'cluster', 'cudaLaunchKernelEx'],
    },
    {
      d: 5,
      q: 'The __grid_constant__ qualifier on a kernel parameter tells the compiler…',
      o: [
        'It is in constant memory',
        'The (by-value) parameter is uniform across the grid and read-only, so it can be accessed efficiently from its constant-bank location without per-thread copies',
        'It changes per block',
        'It is a pointer',
      ],
      a: 1,
      x: [
        'It is shared memory',
        'It is mutable',
        'It is a texture',
      ],
      e: '__grid_constant__ marks a large by-value kernel argument as a grid-wide constant, letting the compiler avoid making local copies and read it directly from the parameter/constant bank — useful for big read-only structs passed by value.',
      ref: 'CUDA C++ Programming Guide (__grid_constant__)',
      tags: ['grid-constant', 'kernel-arguments', 'constant'],
    },
    {
      d: 4,
      q: 'cudaFuncGetAttributes lets you query at runtime a kernel’s…',
      o: [
        'Execution time',
        'Resource usage and metadata: numRegs, sharedSizeBytes, maxThreadsPerBlock, ptxVersion, binaryVersion — useful for occupancy decisions and compatibility checks',
        'Grid dimensions',
        'Output values',
      ],
      a: 1,
      x: [
        'The stream',
        'The DRAM bandwidth',
        'The block index',
      ],
      e: 'cudaFuncGetAttributes returns a kernel’s register/shared-memory usage and version info, so code can make occupancy-aware launch decisions or verify the binary targets the running GPU — the runtime counterpart of ptxas -v.',
      ref: 'CUDA Runtime API (cudaFuncGetAttributes)',
      tags: ['func-attributes', 'registers', 'introspection'],
    },
    {
      d: 5,
      q: 'After a kernel triggers cudaErrorIllegalAddress (out-of-bounds), subsequent CUDA calls in the same process tend to fail because…',
      o: [
        'It is a transient error',
        'It is a "sticky" error that corrupts the CUDA context; the context is unusable and typically the process must be restarted (or the context destroyed/recreated)',
        'cudaGetLastError clears it',
        'It only affects one stream',
      ],
      a: 1,
      x: [
        'It self-heals',
        'It is non-sticky',
        'It only affects the host',
      ],
      e: 'Illegal-address (and similar) errors are sticky: they poison the context, so all later calls return errors. Unlike non-sticky errors (cleared by cudaGetLastError), recovery generally requires destroying/recreating the context or restarting the process.',
      ref: 'CUDA error handling; sticky vs non-sticky errors',
      tags: ['sticky-errors', 'illegal-address', 'context'],
    },
    {
      d: 4,
      q: 'The difference between sticky and non-sticky CUDA errors is…',
      o: [
        'There is none',
        'Non-sticky errors (e.g. invalid launch config) are cleared by cudaGetLastError and don’t corrupt the context; sticky errors (e.g. illegal address, ECC) corrupt the context and persist',
        'Sticky errors are warnings',
        'Non-sticky errors crash the host',
      ],
      a: 1,
      x: [
        'Sticky errors are faster',
        'Both clear automatically',
        'Non-sticky are unrecoverable',
      ],
      e: 'Non-sticky errors report a problem with a specific call and can be cleared/continued past. Sticky errors indicate the device/context entered a bad state (corrupt context), making further work invalid until the context is recreated.',
      ref: 'CUDA error handling semantics',
      tags: ['errors', 'sticky', 'non-sticky'],
    },
    {
      d: 4,
      q: 'An "XID" error (seen in nvidia-smi / dmesg) indicates…',
      o: [
        'A compile error',
        'A GPU/driver-level error event (e.g. hardware fault, illegal memory access, ECC error, fallen-off-bus) — a diagnostic code for serious device issues',
        'A slow kernel',
        'A Python exception',
      ],
      a: 1,
      x: [
        'A network error',
        'A low-occupancy warning',
        'A profiling result',
      ],
      e: 'XID messages are driver-reported GPU error events with codes (e.g. Xid 13/31/79) pointing to causes like illegal access, ECC errors, or hardware faults. They’re key to diagnosing crashes/instability at the system level.',
      ref: 'NVIDIA Xid error documentation',
      tags: ['xid', 'errors', 'diagnostics'],
    },
    {
      d: 4,
      q: '__dp4a(a, b, c) is an intrinsic that…',
      o: [
        'Adds four floats',
        'Computes a 4-element INT8 dot product and accumulates into a 32-bit integer (c += sum of 4 int8 products) in one instruction — accelerating INT8 math on CUDA cores',
        'Performs FP4 math',
        'Reverses bits',
      ],
      a: 1,
      x: [
        'Does a 2-element FP16 product',
        'Loads four bytes',
        'Is a memory fence',
      ],
      e: '__dp4a packs four INT8 multiply-adds into one instruction (DP4A), boosting INT8 throughput on CUDA cores (used before/alongside INT8 tensor cores). __dp2a does a 2-element version. Useful for quantized inference on the SIMD ALU path.',
      ref: 'CUDA integer intrinsics (__dp4a)',
      tags: ['dp4a', 'int8', 'intrinsics'],
    },
    {
      d: 5,
      q: 'The __cluster_dims__(x,y,z) kernel attribute (Hopper) specifies…',
      o: [
        'The grid size',
        'A compile-time thread-block cluster shape, so blocks are launched as clusters of that size (enabling DSMEM and cluster.sync) without setting it per-launch',
        'The block size',
        'The warp size',
      ],
      a: 1,
      x: [
        'The register count',
        'The shared-memory size',
        'The stream',
      ],
      e: '__cluster_dims__ fixes the cluster dimensions at compile time (alternatively set via launch attributes). It groups blocks into co-scheduled clusters within a GPC, unlocking distributed shared memory and cluster-wide barriers.',
      ref: 'CUDA C++ Programming Guide (thread block clusters)',
      tags: ['cluster-dims', 'clusters', 'hopper'],
    },
    {
      d: 4,
      q: 'A device function pointer (calling a __device__ function indirectly) is allowed but…',
      o: [
        'Always free',
        'Can prevent inlining and add call overhead/register pressure, and historically requires relocatable device code; it’s used for runtime polymorphism/dispatch when necessary',
        'Runs on the host',
        'Is not supported',
      ],
      a: 1,
      x: [
        'Is faster than direct calls',
        'Requires tensor cores',
        'Disables divergence',
      ],
      e: 'Indirect device calls support runtime dispatch (e.g. selecting an operator) but inhibit inlining and add overhead; they need separate compilation (-rdc=true) to resolve symbols. Prefer direct/templated calls in hot paths.',
      ref: 'CUDA C++ device function pointers',
      tags: ['function-pointers', 'indirect-call', 'rdc'],
    },
    {
      d: 4,
      q: 'cudaMemset2D exists (alongside cudaMemset) to…',
      o: [
        'Set 1D arrays',
        'Set a 2D region of pitched memory, taking the pitch so each row’s valid bytes are filled correctly despite row padding',
        'Copy memory',
        'Allocate memory',
      ],
      a: 1,
      x: [
        'Set host memory',
        'Set constant memory',
        'Set registers',
      ],
      e: 'For pitched 2D allocations (cudaMallocPitch), cudaMemset2D fills a width×height region honoring the row pitch, analogous to cudaMemcpy2D. Plain cudaMemset would assume contiguous bytes and mishandle the padding.',
      ref: 'CUDA Runtime API (cudaMemset2D)',
      tags: ['memset2d', 'pitch', '2d'],
    },
    {
      d: 4,
      q: 'cudaMemRangeGetAttribute on managed memory can report…',
      o: [
        'The kernel time',
        'Attributes of a managed range such as its preferred location, last-prefetch location, or accessed-by set — useful to inspect/verify Unified Memory placement',
        'The register count',
        'The block size',
      ],
      a: 1,
      x: [
        'The DRAM bandwidth',
        'The grid size',
        'The stream',
      ],
      e: 'It queries Unified Memory range attributes (preferred location, accessibility, last location), letting you confirm that advise/prefetch hints took effect — handy when debugging managed-memory migration performance.',
      ref: 'CUDA Runtime API (cudaMemRangeGetAttribute)',
      tags: ['unified-memory', 'attributes', 'introspection'],
    },
    {
      d: 3,
      q: 'cudaDeviceProp.cooperativeLaunch indicates…',
      o: [
        'The number of SMs',
        'Whether the device supports cooperative kernel launches (cudaLaunchCooperativeKernel) needed for grid-wide synchronization',
        'The warp size',
        'The clock rate',
      ],
      a: 1,
      x: [
        'The memory size',
        'The register count',
        'P2P support',
      ],
      e: 'This capability flag tells you if grid-wide cooperative launch (and thus grid.sync()) is available. Check it before relying on cooperative groups’ grid-level features, which aren’t supported on all devices/configurations.',
      ref: 'cudaDeviceProp (cooperativeLaunch)',
      tags: ['cooperative-launch', 'capability', 'grid-sync'],
    },
    {
      d: 5,
      q: 'cuDevicePrimaryCtxRetain (driver API) is used by frameworks to…',
      o: [
        'Create a new context',
        'Obtain and reference-count the device’s shared PRIMARY context (the one the runtime uses), so multiple components interoperate on the same context instead of creating conflicting ones',
        'Destroy the context',
        'Launch a kernel',
      ],
      a: 1,
      x: [
        'Pin memory',
        'Profile the device',
        'Reset the GPU',
      ],
      e: 'Retaining the primary context lets driver-API code share the runtime’s context (and its allocations/streams) rather than spinning up a separate context. This interop is how frameworks and libraries coexist on one device cleanly.',
      ref: 'CUDA Driver API (cuDevicePrimaryCtxRetain)',
      tags: ['primary-context', 'driver-api', 'interop'],
    },
    {
      d: 4,
      q: 'Hinting alignment to the compiler (e.g. via __builtin_assume_aligned or aligned types) helps because…',
      o: [
        'It allocates memory',
        'It lets the compiler safely emit wide vectorized (e.g. 128-bit) loads/stores it otherwise couldn’t prove are aligned, improving memory throughput',
        'It reduces registers',
        'It enables tensor cores',
      ],
      a: 1,
      x: [
        'It disables coalescing',
        'It runs on the host',
        'It compresses data',
      ],
      e: 'Without alignment knowledge, the compiler conservatively uses narrower accesses. Asserting alignment (when truly aligned) enables vectorized loads/stores (float4 etc.), raising effective bandwidth for memory-bound kernels.',
      ref: 'Vectorization; alignment hints',
      tags: ['alignment', 'vectorization', 'compiler-hint'],
    },
    {
      d: 4,
      q: 'cudaDeviceProp.pageableMemoryAccess (with ATS / HMM support) being true means…',
      o: [
        'All memory is pinned',
        'The GPU can directly access ordinary pageable host memory (via address translation services / heterogeneous memory management) without explicit pinning or cudaMallocManaged',
        'There is no host memory',
        'ECC is enabled',
      ],
      a: 1,
      x: [
        'The GPU has more memory',
        'P2P is enabled',
        'Tensor cores exist',
      ],
      e: 'On platforms with ATS/HMM (e.g. Grace Hopper, some systems), the GPU can page-fault on regular malloc’d host memory, accessing it directly. This blurs the host/device memory distinction, simplifying programming where supported.',
      ref: 'CUDA HMM/ATS; pageableMemoryAccess',
      tags: ['ats', 'hmm', 'pageable-access'],
    },
    {
      d: 3,
      q: 'cudaGetDeviceProperties is relatively expensive; a lighter way to fetch one attribute is…',
      o: [
        'Launch a kernel',
        'cudaDeviceGetAttribute(&value, attr, device) — returns a single attribute without populating the whole struct',
        'cudaMemGetInfo',
        'nvidia-smi',
      ],
      a: 1,
      x: [
        'cudaFuncGetAttributes',
        'cudaStreamQuery',
        'cudaEventQuery',
      ],
      e: 'cudaDeviceGetAttribute fetches just the requested attribute (and exposes some not in cudaDeviceProp), which is cheaper and cleaner for runtime feature/limit checks than building the full properties struct.',
      ref: 'CUDA Runtime API (cudaDeviceGetAttribute)',
      tags: ['device-attributes', 'lightweight', 'introspection'],
    },
    {
      d: 5,
      q: 'A launch attribute cudaLaunchAttributeProgrammaticStreamSerialization / PDL trigger is used to…',
      o: [
        'Set the grid size',
        'Enable programmatic dependent launch so a kernel can signal that dependent kernels may begin, overlapping the producer’s tail with the consumer’s start',
        'Pin memory',
        'Lower precision',
      ],
      a: 1,
      x: [
        'Increase registers',
        'Disable streams',
        'Compress data',
      ],
      e: 'Setting the PDL-related launch attributes (and using cudaGridDependencySynchronize / cudaTriggerProgrammaticLaunchCompletion in the kernels) lets consecutive kernels overlap across their dependency boundary, shrinking inter-kernel gaps on Hopper+.',
      ref: 'CUDA launch attributes (PDL)',
      tags: ['pdl', 'launch-attributes', 'overlap'],
    },
    {
      d: 4,
      q: '__byte_perm(a, b, s) is an intrinsic used to…',
      o: [
        'Add bytes',
        'Select/permute bytes from two 32-bit words according to a selector, useful for fast byte shuffling/packing (e.g. in sorting, hashing, format conversion)',
        'Reverse all bits',
        'Count set bits',
      ],
      a: 1,
      x: [
        'Multiply bytes',
        'Load bytes from memory',
        'Compare bytes',
      ],
      e: '__byte_perm extracts and rearranges bytes from a pair of words per a 4-nibble selector in one instruction — handy for byte-level data movement (endianness, packing, radix digit extraction) without multiple shift/mask ops.',
      ref: 'CUDA integer intrinsics (__byte_perm)',
      tags: ['byte-perm', 'intrinsics', 'bit-manipulation'],
    },
    {
      d: 4,
      q: 'cudaDeviceProp.hostNativeAtomicSupported indicates whether…',
      o: [
        'The GPU has atomics',
        'The system supports native atomic operations on memory shared between the host CPU and the GPU (e.g. system-scope atomics over a coherent link)',
        'Shared memory has atomics',
        'Tensor cores exist',
      ],
      a: 1,
      x: [
        'The warp size',
        'P2P is enabled',
        'ECC is on',
      ],
      e: 'This flag reports whether CPU↔GPU native atomics on shared memory are supported (depends on the interconnect/platform, e.g. coherent NVLink-C2C). It matters for fine-grained host/device coordination via system-scope atomics.',
      ref: 'cudaDeviceProp (hostNativeAtomicSupported)',
      tags: ['system-atomics', 'capability', 'coherence'],
    },
    {
      d: 3,
      q: 'When checking errors, why call cudaGetLastError() AND cudaDeviceSynchronize() after a kernel?',
      o: [
        'They are redundant',
        'cudaGetLastError catches launch/config errors immediately; cudaDeviceSynchronize surfaces RUNTIME errors that occur during execution (returned by the sync) — together they catch both kinds',
        'Only one works',
        'To time the kernel',
      ],
      a: 1,
      x: [
        'To reset the GPU',
        'To free memory',
        'To increase occupancy',
      ],
      e: 'Launch errors appear right after launch (cudaGetLastError); errors during kernel execution (e.g. illegal address) only surface when you synchronize (or at the next call). Checking both is the robust pattern in debug builds.',
      ref: 'CUDA error checking pattern',
      tags: ['error-handling', 'synchronize', 'debugging'],
    },
    {
      d: 5,
      q: 'cudaOccupancyMaxActiveClusters (Hopper) is the cluster analog of the block-occupancy API, used to…',
      o: [
        'Count SMs',
        'Determine how many thread-block clusters of a given size can be co-resident, so you size a cluster-cooperative grid correctly',
        'Set the block size',
        'Profile the kernel',
      ],
      a: 1,
      x: [
        'Allocate memory',
        'Pick the stream',
        'Lower precision',
      ],
      e: 'For cluster-based kernels (DSMEM, cluster.sync), you need to know how many clusters fit. This API computes that, analogous to cudaOccupancyMaxActiveBlocksPerMultiprocessor for blocks — essential for correct cooperative cluster launches.',
      ref: 'CUDA Runtime API (cluster occupancy)',
      tags: ['cluster-occupancy', 'hopper', 'launch'],
    },
    {
      d: 3,
      q: 'Querying ptxVersion vs binaryVersion from cudaFuncGetAttributes tells you…',
      o: [
        'The runtime version',
        'The virtual (PTX) and actual (SASS) architecture the kernel was compiled for — useful to confirm the binary matches the running GPU or relies on JIT',
        'The driver version',
        'The grid size',
      ],
      a: 1,
      x: [
        'The block size',
        'The register count',
        'The memory size',
      ],
      e: 'binaryVersion is the SASS compute capability embedded; ptxVersion is the PTX virtual arch. Comparing them to the device tells you whether the kernel has native SASS or will JIT from PTX — handy for diagnosing compatibility/perf.',
      ref: 'cudaFuncGetAttributes (ptx/binary version)',
      tags: ['func-attributes', 'ptx', 'sass', 'compatibility'],
    },
    {
      d: 4,
      q: 'A kernel argument that is a large read-only lookup struct is best passed as __grid_constant__ by value (vs a device pointer) when…',
      o: [
        'It is mutable',
        'It fits within the parameter limit and is read uniformly — __grid_constant__ avoids an extra global allocation/indirection while the compiler reads it from the constant bank',
        'It is huge',
        'It changes per thread',
      ],
      a: 1,
      x: [
        'It is a pointer already',
        'It needs atomics',
        'It is in shared memory',
      ],
      e: '__grid_constant__ by-value passing suits moderate-size, uniform, read-only parameters: no separate cudaMalloc/copy, and efficient constant-bank access. For data exceeding the parameter limit or that’s mutable, use a device pointer.',
      ref: 'CUDA __grid_constant__ usage',
      tags: ['grid-constant', 'kernel-arguments', 'constant'],
    },
    {
      d: 4,
      q: 'Why does an out-of-bounds write sometimes corrupt UNRELATED data and produce confusing bugs?',
      o: [
        'It is always caught',
        'Without bounds checking, the write lands in whatever device memory that address maps to — possibly another buffer — silently corrupting it until detected (use compute-sanitizer memcheck)',
        'It only affects shared memory',
        'It crashes immediately always',
      ],
      a: 1,
      x: [
        'It is a compile error',
        'It is harmless',
        'It only affects the host',
      ],
      e: 'An OOB write to a valid-but-wrong address corrupts other data without an immediate fault, causing nondeterministic downstream bugs. compute-sanitizer memcheck catches OOB/misaligned accesses precisely, which is why it’s the first debugging tool.',
      ref: 'compute-sanitizer memcheck; OOB bugs',
      tags: ['out-of-bounds', 'memcheck', 'debugging'],
    },
    {
      d: 3,
      q: 'cudaHostUnregister is needed when you previously…',
      o: [
        'Called cudaMalloc',
        'Page-locked existing host memory with cudaHostRegister — you must unregister it before freeing/reusing it normally',
        'Created a stream',
        'Launched a kernel',
      ],
      a: 1,
      x: [
        'Called cudaMallocManaged',
        'Used constant memory',
        'Created an event',
      ],
      e: 'cudaHostRegister pins memory you own; cudaHostUnregister releases that pinning before you free the underlying allocation. Forgetting it can leave memory pinned (system pressure) or cause errors when the buffer is freed.',
      ref: 'CUDA Runtime API (cudaHostUnregister)',
      tags: ['host-register', 'unregister', 'pinned-memory'],
    },
    {
      d: 5,
      q: 'On a Grace Hopper system with ATS, calling a kernel on a pointer from plain malloc() (no cudaMalloc) can work because…',
      o: [
        'It copies to the GPU first',
        'Address Translation Services let the GPU translate and access the host virtual address directly (page-faulting over the coherent link), so unpinned host memory is GPU-accessible',
        'malloc returns device memory',
        'It is undefined behavior',
      ],
      a: 1,
      x: [
        'The CPU runs the kernel',
        'It uses constant memory',
        'It pins automatically',
      ],
      e: 'With ATS/HMM and a coherent CPU-GPU link, the GPU can resolve and access ordinary host virtual addresses (with on-demand faulting/migration), so even non-managed, non-pinned host pointers are usable by kernels — a major programmability simplification where supported.',
      ref: 'CUDA ATS/HMM; Grace Hopper',
      tags: ['ats', 'grace-hopper', 'host-memory'],
    },
    {
      d: 4,
      q: 'A reason to prefer cudaLaunchKernelEx over manual driver-API cluster launch in CUDA C++ is…',
      o: [
        'It is slower',
        'It integrates with the runtime API (kernels by symbol, runtime context) while still exposing cluster/PDL/cooperative attributes — avoiding dropping to the lower-level driver API',
        'It runs on the host',
        'It needs no attributes',
      ],
      a: 1,
      x: [
        'It disables clusters',
        'It compresses kernels',
        'It avoids the GPU',
      ],
      e: 'cudaLaunchKernelEx gives runtime-API ergonomics (launch by kernel symbol, shared primary context) plus the new launch attributes (clusters, PDL, cooperative), so you get Hopper features without the verbosity of explicit driver-API module/launch management.',
      ref: 'CUDA Runtime API (cudaLaunchKernelEx)',
      tags: ['cudaLaunchKernelEx', 'runtime-api', 'clusters'],
    },
    {
      d: 3,
      q: 'cudaDeviceProp.unifiedAddressing being true means…',
      o: [
        'The GPU has one memory',
        'Host and device share a single virtual address space (UVA), so the runtime can infer pointer locations (cudaMemcpyDefault) and enable P2P/mapped memory',
        'There is no host memory',
        'ECC is enabled',
      ],
      a: 1,
      x: [
        'Managed memory is on',
        'Tensor cores exist',
        'P2P is automatic',
      ],
      e: 'UVA (unifiedAddressing) places all allocations in one virtual address space, enabling pointer-type inference (cudaMemcpyDefault, cudaPointerGetAttributes) and underpinning peer access and mapped memory. It’s distinct from managed (Unified) Memory.',
      ref: 'cudaDeviceProp (unifiedAddressing); UVA',
      tags: ['uva', 'capability', 'address-space'],
    },
    {
      d: 4,
      q: 'Why might INT8 inference use __dp4a on older GPUs but tensor cores on newer ones for the same matmul?',
      o: [
        'They are unrelated',
        '__dp4a accelerates INT8 dot products on the CUDA-core ALU path (available widely), while newer GPUs have INT8 TENSOR cores with much higher throughput — the library picks the best available path',
        'dp4a is faster than tensor cores',
        'Tensor cores do not support INT8',
      ],
      a: 1,
      x: [
        'dp4a is FP16',
        'Tensor cores are slower',
        'Both are the same instruction',
      ],
      e: 'DP4A provides INT8 MACs on the general ALUs (a fallback/older path); INT8 tensor cores (Turing+) offer far higher throughput. Quantized-inference libraries dispatch to tensor cores where present and DP4A otherwise.',
      ref: 'INT8 paths; DP4A vs tensor cores',
      tags: ['dp4a', 'int8', 'tensor-cores', 'dispatch'],
    },
  ],
  { idPrefix: 'basics-e' }
)
