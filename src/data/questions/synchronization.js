import { defineQuestions } from './_helpers.js'

// Synchronization & Atomics — barriers, __syncthreads, atomics, fences,
// races, cooperative groups.
export default defineQuestions('synchronization', [
  {
    d: 1,
    q: 'What does __syncthreads() do?',
    o: [
      'Synchronizes all threads in the grid',
      'Acts as a barrier for all threads in the same block',
      'Synchronizes threads within a warp only',
      'Blocks the host until the kernel finishes',
    ],
    a: 1,
    e: '__syncthreads() is a block-level barrier: every thread in the block must reach it before any proceeds, and it also orders shared/global memory accesses within the block. It does not synchronize across blocks.',
    ref: 'CUDA C++ Programming Guide §7.6; PMPP Ch. 5',
    tags: ['syncthreads', 'barrier', 'block'],
  },
  {
    d: 3,
    q: 'Why is calling __syncthreads() inside a divergent branch (where only some threads reach it) dangerous?',
    o: [
      'It uses too many registers',
      'It can deadlock or cause undefined behavior because the barrier requires all threads of the block to arrive',
      'It always works fine',
      'It silently skips the barrier',
    ],
    a: 1,
    e: '__syncthreads() requires every thread in the block to execute the same barrier. If some threads take a path that skips it, the others wait forever (deadlock) or the result is undefined. Barriers must be reached by all threads uniformly.',
    ref: 'CUDA C++ Programming Guide §7.6',
    tags: ['syncthreads', 'divergence', 'deadlock'],
  },
  {
    d: 2,
    q: 'What problem do atomic operations (e.g. atomicAdd) solve?',
    o: [
      'They speed up coalesced writes',
      'They provide read-modify-write to a memory location without race conditions when many threads update it',
      'They synchronize the whole grid',
      'They reduce register usage',
    ],
    a: 1,
    e: 'When multiple threads update the same location (e.g. a histogram bin or accumulator), plain read-modify-write races. Atomics perform the update indivisibly, guaranteeing correctness at the cost of serialization under contention.',
    ref: 'CUDA C++ Programming Guide §B.14; PMPP Ch. 9',
    tags: ['atomics', 'race-condition'],
  },
  {
    d: 3,
    q: 'A histogram kernel with all threads doing atomicAdd to global bins is slow. A common optimization is to…',
    o: [
      'Remove the atomics and accept wrong results',
      'Accumulate into a per-block histogram in shared memory, then atomically merge to global once per bin',
      'Use a larger block size',
      'Switch to constant memory for the bins',
    ],
    a: 1,
    e: 'Privatization: each block builds a partial histogram in fast shared memory (lower-contention atomics), then does far fewer global atomics to combine. This drastically cuts global atomic traffic and contention.',
    ref: 'PMPP Ch. 9; CUDA Best Practices Guide',
    tags: ['atomics', 'histogram', 'privatization', 'shared-memory'],
  },
  {
    d: 2,
    q: 'Which return value does atomicAdd provide?',
    o: [
      'void',
      'The old value stored at the address before the addition',
      'The new value after the addition',
      'A boolean success flag',
    ],
    a: 1,
    e: 'atomicAdd(addr, val) atomically computes *addr += val and returns the OLD value. This is useful, e.g., to atomically claim a unique slot/index in a compaction or queue.',
    ref: 'CUDA C++ Programming Guide §B.14',
    tags: ['atomics', 'semantics'],
  },
  {
    d: 4,
    q: 'What does atomicCAS(addr, compare, val) do, and why is it powerful?',
    o: [
      'Adds val if it is larger; used for max',
      'Atomically: if *addr == compare, set *addr = val; returns old value — it can build arbitrary atomic operations via a retry loop',
      'Copies addr to compare',
      'Clears the address',
    ],
    a: 1,
    e: 'Compare-And-Swap is the universal atomic primitive. With a CAS-retry loop you can implement custom atomics (e.g. atomicMax on float, or atomic updates of structs) for types without native atomic support.',
    ref: 'CUDA C++ Programming Guide §B.14; PMPP Ch. 9',
    tags: ['atomics', 'cas', 'lock-free'],
  },
  {
    d: 4,
    q: 'Why does __syncthreads() alone NOT guarantee correctness for inter-block communication through global memory?',
    o: [
      'It only orders register accesses',
      'It synchronizes within a block; across blocks you need memory fences (__threadfence) and the blocks may not even be concurrently resident',
      'It is too slow',
      'Global memory cannot be shared between blocks',
    ],
    a: 1,
    e: '__syncthreads scopes to a block. For visibility of global writes across blocks you need a memory fence (__threadfence) to order writes, and even then blocks aren’t guaranteed concurrent — true grid sync needs cooperative groups.',
    ref: 'CUDA C++ Programming Guide §B.5 (memory fence), §7.6',
    tags: ['memory-fence', 'threadfence', 'inter-block'],
  },
  {
    d: 4,
    q: 'What is the role of __threadfence()?',
    o: [
      'It blocks all threads until they reach it',
      'It ensures the calling thread’s prior memory writes are visible to other threads (at device scope) before subsequent writes — an ordering fence, not a barrier',
      'It flushes the L2 cache to host',
      'It synchronizes the warp',
    ],
    a: 1,
    e: '__threadfence() is a memory ordering primitive: it guarantees that writes made before the fence become visible (in order) to other threads device-wide before writes after it. It does not make threads wait for each other (that is a barrier).',
    ref: 'CUDA C++ Programming Guide §B.5',
    tags: ['memory-fence', 'ordering', 'visibility'],
  },
  {
    d: 3,
    q: 'In the classic "threadfence reduction" / last-block trick, atomicInc + __threadfence is used to…',
    o: [
      'Speed up shared-memory reductions',
      'Let the last block to finish detect that all other blocks’ partial results are written and visible, so it can do the final combine in one launch',
      'Avoid using shared memory',
      'Synchronize warps',
    ],
    a: 1,
    e: 'Each block writes its partial result, issues __threadfence() so it is globally visible, then atomically increments a counter. The block that sees the count reach gridDim is the last one and safely reduces all partials — single-kernel reduction.',
    ref: 'CUDA SDK threadFenceReduction sample; PMPP Ch. 10',
    tags: ['reduction', 'threadfence', 'atomics'],
  },
  {
    d: 2,
    q: 'A data race in a CUDA kernel occurs when…',
    o: [
      'Two threads read the same value',
      'Two or more threads access the same memory location concurrently, at least one writes, with no synchronization ordering them',
      'A thread reads constant memory',
      'A warp diverges',
    ],
    a: 1,
    e: 'A race needs concurrent accesses to the same location where at least one is a write, without synchronization (barrier/atomic/fence) defining an order. Results become nondeterministic. Tools like compute-sanitizer --tool racecheck detect them.',
    ref: 'CUDA C++ Programming Guide; compute-sanitizer docs',
    tags: ['race-condition', 'correctness'],
  },
  {
    d: 3,
    q: 'Why can atomics on global memory become a serialization bottleneck?',
    o: [
      'They disable caching',
      'When many threads target the same address, the hardware serializes the updates, so contention scales poorly',
      'They require __syncthreads',
      'They always go to local memory',
    ],
    a: 1,
    e: 'Atomic updates to a hot address must be applied one at a time, so high contention (many threads, few addresses) serializes and throughput collapses. Mitigations: privatization, fewer/wider atomics, warp-aggregation, or shared-memory atomics.',
    ref: 'PMPP Ch. 9; CUDA Best Practices Guide',
    tags: ['atomics', 'contention', 'serialization'],
  },
  {
    d: 4,
    q: 'Warp-aggregated atomics improve performance by…',
    o: [
      'Using one atomic per thread but in shared memory',
      'Having the warp compute a single combined update (e.g. via ballot/prefix-sum) and issuing one atomic per warp instead of per thread',
      'Disabling atomics for diverged warps',
      'Replacing atomics with __syncthreads',
    ],
    a: 1,
    e: 'When threads in a warp increment the same counter, they can use __ballot/__popc and a shuffle-based prefix sum to elect one leader that performs a single atomicAdd of the warp’s total, cutting atomic instructions ~32×.',
    ref: 'NVIDIA Developer Blog, "CUDA Pro Tip: Warp-Aggregated Atomics"',
    tags: ['atomics', 'warp-aggregation', 'optimization'],
  },
  {
    d: 5,
    q: 'On Hopper, the asynchronous transaction barrier (mbarrier) is used primarily to…',
    o: [
      'Replace __syncthreads for all kernels',
      'Coordinate completion of asynchronous operations (e.g. TMA/cp.async copies) so consumer threads wait until data has arrived in shared memory',
      'Synchronize across GPUs',
      'Order atomic operations',
    ],
    a: 1,
    e: 'mbarrier (async/transaction barriers) let producer async copies signal arrival of a known number of bytes/threads; consumers wait on the barrier’s phase. This decouples data movement (TMA/cp.async) from compute in pipelined GEMM/attention kernels.',
    ref: 'CUDA C++ Programming Guide (asynchronous barrier); Hopper Whitepaper',
    tags: ['mbarrier', 'async', 'hopper', 'pipelining'],
  },
  {
    d: 2,
    q: 'After writing to shared memory in one phase and reading it in the next, you must place __syncthreads() because…',
    o: [
      'Shared memory is slow',
      'Without the barrier, a thread might read a location before the producing thread has written it',
      'It resets shared memory',
      'It improves coalescing',
    ],
    a: 1,
    e: 'Shared-memory producer/consumer patterns (e.g. tiled GEMM) require a barrier between the write phase and the read phase so all writes are complete and visible before any thread reads, preventing reading stale/uninitialized data.',
    ref: 'PMPP Ch. 5; CUDA C++ Programming Guide §7.6',
    tags: ['syncthreads', 'shared-memory', 'producer-consumer'],
  },
])
