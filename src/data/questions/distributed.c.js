import { defineQuestions } from './_helpers.js'

// Distributed Training — batch C. Device meshes, data sharding, DDP/FSDP knobs,
// vocab/loss parallel, MoE balancing, FLOP/memory estimates. Distinct from distributed.js/.b.js.
export default defineQuestions(
  'distributed',
  [
    {
      d: 4,
      q: 'A "device mesh" abstraction (e.g. torch DeviceMesh) is used to…',
      o: [
        'Pick a GPU',
        'Organize ranks into an N-dimensional grid (e.g. [data, tensor, pipeline]) so each parallelism axis maps to a mesh dimension and gets its own communicator',
        'Allocate memory',
        'Profile kernels',
      ],
      a: 1,
      x: [
        'Compress gradients',
        'Schedule streams',
        'Replace NCCL',
      ],
      e: 'A device mesh names the parallelism dimensions and maps ranks to coordinates, letting frameworks create per-axis process groups (DP across one dim, TP across another) and express sharding cleanly for 2D/3D parallelism.',
      ref: 'PyTorch DeviceMesh; n-D parallelism',
      tags: ['device-mesh', 'parallelism', 'process-groups'],
    },
    {
      d: 3,
      q: 'DistributedSampler in data-parallel training ensures…',
      o: [
        'Gradients are averaged',
        'Each rank sees a disjoint shard of the dataset per epoch (no overlap), so the global batch covers distinct samples',
        'The model is sharded',
        'The optimizer is offloaded',
      ],
      a: 1,
      x: [
        'All ranks see the same data',
        'The loss is parallel',
        'Activations are checkpointed',
      ],
      e: 'DistributedSampler partitions indices across ranks (with shuffling seeded per epoch) so replicas process different samples. Without it, every rank would train on the same data, defeating the purpose of data parallelism.',
      ref: 'PyTorch DistributedSampler',
      tags: ['data-sharding', 'sampler', 'data-parallel'],
    },
    {
      d: 4,
      q: 'DDP’s find_unused_parameters=True is sometimes needed because…',
      o: [
        'It speeds up training',
        'If some parameters don’t receive gradients in a given forward (dynamic graphs), DDP must detect them so the all-reduce doesn’t wait forever for their (never-produced) gradients',
        'It shards the model',
        'It enables FP8',
      ],
      a: 1,
      x: [
        'It increases batch size',
        'It disables NCCL',
        'It checkpoints activations',
      ],
      e: 'DDP marks a bucket ready when all its params’ grads arrive. If a param is unused this step, its grad never comes and the reduce stalls. find_unused_parameters traces the graph to handle this — at some overhead, so avoid it when the graph is static.',
      ref: 'PyTorch DDP (find_unused_parameters)',
      tags: ['ddp', 'unused-parameters', 'dynamic-graph'],
    },
    {
      d: 4,
      q: 'FSDP HYBRID_SHARD shards parameters…',
      o: [
        'Across all GPUs globally',
        'WITHIN a node (or group) while REPLICATING across nodes — combining ZeRO-3-style sharding intra-node with data parallelism inter-node to cut cross-node all-gather traffic',
        'Only the optimizer',
        'Across pipeline stages',
      ],
      a: 1,
      x: [
        'Nothing',
        'Across experts',
        'Across the batch',
      ],
      e: 'Hybrid sharding keeps the expensive all-gather/reduce-scatter on fast intra-node links and uses cheaper data-parallel all-reduce across nodes, trading some memory savings for much less slow inter-node communication at scale.',
      ref: 'PyTorch FSDP (HYBRID_SHARD)',
      tags: ['fsdp', 'hybrid-shard', 'topology'],
    },
    {
      d: 5,
      q: 'Vocabulary (output) parallelism with a "parallel cross entropy" avoids…',
      o: [
        'Computing the loss',
        'Gathering the full (huge) logits tensor across tensor-parallel ranks — each rank computes its shard’s contribution to the softmax/loss with small all-reduces of scalars instead',
        'The backward pass',
        'Using tensor cores',
      ],
      a: 1,
      x: [
        'The embedding lookup',
        'Gradient all-reduce',
        'Activation checkpointing',
      ],
      e: 'For large vocabularies, the logits are sharded across TP ranks. A vocab-parallel cross-entropy computes local max/sum and combines via tiny all-reduces, avoiding materializing/gathering the full [batch×seq×vocab] logits — a big memory/comm saving.',
      ref: 'Megatron-LM (vocab-parallel cross entropy)',
      tags: ['vocab-parallel', 'cross-entropy', 'tensor-parallel'],
    },
    {
      d: 4,
      q: 'A rule-of-thumb for transformer training compute is ~6N FLOPs per token (N = parameters). This is used to…',
      o: [
        'Estimate memory',
        'Estimate total training FLOPs (6 × N × tokens) for planning hardware/time and computing MFU',
        'Set the learning rate',
        'Choose the batch size',
      ],
      a: 1,
      x: [
        'Pick the optimizer',
        'Size the vocabulary',
        'Select precision',
      ],
      e: 'The 6N estimate (2N forward + 4N backward) per token lets you approximate total training FLOPs and required GPU-hours, and to compute MFU = achieved/peak. It’s a planning heuristic, not exact (ignores attention’s seq² term, etc.).',
      ref: 'Kaplan et al.; Chinchilla; FLOP accounting',
      tags: ['flops', '6n-rule', 'mfu'],
    },
    {
      d: 4,
      q: 'For mixed-precision Adam, total optimizer+master memory per parameter is roughly…',
      o: [
        '2 bytes',
        '~16–18 bytes (FP16/BF16 weight 2 + grad 2 + FP32 master 4 + Adam m 4 + v 4)',
        '32 bytes',
        '4 bytes',
      ],
      a: 1,
      x: ['8 bytes', '12 bytes', '64 bytes'],
      e: 'A common accounting: 2 (half weight) + 2 (half grad) + 4 (FP32 master) + 4 (m) + 4 (v) ≈ 16 bytes/param (plus activations separately). This is why a 70B model needs sharding (ZeRO/FSDP) to fit — and what those techniques distribute.',
      ref: 'ZeRO memory accounting; mixed-precision Adam',
      tags: ['memory', 'adam', 'accounting'],
    },
    {
      d: 5,
      q: 'MoE training adds a load-balancing (auxiliary) loss because…',
      o: [
        'It improves accuracy directly',
        'Without it the router may send most tokens to a few experts (collapse), causing imbalance and wasted capacity; the aux loss encourages uniform expert utilization',
        'It reduces memory',
        'It speeds up all-to-all',
      ],
      a: 1,
      x: [
        'It quantizes the experts',
        'It shards the optimizer',
        'It replaces the router',
      ],
      e: 'Routers tend to over-use a few experts. An auxiliary load-balance loss (and capacity limits) spread tokens more evenly, improving utilization and stability — otherwise some experts/GPUs are overloaded while others idle (and tokens are dropped).',
      ref: 'Switch Transformer; GShard (load balancing loss)',
      tags: ['moe', 'load-balancing', 'router'],
    },
    {
      d: 4,
      q: 'MoE "capacity factor" controls…',
      o: [
        'The model size',
        'How many tokens each expert can accept per batch (capacity); tokens beyond capacity are dropped or rerouted, trading compute/memory for handling imbalance',
        'The learning rate',
        'The number of GPUs',
      ],
      a: 1,
      x: [
        'The precision',
        'The sequence length',
        'The optimizer',
      ],
      e: 'Capacity factor sets a per-expert token budget so the all-to-all and expert compute have bounded, balanced sizes. Higher capacity reduces dropped tokens (better quality) but costs more compute/communication; it’s a key MoE tuning knob.',
      ref: 'GShard; MoE capacity factor',
      tags: ['moe', 'capacity-factor', 'routing'],
    },
    {
      d: 4,
      q: 'Elastic / fault-tolerant training (e.g. torchrun --max-restarts, torch.distributed.elastic) aims to…',
      o: [
        'Increase batch size',
        'Let a job survive worker failures or scale the number of workers, by re-rendezvousing the process group and resuming from a checkpoint',
        'Improve accuracy',
        'Reduce memory',
      ],
      a: 1,
      x: [
        'Compress gradients',
        'Disable NCCL',
        'Quantize weights',
      ],
      e: 'Elastic training handles node failures/membership changes by re-forming the process group and restoring from checkpoints, crucial at large scale where hardware failures are frequent over long runs. It pairs with frequent sharded checkpointing.',
      ref: 'PyTorch Elastic (torchrun)',
      tags: ['elastic', 'fault-tolerance', 'checkpointing'],
    },
    {
      d: 3,
      q: 'For reproducible data-parallel training, you typically seed so that…',
      o: [
        'All ranks use the same seed for everything',
        'Model init/dropout are consistent where needed (often same seed for weights via broadcast) but the data sampler differs per rank — careful seeding avoids all ranks seeing identical augmentations/data',
        'Seeds are irrelevant',
        'Only rank 0 is seeded',
      ],
      a: 1,
      x: [
        'Each rank picks a random seed',
        'Seeds disable shuffling',
        'Seeds set the batch size',
      ],
      e: 'You want identical initial weights across replicas (broadcast or same seed) but rank-dependent data ordering/augmentation (so ranks don’t process identical samples). Mismanaged seeding either desyncs weights or makes all ranks see the same data.',
      ref: 'Distributed reproducibility; seeding',
      tags: ['seeding', 'reproducibility', 'data-parallel'],
    },
    {
      d: 4,
      q: 'Overlapping the optimizer step with gradient communication (e.g. optimizer-in-backward) helps by…',
      o: [
        'Reducing FLOPs',
        'Applying updates to parameters whose gradients have already been all-reduced while later gradients are still communicating, hiding optimizer/comm latency',
        'Increasing batch size',
        'Lowering precision',
      ],
      a: 1,
      x: [
        'Disabling the backward',
        'Avoiding the all-reduce',
        'Sharding the data',
      ],
      e: 'Fusing/overlapping the optimizer with the backward (updating each bucket as its all-reduce completes) hides communication and optimizer cost behind ongoing backprop, improving step throughput — used in FSDP/DDP optimizations.',
      ref: 'Optimizer/backward overlap; FSDP',
      tags: ['overlap', 'optimizer', 'communication'],
    },
    {
      d: 3,
      q: 'In tensor-parallel transformers, the token embedding and output projection are often "vocab-parallel," meaning…',
      o: [
        'The whole embedding is replicated',
        'The vocabulary dimension is split across TP ranks, so each holds a slice of the embedding/output weights, combined via all-reduce/all-gather',
        'Embeddings are on the CPU',
        'Each rank has the full vocab',
      ],
      a: 1,
      x: [
        'The batch is split',
        'The sequence is split',
        'Nothing is split',
      ],
      e: 'Splitting the (large) vocabulary across TP ranks shards the embedding and final projection, reducing per-GPU memory. Lookups/logits are reconciled with collectives (and vocab-parallel cross-entropy for the loss).',
      ref: 'Megatron-LM (vocab parallelism)',
      tags: ['vocab-parallel', 'embedding', 'tensor-parallel'],
    },
    {
      d: 5,
      q: 'ZeRO++ reduces ZeRO-3 communication via techniques like…',
      o: [
        'Removing sharding',
        'Quantized weight communication (qwZ), hierarchical (intra-node) weight partitioning to keep all-gathers local, and quantized gradients — cutting the extra ZeRO-3 traffic',
        'Using the CPU only',
        'Disabling all-gather',
      ],
      a: 1,
      x: [
        'Increasing precision',
        'Bigger batches',
        'Pipeline parallelism',
      ],
      e: 'ZeRO-3’s all-gathers add communication; ZeRO++ lowers it by quantizing the gathered weights, holding a secondary partition within the node (so all-gathers stay on fast links), and quantizing gradients for reduce-scatter — improving scaling on slower networks.',
      ref: 'DeepSpeed ZeRO++',
      tags: ['zero++', 'communication', 'quantization'],
    },
    {
      d: 3,
      q: 'DDP "static_graph" mode can be enabled when…',
      o: [
        'The graph changes every step',
        'The model’s autograd graph is the same every iteration, letting DDP optimize bucketing/overlap (and handle some unused-parameter cases) more efficiently',
        'You use a single GPU',
        'You disable NCCL',
      ],
      a: 1,
      x: [
        'You change the batch size',
        'You shard the model',
        'You use FP8',
      ],
      e: 'static_graph tells DDP the structure is fixed across steps, enabling optimizations (stable bucket order, one-time analysis, activation-checkpoint compatibility) — but it’s incorrect if the graph actually varies between iterations.',
      ref: 'PyTorch DDP (static_graph)',
      tags: ['ddp', 'static-graph', 'optimization'],
    },
    {
      d: 4,
      q: 'Activation offloading (moving stored activations to CPU memory during forward, fetching them in backward) trades…',
      o: [
        'Accuracy for speed',
        'PCIe transfer time (and CPU memory) for reduced GPU activation memory — useful when recomputation is too costly but GPU memory is the bottleneck',
        'Compute for communication',
        'Precision for range',
      ],
      a: 1,
      x: [
        'Nothing',
        'GPUs for CPUs entirely',
        'Gradients for weights',
      ],
      e: 'Offloading activations to host RAM frees GPU memory; the cost is moving them back over PCIe for the backward. It complements checkpointing (recompute) — choose based on whether spare PCIe bandwidth or spare compute is cheaper.',
      ref: 'Activation offloading; memory-saving techniques',
      tags: ['activation-offload', 'memory', 'pcie'],
    },
    {
      d: 4,
      q: 'In 3D parallelism, the typical hardware mapping is…',
      o: [
        'DP within node, TP across nodes',
        'TP within a node (NVLink), PP across a few nodes, DP across replica groups — matching each method’s communication intensity to link speed',
        'Everything across nodes equally',
        'PP within a warp',
      ],
      a: 1,
      x: [
        'TP across nodes, PP within node',
        'DP within a warp',
        'All within one GPU',
      ],
      e: 'TP (heaviest, per-layer collectives) maps to intra-node NVLink; PP (light P2P) spans nodes; DP (one overlappable all-reduce/step) spans replicas. This layout minimizes traffic on slow links — the standard Megatron-DeepSpeed recipe.',
      ref: 'Megatron-DeepSpeed; 3D parallelism mapping',
      tags: ['3d-parallelism', 'mapping', 'topology'],
    },
    {
      d: 3,
      q: 'Megatron-LM and DeepSpeed are often combined because…',
      o: [
        'They do the same thing',
        'Megatron provides efficient tensor/pipeline parallelism and DeepSpeed provides ZeRO data-parallel memory optimization — together enabling 3D parallelism for very large models',
        'One is for inference only',
        'They both replace NCCL',
      ],
      a: 1,
      x: [
        'They are incompatible',
        'They are profilers',
        'They are data loaders',
      ],
      e: 'Megatron excels at model (tensor/pipeline) parallelism; DeepSpeed contributes ZeRO sharding and training infrastructure. Megatron-DeepSpeed integrations let you compose them for memory-efficient, large-scale 3D-parallel training.',
      ref: 'Megatron-DeepSpeed',
      tags: ['megatron', 'deepspeed', '3d-parallelism'],
    },
    {
      d: 4,
      q: 'Smaller micro-batches in pipeline parallelism reduce the bubble but…',
      o: [
        'Have no downside',
        'Increase the relative overhead per micro-batch (more pipeline flushes, smaller GEMMs with lower efficiency) — so there is an optimal micro-batch size',
        'Eliminate communication',
        'Increase memory linearly',
      ],
      a: 1,
      x: [
        'Always speed up training',
        'Disable the optimizer',
        'Remove activations',
      ],
      e: 'More micro-batches shrink the bubble fraction but each is smaller, so per-micro-batch overhead grows and matmuls get less efficient (lower MFU per GEMM). The micro-batch count balances bubble reduction against compute efficiency.',
      ref: 'Pipeline parallelism; micro-batch tuning',
      tags: ['pipeline', 'micro-batch', 'bubble'],
    },
    {
      d: 5,
      q: 'Why is the attention term often excluded from the simple 6N FLOP estimate, and when does it matter?',
      o: [
        'It is always negligible',
        'Attention’s cost scales with sequence length² (not just parameters), so for long sequences it can rival or exceed the 6N term and must be added separately',
        'It is part of 6N',
        'It only matters in inference',
      ],
      a: 1,
      x: [
        'It scales with batch only',
        'It is constant',
        'It is part of the optimizer',
      ],
      e: 'The 6N rule counts the dense (parameter-proportional) matmuls. Self-attention adds an O(seq²·d) term per layer; for long contexts this becomes significant, so FLOP/memory planning for long-sequence models must include it explicitly.',
      ref: 'FLOP accounting; attention complexity',
      tags: ['flops', 'attention', 'sequence-length'],
    },
    {
      d: 3,
      q: 'When data-parallel training "scales poorly" (throughput per GPU drops as GPUs increase), the usual first suspect is…',
      o: [
        'The optimizer',
        'Gradient communication (all-reduce) not overlapping/fitting within backward time — comm becomes the bottleneck as the cluster/network grows',
        'The dataset',
        'The learning rate',
      ],
      a: 1,
      x: [
        'The activation function',
        'The random seed',
        'The block size',
      ],
      e: 'DP efficiency hinges on hiding the all-reduce behind backward compute. As you add GPUs (more/slower links) or shrink per-GPU work, comm can exceed compute and stop overlapping — profile the comm/compute ratio and improve overlap/bucketing.',
      ref: 'DP scaling analysis; overlap',
      tags: ['data-parallel', 'scaling', 'communication'],
    },
    {
      d: 4,
      q: 'Pipeline parallelism’s point-to-point activation transfers are relatively cheap, but its main cost is…',
      o: [
        'Network bandwidth',
        'The pipeline bubble (idle stages during fill/drain) and the need to store in-flight activations — addressed by more micro-batches and 1F1B/interleaved/zero-bubble schedules',
        'All-reduce',
        'Optimizer states',
      ],
      a: 1,
      x: [
        'Embedding lookups',
        'Data loading',
        'Tensor cores',
      ],
      e: 'PP’s communication is light, so its efficiency challenge is the bubble (idle time) and activation memory for in-flight micro-batches. Scheduling (1F1B, interleaved, zero-bubble) and micro-batch count are the levers, not bandwidth.',
      ref: 'Pipeline parallelism; bubble and scheduling',
      tags: ['pipeline', 'bubble', 'scheduling'],
    },
    {
      d: 4,
      q: 'A practical reason BF16 (not FP16) is the default for large-model training is…',
      o: [
        'BF16 is more precise',
        'BF16’s FP32-range exponent avoids the overflow/underflow that forces FP16 loss scaling, making large-scale training simpler and more stable',
        'BF16 uses less memory',
        'FP16 is unsupported',
      ],
      a: 1,
      x: [
        'BF16 is faster on all GPUs',
        'BF16 needs no accumulation',
        'FP16 cannot use tensor cores',
      ],
      e: 'At scale, FP16’s narrow range makes loss scaling finicky; BF16 keeps FP32’s range so gradients rarely overflow/underflow, removing loss-scaling complexity. Both are 2 bytes and tensor-core accelerated; BF16 wins on robustness.',
      ref: 'Large-model training practice; BF16 vs FP16',
      tags: ['bf16', 'fp16', 'stability', 'training'],
    },
    {
      d: 5,
      q: 'Context parallelism (e.g. ring/striped attention) is combined with tensor parallelism to…',
      o: [
        'Replace data parallelism',
        'Scale BOTH the model width (TP) and the sequence length (CP) so very long contexts fit and compute is distributed across the sequence dimension',
        'Reduce the batch',
        'Shard the optimizer',
      ],
      a: 1,
      x: [
        'Quantize weights',
        'Remove attention',
        'Increase precision',
      ],
      e: 'TP partitions hidden dimensions; CP partitions the sequence so each GPU holds part of the tokens and exchanges K/V (ring attention) — together enabling long-context training of wide models that neither could handle alone.',
      ref: 'Ring/striped attention; Megatron context parallelism',
      tags: ['context-parallel', 'tensor-parallel', 'long-context'],
    },
    {
      d: 3,
      q: 'Checkpointing (saving) during long training is done frequently because…',
      o: [
        'It speeds up training',
        'Hardware failures and preemption are common over days/weeks of large-scale training; recent checkpoints bound the lost work on restart',
        'It improves accuracy',
        'It reduces memory',
      ],
      a: 1,
      x: [
        'It shards the model',
        'It averages gradients',
        'It quantizes weights',
      ],
      e: 'At scale, failures are routine, so periodic (sharded, parallel) checkpoints limit wasted compute when a job must restart. Checkpoint frequency trades I/O overhead against the amount of recomputation after a failure.',
      ref: 'Large-scale training operations; checkpointing',
      tags: ['checkpointing', 'fault-tolerance', 'operations'],
    },
    {
      d: 4,
      q: 'In FSDP, "reshard_after_forward" controls whether a unit’s gathered parameters are freed after the forward pass. Keeping them (no reshard) trades…',
      o: [
        'Accuracy for speed',
        'Higher memory (params stay unsharded) for avoiding a second all-gather in the backward — a speed/memory knob per FSDP unit',
        'Compute for precision',
        'Communication for nothing',
      ],
      a: 1,
      x: [
        'GPUs for CPUs',
        'Batch for sequence',
        'Gradients for weights',
      ],
      e: 'If you reshard after forward, you must all-gather again for backward (saves memory, costs comm); if not, you keep the full params resident (uses memory, saves the second all-gather). FSDP exposes this trade-off per wrapped unit.',
      ref: 'PyTorch FSDP (reshard_after_forward)',
      tags: ['fsdp', 'reshard', 'memory-comm-tradeoff'],
    },
    {
      d: 5,
      q: 'A common signature that a large-model run is COMMUNICATION-bound rather than compute-bound is…',
      o: [
        'High SM utilization',
        'Low MFU with significant time in NCCL kernels on the timeline (gaps in compute waiting on collectives) — pointing to overlap/topology/bandwidth issues',
        'High DRAM bandwidth',
        'Many register spills',
      ],
      a: 1,
      x: [
        'Tensor cores saturated',
        'A CPU bottleneck only',
        'High occupancy',
      ],
      e: 'Communication-bound jobs show low MFU and timeline gaps where SMs idle during NCCL collectives. Remedies: better overlap (bucketing, prefetch), topology-aware placement, faster interconnect, or shifting the parallelism balance.',
      ref: 'Distributed profiling; MFU and NCCL time',
      tags: ['communication-bound', 'mfu', 'profiling'],
    },
    {
      d: 3,
      q: 'The simplest way to increase the effective batch size without more GPUs or memory is…',
      o: [
        'Tensor parallelism',
        'Gradient accumulation (sum gradients over several micro-steps before one optimizer step)',
        'Pipeline parallelism',
        'ZeRO-3',
      ],
      a: 1,
      x: [
        'Reducing the learning rate',
        'Quantization',
        'Checkpointing',
      ],
      e: 'Gradient accumulation runs K micro-batches and accumulates their grads before stepping, giving an effective batch of K× the micro-batch with the same memory — at the cost of more compute per optimizer step. Adjust LR for the larger batch.',
      ref: 'Gradient accumulation; effective batch',
      tags: ['gradient-accumulation', 'batch-size', 'memory'],
    },
  ],
  { idPrefix: 'distributed-c' }
)
