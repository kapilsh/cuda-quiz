import { defineQuestions } from './_helpers.js'

// Synchronization & Atomics — batch C. libcu++ atomics/scopes/semaphores,
// lock-free structures, ABA, pipelines. Distinct from synchronization.js/.b.js.
export default defineQuestions(
  'synchronization',
  [
    {
      d: 4,
      q: 'cuda::atomic<int, cuda::thread_scope_block> differs from a device-scope atomic in that its ordering guarantees…',
      o: [
        'Extend to the host',
        'Apply only within the block (cheaper)',
        'Are stronger (system-wide)',
        'Do not exist',
      ],
      a: 1,
      x: [
        'Cover all GPUs',
        'Require a cooperative launch',
        'Disable caching',
      ],
      e: 'Scoping an atomic to the block restricts its coherence/ordering to that block, which the hardware can satisfy more cheaply (within the SM) than a device- or system-scoped atomic. Match the scope to the actual sharing.',
      ref: 'libcu++ <cuda/atomic>; thread scopes',
      tags: ['atomics', 'thread-scope', 'libcu++'],
    },
    {
      d: 4,
      q: 'cuda::atomic_ref lets you…',
      o: [
        'Allocate atomics',
        'Do atomics on existing non-atomic memory',
        'Reference the host',
        'Disable atomicity',
      ],
      a: 1,
      x: [
        'Create a lock',
        'Pin memory',
        'Reduce across a warp',
      ],
      e: 'atomic_ref wraps a reference to ordinary memory so you can perform scoped, ordered atomic ops on it on demand — useful when only some accesses to a buffer need to be atomic, without changing the buffer’s type.',
      ref: 'libcu++ <cuda/atomic>; C++20 atomic_ref',
      tags: ['atomic-ref', 'libcu++', 'atomics'],
    },
    {
      d: 5,
      q: 'The ABA problem in lock-free GPU code occurs when…',
      o: [
        'Two atomics race',
        'CAS succeeds after A→B→A hides a change',
        'A barrier is skipped',
        'Memory is uninitialized',
      ],
      a: 1,
      x: [
        'Two blocks deadlock',
        'A fence is missing',
        'Atomics overflow',
      ],
      e: 'CAS only checks the value, not its history. If it goes A→B→A, a stale pointer/index can be reused incorrectly. Mitigations: tagged pointers (value + version counter) so the version differs even when the value repeats.',
      ref: 'Lock-free programming; ABA problem',
      tags: ['aba', 'lock-free', 'cas'],
    },
    {
      d: 4,
      q: 'compare_exchange_weak (vs _strong) may fail "spuriously," which is acceptable in a CAS loop because…',
      o: [
        'It never matters',
        'The loop retries; weak is cheaper',
        'Weak is always wrong',
        'Strong cannot be used in loops',
      ],
      a: 1,
      x: [
        'Weak ignores the expected value',
        'Weak is non-atomic',
        'It avoids the comparison',
      ],
      e: 'compare_exchange_weak can fail even when values match (spurious failure), permitting a more efficient implementation. Since you already loop on failure, weak is fine inside retry loops; use strong for single-shot attempts.',
      ref: 'C++ memory model; libcu++ atomics',
      tags: ['compare-exchange', 'weak-strong', 'cas'],
    },
    {
      d: 4,
      q: 'memory_order_seq_cst (sequential consistency) is the strongest/default ordering. On GPUs it is often avoided for hot atomics because…',
      o: [
        'It is incorrect',
        'Its total global order adds fences/serialization',
        'It is unsupported',
        'It only works on the host',
      ],
      a: 1,
      x: [
        'It is non-atomic',
        'It disables caching',
        'It requires cooperative launch',
      ],
      e: 'seq_cst guarantees a single global order of all seq_cst operations, which can be expensive on a weakly-ordered machine. For counters (relaxed) or producer/consumer publication (release/acquire), weaker orders are correct and faster.',
      ref: 'C++/CUDA memory model; libcu++',
      tags: ['memory-order', 'seq-cst', 'performance'],
    },
    {
      d: 4,
      q: 'cuda::barrier’s arrive_and_wait() splits internally into arrive() + wait(). The advantage over __syncthreads is that a thread can…',
      o: [
        'Skip the barrier',
        'Do work between arrive() and wait()',
        'Synchronize across blocks',
        'Avoid memory ordering',
      ],
      a: 1,
      x: [
        'Run on the host',
        'Use fewer registers',
        'Reduce divergence',
      ],
      e: 'By separating arrive (I’m here) from wait (everyone here?), a thread can perform unrelated work in between, hiding barrier latency — the basis of asynchronous, pipelined synchronization and async-copy completion tracking.',
      ref: 'CUDA C++ Programming Guide (Asynchronous Barrier); libcu++',
      tags: ['barrier', 'arrive-wait', 'overlap'],
    },
    {
      d: 5,
      q: 'In an mbarrier-based pipeline, "expect_tx" / setting the expected transaction count is used so that…',
      o: [
        'Threads count each other',
        'Completes after N bytes (TMA/cp.async) arrive',
        'It counts warps',
        'It times the kernel',
      ],
      a: 1,
      x: [
        'It reduces registers',
        'It picks the GPU',
        'It orders atomics',
      ],
      e: 'Hopper mbarriers track both arrivals and a transaction (byte) count. The consumer’s wait completes when the expected bytes have landed, so async copies signal completion by data volume — decoupling producers (copies) from consumers (math).',
      ref: 'CUDA C++ Programming Guide (mbarrier); Hopper TMA',
      tags: ['mbarrier', 'transaction-count', 'tma', 'pipeline'],
    },
    {
      d: 4,
      q: 'A bounded producer/consumer ring buffer in shared memory typically needs two counters (head/tail) plus…',
      o: [
        'Nothing',
        'Release/acquire ordering on the slots',
        'A spinlock per element',
        'Constant memory',
      ],
      a: 1,
      x: [
        'A global reduction',
        'Texture memory',
        'A cooperative launch',
      ],
      e: 'The counters coordinate slot availability, but you also need memory ordering so the data write "happens before" the consumer’s read (release/acquire or a barrier). Without it the consumer can see an updated index but stale data.',
      ref: 'Producer/consumer patterns; CUDA memory model',
      tags: ['ring-buffer', 'producer-consumer', 'ordering'],
    },
    {
      d: 3,
      q: 'cuda::counting_semaphore on the device can be used to…',
      o: [
        'Replace all atomics',
        'Limit concurrent access to N participants',
        'Synchronize the whole grid for free',
        'Time kernels',
      ],
      a: 1,
      x: [
        'Allocate memory',
        'Broadcast values',
        'Disable divergence',
      ],
      e: 'libcu++ provides counting/binary semaphores with thread scopes for coordinating access to limited resources among GPU threads. As with locks, use carefully under SIMT to avoid warp-level deadlock; lock-free designs are often preferable.',
      ref: 'libcu++ <cuda/semaphore>',
      tags: ['semaphore', 'libcu++', 'synchronization'],
    },
    {
      d: 5,
      q: 'A lock-free Treiber stack on the GPU pushes via a CAS on the head pointer. Its main scalability problem under heavy contention is…',
      o: [
        'It deadlocks',
        'All ops contend on the single head pointer',
        'It uses too much shared memory',
        'It cannot be implemented',
      ],
      a: 1,
      x: [
        'It needs a cooperative launch',
        'It loses data',
        'It requires FP64',
      ],
      e: 'A single shared head is a hot contention point; thousands of threads CAS-looping on it serialize and the retry rate explodes (plus ABA risk). Elimination/back-off, per-warp aggregation, or segmented structures scale better.',
      ref: 'Lock-free data structures on GPUs; Treiber stack',
      tags: ['lock-free', 'treiber-stack', 'contention'],
    },
    {
      d: 3,
      q: 'A device-wide flag that one block sets and others poll requires the readers to use volatile (or an acquire atomic) because…',
      o: [
        'It is faster',
        'Else the compiler caches it in a register',
        'volatile makes it atomic',
        'It reduces registers',
      ],
      a: 1,
      x: [
        'It coalesces the read',
        'It pins the flag in L1',
        'It orders all memory',
      ],
      e: 'Without volatile/atomic-acquire, the compiler can hoist the flag load out of the spin loop (caching it in a register), so the thread never sees the update. volatile forces a fresh memory read each iteration; pair with a fence for data visibility.',
      ref: 'CUDA memory model; spin-wait correctness',
      tags: ['volatile', 'spin-wait', 'flag'],
    },
    {
      d: 4,
      q: 'A memory fence (__threadfence) and a barrier (__syncthreads) differ in that…',
      o: [
        'They are identical',
        'A fence orders writes; a barrier makes threads wait',
        'A fence makes threads wait',
        'A barrier only orders registers',
      ],
      a: 1,
      x: [
        'A fence spans the grid; a barrier spans the warp',
        'A barrier is non-blocking',
        'A fence synchronizes the host',
      ],
      e: 'Ordering vs. waiting: __threadfence guarantees prior writes are visible before later ones (no thread waits), while __syncthreads is a rendezvous that all block threads must reach (and also provides intra-block memory ordering).',
      ref: 'CUDA C++ Programming Guide §B.5, §7.6',
      tags: ['fence', 'barrier', 'ordering'],
    },
    {
      d: 4,
      q: 'A relaxed-atomic global counter incremented by every thread, then read AFTER a grid-wide sync, gives a correct total because…',
      o: [
        'Relaxed atomics are not atomic',
        'Each increment is atomic; the sync makes them visible',
        'Relaxed implies sequential consistency',
        'Counters cannot race',
      ],
      a: 1,
      x: [
        'It needs seq_cst to be correct',
        'Reads are always ordered',
        'The compiler serializes it',
      ],
      e: 'Relaxed only drops ordering with OTHER memory, not atomicity of the increment itself. Once a barrier (grid sync or kernel boundary) ensures all increments completed and are visible, the read sees the correct sum.',
      ref: 'libcu++ memory_order_relaxed; counters',
      tags: ['atomics', 'relaxed', 'counter', 'sync'],
    },
    {
      d: 3,
      q: 'Why must a thread that releases a lock issue a memory fence (or use release ordering) BEFORE clearing the lock?',
      o: [
        'For speed',
        'So its writes are visible to the next acquirer',
        'To reset the lock value',
        'To avoid divergence',
      ],
      a: 1,
      x: [
        'To count waiters',
        'To pin the cache',
        'To synchronize the warp',
      ],
      e: 'Without release ordering, the critical-section writes could become visible AFTER the lock is freed, letting the next acquirer read stale data. The fence/release publishes those writes before the unlock is observed.',
      ref: 'CUDA memory model; lock release semantics',
      tags: ['lock', 'release', 'fence'],
    },
    {
      d: 5,
      q: 'Why is implementing a general mutex per data element (fine-grained locking) often a poor fit for GPUs?',
      o: [
        'Locks are unsupported',
        'SIMT + huge thread counts invite contention/deadlock',
        'Mutexes use too much memory',
        'It is always faster',
      ],
      a: 1,
      x: [
        'Locks require cooperative launch',
        'Mutexes break coalescing',
        'It needs FP64',
      ],
      e: 'GPU concurrency is extreme and SIMT complicates lock acquisition within a warp. Per-element mutexes serialize heavily and risk deadlock. Patterns like privatization, atomics, and lock-free structures usually scale far better.',
      ref: 'GPU concurrency best practices',
      tags: ['mutex', 'lock-free', 'scalability'],
    },
    {
      d: 4,
      q: 'cuda::pipeline (libcu++) with cuda::pipeline_shared_state coordinates async copies by…',
      o: [
        'Blocking the host',
        'Per-stage producer/consumer handoff for overlap',
        'Using global atomics only',
        'Disabling shared memory',
      ],
      a: 1,
      x: [
        'Running on the CPU',
        'Compressing data',
        'Using texture memory',
      ],
      e: 'The pipeline object sequences the producer (issue memcpy_async, commit) and consumer (wait for a stage’s data, then release the buffer) across N stages, giving a portable multi-stage software pipeline over cp.async/TMA.',
      ref: 'CUDA C++ Programming Guide (cuda::pipeline)',
      tags: ['pipeline', 'memcpy-async', 'libcu++'],
    },
    {
      d: 3,
      q: 'On Volta+, a warp-synchronous reduction written WITHOUT __syncwarp between shuffle steps can be incorrect because…',
      o: [
        'Shuffles are slow',
        'Lanes may not be lockstep under ITS',
        'Shuffles need shared memory',
        'The mask is ignored',
      ],
      a: 1,
      x: [
        'Reductions are not allowed',
        'It overflows registers',
        'It causes bank conflicts',
      ],
      e: 'Pre-Volta code assumed implicit lockstep between shuffle steps. With independent scheduling you must either rely on the _sync intrinsics’ built-in reconvergence semantics correctly or add __syncwarp; otherwise reads can race the writes.',
      ref: 'CUDA C++ Programming Guide §4.2; warp-synchronous code',
      tags: ['warp-sync', 'shuffle', 'volta', 'reduction'],
    },
    {
      d: 4,
      q: 'A "double-checked" publication where the producer writes data then sets ready=1, and the consumer reads ready then data, is correct only if…',
      o: [
        'ready is volatile alone',
        'Release store on ready, acquire load to read it',
        'No ordering is used',
        'data is in shared memory',
      ],
      a: 1,
      x: [
        'Both use relaxed atomics',
        'A spinlock guards data',
        'ready is in constant memory',
      ],
      e: 'Release/acquire on the ready flag creates the happens-before edge so the data written before the release is visible after the acquire. Plain volatile prevents caching but does not by itself guarantee the data ordering across threads.',
      ref: 'CUDA/C++ memory model; publication pattern',
      tags: ['release-acquire', 'publication', 'ordering'],
    },
    {
      d: 2,
      q: 'atomicOr on a global bitmap is a common way to…',
      o: [
        'Sort data',
        'Set flag bits concurrently without races',
        'Reduce floats',
        'Synchronize blocks',
      ],
      a: 1,
      x: [
        'Broadcast a value',
        'Allocate memory',
        'Time the kernel',
      ],
      e: 'atomicOr lets threads set bits in a shared bitmap atomically — used for marking visited/active sets in BFS, occupancy grids, etc. Returning the old value also tells you whether your thread was the one that set the bit.',
      ref: 'CUDA atomics; graph algorithms',
      tags: ['atomicOr', 'bitmap', 'graph'],
    },
    {
      d: 5,
      q: 'cuda::barrier supports a "completion function" that runs when the barrier phase completes. This enables…',
      o: [
        'Faster atomics',
        'An action run once per barrier phase',
        'Grid-wide sync',
        'Host callbacks',
      ],
      a: 1,
      x: [
        'Lower precision',
        'Texture sampling',
        'Random number generation',
      ],
      e: 'The completion function runs once when the last participant arrives, before threads are released — handy for phase-transition bookkeeping (swap stage buffers, advance counters) in producer/consumer pipelines without a separate barrier.',
      ref: 'libcu++ <cuda/barrier>; std::barrier semantics',
      tags: ['barrier', 'completion-function', 'pipeline'],
    },
    {
      d: 3,
      q: 'Why does a histogram kernel using shared-memory atomics still need a final step with global atomics?',
      o: [
        'Shared atomics are inexact',
        'Per-block partials must merge into the global one',
        'Global atomics are faster',
        'Shared memory is read-only',
      ],
      a: 1,
      x: [
        'To reset shared memory',
        'To synchronize the grid',
        'To avoid divergence',
      ],
      e: 'Privatization computes per-block partial histograms cheaply in shared memory; combining them across blocks needs a few global atomics per bin (far fewer than per-element global atomics), or a separate reduction kernel.',
      ref: 'PMPP Ch. 9; histogram privatization',
      tags: ['histogram', 'atomics', 'privatization'],
    },
    {
      d: 4,
      q: 'A subtle bug: a kernel uses __syncthreads() inside a loop whose iteration count differs per thread. This is unsafe because…',
      o: [
        'Loops cannot contain barriers',
        'Threads hit mismatched barrier counts (deadlock)',
        'It is too slow',
        'It uses too many registers',
      ],
      a: 1,
      x: [
        'Barriers reset the loop',
        'It causes bank conflicts',
        'It always works',
      ],
      e: 'All threads must execute the same sequence of barriers. A per-thread loop count means some threads reach a barrier that others have already passed (or never reach), violating the uniform-arrival requirement. Restructure so the barrier count is uniform.',
      ref: 'CUDA C++ Programming Guide §7.6',
      tags: ['syncthreads', 'loops', 'deadlock'],
    },
    {
      d: 4,
      q: 'cuda::latch differs from cuda::barrier in that a latch…',
      o: [
        'Is reusable across phases',
        'Single-use: counts to zero once, no reset',
        'Synchronizes the grid',
        'Is an atomic counter only',
      ],
      a: 1,
      x: [
        'Blocks the host',
        'Cannot be used on the device',
        'Replaces __syncthreads',
      ],
      e: 'A latch is a one-shot countdown: once it reaches zero it remains signaled, unlike a barrier which resets each phase for repeated use. Latches fit one-time "wait until N tasks done" handoffs.',
      ref: 'libcu++ <cuda/latch>; std::latch semantics',
      tags: ['latch', 'libcu++', 'one-shot'],
    },
    {
      d: 5,
      q: 'Why might a seemingly-correct lock-free queue work on Pascal but break on Volta+?',
      o: [
        'Volta is slower',
        'It relied on warp lockstep, broken by ITS',
        'Volta lacks atomics',
        'Queues are unsupported',
      ],
      a: 1,
      x: [
        'Volta has fewer registers',
        'It needs cooperative launch',
        'Atomics changed scope',
      ],
      e: 'Volta’s independent thread scheduling removed guaranteed warp lockstep. Lock-free/warp-synchronous code that assumed all lanes step together (no explicit sync) can deadlock or race on Volta+. Use explicit masks/__syncwarp and proper atomics.',
      ref: 'CUDA C++ Programming Guide §4.2; Volta migration',
      tags: ['lock-free', 'volta', 'independent-scheduling'],
    },
    {
      d: 3,
      q: 'To safely have exactly ONE thread in a block perform an initialization that the others then use, a clean pattern is…',
      o: [
        'Let every thread do it',
        'if (threadIdx.x==0) init; then __syncthreads()',
        'Use a spinlock',
        'Use global atomics',
      ],
      a: 1,
      x: [
        'Use texture memory',
        'Use __threadfence only',
        'Use constant memory',
      ],
      e: 'A leader thread (often threadIdx==0) writes shared state, then __syncthreads() makes it visible and ensures all threads wait until initialization completes. This leader-then-barrier idiom is ubiquitous in block-cooperative kernels.',
      ref: 'CUDA C++ Programming Guide §7.6; common patterns',
      tags: ['leader-thread', 'syncthreads', 'initialization'],
    },
    {
      d: 4,
      q: 'For a flag the HOST sets that the DEVICE polls (e.g. a kernel waiting for host input via mapped memory), the device read must…',
      o: [
        'Use __threadfence_block',
        'Use system-scope acquire on the mapped flag',
        'Use shared memory',
        'Use constant memory',
      ],
      a: 1,
      x: [
        'Use __syncthreads',
        'Use a texture fetch',
        'Do nothing special',
      ],
      e: 'Host↔device flags in mapped/managed memory require system-scope visibility: the device polls with volatile/system-scope acquire and the host publishes with the right ordering, so the cross-processor write is actually observed.',
      ref: 'CUDA memory model; system-scope synchronization',
      tags: ['host-device', 'system-scope', 'flag'],
    },
    {
      d: 4,
      q: 'barrier::arrive_and_drop() is used when a thread…',
      o: [
        'Wants to wait twice',
        'It skips future phases (drops its count)',
        'Wants to reset the barrier',
        'Is the leader',
      ],
      a: 1,
      x: [
        'Wants higher priority',
        'Is on the host',
        'Needs more registers',
      ],
      e: 'arrive_and_drop lets a thread bow out of future phases (decrementing the arrival count) — useful when some threads finish early in a pipeline so the remaining threads aren’t left waiting for participants that are done.',
      ref: 'libcu++ <cuda/barrier> (arrive_and_drop)',
      tags: ['barrier', 'arrive-and-drop', 'pipeline'],
    },
    {
      d: 5,
      q: 'Deterministic parallel reduction (run-to-run identical FP results) is achieved by…',
      o: [
        'Using atomicAdd',
        'Fixing the combination order (e.g. a tree)',
        'Using relaxed atomics',
        'Using more threads',
      ],
      a: 1,
      x: [
        'Using FP64 only',
        'Using system-scope atomics',
        'Disabling the L2',
      ],
      e: 'FP non-associativity makes atomic-based sums order-dependent and thus nondeterministic. A fixed-order tree reduction applies additions in the same sequence each run, giving reproducible results (at some performance cost vs. atomics).',
      ref: 'Floating-point reproducibility; deterministic reductions',
      tags: ['determinism', 'reduction', 'floating-point'],
    },
  ],
  { idPrefix: 'synchronization-c' }
)
