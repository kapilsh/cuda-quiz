import { defineQuestions } from './_helpers.js'

// Numerics & Mixed Precision — FP32/TF32/FP16/BF16/FP8/FP4, accumulation,
// loss scaling, numerical stability, range vs. precision.
export default defineQuestions('precision', [
  {
    d: 2,
    q: 'FP16 (half) and BF16 (bfloat16) are both 16-bit. The key difference is…',
    o: [
      'BF16 has more mantissa bits',
      'BF16 keeps FP32’s 8-bit exponent (wide range, less precision); FP16 has a 5-bit exponent (narrow range, more precision)',
      'They are identical',
      'FP16 has a wider range',
    ],
    a: 1,
    e: 'BF16: 1 sign, 8 exponent, 7 mantissa — same dynamic range as FP32 but coarse precision. FP16: 1/5/10 — finer precision but a small range prone to overflow/underflow. BF16’s range makes training more robust (often without loss scaling).',
    ref: 'NVIDIA mixed-precision docs; BF16 vs FP16',
    tags: ['fp16', 'bf16', 'range-precision'],
  },
  {
    d: 3,
    q: 'Why is loss scaling commonly needed when training in FP16 (but rarely in BF16)?',
    o: [
      'To speed up the optimizer',
      'FP16’s small exponent range underflows tiny gradients to zero; scaling the loss up shifts gradients into the representable range, then unscales before the update',
      'To increase the learning rate',
      'To reduce memory',
    ],
    a: 1,
    e: 'Small gradients fall below FP16’s minimum normal and become zero. Multiplying the loss by a large factor scales gradients up into range during backprop; you divide them back out before the optimizer step. BF16’s FP32-like range usually makes this unnecessary.',
    ref: 'Micikevicius et al., "Mixed Precision Training"; NVIDIA AMP',
    tags: ['loss-scaling', 'fp16', 'underflow'],
  },
  {
    d: 3,
    q: 'In tensor-core mixed-precision GEMM, inputs may be FP16/BF16 but accumulation is typically done in…',
    o: [
      'FP16',
      'FP32',
      'INT8',
      'FP8',
    ],
    a: 1,
    e: 'Tensor cores multiply low-precision inputs but accumulate the products in FP32 to preserve accuracy over long dot products. Accumulating in FP16 would lose precision and cause large errors. The FP32 result is then optionally cast back down.',
    ref: 'NVIDIA mixed-precision docs; tensor-core MMA semantics',
    tags: ['accumulation', 'fp32', 'tensor-cores'],
  },
  {
    d: 2,
    q: 'What is "mixed-precision training"?',
    o: [
      'Training with random precision per layer',
      'Using lower precision (FP16/BF16) for most compute/storage while keeping critical parts (e.g. a master copy of weights, accumulations) in FP32 for stability',
      'Training only in FP64',
      'Switching GPUs mid-training',
    ],
    a: 1,
    e: 'Mixed precision runs the bulk of matmuls/activations in FP16/BF16 (faster, less memory) while maintaining FP32 where precision matters — notably FP32 master weights for the optimizer update and FP32 accumulation — capturing speed without sacrificing convergence.',
    ref: 'Micikevicius et al.; NVIDIA Apex/AMP',
    tags: ['mixed-precision', 'master-weights', 'training'],
  },
  {
    d: 4,
    q: 'FP8 commonly comes in two variants, E4M3 and E5M2. Why two?',
    o: [
      'One is for integers',
      'E4M3 (more mantissa) gives more precision for forward activations/weights; E5M2 (more exponent/range) suits gradients which need larger dynamic range',
      'They are for different GPUs',
      'E5M2 is always better',
    ],
    a: 1,
    e: 'E4M3 (4 exp, 3 mantissa) favors precision and is used for weights/activations; E5M2 (5 exp, 2 mantissa) favors range and is used for gradients in the backward pass, where values span a wider magnitude. The Transformer Engine picks per use.',
    ref: 'NVIDIA FP8 formats; Transformer Engine; "FP8 Formats for Deep Learning"',
    tags: ['fp8', 'e4m3', 'e5m2'],
  },
  {
    d: 4,
    q: 'Per-tensor (or per-block) scaling factors are essential for FP8 because…',
    o: [
      'FP8 has no exponent',
      'FP8’s very limited range means values must be scaled into the representable window per tensor to avoid overflow/underflow while using the few available bits',
      'They speed up the matmul',
      'They reduce the number of GPUs needed',
    ],
    a: 1,
    e: 'With only a handful of bits, FP8 has a narrow representable range. A scale factor (chosen from running amax statistics) maps each tensor’s values into that range before quantizing, preserving signal. Mismanaged scaling causes overflow (inf) or underflow (zeros).',
    ref: 'NVIDIA Transformer Engine; FP8 training recipes',
    tags: ['fp8', 'scaling', 'amax'],
  },
  {
    d: 1,
    q: 'How many bytes does a single-precision (FP32) float occupy?',
    o: ['2', '4', '8', '16'],
    a: 1,
    e: 'FP32 is 4 bytes (32 bits: 1 sign, 8 exponent, 23 mantissa). FP16/BF16 are 2 bytes, FP64 is 8 bytes, FP8 is 1 byte. Lower precision means less memory and bandwidth and usually higher math throughput.',
    ref: 'IEEE 754; CUDA C++ Programming Guide',
    tags: ['fp32', 'byte-size'],
  },
  {
    d: 3,
    q: 'A numerically stable softmax subtracts the max before exponentiating because…',
    o: [
      'It is faster',
      'exp of large inputs overflows; subtracting the row max keeps the largest exponent at exp(0)=1, preventing overflow without changing the result',
      'It avoids divergence',
      'It reduces memory',
    ],
    a: 1,
    e: 'softmax(x)_i = exp(x_i)/Σexp(x_j). Large x_i overflow exp. Subtracting max(x) shifts the argument so the largest term is exp(0)=1 (others ≤1), which is mathematically identical (the shift cancels) but avoids inf/NaN — crucial in FP16.',
    ref: 'Numerical computing; FlashAttention online softmax',
    tags: ['softmax', 'numerical-stability', 'overflow'],
  },
  {
    d: 2,
    q: 'Why does lower-precision storage (FP16/BF16) often speed up memory-bound kernels even aside from tensor cores?',
    o: [
      'It changes the algorithm',
      'It halves the bytes moved, so a bandwidth-bound kernel transfers data ~2× faster',
      'It increases occupancy',
      'It avoids atomics',
    ],
    a: 1,
    e: 'A bandwidth-bound kernel’s runtime scales with bytes moved. Halving element size (FP32→FP16/BF16) roughly halves memory traffic and runtime, independent of any tensor-core math speedup. This is why even elementwise ops benefit from lower precision.',
    ref: 'Roofline model; mixed-precision practice',
    tags: ['bandwidth', 'memory', 'half-precision'],
  },
  {
    d: 4,
    q: 'Keeping an FP32 "master copy" of weights in mixed-precision training is important because…',
    o: [
      'FP32 is faster',
      'Tiny weight updates (lr × grad) can be smaller than FP16’s precision near the weight’s magnitude and would be lost; the FP32 master accumulates them faithfully',
      'It saves memory',
      'The GPU requires it',
    ],
    a: 1,
    e: 'When a small update is added to a comparatively large FP16 weight, it can round away entirely (swamping). The optimizer therefore updates an FP32 master copy that retains small increments; an FP16/BF16 copy is derived for the forward/backward compute.',
    ref: 'Micikevicius et al., "Mixed Precision Training"',
    tags: ['master-weights', 'mixed-precision', 'rounding'],
  },
  {
    d: 3,
    q: 'TF32 is used by tensor cores for "FP32" matmuls. The catch is…',
    o: [
      'It is slower than FP32',
      'It silently reduces mantissa precision (~10 bits), so results differ slightly from true FP32 — acceptable for DL but not for high-precision numerics',
      'It overflows easily',
      'It uses 64 bits',
    ],
    a: 1,
    e: 'TF32 truncates inputs to ~10 mantissa bits before the tensor-core multiply (FP32 accumulate). It massively speeds up training-style matmuls with negligible accuracy loss, but it is not bit-accurate FP32 — you can disable it where exact FP32 is required.',
    ref: 'NVIDIA Ampere docs; PyTorch allow_tf32 flag',
    tags: ['tf32', 'precision-loss', 'tensor-cores'],
  },
  {
    d: 5,
    q: 'Catastrophic cancellation in a parallel sum (e.g. summing many similar-magnitude floats) can be mitigated by…',
    o: [
      'Using more threads',
      'Higher-precision or compensated (Kahan) accumulation, or a tree/pairwise reduction that keeps partial sums of similar magnitude',
      'Using atomics',
      'Lowering precision',
    ],
    a: 1,
    e: 'Naive sequential summation loses low-order bits as the running sum grows. Pairwise/tree reduction keeps operands similar in magnitude (lower error growth), and Kahan/compensated summation tracks the lost low-order part — both improve accuracy. GPUs naturally do tree reductions.',
    ref: 'Higham, "Accuracy and Stability of Numerical Algorithms"; Kahan summation',
    tags: ['summation', 'cancellation', 'kahan', 'reduction'],
  },
  {
    d: 2,
    q: 'INT8 quantization for inference primarily provides…',
    o: [
      'Higher accuracy than FP32',
      'Higher throughput and lower memory/bandwidth by representing weights/activations as 8-bit integers with a scale (and possibly zero-point)',
      'Larger model capacity',
      'Exact arithmetic',
    ],
    a: 1,
    e: 'INT8 inference maps real values to 8-bit integers via a scale (symmetric) or scale+zero-point (asymmetric), accelerating matmuls on INT8 tensor cores and shrinking memory/bandwidth ~4× vs FP32, with some accuracy loss managed by calibration or QAT.',
    ref: 'NVIDIA TensorRT INT8 quantization docs',
    tags: ['int8', 'quantization', 'inference'],
  },
  {
    d: 4,
    q: 'The difference between symmetric and asymmetric quantization is…',
    o: [
      'Symmetric uses more bits',
      'Asymmetric adds a zero-point offset so the quantized range need not be centered at zero (better for non-negative activations like post-ReLU)',
      'Symmetric is only for weights on the GPU',
      'They produce identical results',
    ],
    a: 1,
    e: 'Symmetric quantization maps [-a, a] with zero at integer 0 (no offset) — efficient for weights. Asymmetric adds a zero-point so an arbitrary [min, max] (e.g. ReLU outputs in [0, max]) maps fully, using the integer range better at the cost of an offset in the math.',
    ref: 'Quantization literature; TensorRT/PyTorch quantization',
    tags: ['quantization', 'symmetric-asymmetric', 'zero-point'],
  },
  {
    d: 5,
    q: 'Blackwell’s microscaling (MX) formats (e.g. MXFP8/MXFP6/MXFP4) differ from plain FP8 by…',
    o: [
      'Using 16 bits',
      'Sharing a small block-wise scale factor across a group of elements (e.g. 32), giving fine-grained scaling without per-element overhead',
      'Removing the exponent',
      'Being integer-only',
    ],
    a: 1,
    e: 'MX formats attach a shared scale (e.g. an 8-bit exponent) to each block of K elements (commonly 32), so each block is scaled independently. This captures local dynamic range better than a single per-tensor scale while keeping per-element storage tiny — key to FP4/FP6 accuracy.',
    ref: 'OCP Microscaling (MX) spec; NVIDIA Blackwell Whitepaper',
    tags: ['microscaling', 'mxfp', 'blackwell', 'block-scaling'],
  },
  {
    d: 3,
    q: 'A NaN appears in your FP16 training loss. Which is the LEAST likely cause to check first?',
    o: [
      'Gradient overflow to inf (then inf - inf or 0×inf → NaN)',
      'A correct, well-scaled loss producing NaN purely due to using BF16 storage for activations',
      'Division by zero / log of zero in a custom op',
      'Loss scale set too high causing overflow',
    ],
    a: 1,
    e: 'NaNs in FP16 usually stem from overflow (inf) feeding into operations like inf−inf, or from invalid ops (log(0), 0/0). BF16’s wide range rarely overflows, so "BF16 storage" by itself is an unlikely root cause — the others are the usual suspects.',
    ref: 'Mixed-precision debugging; NVIDIA AMP guidance',
    tags: ['nan', 'fp16', 'debugging', 'overflow'],
  },
])
