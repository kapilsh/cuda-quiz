import { defineQuestions } from './_helpers.js'

// Memory — batch D. Little's law/MLP, virtual memory API, 3D copies, layered
// textures, pools, prefetch, NUMA. Distinct from memory.js/.b.js/.c.js.
export default defineQuestions(
  'memory',
  [
    {
      d: 5,
      q: 'By Little’s law, the number of in-flight memory requests needed to saturate bandwidth equals…',
      o: [
        'The number of SMs',
        'Bandwidth × latency (concurrency)',
        'The warp size',
        'One per block',
      ],
      a: 1,
      x: [
        'The block size',
        'The register count',
        'The L2 size',
      ],
      e: 'Little’s law: concurrency = bandwidth × latency. To hide hundreds-of-ns DRAM latency at TB/s, you need thousands of outstanding requests, supplied by enough resident warps and independent loads per thread (MLP). Too few ⇒ latency-bound.',
      ref: "Little's law; memory-level parallelism",
      tags: ['littles-law', 'bandwidth', 'mlp', 'latency-hiding'],
    },
    {
      d: 5,
      q: 'The low-level Virtual Memory Management API (cuMemCreate, cuMemAddressReserve, cuMemMap) is used to…',
      o: [
        'Allocate pinned memory',
        'Decouple VA reservation from physical backing',
        'Compress memory',
        'Pin host memory',
      ],
      a: 1,
      x: [
        'Profile memory',
        'Launch kernels',
        'Run on the host',
      ],
      e: 'This API separates VA reservation from physical allocation/mapping, so you can grow a buffer in place (reserve large, map incrementally) and avoid the realloc-and-copy pattern. Framework caching allocators (e.g. PyTorch expandable segments) use it.',
      ref: 'CUDA Driver API (Virtual Memory Management)',
      tags: ['virtual-memory', 'growable', 'allocation'],
    },
    {
      d: 4,
      q: 'cudaMemcpy3D with a cudaPitchedPtr and cudaExtent is used to…',
      o: [
        'Copy 1D arrays',
        'Copy 3D regions, handling pitch and extent',
        'Copy host-to-host only',
        'Compress data',
      ],
      a: 1,
      x: [
        'Launch a kernel',
        'Allocate memory',
        'Profile transfers',
      ],
      e: 'For 3D data (volumes), cudaMalloc3D returns a cudaPitchedPtr (aligned rows); cudaMemcpy3D uses extent/position descriptors to copy sub-volumes correctly with the pitch. It generalizes cudaMemcpy2D to three dimensions.',
      ref: 'CUDA Runtime API (cudaMemcpy3D, cudaMalloc3D)',
      tags: ['memcpy3d', 'pitched-ptr', '3d'],
    },
    {
      d: 4,
      q: 'A layered texture (texture array) differs from a 3D texture in that…',
      o: [
        'It is writable',
        'An array of 2D textures; no cross-layer filtering',
        'It is stored on the host',
        'It has no filtering',
      ],
      a: 1,
      x: [
        'It is always 1D',
        'It cannot be cached',
        'It uses constant memory',
      ],
      e: 'Layered textures stack 2D slices addressed by an integer layer; there’s no interpolation between layers. A 3D texture treats depth as a continuous, filterable dimension. Choose layered for batches of images, 3D for volumetric sampling.',
      ref: 'CUDA C++ Programming Guide §3.2.14 (layered textures)',
      tags: ['layered-texture', 'texture-array', 'filtering'],
    },
    {
      d: 4,
      q: 'Prefetching managed memory to the CPU uses cudaMemPrefetchAsync with which destination device id?',
      o: [
        'Device 0',
        'cudaCpuDeviceId (the host id)',
        'The number of GPUs',
        '-1 always errors',
      ],
      a: 1,
      x: [
        'cudaInvalidDeviceId',
        'The L2 id',
        'The stream id',
      ],
      e: 'cudaMemPrefetchAsync(ptr, bytes, cudaCpuDeviceId) migrates managed pages to the host ahead of CPU use, avoiding fault-driven migration when the CPU reads results — the symmetric counterpart to prefetching to a GPU.',
      ref: 'CUDA C++ Programming Guide §K (cudaCpuDeviceId)',
      tags: ['unified-memory', 'prefetch', 'cpu'],
    },
    {
      d: 4,
      q: 'NUMA effects matter for pinned host memory because…',
      o: [
        'Pinned memory is on the GPU',
        'Pin near the GPU’s NUMA node to avoid hops',
        'NUMA disables pinning',
        'It has no effect',
      ],
      a: 1,
      x: [
        'NUMA increases bandwidth always',
        'Pinned memory ignores NUMA',
        'Only device memory has NUMA',
      ],
      e: 'Host memory bandwidth/latency to a GPU depends on NUMA locality: allocating pinned buffers on the socket attached to the GPU (numactl/affinity) avoids slower cross-socket QPI/UPI traversal, improving H2D/D2H throughput in multi-socket nodes.',
      ref: 'NUMA-aware GPU programming; host memory locality',
      tags: ['numa', 'pinned-memory', 'multi-socket'],
    },
    {
      d: 3,
      q: 'cudaMemPoolTrimTo (or a pool release threshold) controls…',
      o: [
        'The kernel size',
        'How much the pool caches vs. releases',
        'The block size',
        'The precision',
      ],
      a: 1,
      x: [
        'The number of streams',
        'The clock rate',
        'The bandwidth',
      ],
      e: 'Pools cache freed blocks for fast reuse; trimming (or a release threshold) returns excess to the driver to cap memory use. It balances allocation speed (keep cached) against footprint (release) for long-running services.',
      ref: 'CUDA stream-ordered allocator (pool trim/threshold)',
      tags: ['memory-pool', 'trim', 'footprint'],
    },
    {
      d: 4,
      q: 'Saturating DRAM bandwidth on a copy kernel typically requires…',
      o: [
        'One warp',
        'Enough warps to keep requests in flight',
        'Only constant memory',
        'A single block',
      ],
      a: 1,
      x: [
        'Tensor cores',
        'FP64',
        'Atomics',
      ],
      e: 'Bandwidth is achieved by overlapping many outstanding requests (Little’s law). A copy with too few warps/MLP can’t fill the memory pipeline and falls short of peak. Vectorized loads and adequate occupancy supply the needed concurrency.',
      ref: 'CUDA Best Practices Guide; bandwidth saturation',
      tags: ['bandwidth', 'occupancy', 'saturation'],
    },
    {
      d: 4,
      q: 'Global memory transactions are serviced in 32-byte sectors. A warp reading one float per thread (128 bytes contiguous, aligned) uses…',
      o: [
        '32 sectors',
        '4 sectors, fully utilized',
        '1 sector',
        '128 sectors',
      ],
      a: 1,
      x: [
        '8 sectors',
        '16 sectors',
        '2 sectors',
      ],
      e: 'A coalesced 128-byte aligned access maps to four 32-byte sectors, each fully used. Strided/misaligned access touches more sectors with partial use, lowering efficiency (sectors-requested-per-instruction is the profiler metric).',
      ref: 'GPU memory transactions; 32-byte sectors',
      tags: ['sectors', 'coalescing', 'transactions'],
    },
    {
      d: 5,
      q: 'Framework caching allocators (e.g. PyTorch) use "expandable segments" / the VMM API to…',
      o: [
        'Compress tensors',
        'Grow a segment in place via more pages',
        'Run on the CPU',
        'Pin host memory',
      ],
      a: 1,
      x: [
        'Disable caching',
        'Use texture memory',
        'Avoid the GPU',
      ],
      e: 'Reserving a large virtual range and mapping physical pages on demand lets the allocator expand a segment without moving data, cutting fragmentation and out-of-memory failures from many differently-sized allocations over a long run.',
      ref: 'PyTorch CUDA caching allocator (expandable segments); CUDA VMM',
      tags: ['caching-allocator', 'fragmentation', 'vmm'],
    },
    {
      d: 3,
      q: 'A "partition camping" performance problem occurs when…',
      o: [
        'Too many registers are used',
        'Accesses concentrate on a few DRAM channels',
        'Shared memory overflows',
        'A warp diverges',
      ],
      a: 1,
      x: [
        'The L1 misses',
        'Atomics contend',
        'Occupancy is low',
      ],
      e: 'DRAM is interleaved across many partitions/channels; if a kernel’s addresses map mostly to a subset (e.g. a bad stride/tile mapping), those channels bottleneck while others idle. Remapping blocks/addresses to spread access fixes it.',
      ref: 'GPU memory architecture; partition camping',
      tags: ['partition-camping', 'bandwidth', 'channels'],
    },
    {
      d: 4,
      q: 'Mapped (zero-copy) host memory accessed by a kernel is cached/coherent how?',
      o: [
        'Fully cached in L1 like global memory',
        'Each access goes to host memory, not device-cached',
        'Stored in shared memory',
        'Copied to device first',
      ],
      a: 1,
      x: [
        'Cached in registers',
        'Never accessible',
        'Always faster than device memory',
      ],
      e: 'Zero-copy reads/writes traverse PCIe/NVLink to host memory per access without persistent device caching, so latency/bandwidth are limited. It suits small or one-pass data; for reuse-heavy data, copy to device memory instead.',
      ref: 'CUDA C++ Programming Guide §3.2.4 (mapped memory)',
      tags: ['zero-copy', 'mapped-memory', 'reuse'],
    },
    {
      d: 4,
      q: 'A texture object can provide hardware "gather" (tex2Dgather) which returns…',
      o: [
        'A single filtered sample',
        'The four texels of a bilinear 2×2 neighborhood',
        'A whole row',
        'A reduction',
      ],
      a: 1,
      x: [
        'A random sample',
        'The mipmap level',
        'A 3D voxel',
      ],
      e: 'tex2Dgather fetches the 2×2 neighborhood texels (a chosen component) in one operation, letting you implement custom interpolation/edge detection efficiently using the texture hardware’s addressing/caching.',
      ref: 'CUDA C++ Programming Guide §3.2.14 (texture gather)',
      tags: ['texture-gather', 'filtering'],
    },
    {
      d: 3,
      q: 'cudaMemAdvise vs cudaMemPrefetchAsync: advise is…',
      o: [
        'A copy operation',
        'A locality hint guiding future migration',
        'A synchronization call',
        'A kernel launch',
      ],
      a: 1,
      x: [
        'Faster than prefetch',
        'Only for host memory',
        'A no-op',
      ],
      e: 'Advise sets policy (where pages prefer to live, who accesses them) to reduce future faults/migrations; prefetch is an immediate, proactive migration. They’re complementary tools for tuning Unified Memory performance.',
      ref: 'CUDA C++ Programming Guide §K',
      tags: ['unified-memory', 'memadvise', 'prefetch'],
    },
    {
      d: 5,
      q: 'Why might increasing per-thread memory-level parallelism (independent loads) raise achieved bandwidth more than increasing occupancy?',
      o: [
        'It never does',
        'More loads per thread, not more warps',
        'It reduces latency',
        'It uses tensor cores',
      ],
      a: 1,
      x: [
        'It increases the clock',
        'It compresses data',
        'It avoids the L2',
      ],
      e: 'Bandwidth needs enough in-flight requests. You can supply them via more warps (occupancy) OR more independent loads per thread (MLP). High-MLP kernels saturate bandwidth at modest occupancy — the same insight behind low-occupancy GEMM.',
      ref: 'Volkov; memory-level parallelism vs occupancy',
      tags: ['mlp', 'bandwidth', 'occupancy'],
    },
    {
      d: 4,
      q: 'A growable allocation via the VMM API avoids the cost that plain cudaMalloc+copy incurs when a buffer must expand, namely…',
      o: [
        'JIT compilation',
        'Allocate a bigger buffer and copy all data',
        'Pinning memory',
        'Launching a kernel',
      ],
      a: 1,
      x: [
        'Synchronizing the device',
        'Recompiling',
        'Texture binding',
      ],
      e: 'Resizing a cudaMalloc buffer means allocate-new + copy-old + free-old, costing a full copy and peak double memory. Mapping additional physical pages into a reserved VA range grows in place, avoiding the copy and fragmentation.',
      ref: 'CUDA VMM API; growable allocations',
      tags: ['vmm', 'growable', 'copy-avoidance'],
    },
    {
      d: 3,
      q: 'Surface objects require the backing cudaArray to be created with which flag?',
      o: [
        'cudaArrayDefault',
        'cudaArraySurfaceLoadStore',
        'cudaArrayTextureGather',
        'cudaArrayLayered only',
      ],
      a: 1,
      x: [
        'cudaArrayCubemap',
        'cudaArraySparse',
        'cudaArrayColorAttachment',
      ],
      e: 'To write via a surface, the cudaArray must be allocated with cudaArraySurfaceLoadStore. Textures (read-only) don’t need it. The flags select the access capabilities the array supports.',
      ref: 'CUDA C++ Programming Guide §3.2.14 (surface memory)',
      tags: ['surface', 'cudaarray', 'flags'],
    },
    {
      d: 4,
      q: 'L2 hit rate can be improved for a tiled kernel by "block swizzling" (remapping block→tile assignment) because…',
      o: [
        'It reduces registers',
        'Concurrent blocks reuse overlapping data in L2',
        'It avoids global memory',
        'It increases occupancy',
      ],
      a: 1,
      x: [
        'It compresses tiles',
        'It removes divergence',
        'It pins data in L1',
      ],
      e: 'Default linear block scheduling can spread concurrently-running blocks across distant data. Swizzling (e.g. a tiled/Hilbert mapping of blockIdx to output tiles) clusters their working sets so shared operands stay hot in L2 — used in GEMM.',
      ref: 'GEMM block swizzling; L2 locality',
      tags: ['block-swizzling', 'l2-cache', 'locality'],
    },
    {
      d: 4,
      q: 'For a 3D stencil, using a 3D texture (or read-only cache) over plain global loads can help because…',
      o: [
        'Textures are writable',
        'The texture cache captures 3D neighbor reuse',
        'It avoids the halo',
        'It removes synchronization',
      ],
      a: 1,
      x: [
        'It uses tensor cores',
        'It is always faster',
        'It compresses the volume',
      ],
      e: '3D stencils reuse neighboring voxels across adjacent outputs; the texture cache’s spatial locality captures that reuse (plus free boundary handling), often beating naive global loads — though shared-memory tiling can do even better.',
      ref: 'CUDA texture caching; stencils',
      tags: ['texture', '3d-stencil', 'spatial-locality'],
    },
    {
      d: 3,
      q: 'cudaHostAllocMapped requires which device flag (on older/UVA-limited setups) to obtain usable device pointers?',
      o: [
        'Nothing ever',
        'cudaDeviceMapHost, set before allocation',
        'cudaDeviceScheduleSpin',
        'cudaDeviceLmemResizeToMax',
      ],
      a: 1,
      x: [
        'cudaDeviceBlockingSync',
        'cudaDeviceScheduleYield',
        'cudaArraySurfaceLoadStore',
      ],
      e: 'Mapped (zero-copy) allocations need the device to support and enable host mapping (cudaDeviceMapHost). With UVA the device pointer often equals the host pointer; cudaHostGetDevicePointer remains the portable way to fetch it.',
      ref: 'CUDA Runtime API (mapped host memory)',
      tags: ['mapped-memory', 'device-flags', 'zero-copy'],
    },
    {
      d: 5,
      q: 'Why does a memory-bound kernel sometimes scale SUBLINEARLY when you double the data per thread (coarsening)?',
      o: [
        'It always scales linearly',
        'Once bandwidth saturates, more work won’t help',
        'Coarsening reduces bandwidth',
        'It increases divergence',
      ],
      a: 1,
      x: [
        'It overflows registers always',
        'It disables coalescing',
        'It needs atomics',
      ],
      e: 'Coarsening adds MLP/reuse and helps while you’re latency-bound or under-saturating bandwidth. Once at the bandwidth roof, more per-thread work yields no more throughput (and can hurt via register pressure) — diminishing returns at the roofline.',
      ref: 'Roofline; thread coarsening limits',
      tags: ['coarsening', 'bandwidth', 'roofline'],
    },
    {
      d: 4,
      q: 'cudaMallocManaged with the cudaMemAttachHost flag initially attaches the allocation to…',
      o: [
        'All GPUs',
        'The host, until a stream/GPU touches it',
        'The L2 cache',
        'Constant memory',
      ],
      a: 1,
      x: [
        'A specific stream only forever',
        'Pinned memory',
        'The texture cache',
      ],
      e: 'Attach flags (cudaMemAttachHost/Global/Single) control initial visibility/placement of managed memory and stream association, helping manage migration behavior and concurrency for the allocation.',
      ref: 'CUDA Runtime API (cudaMallocManaged attach flags)',
      tags: ['unified-memory', 'attach', 'placement'],
    },
    {
      d: 3,
      q: 'The achieved memory bandwidth of a well-tuned copy kernel relative to the theoretical peak is typically…',
      o: [
        'Exactly 100%',
        'Around 80–90% of theoretical peak',
        'About 10%',
        'Over 100%',
      ],
      a: 1,
      x: [
        '~50% at best',
        'Exactly 75%',
        'Unbounded',
      ],
      e: 'Real kernels reach a high but sub-100% fraction of theoretical peak due to protocol/refresh/ECC overheads and scheduling. ~80–90% indicates a well-optimized memory-bound kernel; far below suggests coalescing/MLP issues to fix.',
      ref: 'CUDA Best Practices Guide; achievable bandwidth',
      tags: ['bandwidth', 'efficiency', 'benchmark'],
    },
    {
      d: 5,
      q: 'Sparse cudaArrays / sparse textures allow…',
      o: [
        'Compressing textures',
        'Back only the tiles you touch, on demand',
        'Faster filtering',
        'Read-only access only',
      ],
      a: 1,
      x: [
        'Atomic texture writes',
        'Host-side textures',
        'Lower precision',
      ],
      e: 'Sparse texture/array support lets you reserve a huge virtual texture and back only the tiles you actually touch with physical memory, enabling sparse/streamed volumetric or megatexture workloads — virtual memory for the texture path.',
      ref: 'CUDA sparse textures/arrays',
      tags: ['sparse-texture', 'virtual-memory', 'tiles'],
    },
    {
      d: 4,
      q: 'Why is reading data you just wrote from another block via global memory unsafe without a fence, even on the same GPU?',
      o: [
        'Global memory is read-only',
        'Per-SM L1s are incoherent; reads can be stale',
        'It is always safe',
        'It needs atomics only',
      ],
      a: 1,
      x: [
        'Blocks share L1',
        'It requires constant memory',
        'It causes divergence',
      ],
      e: 'Cross-block global communication requires a memory fence to make writes visible past the incoherent per-SM L1s and to order them. (And recall blocks may not even be co-resident — true grid cooperation needs cooperative launch.)',
      ref: 'CUDA memory model; L1 coherence',
      tags: ['fence', 'l1-coherence', 'inter-block'],
    },
    {
      d: 3,
      q: 'Texture mipmaps (cudaMipmappedArray) provide…',
      o: [
        'Compression',
        'Precomputed downsampled LOD levels',
        'Atomic writes',
        'Faster atomics',
      ],
      a: 1,
      x: [
        'A 3D volume',
        'Host caching',
        'Constant broadcast',
      ],
      e: 'Mipmapped arrays store a pyramid of resolutions; the hardware samples the appropriate LOD (with trilinear filtering across levels), reducing aliasing and cost for minified sampling — common in graphics and multiscale image processing.',
      ref: 'CUDA C++ Programming Guide §3.2.14 (mipmaps)',
      tags: ['mipmap', 'lod', 'texture'],
    },
    {
      d: 4,
      q: 'A kernel’s "memory throughput" reported near peak while "compute throughput" is low most accurately means…',
      o: [
        'It is compute-bound',
        'Memory-bound — reduce bytes moved',
        'It is latency-bound',
        'It is optimal',
      ],
      a: 1,
      x: [
        'It needs more registers',
        'It has bank conflicts',
        'It is tensor-core bound',
      ],
      e: 'High memory %, low compute % = bandwidth-bound. You won’t speed it up with more math or occupancy; cut traffic (reuse via tiling, fuse kernels, use smaller types) or fix access efficiency to move off the bandwidth roof.',
      ref: 'Nsight Compute Speed-of-Light; roofline',
      tags: ['memory-bound', 'speed-of-light', 'optimization'],
    },
    {
      d: 5,
      q: 'On Grace Hopper, the GPU accessing the CPU’s LPDDR memory over NVLink-C2C is attractive for huge models because…',
      o: [
        'LPDDR is faster than HBM',
        'A large coherent pool (~900 GB/s) beyond HBM',
        'It replaces HBM',
        'It is on the same die',
      ],
      a: 1,
      x: [
        'It avoids the GPU',
        'It compresses memory',
        'It is host-only',
      ],
      e: 'NVLink-C2C gives the GPU coherent, high-bandwidth (~900 GB/s) access to the CPU’s large LPDDR, expanding the effective memory pool well beyond HBM for capacity-bound workloads (huge models, long-context KV caches) — slower than HBM but far larger.',
      ref: 'NVIDIA Grace Hopper; NVLink-C2C memory',
      tags: ['grace-hopper', 'nvlink-c2c', 'capacity'],
    },
    {
      d: 3,
      q: 'Allocating one large device buffer and sub-allocating from it (a custom arena) instead of many cudaMalloc calls helps because…',
      o: [
        'It increases bandwidth',
        'cudaMalloc/cudaFree are costly, synchronizing',
        'It compresses data',
        'It pins memory',
      ],
      a: 1,
      x: [
        'It enables tensor cores',
        'It reduces precision',
        'It avoids coalescing',
      ],
      e: 'Frequent cudaMalloc/cudaFree add latency and device-wide synchronization. Sub-allocating from a big buffer (or using cudaMallocAsync pools) amortizes that cost — the reason frameworks ship caching allocators.',
      ref: 'CUDA memory management; caching allocators',
      tags: ['arena-allocator', 'cudaMalloc', 'overhead'],
    },
  ],
  { idPrefix: 'memory-d' }
)
