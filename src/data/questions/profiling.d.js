import { defineQuestions } from './_helpers.js'

// Performance & Profiling — batch D. Workflow, counters, sectors/efficiency,
// occupancy analysis, comparative methodology. Distinct from profiling.js/.b.js/.c.js.
export default defineQuestions(
  'profiling',
  [
    {
      d: 3,
      q: 'The recommended top-down profiling workflow is…',
      o: [
        'Start with SASS disassembly',
        'Nsight Systems first (find where time goes / is the GPU busy), then Nsight Compute on the specific hot kernels',
        'Run compute-sanitizer first',
        'Guess and optimize',
      ],
      a: 1,
      x: [
        'Optimize every kernel equally',
        'Only read source code',
        'Use nvidia-smi only',
      ],
      e: 'Profile broad-to-narrow: Nsight Systems reveals whether the bottleneck is CPU, data loading, comm, or a particular kernel; then Nsight Compute deep-dives the few kernels that actually dominate runtime. Avoid optimizing kernels that don’t matter.',
      ref: 'NVIDIA profiling methodology',
      tags: ['workflow', 'nsight-systems', 'nsight-compute'],
    },
    {
      d: 4,
      q: 'Global load efficiency reported as 25% most directly implies…',
      o: [
        'Low occupancy',
        'Each memory request transfers ~4× the bytes the kernel actually uses — heavily uncoalesced/strided access wasting bandwidth',
        'Register spilling',
        'Tensor-core starvation',
      ],
      a: 1,
      x: [
        'A barrier stall',
        'A divergent branch',
        'A host bottleneck',
      ],
      e: 'Load efficiency = useful bytes / bytes fetched (in sectors). 25% means three-quarters of fetched data is wasted — the signature of strided/misaligned access. Fix the access pattern (SoA, vectorize, align) to raise it toward 100%.',
      ref: 'Nsight Compute (load efficiency)',
      tags: ['load-efficiency', 'coalescing', 'sectors'],
    },
    {
      d: 4,
      q: 'When comparing two kernel versions, why prefer comparing "bytes moved" and "instructions executed" over wall-clock alone?',
      o: [
        'Wall-clock is always wrong',
        'Those counters are clock-independent and explain WHY one is faster (less traffic/fewer instructions), giving actionable insight beyond a single time number that boost clocks can distort',
        'They are easier to read',
        'Time cannot be measured',
      ],
      a: 1,
      x: [
        'Counters are exact time',
        'Wall-clock needs no warm-up',
        'Bytes equal time',
      ],
      e: 'Counters like dram__bytes and instructions executed are deterministic and diagnostic: a faster kernel that moves fewer bytes tells you the mechanism. Wall-clock varies with clocks/state; pair it with counters (and locked clocks) for fair, explanatory comparison.',
      ref: 'Nsight Compute; comparative methodology',
      tags: ['counters', 'comparison', 'methodology'],
    },
    {
      d: 4,
      q: 'High "Stall LG Throttle" specifically points to…',
      o: [
        'Tensor cores',
        'The local/global memory instruction pipeline being saturated (too many in-flight LD/ST), so warps wait to issue memory instructions',
        'A barrier',
        'The host',
      ],
      a: 1,
      x: [
        'FP64 units',
        'The instruction cache',
        'Register banks',
      ],
      e: 'LG throttle means the load/store pipe is backed up with memory instructions. Remedies: vectorize (fewer, wider accesses), increase reuse (fewer memory ops), or rebalance toward compute — it indicates memory-instruction pressure, not necessarily DRAM bandwidth.',
      ref: 'Nsight Compute (LG Throttle)',
      tags: ['lg-throttle', 'lsu', 'stall-reasons'],
    },
    {
      d: 3,
      q: 'Achieved occupancy 90% with poor performance most likely means the bottleneck is…',
      o: [
        'Insufficient warps',
        'Elsewhere — memory inefficiency (uncoalesced/bandwidth-bound) or a saturated compute pipe — since occupancy is clearly adequate',
        'Register spilling',
        'Too few blocks',
      ],
      a: 1,
      x: [
        'Tail effect',
        'Low occupancy',
        'A host stall',
      ],
      e: 'With high occupancy, latency hiding isn’t the issue. Look at memory throughput/efficiency and pipe utilization (Speed of Light) to find whether it’s bandwidth-bound, instruction-bound, or compute-bound — not occupancy.',
      ref: 'Nsight Compute; occupancy vs limiter',
      tags: ['occupancy', 'bottleneck', 'analysis'],
    },
    {
      d: 4,
      q: 'You see large gaps between kernels on the Nsight Systems timeline with the CPU idle too. A likely cause is…',
      o: [
        'CPU-bound work',
        'A synchronous wait (e.g. cudaDeviceSynchronize) or a blocking copy stalling the pipeline, or a dependency forcing serialization — both CPU and GPU idle during the wait',
        'Memory bandwidth saturation',
        'Too many registers',
      ],
      a: 1,
      x: [
        'High occupancy',
        'Tensor-core usage',
        'ECC overhead',
      ],
      e: 'Both idle usually means a hard synchronization point or unmet dependency (sync, blocking memcpy, or waiting on an event) draining the pipeline. Replace blocking syncs with async/event-based ordering and overlap to close the gaps.',
      ref: 'Nsight Systems; idle-gap analysis',
      tags: ['nsight-systems', 'gaps', 'synchronization'],
    },
    {
      d: 5,
      q: 'A kernel’s DRAM throughput is 95% but the application is still slow. The next profiling step is…',
      o: [
        'Optimize this kernel more',
        'Step back to Nsight Systems: this kernel is bandwidth-bound (near optimal), so the app-level win is reducing how OFTEN/how much data it processes (fusion, caching, algorithm), not tuning the kernel further',
        'Increase occupancy',
        'Add streams',
      ],
      a: 1,
      x: [
        'Lower precision blindly',
        'Add atomics',
        'Use FP64',
      ],
      e: 'A kernel at 95% DRAM is essentially at its roof; further micro-optimization yields little. The leverage is algorithmic/app-level: fuse to avoid re-reads, cache/reuse across calls, or reduce data — found by zooming out, not deeper in.',
      ref: 'Profiling methodology; bandwidth-bound kernels',
      tags: ['bandwidth-bound', 'methodology', 'app-level'],
    },
    {
      d: 3,
      q: 'The "Source Counters" / source view in Nsight Compute (with -lineinfo) lets you…',
      o: [
        'Edit the source',
        'See per-source-line metrics (e.g. which lines cause the most stalls, memory transactions) to pinpoint the exact hotspots',
        'Recompile',
        'Run on the CPU',
      ],
      a: 1,
      x: [
        'Profile the host',
        'Allocate memory',
        'Lock clocks',
      ],
      e: 'With line info, Nsight maps SASS metrics back to source lines, so you can see which lines incur stalls/transactions/divergence — directing optimization to the precise hot lines rather than guessing.',
      ref: 'Nsight Compute (source-level analysis)',
      tags: ['source-counters', 'lineinfo', 'hotspots'],
    },
    {
      d: 4,
      q: 'For benchmarking, the recommended timing protocol is…',
      o: [
        'Time one run with the host clock',
        'Warm up (discard first runs), lock clocks, then time many iterations with CUDA events and report the median/min — reducing noise from JIT, clocks, and cold caches',
        'Use printf timestamps',
        'Run under the profiler',
      ],
      a: 1,
      x: [
        'Time only the first launch',
        'Average including warm-up',
        'Use nvidia-smi',
      ],
      e: 'Robust timing: warm up to absorb one-time costs, lock clocks for stability, and use device-side events over many iterations, reporting a robust statistic. Profiling overhead and first-run effects otherwise distort measurements.',
      ref: 'CUDA Best Practices Guide §8; benchmarking',
      tags: ['benchmarking', 'events', 'methodology'],
    },
    {
      d: 4,
      q: 'A kernel shows high "Tensor" pipe utilization AND high achieved FLOP/s near the tensor roofline. This means…',
      o: [
        'It is memory-bound',
        'It is compute(tensor)-bound and near optimal — further speedups require algorithmic change (less work, sparsity, lower precision) rather than kernel tuning',
        'It is latency-bound',
        'Occupancy is too low',
      ],
      a: 1,
      x: [
        'It has bank conflicts',
        'It is host-bound',
        'It needs more warps',
      ],
      e: 'Near the tensor roof with high tensor utilization is the success case for a GEMM. To go faster you change the work: fewer FLOPs (sparsity, smaller models), lower precision (FP8/FP4), or different algorithm — the kernel itself is well-tuned.',
      ref: 'Nsight Compute roofline; tensor-bound',
      tags: ['tensor-bound', 'roofline', 'gemm'],
    },
    {
      d: 3,
      q: 'compute-sanitizer --tool initcheck detects…',
      o: [
        'Slow kernels',
        'Reads of uninitialized device global memory — values used before being written',
        'Bank conflicts',
        'Low occupancy',
      ],
      a: 1,
      x: [
        'Out-of-bounds access',
        'Races',
        'Register spills',
      ],
      e: 'initcheck flags use of uninitialized global memory, catching bugs where a kernel reads data it never wrote (e.g. a missing memset or incomplete fill). It complements memcheck (OOB), racecheck (races), and synccheck (illegal sync).',
      ref: 'compute-sanitizer (initcheck)',
      tags: ['initcheck', 'compute-sanitizer', 'uninitialized'],
    },
    {
      d: 4,
      q: 'In a training profile, NCCL all-reduce kernels show on the timeline taking 40% of step time with little overlap. The fix targets…',
      o: [
        'Kernel micro-optimization',
        'Communication/computation overlap — bucketing gradients and launching collectives on a separate stream so they hide behind backward compute (and/or a faster interconnect/topology)',
        'Lower precision in the optimizer',
        'A bigger batch only',
      ],
      a: 1,
      x: [
        'More registers',
        'Disabling NCCL',
        'A larger grid',
      ],
      e: 'Exposed (non-overlapped) communication is a distributed-training bottleneck. Overlapping all-reduce with backprop (bucketing, separate comm stream), topology-aware placement, and faster links reduce its visible cost — not kernel tuning.',
      ref: 'Distributed profiling; comm/compute overlap',
      tags: ['nccl', 'overlap', 'profiling', 'training'],
    },
    {
      d: 3,
      q: 'Why is it useful to check ptxas -v output BEFORE profiling?',
      o: [
        'It times the kernel',
        'It reports registers/shared memory/spills at compile time, so you can anticipate occupancy limits and spills without running anything',
        'It shows the roofline',
        'It measures bandwidth',
      ],
      a: 1,
      x: [
        'It runs the kernel',
        'It locks clocks',
        'It captures a trace',
      ],
      e: 'ptxas -v (or --resource-usage) prints per-kernel register/shared-memory/spill usage at build time, giving early insight into occupancy limiters and spills — a cheap first check before any runtime profiling.',
      ref: 'NVCC --ptxas-options=-v',
      tags: ['ptxas', 'compile-time', 'registers'],
    },
    {
      d: 5,
      q: 'A kernel’s achieved FLOP/s is far below peak, DRAM is not saturated, and there are many "Long Scoreboard" stalls. The diagnosis is…',
      o: [
        'Compute-bound',
        'Memory-LATENCY-bound — warps wait on global loads but bandwidth isn’t saturated, so increase MLP/occupancy or improve locality (more requests in flight / fewer, closer accesses)',
        'Tensor-bound',
        'Host-bound',
      ],
      a: 1,
      x: [
        'Bandwidth-bound',
        'Register-spill bound',
        'Barrier-bound',
      ],
      e: 'Low compute, unsaturated DRAM, and long-scoreboard stalls mean latency, not bandwidth, is the issue: not enough outstanding requests to hide load latency. Add warps/ILP (more in-flight loads) or improve locality (L2/L1 hits) to fix it.',
      ref: 'Nsight Compute; latency-bound diagnosis',
      tags: ['latency-bound', 'long-scoreboard', 'diagnosis'],
    },
    {
      d: 4,
      q: 'Nsight Compute’s "baseline" feature is used to…',
      o: [
        'Set the clock',
        'Save one kernel’s metrics as a reference and view another kernel/version as a diff against it, making before/after comparisons concrete',
        'Profile the host',
        'Allocate memory',
      ],
      a: 1,
      x: [
        'Lock clocks',
        'Generate the roofline',
        'Disassemble SASS',
      ],
      e: 'Baselines let you compare a candidate kernel against a saved reference metric-by-metric (e.g. did duration drop while bytes dropped?), turning optimization into a measured A/B rather than guesswork.',
      ref: 'Nsight Compute (baselines)',
      tags: ['baseline', 'comparison', 'nsight-compute'],
    },
    {
      d: 3,
      q: 'A simple sign that an application is INPUT-PIPELINE bound (data loading) is…',
      o: [
        'High SM utilization',
        'On the timeline, the GPU is idle waiting between steps while CPU/data-loader threads are busy — the GPU is starved for data',
        'High DRAM bandwidth',
        'Register spills',
      ],
      a: 1,
      x: [
        'Tensor cores saturated',
        'Many NCCL kernels',
        'High occupancy',
      ],
      e: 'GPU idle gaps each iteration with busy loader threads point to the input pipeline. Fix with more loader workers, pinned memory + async copies, prefetching, GPU-side decode (DALI/nvJPEG), or GPUDirect Storage — not kernel tuning.',
      ref: 'Nsight Systems; input-pipeline bottleneck',
      tags: ['data-loading', 'input-bound', 'timeline'],
    },
    {
      d: 4,
      q: 'When profiling shows high warp-execution efficiency (≈100%) but the kernel is slow, you can rule out…',
      o: [
        'Memory issues',
        'Control-flow divergence as the cause — efficiency near 100% means lanes are rarely masked off, so look at memory/compute throughput instead',
        'Bandwidth limits',
        'Occupancy',
      ],
      a: 1,
      x: [
        'Register pressure',
        'Tail effects',
        'Barriers',
      ],
      e: 'Warp execution efficiency ≈ active lanes per instruction. Near 100% means little divergence, so the slowdown isn’t branch divergence — redirect attention to memory efficiency, stalls, or compute throughput.',
      ref: 'Nsight Compute (warp execution efficiency)',
      tags: ['warp-efficiency', 'divergence', 'analysis'],
    },
    {
      d: 5,
      q: 'You profile a fused kernel and a two-kernel baseline; the fused one is SLOWER despite less DRAM traffic. The metric to inspect is…',
      o: [
        'DRAM bytes',
        'Occupancy / register & shared-memory usage (and spills) — fusion likely raised resource pressure, dropping occupancy or spilling, which outweighed the traffic savings',
        'Branch efficiency',
        'Clock rate',
      ],
      a: 1,
      x: [
        'L2 hit rate only',
        'The grid size',
        'NVTX ranges',
      ],
      e: 'When fusion saves traffic but loses overall, the culprit is usually increased per-thread resource use (more live state) lowering occupancy or causing spills. Check registers/shared memory and achieved occupancy to confirm the trade-off.',
      ref: 'Nsight Compute; fusion trade-off analysis',
      tags: ['fusion', 'occupancy', 'registers', 'analysis'],
    },
    {
      d: 3,
      q: 'For a multi-GPU job, capturing one Nsight Systems report per rank (a few ranks) helps reveal…',
      o: [
        'Per-kernel SASS',
        'Load imbalance and stragglers across ranks, and whether communication overlaps compute — patterns invisible from a single rank',
        'Bank conflicts',
        'Register counts',
      ],
      a: 1,
      x: [
        'The roofline',
        'Cache hit rates',
        'Occupancy',
      ],
      e: 'Comparing timelines across ranks exposes imbalance (one rank slower), exposed communication, and synchronization stalls at the job level. Deep per-kernel ncu analysis is then reserved for the specific kernels found to dominate.',
      ref: 'Nsight Systems multi-GPU profiling',
      tags: ['multi-gpu', 'imbalance', 'nsight-systems'],
    },
    {
      d: 4,
      q: 'The metric l1tex__t_sector_hit_rate (L1/TEX hit rate) being high while DRAM throughput is low indicates…',
      o: [
        'A memory-bound kernel on DRAM',
        'Good L1 locality is serving most accesses on-chip — so the kernel is NOT DRAM-bound; if slow, look at the L1/LSU pipe or compute',
        'Register spilling',
        'A divergent branch',
      ],
      a: 1,
      x: [
        'Tensor-core starvation',
        'A host stall',
        'Low occupancy',
      ],
      e: 'A high L1 hit rate with low DRAM traffic means reuse is captured in L1, so DRAM bandwidth isn’t the limiter. Slowness then comes from the L1/LSU throughput (memory-instruction pressure) or compute — investigate pipe utilization.',
      ref: 'Nsight Compute (L1 hit rate)',
      tags: ['l1-hit-rate', 'memory-analysis', 'lsu'],
    },
    {
      d: 3,
      q: 'NVTX domains/categories (beyond simple ranges) let you…',
      o: [
        'Speed up kernels',
        'Organize and color timeline annotations by subsystem (e.g. "dataloader", "forward", "backward"), making large traces easier to navigate',
        'Lock clocks',
        'Count FLOPs',
      ],
      a: 1,
      x: [
        'Profile the host only',
        'Disable CUDA tracing',
        'Allocate memory',
      ],
      e: 'NVTX supports named domains and categories so you can group/color ranges by component, structuring a busy timeline. This makes it far easier to correlate GPU activity with logical phases in complex applications.',
      ref: 'NVTX (domains/categories)',
      tags: ['nvtx', 'domains', 'timeline'],
    },
    {
      d: 5,
      q: 'A kernel is "issue-bound": IPC is high but achieved throughput is below peak. The likely interpretation is…',
      o: [
        'It is memory-bound',
        'The SM is issuing near its instruction-issue limit (instruction overhead dominates) — reduce instruction count (vectorize, unroll less or more wisely, use higher-throughput ops) to do more work per instruction',
        'It is latency-bound',
        'Occupancy is too low',
      ],
      a: 1,
      x: [
        'It has bank conflicts',
        'It is host-bound',
        'Tensor cores are idle',
      ],
      e: 'When issue slots are the constraint, the kernel runs too many low-value instructions. Vectorizing (more work per instruction), using packed/tensor ops, and trimming overhead instructions raise useful throughput per issued instruction.',
      ref: 'Nsight Compute; issue-bound kernels',
      tags: ['issue-bound', 'ipc', 'instruction-count'],
    },
    {
      d: 4,
      q: 'Why might two runs of the SAME kernel report different durations even with locked clocks?',
      o: [
        'The code changed',
        'Residual variance from cache/TLB state, neighboring work, and measurement overhead; averaging several iterations and reporting a robust statistic (min/median) mitigates it',
        'Occupancy changes randomly',
        'Registers vary per run',
      ],
      a: 1,
      x: [
        'The GPU recompiles',
        'The grid resizes',
        'ECC toggles',
      ],
      e: 'Even with locked clocks, cache/TLB warmth, contention, and timer overhead introduce run-to-run jitter. Warm up and run many iterations, reporting min or median, for stable, comparable measurements.',
      ref: 'Benchmarking methodology; measurement variance',
      tags: ['variance', 'benchmarking', 'methodology'],
    },
    {
      d: 3,
      q: 'For a quick check of whether a GPU is being used at all (and how hard), the lightest tool is…',
      o: [
        'Nsight Compute',
        'nvidia-smi (or nvidia-smi dmon / dcgm) for live utilization, memory, power — coarse but instant, before deeper profiling',
        'cuobjdump',
        'compute-sanitizer',
      ],
      a: 1,
      x: [
        'ptxas -v',
        'cuda-gdb',
        'NVTX',
      ],
      e: 'nvidia-smi gives an immediate, coarse view (utilization duty cycle, memory, power, temperature). It’s the first sanity check; remember GPU-Util is a duty cycle, not SM efficiency, so confirm with Nsight for real bottleneck analysis.',
      ref: 'nvidia-smi; quick checks',
      tags: ['nvidia-smi', 'utilization', 'quick-check'],
    },
    {
      d: 4,
      q: 'You suspect register spilling. The two complementary signals are…',
      o: [
        'Occupancy and grid size',
        'ptxas -v "spill stores/loads" at compile time AND local memory load/store traffic in Nsight Compute at runtime',
        'DRAM bytes and L2 hit rate',
        'Branch efficiency and IPC',
      ],
      a: 1,
      x: [
        'Bank conflicts and divergence',
        'Tensor utilization and FLOPs',
        'Clock rate and power',
      ],
      e: 'Spills show up at compile time (ptxas spill bytes) and at runtime as local-memory traffic in the profiler. Seeing both confirms spilling; then reduce live state or cap registers (weighing the occupancy trade-off).',
      ref: 'ptxas -v; Nsight Compute local memory',
      tags: ['spilling', 'ptxas', 'local-memory'],
    },
    {
      d: 5,
      q: 'A kernel’s roofline dot sits exactly ON the bandwidth roof at its arithmetic intensity. This means…',
      o: [
        'It is compute-bound',
        'It is achieving the maximum performance possible at that intensity (bandwidth-limited) — to go faster you must INCREASE arithmetic intensity (more reuse/fusion), moving the dot right toward the ridge',
        'It is latency-bound',
        'It is broken',
      ],
      a: 1,
      x: [
        'It needs more occupancy',
        'It is tensor-bound',
        'It is host-bound',
      ],
      e: 'On the bandwidth roof = bandwidth-optimal for that intensity. Higher performance requires raising arithmetic intensity (reuse data more per byte via tiling/fusion/lower-precision storage), shifting the kernel rightward toward (and past) the ridge point.',
      ref: 'Roofline model; increasing arithmetic intensity',
      tags: ['roofline', 'arithmetic-intensity', 'bandwidth-bound'],
    },
    {
      d: 3,
      q: 'Why annotate code with NVTX before capturing a long training run with Nsight Systems?',
      o: [
        'It speeds training',
        'Named ranges (forward/backward/optimizer/dataload) on the timeline let you immediately attribute GPU activity and gaps to phases, instead of decoding anonymous kernels',
        'It reduces memory',
        'It locks clocks',
      ],
      a: 1,
      x: [
        'It disables tracing',
        'It counts FLOPs',
        'It compiles kernels',
      ],
      e: 'NVTX ranges turn a wall of unnamed kernels into a phase-labeled timeline, so you can see which phase dominates and where the GPU idles — essential for navigating and interpreting large, complex training traces.',
      ref: 'NVTX; Nsight Systems workflow',
      tags: ['nvtx', 'training', 'timeline'],
    },
    {
      d: 4,
      q: 'Memory throughput at 60% and compute throughput at 60% (neither near peak), with few stalls, suggests…',
      o: [
        'Memory-bound',
        'A reasonably balanced kernel that may be limited by a mix or by issue/latency — investigate eligible-warps and specific stall reasons to find the marginal limiter',
        'Compute-bound',
        'It is optimal',
      ],
      a: 1,
      x: [
        'Register spilling',
        'A host stall',
        'Pure latency-bound',
      ],
      e: 'When both are moderate, no single roof dominates; the limiter may be instruction issue, partial stalls, or imbalance. Drill into scheduler/warp-state statistics to identify the marginal constraint rather than assuming bandwidth or compute.',
      ref: 'Nsight Compute; balanced-kernel analysis',
      tags: ['balanced', 'analysis', 'speed-of-light'],
    },
    {
      d: 4,
      q: 'compute-sanitizer --tool synccheck flags…',
      o: [
        'Slow synchronization',
        'Invalid use of synchronization primitives — e.g. __syncthreads in divergent code or incorrect warp-sync mask usage that could deadlock or give undefined results',
        'Bank conflicts',
        'Out-of-bounds access',
      ],
      a: 1,
      x: [
        'Register spills',
        'Uninitialized reads',
        'Low occupancy',
      ],
      e: 'synccheck detects illegal barrier/sync usage (e.g. divergent __syncthreads, bad __syncwarp masks) that can deadlock or produce undefined behavior — catching a class of correctness bugs that are otherwise intermittent and hard to reproduce.',
      ref: 'compute-sanitizer (synccheck)',
      tags: ['synccheck', 'compute-sanitizer', 'barriers'],
    },
  ],
  { idPrefix: 'profiling-d' }
)
