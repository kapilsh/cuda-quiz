import { defineQuestions } from './_helpers.js'

// Libraries — batch C. Triton, CuPy/Numba, TensorRT(-LLM), Transformer Engine,
// DALI/nvJPEG/NPP, RAPIDS, CUB select/partition, stdpar. Distinct from libraries.js/.b.js.
export default defineQuestions(
  'libraries',
  [
    {
      d: 3,
      q: 'Triton (OpenAI) is…',
      o: [
        'A profiler',
        'A Python-based DSL/compiler for writing GPU kernels at block granularity, where you express tile-level work and it handles much of the low-level scheduling',
        'A collective communications library',
        'A linear algebra library',
      ],
      a: 1,
      x: [
        'A replacement for the CUDA driver',
        'A dataset loader',
        'A debugger',
      ],
      e: 'Triton lets you write performant kernels in Python at a tile/block abstraction; its compiler manages memory coalescing, shared memory, and scheduling. It is widely used for fused DL kernels (e.g. in PyTorch’s inductor).',
      ref: 'Triton documentation',
      tags: ['triton', 'dsl', 'kernels'],
    },
    {
      d: 2,
      q: 'CuPy is best described as…',
      o: [
        'A C++ template library',
        'A NumPy-compatible array library that runs on the GPU (drop-in ndarray with CUDA-backed operations)',
        'A profiler',
        'A communication library',
      ],
      a: 1,
      x: [
        'A kernel debugger',
        'A dataset format',
        'A linker',
      ],
      e: 'CuPy mirrors the NumPy/SciPy API but executes on the GPU (backed by cuBLAS/cuDNN/cuSPARSE/etc.), making it easy to accelerate array code and also write custom kernels (RawKernel/ElementwiseKernel).',
      ref: 'CuPy documentation',
      tags: ['cupy', 'numpy', 'arrays'],
    },
    {
      d: 3,
      q: 'Numba’s @cuda.jit lets you…',
      o: [
        'Compile C++ templates',
        'Write CUDA kernels in a subset of Python that Numba JIT-compiles to PTX/SASS, with threadIdx/blockIdx-style indexing',
        'Run NumPy on the CPU faster only',
        'Manage NCCL communicators',
      ],
      a: 1,
      x: [
        'Profile kernels',
        'Train neural networks',
        'Allocate pinned memory only',
      ],
      e: 'Numba’s CUDA target compiles annotated Python functions into GPU kernels, exposing the CUDA execution model (grids/blocks/threads, shared memory, atomics) — a low-friction way to prototype kernels without C++.',
      ref: 'Numba CUDA documentation',
      tags: ['numba', 'jit', 'python-kernels'],
    },
    {
      d: 3,
      q: 'TensorRT is primarily a…',
      o: [
        'Training framework',
        'Deep-learning inference optimizer and runtime: it fuses layers, selects kernels/precisions (FP16/INT8/FP8), and builds an optimized engine for deployment',
        'Collective library',
        'Profiler',
      ],
      a: 1,
      x: [
        'A data loader',
        'A linear algebra library',
        'A compiler for CUDA C++',
      ],
      e: 'TensorRT takes a trained model and produces a highly optimized inference engine via layer/tensor fusion, kernel auto-tuning, precision calibration (INT8/FP8), and memory planning — targeting low-latency/high-throughput deployment.',
      ref: 'NVIDIA TensorRT documentation',
      tags: ['tensorrt', 'inference', 'optimization'],
    },
    {
      d: 4,
      q: 'TensorRT-LLM specializes TensorRT for…',
      o: [
        'Image classification',
        'Large language model inference — with optimized attention (paged KV cache), in-flight batching, tensor/pipeline parallelism, and quantization',
        'Training LLMs',
        'Data loading',
      ],
      a: 1,
      x: [
        'Speech only',
        'Reinforcement learning',
        'Graph analytics',
      ],
      e: 'TensorRT-LLM provides LLM-specific optimizations: fused/paged attention, KV-cache management, continuous (in-flight) batching, multi-GPU parallelism, and FP8/INT4 quantization — for high-throughput, low-latency LLM serving.',
      ref: 'NVIDIA TensorRT-LLM documentation',
      tags: ['tensorrt-llm', 'inference', 'llm'],
    },
    {
      d: 4,
      q: 'The Transformer Engine (TE) library’s main job is…',
      o: [
        'Data loading',
        'Providing FP8 (and FP16/BF16) transformer building blocks with automatic scaling/recipe management so models train/infer in FP8 with maintained accuracy',
        'Collective communication',
        'Graph capture',
      ],
      a: 1,
      x: [
        'Sorting',
        'Sparse solves',
        'Image decoding',
      ],
      e: 'TE offers fused, FP8-capable transformer layers (attention, MLP) plus the amax/scaling machinery (per-tensor, delayed scaling) so frameworks can run transformers in FP8 on Hopper/Blackwell with minimal accuracy loss.',
      ref: 'NVIDIA Transformer Engine documentation',
      tags: ['transformer-engine', 'fp8', 'transformers'],
    },
    {
      d: 3,
      q: 'NVIDIA DALI accelerates…',
      o: [
        'Matrix multiply',
        'Data loading and preprocessing (decode, resize, augment) on the GPU, removing CPU input-pipeline bottlenecks in training',
        'Collective communication',
        'Kernel compilation',
      ],
      a: 1,
      x: [
        'Gradient all-reduce',
        'Sparse linear algebra',
        'FFTs',
      ],
      e: 'DALI builds GPU-accelerated input pipelines (JPEG decode via nvJPEG, resize, normalize, augment), offloading preprocessing from the CPU so the GPU isn’t starved — addressing the common data-loading bottleneck.',
      ref: 'NVIDIA DALI documentation',
      tags: ['dali', 'data-loading', 'preprocessing'],
    },
    {
      d: 3,
      q: 'cuSPARSELt is specialized for…',
      o: [
        'Dense GEMM only',
        'Structured-sparse (2:4) matrix multiply on Sparse Tensor Cores, exploiting pruned weights for ~2× throughput',
        'FFTs',
        'Random numbers',
      ],
      a: 1,
      x: [
        'Graph analytics',
        'Image decoding',
        'Unstructured sparse solves',
      ],
      e: 'cuSPARSELt provides high-performance matmul for the 2:4 structured-sparsity format, letting models with appropriately pruned/compressed weights use the Sparse Tensor Cores for higher effective throughput than dense.',
      ref: 'cuSPARSELt documentation',
      tags: ['cusparselt', 'structured-sparsity', '2:4'],
    },
    {
      d: 4,
      q: 'CUB’s DeviceSelect::Flagged / DevicePartition are used for…',
      o: [
        'Sorting',
        'Stream compaction — selecting/partitioning elements that satisfy a predicate (or flag) into a compact output, device-wide and efficiently',
        'Matrix multiply',
        'Random sampling',
      ],
      a: 1,
      x: [
        'FFTs',
        'Reductions only',
        'Histogramming',
      ],
      e: 'These primitives implement parallel compaction/partition (keep elements where a flag/predicate holds), a common building block (filtering, removing invalids) that is tricky to do efficiently by hand due to the prefix-sum of output positions.',
      ref: 'CUB documentation (DeviceSelect/DevicePartition)',
      tags: ['cub', 'compaction', 'select'],
    },
    {
      d: 3,
      q: 'thrust::transform_reduce fuses…',
      o: [
        'Two sorts',
        'An elementwise transform with a reduction in one pass (e.g. sum of squares = transform x→x*x then reduce +), avoiding a temporary array',
        'A scan and a gather',
        'A copy and a sort',
      ],
      a: 1,
      x: [
        'Two reductions',
        'A histogram and a sort',
        'A broadcast and a reduce',
      ],
      e: 'transform_reduce applies a unary op and reduces the results in a single fused pass, so you don’t materialize the transformed array — efficient for norms, dot products (with a zip), and similar patterns.',
      ref: 'Thrust documentation (transform_reduce)',
      tags: ['thrust', 'transform-reduce', 'fusion'],
    },
    {
      d: 4,
      q: 'A thrust::zip_iterator is handy for SoA data because it…',
      o: [
        'Compresses the arrays',
        'Presents several separate arrays as a single sequence of tuples, so an algorithm can process corresponding elements together without interleaving storage',
        'Sorts the arrays',
        'Allocates memory',
      ],
      a: 1,
      x: [
        'Converts SoA to AoS in memory',
        'Runs on the host only',
        'Requires a custom kernel',
      ],
      e: 'zip_iterator virtually combines multiple arrays into tuples on the fly, letting Thrust algorithms operate on logically-grouped fields while keeping the coalescing-friendly SoA storage in memory.',
      ref: 'Thrust documentation (zip_iterator)',
      tags: ['thrust', 'zip-iterator', 'soa'],
    },
    {
      d: 3,
      q: 'The RAPIDS library cuDF provides…',
      o: [
        'GPU deep learning',
        'A GPU DataFrame (pandas-like) for ETL/analytics — joins, group-bys, filters executed on the GPU',
        'Collective communication',
        'Kernel compilation',
      ],
      a: 1,
      x: [
        'Image decoding',
        'A profiler',
        'Sparse solves',
      ],
      e: 'cuDF mirrors pandas on the GPU for data analytics (filter/join/groupby/aggregate at GPU speed). With cuML (ML) and cuGraph (graph analytics), it forms the RAPIDS ecosystem for end-to-end data science on GPUs.',
      ref: 'RAPIDS cuDF documentation',
      tags: ['rapids', 'cudf', 'dataframe'],
    },
    {
      d: 4,
      q: 'NVIDIA stdpar (via nvc++) lets you…',
      o: [
        'Write PTX by hand',
        'Run C++17 standard parallel algorithms (std::transform with std::execution::par) on the GPU automatically, without explicit CUDA code',
        'Profile kernels',
        'Manage NCCL',
      ],
      a: 1,
      x: [
        'Compile Python',
        'Allocate pinned memory',
        'Train models',
      ],
      e: 'nvc++ can offload C++ parallel STL algorithms to the GPU, using managed memory so standard code (std::sort, std::transform with par) runs on the device — a high-level, portable path to GPU acceleration without kernels.',
      ref: 'NVIDIA HPC SDK (nvc++ stdpar)',
      tags: ['stdpar', 'nvc++', 'parallel-stl'],
    },
    {
      d: 3,
      q: 'NPP (NVIDIA Performance Primitives) provides…',
      o: [
        'Neural network layers',
        'Optimized image, video, and signal processing primitives (filters, transforms, statistics) on the GPU',
        'Collective communication',
        'Sparse solves',
      ],
      a: 1,
      x: [
        'A dataframe',
        'A kernel compiler',
        'FFT plans only',
      ],
      e: 'NPP is a large library of GPU-accelerated primitives for image/signal processing (color conversion, filtering, morphology, statistics), used in vision and media pipelines without hand-writing kernels.',
      ref: 'NPP documentation',
      tags: ['npp', 'image-processing', 'primitives'],
    },
    {
      d: 4,
      q: 'The WMMA API (nvcuda::wmma namespace) lets you use tensor cores from CUDA C++ by…',
      o: [
        'Calling cuBLAS',
        'Declaring fragment types, load_matrix_sync/store_matrix_sync, and mma_sync to perform warp-collective tile multiply-accumulate',
        'Using atomics',
        'Writing PTX only',
      ],
      a: 1,
      x: [
        'Using shared memory directly',
        'Per-thread scalar math',
        'A host function',
      ],
      e: 'WMMA exposes warp-level matrix ops: you declare fragments (matrix_a/b/accumulator), load tiles, call wmma::mma_sync, and store results. It’s a portable (if less flexible) way to program tensor cores than inline mma/wgmma or CUTLASS.',
      ref: 'CUDA C++ Programming Guide (Warp Matrix Functions / WMMA)',
      tags: ['wmma', 'tensor-cores', 'fragments'],
    },
    {
      d: 4,
      q: 'cuBLASLt is the path to use FP8 matmul because it…',
      o: [
        'Only supports FP32',
        'Exposes per-tensor scale factors and FP8 (E4M3/E5M2) operand types in its matmul descriptor, with epilogues for dequant/bias/activation',
        'Runs on the CPU',
        'Disables tensor cores',
      ],
      a: 1,
      x: [
        'Is column-major only',
        'Requires no scaling',
        'Is for sparse only',
      ],
      e: 'FP8 GEMM needs explicit input/output scales; cuBLASLt’s descriptor API supports FP8 operand types with scale pointers and fused epilogues, which the legacy cuBLAS API does not expose as flexibly.',
      ref: 'cuBLASLt documentation (FP8)',
      tags: ['cublaslt', 'fp8', 'matmul', 'scaling'],
    },
    {
      d: 3,
      q: 'nvJPEG is used to…',
      o: [
        'Train classifiers',
        'Decode (and encode) JPEG images on the GPU, accelerating image input pipelines',
        'Reduce gradients',
        'Sort arrays',
      ],
      a: 1,
      x: [
        'Compute FFTs',
        'Manage streams',
        'Run collectives',
      ],
      e: 'nvJPEG offloads JPEG decode/encode to the GPU, a key part of GPU input pipelines (used by DALI) so image decoding doesn’t bottleneck the CPU during training/inference on large image datasets.',
      ref: 'nvJPEG documentation',
      tags: ['nvjpeg', 'image-decode', 'data-loading'],
    },
    {
      d: 5,
      q: 'PyTorch’s default GPU backend dispatches a matmul to which library, and an attention op might use…',
      o: [
        'Always a custom kernel',
        'cuBLAS/cuBLASLt for GEMM; attention may use cuDNN or a fused (FlashAttention) kernel, sometimes generated via Triton/inductor',
        'NCCL for both',
        'cuFFT',
      ],
      a: 1,
      x: [
        'cuRAND',
        'NPP',
        'cuSPARSE',
      ],
      e: 'Framework ops map to tuned libraries: GEMM → cuBLAS(Lt); convolutions/attention → cuDNN or fused kernels (FlashAttention, often Triton-compiled in torch.compile/inductor). Knowing the backing kernel helps interpret profiles.',
      ref: 'PyTorch backend; cuBLAS/cuDNN/Triton',
      tags: ['pytorch', 'cublas', 'cudnn', 'backend'],
    },
    {
      d: 4,
      q: 'CUB’s DeviceHistogram differs from a naive atomic histogram by…',
      o: [
        'Using the CPU',
        'Implementing an optimized device-wide histogram (privatization, efficient binning) so you avoid hand-tuning shared/global atomic strategies',
        'Sorting first',
        'Using constant memory',
      ],
      a: 1,
      x: [
        'Producing approximate counts',
        'Requiring one bin',
        'Running per-warp only',
      ],
      e: 'DeviceHistogram provides a tuned, general histogram (even/custom ranges, multi-channel) using privatization and efficient atomics internally, saving you from re-deriving the shared-memory-then-global optimization yourself.',
      ref: 'CUB documentation (DeviceHistogram)',
      tags: ['cub', 'histogram', 'privatization'],
    },
    {
      d: 3,
      q: 'A custom Thrust functor (a struct with __host__ __device__ operator()) is used to…',
      o: [
        'Allocate memory',
        'Define the per-element operation passed to algorithms like transform/transform_reduce, so the GPU runs your logic',
        'Launch kernels manually',
        'Manage streams',
      ],
      a: 1,
      x: [
        'Profile the code',
        'Compile PTX',
        'Set the block size',
      ],
      e: 'Thrust algorithms are parameterized by functors. A __host__ __device__ functor lets you express arbitrary elementwise/binary logic that Thrust applies in parallel, capturing parameters as members — the customization point for Thrust code.',
      ref: 'Thrust documentation (functors)',
      tags: ['thrust', 'functor', 'customization'],
    },
    {
      d: 4,
      q: 'RAPIDS cuGraph provides…',
      o: [
        'CUDA Graphs capture',
        'GPU-accelerated graph analytics algorithms (PageRank, BFS, connected components, etc.) over large graphs',
        'A kernel compiler',
        'Collective communication',
      ],
      a: 1,
      x: [
        'Image decoding',
        'Dense GEMM',
        'FFTs',
      ],
      e: 'cuGraph (note: distinct from CUDA Graphs) implements graph-analytics algorithms on the GPU with cuDF integration, scaling traversal/centrality/community algorithms to large graphs — part of RAPIDS.',
      ref: 'RAPIDS cuGraph documentation',
      tags: ['cugraph', 'graph-analytics', 'rapids'],
    },
    {
      d: 5,
      q: 'JAX/XLA accelerates GPU code by…',
      o: [
        'Interpreting Python at runtime',
        'Tracing and compiling array programs into fused, optimized kernels (operator fusion, layout choices) via the XLA compiler, targeting GPU/TPU',
        'Calling cuBLAS only',
        'Using NCCL for everything',
      ],
      a: 1,
      x: [
        'Running on the CPU only',
        'Avoiding compilation',
        'Writing PTX manually',
      ],
      e: 'JAX traces functions and XLA compiles the computation graph, fusing elementwise ops, choosing layouts, and emitting optimized kernels (using cuBLAS/cuDNN for big ops). jit + fusion reduce launches and memory traffic, similar in spirit to torch.compile.',
      ref: 'JAX/XLA documentation',
      tags: ['jax', 'xla', 'fusion', 'compiler'],
    },
    {
      d: 3,
      q: 'For accelerating an existing CPU code with minimal rewriting, OpenACC offers…',
      o: [
        'A new language',
        'Compiler directives (#pragma acc) that offload loops/regions to the GPU, managing data movement, similar in spirit to OpenMP offload',
        'A kernel debugger',
        'A collective library',
      ],
      a: 1,
      x: [
        'A dataframe',
        'A profiler',
        'A linker',
      ],
      e: 'OpenACC (and OpenMP target offload) let you annotate loops/data regions for GPU execution via directives, a higher-level, less intrusive route than CUDA C++ — popular in HPC/Fortran codebases (NVIDIA HPC SDK).',
      ref: 'OpenACC; NVIDIA HPC SDK',
      tags: ['openacc', 'directives', 'offload'],
    },
    {
      d: 4,
      q: 'cuTENSOR is preferred over reshaping-to-GEMM-then-cuBLAS for tensor contractions when…',
      o: [
        'Never',
        'The contraction’s index pattern would require expensive explicit transposes/copies; cuTENSOR fuses the permutation into the contraction',
        'The tensors are 2D',
        'You need random numbers',
      ],
      a: 1,
      x: [
        'The data is sparse',
        'You need an FFT',
        'You want the CPU',
      ],
      e: 'Arbitrary tensor contractions (einsum) often need permutations to map onto GEMM. cuTENSOR handles general index contractions, fusing the necessary permutations and choosing efficient kernels, avoiding materializing transposed copies.',
      ref: 'cuTENSOR documentation',
      tags: ['cutensor', 'contraction', 'einsum'],
    },
    {
      d: 2,
      q: 'Which library is the right choice for an FFT-based convolution?',
      o: ['NCCL', 'cuFFT (forward FFT, pointwise multiply, inverse FFT)', 'cuRAND', 'cuDF'],
      a: 1,
      x: ['cuBLAS', 'NPP', 'Thrust'],
      e: 'Convolution can be done as elementwise multiplication in the frequency domain: cuFFT to transform, multiply, and inverse-transform. For small filters, direct/Winograd convolution (cuDNN) is usually faster; FFT wins for large kernels.',
      ref: 'cuFFT documentation; convolution methods',
      tags: ['cufft', 'convolution', 'fft'],
    },
    {
      d: 5,
      q: 'CUTLASS also ships a Python interface. Its main benefit is…',
      o: [
        'Running kernels on the CPU',
        'Letting you configure, emit, and autotune CUTLASS GEMM/conv kernels from Python (e.g. generate a specialized kernel) without hand-writing the C++ template instantiation',
        'Profiling',
        'Data loading',
      ],
      a: 1,
      x: [
        'Training models',
        'Collective communication',
        'Replacing cuBLAS at runtime in PyTorch by default',
      ],
      e: 'The CUTLASS Python interface (and emitter) lets you parameterize and generate tuned kernels programmatically, useful for searching tile/schedule configs or producing custom fused kernels — bridging the C++ templates and higher-level workflows.',
      ref: 'CUTLASS Python documentation',
      tags: ['cutlass', 'python', 'codegen'],
    },
    {
      d: 3,
      q: 'cuda-python (the official bindings) gives Python access to…',
      o: [
        'Only NumPy',
        'The CUDA Driver and Runtime APIs (and NVRTC) from Python, so tools/frameworks can manage contexts, modules, memory, and launches without C++',
        'Just cuBLAS',
        'A dataframe',
      ],
      a: 1,
      x: [
        'Only profiling',
        'The host compiler',
        'NCCL only',
      ],
      e: 'cuda-python exposes the low-level CUDA APIs (driver/runtime/NVRTC) to Python, enabling frameworks and tools (e.g. Numba, custom runtimes) to drive the GPU directly — the official binding underpinning much of the Python CUDA ecosystem.',
      ref: 'cuda-python documentation',
      tags: ['cuda-python', 'bindings', 'driver-api'],
    },
    {
      d: 4,
      q: 'A practical reason to use a tuned library (cuBLAS/cuDNN/CUTLASS) instead of Triton/hand-written kernels for standard ops is…',
      o: [
        'Libraries are always slower',
        'They are maintained and re-tuned for each new GPU generation and cover many shapes, so you inherit peak performance and forward support for free',
        'They cannot use tensor cores',
        'They only run on CPUs',
      ],
      a: 1,
      x: [
        'They require no GPU',
        'They are written in Python',
        'They avoid all memory access',
      ],
      e: 'Vendor libraries track new architectures and shapes with ongoing tuning. Custom kernels (Triton/CUTLASS) shine for fused/novel ops or specific shapes, but for standard GEMM/conv the libraries usually win with far less maintenance burden.',
      ref: 'Library vs custom-kernel trade-offs',
      tags: ['libraries', 'maintenance', 'production'],
    },
  ],
  { idPrefix: 'libraries-c' }
)
