import { defineQuestions } from './_helpers.js'

// GPU Architecture — batch D. Pipelines, async hardware, generation specifics,
// scheduling, memory system internals. Distinct from architecture.js/.b.js/.c.js.
export default defineQuestions(
  'architecture',
  [
    {
      d: 4,
      q: 'Why did NVIDIA add a dedicated asynchronous-copy datapath (cp.async on Ampere) instead of relying on the existing load/store path?',
      o: [
        'To compress data',
        'The old path staged global→shared through registers (occupying them and blocking the thread); a direct async global→shared path frees registers and overlaps the copy with compute',
        'To increase clocks',
        'To add tensor cores',
      ],
      a: 1,
      x: [
        'To reduce memory size',
        'For graphics',
        'To enable FP64',
      ],
      e: 'Pre-Ampere, loading into shared memory meant LD into a register then ST to shared — consuming registers and stalling. cp.async streams data directly into shared memory asynchronously, the hardware basis for deep software pipelines in GEMM/attention.',
      ref: 'NVIDIA Ampere Whitepaper; cp.async',
      tags: ['cp-async', 'ampere', 'async-copy'],
    },
    {
      d: 5,
      q: 'TMA on Hopper is a single-thread, descriptor-driven engine. Compared to all 32 lanes issuing cp.async, this reduces…',
      o: [
        'Precision',
        'Address-generation work and register pressure — one thread launches a bulk multidimensional copy instead of every lane computing addresses and holding them in registers',
        'Shared memory',
        'The clock',
      ],
      a: 1,
      x: [
        'Tensor cores',
        'The L2 size',
        'Occupancy floors',
      ],
      e: 'cp.async has each lane compute and issue addresses (register- and instruction-heavy). TMA offloads multidimensional address generation to hardware via a tensor map descriptor, so one thread triggers the whole tile copy — freeing registers/issue slots for math.',
      ref: 'NVIDIA Hopper Whitepaper; TMA',
      tags: ['tma', 'hopper', 'address-generation'],
    },
    {
      d: 4,
      q: 'The unified L1/shared-memory design (Volta+) lets a kernel…',
      o: [
        'Use both at full size always',
        'Trade the on-chip SRAM between L1 cache and shared memory via a configurable carveout, matching the kernel’s needs (cache-heavy vs shared-memory-heavy)',
        'Disable both',
        'Move them off-chip',
      ],
      a: 1,
      x: [
        'Double the register file',
        'Add tensor cores',
        'Increase the L2',
      ],
      e: 'Since Volta, L1 and shared memory share one SRAM pool with an adjustable split (cudaFuncAttributePreferredSharedMemoryCarveout). Tiled kernels favor more shared memory; cache-friendly irregular kernels favor more L1.',
      ref: 'NVIDIA Volta+ whitepapers; unified L1/shared',
      tags: ['l1-shared', 'carveout', 'unified'],
    },
    {
      d: 4,
      q: 'The Sparse Tensor Core processes 2:4 structured-sparse matrices by…',
      o: [
        'Skipping random zeros',
        'Storing 2 nonzeros per 4-element group plus 2-bit metadata indices, and feeding only the nonzeros (with the dense operand selected by the indices) to double throughput',
        'Compressing to FP8',
        'Using the CPU',
      ],
      a: 1,
      x: [
        'Pruning at runtime',
        'Using texture memory',
        'Avoiding the K loop',
      ],
      e: '2:4 sparsity compresses each 4-group to its 2 nonzeros + metadata; the hardware uses the metadata to pick matching dense-operand elements, so it does half the multiplies — roughly 2× throughput for models pruned to that pattern.',
      ref: 'NVIDIA Ampere Sparse Tensor Cores',
      tags: ['sparsity', '2:4', 'tensor-cores'],
    },
    {
      d: 3,
      q: 'A GPU hides memory latency primarily through massive multithreading, whereas a CPU primarily uses…',
      o: [
        'More threads',
        'Large caches, out-of-order execution, prefetchers, and branch prediction to reduce/hide latency for a few threads',
        'Tensor cores',
        'Higher bandwidth',
      ],
      a: 1,
      x: [
        'SIMT',
        'Warps',
        'Shared memory',
      ],
      e: 'CPUs optimize single-thread latency (big caches, OoO, speculation); GPUs optimize throughput by keeping thousands of threads resident and switching among them to hide latency. This architectural difference drives their distinct programming models.',
      ref: 'CPU vs GPU architecture',
      tags: ['cpu-gpu', 'latency-hiding', 'architecture'],
    },
    {
      d: 5,
      q: 'wgmma (Hopper warpgroup MMA) is "asynchronous," meaning…',
      o: [
        'It runs on the host',
        'The instruction launches the matrix multiply and the warpgroup can proceed (e.g. issue more loads) while it executes, with completion tracked via a commit/wait — overlapping math with data movement',
        'It skips accumulation',
        'It uses the CPU',
      ],
      a: 1,
      x: [
        'It blocks the warp',
        'It disables tensor cores',
        'It is synchronous like mma',
      ],
      e: 'Async wgmma decouples issuing the MMA from its completion, letting the warpgroup overlap subsequent operand loads (TMA/async copy) with ongoing tensor-core work — essential to keep the pipeline full in Hopper GEMM.',
      ref: 'Hopper PTX ISA (wgmma async)',
      tags: ['wgmma', 'async', 'hopper', 'pipelining'],
    },
    {
      d: 3,
      q: 'Why are tensor cores far more area/energy efficient than CUDA cores for matmul?',
      o: [
        'They run at higher clocks',
        'A single tensor-core instruction performs a whole small matrix multiply-accumulate, amortizing instruction fetch/decode/scheduling over many MACs and using dense systolic-like datapaths',
        'They use the CPU',
        'They avoid memory',
      ],
      a: 1,
      x: [
        'They use FP64',
        'They have more registers',
        'They skip accumulation',
      ],
      e: 'Doing many MACs per instruction (vs one FMA per CUDA-core instruction) amortizes control overhead and uses efficient dense datapaths, yielding much higher throughput-per-watt and per-area for GEMM-shaped work — at the cost of generality.',
      ref: 'NVIDIA tensor-core design; efficiency',
      tags: ['tensor-cores', 'efficiency', 'matmul'],
    },
    {
      d: 4,
      q: 'The "GigaThread" global scheduler distributes blocks; within an SM, what assigns warps to execution each cycle?',
      o: [
        'The global scheduler',
        'The per-sub-partition warp schedulers (4 per SM), each selecting an eligible warp to issue from its assigned warps',
        'The host',
        'The memory controller',
      ],
      a: 1,
      x: [
        'The L2 controller',
        'The copy engine',
        'The tensor core',
      ],
      e: 'Two scheduling tiers: the global work distributor assigns blocks to SMs; inside an SM, the 4 sub-partition warp schedulers each pick an eligible warp per cycle from their warps. This hierarchy keeps SMs busy and load-balanced.',
      ref: 'NVIDIA SM architecture; scheduling',
      tags: ['warp-scheduler', 'gigathread', 'scheduling'],
    },
    {
      d: 4,
      q: 'On Hopper, a thread block cluster’s blocks can access each other’s shared memory because the hardware provides…',
      o: [
        'A shared L1',
        'A dedicated SM-to-SM interconnect within the GPC, mapping a peer block’s shared memory into the accessing block’s address space (DSMEM)',
        'NVLink',
        'A global barrier',
      ],
      a: 1,
      x: [
        'PCIe',
        'A shared register file',
        'Texture memory',
      ],
      e: 'DSMEM relies on an SM-to-SM network connecting the SMs of a GPC so co-scheduled cluster blocks can read/write/atomically access one another’s shared memory directly — far faster than going through global memory.',
      ref: 'NVIDIA Hopper Whitepaper; DSMEM/SM-to-SM network',
      tags: ['dsmem', 'sm-to-sm', 'clusters', 'hopper'],
    },
    {
      d: 5,
      q: 'Blackwell’s 5th-gen tensor cores (tcgen05) operate at the granularity of…',
      o: [
        'A single thread',
        'A larger cooperative unit (e.g. multiple warps / the whole CTA via the 2-SM "pair") feeding much wider MMAs from Tensor Memory, beyond Hopper’s warpgroup',
        'One warp only',
        'The host',
      ],
      a: 1,
      x: [
        'A single lane',
        'A block of pixels',
        'A GPC',
      ],
      e: 'Blackwell scales the cooperative MMA unit further (and adds Tensor Memory for accumulators/operands), enabling wider tensor-core operations than Hopper’s warpgroup wgmma — extracting more throughput per instruction at very low precision.',
      ref: 'NVIDIA Blackwell Whitepaper; tcgen05',
      tags: ['blackwell', 'tcgen05', 'tensor-cores'],
    },
    {
      d: 3,
      q: 'The reason GPUs schedule work in warps (rather than individual threads) is fundamentally to…',
      o: [
        'Save memory',
        'Amortize control logic (one instruction fetch/decode/schedule drives 32 lanes), trading some flexibility (divergence cost) for huge throughput efficiency',
        'Enable atomics',
        'Increase clocks',
      ],
      a: 1,
      x: [
        'Reduce registers',
        'Improve coalescing only',
        'Allow recursion',
      ],
      e: 'SIMT shares the front-end (fetch/decode/issue) across 32 lanes, so control overhead is paid once per warp. This is far more efficient than per-thread scheduling, at the cost of divergence penalties when lanes disagree.',
      ref: 'SIMT architecture rationale',
      tags: ['simt', 'warp', 'efficiency'],
    },
    {
      d: 4,
      q: 'The L2 cache being the device-wide coherence point means…',
      o: [
        'L1 is coherent across SMs',
        'Atomics and cross-SM visibility resolve at L2; per-SM L1s are not coherent with each other, so cross-block communication needs fences/L1-bypass to reach the L2 coherence point',
        'Registers are shared',
        'Shared memory is global',
      ],
      a: 1,
      x: [
        'L2 is per-SM',
        'There is no L2',
        'L2 is on the host',
      ],
      e: 'L2 is shared and coherent across SMs; L1s are private and not mutually coherent. Therefore global atomics execute at L2, and cross-SM communication via global memory requires ordering (fences) to push past stale L1 state to L2.',
      ref: 'GPU memory architecture; coherence',
      tags: ['l2-coherence', 'l1', 'atomics'],
    },
    {
      d: 4,
      q: 'Why does the data-center A100/H100 have a much higher FP64 ratio than a same-generation GeForce card?',
      o: [
        'Software emulation',
        'They include dedicated FP64 units (and FP64 tensor cores) sized for HPC; consumer cards minimize FP64 hardware to save area/cost since games don’t need it',
        'Higher clocks',
        'More memory',
      ],
      a: 1,
      x: [
        'A different ISA',
        'Tensor cores only',
        'No FP64 at all on consumer',
      ],
      e: 'Data-center parts dedicate real FP64 datapaths (≈1:2 of FP32) for HPC; consumer GPUs include minimal FP64 (≈1:32–1:64) to save silicon, since graphics/most consumer workloads don’t need double precision.',
      ref: 'NVIDIA product segmentation; FP64 ratios',
      tags: ['fp64', 'datacenter-vs-consumer', 'hpc'],
    },
    {
      d: 5,
      q: 'NVLink-C2C (Grace Hopper) gives the GPU coherent access to CPU memory, which architecturally enables…',
      o: [
        'Faster tensor cores',
        'Treating the large CPU LPDDR as an extended, coherent memory tier for the GPU (e.g. for huge models / KV caches) at ~900 GB/s, beyond HBM capacity',
        'More SMs',
        'Lower precision',
      ],
      a: 1,
      x: [
        'A bigger L2',
        'Faster NVLink between GPUs',
        'Compression',
      ],
      e: 'Coherent, high-bandwidth CPU↔GPU linking lets the GPU use the CPU’s big memory pool as an additional tier without explicit copies, valuable when working sets exceed HBM — a key Grace Hopper architectural feature.',
      ref: 'NVIDIA Grace Hopper; NVLink-C2C',
      tags: ['nvlink-c2c', 'grace-hopper', 'memory-tier'],
    },
    {
      d: 3,
      q: 'The number of warp schedulers per SM (4 on modern GPUs) sets…',
      o: [
        'The register file size',
        'The maximum instructions that can be issued per cycle from distinct warps (up to ~4, more with dual-issue) — bounding per-SM instruction throughput',
        'The L2 size',
        'The number of SMs',
      ],
      a: 1,
      x: [
        'The warp size',
        'The clock rate',
        'The memory bandwidth',
      ],
      e: 'Four schedulers can each issue from one (sometimes two) eligible warp per cycle, so the SM issues up to ~4–8 instructions/cycle across warps. This, with pipe throughputs, caps the SM’s instruction-issue rate.',
      ref: 'NVIDIA SM architecture',
      tags: ['warp-scheduler', 'issue-rate', 'sm'],
    },
    {
      d: 4,
      q: 'Why is HBM placed on the same package (via an interposer) as the GPU die?',
      o: [
        'To save cost',
        'A very wide interface (thousands of bits) needs short, dense connections an interposer provides, delivering the enormous bandwidth (TB/s) that off-package GDDR buses cannot',
        'For cooling',
        'To reduce capacity',
      ],
      a: 1,
      x: [
        'For graphics output',
        'To enable NVLink',
        'For ECC only',
      ],
      e: 'HBM stacks connect over a silicon interposer with a very wide bus; the short, dense interconnect enables terabytes/sec of bandwidth that a traditional off-package GDDR bus (narrower, longer) can’t match — at higher manufacturing cost.',
      ref: 'HBM packaging; interposer',
      tags: ['hbm', 'interposer', 'bandwidth'],
    },
    {
      d: 4,
      q: 'A thread block cluster adds a tier between block and grid. The three cooperation tiers on Hopper are…',
      o: [
        'Thread, warp, block',
        'Block (__syncthreads), cluster (cluster.sync + DSMEM), and grid (cooperative launch grid.sync)',
        'Warp, block, grid',
        'SM, GPC, GPU',
      ],
      a: 1,
      x: [
        'Lane, warp, block',
        'Block, GPC, device',
        'Thread, block, device',
      ],
      e: 'Hopper’s clusters insert a cooperation level between the block (intra-SM, __syncthreads) and the grid (cooperative-launch grid.sync). The cluster (co-scheduled blocks in a GPC) supports cluster.sync() and distributed shared memory.',
      ref: 'CUDA C++ Programming Guide (Thread Block Clusters)',
      tags: ['clusters', 'cooperation-tiers', 'hopper'],
    },
    {
      d: 5,
      q: 'Why does Blackwell’s NVFP4 (with micro-scaling) roughly double inference throughput vs Hopper FP8 for some models?',
      o: [
        'Higher clocks',
        'FP4 packs twice the elements per byte of FP8, so tensor cores process about double the elements per cycle — with per-block micro-scaling preserving enough accuracy',
        'More SMs only',
        'Larger HBM',
      ],
      a: 1,
      x: [
        'Software optimization only',
        'FP4 is more precise',
        'It uses the CPU',
      ],
      e: 'Halving bit-width (FP8→FP4) doubles the elements processed per cycle on tensor cores. Micro-scaling (NVFP4) keeps accuracy acceptable, so supported inference workloads see roughly 2× throughput over FP8 — the low-precision scaling trend.',
      ref: 'NVIDIA Blackwell; NVFP4',
      tags: ['nvfp4', 'blackwell', 'throughput', 'inference'],
    },
    {
      d: 3,
      q: 'The on-chip memory hierarchy ordered fastest→slowest (latency) is…',
      o: [
        'Global, L2, L1, shared, registers',
        'Registers, shared memory / L1, L2, global (HBM)',
        'L2, registers, shared, global',
        'Shared, global, L1, registers',
      ],
      a: 1,
      x: [
        'Global, registers, L1, L2',
        'L1, L2, registers, shared',
        'Registers, global, L2, shared',
      ],
      e: 'Registers are fastest, then shared memory/L1 (on-chip SRAM, ~tens of cycles), then the shared L2, then global HBM (hundreds of cycles). Optimization moves data up this hierarchy (tiling into shared/registers) to cut effective latency/traffic.',
      ref: 'CUDA memory hierarchy',
      tags: ['memory-hierarchy', 'latency', 'sram'],
    },
    {
      d: 4,
      q: 'Why can two GPUs connected only by PCIe (no NVLink) bottleneck multi-GPU training more than an NVLink-connected pair?',
      o: [
        'PCIe is more reliable',
        'PCIe bandwidth (tens of GB/s) is far below NVLink (hundreds of GB/s), so gradient all-reduce / activation exchange takes much longer, limiting scaling',
        'PCIe has higher latency only',
        'PCIe disables P2P always',
      ],
      a: 1,
      x: [
        'NVLink is slower',
        'PCIe uses the CPU for compute',
        'They are equal',
      ],
      e: 'Inter-GPU communication volume (collectives, activations) is bandwidth-sensitive. NVLink offers an order-of-magnitude more bandwidth than PCIe, so PCIe-only topologies bottleneck communication-heavy training, capping multi-GPU speedup.',
      ref: 'NVLink vs PCIe; multi-GPU scaling',
      tags: ['nvlink', 'pcie', 'bandwidth', 'scaling'],
    },
    {
      d: 4,
      q: 'The "Transformer Engine" is partly hardware (FP8 tensor cores) and partly software because…',
      o: [
        'Hardware alone suffices',
        'The hardware does fast FP8 matmul, but software manages the per-tensor scaling/amax statistics and chooses precision per layer to keep accuracy — both are needed for usable FP8 training',
        'It is pure software',
        'It runs on the CPU',
      ],
      a: 1,
      x: [
        'It is a separate chip',
        'It avoids tensor cores',
        'It only does inference',
      ],
      e: 'FP8’s narrow range needs careful scaling; the hardware provides FP8 tensor cores while the TE software tracks amax and applies scales/precision choices automatically, so models train in FP8 without manual numerical babysitting.',
      ref: 'NVIDIA Transformer Engine',
      tags: ['transformer-engine', 'fp8', 'hardware-software'],
    },
    {
      d: 3,
      q: 'The register file’s large size (256 KB/SM) relative to L1/shared exists because…',
      o: [
        'Registers are cheap',
        'Thousands of resident threads each need private fast storage simultaneously; the large register file is what makes zero-overhead warp switching (keeping all contexts live) possible',
        'It caches global memory',
        'It stores the program',
      ],
      a: 1,
      x: [
        'It replaces shared memory',
        'It is for tensor cores only',
        'It holds constants',
      ],
      e: 'Latency hiding keeps many warps resident, each with its own registers. A big register file holds all those live contexts so the scheduler can switch among warps with no save/restore — the hardware basis of throughput-oriented design.',
      ref: 'NVIDIA SM architecture; register file',
      tags: ['register-file', 'occupancy', 'context'],
    },
    {
      d: 5,
      q: 'Why does TMA multicast specifically reduce HBM reads in a CLUSTER GEMM but not a single-block GEMM?',
      o: [
        'It compresses data',
        'Multicast writes one HBM-loaded tile into MULTIPLE cluster blocks’ shared memory at once; a single block has no peers to share the load with, so there is nothing to multicast to',
        'It is faster on one block',
        'Single blocks can’t use TMA',
      ],
      a: 1,
      x: [
        'It uses the L2',
        'It needs no descriptor',
        'It lowers precision',
      ],
      e: 'When cluster blocks need the same operand tile, multicast amortizes a single HBM read across them via the SM-to-SM network. A lone block (no cluster peers) would still read the tile from HBM itself — multicast’s benefit is inherently cluster-scoped.',
      ref: 'Hopper TMA multicast; cluster GEMM',
      tags: ['tma', 'multicast', 'clusters', 'gemm'],
    },
    {
      d: 4,
      q: 'Across architecture generations, a recurring theme enabling higher tensor throughput is…',
      o: [
        'Higher clocks only',
        'Lower-precision formats (FP16→TF32/BF16→FP8→FP4) plus better operand feeding (async copy → TMA) and larger cooperative MMA units (warp → warpgroup → wider)',
        'More FP64',
        'Bigger registers only',
      ],
      a: 1,
      x: [
        'Smaller warps',
        'Removing tensor cores',
        'Slower memory',
      ],
      e: 'Each generation pushes precision down (more MACs/byte), improves how operands reach the tensor cores (cp.async→TMA), and enlarges the cooperative MMA granularity — collectively driving the headline throughput gains, alongside scaling techniques to keep accuracy.',
      ref: 'NVIDIA architecture evolution',
      tags: ['generations', 'tensor-cores', 'precision', 'trends'],
    },
    {
      d: 3,
      q: 'Why are CUDA cores and tensor cores both present on the same SM rather than choosing one?',
      o: [
        'For redundancy',
        'Real kernels mix general scalar/vector work (CUDA cores: indexing, activations, reductions) with dense matmul (tensor cores); both are needed and they often run cooperatively',
        'Tensor cores are backups',
        'CUDA cores are deprecated',
      ],
      a: 1,
      x: [
        'They never run together',
        'Tensor cores do everything',
        'For graphics only',
      ],
      e: 'A GEMM-heavy kernel still needs CUDA cores for address math, epilogue activations, softmax, and reductions, while tensor cores do the matmul. Co-locating them lets the SM handle the whole computation efficiently with cooperative use.',
      ref: 'NVIDIA SM design; CUDA + tensor cores',
      tags: ['cuda-cores', 'tensor-cores', 'sm'],
    },
    {
      d: 4,
      q: 'MIG partitions an A100/H100 into instances with dedicated SMs, L2 slices, and memory because…',
      o: [
        'It overclocks',
        'Some workloads (inference, multi-tenant) underuse a whole GPU; hard partitioning gives each tenant guaranteed, isolated resources with predictable performance (QoS)',
        'It increases bandwidth',
        'It enables tensor cores',
      ],
      a: 1,
      x: [
        'It shares one context',
        'It compresses memory',
        'It runs on the CPU',
      ],
      e: 'MIG provides hardware isolation (compute, cache, memory) so multiple jobs share a GPU with guaranteed slices and no interference — ideal for serving many small models or multi-tenant clouds where a full GPU per job would waste capacity.',
      ref: 'NVIDIA MIG',
      tags: ['mig', 'partitioning', 'qos', 'isolation'],
    },
    {
      d: 5,
      q: 'The trend toward rack-scale NVLink domains (e.g. NVL72) is driven by…',
      o: [
        'Cheaper GPUs',
        'Very large models needing tensor/expert parallelism across many GPUs with high all-to-all bandwidth; a unified NVLink fabric makes dozens of GPUs behave like one large accelerator',
        'Lower precision',
        'Graphics',
      ],
      a: 1,
      x: [
        'Smaller models',
        'Single-GPU training',
        'Reduced bandwidth',
      ],
      e: 'Trillion-parameter and MoE models communicate heavily (TP/EP all-to-all). Extending the fast NVLink domain to a whole rack (NVL72) provides the bandwidth to span those parallelism dimensions across many GPUs as if one machine.',
      ref: 'NVIDIA GB200 NVL72; rack-scale NVLink',
      tags: ['nvl72', 'nvlink', 'scale', 'parallelism'],
    },
    {
      d: 3,
      q: 'The achievable fraction of peak tensor FLOP/s on a real GEMM is limited mainly by…',
      o: [
        'The ISA',
        'How well the operand-feeding path (shared memory / TMA / registers) keeps the tensor cores fed — memory feeding, not the tensor cores, is usually the practical limit',
        'The number of SMs',
        'The host',
      ],
      a: 1,
      x: [
        'The warp size',
        'The clock only',
        'FP64 units',
      ],
      e: 'Tensor cores can consume operands faster than naive feeding supplies them, so sustained GEMM efficiency hinges on the data path (tiling, async copy/TMA, pipelining, swizzling). Peak FLOPs assume perfect feeding rarely fully achieved.',
      ref: 'GEMM performance; operand feeding',
      tags: ['tensor-cores', 'operand-feeding', 'gemm'],
    },
    {
      d: 4,
      q: 'Why does each successive architecture tend to increase shared-memory capacity per SM (e.g. 48→96→164→228 KB)?',
      o: [
        'To reduce registers',
        'Larger on-chip tiles enable more data reuse and deeper pipelines for the growing tensor-core throughput, keeping the math units fed — capacity scales with compute',
        'For graphics',
        'To replace HBM',
      ],
      a: 1,
      x: [
        'To shrink the L2',
        'For FP64',
        'To lower precision',
      ],
      e: 'As tensor-core throughput rises, kernels need bigger on-chip operand tiles and more pipeline stages to feed it without HBM stalls. Growing shared-memory capacity per SM supports that reuse/pipelining, keeping the compute fed.',
      ref: 'NVIDIA architecture evolution; shared memory',
      tags: ['shared-memory', 'generations', 'reuse'],
    },
  ],
  { idPrefix: 'architecture-d' }
)
