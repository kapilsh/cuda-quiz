import { defineQuestions } from './_helpers.js'

// Multi-GPU & NCCL — collectives, ring/tree algorithms, P2P, NVLink,
// communicators, NCCL semantics.
export default defineQuestions('multigpu', [
  {
    d: 2,
    q: 'What is NCCL?',
    o: [
      'A CUDA debugger',
      'The NVIDIA Collective Communications Library — optimized multi-GPU/multi-node collectives (all-reduce, broadcast, etc.)',
      'A linear algebra library',
      'A graph compiler',
    ],
    a: 1,
    e: 'NCCL provides topology-aware, high-bandwidth implementations of collective operations (all-reduce, reduce, broadcast, all-gather, reduce-scatter) and point-to-point send/recv across GPUs within and across nodes, used by all major DL frameworks.',
    ref: 'NCCL User Guide',
    tags: ['nccl', 'collectives'],
  },
  {
    d: 2,
    q: 'In data-parallel training, which NCCL collective averages gradients across all GPUs?',
    o: ['Broadcast', 'AllReduce', 'Gather', 'Scatter'],
    a: 1,
    e: 'AllReduce sums (then optionally averages) gradient tensors across all ranks and distributes the result back to every GPU, so each replica updates with identical, globally-reduced gradients. It is the core collective of data-parallel training.',
    ref: 'NCCL User Guide; PyTorch DDP',
    tags: ['allreduce', 'data-parallel', 'gradients'],
  },
  {
    d: 3,
    q: 'The ring all-reduce algorithm is bandwidth-optimal because…',
    o: [
      'It sends all data to a single root',
      'Each GPU sends/receives only ~2×(N-1)/N of the data over its links, and total traffic per GPU is independent of the number of GPUs',
      'It uses broadcast internally',
      'It avoids the network entirely',
    ],
    a: 1,
    e: 'Ring all-reduce (reduce-scatter then all-gather around a ring) moves a fixed ~2× the data per GPU regardless of N, fully utilizing each link’s bandwidth. Its latency grows with N (number of steps), which is why trees/hierarchies help at scale.',
    ref: 'Baidu ring all-reduce; NCCL design',
    tags: ['ring-allreduce', 'bandwidth', 'algorithm'],
  },
  {
    d: 3,
    q: 'AllReduce can be decomposed into which two collectives?',
    o: [
      'Broadcast + Gather',
      'ReduceScatter + AllGather',
      'Scatter + Reduce',
      'Send + Recv',
    ],
    a: 1,
    e: 'AllReduce = ReduceScatter (each GPU ends up with the reduced result for a distinct shard) followed by AllGather (every GPU collects all shards). This decomposition is exactly how ring all-reduce achieves bandwidth optimality.',
    ref: 'NCCL documentation; collective algorithms',
    tags: ['allreduce', 'reduce-scatter', 'all-gather'],
  },
  {
    d: 2,
    q: 'What does AllGather do?',
    o: [
      'Sums tensors across GPUs',
      'Each GPU contributes a shard; afterwards every GPU holds the full concatenation of all shards',
      'Sends data to one root',
      'Splits a tensor across GPUs',
    ],
    a: 1,
    e: 'AllGather collects each rank’s piece and gives every rank the concatenated whole (no reduction). It is used, e.g., to reconstruct full parameters from shards in FSDP/ZeRO before a layer’s forward pass.',
    ref: 'NCCL User Guide',
    tags: ['all-gather', 'collectives'],
  },
  {
    d: 3,
    q: 'ReduceScatter differs from Reduce in that…',
    o: [
      'It does no reduction',
      'It reduces across ranks but each rank keeps only a distinct shard of the result (not the whole thing on one root)',
      'It only works on two GPUs',
      'It is the same as Broadcast',
    ],
    a: 1,
    e: 'Reduce produces the full reduced tensor on a single root. ReduceScatter reduces and then distributes — each rank ends with the reduced values for its own slice. It halves the data each rank holds versus AllReduce and is used in ZeRO/FSDP gradient reduction.',
    ref: 'NCCL User Guide',
    tags: ['reduce-scatter', 'collectives', 'zero'],
  },
  {
    d: 3,
    q: 'What is a NCCL "communicator"?',
    o: [
      'A network cable',
      'An object that defines a group of ranks (GPUs) participating in collectives, each with a unique rank id',
      'A CUDA stream',
      'A profiler session',
    ],
    a: 1,
    e: 'A ncclComm_t represents a communicator: the set of GPUs in a collective group, each holding a rank (0..N-1). Collectives are issued per-communicator. You create them with a shared ncclUniqueId and ncclCommInitRank (or ncclCommInitAll).',
    ref: 'NCCL User Guide (communicators)',
    tags: ['nccl', 'communicator', 'rank'],
  },
  {
    d: 4,
    q: 'NCCL collectives are stream-ordered and asynchronous. What does this imply for correctness?',
    o: [
      'You must call cudaDeviceSynchronize after every collective',
      'The collective is enqueued on a CUDA stream and runs ordered with other GPU work on that stream; you synchronize the stream (not necessarily the whole device) to know it completed',
      'They run on the CPU',
      'They block the host by default',
    ],
    a: 1,
    e: 'NCCL ops enqueue onto a CUDA stream and execute asynchronously, ordered with that stream’s kernels. You overlap them with compute and use stream synchronization/events to establish completion — enabling computation/communication overlap.',
    ref: 'NCCL User Guide (streams and operations)',
    tags: ['nccl', 'streams', 'async', 'overlap'],
  },
  {
    d: 4,
    q: 'Why must all ranks call NCCL collectives in the same order with matching arguments?',
    o: [
      'For logging',
      'Collectives are collective operations — every participating rank must launch the matching call; mismatched order/counts/types cause hangs or corruption',
      'It is only a style preference',
      'To reduce memory usage',
    ],
    a: 1,
    e: 'A collective only completes when every rank participates with consistent op, datatype, count, and ordering. A missing or mismatched call (e.g. due to divergent control flow) deadlocks the group — a common multi-GPU debugging pitfall.',
    ref: 'NCCL User Guide; common pitfalls',
    tags: ['nccl', 'collectives', 'deadlock'],
  },
  {
    d: 3,
    q: 'Peer-to-peer (P2P) access between two GPUs enables…',
    o: [
      'GPUs to share the same memory chip',
      'One GPU to directly read/write another GPU’s memory (e.g. over NVLink/PCIe) without staging through host memory',
      'Faster host-to-device copies',
      'Shared L2 cache',
    ],
    a: 1,
    e: 'With P2P (cudaDeviceEnablePeerAccess), a kernel/copy on one GPU can access another GPU’s memory directly across the interconnect, avoiding a bounce through CPU memory. NCCL exploits P2P/NVLink for fast intra-node collectives.',
    ref: 'CUDA C++ Programming Guide (Peer Device Memory Access)',
    tags: ['p2p', 'nvlink', 'multi-gpu'],
  },
  {
    d: 4,
    q: 'For multi-node (across servers) all-reduce, NCCL typically uses which transport?',
    o: [
      'PCIe only',
      'InfiniBand/RoCE via GPUDirect RDMA (and sockets as fallback), often with NVLink/NVSwitch inside each node',
      'NVLink across nodes',
      'USB',
    ],
    a: 1,
    e: 'Within a node NCCL uses NVLink/NVSwitch or PCIe (P2P); across nodes it uses the network (InfiniBand/RoCE), ideally with GPUDirect RDMA so NICs DMA directly to/from GPU memory, bypassing the CPU. Topology detection picks rings/trees accordingly.',
    ref: 'NCCL User Guide; GPUDirect RDMA',
    tags: ['nccl', 'infiniband', 'rdma', 'multi-node'],
  },
  {
    d: 4,
    q: 'NCCL uses tree algorithms (in addition to rings) primarily to…',
    o: [
      'Increase per-link bandwidth',
      'Reduce latency at large scale, since tree depth grows logarithmically with the number of ranks',
      'Avoid using NVLink',
      'Support only small messages',
    ],
    a: 1,
    e: 'Ring latency grows linearly with rank count; trees reduce the number of sequential steps to O(log N), lowering latency for small messages and large node counts. NCCL chooses ring vs. tree (and double-binary trees) based on size and topology.',
    ref: 'NCCL design; "Massively Scale Deep Learning" NCCL blog',
    tags: ['nccl', 'tree-allreduce', 'latency', 'scaling'],
  },
  {
    d: 2,
    q: 'Broadcast in NCCL does what?',
    o: [
      'Sums a tensor across GPUs',
      'Copies a tensor from one root rank to all other ranks',
      'Gathers shards from all ranks',
      'Scatters distinct chunks to each rank',
    ],
    a: 1,
    e: 'Broadcast sends the root rank’s buffer to every other rank so all hold identical data — used, e.g., to distribute initial model weights from rank 0 to all replicas at the start of training.',
    ref: 'NCCL User Guide',
    tags: ['broadcast', 'collectives'],
  },
  {
    d: 5,
    q: 'How does NCCL overlap communication with computation efficiently during backprop in data-parallel training?',
    o: [
      'It waits for all gradients before any communication',
      'Gradients are bucketed and an all-reduce is launched on a separate stream as soon as each bucket is ready, overlapping with ongoing backward compute',
      'It runs all-reduce on the CPU',
      'It disables computation during communication',
    ],
    a: 1,
    e: 'Frameworks (DDP) group gradients into buckets and kick off NCCL all-reduce on a dedicated stream the moment a bucket’s grads are computed, while the backward pass continues on other layers — hiding most communication behind compute.',
    ref: 'PyTorch DDP design; NCCL streams',
    tags: ['overlap', 'ddp', 'gradient-bucketing', 'nccl'],
  },
  {
    d: 3,
    q: 'ncclGroupStart()/ncclGroupEnd() are used to…',
    o: [
      'Create a new GPU group physically',
      'Batch multiple NCCL calls so they are launched together (e.g. aggregating many send/recv to avoid deadlock and improve efficiency)',
      'Synchronize the host',
      'Allocate communicators',
    ],
    a: 1,
    e: 'Group calls fuse several NCCL operations into one launch. This is essential for point-to-point patterns (issuing matching sends/recvs together to avoid deadlock) and improves efficiency by merging many small operations.',
    ref: 'NCCL User Guide (group calls)',
    tags: ['nccl', 'group-calls', 'send-recv'],
  },
  {
    d: 5,
    q: 'In a node with NVSwitch (e.g. DGX H100), why can all-reduce among 8 GPUs approach full NVLink bandwidth regardless of pairing?',
    o: [
      'NVSwitch compresses the data',
      'NVSwitch provides a non-blocking all-to-all fabric so every GPU pair has full bandwidth, making the topology effectively a fully-connected mesh',
      'It uses PCIe instead',
      'It reduces the data size',
    ],
    a: 1,
    e: 'NVSwitch crossbars connect all GPUs with full bisection bandwidth, so any communication pattern (not just neighbors in a ring) gets full NVLink bandwidth. NCCL detects NVSwitch and uses algorithms that exploit this all-to-all connectivity.',
    ref: 'NVIDIA NVSwitch/DGX documentation; NCCL topology',
    tags: ['nvswitch', 'nvlink', 'bandwidth', 'topology'],
  },
])
