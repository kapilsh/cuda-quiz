import { defineQuestions } from './_helpers.js'

// Execution & Scheduling — batch E. Cluster scheduling, occupancy math cases,
// stall remedies, warp specialization, ITS subtleties. Distinct from execution.js/.b/.c/.d.
export default defineQuestions(
  'execution',
  [
    {
      d: 5,
      q: 'A thread-block cluster of size 2 means two blocks are co-scheduled. The cluster size is limited by…',
      o: [
        'The grid size',
        'How many blocks can fit on SMs of one GPC simultaneously (portable max is 8; the hardware/occupancy may allow fewer) — they must be co-resident',
        'The warp size',
        'The register count only',
      ],
      a: 1,
      x: [
        'The block size only',
        'The L2 size',
        'The number of GPUs',
      ],
      e: 'Cluster blocks must be co-resident within a GPC, so the cluster size is bounded by the GPC’s capacity and the kernel’s per-block resource use (occupancy). The portable maximum is 8; query support with the cluster occupancy API.',
      ref: 'CUDA C++ Programming Guide (cluster size limits)',
      tags: ['clusters', 'co-residency', 'size-limit'],
    },
    {
      d: 4,
      q: 'A kernel uses 96 registers/thread and 32 KB shared memory/block with 256-thread blocks on an SM with 65536 registers, 100 KB shared memory, 2048 max threads. The likely occupancy limiter is…',
      o: [
        'Threads',
        'Registers: 65536/(96×256) ≈ 2.67 ⇒ 2 blocks; shared memory allows 3 and threads allow 8, so registers (2 blocks) bind',
        'Shared memory',
        'Max blocks per SM',
      ],
      a: 1,
      x: [
        'The grid size',
        'Warp size',
        'The clock',
      ],
      e: 'Resident blocks = min over limits. Registers: 65536/(96×256)=2.67→2. Shared mem: 100/32=3. Threads: 2048/256=8. The minimum (2, register-limited) binds — so reducing registers would raise occupancy here.',
      ref: 'CUDA Occupancy Calculator',
      tags: ['occupancy', 'registers', 'calculation'],
    },
    {
      d: 4,
      q: 'High "Stall Wait" (fixed-latency dependency) in Nsight Compute is typically addressed by…',
      o: [
        'Reducing memory traffic',
        'Increasing ILP — independent instructions to fill the fixed instruction latencies (e.g. multiple accumulators, unrolling) — since the warp is waiting on short, fixed-latency results',
        'Adding atomics',
        'Lowering occupancy',
      ],
      a: 1,
      x: [
        'Disabling the L2',
        'Using FP64',
        'More __syncthreads',
      ],
      e: 'Stall Wait reflects waiting on fixed-latency math results. More ILP (independent work per thread) covers those latencies, raising throughput even without more warps — the classic remedy for arithmetic-latency-bound inner loops.',
      ref: 'Nsight Compute (Stall Wait); ILP',
      tags: ['stall-wait', 'ilp', 'latency'],
    },
    {
      d: 5,
      q: 'In a producer/consumer warp-specialized kernel, if the producer warpgroup is too SLOW relative to consumers, the symptom is…',
      o: [
        'Consumers idle waiting on the mbarrier for data — the pipeline is producer-bound (memory feeding limits throughput)',
        'Producers idle',
        'A deadlock always',
        'Register spilling',
      ],
      a: 0,
      x: [
        'Higher occupancy',
        'Lower precision',
        'A bank conflict',
      ],
      e: 'If data isn’t produced fast enough, consumer warpgroups stall at the stage barrier waiting for arrivals — the kernel is feeding-bound. Remedies: more/deeper buffers, faster copies (TMA), or rebalancing warp roles to match consumption rate.',
      ref: 'Warp specialization; producer-bound pipelines',
      tags: ['warp-specialization', 'producer-consumer', 'pipeline'],
    },
    {
      d: 4,
      q: 'Under Independent Thread Scheduling, code like `if (laneid<16) { ... } x = __shfl_sync(0xffffffff, x, src);` is buggy because…',
      o: [
        'shfl is slow',
        'The full-warp mask 0xffffffff claims all 32 lanes participate, but only lanes <16 reach the shuffle (the rest are in the else/after) — diverged lanes don’t participate, giving undefined results',
        'laneid is wrong',
        'It uses too many registers',
      ],
      a: 1,
      x: [
        'shfl needs shared memory',
        'It always deadlocks',
        'src is invalid',
      ],
      e: 'The mask must name exactly the lanes that execute the shuffle. If only some lanes reach it (divergence), using the full mask is undefined. Use __activemask() or move the collective to convergent code so all named lanes participate.',
      ref: 'CUDA C++ Programming Guide §B.22; ITS',
      tags: ['shuffle', 'mask', 'divergence', 'its'],
    },
    {
      d: 4,
      q: 'A kernel achieves 100% occupancy but Nsight shows low eligible-warps-per-scheduler and high long-scoreboard stalls. This means…',
      o: [
        'It is compute-bound',
        'Despite full occupancy, nearly all warps are simultaneously stalled on global loads (memory-latency/bandwidth bound) — so occupancy isn’t the fix; reduce/streamline memory traffic',
        'Too few warps',
        'Register spilling',
      ],
      a: 1,
      x: [
        'A barrier issue',
        'A host stall',
        'Tensor-core starvation',
      ],
      e: 'When even maximal occupancy can’t supply eligible warps because they’re all waiting on memory, the kernel is memory-bound (latency or bandwidth). Adding occupancy is impossible/ineffective; cut bytes moved or improve locality/MLP.',
      ref: 'Nsight Compute; memory-bound at full occupancy',
      tags: ['occupancy', 'long-scoreboard', 'memory-bound'],
    },
    {
      d: 3,
      q: 'Cooperative groups’ grid.sync() inside a cooperative-launched kernel lets you…',
      o: [
        'Synchronize a block',
        'Synchronize ALL blocks at a barrier within one kernel launch — enabling multi-phase algorithms (e.g. iterative solvers) without splitting into multiple launches',
        'Synchronize the host',
        'Synchronize a warp',
      ],
      a: 1,
      x: [
        'Synchronize two GPUs',
        'Reduce registers',
        'Disable divergence',
      ],
      e: 'grid.sync() is a device-wide barrier across all (co-resident) blocks, so a single kernel can do phase1 → grid.sync → phase2. It requires cooperative launch sized to fit all blocks; otherwise it would deadlock.',
      ref: 'Cooperative Groups; grid.sync',
      tags: ['grid-sync', 'cooperative-groups', 'phases'],
    },
    {
      d: 5,
      q: 'Why might a cooperative (grid.sync) kernel be SLOWER than two separate kernels for the same two-phase computation?',
      o: [
        'grid.sync is free',
        'Cooperative launch caps the grid to what fits co-resident (limiting parallelism/occupancy), and the grid barrier itself has cost; two launches can use a larger grid and the kernel-boundary barrier may be cheaper',
        'Two launches are always slower',
        'It uses FP64',
      ],
      a: 1,
      x: [
        'grid.sync disables tensor cores',
        'It needs more registers',
        'It avoids memory',
      ],
      e: 'Cooperative launch restricts grid size (all blocks co-resident), which can reduce parallelism vs an unconstrained grid, and the grid barrier has overhead. For many problems, two ordinary launches (using the launch boundary as the barrier) are faster.',
      ref: 'Cooperative groups vs multi-launch trade-offs',
      tags: ['grid-sync', 'cooperative-launch', 'tradeoff'],
    },
    {
      d: 4,
      q: 'The warp-stall reason "No Instructions" indicates…',
      o: [
        'Memory latency',
        'The warp is waiting on instruction fetch (e.g. instruction-cache miss or branch target not yet fetched) — high values suggest instruction-footprint/I-cache pressure',
        'A barrier',
        'A register conflict',
      ],
      a: 1,
      x: [
        'A divergent branch',
        'Tensor-core wait',
        'A host stall',
      ],
      e: '"No Instructions" / fetch stalls mean the front-end hasn’t supplied the next instruction (I-cache miss, branchy code). If significant, reduce code size (less aggressive unrolling) or improve branch locality to relieve instruction-fetch pressure.',
      ref: 'Nsight Compute (No Instructions stall)',
      tags: ['stall-reasons', 'instruction-fetch', 'i-cache'],
    },
    {
      d: 4,
      q: 'A grid with 132 blocks on a 132-SM GPU where each SM holds 2 resident blocks runs in roughly…',
      o: [
        '1 wave',
        'About half a wave (132 blocks but capacity is 264), so SMs are underfilled — increasing blocks toward 264 (or more, in whole waves) improves utilization',
        '2 waves',
        '132 waves',
      ],
      a: 1,
      x: [
        '4 waves',
        '0.25 waves',
        '66 waves',
      ],
      e: 'Capacity = 132 SMs × 2 blocks = 264 resident blocks. Launching only 132 fills half, leaving each SM with one block (lower occupancy/utilization). Sizing the grid to fill whole waves (multiples of 264) avoids this underutilization.',
      ref: 'Wave quantization; grid sizing',
      tags: ['waves', 'occupancy', 'grid-sizing'],
    },
    {
      d: 5,
      q: 'cluster.map_shared_rank(ptr, rank) in a Hopper kernel returns…',
      o: [
        'A global pointer',
        'A pointer to the shared memory of the block with the given rank in the cluster, so the calling block can access that peer block’s DSMEM',
        'The SM id',
        'A register value',
      ],
      a: 1,
      x: [
        'The grid rank',
        'A constant address',
        'A texture handle',
      ],
      e: 'It maps a shared-memory pointer to the corresponding location in another cluster block’s shared memory (by cluster rank), the mechanism for distributed shared memory access — read/write/atomic on a peer block’s scratchpad over the SM-to-SM network.',
      ref: 'CUDA C++ Programming Guide (cluster DSMEM)',
      tags: ['dsmem', 'map-shared-rank', 'clusters'],
    },
    {
      d: 4,
      q: 'When a kernel is "tail-effect" limited, increasing the number of blocks (e.g. via thread coarsening reduction or splitting work) helps because…',
      o: [
        'It reduces registers',
        'More, smaller blocks fill the SMs more evenly and shrink the relative cost of the final partial wave — improving achieved occupancy/utilization for short kernels',
        'It uses tensor cores',
        'It lowers precision',
      ],
      a: 1,
      x: [
        'It avoids global memory',
        'It increases divergence',
        'It pins memory',
      ],
      e: 'The tail (partial final wave) wastes SMs. Providing more blocks (so waves are fuller and the tail is a smaller fraction) raises utilization. Conversely, too few large blocks exaggerate the tail. Aim for several full waves.',
      ref: 'Tail effect; block count tuning',
      tags: ['tail-effect', 'block-count', 'utilization'],
    },
    {
      d: 3,
      q: 'Independent Thread Scheduling guarantees forward progress, which fixed a class of bugs where…',
      o: [
        'Kernels never finish',
        'Pre-Volta, a spinning lane waiting on another lane in the SAME warp could livelock (the winner couldn’t proceed past reconvergence); ITS lets diverged lanes make independent progress',
        'Memory was incoherent',
        'Atomics failed',
      ],
      a: 1,
      x: [
        'Warps were too small',
        'Registers spilled',
        'Occupancy dropped',
      ],
      e: 'Strict lockstep could deadlock intra-warp producer/consumer or lock patterns (the holder couldn’t advance while peers spun). ITS gives each lane its own progress, resolving such livelocks — but you must use explicit sync for warp-collective correctness.',
      ref: 'Volta Independent Thread Scheduling',
      tags: ['its', 'forward-progress', 'volta'],
    },
    {
      d: 4,
      q: 'A warp executing __syncwarp(0x0000FFFF) synchronizes…',
      o: [
        'All 32 lanes',
        'Only the lower 16 lanes (those set in the mask) — useful when only a subset is cooperating and the others have diverged away',
        'The whole block',
        'No lanes',
      ],
      a: 1,
      x: [
        'The upper 16 lanes',
        'Two warps',
        'The grid',
      ],
      e: '__syncwarp takes a mask of participating lanes; 0x0000FFFF names lanes 0–15. All named lanes must reach it. This lets a subset reconverge/synchronize when the warp is intentionally partitioned, under ITS.',
      ref: 'CUDA C++ Programming Guide §B.6 (__syncwarp mask)',
      tags: ['syncwarp', 'mask', 'partial-warp'],
    },
    {
      d: 5,
      q: 'Why does a deeply warp-specialized GEMM often dedicate FEWER warps to producers (TMA) than consumers (wgmma)?',
      o: [
        'Producers need more registers',
        'TMA bulk copies are issued by a single thread and are hardware-driven, so one (or few) producer warp(s) can feed many consumer warps doing the math — balancing the pipeline',
        'Consumers are slower to launch',
        'Producers use tensor cores',
      ],
      a: 1,
      x: [
        'It reduces occupancy',
        'Producers do the math',
        'It avoids barriers',
      ],
      e: 'Because TMA offloads address generation/copy to hardware (one thread triggers a tile copy), a small number of producer warps can keep many consumer (wgmma) warps fed. The split is tuned so production matches consumption without over-allocating producers.',
      ref: 'CUTLASS Hopper warp specialization; TMA',
      tags: ['warp-specialization', 'tma', 'wgmma', 'balance'],
    },
    {
      d: 4,
      q: 'A kernel’s achieved occupancy oscillates run-to-run for a SHORT kernel because…',
      o: [
        'Registers change',
        'With few blocks and brief runtime, scheduling/tail variability dominates the average active-warps measurement — short kernels have noisy achieved occupancy',
        'The grid changes',
        'ECC toggles',
      ],
      a: 1,
      x: [
        'Occupancy is random',
        'The clock is fixed',
        'It is a profiler bug',
      ],
      e: 'For tiny/short kernels, the SMs spend much of the run ramping/draining with few blocks, so measured average occupancy is dominated by transient scheduling and varies. Longer/larger kernels give stable, meaningful occupancy.',
      ref: 'Achieved occupancy; short-kernel variance',
      tags: ['achieved-occupancy', 'short-kernel', 'variance'],
    },
    {
      d: 3,
      q: 'Why is it generally fine for DIFFERENT warps in a block to be on different instructions, but NOT different lanes of one warp (pre-Volta)?',
      o: [
        'Warps share a PC',
        'Warps are independent scheduling units (no shared PC), so they naturally diverge for free; lanes of one warp shared a PC under classic SIMT, so intra-warp divergence serialized paths',
        'Lanes are independent',
        'Blocks share a PC',
      ],
      a: 1,
      x: [
        'Warps must match',
        'It is always free',
        'Divergence is illegal',
      ],
      e: 'Inter-warp "divergence" is just normal independent scheduling (no penalty). Intra-warp divergence (classic SIMT) serialized the warp’s paths because all 32 lanes shared one PC. Volta gave lanes independent PCs but the divergence cost remains.',
      ref: 'SIMT; warp vs lane divergence',
      tags: ['divergence', 'warp', 'simt'],
    },
    {
      d: 4,
      q: 'Cooperative groups cg::this_grid().sync() in an iterative stencil kernel replaces…',
      o: [
        '__syncthreads',
        'The need to launch a separate kernel per iteration — keeping all iterations in one launch with a grid barrier between them (if all blocks fit co-resident)',
        'Atomics',
        'cudaMemcpy',
      ],
      a: 1,
      x: [
        'Shared memory',
        'The optimizer',
        'NCCL',
      ],
      e: 'Iterative methods that need a global sync each step traditionally relaunch the kernel per iteration. With cooperative launch, grid.sync() provides the cross-block barrier in-kernel — avoiding relaunch overhead, but constrained by co-residency limits.',
      ref: 'Cooperative groups; iterative kernels',
      tags: ['grid-sync', 'iterative', 'stencil'],
    },
    {
      d: 5,
      q: 'A "persistent" reduction kernel (one wave of blocks looping over the input via atomics/work queue) can beat a multi-launch tree reduction when…',
      o: [
        'Never',
        'The input is moderate and launch overhead / multiple passes dominate — keeping blocks resident and combining via shared/global avoids relaunch overhead and extra memory passes',
        'The input is huge',
        'It uses FP64',
      ],
      a: 1,
      x: [
        'It needs more occupancy',
        'It avoids atomics',
        'It uses tensor cores',
      ],
      e: 'For inputs where per-launch overhead and extra global passes matter, a persistent single-launch reduction (resident blocks streaming the data and combining with limited atomics/shared reduction) can outperform a multi-kernel tree reduction.',
      ref: 'Persistent kernels; reduction strategies',
      tags: ['persistent-kernel', 'reduction', 'launch-overhead'],
    },
    {
      d: 4,
      q: 'On Nsight Compute, "Issued Warp Per Scheduler" near the theoretical max with low achieved throughput suggests…',
      o: [
        'Memory-bound',
        'The SM is issuing well but the work itself is low-value (e.g. overhead instructions, poor vectorization) — reduce instruction count / increase useful work per instruction',
        'Latency-bound',
        'Too few warps',
      ],
      a: 1,
      x: [
        'A barrier issue',
        'Register spilling',
        'A host stall',
      ],
      e: 'High issue rate but low useful throughput means the instructions accomplish little per issue (overhead, scalar where vector would do). Vectorize, remove redundant instructions, or use higher-throughput ops to raise work per issued instruction.',
      ref: 'Nsight Compute; issue-bound analysis',
      tags: ['issue-rate', 'instruction-efficiency', 'analysis'],
    },
    {
      d: 4,
      q: 'cudaFuncSetAttribute(kernel, cudaFuncAttributeNonPortableClusterSizeAllowed, 1) is needed when…',
      o: [
        'Using any cluster',
        'You request a cluster size larger than the portable maximum (8); you opt in to a hardware-specific larger cluster, accepting reduced portability',
        'Using shared memory',
        'Using tensor cores',
      ],
      a: 1,
      x: [
        'Lowering precision',
        'Using streams',
        'Reducing registers',
      ],
      e: 'Cluster sizes up to 8 are portable; larger sizes (where the hardware allows) require explicitly opting in via this attribute, signaling the code may not run on devices that don’t support that size. It trades portability for a bigger cluster.',
      ref: 'CUDA C++ Programming Guide (non-portable cluster size)',
      tags: ['clusters', 'portability', 'attribute'],
    },
    {
      d: 3,
      q: 'A kernel with a data-dependent `while` loop per thread shows high warp-execution-efficiency early but low efficiency later because…',
      o: [
        'It speeds up',
        'As threads finish their loops at different times, fewer lanes remain active in each warp, so the warp runs with idle lanes until its slowest thread completes (intra-warp imbalance)',
        'It uses atomics',
        'Registers spill',
      ],
      a: 1,
      x: [
        'It is memory-bound',
        'It deadlocks',
        'It uses tensor cores',
      ],
      e: 'Per-thread variable trip counts cause progressive divergence: once some lanes exit, the warp executes with masked-off (idle) lanes until all finish. Load balancing / work redistribution (e.g. persistent threads pulling new work) mitigates the tail.',
      ref: 'Divergence; data-dependent loops',
      tags: ['divergence', 'load-imbalance', 'loops'],
    },
    {
      d: 5,
      q: 'In a multi-stage GEMM with a producer warpgroup and TWO consumer warpgroups alternating (ping-pong), the goal is to…',
      o: [
        'Use one warp',
        'Keep the tensor cores continuously busy: while one consumer runs wgmma on stage k, the other prepares/consumes stage k+1, with the producer (TMA) staying ahead — hiding all latencies',
        'Lower precision',
        'Avoid shared memory',
      ],
      a: 1,
      x: [
        'Reduce occupancy',
        'Use the CPU',
        'Disable tensor cores',
      ],
      e: 'Ping-pong scheduling overlaps two consumer warpgroups so one always has tensor-core work while the other handles stage transitions, with the TMA producer prefetching — maximizing sustained tensor-core utilization in Hopper GEMM.',
      ref: 'CUTLASS Hopper ping-pong schedule',
      tags: ['pingpong', 'warp-specialization', 'gemm', 'hopper'],
    },
    {
      d: 4,
      q: 'Why does the occupancy calculator account for register ALLOCATION GRANULARITY rather than just dividing register file by usage?',
      o: [
        'For simplicity',
        'Registers are allocated to warps in fixed-size chunks, so usage rounds UP to the granularity — meaning a few extra registers can reduce resident warps more than a naive division suggests',
        'Registers are unlimited',
        'It ignores registers',
      ],
      a: 1,
      x: [
        'Granularity is per block',
        'It uses shared memory',
        'It is per grid',
      ],
      e: 'Because per-warp register allocation rounds up to a granularity, effective per-warp register cost can exceed the raw count, and crossing a granularity boundary drops occupancy in steps (the cliff). The calculator models this rounding.',
      ref: 'CUDA Occupancy Calculator; register granularity',
      tags: ['occupancy', 'register-granularity', 'cliff'],
    },
    {
      d: 3,
      q: 'A kernel that is purely INSTRUCTION-bound (compute pipe saturated, memory idle) is best sped up by…',
      o: [
        'More memory accesses',
        'Reducing the number/cost of instructions: vectorize/pack (half2/dp4a), use FMA/tensor cores, eliminate redundant work, or use cheaper math',
        'More occupancy',
        'Larger grids',
      ],
      a: 1,
      x: [
        'Disabling the L2',
        'Using FP64',
        'More __syncthreads',
      ],
      e: 'When the math pipe is the limit, do more work per instruction (vector/packed ops, tensor cores) and cut overhead/redundant instructions. Occupancy/memory tuning won’t help a compute-pipe-saturated kernel.',
      ref: 'Compute-bound optimization',
      tags: ['compute-bound', 'instruction-count', 'optimization'],
    },
    {
      d: 4,
      q: 'The reason elect.sync (or invoke_one) is preferred over `if (threadIdx.x % 32 == 0)` for a warp leader under ITS is…',
      o: [
        'It is the same',
        'It elects a leader among the ACTIVE lanes (handling divergence correctly) and returns a predicate, rather than assuming lane 0 is active — safer when the warp may be partially diverged',
        'It is faster only',
        'It uses shared memory',
      ],
      a: 1,
      x: [
        'It synchronizes the block',
        'It avoids registers',
        'It needs cooperative launch',
      ],
      e: 'Hard-coding lane 0 fails if lane 0 isn’t active (divergence) or for the currently-converged subset. elect.sync/invoke_one picks a leader from the active mask and signals it, robustly handling partial participation under independent thread scheduling.',
      ref: 'PTX elect.sync; cooperative groups invoke_one',
      tags: ['elect', 'leader', 'its'],
    },
    {
      d: 5,
      q: 'Why can adding a cluster (size 2) to a GEMM improve performance even though each block already uses shared-memory tiling?',
      o: [
        'It reduces registers',
        'The cluster enables DSMEM + TMA multicast so the two blocks SHARE operand tiles (one HBM read serves both) and can synchronize, cutting redundant DRAM traffic and improving reuse',
        'It lowers precision',
        'It avoids tensor cores',
      ],
      a: 1,
      x: [
        'It increases divergence',
        'It uses the CPU',
        'It disables the L2',
      ],
      e: 'Within a cluster, blocks can share tiles via DSMEM/TMA multicast (fetch from HBM once, distribute on-chip) and coordinate, raising effective reuse beyond a single block’s shared memory — a Hopper-specific GEMM optimization.',
      ref: 'Hopper cluster GEMM; DSMEM/TMA multicast',
      tags: ['clusters', 'dsmem', 'gemm', 'reuse'],
    },
    {
      d: 3,
      q: 'For a kernel limited by __syncthreads frequency (high barrier stalls), a structural fix is…',
      o: [
        'Add more barriers',
        'Reduce the number of barriers (e.g. double-buffer so the next tile loads without an extra sync, or do more work between barriers), and balance per-thread work before each barrier',
        'Use FP64',
        'Lower occupancy',
      ],
      a: 1,
      x: [
        'Disable the L2',
        'Use constant memory',
        'Add atomics',
      ],
      e: 'Frequent barriers serialize the block on its slowest warp. Cutting unnecessary syncs (double buffering, coarser phases) and balancing work before each barrier reduces barrier-stall time — a common tiled-kernel optimization.',
      ref: 'Nsight Compute (barrier stalls); double buffering',
      tags: ['barrier-stall', 'syncthreads', 'optimization'],
    },
    {
      d: 4,
      q: 'A warp reduction using cg::reduce on Ampere+ may be faster than a manual shuffle tree because it…',
      o: [
        'Uses global memory',
        'Can lower to the redux.sync hardware reduction instruction for integer ops (one instruction) instead of log2(32) shuffle steps',
        'Uses shared memory',
        'Runs on the host',
      ],
      a: 1,
      x: [
        'Avoids registers',
        'Is less accurate',
        'Needs a barrier',
      ],
      e: 'cg::reduce picks the best implementation: on Ampere+ integer reductions use redux.sync (hardware, single instruction), beating a 5-step shfl_xor tree. For FP it falls back to shuffles. Portability + optimal lowering is the benefit.',
      ref: 'Cooperative groups cg::reduce; redux.sync',
      tags: ['cg-reduce', 'redux', 'warp-reduction'],
    },
  ],
  { idPrefix: 'execution-e' }
)
