import { defineQuestions } from './_helpers.js'

// Streams, Events & Graphs — batch C. MPS/MIG/green contexts, stream mem ops,
// PDL, manual graph construction, fork/join capture. Distinct from streams.js/.b.js.
export default defineQuestions(
  'streams',
  [
    {
      d: 4,
      q: 'CUDA MPS (Multi-Process Service) allows…',
      o: [
        'One process to use many GPUs',
        'Many processes share one GPU concurrently via one context',
        'Faster kernels',
        'More memory',
      ],
      a: 1,
      x: [
        'Partitioning into isolated instances',
        'GPU-to-GPU copies',
        'Lower precision',
      ],
      e: 'MPS lets several processes’ kernels run concurrently on one GPU (sharing a context), avoiding the serialization of default time-slicing. It boosts utilization when individual processes underuse the GPU (e.g. many small inference jobs).',
      ref: 'NVIDIA MPS documentation',
      tags: ['mps', 'multi-process', 'concurrency'],
    },
    {
      d: 4,
      q: 'How do MPS, MIG, and time-slicing differ for sharing a GPU?',
      o: [
        'They are identical',
        'Time-sliced=serial; MPS=shared; MIG=hard partitions',
        'MIG is software-only',
        'MPS gives memory isolation',
      ],
      a: 1,
      x: [
        'Time-slicing is fastest',
        'MIG shares one context',
        'MPS partitions memory',
      ],
      e: 'Time-slicing context-switches (one at a time). MPS spatially shares with concurrent kernels but no strong isolation/QoS. MIG physically partitions SMs/L2/memory into isolated instances with guaranteed resources — strongest isolation, less flexible.',
      ref: 'NVIDIA MPS/MIG documentation',
      tags: ['mps', 'mig', 'time-slicing', 'sharing'],
    },
    {
      d: 5,
      q: 'Programmatic Dependent Launch (PDL) on Hopper lets a kernel…',
      o: [
        'Launch from the host faster',
        'Overlap its prologue with the predecessor’s tail',
        'Run on two GPUs',
        'Skip synchronization',
      ],
      a: 1,
      x: [
        'Replace streams',
        'Disable the scheduler',
        'Compress kernels',
      ],
      e: 'PDL (cudaGridDependencySynchronize / trigger) allows a dependent kernel to start preamble work (e.g. loading data) before the prior kernel’s grid completes, and to release the next early — shaving inter-kernel gaps in back-to-back launches.',
      ref: 'CUDA C++ Programming Guide (Programmatic Dependent Launch)',
      tags: ['pdl', 'hopper', 'overlap', 'launch'],
    },
    {
      d: 4,
      q: 'Stream memory operations cuStreamWriteValue32 / cuStreamWaitValue32 let you…',
      o: [
        'Allocate memory',
        'Memory write/wait as a GPU stream op',
        'Launch kernels',
        'Profile streams',
      ],
      a: 1,
      x: [
        'Compress data',
        'Set priorities',
        'Bind devices',
      ],
      e: 'These ops insert a value-write or a spin-wait-on-value into the stream, so the GPU itself can signal/await flags (e.g. coordinate with RDMA/GPUDirect or other queues) entirely device-side — used in advanced comm/IO pipelines.',
      ref: 'CUDA Driver API (stream memory operations)',
      tags: ['stream-memops', 'synchronization', 'gpu-side'],
    },
    {
      d: 4,
      q: 'You can build a CUDA Graph manually (without capture) by…',
      o: [
        'Recompiling the kernels',
        'Add nodes and edges explicitly, then instantiate',
        'Calling cudaDeviceSynchronize',
        'Using printf',
      ],
      a: 1,
      x: [
        'Writing PTX',
        'Using NVTX',
        'Setting the block size',
      ],
      e: 'Besides stream capture, you can construct a graph node-by-node, passing each node’s dependencies, giving precise control over the DAG. Manual construction suits statically-known graphs; capture is easier for existing stream code.',
      ref: 'CUDA C++ Programming Guide (explicit graph construction)',
      tags: ['cuda-graphs', 'manual-construction', 'nodes'],
    },
    {
      d: 3,
      q: 'To capture work issued across MULTIPLE streams into ONE graph (fork/join), you…',
      o: [
        'Cannot — capture is single-stream',
        'Capture on an origin stream and fork to others via events',
        'Use cudaDeviceSynchronize between streams',
        'Run them sequentially',
      ],
      a: 1,
      x: [
        'Use the default stream only',
        'Disable events',
        'Use MPS',
      ],
      e: 'Stream capture supports fork/join: from the captured (origin) stream you branch into side streams (linked by events) and rejoin; the resulting graph encodes the parallel structure, which then replays with all its concurrency.',
      ref: 'CUDA C++ Programming Guide (multi-stream capture)',
      tags: ['cuda-graphs', 'capture', 'fork-join'],
    },
    {
      d: 4,
      q: 'cudaGraphUpload (uploading an instantiated graph to a stream before launch) is useful because…',
      o: [
        'It runs the graph',
        'Pre-stages graph on device; faster first launch',
        'It compiles the kernels',
        'It frees the graph',
      ],
      a: 1,
      x: [
        'It captures the graph',
        'It profiles the graph',
        'It updates parameters',
      ],
      e: 'Uploading transfers the graph’s scheduling info to the device ahead of time, so the initial launch is faster — helpful for latency-sensitive first iterations after instantiation or update.',
      ref: 'CUDA Runtime API (cudaGraphUpload)',
      tags: ['cuda-graphs', 'upload', 'latency'],
    },
    {
      d: 3,
      q: 'A clean fork/join with events: stream B and C both depend on a kernel in stream A. You implement it by…',
      o: [
        'cudaDeviceSynchronize after A',
        'Event in A; B and C wait, then run in parallel',
        'Putting everything in one stream',
        'Using MPS',
      ],
      a: 1,
      x: [
        'Using constant memory',
        'A spinlock',
        'A host callback',
      ],
      e: 'Event-based dependencies express a DAG without a full device sync: A signals an event; B and C wait on it and then run in parallel. This keeps the host free and lets independent successors overlap.',
      ref: 'CUDA events; fork/join with cudaStreamWaitEvent',
      tags: ['events', 'fork-join', 'streams'],
    },
    {
      d: 5,
      q: 'Hopper "green contexts" provide…',
      o: [
        'Power savings only',
        'SM reservation per context within a process',
        'A new precision',
        'Faster NVLink',
      ],
      a: 1,
      x: [
        'Memory compression',
        'A profiler',
        'A data loader',
      ],
      e: 'Green contexts let an application reserve a subset of SMs for a context, enabling spatial partitioning/QoS without MIG’s hard isolation — useful for co-locating latency-critical and throughput work within one process.',
      ref: 'CUDA green contexts (Hopper+)',
      tags: ['green-contexts', 'sm-partitioning', 'qos'],
    },
    {
      d: 4,
      q: 'cudaGraphClone is handy when you want to…',
      o: [
        'Run a graph twice',
        'Copy a graph for independent modification',
        'Profile the graph',
        'Free the graph',
      ],
      a: 1,
      x: [
        'Capture a new graph',
        'Upload the graph',
        'Compress nodes',
      ],
      e: 'Cloning duplicates a graph’s topology so you can tweak the copy (different params/nodes) and instantiate it independently of the original — useful when generating many related graph variants.',
      ref: 'CUDA Runtime API (cudaGraphClone)',
      tags: ['cuda-graphs', 'clone', 'templates'],
    },
    {
      d: 3,
      q: 'cudaLaunchKernel (the API form) vs the <<<>>> syntax: cudaLaunchKernel…',
      o: [
        'Runs on the host',
        'Explicit launch: func-ptr, dims, args, stream',
        'Is slower',
        'Cannot use streams',
      ],
      a: 1,
      x: [
        'Requires the driver API',
        'Is deprecated',
        'Disables async',
      ],
      e: '<<<>>> is syntactic sugar over cudaLaunchKernel. The explicit API (args as a void* array) is handy for wrappers/launch helpers and is what tools/frameworks use when the kernel/args are determined generically.',
      ref: 'CUDA Runtime API (cudaLaunchKernel)',
      tags: ['cudaLaunchKernel', 'launch', 'api'],
    },
    {
      d: 4,
      q: 'A child-graph node lets a CUDA Graph…',
      o: [
        'Launch on another GPU',
        'Nest a sub-graph as one node for reuse',
        'Run on the host',
        'Skip dependencies',
      ],
      a: 1,
      x: [
        'Compress the graph',
        'Profile sub-graphs',
        'Disable capture',
      ],
      e: 'Child-graph nodes nest a graph inside another, so you can build modular, reusable sub-graphs (e.g. a per-layer graph) and compose them — improving structure and reuse of complex graphs.',
      ref: 'CUDA C++ Programming Guide (child graph nodes)',
      tags: ['cuda-graphs', 'child-graph', 'composition'],
    },
    {
      d: 3,
      q: 'cudaGraphDebugDotPrint outputs…',
      o: [
        'A performance report',
        'Graphviz DOT file of the graph topology',
        'The SASS',
        'A memory dump',
      ],
      a: 1,
      x: [
        'A flame graph',
        'A timeline',
        'The kernel source',
      ],
      e: 'It emits a DOT file you can render to see the graph topology (nodes, edges, types), invaluable for understanding/debugging complex captured or constructed graphs.',
      ref: 'CUDA Runtime API (cudaGraphDebugDotPrint)',
      tags: ['cuda-graphs', 'debug', 'visualization'],
    },
    {
      d: 5,
      q: 'Conditional graph nodes use a cudaGraphConditionalHandle that is set…',
      o: [
        'At instantiation only',
        'A device kernel at runtime, to drive the if/while node',
        'By the host before launch only',
        'Randomly',
      ],
      a: 1,
      x: [
        'By the compiler',
        'By the profiler',
        'Never',
      ],
      e: 'A kernel sets the conditional handle’s value during graph execution; the conditional (if/while) node then branches/loops accordingly, letting graphs express data-dependent control flow that’s decided on the GPU during replay.',
      ref: 'CUDA C++ Programming Guide (conditional nodes)',
      tags: ['cuda-graphs', 'conditional', 'control-flow'],
    },
    {
      d: 4,
      q: 'For a training loop, the recommended way to use graphs across iterations with changing input pointers is to…',
      o: [
        'Re-capture every iteration',
        'Capture once; update params only, not the structure',
        'Avoid graphs',
        'Use a new stream each time',
      ],
      a: 1,
      x: [
        'Re-instantiate every step',
        'Disable capture mode',
        'Use the default stream',
      ],
      e: 'Re-capturing/instantiating each step would erase the benefit. Capture once and either keep buffers fixed or update node params (cudaGraphExecUpdate) so each iteration is a cheap launch — the standard graphs-in-training pattern.',
      ref: 'CUDA Graphs in training; graph update',
      tags: ['cuda-graphs', 'training', 'update'],
    },
    {
      d: 3,
      q: 'Why does MPS particularly help INFERENCE serving of many small models/requests?',
      o: [
        'It increases precision',
        'MPS co-executes small requests to fill the GPU',
        'It shards the model',
        'It reduces memory',
      ],
      a: 1,
      x: [
        'It compresses weights',
        'It partitions memory',
        'It locks clocks',
      ],
      e: 'Small inference kernels leave SMs idle; default time-slicing serializes processes. MPS lets multiple clients’ kernels co-execute, filling the GPU and improving aggregate throughput/utilization for many concurrent small requests.',
      ref: 'NVIDIA MPS; inference serving',
      tags: ['mps', 'inference', 'utilization'],
    },
    {
      d: 4,
      q: 'A subtle requirement when using CUDA Graphs with cudaMallocAsync inside the captured region is that…',
      o: [
        'It is forbidden',
        'Stream-ordered; captured as graph memory nodes',
        'It runs on the host',
        'It disables the pool',
      ],
      a: 1,
      x: [
        'It needs pinned memory',
        'It requires MPS',
        'It must use the default stream',
      ],
      e: 'Stream-ordered allocations integrate with graphs as memory nodes, so they can live inside captured/replayed iterations; plain cudaMalloc/cudaFree synchronize and would violate capture. This is a key reason to use the async allocator with graphs.',
      ref: 'CUDA Graphs + stream-ordered allocator',
      tags: ['cuda-graphs', 'cudaMallocAsync', 'capture'],
    },
    {
      d: 2,
      q: 'Two independent small kernels on two streams don’t overlap on a busy GPU. The most likely reason is…',
      o: [
        'Streams never overlap',
        'SMs saturated; no spare capacity for concurrency',
        'They are too large',
        'Events block them',
      ],
      a: 1,
      x: [
        'They use the same registers',
        'Constant memory conflict',
        'They are on the host',
      ],
      e: 'Concurrency requires idle resources. If the SMs are saturated (by these or other kernels), there’s nothing to overlap into. Stream concurrency helps when kernels individually underutilize the GPU.',
      ref: 'CUDA concurrency; resource availability',
      tags: ['concurrency', 'streams', 'occupancy'],
    },
    {
      d: 4,
      q: 'For latency-critical inference, combining CUDA Graphs with a high-priority stream addresses…',
      o: [
        'Memory capacity',
        'Launch cost (via graph) + SM priority',
        'Precision',
        'Bank conflicts',
      ],
      a: 1,
      x: [
        'Bandwidth only',
        'Register pressure',
        'Divergence',
      ],
      e: 'Graphs collapse many small launches into one replay (cutting CPU overhead), and a high-priority stream lets the latency-critical graph win SM scheduling over throughput work — together tightening tail latency in serving.',
      ref: 'CUDA Graphs + stream priorities; inference latency',
      tags: ['cuda-graphs', 'priority', 'inference', 'latency'],
    },
    {
      d: 3,
      q: 'cudaEventRecord on a stream, then cudaStreamWaitEvent on another stream, with cudaEventDisableTiming events, is the idiomatic way to…',
      o: [
        'Time the kernel',
        'Cross-stream ordering without timing cost',
        'Allocate memory',
        'Launch a graph',
      ],
      a: 1,
      x: [
        'Synchronize the host',
        'Reset the device',
        'Pin memory',
      ],
      e: 'For pure ordering between streams, timing-disabled events are cheaper. Record on the producer stream and wait on it from the consumer stream to enforce "B after A’s event" without blocking the host or measuring time.',
      ref: 'CUDA events (disable timing); cross-stream sync',
      tags: ['events', 'cross-stream', 'ordering'],
    },
    {
      d: 5,
      q: 'Programmatic Dependent Launch most benefits a chain of kernels where…',
      o: [
        'Each kernel is huge',
        'Short back-to-back kernels where launch gaps matter',
        'There is only one kernel',
        'They run on different GPUs',
      ],
      a: 1,
      x: [
        'They are memory-bound only',
        'They use FP64',
        'They never depend on each other',
      ],
      e: 'PDL overlaps a kernel’s tail with the next kernel’s prologue. The win is largest for sequences of short kernels where launch/drain/ramp gaps are significant relative to execution — similar in spirit to why graphs help there.',
      ref: 'CUDA PDL; back-to-back kernel chains',
      tags: ['pdl', 'kernel-chain', 'overlap'],
    },
    {
      d: 3,
      q: 'cudaGraphInstantiate is relatively expensive, so the performance model of graphs is…',
      o: [
        'Cheap to build, cheap to run',
        'High upfront cost; amortized over many cheap replays',
        'Expensive every launch',
        'Free everywhere',
      ],
      a: 1,
      x: [
        'Cheaper than one kernel',
        'Only for one launch',
        'Independent of launch count',
      ],
      e: 'Graphs trade an upfront capture/instantiate cost for very cheap relaunches. They pay off when the same (or update-able) graph runs many times (training steps, inference requests); for one-off work the overhead isn’t worth it.',
      ref: 'CUDA Graphs performance model',
      tags: ['cuda-graphs', 'amortization', 'instantiate'],
    },
    {
      d: 4,
      q: 'When capturing a region into a graph, calling cudaStreamSynchronize or cudaDeviceSynchronize inside it will…',
      o: [
        'Be recorded as a node',
        'Error: sync calls are illegal during capture',
        'Speed up capture',
        'Be ignored silently',
      ],
      a: 1,
      x: [
        'Split the graph',
        'Create a child graph',
        'Lower precision',
      ],
      e: 'Synchronizing calls break the asynchronous recording model and are disallowed during capture (the capture mode governs how strictly this is enforced). You express dependencies via events/stream ordering, not host syncs, inside a captured region.',
      ref: 'CUDA C++ Programming Guide (capture restrictions)',
      tags: ['cuda-graphs', 'capture', 'restrictions'],
    },
    {
      d: 4,
      q: 'A high-priority stream does NOT preempt an already-running block; instead it…',
      o: [
        'Stops other kernels immediately',
        'Biases block dispatch; doesn’t preempt running blocks',
        'Runs on a separate GPU',
        'Doubles the clock',
      ],
      a: 1,
      x: [
        'Pins the kernel to an SM',
        'Disables other streams',
        'Increases occupancy',
      ],
      e: 'Stream priority biases dispatch of pending blocks; running blocks generally finish (block-level scheduling). So a high-priority kernel gets its blocks scheduled sooner as SMs free up, but doesn’t instantly evict in-flight work.',
      ref: 'CUDA stream priorities (semantics)',
      tags: ['stream-priority', 'scheduling', 'preemption'],
    },
    {
      d: 3,
      q: 'To pipeline disjoint chunks of work across N streams, a common host-side pattern is…',
      o: [
        'One giant kernel',
        'Rotate H2D/kernel/D2H across N pinned-memory streams',
        'cudaDeviceSynchronize per chunk',
        'A single default stream',
      ],
      a: 1,
      x: [
        'Constant memory staging',
        'MPS',
        'A spinlock',
      ],
      e: 'Round-robining chunks over a small pool of streams (with pinned memory and async copies) lets the copy of one chunk overlap the compute of another, approximating a software pipeline that hides transfer time behind compute.',
      ref: 'CUDA Best Practices Guide §9.1; streamed pipelining',
      tags: ['streams', 'pipelining', 'overlap'],
    },
    {
      d: 5,
      q: 'Stream-ordered memory pools (cudaMemPool) help long-running services because…',
      o: [
        'They never free memory',
        'Reuses freed blocks in the pool; no driver call',
        'They run on the host',
        'They compress memory',
      ],
      a: 1,
      x: [
        'They double capacity',
        'They pin memory',
        'They disable the L2',
      ],
      e: 'A memory pool caches freed blocks for fast reuse (stream-ordered), avoiding repeated expensive driver alloc/free and curbing fragmentation in services that allocate/free constantly. You can set release thresholds to control footprint.',
      ref: 'CUDA stream-ordered allocator (memory pools)',
      tags: ['memory-pool', 'cudaMallocAsync', 'services'],
    },
    {
      d: 4,
      q: 'Why can adding MORE streams eventually REDUCE performance?',
      o: [
        'Streams are free',
        'Past max concurrency, extra streams add overhead',
        'Streams disable kernels',
        'It always helps',
      ],
      a: 1,
      x: [
        'They increase precision',
        'They pin memory',
        'They reduce occupancy to zero',
      ],
      e: 'Concurrency is bounded by available SM resources and copy engines. Past that, more streams just add overhead and contention with no extra overlap. Size the stream pool to the achievable concurrency, not arbitrarily large.',
      ref: 'CUDA streams; diminishing returns',
      tags: ['streams', 'overhead', 'concurrency'],
    },
  ],
  { idPrefix: 'streams-c' }
)
