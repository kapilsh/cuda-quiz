import { defineQuestions } from './_helpers.js'

// Execution & Scheduling — warps, SIMT, divergence, occupancy, the SM,
// latency hiding, scheduling.
export default defineQuestions('execution', [
  {
    d: 1,
    q: 'What is a warp?',
    o: [
      'A block of 1024 threads',
      'A group of 32 threads that execute instructions together in lockstep',
      'The unit of work assigned to one SM',
      'A synchronization primitive',
    ],
    a: 1,
    e: 'A warp is 32 threads scheduled and executed together under SIMT. The warp is the fundamental unit of execution; instruction issue, divergence, and scheduling all happen at warp granularity.',
    ref: 'CUDA C++ Programming Guide §4.1; PMPP Ch. 4',
    tags: ['warp', 'simt'],
  },
  {
    d: 2,
    q: 'What is "warp divergence"?',
    o: [
      'When warps from different blocks run on the same SM',
      'When threads in a warp take different branches, forcing the paths to be executed serially',
      'When a warp accesses memory out of bounds',
      'When the warp scheduler stalls on a memory load',
    ],
    a: 1,
    e: 'If threads within a warp follow different control-flow paths (e.g. an if/else split by threadIdx), the hardware executes each taken path with the non-participating threads masked off, serializing the branches and reducing efficiency.',
    ref: 'CUDA C++ Programming Guide §4.2; PMPP Ch. 4',
    tags: ['divergence', 'control-flow'],
  },
  {
    d: 2,
    q: 'Which branch condition is LEAST likely to cause warp divergence?',
    o: [
      'if (threadIdx.x % 2 == 0)',
      'if ((threadIdx.x / 32) % 2 == 0)',
      'if (data[threadIdx.x] > 0) with random data',
      'if (threadIdx.x < 17)',
    ],
    a: 1,
    e: 'Branching on (threadIdx.x / 32) keeps each warp on a single path because every thread in a 32-wide warp shares the same warp index — no intra-warp divergence. The others split a warp across both paths.',
    ref: 'PMPP Ch. 4',
    tags: ['divergence', 'warp', 'branching'],
  },
  {
    d: 1,
    q: 'What does SM stand for in GPU architecture?',
    o: [
      'Shared Memory',
      'Streaming Multiprocessor',
      'Scalar Module',
      'Synchronous Multiplexer',
    ],
    a: 1,
    e: 'A Streaming Multiprocessor is the core compute unit of the GPU. It contains CUDA cores, warp schedulers, register file, shared memory/L1, and (on recent GPUs) tensor cores. A GPU has many SMs.',
    ref: 'PMPP Ch. 4; CUDA C++ Programming Guide §4',
    tags: ['sm', 'architecture'],
  },
  {
    d: 2,
    q: 'How does a GPU primarily hide global-memory latency?',
    o: [
      'By prefetching all data into registers',
      'By switching among many resident warps so that when one stalls, another executes',
      'By running each warp on a dedicated core',
      'By compressing memory traffic',
    ],
    a: 1,
    e: 'GPUs are throughput machines: each SM keeps many warps resident and the scheduler issues from any ready warp. While one warp waits on a long-latency load, others make progress, hiding latency through massive thread-level parallelism.',
    ref: 'CUDA C++ Programming Guide §5.2; PMPP Ch. 4',
    tags: ['latency-hiding', 'scheduling', 'occupancy'],
  },
  {
    d: 2,
    q: 'What is "occupancy"?',
    o: [
      'The fraction of SMs that are active',
      'The ratio of active warps per SM to the maximum possible warps per SM',
      'The percentage of memory bandwidth used',
      'The number of blocks in the grid',
    ],
    a: 1,
    e: 'Occupancy = active warps per SM ÷ maximum warps per SM. It is bounded by registers/thread, shared memory/block, block size, and the per-SM hardware limits. Higher occupancy gives more latency-hiding potential (but is not the only factor in performance).',
    ref: 'CUDA C++ Programming Guide §5.2.3; CUDA Occupancy Calculator',
    tags: ['occupancy', 'scheduling'],
  },
  {
    d: 3,
    q: 'Which is a correct statement about occupancy and performance?',
    o: [
      'Maximizing occupancy always maximizes performance',
      'Higher occupancy helps hide latency, but a kernel can be fast at moderate occupancy if it has enough instruction-level parallelism and memory parallelism',
      'Occupancy is irrelevant to performance',
      'Occupancy only matters for compute-bound kernels',
    ],
    a: 1,
    e: '"Occupancy is a means, not an end." It must be high enough to hide latency, but beyond that, more occupancy doesn’t help and can hurt (less shared memory/registers per thread). High-ILP kernels (e.g. Volkov’s work) hit peak at modest occupancy.',
    ref: 'Volkov, "Better Performance at Lower Occupancy"; CUDA Best Practices Guide',
    tags: ['occupancy', 'ilp', 'performance'],
  },
  {
    d: 3,
    q: 'What limits the number of resident blocks on an SM? (choose the most complete answer)',
    o: [
      'Only the number of CUDA cores',
      'The minimum across: registers, shared memory, threads/warps per SM, and the hardware block-per-SM limit',
      'Only shared memory',
      'The grid size',
    ],
    a: 1,
    e: 'Resident blocks per SM are the minimum allowed by all constraints simultaneously: register file size, shared memory, max warps/threads per SM, and the architectural max blocks per SM. Whichever resource runs out first caps occupancy.',
    ref: 'CUDA C++ Programming Guide §5.2.3; Occupancy Calculator',
    tags: ['occupancy', 'resources', 'sm'],
  },
  {
    d: 4,
    q: 'Since the Volta architecture, "Independent Thread Scheduling" means…',
    o: [
      'Each thread runs on its own physical core',
      'Threads in a warp have individual program counters and can interleave/reconverge, so you can no longer assume implicit warp-synchronous behavior',
      'Warps are no longer 32 threads',
      'Divergence has zero cost',
    ],
    a: 1,
    e: 'Volta gave each thread its own PC and call stack, allowing diverged threads to interleave and progress independently. Code that relied on implicit warp-lockstep (no __syncwarp) can break; use explicit __syncwarp() and *_sync warp intrinsics.',
    ref: 'NVIDIA Volta Whitepaper; CUDA C++ Programming Guide §4.2',
    tags: ['volta', 'independent-thread-scheduling', 'warp'],
  },
  {
    d: 3,
    q: 'Why should warp-level primitives use the explicit-mask "_sync" variants (e.g. __shfl_down_sync) on Volta+?',
    o: [
      'They are faster',
      'They specify exactly which lanes participate and enforce reconvergence/synchronization, which is required for correctness under independent thread scheduling',
      'The non-sync versions use more registers',
      'They enable tensor cores',
    ],
    a: 1,
    e: 'With independent thread scheduling, you must name the participating lanes via a mask and synchronize them. The deprecated non-sync intrinsics assume implicit lockstep and can produce undefined results on Volta and later.',
    ref: 'CUDA C++ Programming Guide §B.22 (Warp Shuffle); Volta migration guide',
    tags: ['warp-primitives', 'shuffle', 'volta'],
  },
  {
    d: 2,
    q: 'What does __shfl_down_sync allow threads in a warp to do?',
    o: [
      'Write to global memory atomically',
      'Read a register value directly from another lane in the same warp without using shared memory',
      'Synchronize across blocks',
      'Down-convert FP32 to FP16',
    ],
    a: 1,
    e: 'Warp shuffle instructions exchange register values directly between lanes of a warp, bypassing shared memory. __shfl_down_sync shifts values down by a delta, enabling fast in-warp reductions and scans.',
    ref: 'CUDA C++ Programming Guide §B.22',
    tags: ['shuffle', 'warp', 'reduction'],
  },
  {
    d: 4,
    q: 'A kernel has 100% theoretical occupancy but poor performance. Which is the LEAST likely cause to investigate?',
    o: [
      'Memory access pattern is uncoalesced',
      'The kernel is memory-bandwidth bound and already saturating DRAM',
      'Too few registers per thread causing spills',
      'Warp divergence serializing branches',
    ],
    a: 2,
    e: 'Full occupancy generally means register/shared-memory pressure is low, so register spilling is unlikely. Poor performance at high occupancy usually points to memory inefficiency (uncoalesced/bandwidth-bound) or divergence — not too few registers.',
    ref: 'CUDA Best Practices Guide; Nsight Compute analysis',
    tags: ['occupancy', 'performance', 'debugging'],
  },
  {
    d: 3,
    q: 'On a modern SM with 4 warp schedulers, how many warps can issue instructions in a single cycle (best case)?',
    o: ['1', '4 (one per scheduler, possibly dual-issue for more)', '32', '128'],
    a: 1,
    e: 'Each warp scheduler can issue from one (sometimes two, via dual-issue) ready warp per cycle. With 4 schedulers, up to ~4–8 instructions across warps can be issued per cycle on the SM, sustaining high throughput across many resident warps.',
    ref: 'NVIDIA architecture whitepapers (Volta/Ampere/Hopper)',
    tags: ['warp-scheduler', 'issue', 'sm'],
  },
  {
    d: 3,
    q: 'What is a "tail effect" in GPU execution?',
    o: [
      'The latency of the last memory transaction',
      'Underutilization when the final wave of blocks does not fill all SMs, so the kernel runtime is dominated by a partial wave',
      'Divergence at the end of a loop',
      'Cache eviction at kernel exit',
    ],
    a: 1,
    e: 'If the grid’s blocks don’t evenly fill the available SM "waves," the last wave runs few blocks while most SMs idle, inflating runtime. Choosing grid sizes that are multiples of the number of resident-block slots mitigates the tail.',
    ref: 'CUDA Best Practices Guide; Nsight Compute',
    tags: ['tail-effect', 'wave-quantization', 'scheduling'],
  },
  {
    d: 2,
    q: 'Threads within a single block are guaranteed to…',
    o: [
      'Run on the same SM and be able to share shared memory and synchronize',
      'Run on different SMs for load balancing',
      'Execute strictly in threadIdx order',
      'Each get a private copy of shared memory',
    ],
    a: 0,
    e: 'All threads of a block are co-resident on one SM, so they can cooperate via that SM’s shared memory and synchronize with __syncthreads(). Different blocks may land on different SMs and cannot rely on each other.',
    ref: 'CUDA C++ Programming Guide §2.2; PMPP Ch. 4',
    tags: ['block', 'sm', 'cooperation'],
  },
  {
    d: 4,
    q: 'What is the practical effect of choosing a block size that is not a multiple of 32?',
    o: [
      'The launch fails',
      'The last warp is partially filled, wasting execution slots for the inactive lanes',
      'Shared memory is disabled',
      'Occupancy automatically rounds up',
    ],
    a: 1,
    e: 'Warps are 32 wide. A block of, say, 100 threads forms 4 warps (128 lanes) where the last warp has 28 inactive lanes that still occupy scheduling slots. Block sizes that are multiples of 32 avoid this waste.',
    ref: 'CUDA Best Practices Guide; PMPP Ch. 4',
    tags: ['block-size', 'warp', 'efficiency'],
  },
  {
    d: 5,
    q: 'Cooperative Groups’ grid-wide synchronization (grid.sync()) requires which launch mechanism and constraint?',
    o: [
      'A normal <<<>>> launch with any grid size',
      'cudaLaunchCooperativeKernel, with a grid sized so all blocks are simultaneously resident (co-scheduled)',
      'Dynamic parallelism',
      'A separate kernel per block',
    ],
    a: 1,
    e: 'Grid-level sync needs every block to be concurrently resident; you must launch via cudaLaunchCooperativeKernel and size the grid to the device’s capacity (query with cudaOccupancyMaxActiveBlocksPerMultiprocessor × SM count). Otherwise blocks can’t all wait at the barrier.',
    ref: 'CUDA C++ Programming Guide §C (Cooperative Groups)',
    tags: ['cooperative-groups', 'grid-sync', 'launch'],
  },
  {
    d: 3,
    q: 'A reduction that maps active threads to contiguous lanes each step (instead of strided with modulo) is preferred because…',
    o: [
      'It uses less shared memory',
      'It keeps active threads packed into full warps, reducing divergence and idle warps as the reduction shrinks',
      'It avoids atomics',
      'It is required by __syncthreads',
    ],
    a: 1,
    e: 'In the classic reduction optimization, indexing active threads consecutively (s = blockDim/2, halving) keeps participation within whole warps, so entire warps retire together with minimal divergence — unlike the modulo version where active threads scatter across warps.',
    ref: 'Harris, "Optimizing Parallel Reduction in CUDA"; PMPP Ch. 10',
    tags: ['reduction', 'divergence', 'optimization'],
  },
  {
    d: 4,
    q: 'What is "register pressure" and a common symptom in Nsight Compute?',
    o: [
      'Too few registers, shown as high occupancy',
      'High per-thread register demand that lowers occupancy and may cause local-memory spills (visible as local load/store traffic)',
      'Bank conflicts in the register file',
      'Excessive atomic contention',
    ],
    a: 1,
    e: 'Register pressure is when a kernel needs many registers per thread. It caps occupancy and, beyond the per-thread limit, spills to local memory. Nsight Compute shows it via achieved occupancy limits and local memory (spill) load/store metrics.',
    ref: 'Nsight Compute documentation; CUDA Best Practices Guide',
    tags: ['registers', 'spilling', 'occupancy'],
  },
  {
    d: 5,
    q: 'Hopper introduced thread block clusters. What scheduling guarantee do they add beyond ordinary blocks?',
    o: [
      'Blocks in a cluster are co-scheduled on SMs within the same GPC and can synchronize and share memory',
      'Blocks in a cluster run on different GPUs',
      'Clusters remove the concept of warps',
      'Clusters guarantee global ordering of all blocks',
    ],
    a: 0,
    e: 'A cluster is a group of blocks guaranteed to be co-scheduled on SMs of one GPC, enabling cluster-wide barriers (cluster.sync()) and distributed shared memory access between member blocks — a new tier between block and grid.',
    ref: 'NVIDIA Hopper Whitepaper; CUDA C++ Programming Guide (Thread Block Clusters)',
    tags: ['hopper', 'clusters', 'scheduling'],
  },
])
