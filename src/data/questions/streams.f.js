import { defineQuestions } from './_helpers.js'

// Streams, Events & Graphs — batch F. Practical scenarios: inference latency,
// overlap, capture pitfalls, sharing. Distinct from streams.js/.b/.c/.d/.e.
export default defineQuestions(
  'streams',
  [
    {
      d: 4,
      q: 'A fixed-shape inference model is launch-overhead-bound at high QPS. The highest-impact fix is…',
      o: [
        'Lower precision only',
        'Capture as a graph; remove per-launch overhead',
        'More streams',
        'A bigger batch',
      ],
      a: 1,
      x: [
        'cudaDeviceSynchronize',
        'FP64',
        'More registers',
      ],
      e: 'Fixed-shape, high-QPS inference is dominated by per-kernel launch overhead. Graph capture+replay turns the step into one launch, cutting CPU overhead and gaps — typically a large latency win, often paired with a high-priority stream.',
      ref: 'CUDA Graphs; inference latency',
      tags: ['cuda-graphs', 'inference', 'latency'],
    },
    {
      d: 5,
      q: 'You capture a training step as a graph, but each iteration uses different input/grad buffers. The correct approach is…',
      o: [
        'Re-capture every step',
        'Refill static buffers, or use GraphExecUpdate',
        'Avoid graphs',
        'Use the default stream',
      ],
      a: 1,
      x: [
        'Re-instantiate each step',
        'Disable capture',
        'Use MPS',
      ],
      e: 'A captured graph replays fixed pointers/structure. Either keep buffers static and refill them, or update node parameters via cudaGraphExecUpdate. Both preserve the cheap-replay benefit; re-capturing each iteration defeats the purpose.',
      ref: 'CUDA Graphs; static buffers / update',
      tags: ['cuda-graphs', 'static-buffers', 'training'],
    },
    {
      d: 5,
      q: 'Your captured training-step graph reproduces the SAME dropout mask every replay. The cause/fix is…',
      o: [
        'Graphs randomize automatically',
        'Feed fresh RNG state from an updatable buffer each step',
        'Disable dropout',
        'Use FP64',
      ],
      a: 1,
      x: [
        'Re-capture each step',
        'Use the CPU',
        'It is automatic',
      ],
      e: 'Graph replay is deterministic over its captured values. Per-iteration randomness must come from state the graph reads from an updatable buffer (e.g. a counter/seed advanced each step), not values fixed at capture — otherwise the mask repeats.',
      ref: 'CUDA Graphs; RNG state',
      tags: ['cuda-graphs', 'rng', 'dropout'],
    },
    {
      d: 4,
      q: 'Graph capture aborts with an error when your code calls cudaMalloc inside the captured region. The fix is…',
      o: [
        'Remove all allocations',
        'Pre-allocate or use cudaMallocAsync',
        'Use FP64',
        'Use the default stream',
      ],
      a: 1,
      x: [
        'Capture on the host',
        'Disable graphs',
        'Add __syncthreads',
      ],
      e: 'Blocking allocation synchronizes, violating capture’s async-only rule. Either allocate before capture or use the stream-ordered allocator (cudaMallocAsync), which is captured as a graph memory node — so allocation can live inside the replayed step.',
      ref: 'CUDA Graphs; capture allocation rules',
      tags: ['cuda-graphs', 'cudaMalloc', 'capture'],
    },
    {
      d: 4,
      q: 'Two kernels on different streams don’t overlap despite spare SMs and no default-stream use. Check for…',
      o: [
        'Streams never overlap',
        'Hidden dependency or hardware-queue funneling',
        'Too few registers',
        'For FP64',
      ],
      a: 1,
      x: [
        'They are too large',
        'Constant memory',
        'ECC',
      ],
      e: 'Beyond default-stream/occupancy issues, an explicit/implicit dependency (event waits, ordered library calls) or hardware-queue contention can serialize independent kernels. The Nsight Systems timeline plus checking for dependencies reveals the cause.',
      ref: 'Stream concurrency; hidden dependencies',
      tags: ['concurrency', 'dependencies', 'diagnosis'],
    },
    {
      d: 4,
      q: 'To co-locate a latency-critical kernel with background throughput work on one GPU, you…',
      o: [
        'Use one stream',
        'High-priority stream for preferential block dispatch',
        'cudaDeviceSynchronize',
        'Use constant memory',
      ],
      a: 1,
      x: [
        'Use FP64',
        'Disable streams',
        'Use the default stream only',
      ],
      e: 'Stream priorities bias dispatch of pending blocks toward the high-priority stream, keeping a latency-critical task responsive amid throughput work. It’s a scheduling hint (not strict preemption of running blocks).',
      ref: 'CUDA stream priorities',
      tags: ['stream-priority', 'latency', 'colocate'],
    },
    {
      d: 4,
      q: 'A serving system runs many small inference requests that each underuse the GPU. To raise utilization across processes, use…',
      o: [
        'MIG',
        'MPS: concurrent multi-process kernels on one GPU',
        'More streams in one process',
        'FP64',
      ],
      a: 1,
      x: [
        'cudaDeviceSynchronize',
        'A bigger batch only',
        'Constant memory',
      ],
      e: 'Default time-slicing serializes processes. MPS funnels their work through one context so kernels run concurrently, filling the GPU for many small per-process workloads — boosting aggregate utilization/throughput.',
      ref: 'NVIDIA MPS; inference',
      tags: ['mps', 'inference', 'utilization'],
    },
    {
      d: 4,
      q: 'You need STRICT isolation/QoS between multiple tenants on one H100. The right mechanism is…',
      o: [
        'MPS',
        'MIG: hard-partitioned isolated instances',
        'Streams',
        'Time-slicing',
      ],
      a: 1,
      x: [
        'Green contexts only',
        'cudaDeviceSynchronize',
        'FP64',
      ],
      e: 'MIG provides hard hardware isolation (compute, cache, memory) per instance — strong QoS for multi-tenant. MPS shares spatially without strong isolation; time-slicing serializes. Choose MIG when isolation/guarantees matter most.',
      ref: 'MIG vs MPS',
      tags: ['mig', 'isolation', 'qos'],
    },
    {
      d: 4,
      q: 'A chain of short back-to-back kernels has visible inter-kernel gaps. Beyond CUDA Graphs, Hopper offers…',
      o: [
        'Nothing',
        'PDL: overlap tail-of-one with start-of-next',
        'More streams',
        'FP64',
      ],
      a: 1,
      x: [
        'cudaDeviceSynchronize',
        'Constant memory',
        'A bigger grid',
      ],
      e: 'Graphs cut launch overhead but kernels still run strictly ordered. PDL allows partial overlap across the dependency boundary (next kernel’s preamble during the prior kernel’s drain), shrinking inter-kernel gaps for short kernel chains.',
      ref: 'CUDA PDL',
      tags: ['pdl', 'kernel-chain', 'overlap'],
    },
    {
      d: 4,
      q: 'To pipeline disjoint chunks of a large workload, you rotate H2D-copy, kernel, D2H-copy across a small set of streams so that…',
      o: [
        'One giant kernel runs',
        'Transfer of one chunk overlaps compute of another',
        'cudaDeviceSynchronize per chunk',
        'A single default stream',
      ],
      a: 1,
      x: [
        'Constant memory staging',
        'MPS',
        'FP64',
      ],
      e: 'Round-robining chunks over a few streams (pinned memory + async copies) lets the copy of one chunk overlap the compute of another, approximating a software pipeline that hides transfer time behind compute. The slowest stage sets throughput.',
      ref: 'Streamed pipelining; overlap',
      tags: ['pipelining', 'streams', 'overlap'],
    },
    {
      d: 5,
      q: 'A producer process and consumer process on the same node need to share GPU data with correct ordering. Use…',
      o: [
        'A file',
        'IPC mem for data + IPC events for ordering',
        'Host sockets only',
        'Constant memory',
      ],
      a: 1,
      x: [
        'A single process',
        'cudaDeviceSynchronize',
        'FP64',
      ],
      e: 'Cross-process GPU sharing needs both data sharing (IPC memory) and synchronization (IPC events). The producer records an event; the consumer (sharing the handle) waits on it before reading — a correct, node-local, zero-copy pipeline.',
      ref: 'CUDA IPC memory + events',
      tags: ['ipc', 'multi-process', 'synchronization'],
    },
    {
      d: 5,
      q: 'A CUDA post-processing kernel must read a Vulkan-rendered frame without a host copy. The mechanism is…',
      o: [
        'cudaMemcpy from the host',
        'Import the buffer into CUDA + sync via semaphores',
        'Recompile the kernel',
        'Constant memory',
      ],
      a: 1,
      x: [
        'Use the CPU',
        'Disable the GPU',
        'FP64',
      ],
      e: 'Importing the graphics API’s allocation (external memory) and synchronizing with imported semaphores lets CUDA read/write the shared GPU memory directly — enabling zero-copy graphics↔compute pipelines (render → CUDA filter → display).',
      ref: 'CUDA external resource interop',
      tags: ['interop', 'external-memory', 'graphics'],
    },
    {
      d: 5,
      q: 'An iterative solver should loop on the GPU until convergence without host round-trips. The graph feature is…',
      o: [
        'Static graphs only',
        'While-nodes set by a kernel at runtime',
        'More streams',
        'cudaDeviceSynchronize',
      ],
      a: 1,
      x: [
        'Host-side loops',
        'Constant memory',
        'FP64',
      ],
      e: 'A while-node loops based on a device-set condition; a kernel computes the residual and sets the conditional handle to continue/stop. This keeps the iterate-until-converged loop entirely on the GPU, avoiding per-iteration host synchronization.',
      ref: 'CUDA conditional graph nodes',
      tags: ['cuda-graphs', 'conditional', 'iterative'],
    },
    {
      d: 4,
      q: 'For a recurring set of CONCURRENT small kernels each iteration, capturing them in one graph can beat many stream launches because…',
      o: [
        'Graphs serialize',
        'Independent nodes + no per-launch overhead',
        'Graphs run on the host',
        'FP64',
      ],
      a: 1,
      x: [
        'Streams are illegal',
        'It compresses data',
        'Constant memory',
      ],
      e: 'A graph captures independent nodes (their concurrency) once and replays them with minimal host overhead, whereas many stream launches incur per-launch CPU cost each iteration. For recurring concurrent kernels, the graph is more efficient.',
      ref: 'CUDA Graphs; concurrency + overhead',
      tags: ['cuda-graphs', 'concurrency', 'overhead'],
    },
    {
      d: 4,
      q: 'Why might a high-priority stream NOT improve a latency-critical kernel’s tail under heavy load?',
      o: [
        'Priorities are ignored',
        'Priority biases dispatch, not preemption',
        'It runs on the host',
        'FP64',
      ],
      a: 1,
      x: [
        'It compresses data',
        'It pins memory',
        'Constant memory',
      ],
      e: 'Block scheduling generally doesn’t evict running blocks. A high-priority kernel’s blocks dispatch sooner only as SMs free up; if long low-priority blocks hold the SMs, the hint can’t preempt them — so tail latency can still suffer.',
      ref: 'Stream priority; block scheduling',
      tags: ['stream-priority', 'latency', 'preemption'],
    },
    {
      d: 3,
      q: 'In Nsight Systems, a workload using CUDA Graphs appears on the timeline as…',
      o: [
        'Each kernel a separate host launch',
        'One graph launch with kernels tightly packed (gaps gone)',
        'Nothing',
        'Only the CPU',
      ],
      a: 1,
      x: [
        'Kernels on the host',
        'Random gaps',
        'Higher overhead',
      ],
      e: 'Graphs collapse many host launches into one; the timeline shows the single launch and tightly-packed kernels without per-kernel CPU gaps — the visible payoff for launch-bound workloads.',
      ref: 'Nsight Systems; graphs on timeline',
      tags: ['cuda-graphs', 'nsight-systems', 'timeline'],
    },
    {
      d: 4,
      q: 'A kernel launched on a non-default stream still serializes with default-stream work. The reason is…',
      o: [
        'Streams always serialize',
        'Legacy null stream syncs with blocking streams',
        'Too many registers',
        'The GPU is busy',
      ],
      a: 1,
      x: [
        'Events block it',
        'Constant memory',
        'It is high priority',
      ],
      e: 'The legacy null stream synchronizes with other blocking streams, forcing ordering. Compile with --default-stream per-thread or create streams with cudaStreamNonBlocking to get true concurrency with default-stream work.',
      ref: 'CUDA default stream semantics',
      tags: ['default-stream', 'serialization', 'non-blocking'],
    },
    {
      d: 4,
      q: 'cudaGraphExecUpdate fails between iterations. The most common reason is…',
      o: [
        'Out of memory',
        'Topology changed; must re-instantiate',
        'Wrong precision',
        'A divergent branch',
      ],
      a: 1,
      x: [
        'Too many streams',
        'A host callback',
        'A bank conflict',
      ],
      e: 'Update refreshes node parameters of an identical-topology graph. If the structure changed (nodes/edges added/removed/retyped), update fails with a topology-change result and you fall back to instantiating a new executable graph.',
      ref: 'cudaGraphExecUpdate',
      tags: ['cuda-graphs', 'update', 'topology'],
    },
    {
      d: 4,
      q: 'To express "B and C both depend on A’s kernel, and run concurrently after it," use…',
      o: [
        'cudaDeviceSynchronize after A',
        'Event in A; B and C wait then run in parallel',
        'One stream for all',
        'MPS',
      ],
      a: 1,
      x: [
        'Constant memory',
        'A spinlock',
        'A host callback',
      ],
      e: 'Event-based dependencies express a DAG without a global sync: A signals an event; B and C wait on it and then run concurrently. This keeps the host free and lets independent successors overlap.',
      ref: 'CUDA events; fork/join',
      tags: ['events', 'fork-join', 'streams'],
    },
    {
      d: 4,
      q: 'CUDA Graphs reduce step-time JITTER across distributed ranks because…',
      o: [
        'Faster math',
        'One replay removes per-launch CPU jitter',
        'Lower precision',
        'More memory',
      ],
      a: 1,
      x: [
        'They disable the GPU',
        'They add streams',
        'Constant memory',
      ],
      e: 'Per-kernel launches incur variable CPU overhead, adding jitter that desynchronizes ranks at collectives. Replaying a captured step as one unit removes that variability and gaps, reducing step-time variance across ranks.',
      ref: 'CUDA Graphs; jitter',
      tags: ['cuda-graphs', 'jitter', 'distributed'],
    },
    {
      d: 5,
      q: 'Device-side graph launch (a kernel launching a graph) is enabled by instantiating the graph with…',
      o: [
        'Any flag',
        'Device-launch opt-in flag for the graph',
        'No flag',
        'A host callback',
      ],
      a: 1,
      x: [
        'cudaMallocAsync',
        'A new stream',
        'Constant memory',
      ],
      e: 'To launch a graph from device code, instantiate it as device-launchable (the flag). This restricts what the graph may contain but lets a kernel trigger it — keeping adaptive/recursive control flow on the GPU without host round-trips.',
      ref: 'CUDA device-launchable graphs',
      tags: ['cuda-graphs', 'device-launch', 'instantiate'],
    },
    {
      d: 4,
      q: 'A GPU has 2 copy engines (asyncEngineCount=2). This lets you…',
      o: [
        'Run two kernels',
        'H2D + D2H + compute all at once (separate engines)',
        'Double the SMs',
        'Disable the default stream',
      ],
      a: 1,
      x: [
        'Pin host memory',
        'Use FP64',
        'Constant memory',
      ],
      e: 'Two copy engines handle H2D and D2H concurrently, and the SMs compute — so a pipelined kernel can stream data in, compute, and stream results out all at once (with pinned memory and multiple streams). Query asyncEngineCount to know the capability.',
      ref: 'Copy engines; asyncEngineCount',
      tags: ['copy-engines', 'overlap', 'streaming'],
    },
    {
      d: 4,
      q: 'Why is a small fixed pool of streams usually better than creating/destroying streams each iteration?',
      o: [
        'Streams are global',
        'Reuse a pool sized to achievable concurrency',
        'Streams run on the host',
        'FP64',
      ],
      a: 1,
      x: [
        'It compresses data',
        'It pins memory',
        'Constant memory',
      ],
      e: 'Per-iteration stream creation/destruction adds overhead and potential synchronization. A reused pool, sized to the real overlap (limited by hardware queues + SM capacity), avoids that cost and the diminishing returns of too many streams.',
      ref: 'CUDA streams; pooling',
      tags: ['streams', 'pooling', 'overhead'],
    },
    {
      d: 4,
      q: 'cudaGraphNodeSetEnabled lets you implement optional steps in a graph by…',
      o: [
        'Adding nodes',
        'Toggle a node on/off between launches',
        'Re-capturing',
        'A host callback',
      ],
      a: 1,
      x: [
        'cudaMallocAsync',
        'A new stream',
        'Constant memory',
      ],
      e: 'Toggling nodes on/off in the executable graph skips/includes steps per iteration cheaply (topology unchanged), a lightweight conditionality complementary to conditional nodes — avoiding the cost of building a new graph.',
      ref: 'cudaGraphNodeSetEnabled',
      tags: ['cuda-graphs', 'node-enable', 'conditional'],
    },
    {
      d: 4,
      q: 'For multi-GPU work in one process, each GPU needs its own stream(s) because…',
      o: [
        'Streams are global',
        'Streams are device-bound at creation; one per GPU',
        'One stream covers all GPUs',
        'Streams run on the host',
      ],
      a: 1,
      x: [
        'Streams auto-pick the GPU',
        'It saves memory',
        'FP64',
      ],
      e: 'Streams belong to the device current at creation. To drive multiple GPUs you set each device and create its streams; launching on a stream runs on its device. Mixing this up (wrong current device) is a classic multi-GPU bug.',
      ref: 'CUDA streams; multi-GPU',
      tags: ['streams', 'multi-gpu', 'device'],
    },
    {
      d: 5,
      q: 'Green contexts (Hopper) reserve a subset of SMs for a context, useful to…',
      o: [
        'Increase clocks',
        'Intra-process SM reservation for QoS',
        'Compress data',
        'Pin memory',
      ],
      a: 1,
      x: [
        'Lower precision',
        'Disable streams',
        'For graphics',
      ],
      e: 'Green contexts reserve SMs for a context, providing spatial partitioning/QoS without MIG’s hard isolation — handy to co-locate a real-time/latency-sensitive task with throughput work inside one process/GPU.',
      ref: 'CUDA green contexts',
      tags: ['green-contexts', 'sm-reservation', 'qos'],
    },
    {
      d: 3,
      q: 'For pure ordering (not timing) between streams, create events with…',
      o: [
        'Default flags',
        'cudaEventDisableTiming — cheaper for pure ordering',
        'cudaEventBlockingSync only',
        'No events',
      ],
      a: 1,
      x: [
        'cudaEventInterprocess',
        'Timing enabled',
        'Constant memory',
      ],
      e: 'Timing-enabled events carry timestamp overhead. For pure cross-stream ordering, cudaEventDisableTiming events are cheaper — record on the producer and wait on it from the consumer to enforce "B after A’s event" without measuring time.',
      ref: 'CUDA events; disable timing',
      tags: ['events', 'ordering', 'flags'],
    },
    {
      d: 4,
      q: 'Adding MORE streams eventually REDUCES performance because…',
      o: [
        'Streams are free',
        'Past max concurrency, extra streams add overhead',
        'Streams disable kernels',
        'It always helps',
      ],
      a: 1,
      x: [
        'They increase precision',
        'They pin memory',
        'Constant memory',
      ],
      e: 'Past the achievable concurrency (hardware queues + idle SM resources), more streams just add overhead and contention. Size the stream pool to the real overlap, not arbitrarily large.',
      ref: 'CUDA streams; diminishing returns',
      tags: ['streams', 'overhead', 'concurrency'],
    },
    {
      d: 5,
      q: 'cudaMallocAsync pairs especially well with CUDA Graphs because…',
      o: [
        'It disables the pool',
        'Capturable; no device-wide sync',
        'It runs on the host',
        'It compresses memory',
      ],
      a: 1,
      x: [
        'It pins memory',
        'It doubles bandwidth',
        'Constant memory',
      ],
      e: 'Plain cudaMalloc/cudaFree synchronize and can’t be captured. The stream-ordered allocator integrates with graphs (memory nodes) and avoids those syncs, so allocation can live inside captured, replayable iterations.',
      ref: 'CUDA Graphs + stream-ordered allocator',
      tags: ['cudaMallocAsync', 'cuda-graphs', 'capture'],
    },
    {
      d: 4,
      q: 'A reason to reuse a small pool of (timing-disabled) events rather than creating them each iteration is…',
      o: [
        'Events are global',
        'Reuse events; avoids create/destroy overhead',
        'Events run on the host',
        'It increases precision',
      ],
      a: 1,
      x: [
        'It compresses data',
        'It pins memory',
        'Constant memory',
      ],
      e: 'Like streams, events have creation overhead. Pre-creating and reusing events (timing disabled for pure ordering) avoids per-iteration allocation cost in tight loops/pipelines.',
      ref: 'CUDA events; reuse',
      tags: ['events', 'pooling', 'overhead'],
    },
  ],
  { idPrefix: 'streams-f' }
)
