import { defineQuestions } from './_helpers.js'

// Execution & Scheduling — batch F. Latency-hiding math, divergence/occupancy
// calculations, stall scenarios, cluster sizing. Distinct from execution.js/.b/.c/.d/.e.
export default defineQuestions(
  'execution',
  [
    {
      d: 5,
      q: 'A pipe has 4-cycle latency and accepts 1 op/cycle. To keep it full with a single dependency chain, you need about…',
      o: [
        '1 independent op',
        '4 independent ops in flight (latency × throughput) — e.g. 4 accumulators — so each op’s result is ready by the time it’s reused',
        '32 ops',
        '0 ops',
      ],
      a: 1,
      x: ['2 ops', '8 ops', '64 ops'],
      e: 'By Little’s law, in-flight work = latency × throughput = 4 × 1 = 4. Four independent operations (e.g. 4 partial accumulators) cover the 4-cycle latency so the pipe issues every cycle — the basis of multiple-accumulator unrolling.',
      ref: "Little's law; ILP and accumulators",
      tags: ['ilp', 'latency-hiding', 'accumulators'],
    },
    {
      d: 5,
      q: 'To hide ~600-cycle memory latency at 1 memory op issued per cycle per scheduler, you need on the order of…',
      o: [
        '4 warps',
        'Hundreds of cycles of independent memory work in flight — i.e. enough warps/ILP to issue ~600 outstanding requests (achieved via occupancy and/or per-thread MLP)',
        '1 warp',
        '32 warps exactly',
      ],
      a: 1,
      x: [
        '2 warps',
        '10 warps always',
        '0 warps',
      ],
      e: 'Hiding hundreds of cycles of latency requires hundreds of outstanding requests (Little’s law). That comes from many resident warps and/or per-thread memory-level parallelism (multiple independent loads). Too little in flight ⇒ latency-bound.',
      ref: "Little's law; memory latency hiding",
      tags: ['latency-hiding', 'mlp', 'occupancy'],
    },
    {
      d: 4,
      q: 'A warp where 8 of 32 lanes take a branch and the other 24 take the else runs the divergent region at about…',
      o: [
        'Full efficiency',
        'Reduced efficiency — both paths execute serially (8 active then 24 active), so the region runs at ~(8+24)/(32+32) ... effectively splitting one warp’s slot across two passes',
        'Double speed',
        'Zero speed',
      ],
      a: 1,
      x: [
        'Quarter speed always',
        'No effect',
        'It deadlocks',
      ],
      e: 'A divergent if/else executes each path with the non-participating lanes masked, so the warp does two passes (8-active and 24-active) instead of one full pass — wasting execution slots. Minimizing or restructuring such branches recovers efficiency.',
      ref: 'SIMT divergence; efficiency',
      tags: ['divergence', 'efficiency', 'branching'],
    },
    {
      d: 4,
      q: 'A kernel uses 128 registers/thread on an SM with 65,536 registers and 2048 max threads. Register-limited resident threads =…',
      o: ['2048', '1024', '512', '256'],
      a: 2,
      x: ['768', '1536', '128'],
      e: '65,536 / 128 = 512 threads (register-limited), i.e. 25% of the 2048 max. Halving registers to 64 would allow 1024 (50%); 32 would allow the full 2048. Register usage directly caps occupancy.',
      ref: 'Occupancy; register limit calculation',
      tags: ['registers', 'occupancy', 'calculation'],
    },
    {
      d: 4,
      q: 'A grid of 300 blocks runs on a GPU with capacity for 264 resident blocks (132 SMs × 2). The execution takes…',
      o: [
        '1 wave',
        '2 waves: 264 blocks in wave 1, then only 36 in wave 2 (a small "tail" wave underfilling the SMs) — sizing the grid to a multiple of 264 avoids the wasteful tail',
        '300 waves',
        '0.5 waves',
      ],
      a: 1,
      x: ['3 waves', '132 waves', '4 waves'],
      e: 'Capacity is 264 resident blocks; 300 needs 2 waves (264 + 36). The second wave runs only 36 blocks while most SMs idle — the tail effect. Choosing grid sizes near a multiple of capacity (or more blocks) reduces the relative tail cost.',
      ref: 'Wave quantization; tail effect',
      tags: ['waves', 'tail-effect', 'calculation'],
    },
    {
      d: 3,
      q: 'A warp-shuffle reduction over 32 lanes takes how many steps?',
      o: ['32', '5 (log2 32)', '16', '1'],
      a: 1,
      x: ['10', '6', '31'],
      e: 'A butterfly/tree shuffle reduction halves the active span each step: log2(32) = 5 steps (offsets 16,8,4,2,1). This is why the warp portion of a reduction is cheap and needs no shared memory or barriers.',
      ref: 'Warp shuffle reduction; step count',
      tags: ['shuffle', 'reduction', 'log-steps'],
    },
    {
      d: 4,
      q: 'For a cooperative (grid.sync) kernel on a 132-SM GPU where the kernel’s occupancy allows 3 resident blocks/SM, the cooperative grid should have at most…',
      o: [
        'Any number',
        '396 blocks (132 × 3) — all blocks must be co-resident, so the grid is capped at SMs × resident-blocks-per-SM',
        '132 blocks',
        '2048 blocks',
      ],
      a: 1,
      x: ['264 blocks', '512 blocks', '66 blocks'],
      e: 'grid.sync requires all blocks simultaneously resident. With 3 blocks/SM × 132 SMs = 396, the cooperative grid can’t exceed 396 blocks. You query the per-SM resident count (occupancy API) and multiply by SM count to size it.',
      ref: 'Cooperative launch; grid sizing',
      tags: ['cooperative-launch', 'grid-sync', 'calculation'],
    },
    {
      d: 4,
      q: 'Nsight shows a kernel dominated by "Math Pipe Throttle" with heavy __expf usage. The fix is…',
      o: [
        'More memory accesses',
        'Reduce transcendental (SFU) pressure: fewer exp/log/sin (algebraic simplification, reuse), or accept the fast-intrinsic accuracy and overlap with other pipes — the SFU is the bottleneck',
        'More occupancy',
        'Larger grid',
      ],
      a: 1,
      x: [
        'Disable the L2',
        'Use FP64',
        'Add __syncthreads',
      ],
      e: 'Math-pipe throttle with transcendental-heavy code means the SFU is saturated. Cut the count of such ops or balance with other work; occupancy/memory tuning won’t help a compute-pipe-bound kernel.',
      ref: 'Nsight Compute; SFU/math throttle',
      tags: ['math-throttle', 'sfu', 'transcendental'],
    },
    {
      d: 4,
      q: 'A block of 48 threads forms how many warps, and how many lanes are idle?',
      o: [
        '1 warp, 0 idle',
        '2 warps, 16 idle (48 → 2 warps of 32; the second warp has 16 inactive lanes)',
        '2 warps, 0 idle',
        '3 warps, 48 idle',
      ],
      a: 1,
      x: ['1 warp, 16 idle', '2 warps, 32 idle', '48 warps'],
      e: '48 threads = ceil(48/32) = 2 warps (64 lanes), with 64−48 = 16 inactive lanes in the second warp that still occupy scheduling slots. Block sizes that are multiples of 32 avoid this waste.',
      ref: 'Warp formation; partial warps',
      tags: ['warps', 'block-size', 'idle-lanes'],
    },
    {
      d: 5,
      q: 'Why might a kernel at 25% occupancy with 4 independent accumulators per thread match a 75%-occupancy version with 1 accumulator?',
      o: [
        'Occupancy is everything',
        'The 4 accumulators provide ILP that hides FMA latency WITHOUT extra warps; the low-occupancy/high-ILP version can keep the pipes as full as the high-occupancy/low-ILP one',
        'They differ by 3×',
        'Accumulators reduce memory',
      ],
      a: 1,
      x: [
        'ILP is irrelevant',
        'Occupancy must be 100%',
        'More warps always win',
      ],
      e: 'Latency hiding needs enough work in flight, supplied by warps (occupancy) OR independent instructions per thread (ILP). Four accumulators give the ILP to cover FMA latency at low occupancy, matching a higher-occupancy single-accumulator kernel — Volkov’s insight.',
      ref: 'Volkov; ILP vs occupancy',
      tags: ['ilp', 'occupancy', 'accumulators'],
    },
    {
      d: 4,
      q: 'A 64-thread block on an SM capped at 32 resident blocks and 2048 threads is limited by…',
      o: [
        'Threads (it reaches 2048)',
        'The block-count cap: 32 blocks × 64 = 2048 threads exactly, so it just fills threads at the block limit — but a 32-thread block would hit 32 blocks at only 1024 threads (block-cap-limited, wasting half)',
        'Registers',
        'Shared memory',
      ],
      a: 1,
      x: [
        'The grid size',
        'The warp size',
        'The clock',
      ],
      e: 'With 64-thread blocks, 32 blocks (the cap) gives exactly 2048 threads — fine. Smaller blocks (e.g. 32 threads) hit the block-count cap before filling the thread budget (32×32=1024), wasting occupancy. Hence prefer ≥128-thread blocks.',
      ref: 'Occupancy; block-count limit',
      tags: ['occupancy', 'block-size', 'block-cap'],
    },
    {
      d: 4,
      q: 'An uncoalesced load where a warp touches 32 different cache lines generates roughly how many memory transactions vs the coalesced ideal?',
      o: [
        'The same',
        '~32 transactions (one per line) vs ~1–4 for the coalesced case — a large over-fetch and instruction-replay penalty',
        '2',
        '0',
      ],
      a: 1,
      x: ['8', '16', '64'],
      e: 'If each lane’s address falls in a distinct cache line, the warp’s single load instruction replays for each line (~32 transactions) versus ~1–4 when contiguous/aligned. This memory divergence is a leading cause of poor memory-bound performance.',
      ref: 'Memory divergence; transaction count',
      tags: ['coalescing', 'replays', 'calculation'],
    },
    {
      d: 5,
      q: 'A Hopper cluster of 4 blocks needs all 4 co-resident. If the kernel’s occupancy allows only 2 blocks/SM and a GPC has 4 SMs, the cluster…',
      o: [
        'Cannot fit',
        'Fits — 4 SMs × 2 blocks = 8 block slots in the GPC, more than the 4 cluster blocks; co-residency within the GPC is satisfiable',
        'Needs 8 SMs',
        'Runs on the host',
      ],
      a: 1,
      x: [
        'Needs the whole GPU',
        'Requires 1 block/SM',
        'Is illegal',
      ],
      e: 'Cluster blocks must co-reside on SMs of one GPC. With 4 SMs × 2 resident blocks = 8 slots available and only 4 cluster blocks needed, the cluster fits. The cluster occupancy API verifies this for a given kernel/size.',
      ref: 'Hopper clusters; co-residency',
      tags: ['clusters', 'co-residency', 'calculation'],
    },
    {
      d: 4,
      q: 'A kernel shows ~0.5 eligible warps per scheduler per cycle with high long-scoreboard stalls. This indicates…',
      o: [
        'Plenty of parallelism',
        'The scheduler is starved — most warps are stalled on global loads (memory-latency/bandwidth bound), so few are ready to issue; increase MLP/occupancy or improve locality',
        'Compute-bound',
        'A barrier issue',
      ],
      a: 1,
      x: [
        'Register spilling',
        'A host stall',
        'Tensor starvation',
      ],
      e: 'Few eligible warps + long-scoreboard stalls = warps waiting on memory with little ready to issue. The kernel is memory-latency-bound; remedies are more outstanding requests (occupancy/ILP) or better locality (fewer/closer accesses).',
      ref: 'Nsight Compute; eligible warps and stalls',
      tags: ['eligible-warps', 'long-scoreboard', 'memory-bound'],
    },
    {
      d: 3,
      q: 'The maximum resident warps per SM on recent architectures is…',
      o: ['32', '48', '64', '128'],
      a: 2,
      x: ['16', '96', '256'],
      e: '64 warps (2048 threads) is the typical max resident per SM on recent GPUs. Achieved occupancy = active warps / 64. Resource limits (registers/shared memory/blocks) usually keep you below the max.',
      ref: 'CUDA Compute Capabilities; max warps',
      tags: ['max-warps', 'occupancy', 'sm'],
    },
    {
      d: 5,
      q: 'In a warp-specialized GEMM with 1 producer warpgroup (TMA) and 2 consumer warpgroups (wgmma), the producer feeds both consumers because…',
      o: [
        'Producers do math',
        'A TMA copy is issued by one thread and runs in hardware, so a single producer warpgroup can supply enough tiles to keep two consumer warpgroups’ tensor cores busy — balancing the pipeline',
        'Consumers copy data',
        'It uses the CPU',
      ],
      a: 1,
      x: [
        'It reduces occupancy',
        'Producers use tensor cores',
        'It avoids barriers',
      ],
      e: 'Because TMA offloads the copy to hardware (one thread triggers a tile transfer), few producer warps can feed many consumer (wgmma) warps. The 1:2 split balances production (TMA bandwidth) against consumption (tensor-core rate) to keep the cores fed.',
      ref: 'CUTLASS Hopper; warp specialization',
      tags: ['warp-specialization', 'tma', 'wgmma'],
    },
    {
      d: 4,
      q: 'Increasing a kernel from 32 to 40 registers/thread drops resident blocks from 8 to 6 (an occupancy cliff). This happens because…',
      o: [
        'Registers are unlimited',
        'Per-warp register allocation rounds up to a granularity; crossing a threshold reduces how many warps/blocks fit, so a small register increase causes a discrete occupancy drop',
        'Shared memory changed',
        'The grid shrank',
      ],
      a: 1,
      x: [
        'Threads doubled',
        'It is a profiler bug',
        'ECC',
      ],
      e: 'Registers are allocated per warp in granular chunks, so usage rounds up and crossing a granularity boundary cuts resident warps/blocks in a step — the occupancy cliff. Watching ptxas -v and the occupancy calculator helps you stay below such thresholds.',
      ref: 'Occupancy cliff; register granularity',
      tags: ['occupancy-cliff', 'registers', 'granularity'],
    },
    {
      d: 4,
      q: 'A persistent kernel on a 132-SM GPU with occupancy of 2 blocks/SM launches how many blocks (one wave)?',
      o: ['132', '264', '2048', '396'],
      a: 1,
      x: ['66', '512', '128'],
      e: '132 SMs × 2 resident blocks = 264 blocks fill the GPU exactly once. A persistent kernel launches that many and loops over the work, amortizing launch overhead and enabling grid-wide cooperation without oversubscribing the SMs.',
      ref: 'Persistent kernels; grid sizing',
      tags: ['persistent-kernel', 'grid-sizing', 'calculation'],
    },
    {
      d: 4,
      q: 'Nsight shows high "Barrier" stalls in a tiled kernel. The most direct remedy is…',
      o: [
        'More memory accesses',
        'Reduce/balance the work so threads reach __syncthreads more uniformly, and cut unnecessary barriers (e.g. double-buffer to overlap the next load with compute) — barriers wait for the slowest warp',
        'More registers',
        'A larger grid',
      ],
      a: 1,
      x: [
        'Disable the L2',
        'Use FP64',
        'Add atomics',
      ],
      e: 'Barrier stalls mean time lost waiting at __syncthreads for stragglers. Balance per-thread work before each barrier and remove avoidable syncs (double buffering overlaps load and compute), reducing the synchronization overhead.',
      ref: 'Nsight Compute; barrier stalls',
      tags: ['barrier-stall', 'syncthreads', 'tiling'],
    },
    {
      d: 4,
      q: 'redux.sync gives a warp-wide INTEGER reduction in 1 instruction; a float warp reduction instead needs…',
      o: [
        '1 instruction too',
        '~5 shuffle (shfl_xor) steps, because there is no hardware float warp-reduction — floats use the log2(32) shuffle tree',
        '32 instructions',
        'A barrier',
      ],
      a: 1,
      x: ['2 instructions', 'Global atomics', '10 steps'],
      e: 'redux.sync (Ampere+) does integer add/min/max/logical warp reductions in hardware (one instruction). Floating-point has no such instruction, so FP warp reductions use the 5-step shfl_xor butterfly. cg::reduce picks the right path automatically.',
      ref: 'PTX redux.sync; warp reductions',
      tags: ['redux', 'reduction', 'integer-vs-float'],
    },
    {
      d: 5,
      q: 'Why can a kernel be simultaneously "not memory-bound" (DRAM 30%) and "not compute-bound" (SM 30%) yet slow?',
      o: [
        'It is optimal',
        'It is latency-bound: too little work in flight (low occupancy/ILP), so neither resource is saturated while warps stall — increase parallelism (warps and/or ILP) to fill the pipelines',
        'It is a bug',
        'It needs FP64',
      ],
      a: 1,
      x: [
        'It is bandwidth-bound',
        'It is host-bound',
        'It has bank conflicts',
      ],
      e: 'Both resources idle with frequent stalls = latency-bound. There aren’t enough eligible warps/independent instructions to hide latencies. Raise occupancy or expose more ILP/MLP so the scheduler always has something ready to issue.',
      ref: 'Latency-bound diagnosis; Speed of Light',
      tags: ['latency-bound', 'occupancy', 'ilp'],
    },
    {
      d: 4,
      q: 'A block reduction does 8 shared-memory tree steps for 256 threads then 5 shuffle steps for the last warp. The shuffle steps replace…',
      o: [
        'Global atomics',
        'The shared-memory + __syncthreads steps for the final 32 elements — once down to one warp, shuffles (no shared memory/barriers) finish the reduction faster',
        'The whole reduction',
        'The load phase',
      ],
      a: 1,
      x: [
        'Constant memory',
        'The grid',
        'The optimizer',
      ],
      e: 'log2(256)=8 shared-memory halving steps reduce to 32 partials; the last warp finishes with 5 shuffle steps, dropping the shared-memory traffic and __syncthreads for those final stages — a standard reduction optimization.',
      ref: 'Harris reduction; warp-synchronous tail',
      tags: ['reduction', 'shuffle', 'syncthreads'],
    },
    {
      d: 4,
      q: 'A 2D block of (16, 16) forms warps by linearizing as x + y*16. Warp 0 therefore contains threads with…',
      o: [
        'x in [0,31], y=0',
        'y in {0,1}, x in [0,15] — the first 32 linear indices span 2 full rows (16 wide), so a warp covers 2 rows',
        'x=0, y in [0,31]',
        'Random threads',
      ],
      a: 1,
      x: [
        'One row only',
        'A column',
        'All 256 threads',
      ],
      e: 'Linear index = x + y*16; the first 32 indices are (x=0..15,y=0) and (x=0..15,y=1) — two rows. So a warp spans 2 rows of the 16-wide block. This affects coalescing (the warp’s x access pattern) and divergence analysis.',
      ref: 'CUDA warp linearization; 2D blocks',
      tags: ['warp', '2d-block', 'linearization'],
    },
    {
      d: 4,
      q: 'A kernel’s achieved occupancy is 30% while theoretical is 75%. With many blocks and a long runtime, the gap most likely comes from…',
      o: [
        'Register spilling',
        'Load imbalance / divergence causing uneven warp completion (so average active warps is well below the resource ceiling), rather than the tail effect (which a long, many-block kernel minimizes)',
        'Too few blocks',
        'Bank conflicts only',
      ],
      a: 1,
      x: [
        'A profiler bug',
        'High register use',
        'ECC',
      ],
      e: 'For a long, many-block kernel the tail is small, so a big achieved-vs-theoretical gap points to imbalance/divergence: warps finishing at different times lowers the average active count. Investigate work distribution and control flow.',
      ref: 'Achieved vs theoretical occupancy; imbalance',
      tags: ['achieved-occupancy', 'imbalance', 'divergence'],
    },
    {
      d: 5,
      q: 'Dynamic parallelism launches a child grid per parent thread for an irregular workload. The overhead concern is…',
      o: [
        'None',
        'Per-launch overhead × (potentially many) child launches can dominate; coarsening (one child per block/warp, batched work) or flattening the algorithm reduces the number of device launches',
        'It uses FP64',
        'It needs the host',
      ],
      a: 1,
      x: [
        'Lower occupancy only',
        'Bank conflicts',
        'It is free',
      ],
      e: 'Each device-side launch has overhead and pending-launch buffer limits. Launching per-thread can explode the launch count; better to coarsen (per-block/warp child grids, batched work) or restructure so dynamic parallelism’s benefit outweighs the launch cost.',
      ref: 'Dynamic parallelism; launch overhead',
      tags: ['dynamic-parallelism', 'overhead', 'irregular'],
    },
    {
      d: 4,
      q: 'For a memory-bound copy at 50% of peak bandwidth with 100% occupancy, the FIRST thing to check is…',
      o: [
        'Register count',
        'Coalescing/vectorization: at full occupancy the limiter isn’t latency hiding; sub-peak bandwidth usually means uncoalesced/misaligned/non-vectorized accesses (over-fetch)',
        'Tensor-core usage',
        'The block-per-SM limit',
      ],
      a: 1,
      x: [
        'Shared-memory size',
        'The grid dimensions',
        'Divergence in a branchless copy',
      ],
      e: 'Full occupancy means latency is hidden, so 50% bandwidth points to memory inefficiency: check that the copy is coalesced, aligned, and vectorized (float4). Fixing the access pattern typically pushes a copy toward 80–90% of peak.',
      ref: 'Memory-bound analysis; coalescing',
      tags: ['coalescing', 'bandwidth', 'occupancy'],
    },
    {
      d: 4,
      q: 'cooperative_groups::reduce on a 32-thread tile on an Ampere GPU for an int sum will likely…',
      o: [
        'Use global atomics',
        'Lower to a single redux.sync hardware instruction (integer reduction), faster than the 5-step shuffle tree',
        'Use shared memory',
        'Run on the host',
      ],
      a: 1,
      x: [
        'Use 32 instructions',
        'Need a barrier',
        'Use FP64',
      ],
      e: 'For integer reductions on Ampere+, cg::reduce uses the hardware redux.sync (one instruction) instead of a shuffle tree — faster and simpler. The cooperative-groups abstraction selects the optimal available implementation portably.',
      ref: 'cg::reduce; redux.sync',
      tags: ['cooperative-groups', 'redux', 'reduction'],
    },
    {
      d: 5,
      q: 'A GEMM at 80% tensor-pipe utilization but 55% of peak FLOP/s most likely suffers from…',
      o: [
        'DRAM saturation',
        'Bubbles between MMAs — operand-feeding stalls (shared-memory loads/ldmatrix, barriers); deeper pipelining/better SMEM feeding (conflict-free ldmatrix) closes the gap',
        'Low occupancy',
        'Branch divergence',
      ],
      a: 1,
      x: [
        'Register spilling',
        'A host stall',
        'Bank conflicts in registers',
      ],
      e: 'High issue/utilization but lower achieved FLOP/s means the tensor cores idle between MMAs waiting on operands. Improving the feed (deeper async pipeline, conflict-free swizzled ldmatrix, larger tiles) raises effective throughput toward the pipe utilization.',
      ref: 'Tensor-core feeding; GEMM analysis',
      tags: ['tensor-core', 'utilization', 'gemm'],
    },
    {
      d: 4,
      q: 'Why does choosing block dimensions (32, 4) often beat (4, 32) for a 2D kernel reading row-major data?',
      o: [
        'Total threads differ',
        'Warps form along x; with x=32 a warp spans 32 contiguous columns (coalesced reads of a row), whereas x=4 scatters a warp across 8 rows (strided, uncoalesced)',
        'It uses more registers',
        'It avoids divergence',
      ],
      a: 1,
      x: [
        'It enables tensor cores',
        'It reduces occupancy',
        'For FP64',
      ],
      e: 'With x=32, a warp’s threads cover 32 contiguous elements along the unit-stride (row) dimension — coalesced. With x=4, a warp spans 8 rows (stride = row length per row), scattering accesses. Put the contiguous dimension on x for coalescing.',
      ref: 'Block shape; coalescing',
      tags: ['block-shape', 'coalescing', '2d'],
    },
  ],
  { idPrefix: 'execution-f' }
)
