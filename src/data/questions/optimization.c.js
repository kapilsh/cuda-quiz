import { defineQuestions } from './_helpers.js'

// Kernel Optimization — batch C. Instruction-level tricks, reduction refinements,
// tensor-core tiling/alignment, carveout. Distinct from optimization.js/.b.js.
export default defineQuestions(
  'optimization',
  [
    {
      d: 3,
      q: 'Replacing an if/else that selects one of two values with a branchless expression (e.g. fminf/fmaxf or a select) helps because…',
      o: [
        'It uses less memory',
        'Avoids divergence; both computed, one selected',
        'It increases occupancy',
        'It enables tensor cores',
      ],
      a: 1,
      x: [
        'It reduces register usage',
        'It coalesces memory',
        'It is required by the compiler',
      ],
      e: 'Short data-dependent branches cause divergence. Computing both options and selecting (min/max/select/predication) keeps all lanes active. The compiler often does this automatically for tiny bodies, but explicit branchless code guarantees it.',
      ref: 'CUDA Best Practices Guide; branchless code',
      tags: ['branchless', 'divergence', 'select'],
    },
    {
      d: 4,
      q: 'For tensor-core GEMM, padding M, N, K to multiples of the MMA tile (e.g. 8 or 16) matters because…',
      o: [
        'It reduces memory',
        'Non-multiples waste tensor cores on partial tiles',
        'It improves coalescing only',
        'It is required to compile',
      ],
      a: 1,
      x: [
        'It increases precision',
        'It avoids atomics',
        'It disables the L2',
      ],
      e: 'Tensor cores work on fixed tile shapes. Dimensions that aren’t multiples force partial/predicated tiles or library fallbacks, losing efficiency. Padding (or choosing shapes) to MMA multiples keeps the math units fully utilized.',
      ref: 'cuBLAS/CUTLASS tiling; tensor-core alignment',
      tags: ['tensor-core', 'padding', 'gemm', 'tiling'],
    },
    {
      d: 4,
      q: 'The reduction optimization "first add during load" (each thread loads two elements and adds them before the tree) improves performance by…',
      o: [
        'Reducing precision',
        'Halves threads and works during the load phase',
        'Avoiding __syncthreads',
        'Using atomics',
      ],
      a: 1,
      x: [
        'Eliminating shared memory',
        'Increasing divergence',
        'Disabling the L2',
      ],
      e: 'In Harris’s reduction, having each thread add two (or more) loaded elements before the shared-memory tree halves the launch and keeps threads busy during loading, raising effective bandwidth — a key step toward peak reduction throughput.',
      ref: 'Harris, "Optimizing Parallel Reduction in CUDA"',
      tags: ['reduction', 'first-add', 'bandwidth'],
    },
    {
      d: 4,
      q: 'Completely unrolling the last 6 steps of a block reduction (the final warp) lets you…',
      o: [
        'Use more shared memory',
        'Drop block barriers for the final warp steps',
        'Avoid loading data',
        'Use tensor cores',
      ],
      a: 1,
      x: [
        'Increase divergence',
        'Use atomics',
        'Reduce precision',
      ],
      e: 'Once the reduction shrinks to one warp, threads are in a single warp so block barriers are unnecessary; unrolling those steps (with shuffles or volatile/__syncwarp) removes loop and __syncthreads overhead — a classic final optimization.',
      ref: 'Harris reduction; warp-synchronous final stage',
      tags: ['reduction', 'unrolling', 'warp-synchronous'],
    },
    {
      d: 3,
      q: 'Hoisting a loop-invariant computation (e.g. a reciprocal used every iteration) out of the loop is beneficial because…',
      o: [
        'It uses more registers always',
        'Computes the value once, not every iteration',
        'It improves coalescing',
        'It enables tensor cores',
      ],
      a: 1,
      x: [
        'It increases divergence',
        'It reduces precision',
        'It is required for atomics',
      ],
      e: 'Loop-invariant code motion computes unchanging values once. The compiler often does this, but complex cases (or aliasing concerns) may need manual hoisting, especially for expensive ops like division/reciprocal/transcendentals.',
      ref: 'CUDA Best Practices Guide; loop-invariant code motion',
      tags: ['hoisting', 'loop-invariant', 'optimization'],
    },
    {
      d: 3,
      q: 'When dividing by the same value many times in a loop, a strength-reduction trick is to…',
      o: [
        'Use double precision',
        'Precompute the reciprocal, then multiply',
        'Use atomics',
        'Move the divisor to constant memory',
      ],
      a: 1,
      x: [
        'Use a lookup table for the quotient',
        'Cast to int',
        'Unroll the loop',
      ],
      e: 'GPU division is multi-instruction and slow. If the divisor is constant within the loop, precompute r = 1/d and multiply — turning many divides into one divide plus cheap multiplies (watch accuracy with --prec-div=false / fast math).',
      ref: 'CUDA Best Practices Guide §G; strength reduction',
      tags: ['strength-reduction', 'reciprocal', 'division'],
    },
    {
      d: 4,
      q: 'cudaFuncSetAttribute(kernel, cudaFuncAttributePreferredSharedMemoryCarveout, percent) is used to…',
      o: [
        'Set the grid size',
        'Bias the L1/shared split per kernel',
        'Pin the kernel to an SM',
        'Increase registers',
      ],
      a: 1,
      x: [
        'Disable the L2',
        'Set the stream priority',
        'Enable tensor cores',
      ],
      e: 'On Volta+ where L1 and shared memory share SRAM, this attribute hints the preferred carveout percentage. A shared-memory-heavy tiled kernel may prefer more shared; a cache-friendly kernel may prefer more L1.',
      ref: 'CUDA Runtime API (cudaFuncAttributePreferredSharedMemoryCarveout)',
      tags: ['carveout', 'l1-shared', 'tuning'],
    },
    {
      d: 4,
      q: 'For tensor-core GEMM, aligning the matrices’ leading dimension (row/column stride) to a multiple of, e.g., 16 elements matters because…',
      o: [
        'It reduces memory',
        'Misalignment breaks the wide aligned loads',
        'It changes the result',
        'It increases occupancy',
      ],
      a: 1,
      x: [
        'It avoids atomics',
        'It enables FP64',
        'It is required to compile',
      ],
      e: 'Tensor-core load instructions assume aligned operands. An unaligned leading dimension prevents 128-bit aligned loads, degrading the feed to the math units. cuBLAS/CUTLASS document alignment requirements for peak performance.',
      ref: 'cuBLAS/CUTLASS alignment requirements',
      tags: ['alignment', 'leading-dimension', 'tensor-core', 'gemm'],
    },
    {
      d: 3,
      q: 'Processing multiple elements per thread in a reduction (grid-stride accumulate into a register, then reduce) helps because…',
      o: [
        'It avoids shared memory',
        'Amortizes overhead with coalesced loads',
        'It increases divergence',
        'It uses tensor cores',
      ],
      a: 1,
      x: [
        'It reduces precision',
        'It needs atomics',
        'It disables the L2',
      ],
      e: 'Each thread sums many elements (grid-stride) into a register before the block tree reduction. This raises arithmetic work per launched thread and sustains high read bandwidth, making the reduction bandwidth-bound (its ideal).',
      ref: 'Harris reduction; grid-stride reduction',
      tags: ['reduction', 'grid-stride', 'bandwidth'],
    },
    {
      d: 4,
      q: 'Computing x*x instead of powf(x, 2.0f) is a micro-optimization because…',
      o: [
        'powf is more accurate',
        'powf is costly; a multiply is far cheaper',
        'They compile identically always',
        'powf uses tensor cores',
      ],
      a: 1,
      x: [
        'x*x overflows less',
        'powf is not available on the GPU',
        'It improves coalescing',
      ],
      e: 'pow/powf handle arbitrary exponents via log/exp and are slow. For small integer powers, explicit multiplies (x*x, x*x*x) are much cheaper. Some compilers optimize constant-integer pow, but writing the multiply is safe and clear.',
      ref: 'CUDA Best Practices Guide §G; transcendental cost',
      tags: ['pow', 'strength-reduction', 'intrinsics'],
    },
    {
      d: 5,
      q: 'In a warp-level GEMM (each warp computes a tile using mma.sync), operands are typically staged from shared memory into registers via…',
      o: [
        'Global loads',
        'ldmatrix from shared memory',
        'Constant memory',
        'Texture fetches',
      ],
      a: 1,
      x: [
        'atomicAdd',
        'cudaMemcpy',
        'A reduction',
      ],
      e: 'ldmatrix cooperatively loads small matrix tiles from shared memory into the distributed register fragments mma.sync needs (handling the required transpose/permutation), efficiently feeding the tensor cores in CUTLASS-style warp GEMM.',
      ref: 'PTX ISA (ldmatrix); CUTLASS warp-level GEMM',
      tags: ['ldmatrix', 'mma', 'warp-gemm', 'tensor-core'],
    },
    {
      d: 3,
      q: 'Prefetching the next loop iteration’s global data into registers before computing on the current data…',
      o: [
        'Reduces memory traffic',
        'Overlaps the next load with current compute',
        'Avoids coalescing',
        'Uses tensor cores',
      ],
      a: 1,
      x: [
        'Increases divergence',
        'Reduces precision',
        'Disables the L1',
      ],
      e: 'Issuing the next iteration’s load early (into a register) while computing on already-loaded data overlaps memory and compute, hiding latency even at modest occupancy — a register-level analog of shared-memory double buffering.',
      ref: 'Software pipelining; CUDA Best Practices Guide',
      tags: ['prefetch', 'software-pipelining', 'latency-hiding'],
    },
    {
      d: 4,
      q: 'Minimizing host↔device synchronization points (e.g. avoiding cudaDeviceSynchronize in a loop) helps because…',
      o: [
        'Sync is free',
        'Each sync stalls the CPU and drains the GPU',
        'It reduces registers',
        'It improves coalescing',
      ],
      a: 1,
      x: [
        'It increases occupancy',
        'It enables tensor cores',
        'It avoids atomics',
      ],
      e: 'Frequent synchronization forces the host to wait and empties the GPU’s queued work, killing overlap. Issue work asynchronously, use events/streams for ordering, and synchronize only when you actually need results on the host.',
      ref: 'CUDA Best Practices Guide; synchronization overhead',
      tags: ['synchronization', 'overlap', 'host-device'],
    },
    {
      d: 4,
      q: 'A kernel does 10 FLOPs and moves 40 bytes per element (intensity 0.25 FLOP/byte). On a GPU with 3 TB/s and 60 TFLOP/s, it is…',
      o: [
        'Compute-bound',
        'Memory-bound: below the 20 ridge',
        'Perfectly balanced',
        'Latency-bound',
      ],
      a: 1,
      x: [
        'Limited by registers',
        'Limited by occupancy',
        'Compute-bound at high occupancy',
      ],
      e: 'Ridge = peak_FLOPs / peak_BW = 20 FLOP/byte. The kernel’s 0.25 is far below, so it sits on the bandwidth roof (memory-bound). Speedups require reducing bytes (fusion, reuse, lower precision), not more compute.',
      ref: 'Roofline model',
      tags: ['roofline', 'memory-bound', 'arithmetic-intensity'],
    },
    {
      d: 3,
      q: 'Choosing INT32 index arithmetic over INT64 where the range allows can help a hot loop because…',
      o: [
        'INT64 is unsupported',
        '64-bit ops cost more than 32-bit',
        'INT32 is more accurate',
        'It enables tensor cores',
      ],
      a: 1,
      x: [
        'INT64 disables coalescing',
        'INT32 avoids divergence',
        'It uses constant memory',
      ],
      e: '64-bit integer arithmetic is more expensive (multiple ops, more registers) than 32-bit. When the index range safely fits in 32 bits, using int/uint for inner-loop addressing trims overhead — but switch to 64-bit when overflow is possible.',
      ref: 'CUDA Best Practices Guide; integer arithmetic cost',
      tags: ['integer-arithmetic', 'indexing', 'micro-optimization'],
    },
    {
      d: 5,
      q: 'In CUTLASS, increasing the threadblock tile (e.g. 128×128 vs 64×64) generally raises GEMM efficiency until…',
      o: [
        'It never helps',
        'Pressure caps occupancy or tiles stay unfilled',
        'The grid is too large',
        'Tensor cores turn off',
      ],
      a: 1,
      x: [
        'Precision drops',
        'Atomics increase',
        'The L2 disables',
      ],
      e: 'Bigger tiles increase reuse (higher arithmetic intensity) but cost shared memory and registers, lowering occupancy and needing enough work to fill them. The optimal tile balances reuse, occupancy, and the problem’s shape — hence CUTLASS’s many tile configs.',
      ref: 'CUTLASS tile selection; GEMM tuning',
      tags: ['tile-size', 'gemm', 'cutlass', 'occupancy'],
    },
    {
      d: 3,
      q: 'For an elementwise kernel, using a grid-stride loop with a grid sized to the GPU (rather than one thread per element) helps because…',
      o: [
        'It avoids bounds checks',
        'A bounded, occupancy-tuned grid fills the GPU',
        'It increases divergence',
        'It needs no indexing',
      ],
      a: 1,
      x: [
        'It uses tensor cores',
        'It reduces precision',
        'It avoids global memory',
      ],
      e: 'A grid-stride loop decouples grid size from N, so you launch enough blocks to fill the device and each thread strides over multiple elements. It tunes occupancy independently and handles any N without launching billions of blocks.',
      ref: 'NVIDIA "Grid-Stride Loops"',
      tags: ['grid-stride', 'elementwise', 'occupancy'],
    },
    {
      d: 4,
      q: 'A memory-bound elementwise kernel achieves 60% of peak bandwidth. Before chasing occupancy, the FIRST thing to check is…',
      o: [
        'Register count',
        'Whether accesses are coalesced and vectorized',
        'Tensor-core usage',
        'The block-per-SM limit',
      ],
      a: 1,
      x: [
        'Shared-memory size',
        'The grid dimensions',
        'Divergence in a branchless kernel',
      ],
      e: 'For bandwidth-bound kernels, sub-peak throughput most often comes from imperfect coalescing/alignment or non-vectorized loads. Fix the access pattern (vectorize, align, ensure stride-1) before considering occupancy, which rarely is the limiter here.',
      ref: 'CUDA Best Practices Guide; memory throughput',
      tags: ['coalescing', 'bandwidth', 'vectorization'],
    },
    {
      d: 5,
      q: 'In FlashAttention, keeping Q tile in registers and streaming K,V tiles works well because…',
      o: [
        'Q is larger than K and V',
        'Q is reused against every K/V tile (high reuse)',
        'K and V are constants',
        'It avoids softmax',
      ],
      a: 1,
      x: [
        'Registers are off-chip',
        'It needs no shared memory',
        'It uses FP64',
      ],
      e: 'The query tile participates in every block of keys/values, so holding it in registers (high reuse) while streaming K/V once each (low reuse) and updating the online softmax keeps the inner loop compute-bound and HBM-light.',
      ref: 'Dao et al., "FlashAttention"',
      tags: ['flash-attention', 'registers', 'reuse', 'tiling'],
    },
    {
      d: 3,
      q: 'Why might converting an array-of-structs hot path to struct-of-arrays be one of the highest-impact changes?',
      o: [
        'It reduces total memory',
        'Turns strided accesses into coalesced stride-1',
        'It enables tensor cores',
        'It reduces register usage',
      ],
      a: 1,
      x: [
        'It avoids atomics',
        'It increases occupancy',
        'It removes divergence',
      ],
      e: 'When a warp reads one field across elements, SoA makes those reads contiguous (coalesced) whereas AoS strides by the struct size, wasting most of each cache line. For memory-bound kernels this layout change can be transformative.',
      ref: 'CUDA Best Practices Guide; SoA vs AoS',
      tags: ['soa-aos', 'coalescing', 'data-layout'],
    },
    {
      d: 4,
      q: 'Selecting a block size that maximizes occupancy via cudaOccupancyMaxPotentialBlockSize is a good default, but you might override it when…',
      o: [
        'Never; it is always optimal',
        'The algorithm needs a specific tile/block shape',
        'The grid is small',
        'You use streams',
      ],
      a: 1,
      x: [
        'You use constant memory',
        'The kernel is tiny',
        'You disable the L2',
      ],
      e: 'The occupancy API maximizes theoretical occupancy, but real kernels often need specific block shapes for tiling/coalescing, or deliberately use fewer larger threads for ILP/register reuse. Use it as a starting point, then profile.',
      ref: 'CUDA Runtime API (Occupancy); tuning',
      tags: ['occupancy-api', 'block-size', 'tuning'],
    },
    {
      d: 4,
      q: 'A common reason a fused kernel UNDERperforms two separate kernels is…',
      o: [
        'Fusion never helps',
        'Combined resource needs drop occupancy/spill',
        'It uses more global memory',
        'It cannot use tensor cores',
      ],
      a: 1,
      x: [
        'It increases launch overhead',
        'It disables coalescing',
        'Fusion adds atomics',
      ],
      e: 'Fusion saves DRAM round-trips but increases per-thread resource demand (more live values). If that tanks occupancy or causes spills, the kernel can be slower than two simpler kernels. Profile both; resource pressure is the usual culprit.',
      ref: 'Kernel fusion trade-offs; Nsight Compute',
      tags: ['fusion', 'register-pressure', 'tradeoff'],
    },
    {
      d: 3,
      q: 'Using fminf/fmaxf for a clamp (fmaxf(lo, fminf(x, hi))) instead of two ifs is preferred because…',
      o: [
        'It is more accurate',
        'Branchless min/max; no divergence',
        'It uses tensor cores',
        'It reduces memory',
      ],
      a: 1,
      x: [
        'It increases occupancy',
        'It avoids registers',
        'It is required by IEEE',
      ],
      e: 'min/max are single hardware instructions with no branching, so a clamp via fminf/fmaxf stays converged and is cheap. The if-based version risks divergence and more instructions for the same result.',
      ref: 'CUDA Best Practices Guide; branchless clamp',
      tags: ['clamp', 'branchless', 'min-max'],
    },
    {
      d: 5,
      q: 'Why do high-performance GEMM kernels often run at relatively LOW occupancy (e.g. 25–50%)?',
      o: [
        'They are poorly written',
        'Register-blocked tiles trade occupancy for ILP',
        'They are memory-bound',
        'They avoid tensor cores',
      ],
      a: 1,
      x: [
        'They use tiny blocks',
        'They disable shared memory',
        'They need cooperative launch',
      ],
      e: 'GEMM micro-kernels deliberately use many registers per thread for accumulator tiles and operand reuse, capping occupancy. The high ILP and reuse keep the pipelines full, so peak throughput is reached at modest occupancy — the classic counter-example to "more occupancy is better."',
      ref: 'CUTLASS; Volkov; GEMM design',
      tags: ['occupancy', 'gemm', 'register-blocking', 'ilp'],
    },
    {
      d: 4,
      q: 'For a transpose kernel, the optimization that most improves global write coalescing is…',
      o: [
        'Using atomics',
        'Stage in padded shared memory; write coalesced',
        'Increasing the block size to 1024',
        'Using constant memory',
      ],
      a: 1,
      x: [
        'Using double precision',
        'Disabling the L1',
        'Using texture memory',
      ],
      e: 'A naive transpose has either reads or writes strided. Loading a tile coalesced into shared memory (padded to avoid bank conflicts) and writing it out transposed keeps BOTH global accesses coalesced — the standard high-bandwidth transpose.',
      ref: 'NVIDIA "Efficient Matrix Transpose"; PMPP Ch. 6',
      tags: ['transpose', 'coalescing', 'shared-memory'],
    },
    {
      d: 4,
      q: 'Issuing several independent global loads before using any of them (load batching) primarily improves…',
      o: [
        'Coalescing',
        'Memory-level parallelism',
        'Occupancy',
        'Precision',
      ],
      a: 1,
      x: [
        'Register count',
        'Divergence',
        'Tensor-core usage',
      ],
      e: 'Batching independent loads exposes MLP: their latencies overlap, so the warp waits roughly once for many results. This complements occupancy-based hiding and is why unrolling/multiple accumulators help even at low occupancy.',
      ref: 'CUDA Best Practices Guide; memory-level parallelism',
      tags: ['mlp', 'load-batching', 'latency-hiding'],
    },
    {
      d: 3,
      q: 'Marking inputs const __restrict__ in a memory-bound kernel can speed it up because the compiler may…',
      o: [
        'Move them to the host',
        'Assume no aliasing; use the read-only cache',
        'Increase occupancy',
        'Use tensor cores',
      ],
      a: 1,
      x: [
        'Compress the data',
        'Disable the L2',
        'Force coalescing',
      ],
      e: 'const __restrict__ tells the compiler the data is read-only and non-aliasing, enabling read-only-cache (LDG) use and load reuse/reordering. For reuse-heavy, read-only inputs this can noticeably improve memory-bound kernels.',
      ref: 'CUDA Best Practices Guide; __restrict__ + const',
      tags: ['restrict', 'read-only-cache', 'aliasing'],
    },
    {
      d: 5,
      q: 'Persistent-kernel GEMM (Stream-K style) reduces the "tail" inefficiency of tile-per-block GEMM by…',
      o: [
        'Using larger tiles',
        'A fixed wave loops over a balanced MAC share',
        'Disabling tensor cores',
        'Lowering precision',
      ],
      a: 1,
      x: [
        'Using one block',
        'Avoiding shared memory',
        'Adding atomics everywhere',
      ],
      e: 'Fixed tile-per-block schedules leave a ragged final wave (tail). Persistent/Stream-K kernels distribute the flattened MAC iteration space evenly across a full wave of resident blocks, eliminating wave quantization at the cost of partial-tile reductions.',
      ref: 'Stream-K; persistent GEMM',
      tags: ['persistent-kernel', 'stream-k', 'tail-effect', 'gemm'],
    },
    {
      d: 4,
      q: 'When a kernel is bound by special-function (SFU) throughput (heavy expf/sinf), a useful optimization is to…',
      o: [
        'Add more global loads',
        'Cut transcendental count or use __expf',
        'Increase the block size',
        'Use atomics',
      ],
      a: 1,
      x: [
        'Disable the L2',
        'Use double precision',
        'Add __syncthreads',
      ],
      e: 'SFUs have lower throughput than FMA units, so transcendental-heavy kernels bottleneck there. Cut the number of such ops (factor/reuse) and, where tolerable, use the fast intrinsic variants (__expf/__sinf, --use_fast_math).',
      ref: 'CUDA C++ Programming Guide §G; SFU-bound kernels',
      tags: ['sfu', 'intrinsics', 'transcendental'],
    },
  ],
  { idPrefix: 'optimization-c' }
)
