import { defineQuestions } from './_helpers.js'

// Libraries — batch F. Practical "which API / how to" scenarios across cuBLAS,
// cuDNN, CUB/Thrust, TensorRT, vLLM, CUTLASS. Distinct from libraries.js/.b/.c/.d/.e.
export default defineQuestions(
  'libraries',
  [
    {
      d: 3,
      q: 'You pass a row-major C++ matrix to cublasSgemm and get a transposed/garbled result. The clean fix is…',
      o: [
        'Manually transpose every matrix',
        'Exploit that cuBLAS is column-major: compute Cᵀ = BᵀAᵀ by swapping operands and using the transpose flags / swapped dimensions, so row-major data is interpreted correctly',
        'Use FP64',
        'Use the CPU',
      ],
      a: 1,
      x: [
        'Change the GPU',
        'Use cuFFT',
        'Disable tensor cores',
      ],
      e: 'cuBLAS is column-major (Fortran). A row-major M×N matrix is a column-major N×M; computing (A·B)ᵀ = Bᵀ·Aᵀ with operands swapped lets cuBLAS produce the right result without explicit transposes — the standard idiom.',
      ref: 'cuBLAS column-major; row-major interop',
      tags: ['cublas', 'column-major', 'transpose'],
    },
    {
      d: 4,
      q: 'For 1000 independent 32×32 GEMMs, the fastest cuBLAS path is…',
      o: [
        'A loop of cublasSgemm',
        'cublasGemmStridedBatched (or grouped GEMM) — one launch processes the whole batch, amortizing per-call overhead and filling the GPU',
        'cuFFT',
        'The CPU',
      ],
      a: 1,
      x: [
        'cuSPARSE',
        'A single big GEMM',
        'cuRAND',
      ],
      e: 'Looping incurs per-call launch overhead and underutilization for tiny matrices. Batched/grouped GEMM runs all matrices in one launch with internal scheduling — far faster for many small/medium GEMMs (common in attention/MoE).',
      ref: 'cuBLAS batched GEMM',
      tags: ['cublas', 'batched-gemm', 'small-matrices'],
    },
    {
      d: 4,
      q: 'Your inference model has DYNAMIC input shapes and cuDNN’s exhaustive "find" mode is too slow per call. A better approach is…',
      o: [
        'Always exhaustive search',
        'Use heuristic algorithm selection (predicts a good algorithm fast) and/or cache the chosen algorithm per shape — avoiding re-benchmarking every call',
        'Disable cuDNN',
        'Use FP64',
      ],
      a: 1,
      x: [
        'Use the CPU',
        'Recompile the model',
        'Lower precision',
      ],
      e: 'Exhaustive find benchmarks all algorithms per shape (slow setup). Heuristics predict a strong algorithm instantly; caching per (shape, dtype, layout) avoids re-selecting for recurring shapes. Shape churn (many unique shapes) makes this costly — minimize it.',
      ref: 'cuDNN heuristic vs find; caching',
      tags: ['cudnn', 'heuristics', 'dynamic-shapes'],
    },
    {
      d: 4,
      q: 'To run a Thrust reduce asynchronously on a specific stream (overlapping with other GPU work), you use…',
      o: [
        'thrust::reduce only',
        'thrust::cuda::par.on(stream) as the execution policy (or thrust::async::reduce) so it runs on that stream instead of synchronizing the default stream',
        'The CPU',
        'cudaDeviceSynchronize',
      ],
      a: 1,
      x: [
        'cuBLAS',
        'A custom kernel only',
        'Constant memory',
      ],
      e: 'Passing par.on(stream) runs the algorithm on that stream (overlapping/ordered with your work); thrust::async returns a future. Without it, Thrust may use the default stream and synchronize, killing concurrency.',
      ref: 'Thrust execution policies; async',
      tags: ['thrust', 'execution-policy', 'streams'],
    },
    {
      d: 4,
      q: 'CUB device primitives use a two-call pattern (query size, then run) because…',
      o: [
        'For thread safety',
        'CUB never allocates internally — the first call returns the temp-storage bytes; you allocate (and can reuse) it, then call again to execute, giving you allocation control',
        'To validate inputs',
        'For error checking',
      ],
      a: 1,
      x: [
        'To pick an algorithm',
        'To set block size',
        'For graphics',
      ],
      e: 'CUB returns required temp-storage size on the first call (d_temp_storage = nullptr), you allocate it, then call again to run. This makes allocation explicit and lets you reuse the buffer across launches — avoiding hidden allocations.',
      ref: 'CUB temp storage pattern',
      tags: ['cub', 'temp-storage', 'device-primitives'],
    },
    {
      d: 4,
      q: 'A large cuBLASLt GEMM needs split-K but fails to pick that algorithm. The likely missing ingredient is…',
      o: [
        'A bigger matrix',
        'A workspace — split-K (and some tensor-core paths) need scratch memory; providing cublasLtMatmul a sufficient workspace lets the heuristic choose them',
        'FP64',
        'The CPU',
      ],
      a: 1,
      x: [
        'A smaller grid',
        'Disabling tensor cores',
        'Column-major only',
      ],
      e: 'Split-K reductions and certain tensor-core kernels need scratch space. Without enough workspace, cuBLASLt falls back to slower algorithms. Allocate and pass a workspace so the heuristic can select the higher-performance split-K path.',
      ref: 'cuBLASLt workspace; split-K',
      tags: ['cublaslt', 'workspace', 'split-k'],
    },
    {
      d: 4,
      q: 'PyTorch’s scaled_dot_product_attention dispatches to flash-attn / cuDNN / memory-efficient backends based on…',
      o: [
        'Random choice',
        'Shapes, dtype, head dim, mask type, and GPU arch — picking the fastest applicable kernel (or a math fallback) for that configuration',
        'The optimizer',
        'The batch size only',
      ],
      a: 1,
      x: [
        'The learning rate',
        'The dataset',
        'The seed',
      ],
      e: 'SDPA backends have different constraints (supported head dims, masks, dtypes, arch). PyTorch selects the fastest applicable one, which is why attention performance varies with configuration — and why unsupported configs fall back to a slower math path.',
      ref: 'PyTorch SDPA; backend selection',
      tags: ['sdpa', 'flash-attention', 'pytorch'],
    },
    {
      d: 4,
      q: 'torch.compile speeds up a transformer mostly by…',
      o: [
        'Using FP64',
        'Fusing the many small memory-bound ops (norms, activations, residuals, bias) around the matmuls into fewer Triton/C++ kernels — cutting launches and HBM round-trips',
        'Sharding the model',
        'Quantizing weights',
      ],
      a: 1,
      x: [
        'Running eagerly',
        'Disabling autograd',
        'Increasing precision',
      ],
      e: 'GEMMs already use tuned libraries; torch.compile’s leverage is fusing the surrounding elementwise/reduction ops so intermediates stay on-chip, reducing launches and memory traffic — the main source of its speedups for memory-bound model code.',
      ref: 'torch.compile / TorchInductor',
      tags: ['torch-compile', 'fusion', 'inductor'],
    },
    {
      d: 4,
      q: 'cuFFT performance for a repeated transform of the same size is best when you…',
      o: [
        'Recreate the plan each call',
        'Create the plan ONCE and reuse it across executions — plan creation (algorithm selection, twiddle setup) is expensive and shouldn’t be repeated per transform',
        'Use the CPU',
        'Use FP64',
      ],
      a: 1,
      x: [
        'Use cuBLAS',
        'Disable batching',
        'Use a new stream each time',
      ],
      e: 'A cuFFT plan encapsulates the chosen algorithm and precomputed data; creating it is costly. For repeated same-size transforms, create the plan once and call cufftExec many times (optionally batched) — reusing the plan amortizes setup.',
      ref: 'cuFFT plan reuse',
      tags: ['cufft', 'plan-reuse', 'fft'],
    },
    {
      d: 4,
      q: 'For reproducible per-thread dropout inside a kernel, cuRAND’s recommended generator is…',
      o: [
        'XORWOW with shared state',
        'Philox (a counter-based generator) — it produces reproducible, independent streams per thread from a (seed, counter) without large per-thread state',
        'The host API only',
        'rand() from libc',
      ],
      a: 1,
      x: [
        'A single global RNG',
        'cuFFT',
        'cuBLAS',
      ],
      e: 'Counter-based Philox gives each thread a reproducible, statistically independent stream from a seed + counter (e.g. element index), with minimal state — ideal for dropout/noise where you want determinism and per-element independence.',
      ref: 'cuRAND Philox; dropout',
      tags: ['curand', 'philox', 'dropout'],
    },
    {
      d: 3,
      q: 'A GNN aggregates neighbor features via a sparse adjacency × dense feature matmul. The right library call is…',
      o: [
        'cuBLAS GEMM',
        'cuSPARSE SpMM (sparse × dense) — designed for exactly this sparse-matrix times dense-matrix pattern',
        'cuFFT',
        'cuRAND',
      ],
      a: 1,
      x: [
        'NCCL',
        'OptiX',
        'NPP',
      ],
      e: 'SpMM multiplies a sparse matrix (the adjacency) by a dense matrix (features) — the core GNN aggregation. cuSPARSE provides tuned SpMM (and SpGEMM for sparse×sparse) for graph/scientific sparse workloads.',
      ref: 'cuSPARSE SpMM; GNN',
      tags: ['cusparse', 'spmm', 'gnn'],
    },
    {
      d: 5,
      q: 'You need fine-grained overlap of communication with computation INSIDE a kernel across GPUs. The right tool is…',
      o: [
        'NCCL collectives',
        'NVSHMEM — device-initiated one-sided put/get lets a kernel communicate while computing, finer-grained than NCCL’s host-launched bulk collectives',
        'cuBLAS',
        'cuFFT',
      ],
      a: 1,
      x: [
        'Thrust',
        'cuRAND',
        'OptiX',
      ],
      e: 'NCCL is host-launched and coarse-grained. NVSHMEM exposes in-kernel one-sided communication (PGAS), enabling tight comm/compute fusion at fine granularity — used in custom communication-fused kernels (some MoE/attention systems).',
      ref: 'NVSHMEM vs NCCL',
      tags: ['nvshmem', 'overlap', 'one-sided'],
    },
    {
      d: 4,
      q: 'You want a fused GEMM with a custom epilogue (e.g. matmul + custom activation + residual) at near-peak tensor-core speed. Best fit:',
      o: [
        'cuBLAS (closed)',
        'CUTLASS — composable templates let you build a custom fused kernel (custom epilogue, layouts) while hitting tensor-core peak; cuBLASLt covers common epilogues',
        'cuFFT',
        'NCCL',
      ],
      a: 1,
      x: [
        'Thrust',
        'cuRAND',
        'NPP',
      ],
      e: 'cuBLAS is fixed; cuBLASLt supports common fused epilogues (bias/activation). For arbitrary custom fusion at peak performance, CUTLASS’s composable components (or its EVT epilogues) let you build the kernel — the right tool for novel fused GEMMs.',
      ref: 'CUTLASS; custom fused GEMM',
      tags: ['cutlass', 'fusion', 'epilogue'],
    },
    {
      d: 4,
      q: 'Triton’s @triton.autotune helps by…',
      o: [
        'Compiling once',
        'Benchmarking several kernel configurations (block sizes, num_warps, num_stages) for the given problem and caching the best — so the kernel adapts to shape/hardware',
        'Running on the CPU',
        'Disabling the GPU',
      ],
      a: 1,
      x: [
        'Using FP64',
        'Sharding the model',
        'Lowering precision',
      ],
      e: 'autotune sweeps a set of meta-parameter configs, benchmarks them for the actual inputs, and caches the winner keyed on problem characteristics — so the Triton kernel runs near-optimally across shapes/GPUs without manual tuning.',
      ref: 'Triton autotune',
      tags: ['triton', 'autotune', 'caching'],
    },
    {
      d: 4,
      q: 'TensorRT INT8 deployment requires a calibration step to…',
      o: [
        'Train the model',
        'Determine per-tensor activation scales (via representative data, e.g. entropy/percentile calibration) so quantization preserves accuracy',
        'Shard the model',
        'Lower the batch',
      ],
      a: 1,
      x: [
        'Compile the kernels',
        'Pick the GPU',
        'Disable tensor cores',
      ],
      e: 'Static INT8 needs activation ranges; TensorRT runs a small calibration set to estimate them and choose scales (clipping outliers via KL/percentile). Good calibration is essential for INT8 accuracy; QAT can do better at lower bits.',
      ref: 'TensorRT INT8 calibration',
      tags: ['tensorrt', 'int8', 'calibration'],
    },
    {
      d: 4,
      q: 'vLLM serves many concurrent LLM requests efficiently primarily via…',
      o: [
        'Quantizing weights',
        'PagedAttention (paged KV cache, less fragmentation/more concurrency) + continuous batching (swap finished sequences for new ones each step) — maximizing GPU utilization',
        'FP64',
        'A single big batch',
      ],
      a: 1,
      x: [
        'Sharding the optimizer',
        'Disabling attention',
        'The CPU',
      ],
      e: 'vLLM’s throughput comes from PagedAttention (KV cache as shared fixed-size pages, packing more/longer sequences) and continuous batching (dynamically admitting requests as slots free), keeping the GPU busy for variable-length generation.',
      ref: 'vLLM; PagedAttention + continuous batching',
      tags: ['vllm', 'paged-attention', 'serving'],
    },
    {
      d: 3,
      q: 'For an in-kernel block-wide prefix sum inside your own fused kernel, use…',
      o: [
        'thrust::inclusive_scan',
        'CUB BlockScan — a cooperative block-scope primitive you compose inside the kernel (Thrust/CUB device-wide calls are whole-kernel, not in-kernel building blocks)',
        'cuBLAS',
        'NCCL',
      ],
      a: 1,
      x: [
        'cuFFT',
        'cuRAND',
        'OptiX',
      ],
      e: 'CUB’s block/warp-scope primitives (BlockScan, BlockReduce, WarpScan) are meant to be used INSIDE kernels. Thrust/CUB device algorithms are whole-device operations — not the right tool when you need a cooperative scan within your custom kernel.',
      ref: 'CUB BlockScan; in-kernel',
      tags: ['cub', 'block-scan', 'in-kernel'],
    },
    {
      d: 4,
      q: 'A fused optimizer (e.g. Apex FusedAdam) speeds training by…',
      o: [
        'Using FP64 states',
        'Updating ALL parameter tensors in one (or few) multi-tensor kernels instead of many small per-parameter kernels — cutting launch overhead and memory passes',
        'Sharding the model',
        'Quantizing gradients',
      ],
      a: 1,
      x: [
        'Disabling the optimizer',
        'Running on the CPU',
        'Lowering precision',
      ],
      e: 'A naive optimizer launches a small kernel per parameter tensor. multi_tensor_apply batches the update over all tensors in one launch, amortizing overhead and improving utilization — a notable training-throughput gain.',
      ref: 'Apex FusedAdam; multi-tensor apply',
      tags: ['fused-optimizer', 'adam', 'multi-tensor'],
    },
    {
      d: 4,
      q: 'For a dense LU factorization too large for one GPU, the right NVIDIA library is…',
      o: [
        'cuBLAS',
        'cuSOLVERMg — multi-GPU dense linear algebra (distributed LU/Cholesky/etc.) for matrices exceeding a single GPU’s memory',
        'cuFFT',
        'NCCL',
      ],
      a: 1,
      x: [
        'cuRAND',
        'NPP',
        'Thrust',
      ],
      e: 'cuSOLVER does single-GPU factorizations; cuSOLVERMg distributes dense solves/factorizations across multiple GPUs for large problems — the multi-GPU path for big HPC linear algebra.',
      ref: 'cuSOLVERMg',
      tags: ['cusolvermg', 'multi-gpu', 'lu'],
    },
    {
      d: 4,
      q: 'For RAG, the GPU library to serve high-QPS nearest-neighbor search over millions of embeddings is…',
      o: [
        'cuBLAS',
        'RAFT/cuVS (or FAISS-GPU) — GPU-accelerated ANN indexes deliver the throughput/latency retrieval needs to feed the LLM',
        'cuFFT',
        'NCCL',
      ],
      a: 1,
      x: [
        'cuRAND',
        'OptiX',
        'NPP',
      ],
      e: 'RAG issues many similarity queries against large embedding sets; GPU ANN (RAFT/cuVS, FAISS-GPU) provides the high-throughput, low-latency search to retrieve context for the model — a core component of GPU RAG pipelines.',
      ref: 'RAFT/cuVS; FAISS-GPU; RAG',
      tags: ['vector-search', 'rag', 'raft'],
    },
    {
      d: 3,
      q: 'CCCL (Thrust + CUB + libcu++) interoperate well because…',
      o: [
        'They conflict',
        'They are designed together (Thrust uses CUB; both use libcu++ types) with consistent versioning — so high-level algorithms, in-kernel primitives, and device std facilities compose seamlessly',
        'They run on the CPU',
        'They replace cuBLAS',
      ],
      a: 1,
      x: [
        'They are unrelated',
        'They need separate installs',
        'They are Python-only',
      ],
      e: 'CCCL unifies Thrust (algorithms), CUB (cooperative primitives), and libcu++ (device std library) with shared types/versioning, so you can mix a Thrust call, a CUB-based custom kernel, and cuda::std utilities cleanly.',
      ref: 'CCCL',
      tags: ['cccl', 'thrust', 'cub', 'libcu++'],
    },
    {
      d: 4,
      q: 'To atomically update an element of an existing (non-atomic) device array with explicit scope/ordering, use…',
      o: [
        'A plain assignment',
        'cuda::atomic_ref<T, scope>(elem).fetch_add(v, order) — it applies scoped, ordered atomics to ordinary memory without making the whole array atomic',
        'cuBLAS',
        'A spinlock',
      ],
      a: 1,
      x: [
        'thrust::reduce',
        'cuFFT',
        'NCCL',
      ],
      e: 'atomic_ref wraps a reference to plain memory for on-demand scoped/ordered atomic operations — ideal when only some accesses to a buffer need atomicity, with control over thread scope and memory order (vs the fixed legacy atomicAdd).',
      ref: 'libcu++ atomic_ref',
      tags: ['atomic-ref', 'libcu++', 'scoped'],
    },
    {
      d: 4,
      q: 'TensorRT-LLM improves LLM serving throughput notably via…',
      o: [
        'Static batching',
        'In-flight (continuous) batching plus paged KV cache and optimized fused attention — adding/removing requests each step to keep the GPU busy',
        'FP64',
        'A single request',
      ],
      a: 1,
      x: [
        'Sharding the optimizer',
        'Disabling attention',
        'The CPU',
      ],
      e: 'TensorRT-LLM uses in-flight batching (dynamically swapping completed sequences for new ones), paged KV management, and fused/optimized attention to maximize utilization for variable-length generation — much higher throughput than static batching.',
      ref: 'TensorRT-LLM; in-flight batching',
      tags: ['tensorrt-llm', 'in-flight-batching', 'serving'],
    },
    {
      d: 4,
      q: 'A reason to prefer a tuned library (cuBLAS/cuDNN/CUTLASS) over a hand-written kernel for STANDARD ops is…',
      o: [
        'Libraries are slower',
        'They are re-tuned for each GPU generation and cover many shapes, so you inherit near-peak performance and forward support with far less maintenance',
        'They cannot use tensor cores',
        'They only run on CPUs',
      ],
      a: 1,
      x: [
        'They need no GPU',
        'They are written in Python',
        'They avoid memory',
      ],
      e: 'Vendor libraries track new architectures and shapes with ongoing tuning. Custom kernels (Triton/CUTLASS) shine for fused/novel ops, but for standard GEMM/conv the libraries usually win with much less maintenance burden.',
      ref: 'Library vs custom-kernel trade-offs',
      tags: ['libraries', 'maintenance', 'production'],
    },
    {
      d: 4,
      q: 'cuDNN’s graph (frontend) API enables better performance than the legacy per-op API by…',
      o: [
        'Validating inputs only',
        'Representing a computation as fused nodes (e.g. conv + bias + activation) that cuDNN matches to optimized fused engines — reducing launches and intermediate traffic',
        'Running on the CPU',
        'Disabling tensor cores',
      ],
      a: 1,
      x: [
        'Using FP64',
        'A single op only',
        'Random selection',
      ],
      e: 'The graph/frontend API describes fused operation graphs that cuDNN maps to optimized engines (often fusing conv/matmul with pointwise ops), giving better performance and flexibility than calling each op separately (the legacy API).',
      ref: 'cuDNN graph API',
      tags: ['cudnn', 'graph-api', 'fusion'],
    },
    {
      d: 4,
      q: 'For deterministic training results, putting cuBLAS/cuDNN in deterministic mode typically…',
      o: [
        'Has no cost',
        'Selects algorithms that avoid atomic-based / variable-order reductions (fixed accumulation order) — trading some speed for run-to-run reproducibility',
        'Increases precision to FP64',
        'Disables tensor cores entirely',
      ],
      a: 1,
      x: [
        'Runs on the CPU',
        'Uses random seeds',
        'Requires more GPUs',
      ],
      e: 'Nondeterminism often comes from atomic/variable-order reductions. Deterministic modes pick fixed-order algorithms (reproducible accumulation), costing some performance — useful for debugging/regulated training where bitwise reproducibility matters.',
      ref: 'cuBLAS/cuDNN deterministic behavior',
      tags: ['determinism', 'cublas', 'cudnn'],
    },
    {
      d: 5,
      q: 'Triton is popular for research kernels because…',
      o: [
        'It is always faster than CUTLASS',
        'You write tile-level Python and the compiler handles coalescing, shared memory, vectorization, and scheduling — far less boilerplate to get a performant fused kernel (peak GEMM still favors CUTLASS/cuBLAS)',
        'It runs on the CPU',
        'It needs no GPU knowledge',
      ],
      a: 1,
      x: [
        'It only does FP64',
        'It replaces cuBLAS by default',
        'It avoids the GPU',
      ],
      e: 'Triton raises the abstraction to tiles/blocks and automates much low-level CUDA work, so researchers write fused, performant kernels (attention, norms) quickly. For peak dense GEMM, hand-tuned CUTLASS/cuBLAS usually still win.',
      ref: 'Triton; research kernels',
      tags: ['triton', 'productivity', 'fusion'],
    },
    {
      d: 4,
      q: 'For a device-wide sort of 100M FP32 keys with values, the simplest high-performance call is…',
      o: [
        'A hand-written sort',
        'thrust::sort_by_key (or CUB DeviceRadixSort) — tuned GPU radix sort, far faster and simpler than rolling your own',
        'cuBLAS',
        'cuFFT',
      ],
      a: 1,
      x: [
        'cuRAND',
        'NCCL',
        'OptiX',
      ],
      e: 'Thrust sort_by_key / CUB DeviceRadixSort provide highly optimized GPU radix sorts (with payloads). They vastly outperform naive hand-rolled sorts and handle the cooperative scan/scatter complexity for you.',
      ref: 'Thrust/CUB sort',
      tags: ['thrust', 'cub', 'sort'],
    },
    {
      d: 4,
      q: 'cuBLASLt is required (over legacy cuBLAS) for FP8 GEMM because it…',
      o: [
        'Runs on the CPU',
        'Exposes FP8 operand types with per-tensor scale pointers and fused epilogues in its descriptor-based matmul — the legacy API can’t express FP8 scaling',
        'Only does FP64',
        'Disables tensor cores',
      ],
      a: 1,
      x: [
        'Is column-major only',
        'Needs no handle',
        'For graphics',
      ],
      e: 'FP8 GEMM needs explicit input/output scales and supported tile shapes. cuBLASLt’s descriptor API supports FP8 operand types with scale pointers and fused epilogues; the legacy cublasGemm interface doesn’t expose this, so Lt is the FP8 path.',
      ref: 'cuBLASLt FP8',
      tags: ['cublaslt', 'fp8', 'scaling'],
    },
  ],
  { idPrefix: 'libraries-f' }
)
