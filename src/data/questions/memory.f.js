import { defineQuestions } from './_helpers.js'

// Memory — batch F. Bandwidth/intensity calculations, specific specs, carveout
// values, page granularity, swizzle. Distinct from memory.js/.b/.c/.d/.e.
export default defineQuestions(
  'memory',
  [
    {
      d: 4,
      q: 'A kernel reads 8 GB and writes 4 GB in 6 ms. Its achieved bandwidth is about…',
      o: ['1 TB/s', '2 TB/s', '12 TB/s', '0.5 TB/s'],
      a: 1,
      x: ['6 TB/s', '4 TB/s', '0.2 TB/s'],
      e: 'Total traffic = 12 GB; bandwidth = 12 GB / 6 ms = 2,000 GB/s = ~2 TB/s. Compare to the GPU’s peak (e.g. ~3.35 TB/s on H100) to judge efficiency: ~2 TB/s would be ~60% of peak here.',
      ref: 'Bandwidth calculation; CUDA Best Practices',
      tags: ['bandwidth', 'calculation', 'memory-bound'],
    },
    {
      d: 4,
      q: 'SAXPY (y[i] = a*x[i] + y[i]) in FP32 has an arithmetic intensity of about…',
      o: [
        '1 FLOP/byte',
        '~0.17 FLOP/byte (2 FLOPs per element ÷ 12 bytes: read x, read y, write y)',
        '10 FLOP/byte',
        '2 FLOP/byte',
      ],
      a: 1,
      x: ['0.5 FLOP/byte', '4 FLOP/byte', '1.5 FLOP/byte'],
      e: 'Per element: 2 FLOPs (a multiply + an add) and 12 bytes (read x[i] 4B, read y[i] 4B, write y[i] 4B). Intensity = 2/12 ≈ 0.17 FLOP/byte — far below any ridge point, so SAXPY is strongly memory-bound.',
      ref: 'Roofline; arithmetic intensity calculation',
      tags: ['arithmetic-intensity', 'saxpy', 'memory-bound'],
    },
    {
      d: 5,
      q: 'A warp accesses a shared float array as s[threadIdx.x * 2] (stride 2). The bank conflict degree is…',
      o: [
        'Conflict-free',
        '2-way: stride-2 over 32 banks maps two threads to each bank (threads i and i+16 collide), serializing into 2 transactions',
        '32-way',
        '16-way',
      ],
      a: 1,
      x: ['4-way', '8-way', 'No access'],
      e: 'With 32 banks and stride 2, thread i hits bank (2i mod 32); threads i and i+16 map to the same bank → 2-way conflict. Stride-1 is conflict-free; larger even strides increase the conflict degree (stride 32 → 32-way).',
      ref: 'Shared memory bank conflicts; stride analysis',
      tags: ['bank-conflict', 'stride', 'shared-memory'],
    },
    {
      d: 3,
      q: 'A 64×64 float shared-memory tile (without padding) consumes…',
      o: ['4 KB', '16 KB', '64 KB', '1 KB'],
      a: 1,
      x: ['8 KB', '32 KB', '256 KB'],
      e: '64 × 64 × 4 bytes = 16,384 bytes = 16 KB. With [64][65] padding it’s 64×65×4 = 16.6 KB. This per-block shared-memory cost factors into how many blocks fit per SM (occupancy).',
      ref: 'Shared-memory sizing; tiling',
      tags: ['shared-memory', 'tile-size', 'calculation'],
    },
    {
      d: 4,
      q: 'The H100 SXM (HBM3) memory bandwidth is approximately…',
      o: ['900 GB/s', '2 TB/s', '3.35 TB/s', '8 TB/s'],
      a: 2,
      x: ['1 TB/s', '4.8 TB/s', '5.3 TB/s'],
      e: 'H100 SXM with HBM3 is ~3.35 TB/s (A100 ~2 TB/s, H200 HBM3e ~4.8 TB/s, B200 ~8 TB/s). Knowing your GPU’s peak lets you compute bandwidth efficiency (achieved/peak) for memory-bound kernels.',
      ref: 'NVIDIA H100 datasheet',
      tags: ['hbm3', 'bandwidth', 'h100'],
    },
    {
      d: 4,
      q: 'Blackwell (B200) HBM3e memory bandwidth is roughly…',
      o: ['2 TB/s', '3.35 TB/s', '~8 TB/s', '900 GB/s'],
      a: 2,
      x: ['4.8 TB/s', '1 TB/s', '16 TB/s'],
      e: 'B200 provides about 8 TB/s of HBM3e bandwidth (across its two dies), roughly 2.4× H100. Higher bandwidth directly benefits memory-bound workloads (LLM decode, elementwise, reductions).',
      ref: 'NVIDIA Blackwell B200 specs',
      tags: ['hbm3e', 'bandwidth', 'blackwell'],
    },
    {
      d: 4,
      q: 'Hopper’s configurable shared-memory carveout offers discrete sizes up to…',
      o: ['48 KB', '96 KB', '~228 KB per SM (opt-in via cudaFuncSetAttribute)', '1 MB'],
      a: 2,
      x: ['64 KB', '164 KB', '512 KB'],
      e: 'Hopper allows up to ~228 KB shared memory per block (opt-in), selected from discrete carveout sizes. Larger shared memory enables bigger tiles/deeper pipelines for GEMM/attention — you request it explicitly via the max-dynamic-shared-memory attribute.',
      ref: 'NVIDIA Hopper; shared-memory carveout',
      tags: ['shared-memory', 'carveout', 'hopper'],
    },
    {
      d: 4,
      q: 'Unified Memory page migration on demand operates at a granularity of…',
      o: [
        '1 byte',
        'Memory pages (e.g. 4 KB, often migrated in larger 2 MB blocks) — so a single byte access can trigger migrating a whole page/block',
        'The whole allocation',
        '32 bytes',
      ],
      a: 1,
      x: ['1 KB', 'One cache line', 'One warp'],
      e: 'Managed-memory faults migrate at page granularity (4 KB base, frequently coalesced into larger 2 MB transfers). Touching one element pulls a whole page/block, so scattered first-touches cause heavy migration — prefetching avoids it.',
      ref: 'CUDA Unified Memory; page granularity',
      tags: ['unified-memory', 'page-migration', 'granularity'],
    },
    {
      d: 4,
      q: 'cp.async copies are batched into commit groups; cp.async.wait_group(N) waits until…',
      o: [
        'All copies finish',
        'All but the N most-recently-committed groups have completed — letting you keep N groups in flight (pipeline depth) while consuming earlier ones',
        'The kernel ends',
        'A barrier is reached',
      ],
      a: 1,
      x: [
        'N copies start',
        'Nothing',
        'The host signals',
      ],
      e: 'wait_group(N) blocks until only N committed groups remain outstanding, so the pipeline keeps N stages in flight. Setting N controls the prefetch depth — more in-flight groups hide more latency at the cost of shared memory/registers.',
      ref: 'PTX cp.async.wait_group; pipelining',
      tags: ['cp-async', 'wait-group', 'pipeline'],
    },
    {
      d: 4,
      q: 'For a thread to use a vectorized float4 LOAD, the source address must be aligned to…',
      o: ['4 bytes', '8 bytes', '16 bytes', '128 bytes'],
      a: 2,
      x: ['1 byte', '32 bytes', '64 bytes'],
      e: 'float4 is a 16-byte type; its loads/stores require 16-byte alignment. cudaMalloc returns ≥256-byte aligned memory, so element 0 is aligned, but offset views must preserve 16-byte alignment or the vectorized access faults/falls back.',
      ref: 'CUDA vectorized access; alignment',
      tags: ['vectorized-load', 'alignment', 'float4'],
    },
    {
      d: 4,
      q: 'A warp reads field .x of an array-of-structs where each struct is 32 bytes. Compared to SoA, the AoS read…',
      o: [
        'Is equally efficient',
        'Wastes ~7/8 of each cache line: a warp’s 32 .x reads are 32 bytes apart, so each 128-byte line delivers only 4 useful floats (16 of 128 bytes used)',
        'Is faster',
        'Causes a compile error',
      ],
      a: 1,
      x: [
        'Uses no bandwidth',
        'Is conflict-free',
        'Doubles bandwidth',
      ],
      e: 'With a 32-byte struct, consecutive .x fields are 32 bytes apart; a 128-byte line covers only 4 structs, so 4×4=16 of 128 bytes are useful (~12.5%). SoA makes .x contiguous (100% line utilization) — often a multi× bandwidth improvement.',
      ref: 'AoS vs SoA; cache-line utilization',
      tags: ['aos-soa', 'coalescing', 'calculation'],
    },
    {
      d: 4,
      q: 'XOR-based shared-memory "swizzling" eliminates bank conflicts for tensor-core operand loads by…',
      o: [
        'Randomizing addresses',
        'Permuting the column index with an XOR of (some bits of) the row, so each row’s elements land in different banks — making the ldmatrix/wgmma access pattern conflict-free',
        'Padding rows',
        'Compressing data',
      ],
      a: 1,
      x: [
        'Transposing in global memory',
        'Using constant memory',
        'Disabling banks',
      ],
      e: 'Swizzling applies col ^= f(row) so successive rows offset their bank mapping, spreading a warp’s 2D access across all banks without the extra memory of padding. CuTe/CUTLASS encode these XOR swizzles in layout types for tensor-core feeds.',
      ref: 'CUTLASS/CuTe swizzle; bank conflicts',
      tags: ['swizzle', 'bank-conflict', 'tensor-cores'],
    },
    {
      d: 3,
      q: 'On modern SMs, the unified L1/shared SRAM is about…',
      o: ['16 KB', '48 KB', '~192–256 KB per SM (split between L1 and shared)', '4 MB'],
      a: 2,
      x: ['64 KB', '1 MB', '128 KB'],
      e: 'Recent SMs have ~192–256 KB of unified L1/shared SRAM per SM, partitioned by the carveout. This pool backs both the L1 data cache and the configurable shared memory — a big increase over earlier generations, aiding tiling/caching.',
      ref: 'NVIDIA SM architecture; unified L1/shared',
      tags: ['l1-shared', 'sram', 'sm'],
    },
    {
      d: 4,
      q: 'Blackwell’s 5th-gen NVLink provides per-GPU bandwidth of approximately…',
      o: ['600 GB/s', '900 GB/s', '~1.8 TB/s', '64 GB/s'],
      a: 2,
      x: ['300 GB/s', '450 GB/s', '5 TB/s'],
      e: 'Blackwell NVLink (5th gen) delivers ~1.8 TB/s aggregate per GPU, roughly double Hopper’s ~900 GB/s. With NVLink Switch (NVL72) this scales to a large rack-wide domain for communication-heavy parallelism.',
      ref: 'NVIDIA Blackwell NVLink',
      tags: ['nvlink', 'bandwidth', 'blackwell'],
    },
    {
      d: 4,
      q: 'A kernel’s effective bandwidth is 1.5 TB/s on a GPU with 3 TB/s peak. The most useful conclusion is…',
      o: [
        'It is compute-bound',
        'It achieves ~50% of peak — if it should be bandwidth-bound, investigate coalescing/vectorization/alignment to push toward ~80–90% of peak',
        'It is optimal',
        'It is latency-bound on compute',
      ],
      a: 1,
      x: [
        'Add more compute',
        'Increase the grid only',
        'Use FP64',
      ],
      e: '50% of peak suggests inefficiency for a memory-bound kernel (well-tuned ones hit 80–90%). Check for uncoalesced/misaligned/non-vectorized access or insufficient memory-level parallelism — the usual reasons for sub-peak bandwidth.',
      ref: 'Bandwidth efficiency; tuning',
      tags: ['bandwidth', 'efficiency', 'analysis'],
    },
    {
      d: 4,
      q: 'The constant cache works best when a warp’s accesses to __constant__ data are…',
      o: [
        'Scattered',
        'To the SAME address (uniform) — served as a single broadcast; divergent constant addresses serialize and can miss the small (~8 KB working set) constant cache',
        'Strided',
        'Random',
      ],
      a: 1,
      x: [
        'Vectorized',
        'Atomic',
        'Per-thread unique',
      ],
      e: 'Constant memory is optimized for broadcast: one address read by all warp lanes is a single access. Different addresses per lane serialize and stress the small constant cache, losing the benefit — so use it for warp-uniform read-only values.',
      ref: 'CUDA constant cache; broadcast',
      tags: ['constant-cache', 'broadcast', 'uniform'],
    },
    {
      d: 5,
      q: 'ldmatrix loads a matrix tile from shared memory into registers in the layout mma.sync expects. Combined with a swizzled shared layout, it…',
      o: [
        'Avoids tensor cores',
        'Delivers operands conflict-free and in the right per-lane fragment arrangement, so the tensor cores are fed without bank-conflict stalls or manual transposes',
        'Compresses the tile',
        'Runs on the host',
      ],
      a: 1,
      x: [
        'Uses global memory',
        'Disables MMA',
        'Needs atomics',
      ],
      e: 'ldmatrix cooperatively loads the fragment layout mma/wgmma needs (handling the transpose). Pairing it with an XOR-swizzled shared layout makes the load conflict-free, so operands reach the tensor cores efficiently — core to CUTLASS GEMM.',
      ref: 'PTX ldmatrix; CUTLASS swizzle',
      tags: ['ldmatrix', 'swizzle', 'tensor-cores'],
    },
    {
      d: 4,
      q: 'Setting the shared-memory bank mode to 8 bytes (cudaSharedMemBankSizeEightByte) helps when…',
      o: [
        'Using floats',
        'A kernel heavily accesses DOUBLE (8-byte) shared data — 8-byte banks let a warp’s doubles map to distinct banks, reducing conflicts that the default 4-byte mode would cause',
        'Using INT8',
        'Always',
      ],
      a: 1,
      x: [
        'Using FP16',
        'Using constant memory',
        'For graphics',
      ],
      e: 'In the default 4-byte mode, an 8-byte double spans two banks, so warps of doubles can conflict. The 8-byte bank mode (where supported) maps one double per bank, reducing conflicts for double-precision shared-memory-heavy kernels.',
      ref: 'cudaDeviceSetSharedMemConfig; bank size',
      tags: ['bank-mode', 'double', 'shared-memory'],
    },
    {
      d: 4,
      q: 'A100 (HBM2e) memory bandwidth is approximately…',
      o: ['900 GB/s', '~2 TB/s', '3.35 TB/s', '8 TB/s'],
      a: 1,
      x: ['1 TB/s', '4.8 TB/s', '600 GB/s'],
      e: 'A100 80GB (HBM2e) is ~2 TB/s. (H100 ~3.35 TB/s, H200 ~4.8 TB/s, B200 ~8 TB/s.) These figures anchor bandwidth-efficiency analysis: achieved/peak tells you how close a memory-bound kernel is to the roof.',
      ref: 'NVIDIA A100 datasheet',
      tags: ['hbm2e', 'bandwidth', 'a100'],
    },
    {
      d: 5,
      q: 'A persistent reuse buffer of 30 MB accessed by all blocks fits in H100’s ~50 MB L2. Marking it persisting helps because…',
      o: [
        'It is too big',
        'The 30 MB working set fits within the L2 set-aside, so persisting access keeps it resident across blocks — cross-block reads hit L2 instead of HBM, cutting DRAM traffic',
        'It uses registers',
        'It needs constant memory',
      ],
      a: 1,
      x: [
        'It overflows L2',
        'It disables the L1',
        'It pins host memory',
      ],
      e: 'Since the reused buffer fits in the L2 set-aside, the persisting access policy keeps it cached, so every block’s accesses hit L2 rather than re-reading HBM — a real bandwidth saving for cross-block reuse. (If it exceeded the set-aside, it would thrash.)',
      ref: 'CUDA L2 persisting; reuse',
      tags: ['l2-persisting', 'reuse', 'h100'],
    },
    {
      d: 4,
      q: 'Measuring pinned vs pageable H2D bandwidth on PCIe Gen4 x16 (~25 GB/s peak each way), you’d expect pinned to be…',
      o: [
        'Slower',
        'Near peak (~24 GB/s) while pageable is notably lower (staged through an internal pinned buffer) — confirming pinned memory’s transfer advantage',
        'The same',
        'Over 50 GB/s',
      ],
      a: 1,
      x: [
        '~1 GB/s pinned',
        'Pinned at 100 GB/s',
        'Pageable faster',
      ],
      e: 'Pinned memory enables direct DMA near the PCIe peak; pageable transfers stage through an internal pinned buffer (extra copy), measurably slower. bandwidthTest with/without pinned memory demonstrates this on any system.',
      ref: 'bandwidthTest; pinned vs pageable',
      tags: ['pinned-memory', 'pcie', 'bandwidth'],
    },
    {
      d: 4,
      q: 'A kernel’s DRAM read bytes (from Nsight) are 3× the minimum needed for its data. The most likely cause is…',
      o: [
        'Perfect coalescing',
        'Uncoalesced/strided access fetching cache-line sectors that are only partly used (over-fetch), or missing reuse causing re-reads — both inflate DRAM bytes beyond the ideal',
        'Too many registers',
        'A divergent branch',
      ],
      a: 1,
      x: [
        'Tensor-core usage',
        'A host stall',
        'High occupancy',
      ],
      e: 'Moving 3× the necessary bytes means either over-fetch (poor coalescing pulls partly-used sectors) or redundant re-reads (no caching/tiling). Fix the access pattern (coalesce/vectorize) and add reuse (tiling/L2) to approach the minimum traffic.',
      ref: 'Nsight Compute; DRAM bytes vs ideal',
      tags: ['dram-bytes', 'coalescing', 'reuse'],
    },
    {
      d: 4,
      q: 'Hopper TMA can copy multi-dimensional tiles up to (per the descriptor) several dimensions. A key benefit for a 2D tile copy is…',
      o: [
        'Compression',
        'The hardware handles boundary/stride logic and out-of-bounds padding for the tile, so partial edge tiles are copied correctly without per-thread bounds checks',
        'Higher precision',
        'Running on the host',
      ],
      a: 1,
      x: [
        'Avoiding shared memory',
        'Disabling tensor cores',
        'Using constant memory',
      ],
      e: 'TMA’s descriptor encodes the tensor’s shape/strides and the tile; the hardware computes addresses and can handle out-of-bounds elements (e.g. zero-fill) for edge tiles — removing the per-thread boundary checks cp.async needed.',
      ref: 'Hopper TMA; boundary handling',
      tags: ['tma', 'boundary', 'hopper'],
    },
    {
      d: 3,
      q: 'For a reduction reading 4 GB in 2 ms, the achieved read bandwidth is…',
      o: ['1 TB/s', '2 TB/s', '8 TB/s', '0.5 TB/s'],
      a: 1,
      x: ['4 TB/s', '0.25 TB/s', '16 TB/s'],
      e: '4 GB / 2 ms = 2,000 GB/s = 2 TB/s. A reduction is read-dominated, so this is essentially its read bandwidth; comparing to peak shows how close the reduction is to bandwidth-bound (its ideal regime).',
      ref: 'Reduction bandwidth; calculation',
      tags: ['bandwidth', 'reduction', 'calculation'],
    },
    {
      d: 5,
      q: 'Why does a swizzled shared-memory layout avoid the extra memory that padding to [N][N+1] costs, while still being conflict-free?',
      o: [
        'It compresses data',
        'Swizzling permutes addresses via XOR (no extra storage) to spread accesses across banks, whereas padding adds a column of unused elements per row — both fix conflicts, but swizzle uses no extra shared memory',
        'It uses registers',
        'It disables banks',
      ],
      a: 1,
      x: [
        'It uses global memory',
        'It is slower',
        'Padding is conflict-free only',
      ],
      e: 'Padding wastes a column per row to offset bank mapping. Swizzling achieves the same bank spreading by XOR-permuting indices with no added storage — important when shared memory is scarce (large tiles), at the cost of more complex indexing.',
      ref: 'Swizzle vs padding; shared memory',
      tags: ['swizzle', 'padding', 'shared-memory'],
    },
    {
      d: 4,
      q: 'A kernel uses 100 KB shared memory per block on an SM with ~228 KB usable. The max resident blocks from shared memory alone is…',
      o: ['1', '2', '4', '8'],
      a: 1,
      x: ['3', '5', '6'],
      e: 'floor(228 / 100) = 2 blocks fit by shared memory. The actual resident count is the min over all limits (registers, threads, blocks too); here shared memory caps it at 2, which you’d weigh against the reuse benefit of the large tile.',
      ref: 'Occupancy; shared-memory limit',
      tags: ['occupancy', 'shared-memory', 'calculation'],
    },
    {
      d: 4,
      q: 'Why might cudaMemcpyAsync H2D NOT overlap with a kernel even with pinned memory and separate streams?',
      o: [
        'Pinned memory blocks it',
        'The kernel and copy may both be on the legacy default stream (which serializes), or the GPU has a single copy engine already busy — check streams and asyncEngineCount',
        'Async copies never overlap',
        'Kernels never overlap copies',
      ],
      a: 1,
      x: [
        'Pageable is required',
        'It needs FP64',
        'Constant memory conflict',
      ],
      e: 'Overlap requires the copy and kernel on independent (non-default) streams and a free copy engine. If either is on the legacy default stream, or the single/only copy engine is occupied, they serialize. Verify stream usage and asyncEngineCount.',
      ref: 'CUDA overlap; copy engines and streams',
      tags: ['overlap', 'copy-engine', 'streams'],
    },
    {
      d: 5,
      q: 'For a GEMM, increasing the shared-memory tile from 64×64 to 128×128 (FP16) raises operand reuse by ~2× but the shared memory needed goes from…',
      o: [
        '16 KB to 64 KB (operands roughly 4×)',
        '~16 KB to ~64 KB for the A+B tiles — quadrupling shared memory (each dimension doubled), which may drop occupancy; the reuse gain must outweigh that',
        '8 KB to 16 KB',
        'No change',
      ],
      a: 1,
      x: [
        '32 KB to 64 KB',
        '64 KB to 128 KB',
        '4 KB to 8 KB',
      ],
      e: 'Doubling each tile dimension roughly quadruples the tile’s shared-memory footprint (and the A+B operands scale similarly), which can lower occupancy. The larger tile reuses operands more (higher intensity); GEMM tuning balances this reuse-vs-occupancy trade-off.',
      ref: 'GEMM tiling; shared-memory scaling',
      tags: ['gemm', 'tile-size', 'shared-memory', 'reuse'],
    },
    {
      d: 4,
      q: 'A managed-memory program is slow with many "page fault" migration events; prefetching the working set to the GPU helps because…',
      o: [
        'It compresses pages',
        'It moves the pages to the device BEFORE the kernel accesses them (one bulk transfer), eliminating the per-access fault-and-migrate stalls during execution',
        'It pins host memory',
        'It disables the L2',
      ],
      a: 1,
      x: [
        'It increases registers',
        'It uses constant memory',
        'It locks clocks',
      ],
      e: 'On-demand migration faults per first-touched page (slow, serialized). cudaMemPrefetchAsync to the device bulk-migrates the working set ahead of time, so the kernel runs against resident memory — usually the key Unified Memory optimization.',
      ref: 'CUDA Unified Memory; prefetch',
      tags: ['unified-memory', 'prefetch', 'page-faults'],
    },
    {
      d: 3,
      q: 'The texture/read-only data cache is most beneficial for data that is…',
      o: [
        'Written frequently',
        'Read-only and reused with spatial locality (or scattered reuse) — it caches such loads (via const __restrict__/__ldg or texture objects), reducing redundant global reads',
        'Per-thread scratch',
        'In registers',
      ],
      a: 1,
      x: [
        'Atomic',
        'Uniform-broadcast only',
        'In shared memory',
      ],
      e: 'The read-only (texture) cache captures reuse of read-only data, including irregular/scattered reuse with spatial locality. Route such inputs through it (const __restrict__/__ldg or texture objects) to cut global-memory traffic without manual shared-memory staging.',
      ref: 'CUDA read-only/texture cache',
      tags: ['read-only-cache', 'texture', 'reuse'],
    },
  ],
  { idPrefix: 'memory-f' }
)
