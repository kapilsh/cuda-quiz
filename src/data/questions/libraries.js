import { defineQuestions } from './_helpers.js'

// Libraries — cuBLAS, cuDNN, Thrust, CUB, CUTLASS, CuTe.
export default defineQuestions('libraries', [
  {
    d: 2,
    q: 'What does cuBLAS provide?',
    o: [
      'A deep-learning training framework',
      'GPU-accelerated BLAS: GEMM, GEMV, dot, axpy, etc.',
      'A profiler',
      'A graph compiler',
    ],
    a: 1,
    e: 'cuBLAS is NVIDIA’s GPU implementation of the BLAS API — level 1/2/3 routines like GEMM and GEMV — heavily tuned per architecture. cuBLASLt adds a more flexible "Lt" API for fused epilogues and mixed precision.',
    ref: 'cuBLAS documentation',
    tags: ['cublas', 'blas', 'gemm'],
  },
  {
    d: 3,
    q: 'A frequent gotcha: cuBLAS expects matrices in which storage order?',
    o: [
      'Row-major (like C/C++)',
      'Column-major (like Fortran)',
      'Either, auto-detected',
      'Always transposed',
    ],
    a: 1,
    e: 'cuBLAS uses column-major storage (Fortran convention). C/C++ row-major arrays must be handled by transposing or by computing Bᵀ·Aᵀ = (A·B)ᵀ, swapping operands and dimensions. This is a classic source of bugs.',
    ref: 'cuBLAS documentation, data layout',
    tags: ['cublas', 'column-major', 'layout'],
  },
  {
    d: 2,
    q: 'What is cuDNN used for?',
    o: [
      'General linear algebra',
      'DNN primitives: conv, pool, norm, activations',
      'Multi-GPU communication',
      'Memory allocation',
    ],
    a: 1,
    e: 'cuDNN is NVIDIA’s deep-learning primitive library: highly tuned convolutions (multiple algorithms), pooling, batchnorm, activations, RNN/attention building blocks, used as a backend by PyTorch, TensorFlow, etc.',
    ref: 'cuDNN documentation',
    tags: ['cudnn', 'deep-learning', 'convolution'],
  },
  {
    d: 2,
    q: 'Thrust is best described as…',
    o: [
      'A low-level PTX assembler',
      'C++ parallel algorithms (STL-like) for the GPU',
      'A profiler',
      'A networking library',
    ],
    a: 1,
    e: 'Thrust provides STL-style containers and parallel algorithms (thrust::sort, reduce, transform, inclusive_scan) with host/device execution policies, letting you write high-level GPU code without hand-writing kernels.',
    ref: 'Thrust documentation',
    tags: ['thrust', 'algorithms', 'high-level'],
  },
  {
    d: 3,
    q: 'CUB differs from Thrust in that it…',
    o: [
      'Only runs on the CPU',
      'Warp/block/device-scope reusable primitives',
      'Is a Python library',
      'Replaces cuBLAS',
    ],
    a: 1,
    e: 'CUB offers cooperative primitives at three layers — warp-wide, block-wide, and device-wide (reduce, scan, sort, histogram, radix) — that you compose inside custom kernels. Thrust’s device backend is actually built on CUB.',
    ref: 'CUB documentation',
    tags: ['cub', 'primitives', 'reduce-scan'],
  },
  {
    d: 4,
    q: 'What is CUTLASS?',
    o: [
      'A closed-source replacement for cuBLAS',
      'Open-source C++ templates for GEMM/conv with tensor cores',
      'A Python autodiff framework',
      'A profiler GUI',
    ],
    a: 1,
    e: 'CUTLASS is NVIDIA’s open-source, header-only template library implementing efficient GEMM and related kernels with composable, hierarchical components (threadblock/warp/instruction tiles), letting you build and customize tensor-core kernels.',
    ref: 'CUTLASS documentation/GitHub',
    tags: ['cutlass', 'gemm', 'templates'],
  },
  {
    d: 5,
    q: 'In CUTLASS/CuTe, a "Layout" is fundamentally…',
    o: [
      'A memory allocator',
      '(Shape, Stride) map',
      'A thread scheduler',
      'A precision format',
    ],
    a: 1,
    e: 'CuTe’s Layout = (Shape, Stride): a function from multi-dimensional logical coordinates to a 1D offset. Layouts compose, tile, and partition algebraically, giving a unified way to describe tensors, tiling, swizzles, and thread/data assignment.',
    ref: 'CuTe documentation (CUTLASS 3.x)',
    tags: ['cute', 'layout', 'shape-stride'],
  },
  {
    d: 5,
    q: 'In CuTe, what does a Tensor combine?',
    o: [
      'Two GPUs',
      'Engine + Layout',
      'A kernel and a stream',
      'A shape and a precision',
    ],
    a: 1,
    e: 'A CuTe Tensor pairs an "engine" (data pointer/iterator, e.g. into global/shared/register memory) with a Layout. Indexing the tensor applies the layout to compute the address. The same abstraction works across all memory spaces.',
    ref: 'CuTe documentation',
    tags: ['cute', 'tensor', 'engine-layout'],
  },
  {
    d: 4,
    q: 'A key advantage of CuTe’s layout algebra for kernel authors is…',
    o: [
      'It removes the need for shared memory',
      'Tiling/partition/swizzle as composable layout algebra',
      'It automatically picks the GPU',
      'It compiles to Python',
    ],
    a: 1,
    e: 'By treating data and thread mappings as composable layouts, CuTe lets you derive per-thread/per-warp views (local_partition, local_tile), apply swizzles, and reason about coalescing/bank-conflicts algebraically — replacing brittle hand-written indexing in modern GEMM kernels.',
    ref: 'CuTe documentation; CUTLASS 3.x design',
    tags: ['cute', 'layout-algebra', 'tiling'],
  },
  {
    d: 3,
    q: 'cuBLASLt and the "epilogue" concept allow you to…',
    o: [
      'Run BLAS on the CPU',
      'Fuse bias/activation/scaling into the GEMM',
      'Skip the matmul',
      'Compress the output',
    ],
    a: 1,
    e: 'cuBLASLt exposes a flexible matmul with configurable epilogues that fuse bias and activation (and scaling for FP8) directly into the GEMM, eliminating separate elementwise kernels and the associated global-memory round-trips.',
    ref: 'cuBLASLt documentation',
    tags: ['cublaslt', 'epilogue', 'fusion'],
  },
  {
    d: 3,
    q: 'Why might you choose cuBLAS over a hand-written GEMM kernel for production?',
    o: [
      'cuBLAS is always slower but simpler',
      'Autotuned per arch/shape; usually matches hand-tuned',
      'cuBLAS supports only tiny matrices',
      'Hand-written kernels cannot use tensor cores',
    ],
    a: 1,
    e: 'cuBLAS ships many tuned kernels and a heuristic/autotuning selector per GPU and shape, exploiting tensor cores and the latest instructions. Beating it requires substantial expertise (and is the domain of CUTLASS); for most uses it is the pragmatic choice.',
    ref: 'cuBLAS documentation; CUTLASS rationale',
    tags: ['cublas', 'autotuning', 'production'],
  },
  {
    d: 4,
    q: 'For a batched many-small-matrix multiply, the most efficient cuBLAS entry point is usually…',
    o: [
      'A loop calling cublasSgemm once per matrix',
      'cublasGemmStridedBatched in one launch',
      'cuDNN convolution',
      'Thrust transform',
    ],
    a: 1,
    e: 'Looping per-matrix incurs launch overhead and underutilizes the GPU for small matrices. The batched GEMM APIs run the whole batch in a single kernel, amortizing overhead and keeping the device busy — much faster for many small GEMMs.',
    ref: 'cuBLAS batched GEMM documentation',
    tags: ['cublas', 'batched-gemm', 'small-matrices'],
  },
  {
    d: 2,
    q: 'thrust::reduce and thrust::inclusive_scan are examples of which parallel patterns?',
    o: [
      'Map and gather',
      'Reduction and prefix-sum (scan)',
      'Sort and search',
      'Stencil and convolution',
    ],
    a: 1,
    e: 'reduce collapses a range to a single value (reduction); inclusive_scan produces running prefixes (scan). These are foundational data-parallel primitives; Thrust/CUB provide efficient, tuned implementations.',
    ref: 'Thrust documentation; PMPP Ch. 8 (prefix sum)',
    tags: ['thrust', 'reduction', 'scan'],
  },
  {
    d: 5,
    q: 'CUTLASS 3.x organizes a GEMM into a hierarchy that maps onto the architecture. Which ordering (largest to smallest tile) is correct?',
    o: [
      'Instruction → Warp → Threadblock',
      'Cluster → Warpgroup → MMA tile',
      'Grid → Block → Thread',
      'Device → Stream → Kernel',
    ],
    a: 1,
    e: 'CUTLASS decomposes GEMM hierarchically: the threadblock (and on Hopper, cluster) tile is split into warp/warpgroup tiles, which are computed by sequences of instruction-level MMA tiles. Each level maps to a hardware tier (SMEM, registers, tensor-core op).',
    ref: 'CUTLASS documentation (GEMM hierarchy)',
    tags: ['cutlass', 'gemm-hierarchy', 'tiling'],
  },
  {
    d: 4,
    q: 'ldmatrix (used by CUTLASS/CuTe) is a hardware instruction that…',
    o: [
      'Loads a matrix from global to global memory',
      'Warp-loads SMEM into tensor-core register fragments',
      'Multiplies two matrices',
      'Transposes a matrix in DRAM',
    ],
    a: 1,
    e: 'ldmatrix has a warp load small matrix tiles from shared memory directly into the distributed register fragments required by mma/wgmma, handling the transpose/permutation the tensor cores need. It is key to feeding tensor cores efficiently.',
    ref: 'PTX ISA (ldmatrix); CUTLASS/CuTe',
    tags: ['ldmatrix', 'tensor-cores', 'fragments'],
  },
  {
    d: 3,
    q: 'Which library would you reach for to compute a device-wide radix sort of 100M keys with minimal code?',
    o: [
      'cuBLAS',
      'Thrust or CUB',
      'cuDNN',
      'NCCL',
    ],
    a: 1,
    e: 'Thrust::sort and CUB’s DeviceRadixSort provide highly optimized GPU radix sorts. Thrust is the one-liner; CUB gives more control (temp storage, custom decomposers). Both vastly outperform a naive hand-rolled sort.',
    ref: 'Thrust / CUB documentation',
    tags: ['thrust', 'cub', 'radix-sort'],
  },
])
