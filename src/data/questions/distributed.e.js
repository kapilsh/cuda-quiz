import { defineQuestions } from './_helpers.js'

// Distributed Training & Inference — batch E. Prefill/decode, serving, scaling
// laws, HFU/MFU, RLHF, gradient compression. Distinct from distributed.js/.b/.c/.d.
export default defineQuestions(
  'distributed',
  [
    {
      d: 4,
      q: 'In LLM inference, the "prefill" phase is compute-bound while "decode" is memory-bound because…',
      o: [
        'They are identical',
        'Prefill: big matmuls; decode: reads weights/KV',
        'Decode is parallel',
        'Prefill is sequential',
      ],
      a: 1,
      x: [
        'Prefill uses the CPU',
        'Decode is compute-bound',
        'Both are network-bound',
      ],
      e: 'Prefill computes all prompt tokens at once (large, efficient matmuls — compute-bound). Decode is autoregressive: each step does small matmuls but must read all weights (and the growing KV cache), making it bandwidth/memory-bound — the two phases have different bottlenecks.',
      ref: 'LLM inference; prefill vs decode',
      tags: ['prefill', 'decode', 'inference'],
    },
    {
      d: 5,
      q: 'Disaggregated serving (separate prefill and decode pools) is motivated by…',
      o: [
        'Simplicity',
        'Different bottlenecks per phase',
        'Lower accuracy',
        'Using FP64',
      ],
      a: 1,
      x: [
        'One GPU',
        'Disabling attention',
        'Compressing the model',
      ],
      e: 'Because prefill and decode stress different resources, co-locating them causes interference (a long prefill stalls decodes). Disaggregating lets each pool be sized/batched/optimized for its bottleneck, improving overall serving efficiency and latency.',
      ref: 'Disaggregated prefill/decode serving',
      tags: ['disaggregated', 'serving', 'prefill', 'decode'],
    },
    {
      d: 4,
      q: 'Chunked prefill (splitting a long prompt’s prefill into chunks interleaved with decodes) helps serving by…',
      o: [
        'Improving accuracy',
        'A long prefill no longer blocks decodes',
        'Using FP64',
        'Sharding the model',
      ],
      a: 1,
      x: [
        'Disabling attention',
        'Compressing the KV cache',
        'Removing batching',
      ],
      e: 'A monolithic long prefill can stall the batch, spiking decode latency for other requests. Chunking the prefill and interleaving it with decode steps smooths latency while progressing the long prompt — a continuous-batching scheduling technique.',
      ref: 'Chunked prefill; continuous batching',
      tags: ['chunked-prefill', 'serving', 'latency'],
    },
    {
      d: 4,
      q: 'HFU (Hardware FLOPs Utilization) differs from MFU (Model FLOPs Utilization) in that HFU…',
      o: [
        'Ignores hardware',
        'Counts all FLOPs done, incl. recompute',
        'Is always lower',
        'Measures memory',
      ],
      a: 1,
      x: [
        'Counts only matmuls',
        'Ignores the model',
        'Measures bandwidth',
      ],
      e: 'MFU = useful model FLOPs / peak; HFU includes redundant FLOPs (e.g. recompute from checkpointing). With activation checkpointing, the hardware does extra work, so HFU exceeds MFU. Both gauge efficiency; the gap reflects recomputation overhead.',
      ref: 'MFU vs HFU; training efficiency',
      tags: ['hfu', 'mfu', 'efficiency'],
    },
    {
      d: 5,
      q: 'The Chinchilla scaling result says, for a compute budget, you should roughly…',
      o: [
        'Maximize parameters only',
        'Balance model size and tokens (~20 tokens/param)',
        'Minimize tokens',
        'Use FP64',
      ],
      a: 1,
      x: [
        'Train on one example',
        'Maximize batch size',
        'Use one GPU',
      ],
      e: 'Hoffmann et al. (Chinchilla) found compute-optimal training scales parameters and tokens together (~20 tokens/param as a rule of thumb), implying many large models were under-trained. It guides how to spend a fixed compute budget — affecting cluster/time planning.',
      ref: 'Hoffmann et al., "Chinchilla"',
      tags: ['scaling-laws', 'chinchilla', 'compute-optimal'],
    },
    {
      d: 4,
      q: 'A cosine learning-rate schedule with warmup is common in large-model training because warmup…',
      o: [
        'Slows training',
        'Avoids early instability from a large LR',
        'Increases the batch',
        'Shards the model',
      ],
      a: 1,
      x: [
        'Uses FP64',
        'Disables the optimizer',
        'Compresses gradients',
      ],
      e: 'Early training with a large LR (especially at large batch) can diverge; warmup ramps the LR to let statistics stabilize, then cosine decay reduces it for fine convergence. This schedule is a staple for transformer pretraining at scale.',
      ref: 'LR warmup + cosine decay; large-batch training',
      tags: ['learning-rate', 'warmup', 'cosine'],
    },
    {
      d: 5,
      q: 'RLHF/PPO training is resource-heavy partly because it holds MULTIPLE models in memory, typically…',
      o: [
        'Just the policy',
        'Actor, critic, reward, and reference models',
        'Only the reward model',
        'One model',
      ],
      a: 1,
      x: [
        'The dataset only',
        'Two optimizers',
        'A single GPU',
      ],
      e: 'PPO-based RLHF runs several models simultaneously (actor, critic, reward, reference), each potentially large. Managing their placement/sharding and the generation+training loop makes RLHF systems substantially more complex and memory-hungry than supervised training.',
      ref: 'RLHF/PPO systems',
      tags: ['rlhf', 'ppo', 'memory'],
    },
    {
      d: 4,
      q: 'Weight streaming / offloading for inference of a very large model means…',
      o: [
        'Quantizing weights',
        'Stream each layer to the GPU on demand',
        'Sharding gradients',
        'Using FP64',
      ],
      a: 1,
      x: [
        'Removing layers',
        'Disabling attention',
        'Compressing the KV cache',
      ],
      e: 'Weight streaming fetches layer weights to the GPU on demand (overlapping with compute) so a model exceeding GPU memory can run, bottlenecked by the host↔GPU bandwidth. It trades throughput for the ability to serve huge models on limited GPU memory.',
      ref: 'Weight offloading/streaming; large-model inference',
      tags: ['weight-streaming', 'offload', 'inference'],
    },
    {
      d: 5,
      q: '1-bit Adam / PowerSGD reduce communication by…',
      o: [
        'Removing gradients',
        'Compress gradients with error feedback',
        'Using FP64',
        'Sharding the model',
      ],
      a: 1,
      x: [
        'Skipping steps',
        'Disabling the optimizer',
        'Using one GPU',
      ],
      e: 'These methods shrink the gradient communicated (1-bit or low-rank) and feed the residual error back into the next step so the lost information isn’t discarded — cutting comm volume on bandwidth-limited setups while maintaining convergence.',
      ref: '1-bit Adam; PowerSGD; error feedback',
      tags: ['gradient-compression', 'error-feedback', 'communication'],
    },
    {
      d: 4,
      q: 'For LLM decode, throughput is increased by BATCHING many requests because…',
      o: [
        'It reduces accuracy',
        'Amortizes the weight read across sequences',
        'It uses FP64',
        'It shards the model',
      ],
      a: 1,
      x: [
        'It removes the KV cache',
        'It disables attention',
        'It uses one GPU',
      ],
      e: 'Each decode step reads the full weights regardless of batch; processing many sequences together amortizes that read over more useful work (higher intensity), boosting throughput. KV-cache memory then limits how many sequences fit (hence paged/quantized KV).',
      ref: 'LLM decode batching; throughput',
      tags: ['decode', 'batching', 'throughput'],
    },
    {
      d: 4,
      q: 'Tensor parallelism for inference reduces single-request LATENCY, while pipeline parallelism mainly improves…',
      o: [
        'Accuracy',
        'Throughput (many requests in flight)',
        'Memory only',
        'Precision',
      ],
      a: 1,
      x: [
        'Latency only',
        'Nothing',
        'The dataset',
      ],
      e: 'TP parallelizes each token’s computation (lower latency); PP serializes a token through stages (higher per-token latency) but overlaps many requests across stages (higher throughput). Serving systems combine them per latency/throughput goals.',
      ref: 'Inference parallelism; TP vs PP',
      tags: ['tensor-parallel', 'pipeline', 'inference'],
    },
    {
      d: 5,
      q: 'Why does activation checkpointing INCREASE HFU but not necessarily MFU?',
      o: [
        'It reduces FLOPs',
        'Recompute adds hardware FLOPs, not model ones',
        'It increases accuracy',
        'It shards the model',
      ],
      a: 1,
      x: [
        'It removes the backward pass',
        'It uses FP64',
        'It compresses gradients',
      ],
      e: 'Checkpointing trades memory for recomputed activations — extra FLOPs the hardware performs (raising HFU) that don’t add to the model’s logical FLOPs (MFU unchanged). The HFU−MFU gap quantifies the recomputation overhead.',
      ref: 'Activation checkpointing; HFU vs MFU',
      tags: ['activation-checkpointing', 'hfu', 'mfu'],
    },
    {
      d: 4,
      q: 'In serving, continuous (in-flight) batching beats static batching because…',
      o: [
        'It is more accurate',
        'Swaps finished sequences for new ones each step',
        'It uses FP64',
        'It shards the model',
      ],
      a: 1,
      x: [
        'It removes the KV cache',
        'It disables attention',
        'It uses one GPU',
      ],
      e: 'Static batching wastes compute when some sequences finish early (the batch waits). Continuous batching dynamically admits new requests as slots free up each iteration, maximizing GPU utilization for variable-length generation — core to vLLM/TensorRT-LLM serving.',
      ref: 'Continuous batching; serving',
      tags: ['continuous-batching', 'serving', 'throughput'],
    },
    {
      d: 4,
      q: 'The reason KV-cache memory (not compute) limits decode batch size is…',
      o: [
        'Decode is compute-bound',
        'The KV cache, not the matmuls, fills memory',
        'Weights dominate',
        'The optimizer is resident',
      ],
      a: 1,
      x: [
        'Gradients dominate',
        'The dataset is loaded',
        'For FP64',
      ],
      e: 'Decode does small matmuls but accumulates KV per token per layer per sequence; this cache dominates memory and limits batch size/context. Paged/quantized KV and GQA/MQA target it to fit more/longer sequences.',
      ref: 'KV cache; decode batch limits',
      tags: ['kv-cache', 'decode', 'memory'],
    },
    {
      d: 5,
      q: 'A practical reason RLHF generation+training loops are hard to make efficient is…',
      o: [
        'They use FP64',
        'They alternate generation with training',
        'They have no models',
        'They are single-GPU',
      ],
      a: 1,
      x: [
        'They avoid attention',
        'They need no optimizer',
        'They are compute-free',
      ],
      e: 'RLHF interleaves slow generation (decode) with training updates over several models; balancing these phases (and their differing bottlenecks) and moving data between them efficiently is a major systems challenge, prompting specialized RLHF frameworks.',
      ref: 'RLHF systems; generation+training',
      tags: ['rlhf', 'generation', 'efficiency'],
    },
    {
      d: 4,
      q: 'Why is MFU rarely close to 100% even for well-optimized LLM training?',
      o: [
        'Bugs',
        'Non-matmul ops, comm, and bubbles',
        'FP64 usage',
        'The dataset',
      ],
      a: 1,
      x: [
        'Always a config error',
        'Too few GPUs',
        'Low precision',
      ],
      e: 'Peak FLOPs assume continuous tensor-core matmul. Real training spends time on non-matmul ops, communication, bubbles, and memory-bound phases, so MFU tops out well below 100%; 40–55% is considered very good for large-scale training.',
      ref: 'MFU; training efficiency reality',
      tags: ['mfu', 'efficiency', 'training'],
    },
    {
      d: 4,
      q: 'For very long context training, sequence/context parallelism is needed because…',
      o: [
        'It speeds the optimizer',
        'Attention scales with sequence length; split it',
        'It reduces parameters',
        'It uses FP64',
      ],
      a: 1,
      x: [
        'It shards the optimizer',
        'It disables attention',
        'It uses one GPU',
      ],
      e: 'Long sequences blow up attention memory/compute (O(s²) scores, large activations). Context parallelism (e.g. ring attention) partitions the sequence across GPUs and exchanges K/V, fitting and parallelizing long-context training that one GPU can’t hold.',
      ref: 'Context/sequence parallelism; long context',
      tags: ['context-parallel', 'long-context', 'attention'],
    },
    {
      d: 5,
      q: 'A reason FSDP can be slower than TP+DP for some models despite both fitting memory is…',
      o: [
        'FSDP is always slower',
        'FSDP all-gathers can exceed the compute',
        'TP uses no comm',
        'FSDP uses FP64',
      ],
      a: 1,
      x: [
        'DP is free',
        'They are identical',
        'For one GPU',
      ],
      e: 'FSDP’s just-in-time parameter all-gathers add communication that grows with model size; if it can’t hide behind compute, throughput suffers. A tensor-parallel layout (heavy comm on NVLink) plus DP can be more efficient for certain model/cluster shapes.',
      ref: 'FSDP vs TP+DP; communication',
      tags: ['fsdp', 'tensor-parallel', 'communication', 'tradeoff'],
    },
    {
      d: 4,
      q: 'The "effective batch size" for convergence in a 3D-parallel + gradient-accumulation setup is…',
      o: [
        'micro_batch × tensor_parallel',
        'micro_batch × grad_accum × DP_size',
        'micro_batch × pipeline_stages',
        'micro_batch × world_size',
      ],
      a: 1,
      x: [
        'micro_batch only',
        'global_batch × DP',
        'micro_batch × TP × PP',
      ],
      e: 'Only data parallelism and gradient accumulation increase the batch; tensor/pipeline parallelism shard a single replica (same batch). Effective batch = micro_batch × grad_accum × DP_size — the quantity that governs LR/convergence.',
      ref: 'Effective batch; 3D parallelism',
      tags: ['effective-batch', '3d-parallelism', 'convergence'],
    },
    {
      d: 4,
      q: 'Gradient accumulation interacts with BatchNorm/normalization statistics such that…',
      o: [
        'No interaction',
        'BN stats stay per micro-batch, not combined',
        'It averages BN stats',
        'It disables BN',
      ],
      a: 1,
      x: [
        'It uses FP64',
        'It shards the model',
        'It compresses gradients',
      ],
      e: 'Gradient accumulation sums gradients but each micro-batch’s normalization uses its own statistics; the large effective batch doesn’t improve BN’s statistic quality. LayerNorm (per-sample) sidesteps this, which is one reason transformers use it.',
      ref: 'Gradient accumulation; normalization stats',
      tags: ['gradient-accumulation', 'batchnorm', 'layernorm'],
    },
    {
      d: 5,
      q: 'Pipeline "1F1B" vs GPipe scheduling: 1F1B has the SAME bubble but…',
      o: [
        'More memory',
        'Lower peak activation memory',
        'No bubble',
        'Higher accuracy',
      ],
      a: 1,
      x: [
        'More communication',
        'FP64',
        'One stage',
      ],
      e: '1F1B interleaves forward and backward so each stage holds activations for only ~(depth) micro-batches, versus GPipe storing all forwards before any backward (memory scales with micro-batch count). Same bubble fraction, much less activation memory.',
      ref: '1F1B vs GPipe; activation memory',
      tags: ['1f1b', 'gpipe', 'pipeline', 'activation-memory'],
    },
    {
      d: 4,
      q: 'For inference, GQA/MQA reduce decode bandwidth because…',
      o: [
        'They quantize weights',
        'Fewer K/V heads shrink the KV cache to read',
        'They add heads',
        'They use FP64',
      ],
      a: 1,
      x: [
        'They shard the model',
        'They disable attention',
        'They remove weights',
      ],
      e: 'Decode reads the KV cache every step; GQA/MQA reduce the number of K/V heads (smaller cache), cutting both memory and the per-step bandwidth — improving decode throughput and enabling longer contexts/larger batches with small quality impact.',
      ref: 'GQA/MQA; decode bandwidth',
      tags: ['gqa', 'mqa', 'decode', 'bandwidth'],
    },
    {
      d: 5,
      q: 'A reason to overlap the parameter all-gather of layer N+1 with layer N’s compute in FSDP is correct ONLY if…',
      o: [
        'Always',
        'Layer N compute hides the gather',
        'Compute is zero',
        'For FP64',
      ],
      a: 1,
      x: [
        'NCCL is disabled',
        'It uses the CPU',
        'Streams serialize',
      ],
      e: 'FSDP prefetches the next unit’s params on a comm stream during current compute. The hide only works if the gather (sized by params) fits within layer N’s compute time; for small compute or large gathers, the gather is exposed — limiting throughput.',
      ref: 'FSDP prefetch/overlap',
      tags: ['fsdp', 'prefetch', 'overlap'],
    },
    {
      d: 4,
      q: 'Speculative decoding’s speedup depends critically on…',
      o: [
        'Model size',
        'The draft-token acceptance rate',
        'FP64',
        'The dataset',
      ],
      a: 1,
      x: [
        'The batch size only',
        'The optimizer',
        'The KV layout',
      ],
      e: 'The target model verifies k draft tokens in one pass and accepts the agreed prefix. High acceptance ⇒ many tokens per target step (big speedup); low acceptance ⇒ frequent rejects and wasted draft compute. Draft quality/alignment drives the gain.',
      ref: 'Speculative decoding; acceptance rate',
      tags: ['speculative-decoding', 'acceptance-rate', 'inference'],
    },
    {
      d: 4,
      q: 'Why does increasing pipeline micro-batches reduce the bubble but eventually hurt?',
      o: [
        'It never hurts',
        'Smaller micro-batches are less efficient GEMMs',
        'It removes communication',
        'For FP64',
      ],
      a: 1,
      x: [
        'It increases accuracy',
        'It shards the model',
        'It uses one GPU',
      ],
      e: 'The bubble fraction ≈ (p−1)/(m+p−1) shrinks with more micro-batches m, but smaller micro-batches reduce GEMM efficiency and add overhead. The micro-batch count balances bubble reduction against per-micro-batch efficiency — an empirical optimum.',
      ref: 'Pipeline bubble; micro-batch count',
      tags: ['pipeline', 'micro-batch', 'bubble'],
    },
    {
      d: 5,
      q: 'A reason large-batch training needs LR warmup AND careful schedules (beyond linear scaling) is…',
      o: [
        'Bugs',
        'Linear LR scaling breaks down at huge batches',
        'Small batches diverge',
        'For FP64',
      ],
      a: 1,
      x: [
        'It reduces accuracy always',
        'It shards the model',
        'It disables the optimizer',
      ],
      e: 'Linear LR scaling holds up to a point; past it, the large LR destabilizes early training. Warmup, layer-wise adaptive optimizers (LAMB/LARS), and tuned decay are needed for stable convergence at extreme batch sizes — a known large-batch challenge.',
      ref: 'Large-batch training; warmup + LAMB',
      tags: ['large-batch', 'warmup', 'lamb'],
    },
    {
      d: 4,
      q: 'The systems benefit of paged KV cache (vLLM) over a contiguous per-sequence cache is…',
      o: [
        'Higher precision',
        'Packs KV into shared pages, no waste',
        'Faster matmul',
        'Using FP64',
      ],
      a: 1,
      x: [
        'Disabling attention',
        'Removing the KV cache',
        'Sharding the model',
      ],
      e: 'Contiguous per-sequence KV buffers waste memory (reserved for max length, fragmented). Paging KV into shared blocks (like OS virtual memory) removes that waste and allows sharing (e.g. common prefixes), increasing serving concurrency/throughput.',
      ref: 'vLLM PagedAttention; KV memory',
      tags: ['paged-kv', 'serving', 'memory'],
    },
    {
      d: 5,
      q: 'When scaling a model 10×, keeping training efficient typically requires CHANGING the parallelism mix because…',
      o: [
        'It does not',
        'Bigger models need more TP/PP',
        'Only DP matters',
        'For FP64',
      ],
      a: 1,
      x: [
        'Parallelism is fixed',
        'It uses one GPU',
        'Precision changes',
      ],
      e: 'Optimal parallelism is model- and hardware-specific. Scaling up shifts memory pressure and communication patterns, so the DP/TP/PP/CP/EP degrees must be retuned (e.g. more TP to fit, mapped to NVLink) to maintain efficiency — there’s no one fixed recipe.',
      ref: 'Parallelism tuning; scaling',
      tags: ['parallelism', 'scaling', 'tuning'],
    },
    {
      d: 4,
      q: 'A reason inference is often MEMORY-BANDWIDTH-bound while training is more COMPUTE-bound is…',
      o: [
        'They are the same',
        'Training: big matmuls; decode: reads weights/KV',
        'Inference uses FP64',
        'Training has no matmuls',
      ],
      a: 1,
      x: [
        'Inference is parallel',
        'Training is sequential',
        'For one GPU',
      ],
      e: 'Training’s large batches give high arithmetic intensity (compute-bound), with a heavy backward pass. Decode processes one token at a time, so weight/KV reads dominate (bandwidth-bound). This is why inference optimization centers on memory (quantization, KV management).',
      ref: 'Inference vs training bottlenecks',
      tags: ['inference', 'training', 'bandwidth', 'compute'],
    },
  ],
  { idPrefix: 'distributed-e' }
)
