import { defineQuestions } from './_helpers.js'

// CUDA Basics & Programming Model — threads, blocks, grids, kernels,
// host/device, qualifiers, launch configuration, indexing.
//
// `x` holds extra distractors beyond the primary `o` set: the engine samples 4
// options (correct + 3 random distractors) and shuffles them each serving, so
// the choices and answer position vary between players and replays.
export default defineQuestions('basics', [
  {
    d: 1,
    q: 'In CUDA terminology, what is a "kernel"?',
    o: [
      'The part of the operating system that schedules GPU work',
      'A function that runs on the GPU and is executed by many threads in parallel',
      'The on-chip cache shared by all streaming multiprocessors',
      'A compiled binary blob containing host-side driver code',
    ],
    a: 1,
    x: [
      'A single thread of execution on one CUDA core',
      'A wrapper that copies memory between host and device',
      'The smallest unit of GPU memory allocation',
    ],
    e: 'A CUDA kernel is a function (declared with __global__) launched from the host and executed on the device by a grid of threads, each running the same code on different data (SIMT).',
    ref: 'CUDA C++ Programming Guide §2.1; PMPP Ch. 2',
    tags: ['kernel', 'programming-model'],
  },
  {
    d: 1,
    q: 'Which function qualifier declares a kernel that is callable from the host and runs on the device?',
    o: ['__device__', '__host__', '__global__', '__kernel__'],
    a: 2,
    x: ['__launch__', '__gpu__', '__entry__'],
    e: '__global__ marks a kernel: launched from host (or device, with dynamic parallelism) and executed on the device. It must return void. __device__ functions run on and are called from the device; __host__ run on the host.',
    ref: 'CUDA C++ Programming Guide §7.1',
    tags: ['qualifiers', 'kernel'],
  },
  {
    d: 1,
    q: 'A kernel is launched with the configuration <<<4, 256>>>. How many threads run in total?',
    o: ['256', '4', '1024', '64'],
    a: 2,
    x: ['260', '512', '32'],
    e: 'The first launch parameter is the number of blocks in the grid (4) and the second is the number of threads per block (256). Total threads = 4 × 256 = 1024.',
    ref: 'CUDA C++ Programming Guide §2.2',
    tags: ['launch-config', 'grid', 'block'],
  },
  {
    d: 1,
    q: 'Within a kernel, which built-in variable gives a thread its index inside its block?',
    o: ['blockIdx', 'threadIdx', 'gridDim', 'warpSize'],
    a: 1,
    x: ['blockDim', 'laneId', 'globalIdx'],
    e: 'threadIdx is the thread’s coordinate within its block. blockIdx is the block’s coordinate within the grid, blockDim is the block size, and gridDim is the grid size.',
    ref: 'CUDA C++ Programming Guide §2.2',
    tags: ['indexing', 'built-in-variables'],
  },
  {
    d: 1,
    q: 'What is the standard expression to compute a unique global index for a 1D grid of 1D blocks?',
    o: [
      'threadIdx.x',
      'blockIdx.x * gridDim.x + threadIdx.x',
      'blockIdx.x * blockDim.x + threadIdx.x',
      'blockIdx.x + threadIdx.x',
    ],
    a: 2,
    x: [
      'threadIdx.x * blockDim.x + blockIdx.x',
      'blockDim.x * gridDim.x + threadIdx.x',
      'blockIdx.x * threadIdx.x + blockDim.x',
    ],
    e: 'Each block contributes blockDim.x threads, so the global index is blockIdx.x * blockDim.x + threadIdx.x. A common bug is using gridDim instead of blockDim.',
    ref: 'PMPP Ch. 2; CUDA C++ Programming Guide §2.2',
    tags: ['indexing', 'global-index'],
  },
  {
    d: 2,
    q: 'Why do most kernels include a guard like `if (i < n) { ... }` after computing the global index `i`?',
    o: [
      'To skip threads in diverged warps for performance',
      'Because grids are usually launched with more threads than data elements (rounded up to whole blocks)',
      'To prevent threads from writing to shared memory',
      'Because threadIdx can legally exceed blockDim',
    ],
    a: 1,
    x: [
      'To force the compiler to coalesce global memory accesses',
      'Because shared memory must be zeroed before the first use',
      'To keep every warp aligned to a 32-thread boundary',
    ],
    e: 'Block size rarely divides the problem size exactly, so the grid is rounded up (e.g. ceil(n/blockDim)) and launches extra threads. The bounds check stops those surplus threads from reading/writing out of range.',
    ref: 'PMPP Ch. 2',
    tags: ['bounds-check', 'launch-config'],
  },
  {
    d: 2,
    q: 'What does cudaMemcpy with kind cudaMemcpyHostToDevice do?',
    o: [
      'Allocates device memory and returns a pointer',
      'Copies data from host (CPU) memory to device (GPU) memory',
      'Copies data between two GPUs over NVLink',
      'Maps host memory into the device address space without copying',
    ],
    a: 1,
    x: [
      'Copies data from device memory back to host memory',
      'Prefetches managed memory onto the device',
      'Registers a host pointer for zero-copy device access',
    ],
    e: 'cudaMemcpy(dst, src, bytes, cudaMemcpyHostToDevice) performs a blocking copy from CPU memory to GPU memory. Allocation is cudaMalloc; zero-copy mapping is a different mechanism (mapped pinned memory).',
    ref: 'CUDA Runtime API; PMPP Ch. 2',
    tags: ['memcpy', 'host-device'],
  },
  {
    d: 1,
    q: 'A kernel launch in CUDA is, by default, what with respect to the host?',
    o: [
      'Synchronous — the host blocks until the kernel finishes',
      'Asynchronous — control returns to the host immediately',
      'It depends on the number of blocks',
      'Synchronous only in debug builds',
    ],
    a: 1,
    x: [
      'Synchronous unless cudaMemcpyAsync is used',
      'Asynchronous only on the non-default stream',
      'Blocking until the first __syncthreads() is reached',
    ],
    e: 'Kernel launches are asynchronous: the call returns to the host right away and the kernel runs on the device. You must synchronize (e.g. cudaDeviceSynchronize, or an implicit sync via a blocking copy) before relying on results.',
    ref: 'CUDA C++ Programming Guide §6.2.8',
    tags: ['async', 'launch'],
  },
  {
    d: 2,
    q: 'What must be true of a kernel’s return type?',
    o: [
      'It must return an int error code',
      'It must return void',
      'It can return any POD type',
      'It must return a cudaError_t',
    ],
    a: 1,
    x: [
      'It must return a device pointer to the result',
      'It must return the number of threads launched',
      'It must return __global__',
    ],
    e: 'A __global__ kernel must return void. Results are communicated through memory (device pointers passed as arguments). Errors are reported separately via cudaGetLastError / cudaDeviceSynchronize return codes.',
    ref: 'CUDA C++ Programming Guide §7.1',
    tags: ['kernel', 'qualifiers'],
  },
  {
    d: 2,
    q: 'Which call would you use right after a kernel launch to catch launch-configuration errors (e.g. too many threads per block)?',
    o: [
      'cudaMalloc',
      'cudaGetLastError',
      'cudaMemcpy with the same pointer',
      'There is no way to detect launch errors',
    ],
    a: 1,
    x: [
      'cudaStreamQuery',
      'A C++ try/catch around the launch',
      'cudaEventRecord',
    ],
    e: 'Because launches are asynchronous and return void, errors surface via cudaGetLastError() immediately after launch (for launch/config errors) and via cudaDeviceSynchronize() (for runtime errors during execution).',
    ref: 'CUDA C++ Programming Guide §6.2; CUDA Runtime API',
    tags: ['error-handling', 'launch'],
  },
  {
    d: 1,
    q: 'What is the maximum number of threads per block on all current CUDA-capable GPUs (compute capability 2.0+)?',
    o: ['256', '512', '1024', '2048'],
    a: 2,
    x: ['768', '1536', '4096'],
    e: 'The hard limit is 1024 threads per block. The product blockDim.x * blockDim.y * blockDim.z must not exceed 1024. (2048 is a common per-SM resident-thread limit, which is different.)',
    ref: 'CUDA C++ Programming Guide, Compute Capabilities table',
    tags: ['limits', 'block'],
  },
  {
    d: 2,
    q: 'You want a 2D grid covering an image of width W and height H with 16×16 blocks. Which dim3 grid size is correct?',
    o: [
      'dim3((W+15)/16, (H+15)/16)',
      'dim3(W/16, H/16)',
      'dim3(W*H/256)',
      'dim3(16, 16)',
    ],
    a: 0,
    x: [
      'dim3((W+16)/16, (H+16)/16)',
      'dim3(W, H)',
      'dim3(256, W*H)',
    ],
    e: 'You need ceil(W/16) × ceil(H/16) blocks so every pixel is covered even when W or H isn’t a multiple of 16. Integer (W+15)/16 implements the ceiling. Plain W/16 truncates and misses edge pixels.',
    ref: 'PMPP Ch. 3',
    tags: ['2d-grid', 'launch-config', 'indexing'],
  },
  {
    d: 3,
    q: 'A __device__ function and a __host__ function can be the same function if it is marked how, and what does that imply?',
    o: [
      '__global__ — it is compiled once for both targets',
      '__host__ __device__ — the compiler generates both a host and a device version',
      '__inline__ — it is inlined on whichever side calls it',
      'You cannot share a function between host and device',
    ],
    a: 1,
    x: [
      '__managed__ — it runs wherever the data currently lives',
      '__both__ — a single fat binary is emitted',
      '__device__ alone — host code may also call __device__ functions',
    ],
    e: 'Marking a function __host__ __device__ tells nvcc to compile two versions, one for each target, so the same source can run on CPU and GPU. This is heavily used in header-only libraries (Thrust, CUTLASS).',
    ref: 'CUDA C++ Programming Guide §7.1',
    tags: ['qualifiers', 'host-device'],
  },
  {
    d: 1,
    q: 'Which CUDA API allocates memory on the device (GPU)?',
    o: ['malloc', 'cudaMalloc', 'cudaHostAlloc', 'new'],
    a: 1,
    x: ['cudaMemcpy', 'gpuMalloc', 'cudaDeviceAlloc'],
    e: 'cudaMalloc(&ptr, bytes) allocates linear device memory and returns a device pointer. malloc/new allocate host memory; cudaHostAlloc allocates page-locked host memory.',
    ref: 'CUDA Runtime API',
    tags: ['memory', 'allocation'],
  },
  {
    d: 2,
    q: 'What is the third launch parameter in <<<grid, block, shmem, stream>>>?',
    o: [
      'The number of registers per thread',
      'The amount of dynamic shared memory in bytes per block',
      'The L2 cache reservation',
      'A priority hint',
    ],
    a: 1,
    x: [
      'The number of blocks per cluster',
      'The maximum number of warps per SM',
      'The constant memory size to reserve',
    ],
    e: 'The optional third argument is the per-block dynamic shared-memory size in bytes, accessed in the kernel via an `extern __shared__` array. The optional fourth argument selects the stream.',
    ref: 'CUDA C++ Programming Guide §7.3',
    tags: ['launch-config', 'shared-memory'],
  },
  {
    d: 2,
    q: 'On a GPU, what does SIMT stand for and what is the key idea?',
    o: [
      'Single Instruction Multiple Threads — groups of threads execute the same instruction in lockstep',
      'Serial Instruction Multi-Tasking — threads time-slice on one core',
      'Streaming Inter-Module Transfer — the on-chip data path',
      'Single Instruction Multiple Tasks — one thread runs many tasks',
    ],
    a: 0,
    x: [
      'Single Instruction Multiple Data — vector lanes share one program counter',
      'Synchronous Independent Multi-Threading — threads never diverge',
      'Scalar Instruction Memory Transfer — a DMA mechanism',
    ],
    e: 'SIMT = Single Instruction, Multiple Threads. The hardware executes one instruction across a warp (32 threads) at a time. It resembles SIMD but each thread has its own registers and can follow its own control path (with divergence penalties).',
    ref: 'CUDA C++ Programming Guide §4; PMPP Ch. 4',
    tags: ['simt', 'execution-model'],
  },
  {
    d: 3,
    q: 'Why can’t you simply dereference a device pointer (returned by cudaMalloc) on the host?',
    o: [
      'Device pointers are encrypted',
      'It points into the GPU’s address space, which is not directly accessible from host code in general',
      'Host code runs in kernel mode and lacks permission',
      'You actually can; it is always safe',
    ],
    a: 1,
    x: [
      'Device pointers are only 32 bits wide on the host',
      'The pointer is valid only inside the kernel that received it',
      'You must call cudaDeviceSynchronize to make it host-readable',
    ],
    e: 'A plain cudaMalloc pointer refers to GPU memory. Host code dereferencing it is undefined behavior (typically a segfault). Use cudaMemcpy, or use managed/unified memory (cudaMallocManaged) for pointers valid on both sides.',
    ref: 'CUDA C++ Programming Guide §6.2',
    tags: ['memory', 'pointers', 'host-device'],
  },
  {
    d: 2,
    q: 'What does cudaDeviceSynchronize() do?',
    o: [
      'Resets the GPU to a clean state',
      'Blocks the host until all previously issued device work has completed',
      'Synchronizes threads within a block',
      'Copies all device memory back to the host',
    ],
    a: 1,
    x: [
      'Blocks the host until only the default stream is idle',
      'Flushes the L2 cache to device memory',
      'Frees all outstanding device allocations',
    ],
    e: 'cudaDeviceSynchronize() blocks the calling host thread until the device has finished all preceding work on all streams. (Intra-block thread sync is __syncthreads(), which is a different concept.)',
    ref: 'CUDA Runtime API',
    tags: ['synchronization', 'host-device'],
  },
  {
    d: 1,
    q: 'What is warpSize on all current NVIDIA GPUs?',
    o: ['16', '32', '64', '128'],
    a: 1,
    x: ['8', '48', '256'],
    e: 'A warp is 32 threads on every NVIDIA GPU to date. The built-in variable warpSize equals 32. Threads are scheduled and execute in warp-sized groups.',
    ref: 'CUDA C++ Programming Guide §4.1',
    tags: ['warp', 'limits'],
  },
  {
    d: 3,
    q: 'In a 2D block, how are threads grouped into warps?',
    o: [
      'By columns of threadIdx.y',
      'By the linearized index threadIdx.x + threadIdx.y*blockDim.x, in groups of 32',
      'Randomly each launch',
      'Each row is exactly one warp regardless of width',
    ],
    a: 1,
    x: [
      'In column-major order: threadIdx.y + threadIdx.x*blockDim.y, in groups of 32',
      'By the hardware scheduler with no fixed mapping',
      'Each warp is one full value of threadIdx.y',
    ],
    e: 'Threads are linearized as x + y*blockDim.x + z*blockDim.x*blockDim.y, then partitioned into consecutive groups of 32 to form warps. This is why making blockDim.x a multiple of 32 helps coalescing and warp utilization.',
    ref: 'CUDA C++ Programming Guide §4.1',
    tags: ['warp', 'indexing', '2d-block'],
  },
  {
    d: 2,
    q: 'Which statement about blocks in a grid is TRUE?',
    o: [
      'Blocks always execute in increasing blockIdx order',
      'Blocks may execute in any order, concurrently or sequentially, and must not assume a global ordering',
      'Blocks can synchronize with each other using __syncthreads()',
      'All blocks share the same shared memory contents',
    ],
    a: 1,
    x: [
      'All blocks of a grid are always resident on the GPU at once',
      'Block execution order is determined by the stream’s priority',
      'Blocks execute in reverse blockIdx order on some architectures',
    ],
    e: 'The model guarantees no ordering or concurrency relationship between blocks; this independence is what lets the same code scale across GPUs with different SM counts. __syncthreads() only synchronizes threads within one block.',
    ref: 'CUDA C++ Programming Guide §2.2; PMPP Ch. 4',
    tags: ['blocks', 'scalability', 'ordering'],
  },
  {
    d: 3,
    q: 'What does the nvcc flag -arch=sm_90 (or --gpu-architecture) control?',
    o: [
      'The number of SMs to use',
      'The target compute capability / SASS architecture the code is compiled for',
      'The CUDA runtime version',
      'The maximum threads per block',
    ],
    a: 1,
    x: [
      'The number of registers allocated per thread',
      'The CUDA driver API version to link against',
      'The PTX optimization level (-O)',
    ],
    e: 'It selects the target GPU architecture (compute capability), e.g. sm_90 for Hopper H100. This determines which SASS is generated and which features (e.g. TMA, wgmma) are available. -code/-gencode let you embed multiple targets and PTX for forward compatibility.',
    ref: 'CUDA Compiler Driver NVCC docs',
    tags: ['nvcc', 'compute-capability', 'compilation'],
  },
  {
    d: 2,
    q: 'What is the difference between cudaMalloc and cudaMallocManaged?',
    o: [
      'No difference; they are aliases',
      'cudaMallocManaged returns Unified Memory accessible from both host and device, migrated on demand',
      'cudaMallocManaged is faster but device-only',
      'cudaMalloc is for pinned host memory',
    ],
    a: 1,
    x: [
      'cudaMallocManaged pins the allocation in host RAM only',
      'cudaMalloc returns managed memory by default since CUDA 12',
      'cudaMallocManaged allocates constant memory',
    ],
    e: 'cudaMallocManaged allocates Unified (managed) Memory with a single pointer usable on host and device; the driver migrates pages between CPU and GPU on access (or via prefetch/hints). cudaMalloc is device-only.',
    ref: 'CUDA C++ Programming Guide §K (Unified Memory)',
    tags: ['unified-memory', 'allocation'],
  },
  {
    d: 4,
    q: 'A kernel computes correctly for n = 1000 with <<< (n+255)/256, 256 >>> but a colleague rewrites the launch as <<< n/256, 256 >>>. What is the bug?',
    o: [
      'It launches too many blocks and reads out of bounds',
      'n/256 = 3 blocks cover only 768 elements; elements 768..999 are never processed',
      'Nothing — integer division rounds up',
      'It causes a shared-memory overflow',
    ],
    a: 1,
    x: [
      'n/256 rounds up to 4 blocks and processes 24 elements twice',
      'It allocates too little shared memory for the final block',
      'The block size 256 is not a multiple of the warp size',
    ],
    e: 'Integer division truncates: 1000/256 = 3, launching 768 threads and silently skipping the last 232 elements. The ceiling form (n+255)/256 = 4 blocks covers all elements (with a bounds check guarding the surplus).',
    ref: 'PMPP Ch. 2',
    tags: ['launch-config', 'bugs', 'indexing'],
  },
])
