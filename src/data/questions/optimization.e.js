import { defineQuestions } from './_helpers.js'

// Kernel Optimization — batch E. Dataflow, conv methods, scan algorithms, SpMV,
// layout for tensor cores, fused epilogues. Distinct from optimization.js/.b/.c/.d.
export default defineQuestions(
  'optimization',
  [
    {
      d: 5,
      q: 'In GEMM/conv accelerator dataflow terms, "output stationary" means…',
      o: [
        'Inputs stay in registers',
        'Each output (accumulator) tile stays resident (in registers/accumulator) while inputs stream through it — maximizing accumulator reuse and minimizing output writes',
        'Weights stay fixed',
        'Nothing moves',
      ],
      a: 1,
      x: [
        'Outputs are written every step',
        'It avoids tiling',
        'It uses the host',
      ],
      e: 'Output-stationary keeps the partial-sum tile in fast storage across the contraction, accumulating as operands stream by, so the output is written once at the end. (Weight-/input-stationary instead keep weights/inputs resident.) GPU GEMM is largely output-stationary in registers.',
      ref: 'Dataflow taxonomy (Eyeriss); GEMM',
      tags: ['dataflow', 'output-stationary', 'gemm'],
    },
    {
      d: 4,
      q: 'Implicit GEMM for convolution avoids the memory blow-up of im2col by…',
      o: [
        'Skipping the convolution',
        'Computing the convolution AS a GEMM without materializing the lowered (im2col) matrix — generating the needed input patches on the fly from indices',
        'Using FP64',
        'Using the CPU',
      ],
      a: 1,
      x: [
        'Storing all patches',
        'Avoiding tensor cores',
        'Using texture memory only',
      ],
      e: 'im2col explicitly expands input patches into a big matrix (memory- and bandwidth-heavy). Implicit GEMM maps convolution onto a matmul while computing patch addresses on the fly, so it uses tensor cores without materializing the lowered matrix — cuDNN/CUTLASS use it.',
      ref: 'Implicit GEMM convolution; CUTLASS',
      tags: ['implicit-gemm', 'convolution', 'im2col'],
    },
    {
      d: 5,
      q: 'Winograd convolution speeds up small-filter convs by…',
      o: [
        'Using FFT',
        'Transforming inputs/filters so the convolution needs FEWER multiplications (trading multiplies for cheaper adds/transforms) — beneficial for 3×3 filters',
        'Skipping channels',
        'Using FP64',
      ],
      a: 1,
      x: [
        'Materializing patches',
        'Using the CPU',
        'Avoiding accumulation',
      ],
      e: 'Winograd applies linear transforms so the elementwise stage uses fewer multiplications than direct convolution, lowering arithmetic for small filters (e.g. 3×3). The trade-off is extra transform overhead and numerical sensitivity. cuDNN offers it as an algorithm.',
      ref: 'Winograd convolution; cuDNN',
      tags: ['winograd', 'convolution', 'arithmetic-reduction'],
    },
    {
      d: 4,
      q: 'For convolutions on tensor cores, the NHWC ("channels-last") layout is preferred over NCHW because…',
      o: [
        'It uses less memory',
        'NHWC makes the channel dimension contiguous (innermost), matching tensor-core/implicit-GEMM access patterns for coalesced, aligned loads of channel vectors',
        'NCHW is faster',
        'It avoids accumulation',
      ],
      a: 1,
      x: [
        'It lowers precision',
        'It avoids tensor cores',
        'It is required by FP64',
      ],
      e: 'Channels-last (NHWC) puts channels contiguous, aligning with how implicit-GEMM/tensor-core kernels consume channel vectors — enabling coalesced, vectorized loads. NCHW strides channels by H×W, hurting those accesses. Frameworks offer channels-last memory format for this.',
      ref: 'NHWC channels-last; tensor-core conv',
      tags: ['nhwc', 'channels-last', 'convolution', 'tensor-cores'],
    },
    {
      d: 4,
      q: 'Padding the channel count to a multiple of 8 (FP16) for tensor-core convolution helps because…',
      o: [
        'It reduces memory',
        'Tensor-core MMAs operate on fixed tile widths; non-multiple channel counts cause partial/predicated tiles or slower fallbacks, so padding aligns to the MMA shape',
        'It increases precision',
        'It avoids the K loop',
      ],
      a: 1,
      x: [
        'It avoids tensor cores',
        'It uses the CPU',
        'It is for FP64',
      ],
      e: 'Like padding GEMM dimensions, padding channels to the tensor-core tile multiple (e.g. 8 for FP16) avoids wasted/partial MMA tiles and library fallbacks, keeping the tensor cores fully utilized.',
      ref: 'Tensor-core conv; channel alignment',
      tags: ['padding', 'channels', 'tensor-cores'],
    },
    {
      d: 5,
      q: 'A work-EFFICIENT parallel scan (Blelloch) does O(n) work in two sweeps, vs Hillis-Steele’s O(n log n). The trade-off is…',
      o: [
        'Blelloch is always best',
        'Blelloch does less total work (better for large n / bandwidth) but has two phases and more complex indexing; Hillis-Steele is simpler and lower-latency for small n despite more work',
        'They are identical',
        'Hillis-Steele is work-efficient',
      ],
      a: 1,
      x: [
        'Blelloch is O(n log n)',
        'Both are O(n²)',
        'Neither parallelizes',
      ],
      e: 'Blelloch (up-sweep/down-sweep) is work-efficient (O(n)); Hillis-Steele is step-efficient but does O(n log n) work. For large arrays the work-efficient version wins (less total work/bandwidth); for tiny warp/block scans the simpler one can be faster.',
      ref: 'Parallel prefix sum; Blelloch vs Hillis-Steele',
      tags: ['scan', 'work-efficient', 'prefix-sum'],
    },
    {
      d: 5,
      q: 'CUB’s single-pass "decoupled look-back" scan achieves near-bandwidth-optimal device scan by…',
      o: [
        'Two full passes',
        'Each block computing its local aggregate, publishing it, then "looking back" at predecessors’ published partials to derive its prefix — avoiding a separate global pass over the data',
        'Sorting first',
        'Using atomics per element',
      ],
      a: 1,
      x: [
        'Using the CPU',
        'Recomputing everything',
        'Using FP64',
      ],
      e: 'Decoupled look-back lets blocks compute prefixes in one pass: a block publishes its aggregate, then inspects predecessors’ states (aggregate/prefix) to compute its exclusive prefix, propagating prefixes without a second data pass — a key to fast device scan.',
      ref: 'Merrill & Garland, "Single-pass Parallel Prefix Scan"; CUB',
      tags: ['scan', 'decoupled-lookback', 'cub'],
    },
    {
      d: 4,
      q: 'For SpMV in CSR, the "vector" kernel (a warp per row) beats the "scalar" kernel (a thread per row) when…',
      o: [
        'Rows are very short',
        'Rows have many nonzeros — a warp cooperatively processing a row coalesces the column/value reads, whereas one thread per row causes uncoalesced, imbalanced access',
        'The matrix is dense',
        'Always',
      ],
      a: 1,
      x: [
        'Rows have one element',
        'It uses tensor cores',
        'For FP64 only',
      ],
      e: 'Scalar SpMV (thread/row) suffers load imbalance and uncoalesced access for varied/long rows. The vector kernel assigns a warp per row so the row’s nonzeros are read coalesced and reduced via shuffles — better for rows with many nonzeros.',
      ref: 'CSR SpMV; scalar vs vector kernels',
      tags: ['spmv', 'csr', 'coalescing'],
    },
    {
      d: 4,
      q: 'Fusing LayerNorm (compute mean/var, normalize, scale/shift) into one kernel rather than several passes mainly saves…',
      o: [
        'Compute',
        'Repeated reads/writes of the activation row to/from HBM — the fused kernel computes statistics and normalizes with the row kept on-chip (often via Welford), cutting memory traffic',
        'Registers',
        'Occupancy',
      ],
      a: 1,
      x: [
        'Launch latency only',
        'Bank conflicts',
        'Divergence',
      ],
      e: 'Separate mean, variance, and normalize passes each stream the row through HBM. Fusing them keeps the row on-chip across the steps (computing stats in one pass via Welford, then normalizing), eliminating redundant global traffic — a big win for this memory-bound op.',
      ref: 'Fused LayerNorm; memory traffic',
      tags: ['fusion', 'layernorm', 'memory-traffic'],
    },
    {
      d: 5,
      q: 'Top-k selection on the GPU for small k is often done with…',
      o: [
        'A full sort always',
        'A bitonic/sorting network or a per-thread/warp heap that tracks the k largest, avoiding a full O(n log n) sort of the whole array',
        'atomicMax only',
        'The CPU',
      ],
      a: 1,
      x: [
        'A reduction',
        'A histogram',
        'FP64',
      ],
      e: 'For small k, maintaining a small candidate set (bitonic top-k, warp/block heaps, or threshold-based selection) is far cheaper than fully sorting n elements. Libraries provide tuned top-k; the key is avoiding a complete sort.',
      ref: 'GPU top-k; selection algorithms',
      tags: ['top-k', 'selection', 'sorting-network'],
    },
    {
      d: 4,
      q: 'Triple buffering (vs double) in a software pipeline is warranted when…',
      o: [
        'Always',
        'The producer latency (e.g. HBM load) exceeds what two buffers can hide given the per-stage compute — a third buffer adds more in-flight slack, at extra shared-memory cost',
        'Shared memory is scarce',
        'The kernel is compute-bound',
      ],
      a: 1,
      x: [
        'Never',
        'For elementwise ops',
        'To reduce registers',
      ],
      e: 'More buffers deepen the pipeline to hide longer load latency. If two stages can’t cover the load time (math waits), a third helps — but each buffer costs shared memory/occupancy, so add stages only until latency is hidden.',
      ref: 'Software pipelining; buffer count',
      tags: ['triple-buffering', 'pipelining', 'latency-hiding'],
    },
    {
      d: 4,
      q: 'A vectorized epilogue in a GEMM (writing the output tile with float4 and a fused activation) helps because it…',
      o: [
        'Increases precision',
        'Reduces the number/width of store instructions and applies bias/activation while results are in registers — improving the epilogue’s store throughput and avoiding a separate kernel',
        'Avoids tensor cores',
        'Uses the CPU',
      ],
      a: 1,
      x: [
        'Reduces occupancy',
        'Disables coalescing',
        'Adds atomics',
      ],
      e: 'The epilogue applies scaling/bias/activation to the accumulator and stores it. Vectorizing the stores (and fusing the elementwise ops) cuts store instructions and avoids a separate elementwise pass over the output — a standard CUTLASS/cuBLASLt optimization.',
      ref: 'GEMM epilogue; vectorized stores',
      tags: ['epilogue', 'vectorized-stores', 'fusion'],
    },
    {
      d: 4,
      q: 'Balancing loop unrolling against register pressure means…',
      o: [
        'Always unroll fully',
        'Unrolling exposes ILP but increases live registers; past a point it spills (or cuts occupancy), so you pick an unroll factor that maximizes ILP without triggering spills/occupancy cliffs',
        'Never unroll',
        'Unrolling reduces registers',
      ],
      a: 1,
      x: [
        'Unrolling is free',
        'It only affects memory',
        'It uses tensor cores',
      ],
      e: 'Unrolling helps ILP/throughput but more iterations live at once means more registers. The optimum balances ILP gains against register pressure (spills/occupancy). Profile unroll factors; the best depends on the kernel and architecture.',
      ref: 'Unrolling vs register pressure',
      tags: ['unrolling', 'registers', 'ilp'],
    },
    {
      d: 5,
      q: 'In FlashAttention, storing the running statistics (max m and sum l) per query row rather than the full scores enables…',
      o: [
        'Higher precision',
        'O(1) extra state per query instead of O(N) scores — combined with rescaling, this lets the kernel process K/V tiles incrementally without ever materializing the N×N matrix',
        'Avoiding softmax',
        'Using FP64',
      ],
      a: 1,
      x: [
        'Storing all scores',
        'Using the CPU',
        'Avoiding tensor cores',
      ],
      e: 'Tracking only (m, l) and the output accumulator per query row (online softmax) means constant extra state. As each K/V tile is processed, the running max/sum and output are rescaled — so the quadratic score matrix is never stored in HBM.',
      ref: 'FlashAttention; online softmax state',
      tags: ['flash-attention', 'online-softmax', 'state'],
    },
    {
      d: 4,
      q: 'A "weight stationary" dataflow is attractive when…',
      o: [
        'Weights change often',
        'The same weights are reused across many inputs (e.g. a conv filter over a feature map / batched inference), so keeping weights resident amortizes their load over many MACs',
        'There are no weights',
        'Inputs are reused more',
      ],
      a: 1,
      x: [
        'Outputs are reused most',
        'It avoids tiling',
        'For FP64',
      ],
      e: 'Weight-stationary keeps filters/weights in fast storage while streaming inputs, maximizing weight reuse — good when weights are reused heavily relative to inputs/outputs (e.g. convolution, batched matmul with shared weights).',
      ref: 'Dataflow taxonomy; weight stationary',
      tags: ['dataflow', 'weight-stationary', 'reuse'],
    },
    {
      d: 4,
      q: 'Blocking a matmul for the L2 cache (a coarse tile larger than the threadblock tile) helps because…',
      o: [
        'It reduces registers',
        'It groups the work of multiple threadblocks so their shared operands stay resident in L2 across them, cutting HBM re-reads of the same data (complementing shared-memory tiling)',
        'It avoids tensor cores',
        'It lowers precision',
      ],
      a: 1,
      x: [
        'It increases divergence',
        'It uses the CPU',
        'It disables the L1',
      ],
      e: 'A second blocking level sized to L2 (e.g. via threadblock swizzle / rasterization) keeps operands reused by neighboring threadblocks in L2, reducing HBM traffic beyond what per-block shared memory captures — multi-level tiling.',
      ref: 'L2 blocking; threadblock swizzle',
      tags: ['l2-blocking', 'tiling', 'gemm'],
    },
    {
      d: 4,
      q: 'For a memory-bound reduction, the most impactful optimization is usually…',
      o: [
        'More atomics',
        'Maximizing read bandwidth: each thread streams many coalesced (vectorized) elements (grid-stride), so the kernel saturates DRAM — the reduction tree itself is cheap',
        'Using tensor cores',
        'Lowering occupancy',
      ],
      a: 1,
      x: [
        'Using FP64',
        'Smaller grids',
        'Constant memory',
      ],
      e: 'A reduction’s cost is dominated by reading the input. Coalesced, vectorized, multi-element-per-thread loads (grid-stride) push it to the bandwidth roof; the combination (shuffles/shared/atomic) is comparatively negligible. Optimize the read path first.',
      ref: 'Reduction; bandwidth-bound',
      tags: ['reduction', 'bandwidth', 'vectorization'],
    },
    {
      d: 5,
      q: 'A blocked-ELL or hybrid sparse format can outperform CSR for SpMV when…',
      o: [
        'Rows vary wildly',
        'The matrix has fairly uniform row lengths (or dense sub-blocks), so a regular format gives coalesced, balanced access and can use vector/tensor units — CSR’s irregularity hurts there',
        'The matrix is dense',
        'Always',
      ],
      a: 1,
      x: [
        'There are no nonzeros',
        'For FP64 only',
        'It uses the CPU',
      ],
      e: 'ELL/blocked formats store regular structure (fixed row length / dense blocks), enabling coalesced, load-balanced, sometimes tensor-core-friendly access. They win when sparsity is structured/uniform; CSR is more general but irregular. Hybrid (HYB) mixes them.',
      ref: 'Sparse formats; ELL/blocked vs CSR',
      tags: ['spmv', 'ell', 'sparse-formats'],
    },
    {
      d: 4,
      q: 'Fusing a residual add and dropout into the preceding kernel’s epilogue is beneficial because…',
      o: [
        'It improves accuracy',
        'It avoids extra HBM round-trips for the intermediate tensor — applying residual/dropout while the data is on-chip instead of launching separate elementwise kernels',
        'It uses tensor cores',
        'It reduces registers',
      ],
      a: 1,
      x: [
        'It increases launches',
        'It disables coalescing',
        'It needs FP64',
      ],
      e: 'Each separate elementwise op (residual add, dropout) would re-read and re-write the tensor from HBM. Fusing them into the producer’s epilogue keeps the data on-chip, cutting memory traffic — a major source of speedup for transformer blocks (torch.compile/Triton).',
      ref: 'Elementwise fusion; transformer blocks',
      tags: ['fusion', 'residual', 'dropout', 'memory-traffic'],
    },
    {
      d: 4,
      q: 'Radix sort’s efficiency on GPUs comes largely from…',
      o: [
        'Comparisons',
        'Processing keys digit-by-digit with high-throughput, coalesced passes and cooperative scans/histograms to scatter keys — avoiding comparison-based branchy work',
        'Recursion',
        'Atomics per element',
      ],
      a: 1,
      x: [
        'Sorting networks',
        'FP64',
        'The CPU',
      ],
      e: 'Radix sort does fixed passes over digits using histograms + prefix sums to compute scatter offsets, with coalesced, branch-light memory access — mapping well to GPUs (CUB’s DeviceRadixSort). Comparison sorts have more divergence/branching.',
      ref: 'CUB DeviceRadixSort; radix sort',
      tags: ['radix-sort', 'scan', 'coalescing'],
    },
    {
      d: 5,
      q: 'Why is the attention KV-cache often stored with a layout that places the head dimension contiguous?',
      o: [
        'It saves memory',
        'Attention reads each token’s K/V vectors per head; a head-contiguous layout makes those reads coalesced/vectorizable and matches the kernel’s access pattern, improving decode bandwidth',
        'It increases precision',
        'It avoids tensor cores',
      ],
      a: 1,
      x: [
        'It is random',
        'For FP64',
        'It uses the CPU',
      ],
      e: 'Decode reads K/V along the head dimension; storing head-dim contiguous yields coalesced, vectorized loads of the KV cache (the bandwidth-bound part of decode). Paged/contiguous layouts are designed around these access patterns.',
      ref: 'KV-cache layout; attention decode',
      tags: ['kv-cache', 'layout', 'attention'],
    },
    {
      d: 4,
      q: 'A practical reason channels-last (NHWC) memory format matters in PyTorch for conv nets on tensor cores is…',
      o: [
        'It changes the math',
        'cuDNN’s fastest tensor-core conv kernels expect NHWC; using NCHW forces layout transposes or slower kernels, so setting channels-last lets the model hit the fast path',
        'It reduces parameters',
        'It increases precision',
      ],
      a: 1,
      x: [
        'It avoids tensor cores',
        'It uses the CPU',
        'It is for FP64',
      ],
      e: 'The high-performance tensor-core conv kernels assume NHWC. With default NCHW, cuDNN may insert transposes or pick slower kernels. Converting tensors to channels-last memory format aligns with the fast kernels, improving conv throughput.',
      ref: 'PyTorch channels-last; cuDNN',
      tags: ['nhwc', 'channels-last', 'pytorch', 'convolution'],
    },
    {
      d: 4,
      q: 'A warp-level reduction in a GEMM epilogue (e.g. for a fused row-sum) is preferred over shared memory because…',
      o: [
        'It is less accurate',
        'The values are already distributed across a warp’s registers; shuffles combine them without shared-memory traffic or a block barrier, fitting the epilogue’s warp-tile structure',
        'It uses global memory',
        'It needs the host',
      ],
      a: 1,
      x: [
        'It avoids tensor cores',
        'It uses constant memory',
        'For FP64',
      ],
      e: 'In the epilogue, each warp holds part of the output tile in registers; a warp-shuffle reduction combines them directly (no shared memory/barrier), matching the warp-tile layout and keeping the epilogue cheap.',
      ref: 'GEMM epilogue; warp reduction',
      tags: ['epilogue', 'warp-reduction', 'shuffle'],
    },
    {
      d: 5,
      q: 'Stream-K differs from split-K in HOW it parallelizes the K dimension by…',
      o: [
        'Not using K',
        'Distributing the FLATTENED MAC iteration space evenly across a fixed set of blocks (so all SMs stay busy regardless of shape) rather than statically splitting K into a fixed number of partitions',
        'Using larger tiles',
        'Avoiding reduction',
      ],
      a: 1,
      x: [
        'Using one block',
        'Disabling tensor cores',
        'For FP64',
      ],
      e: 'Split-K statically partitions K into a chosen number of slices. Stream-K instead spreads the total MAC work uniformly across blocks (each may handle partial tiles), achieving near-perfect load balance across many shapes — then reduces partials.',
      ref: 'Stream-K vs split-K',
      tags: ['stream-k', 'split-k', 'load-balance'],
    },
    {
      d: 4,
      q: 'A reason to choose a work-INEFFICIENT (Hillis-Steele) scan for a small per-warp prefix sum is…',
      o: [
        'It does less work',
        'For 32 elements its simplicity and use of shuffles make it fast and branch-light; the asymptotic extra work is negligible at warp size, and it avoids Blelloch’s two-phase complexity',
        'It is always best',
        'It uses atomics',
      ],
      a: 1,
      x: [
        'It is O(n)',
        'It needs shared memory',
        'For FP64',
      ],
      e: 'At warp scale (32), the log-step Hillis-Steele scan via shuffles is simple and fast; the O(n log n) vs O(n) difference is immaterial for tiny n. Work-efficient Blelloch matters for large device-wide scans, not warp prefix sums.',
      ref: 'Warp scan; Hillis-Steele',
      tags: ['scan', 'warp', 'hillis-steele'],
    },
    {
      d: 4,
      q: 'Choosing the GEMM threadblock tile (e.g. 128×256 vs 128×128) trades…',
      o: [
        'Precision for speed',
        'Data reuse / arithmetic intensity (bigger tile = more reuse) against shared-memory & register usage (which lower occupancy and need enough work to fill the tiles)',
        'Memory for compute',
        'Nothing',
      ],
      a: 1,
      x: [
        'Latency for bandwidth',
        'Registers for memory',
        'Divergence for coalescing',
      ],
      e: 'Larger threadblock tiles increase operand reuse (higher intensity, fewer HBM reads) but consume more shared memory/registers, lowering occupancy and needing enough problem size to fill them. The best tile balances reuse, occupancy, and shape.',
      ref: 'CUTLASS tile selection; GEMM',
      tags: ['tile-size', 'gemm', 'reuse', 'occupancy'],
    },
    {
      d: 5,
      q: 'Why does FlashAttention parallelize over the QUERY blocks (and heads/batch) rather than the key dimension in its primary loop?',
      o: [
        'Keys are smaller',
        'Each query block can be processed independently (its own running softmax state), giving abundant parallelism; the inner loop streams K/V tiles sequentially to update that query block’s output',
        'It avoids softmax',
        'Keys are reused once',
      ],
      a: 1,
      x: [
        'It uses the CPU',
        'For FP64',
        'It materializes scores',
      ],
      e: 'Queries are independent across blocks (each maintains its own online-softmax state), so parallelizing over query blocks (and batch/heads) exposes lots of parallelism, while the sequential inner loop over K/V tiles keeps memory traffic low. FA-2 further refines this partitioning.',
      ref: 'FlashAttention / FlashAttention-2 parallelization',
      tags: ['flash-attention', 'parallelization', 'attention'],
    },
    {
      d: 4,
      q: 'A fused "bias + GELU" epilogue computes GELU in FP32 (even for FP16 outputs) because…',
      o: [
        'FP16 GELU is faster',
        'GELU involves transcendentals/erf where FP16 precision would degrade accuracy; computing it in FP32 on the in-register accumulator preserves quality before casting to FP16 for storage',
        'It avoids tensor cores',
        'For memory',
      ],
      a: 1,
      x: [
        'It uses the CPU',
        'GELU is integer',
        'For FP64 only',
      ],
      e: 'The accumulator is FP32; applying bias+GELU there (FP32 transcendental math) and then casting to FP16 for output keeps accuracy where it matters while still storing in low precision — the typical fused-epilogue precision strategy.',
      ref: 'Fused epilogue; precision',
      tags: ['epilogue', 'gelu', 'precision'],
    },
    {
      d: 5,
      q: 'The biggest reason torch.compile/Triton fusion helps transformer training throughput is…',
      o: [
        'Faster matmul',
        'Fusing the many small memory-bound ops around the matmuls (norms, activations, residuals, dropout, bias) into few kernels — eliminating intermediate HBM round-trips that dominate those ops',
        'Lower precision',
        'More occupancy',
      ],
      a: 1,
      x: [
        'Avoiding tensor cores',
        'Using the CPU',
        'Smaller grids',
      ],
      e: 'GEMMs already use tuned libraries; the leverage is fusing the surrounding elementwise/reduction ops so their intermediates stay on-chip instead of bouncing through HBM. That memory-traffic reduction is where compiler fusion yields most of its speedup.',
      ref: 'torch.compile/Triton fusion; transformer blocks',
      tags: ['fusion', 'torch-compile', 'memory-traffic'],
    },
  ],
  { idPrefix: 'optimization-e' }
)
