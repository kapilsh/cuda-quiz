import { defineQuestions } from './_helpers.js'

// Synchronization & Atomics — batch E. Cluster barriers, async pipelines,
// reduction determinism, advanced atomics. Distinct from synchronization.js/.b/.c/.d.
export default defineQuestions(
  'synchronization',
  [
    {
      d: 5,
      q: 'cluster.sync() (Hopper) differs from __syncthreads() in that it synchronizes…',
      o: [
        'One warp',
        'All threads across all blocks of the CLUSTER (co-scheduled on a GPC), a barrier wider than a block but narrower than the grid',
        'The whole grid',
        'Two GPUs',
      ],
      a: 1,
      x: [
        'The host and device',
        'Only producer warps',
        'One sub-partition',
      ],
      e: 'cluster.sync() is a cluster-wide barrier (all blocks in the cluster), enabled by their guaranteed co-residency in a GPC. It sits between the block barrier (__syncthreads) and grid-wide cooperative sync, and pairs with DSMEM.',
      ref: 'CUDA C++ Programming Guide (cluster.sync)',
      tags: ['cluster-sync', 'clusters', 'hopper'],
    },
    {
      d: 5,
      q: 'When a TMA copy targets a peer block’s shared memory (DSMEM) in a cluster, completion is signaled via…',
      o: [
        '__syncthreads',
        'An mbarrier in the TARGET block’s shared memory whose transaction count the TMA updates, so the consumer block waits on its own barrier',
        'A global flag',
        'cudaDeviceSynchronize',
      ],
      a: 1,
      x: [
        'A spinlock',
        'An atomic counter only',
        'A texture fetch',
      ],
      e: 'Cluster TMA (incl. multicast) signals arrival by updating an mbarrier’s transaction (byte) count in the destination shared memory; the consuming block waits on that mbarrier — coordinating cross-block async data movement within the cluster.',
      ref: 'Hopper cluster TMA; mbarrier',
      tags: ['tma', 'mbarrier', 'dsmem', 'clusters'],
    },
    {
      d: 4,
      q: 'A deterministic block reduction (reproducible bitwise FP result) requires…',
      o: [
        'atomicAdd',
        'A FIXED-order combination (e.g. a tree reduction with a deterministic schedule, no atomics) so the floating-point rounding sequence is identical every run',
        'Relaxed atomics',
        'More warps',
      ],
      a: 1,
      x: [
        'System-scope atomics',
        'Higher occupancy',
        'Texture memory',
      ],
      e: 'FP non-associativity makes atomic-based or scheduling-dependent orders nondeterministic. A fixed-schedule tree reduction applies additions in the same order each run, giving reproducible results (at some performance cost vs atomics).',
      ref: 'Deterministic reduction; FP reproducibility',
      tags: ['determinism', 'reduction', 'floating-point'],
    },
    {
      d: 5,
      q: 'In a double-buffered pipeline with mbarriers, why are TWO barriers per buffer (full and empty) typically used?',
      o: [
        'For speed only',
        'One signals the buffer is FULL (data arrived, consumer may read); the other signals it is EMPTY (consumer done, producer may refill) — so producer and consumer hand off the buffer without overwriting in-use data',
        'To synchronize the grid',
        'To reduce registers',
      ],
      a: 1,
      x: [
        'To avoid divergence',
        'For the host',
        'To disable the L2',
      ],
      e: 'A full/empty barrier pair implements bounded producer/consumer handoff per buffer: the consumer waits for full before reading; the producer waits for empty before refilling. This prevents the producer from clobbering data the consumer hasn’t read.',
      ref: 'Pipelining; full/empty barriers',
      tags: ['mbarrier', 'double-buffer', 'producer-consumer'],
    },
    {
      d: 4,
      q: 'Why is atomicAdd on a per-block SHARED histogram, then one global atomic per bin, both correct AND fast?',
      o: [
        'Shared atomics are inexact',
        'Correct because atomicity holds at each level; fast because shared atomics are low-latency and the global atomics are reduced to ~gridDim per bin instead of one per element',
        'Global atomics are faster',
        'It avoids barriers',
      ],
      a: 1,
      x: [
        'It uses tensor cores',
        'Shared memory is read-only',
        'It needs no sync',
      ],
      e: 'Privatization keeps correctness (atomic at each stage) while cutting contention: most updates hit fast shared memory, and only a few global atomics merge per-block partials — drastically fewer than per-element global atomics.',
      ref: 'PMPP Ch. 9; histogram privatization',
      tags: ['histogram', 'atomics', 'privatization'],
    },
    {
      d: 4,
      q: 'cuda::atomic with thread_scope_cluster (Hopper) provides atomicity/ordering scoped to…',
      o: [
        'One block',
        'All blocks of the cluster — useful for atomics on distributed shared memory accessed by cluster peers, cheaper than device scope',
        'The whole device',
        'The host',
      ],
      a: 1,
      x: [
        'One warp',
        'Two GPUs',
        'The grid',
      ],
      e: 'Cluster-scope atomics coordinate the threads of a cluster (e.g. updating DSMEM shared across blocks) with ordering guarantees limited to the cluster — a tighter, cheaper scope than device-wide when only cluster peers share the data.',
      ref: 'libcu++ thread scopes; Hopper cluster',
      tags: ['atomics', 'cluster-scope', 'dsmem'],
    },
    {
      d: 4,
      q: 'A grid-wide barrier via cooperative groups vs the two-kernel approach: the cooperative barrier is preferable when…',
      o: [
        'Always',
        'The per-iteration state is large/expensive to reload and you want to keep it on-chip across the barrier (avoiding the global round-trip a kernel relaunch implies), AND all blocks fit co-resident',
        'The grid is huge',
        'You need maximum parallelism',
      ],
      a: 1,
      x: [
        'Never',
        'For elementwise ops',
        'For data loading',
      ],
      e: 'grid.sync() lets you keep working state in shared/registers across the barrier (no relaunch, no reload from global). It’s worth it when that reuse outweighs the parallelism limit of cooperative launch; otherwise two launches (larger grid) win.',
      ref: 'Cooperative groups vs multi-launch',
      tags: ['grid-sync', 'state-reuse', 'tradeoff'],
    },
    {
      d: 5,
      q: 'A "split barrier" (arrive then later wait) on Hopper enables a thread to…',
      o: [
        'Skip synchronization',
        'Signal arrival (e.g. its async copies are issued), do INDEPENDENT work, then wait — overlapping useful computation with the barrier/copy-completion latency',
        'Synchronize the grid',
        'Run on the host',
      ],
      a: 1,
      x: [
        'Reduce registers',
        'Avoid divergence',
        'Disable the L2',
      ],
      e: 'Splitting the barrier into arrive() and wait() lets a thread perform unrelated work between signaling and blocking, hiding the barrier (and async-copy completion) latency — the foundation of asynchronous, pipelined synchronization.',
      ref: 'CUDA asynchronous barrier (arrive/wait)',
      tags: ['split-barrier', 'arrive-wait', 'overlap'],
    },
    {
      d: 4,
      q: 'Why does a warp-aggregated atomic need a leader election (e.g. via __ballot/elect) rather than every lane doing the atomic?',
      o: [
        'For correctness',
        'After computing the per-warp total, exactly ONE lane should issue the single combined atomicAdd; without electing a unique leader, multiple lanes would add the total repeatedly',
        'For divergence',
        'For coalescing',
      ],
      a: 1,
      x: [
        'To use shared memory',
        'To avoid registers',
        'For the host',
      ],
      e: 'Aggregation replaces 32 atomics with one of the warp’s total, so a single elected lane performs that atomic; the others contribute via the in-warp scan but must not also issue the atomic, or the result would be over-counted.',
      ref: 'Warp-aggregated atomics; leader election',
      tags: ['warp-aggregation', 'leader-election', 'atomics'],
    },
    {
      d: 4,
      q: 'In a lock-free work queue using atomicAdd to claim indices, a subtle bug is claiming an index then…',
      o: [
        'Adding too much',
        'Reading/writing the queue slot without ensuring the producer’s write to that slot is visible (missing acquire ordering) — you may consume stale/uninitialized data',
        'Using too many registers',
        'Diverging',
      ],
      a: 1,
      x: [
        'Coalescing',
        'Using shared memory',
        'A barrier',
      ],
      e: 'Claiming an index is atomic, but you also need ordering so the slot’s data (written by a producer) is visible before you read it — i.e. acquire on the consumer / release on the producer, or a fence. Atomicity of the index alone isn’t enough.',
      ref: 'Lock-free queues; ordering',
      tags: ['lock-free', 'work-queue', 'ordering'],
    },
    {
      d: 3,
      q: 'Why must every thread of a block reach the SAME __syncthreads(), even in a tiled GEMM with a remainder (edge) tile?',
      o: [
        'For speed',
        'The barrier requires uniform arrival; edge handling must be done so all threads still execute the shared barriers (e.g. load zeros for out-of-range elements rather than returning) to avoid deadlock',
        'For coalescing',
        'For atomics',
      ],
      a: 1,
      x: [
        'It is optional',
        'For divergence',
        'For the host',
      ],
      e: 'Threads handling out-of-range tile elements can’t skip the barrier. The standard fix is to load 0 (or guard the math, not the barrier) so every thread reaches each __syncthreads — keeping the barrier’s uniform-arrival requirement satisfied.',
      ref: 'Tiled GEMM; edge handling and barriers',
      tags: ['syncthreads', 'edge-tiles', 'deadlock'],
    },
    {
      d: 5,
      q: 'A reduction that combines warp-shuffle (within warp), shared memory (across warps), and ONE atomic (across blocks) is a good design because each level…',
      o: [
        'Uses the same mechanism',
        'Uses the cheapest correct synchronization for its scope: shuffles (no shared/barrier) within a warp, shared+barrier within a block, and a single atomic per block across blocks — minimizing contention/overhead at each tier',
        'Avoids all sync',
        'Uses global memory only',
      ],
      a: 1,
      x: [
        'Uses atomics everywhere',
        'Uses the host',
        'Uses tensor cores',
      ],
      e: 'Matching the synchronization to the scope (warp shuffles → block shared+barrier → minimal global atomics) minimizes cost: in-warp needs no shared memory, in-block uses one barrier path, and cross-block uses just one atomic per block.',
      ref: 'Hierarchical reduction design',
      tags: ['reduction', 'hierarchical', 'synchronization'],
    },
    {
      d: 4,
      q: 'cuda::atomic_ref(x).fetch_max(v) on a float — if not natively supported — is internally implemented via…',
      o: [
        'A barrier',
        'An atomicCAS retry loop on the float’s bit pattern (read old, compute max, CAS) — the universal way to build atomics lacking hardware support',
        'A spinlock',
        'Shared memory',
      ],
      a: 1,
      x: [
        'A reduction',
        'A texture fetch',
        'The host',
      ],
      e: 'Where a hardware atomic max for floats isn’t available, libraries implement it with a CAS loop on the bitcast integer (with sign handling) — CAS being the universal primitive for constructing arbitrary atomic read-modify-writes.',
      ref: 'Atomic max via CAS; libcu++',
      tags: ['atomic-max', 'cas', 'float'],
    },
    {
      d: 4,
      q: 'Why is a memory FENCE alone insufficient to make two BLOCKS rendezvous (wait for each other)?',
      o: [
        'Fences are slow',
        'A fence only orders one thread’s memory visibility; it does not make threads WAIT. Inter-block waiting needs a flag + spin (with ordering) and the blocks must be co-resident — true rendezvous needs cooperative launch',
        'Fences synchronize blocks',
        'Fences are for warps',
      ],
      a: 1,
      x: [
        'Fences block the host',
        'Fences are atomic',
        'Fences reset state',
      ],
      e: 'A fence is an ordering primitive, not a barrier. Making blocks wait for each other requires a published flag (with release/acquire) plus spinning — and even then, blocks aren’t guaranteed co-resident, so robust grid sync needs cooperative launch.',
      ref: 'CUDA memory model; fences vs barriers',
      tags: ['fence', 'inter-block', 'rendezvous'],
    },
    {
      d: 3,
      q: 'A spin-wait loop on a flag should include __nanosleep (or a backoff) to…',
      o: [
        'Speed up the wait',
        'Reduce contention and power by backing off between polls, instead of hammering the memory system with continuous reads while waiting',
        'Make it atomic',
        'Synchronize the block',
      ],
      a: 1,
      x: [
        'Order memory',
        'Increase occupancy',
        'Avoid divergence',
      ],
      e: 'Busy-spinning floods the memory subsystem and wastes power. __nanosleep with exponential backoff spaces out polls, reducing contention on the flag’s cache line and power draw while waiting — a standard spin-loop refinement (Volta+).',
      ref: 'CUDA __nanosleep; spin-wait backoff',
      tags: ['spin-wait', 'nanosleep', 'backoff'],
    },
    {
      d: 5,
      q: 'In a single-kernel grid reduction without cooperative launch, why is the order "write partial → __threadfence → atomic-increment counter" significant?',
      o: [
        'Order does not matter',
        'The fence must make the partial GLOBALLY VISIBLE before the counter increment announces completion; otherwise the last block could read a partial that isn’t yet visible',
        'The counter must be first',
        'The fence is optional',
      ],
      a: 1,
      x: [
        'Atomics order memory',
        'It avoids divergence',
        'It is for coalescing',
      ],
      e: 'The increment signals "my partial is ready." Without the preceding fence, the partial write might not be visible when another block sees the count reach gridDim and reads all partials — yielding stale data. The fence enforces the happens-before.',
      ref: 'threadFenceReduction; ordering',
      tags: ['reduction', 'threadfence', 'ordering'],
    },
    {
      d: 4,
      q: 'Why might replacing block-level __syncthreads reductions with cooperative-groups tile reductions simplify AND speed up code?',
      o: [
        'It uses global memory',
        'cg tile reduce encapsulates the optimal warp/redux-based reduction (no manual shared-memory tree or barriers for the warp portion), reducing both code complexity and synchronization overhead',
        'It avoids correctness',
        'It needs the host',
      ],
      a: 1,
      x: [
        'It increases divergence',
        'It uses atomics',
        'It lowers precision',
      ],
      e: 'cg::reduce on a tile handles the warp reduction optimally (shuffles/redux) and composes for block-level work, replacing hand-written shared-memory trees and their barriers — cleaner and often faster, with portable lowering.',
      ref: 'Cooperative groups reductions',
      tags: ['cooperative-groups', 'reduction', 'simplicity'],
    },
    {
      d: 4,
      q: 'A correctness requirement for cuda::barrier shared among threads is that…',
      o: [
        'It is in global memory',
        'It lives in shared memory and is initialized (init(&bar, count)) by one thread with a __syncthreads before use, so all participants see the initialized barrier',
        'Each thread has its own',
        'It needs no init',
      ],
      a: 1,
      x: [
        'It is in registers',
        'It is per-warp only',
        'It uses the L2',
      ],
      e: 'A shared cuda::barrier must be constructed/initialized once (with the expected arrival count) and made visible (a barrier after init) before threads call arrive/wait. Uninitialized or racily-initialized barriers cause undefined behavior.',
      ref: 'libcu++ <cuda/barrier>; initialization',
      tags: ['barrier', 'initialization', 'shared-memory'],
    },
    {
      d: 5,
      q: 'Why is a global counter incremented with atomicAdd and read by the SAME kernel (without a grid barrier) generally unsafe to interpret as a "final total"?',
      o: [
        'Atomics lose updates',
        'Other blocks may not have run/finished their increments yet (no grid-wide ordering), so a read mid-kernel sees a partial total — you need a grid barrier or a second kernel to read the final value',
        'It overflows',
        'It deadlocks',
      ],
      a: 1,
      x: [
        'It needs FP64',
        'It is always safe',
        'Counters are read-only',
      ],
      e: 'Atomicity ensures no lost increments, but without grid-wide synchronization there’s no point at which all blocks have contributed. Reading the counter mid-kernel gives a partial sum; use grid.sync() or a separate kernel to read the completed total.',
      ref: 'Grid-wide ordering; counters',
      tags: ['atomics', 'grid-ordering', 'counter'],
    },
    {
      d: 4,
      q: 'A reader that polls a producer’s flag with a RELAXED atomic load (no acquire) may…',
      o: [
        'Always be correct',
        'See the flag set but read STALE data, because relaxed ordering doesn’t guarantee the producer’s data writes are visible — use acquire (or a fence) on the flag load',
        'Deadlock',
        'Lose the flag',
      ],
      a: 1,
      x: [
        'Be faster and correct',
        'Need a barrier',
        'Use shared memory',
      ],
      e: 'Relaxed only guarantees atomicity of the flag access, not ordering with the data. Without acquire, the data read can be reordered before observing the published flag value, yielding stale data. The flag load must use acquire ordering.',
      ref: 'Memory model; acquire on flag',
      tags: ['relaxed', 'acquire', 'flag', 'ordering'],
    },
    {
      d: 3,
      q: 'For a kernel where one thread per block must safely initialize a shared array all threads then use, the idiom is…',
      o: [
        'Every thread initializes',
        'Leader initializes (if threadIdx.x==0) → __syncthreads() → all threads read; the barrier publishes the initialization and makes others wait for it',
        'Use atomics',
        'Use global memory',
      ],
      a: 1,
      x: [
        'Use a spinlock',
        'Use texture memory',
        'No sync needed',
      ],
      e: 'A leader writes the shared state and __syncthreads makes it visible to (and forces waiting by) the rest of the block — the canonical leader-then-barrier initialization pattern in block-cooperative kernels.',
      ref: 'CUDA C++ Programming Guide §7.6; leader pattern',
      tags: ['leader', 'syncthreads', 'initialization'],
    },
    {
      d: 5,
      q: 'Why can a correct CPU lock-free algorithm need EXTRA fences when ported to CUDA, beyond translating atomics?',
      o: [
        'CUDA atomics are weaker',
        'CUDA’s memory model and the per-SM non-coherent L1s plus scoped atomics mean visibility you got "for free" on a coherent CPU must be made explicit with the right-scope fences/atomics on the GPU',
        'CUDA has no atomics',
        'It always just works',
      ],
      a: 1,
      x: [
        'CPUs are weaker',
        'It needs cooperative launch',
        'Registers are shared',
      ],
      e: 'CPU cache coherence provides visibility that GPUs (non-coherent L1s, scoped atomics, weak model) don’t implicitly. Porting requires choosing the correct atomic scopes and inserting fences to establish the happens-before edges the algorithm assumes.',
      ref: 'CUDA memory model; porting lock-free code',
      tags: ['memory-model', 'lock-free', 'porting', 'scope'],
    },
    {
      d: 4,
      q: 'cuda::pipeline with N stages requires the consumer to call consumer_release() after using a stage so that…',
      o: [
        'It frees the kernel',
        'The buffer becomes available for the producer to refill — without release, the producer eventually stalls waiting for a free stage (the pipeline back-pressures)',
        'It synchronizes the grid',
        'It lowers precision',
      ],
      a: 1,
      x: [
        'It reduces registers',
        'It avoids divergence',
        'It uses the host',
      ],
      e: 'The pipeline tracks buffer availability; consumer_release signals the stage is free. Forgetting it deadlocks/stalls the producer once all stages are full, since no buffer is ever released for reuse — the bounded-buffer back-pressure mechanism.',
      ref: 'CUDA cuda::pipeline; consumer_release',
      tags: ['pipeline', 'consumer-release', 'back-pressure'],
    },
    {
      d: 4,
      q: 'Atomics with thread_scope_device are required (over block scope) when…',
      o: [
        'Threads are in one block',
        'Threads from DIFFERENT blocks update the same global location and must see consistent, ordered results across the whole GPU',
        'Using shared memory',
        'Within a warp',
      ],
      a: 1,
      x: [
        'On the host',
        'For one thread',
        'For constant memory',
      ],
      e: 'Block scope only coordinates one block. Cross-block updates to global memory need device-scope atomics so the operation is atomic and properly ordered across all SMs. Use the narrowest correct scope for performance.',
      ref: 'libcu++ thread scopes; device scope',
      tags: ['atomics', 'device-scope', 'inter-block'],
    },
    {
      d: 3,
      q: 'A reduction kernel that uses atomicAdd into ONE global accumulator gives correct sums but is slow AND nondeterministic in FP because…',
      o: [
        'Atomics drop updates',
        'All updates serialize on one address (slow), and the scheduling-dependent order combined with FP non-associativity changes the rounded result run-to-run (nondeterministic)',
        'It overflows',
        'It deadlocks',
      ],
      a: 1,
      x: [
        'It needs FP64',
        'Atomics are inexact',
        'It is always fast',
      ],
      e: 'A single hot address serializes atomics (contention) and the order of FP additions varies with scheduling; FP non-associativity then yields different rounding each run. Hierarchical reduction (or fixed-order tree) fixes both speed and determinism.',
      ref: 'Atomic reduction; contention and determinism',
      tags: ['atomics', 'contention', 'determinism'],
    },
    {
      d: 5,
      q: 'In a cluster, an atomic on a peer block’s DSMEM uses which scope to be both correct and efficient?',
      o: [
        'thread_scope_system',
        'thread_scope_cluster — atomicity/ordering across the cluster’s blocks, cheaper than device/system scope while covering the DSMEM sharers',
        'thread_scope_block',
        'No scope needed',
      ],
      a: 1,
      x: [
        'thread_scope_thread',
        'thread_scope_grid',
        'thread_scope_warp',
      ],
      e: 'DSMEM is shared among cluster blocks, so cluster-scope atomics give the needed cross-block guarantees within the cluster without the cost of device/system scope. Block scope would be too narrow (misses peer blocks).',
      ref: 'libcu++ cluster scope; Hopper DSMEM',
      tags: ['atomics', 'cluster-scope', 'dsmem'],
    },
    {
      d: 4,
      q: 'Why do warp vote functions (__all/__any/__ballot) require a mask under ITS even though they "feel" warp-wide?',
      o: [
        'For speed',
        'The mask names exactly which lanes participate; under independent thread scheduling not all 32 lanes are guaranteed converged, so the result is defined only over the named, participating lanes',
        'For coalescing',
        'For divergence',
      ],
      a: 1,
      x: [
        'Masks are ignored',
        'It is optional',
        'For the host',
      ],
      e: 'Vote functions aggregate over the masked lanes; with possible divergence, the mask must reflect the actually-participating lanes (e.g. __activemask()), or the vote is undefined. The mask makes the participant set explicit and correct.',
      ref: 'CUDA C++ Programming Guide §B.22; vote masks',
      tags: ['warp-vote', 'mask', 'its'],
    },
    {
      d: 3,
      q: 'A correctness pattern for double-buffered shared-memory tiling is two __syncthreads per iteration because…',
      o: [
        'One barrier suffices',
        'One barrier ensures the just-loaded tile is fully written before compute reads it; a second ensures compute finishes reading before the buffer is overwritten by the next load',
        'It is faster',
        'For atomics',
      ],
      a: 1,
      x: [
        'For coalescing',
        'For divergence',
        'No barriers needed',
      ],
      e: 'Without the second barrier, the next iteration’s load could overwrite the tile while some threads are still reading it. The two barriers bracket the read phase (after load, after compute), preventing read/write races on the shared buffer.',
      ref: 'Tiled GEMM; double-buffer barriers',
      tags: ['syncthreads', 'double-buffer', 'race'],
    },
    {
      d: 5,
      q: 'An async (split) barrier hides latency that __syncthreads cannot because __syncthreads…',
      o: [
        'Is faster',
        'Is a single blocking rendezvous — a thread cannot do useful work "during" it, whereas arrive/wait lets a thread compute between signaling arrival and waiting for others/async-copy completion',
        'Synchronizes the grid',
        'Orders nothing',
      ],
      a: 1,
      x: [
        'Is per-warp',
        'Runs on the host',
        'Is non-blocking',
      ],
      e: 'A classic barrier blocks at one point with no overlap opportunity. The split arrive/wait barrier exposes a window for independent work (and tracks async-copy completion), hiding barrier/copy latency — key to Hopper pipelined kernels.',
      ref: 'Asynchronous barrier vs __syncthreads',
      tags: ['async-barrier', 'syncthreads', 'overlap'],
    },
  ],
  { idPrefix: 'synchronization-e' }
)
